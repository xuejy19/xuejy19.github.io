<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>贝叶斯理论</title>
    <url>/2020/07/23/bayes/</url>
    <content><![CDATA[<hr>
<p>该部分按照以下层次进行组织：</p>
<blockquote>
<ul>
<li>从贝叶斯公式谈起</li>
<li>贝叶斯决策</li>
</ul>
</blockquote>
<h2 id="more"><a id="more"></a></h2>
<h2 id="贝叶斯公式">贝叶斯公式</h2>
<p><strong>概念介绍</strong>(From 维基百科)：</p>
<blockquote>
<ul>
<li><strong>条件概率</strong>：条件概率就是事件A在事件B发生条件下的概率，记做$P(A|B)$.</li>
<li><strong>先验概率</strong>：先验概率是指在考虑“观测数据”之前，对某一不确定量的估计,如$P(A)$</li>
<li><strong>后验概率</strong>：在贝叶斯统计中，一个随机事件或者一个不确定事件的后验概率是在考虑和给出相关证据或数据后所得到的条件概率，“后验”指代考虑了被测试事件的相关证据，如$P(B|A)$可以看作事件B的后验概率</li>
</ul>
</blockquote>
<p>贝叶斯公式形式如下：<br>
$$ P(A|B) = \frac{P(A)P(A|B)}{P(B)} $$<br>
再根据全概率公式：<br>
$$P(B) = \sum_j P(B|A_j)P(A_j) $$<br>
可以得到贝叶斯公式另一种形式：<br>
$$P(A_i|B) = \frac{P(A_i)P(B|A_i)}{\sum_j P(B|A_j)P(A_j) }$$</p>
<blockquote>
<p>公式分析：在现实世界中，我们往往期望通过一些观察到的事件来对一些不可以直接观测的事件进行推断，公式中的$B$变量指代可以直接观测到的事件，而我们的目的则是希望通过$B$事件与$A$事件之间的关联(因果性,相关性)来间接对A事件进行观测，贝叶斯公式便为我们搭起了这样一做桥梁。</p>
</blockquote>
<p>从公式角度出发，可以看出这样进行推理是有代价的，或者是必须拥有某些知识：</p>
<blockquote>
<ul>
<li>先验概率$P(A)$</li>
<li>类条件概率$P(B|A)$</li>
</ul>
</blockquote>
<hr>
<h2 id="贝叶斯决策">贝叶斯决策</h2>
<p>这部分主要介绍三种基于贝叶斯理论的决策策略：</p>
<blockquote>
<ul>
<li>基于最小错误率的贝叶斯决策</li>
<li>基于最小风险的贝叶斯决策</li>
<li>在限定一类错误率条件下使另一类错误率为最小的两类别决策</li>
</ul>
</blockquote>
<hr>
<h3 id="最小错误率贝叶斯决策">最小错误率贝叶斯决策</h3>
<p>从贝叶斯公式出发，通过比较后验概率大小，选择后验概率大的类别作为label。以而分类问题为例，$\omega_i(i=1,2)$是类别标签，$x$为特征相量，由贝叶斯公式，可得$P(\omega_i|x)$：<br>
$$ P(\omega_i|x) = \frac{P(x|\omega_i)P(\omega_i)}{\sum_{j=1}^2 P(x|\omega_j)P(\omega_j)}$$<br>
最小错误率下等价的决策规则有：</p>
<blockquote>
<ul>
<li>若$P(\omega_i|x) = max_{j=1,2}P(\omega_j|x)$,则$x \in \omega_i$</li>
<li>若$P(x|\omega_i)P(\omega_i) = max_{j=1,2}P(x|\omega_j)P(\omega_j)$,则$x \in \omega_i$</li>
<li>对于二分类问题，若$l(x) = \frac{P(x|\omega_1)}{P(x|\omega_2)} &gt; \frac{P(\omega_2)}{P(\omega_1)}$,则$x \in \omega_1$</li>
</ul>
</blockquote>
<p>下面证明该种决策策略为最小错误率，首先给出平均错误率公式：<br>
$$ P(e) = \int_{-\infty}^{\infty}P(e,x)dx = \int_{-\infty}^{\infty} P(e|x)p(x)dx$$<br>
由上面给出的决策规则可知：<br>
$$ P(e|x) = \begin{cases} P(\omega_1|x),if P(\omega_2|x) &gt; P(\omega_1|x) \<br>
P(\omega_2|x),if P(\omega_1|x) &gt; P(\omega_2|x) \end{cases}$$<br>
若$t$为两类的决策分界面，在该分界面上$P(\omega_1|x) = P(\omega_2|x)$，$t$将整个决策空间分为了两部分$\mathcal{R_1}$和$\mathcal{R_2}$,分别对应将$x$归为$\omega_1$和$\omega_2$,据此平均错误率可以写为：<br>
$$<br>
\begin{aligned}<br>
P(e) &amp;= P(x \in \mathcal{R_1},\omega_2) + P(x \in \mathcal{R_2},\omega_1) \<br>
&amp;= P(x\in \mathcal{R_1}|\omega_2) P(\omega_2) + P(x \in \mathcal{R_2}|\omega_1) P(\omega_1) \<br>
&amp;= P(\omega_2) \int_{\mathcal{R_1}} p(x|\omega_2)dx + P(\omega_1)\int_{\mathcal{R_2}}p(x|\omega_1)dx \<br>
&amp;= P(\omega_2) P_2(e) + P(\omega_1) P_1(e)<br>
\end{aligned}<br>
$$<br>
<img src="https://raw.githubusercontent.com/xuejy19/Images/master/bayes2.png" alt="最小错误率"></p>
<p>图中斜线部分代表$P(e)$的第一项，纹线部分代表第二项，而从最小错误率公式来看，若要使平均错误率$P(e)$最小，只需要对于任意的$x$，保证$P(e|x)$最小，而这恰恰是贝叶斯最小错误率决策规则。</p>
<h3 id="最小风险贝叶斯决策">最小风险贝叶斯决策</h3>
<p>最小风险贝叶斯决策，或者称为最小损失贝叶斯决策，这种决策思想也是非常朴素：</p>
<blockquote>
<p>同样是决策错误，但不同的错误带来的损失并不相同，有时甚至天差地别，比如去医院看病，对于早起癌症，漏警要比虚警严重的多，可能会让患者失去早期治疗的时机。</p>
</blockquote>
<p>最小风险错误率准则正是考虑各种错误造成损失不同而提出的一种决策规则，若将决策空间记做$\mathcal{A}$,而每个决策都将带来一定的损失，它通常是决策和自然状态的函数，比如$\lambda(a_i,\omega_j)$则指代在$x$处于状态$\omega_j$而做出$a_i$决策时的损失，下面给出数学符号定义：</p>
<ul>
<li>观测向量$x$为$d$维随机向量<br>
$$ x = [x_1, x_2 , \dots, x_d]^T$$</li>
<li>状态空间$\Omega$由$c$个自然状态(c类)组成<br>
$$ \Omega = { \omega_1, \dots, \omega_c }$$</li>
<li>决策空间由$n$个决策组成<br>
$$ \mathcal{A} = { a_1, \dots, a_n } $$</li>
<li>决策表，决策表中第$i$行$j$列为相应损失值$\lambda(a_i,\omega_j)$</li>
</ul>
<p>由于引入了损失的概念，在进行决策时便不能够只根据后验概率大小来做决策，必须考虑所采取的决策是否使得损失最小。对于给定的$x$，如果我们采取决策$a_i$,其对应$c$种可能的损失$\lambda(a_i,\omega_j)$,每种损失出现的概率为$P(\omega_i|x)$,由此可以计算条件期望损失$R(a_i|x)$:<br>
$$<br>
R(a_i|x) = E(\lambda(a_i,\omega_j)) = \sum_{j=1}^c \lambda(a_i,\omega_j) P(\omega_j|x), i=1,\dots,n<br>
$$<br>
在决策论种$R(a_i|x)$被称作条件风险，由于$x$是随机向量的观察值，因此对于不同的$x$，采取决策$a_i$时，其条件风险是不同的，所以最终采取何种决策应当与$x$有关，在这种意义下，可以将决策$a$看作是$x$的函数，记做$a(x)$,可以定义期望风险$R$为：<br>
$$<br>
R = \int R(a(x)|x) p(x)dx<br>
$$<br>
若想使得期望风险$R$最小，只需要保证对于任意的$x$，$R(a(x)|x)$最小，因此最小损失贝叶斯决策规则为：<br>
$$<br>
if R(a_k|x) = \min \limits_{i=1,\dots,n}R(a_i|x) \Rightarrow a = a_k<br>
$$</p>
<h3 id="在限定一类错误率条件下，使得另一类错误率最小">在限定一类错误率条件下，使得另一类错误率最小</h3>
<p>在两类别决策问题中，有两种错误分类的可能性,分别对应两个错误率$P_1(e)$和$P_2(e)$,而在实际应用中，我们往往期望某一个错误率不大于某个常数，而使另一类错误率尽可能小。比如在癌症诊断中，我们认识到将异常判断为正常非常严重，我们期望这类错误率为一个很小的值$P_2(e) = \epsilon_0$,在满足该条件下，期望另一类错误率$P_1(e)$尽可能低，这其实是一个带有等式约束的优化问题，可以用拉格朗日乘子法进行求解，拉格朗日函数$L$为:<br>
$$<br>
L = P_1(e) + \lambda(P_2(e) - \epsilon_0)<br>
$$<br>
其中$P_1(e)$为：<br>
$$<br>
P_1(e) = \int_{\mathcal{R_2}} p(x|\omega_1)dx<br>
$$<br>
$P_2(e)$类似，因此拉格朗日函数可以写做：<br>
$$<br>
\begin{aligned}<br>
L &amp;= \int_{\mathcal{R_2}} p(x|\omega_1) + \lambda(\int_{\mathcal{R_1}} p(x|\omega_2)dx - \epsilon_0)  \<br>
&amp;= 1-\lambda \epsilon_0 + \int_{\mathcal{R_1}} [\lambda  p(x|\omega_2) - p(x|\omega_1)dx]<br>
\end{aligned}<br>
$$<br>
$L$分别对分界点$t$和$\lambda$求导，可得：<br>
$$<br>
\begin{cases}<br>
\frac{\partial L}{\partial t} = 0 \Rightarrow \lambda = \frac{p(t|\omega_1)}{p(t|\omega_2)} \<br>
\frac{\partial L}{\partial \lambda} = 0 \Rightarrow P_2(e) = \epsilon_0<br>
\end{cases}<br>
$$<br>
由此便可得到Neyman-Pearson决策准则：<br>
$$<br>
if \frac{p(x|\omega_1)}{p(x|omega_2)} &gt; \lambda \Rightarrow x \in \omega_1<br>
$$</p>
]]></content>
      <categories>
        <category>统计学习</category>
      </categories>
  </entry>
  <entry>
    <title>马尔可夫网络</title>
    <url>/2020/07/25/markov-network/</url>
    <content><![CDATA[<p>在这一部分我将介绍另一种常见的概率图模型:马尔可夫网络，或称马尔可夫随机场。接下来分为以下几部分进行组织<a id="more"></a>：</p>
<ul>
<li>马尔可夫网络</li>
<li>独立性假设</li>
<li>势函数</li>
</ul>
<h3 id="马尔可夫网络">马尔可夫网络</h3>
<p>首先给出维基百科上关于马尔可夫网络的定义：</p>
<blockquote>
<p>马尔可夫网络是一组有马尔可夫性质随机变量$X$的全联合概率分布模型。马尔可夫网络类似贝叶斯网络用于表示依赖关系。但是，一方面它可以表示贝叶斯网络无法表示的一些关系，如循环依赖；另一方面，它不能表示贝叶斯网络能够表示的某些关系，如推导关系。</p>
</blockquote>
<p>马尔可夫网络具有以下特征：</p>
<ul>
<li>无向图$H = (V,E)$</li>
<li>每个顶点$v \in V$表示一个在集合$X$的随机变量，每条边${u,v}\in E$ 表示随机变量$u$和$v$之间的依赖关系</li>
</ul>
<h3 id="独立性假设">独立性假设</h3>
<p>马尔可夫网络与贝叶斯网络类似，以图的形式对独立性假设进行编码，首先给出图中激活路径的概念：</p>
<ul>
<li>激活路径：一个路径$X_1,X_2,\dots,X_n$是激活的 $\Leftrightarrow$ 没有路径上的变量被观测到</li>
</ul>
<p>有了激活路径概念之后，便可以讨论图中的条件独立关系：</p>
<ul>
<li>在图$H$中，节点集$X$与节点集$Y$相对于是节点集$Z$是分离的 $\Leftrightarrow$ $X$与$Y$之间不存在激活路径</li>
<li>表示为$sep_H(X;Y|Z)$,栗子如下图所示。</li>
</ul>
<p><img src="https://raw.githubusercontent.com/xuejy19/Images/master/mav1.png" alt="栗子"></p>
<h3 id="势函数">势函数</h3>
<p>在贝叶斯网络中，节点之间的关系通过条件概率进行表征，而在马尔可夫网络中，节点之间的关系通过势函数来表征，势函数定义如下：</p>
<blockquote>
<p>一个函数集合$f_k$(也称为因子或者团因子有时也称为特征)，每一个$f_k$的定义域是图$G$的团或子团$k$,每一个$f_k$是从可能的特定联合的指派(到元素$k$)到非负实数的映射。</p>
</blockquote>
<p>总结而言，势函数具有以下特征：</p>
<blockquote>
<ul>
<li>非负</li>
<li>与条件概率不同，其求和不必为1</li>
</ul>
</blockquote>
<h3 id="吉布斯分布">吉布斯分布</h3>
<p>我们引入该图模型的目的在于：<strong>通过马尔可夫网络来对变量之间的独立性关系进行编码，然后获得对于联合概率分布更加紧凑的表示形式</strong>，而吉布斯分布便是将联合概率与势函数相连接的桥梁。<br>
一个概率分布$P$可以分解为图$H$：</p>
<blockquote>
<ul>
<li>$H$可以分为若干子团$D_i$</li>
<li>在每个子团上相应定义了势函数$\pi_i$</li>
</ul>
</blockquote>
<p>由此便可以写出吉布斯分布形式：<br>
$$<br>
\begin{aligned}<br>
P(X_1,X_2,\dots,X_n) = \frac{1}{Z} f(X_1,X_2,\dots,X_n)<br>
\end{aligned}<br>
$$</p>
<p>其中:</p>
<blockquote>
<ul>
<li>$f(X_1,X_2,\dots,X_n) = \prod_{i=1}^m \pi_i(D_i)$</li>
<li>$Z = \sum_{X_1,\dots,X_n} f(X_1,\dots,X_n)$</li>
</ul>
</blockquote>
<p>每一个势函数定义了一个映射:$Sub(X) \rightarrow \mathcal{R^+}$ ,一个势函数的定义域相当于是一个集合，而这个集合中的变量彼此之间是直接依赖关系，从这个视角来看，条件概率可以看作是势函数的特例(仅仅表征两个变量之间依赖关系)。</p>
<p>下面我们讨论下一个势函数(因子)应该定义在一个多大的集合上，首先考虑如果在每条边上均定义一个势函数，那么马尔可夫网络能否表征联合概率分布？答案是并不行，为此我以一个7个节点(均为二值变量)的全连接网络为例，分别讨论两种情况下具有的独立参数的数量：</p>
<ul>
<li>联合概率分布：$2^7 - 1 = 127$</li>
<li>边上势函数：$4*C_7^2 = 84$</li>
</ul>
<p>显然对于全连接网络，若给予每个边一个势函数并不足以表征联合概率分布，在前面已经说明一个势函数定义域中的变量应当是两两直接相关，因此势函数的定义域应当是一个团(clique)。<br>
$\Rightarrow$ <strong>马尔可夫网络中的局部结构是团</strong></p>
<h3 id="马尔可夫网络分解">马尔可夫网络分解</h3>
<p>首先给出$I-Map$的概念:$H$为一个马尔可夫网络，$P$为联合概率分布，若有$I(H) \subseteq I§$，则称马尔可夫网络$H$为分布$P$的一个$I-Map$。</p>
<p>记$D_i$为马尔可夫网络$H$中的子团，Hammersley-Clifford定理如下：</p>
<blockquote>
<p>对于一个无向图$H$和分布$P$,$P$可以被分解成$P(X) = \frac{1}{Z}\prod \pi_i (D_i) \Leftrightarrow$ $H$是$P$的$I-Map$</p>
</blockquote>
<p>在实际进行分解时一般都是在最大团上定义势函数</p>
<h3 id="对数表示">对数表示</h3>
<p>在马尔可夫网络中，联合分布除了可以利用吉布斯分布来进行表示，也可以采取对数表示的方法，将原始的势函数$\pi_i(D_i)$ 表示成$exp(-\epsilon(D_i))$的形式，其中$\epsilon(D_i) = -log(\pi_i(D_i))$,据此，联合概率分布可以表示成：<br>
$$<br>
P(X_1,X_2,\dots,X_n) = \frac{1}{Z} exp(-\sum_{i=1}^m \epsilon(D_i))<br>
$$<br>
这种表示方法将连乘变成了连家，计算更加方便，同时有定理证明，若无向图$H$是分布$P$的一个独立映射，那么$\epsilon(D_i) = \omega_i \phi_i(D_i)$,此时$\phi$被称作特征函数，与势函数关系为：<br>
$$<br>
\pi_i(D_i) = exp(\omega_i \phi_i(D_i))<br>
$$</p>
]]></content>
      <categories>
        <category>统计学习</category>
      </categories>
  </entry>
  <entry>
    <title>贝叶斯网络</title>
    <url>/2020/07/24/bayes-network/</url>
    <content><![CDATA[<p>在该部分对贝叶斯网络进行介绍，按照递进关系总结以下知识点：<a id="more"></a></p>
<ul>
<li>条件独立性</li>
<li>条件参数化</li>
<li>朴素贝叶斯模型</li>
<li><strong>贝叶斯网络</strong></li>
</ul>
<hr>
<h3 id="问题来源">问题来源</h3>
<p>首先探讨下为什么我们需要利用变量之间的条件独立性性质，假设我们要描述的对象为$n$个二值变量：$X_1,\dots,X_n$,这些变量的联合分布为$P$,那么我们需要$\bf{2^n - 1}$个参数来描述联合分布$P$，所需参数量是$O(2^n)$级别，我们便考虑能否利用变量之间的某种性质使得$P$可以用更少的参数来描述，由此便引出了变量之间的独立性性质。</p>
<h3 id="独立性">独立性</h3>
<p>下面给出一些独立性等价的定义:</p>
<blockquote>
<ul>
<li>若$P(X = x|Y = y) = P(X=x)$对于任意的$x,y$均成立 $\Rightarrow$ 变量$X$与$Y$相互独立</li>
<li>若$P(X,Y) = P(X|Y)P(Y) = P(X)P(Y)$ $\Rightarrow$ 变量$X$与$Y$相互独立</li>
</ul>
</blockquote>
<p>若问题来源中的$n$个变量相互独立，则联合分布:<br>
$$<br>
P(X_1,X_2,\dots,X_n) = P(X_1) P(X_2) \dots P(X_n)<br>
$$<br>
此时仅需$O(n)$个变量来对联合分布进行描述，参数花销大大减少，但在实际中，独立性是一个非常强的假设，往往不能够得到满足，由此便引出了条件独立性性质。</p>
<h3 id="条件独立性">条件独立性</h3>
<blockquote>
<p>定义： 两个变量$X$和$Y$关于变量$Z$是条件独立的 $\Leftrightarrow$ 对于$\forall x,y,z$,$P(X = x|Y = y, Z = z) = P(X = x|Z = z)$ $\Leftrightarrow$ $P(X = x,Y = y|Z = z) = P(X = x|Z = z) \cdot P(Y=y|Z= z)$</p>
</blockquote>
<p>这里需要特别说明的是条件独立性并不能推出独立性，独立性也并不能推出条件独立性，下面给出两个反例：</p>
<ul>
<li>独立性 $\nRightarrow$ 条件独立性: 有两枚正反概率均为$50%$的硬币,设事件A为第一枚硬币为正面，事件B为第二枚硬币为正面，事件C为两枚硬币同面，若不考虑事件C，事件A与事件B显然独立;但若事件C已经发生，则此时A与B便不再独立，即A与B关于C不条件独立。</li>
<li>条件独立性 $\nRightarrow$ 独立性: 有两枚硬币，一枚正面概率为$90%$,另一枚反面概率为$90%$,随机拿出一枚抛掷硬币两次, 事件A表示第一次为正面，事件B表示第二次为正面，事件C表示选择的是第一枚硬币，则抛开事件C，事件A与事件B并不独立($0.5 = P(B) \neq P(B|A) = 0.9802$),但若事件C发生，则相当于进行了两次独立重复实验，此时事件A与事件B独立。</li>
</ul>
<p>条件独立性符号表示：</p>
<ul>
<li>$Ind(X;Y|Z)$ or $(X \perp Y|Z)$</li>
</ul>
<h3 id="条件参数化">条件参数化</h3>
<p>在了解了变量条件独立性后，下面以一个例子分析如何利用变量之间的条件独立性性质来减少描述联合分布所需的参数。</p>
<div align=center>
<p><img src="https://raw.githubusercontent.com/xuejy19/Images/master/Ind.png" alt="栗子"></p>
</div>
<p>图中大写字母均代表随机变量，含义如下：</p>
<ul>
<li>$D:$课程难度，$Val(D) = {d^0, d^1}$</li>
<li>$I:$智力，$Val(I) = {i^0,i^1}$</li>
<li>$S:$SAT分数，$Val(S) = {s^0,s^1}$</li>
<li>$G:$课程成绩，$Val(G) = {g^0,g^1, g^2}$</li>
<li>$L:$推荐信，$Val(L) = {l^0,l^1}$</li>
</ul>
<p>假设$G$和$S$关于$I$条件独立，下面我们分析两种情况的条件参数化</p>
<p>首先以$I$和$S$两个变量为例，我们既可以通过联合分布$P(I,S)$来进行描述，也可以通过$P(I)$和$P(S|I)$进行描述，两种描述均是三个独立参数，如下图所示。<br>
<img src="https://raw.githubusercontent.com/xuejy19/Images/master/eg2.png" alt=""></p>
<p>接下来分析$I、D、G$三个变量的条件参数化，其中$I$与$D$相互独立，下面分析两种表示方法所需的参数个数：</p>
<ul>
<li>联合分布: $P(I,D,G)$,所需参数个数为$2 \times 2 \times 3 - 1=11$</li>
<li>利用独立性条件: $P(I,D,G) = P(I)P(D)P(G|I,D)$,所需独立参数为$1+1+2\times 2 \times (3-1) =10 $<br>
$\Rightarrow$ <strong>利用独立性可以减少表示联合分布所需的参数个数</strong></li>
</ul>
<h3 id="朴素贝叶斯模型">朴素贝叶斯模型</h3>
<p>在这一部分对朴素贝叶斯模型做简要介绍，首先我们有两组变量：</p>
<ul>
<li>$C$:类别变量，$C = {c_1,\dots, c_k}$</li>
<li>观测变量：$X_1,X_2,\dots, X_d$</li>
</ul>
<p>在朴素贝叶斯模型中，我们的目标便是利用观测变量来对类别进行预测/推理。所有这些变量其实可以看做一个联合概率分布$P(C,X_1,\dots,X_d)$,然而该概率分布很难计算，所需的参数量是指数级别，因此在朴素贝叶斯模型中有一个很强的假设：</p>
<blockquote>
<p>观测变量彼此之间关于类别变量$C$是相互独立的，据此联合概率分布便很容易计算：<br>
$$   P(C,X_1,\dots,X_d) = P(X_1,\dots,X_d|C) P© = P(X_1|C)\dots P(X_d|C) P©  $$</p>
</blockquote>
<p>这是一个很强的假设，在真实世界中往往不能满足，同时因为没有充分利用变量之间的相关性性质，也造成了计算上的浪费。</p>
<h3 id="贝叶斯网络">贝叶斯网络</h3>
<blockquote>
<p>贝叶斯网路定义：贝叶斯网络，又称信念网络，是一种概率图模型，记做$(G,P)$,借由有向无环图表示一组随机变量及它们之间的条件概率分布。</p>
</blockquote>
<p>贝叶斯网络是一种以有向无环图方式对独立性假设进行编码的手段，在贝叶斯网络中，任意变量与它的非后代变量对于其父节点变量是条件独立的：<br>
$$<br>
(X_i \perp Nondesc(X_i)|Pa(X_i))<br>
$$<br>
<strong>独立性映射(I-Maps):</strong> 记$P$为一组变量$\mathbf{X}$的联合概率分布，$I§$为表征$P$中独立性的集合</p>
<blockquote>
<p>定义：$G$为贝叶斯网路，若$I(G) \subset I§$,则称$G$为$P$的一个独立映射</p>
</blockquote>
<blockquote>
<p>分解定理：若$G$是$P$的独立映射 $\Leftrightarrow$ $P(X_1,\dots,X_d) = \prod \limits_{i=1}^d P(X_i|Pa(X_i))$,通过该分解定理可以将全局的概率分布用局部的条件概率来表示</p>
</blockquote>
<p>下面分析下与联合概率分布相比，贝叶斯网络所需参数,以$n$个二值变量为例：</p>
<ul>
<li>联合概率分布：$2^n$</li>
<li>贝叶斯网络(限制各个节点入度为$k$):$n2^k$</li>
</ul>
<h3 id="d-Separation-有向分离">d-Separation(有向分离)</h3>
<p>在前面我们已经讨论了贝叶斯网络$G$编码了局部独立性假设，现在我们希望能够了解$G$是否还编码了其他独立性假设，因此我们需要一个方法来挖掘出$G$中所有的独立性假设<br>
$\Rightarrow$ <strong>d-Separation</strong><br>
下面分别讨论不分离两种情况：</p>
<ul>
<li>不分离
<ul>
<li>两个节点直接相连：此时不存在一个节点使得这两个节点关于该节点条件独立<br>
$e.g. X \rightarrow Y$</li>
<li>两个节点间接相连：head2tail,head2head,tail2tail</li>
<li>一般情况：对于图$G$中的路径$X_1 \leftrightarrow \dots \leftrightarrow X_n$关于节点集合$E$是激活的如果
<ul>
<li>对于每个V状结构$X_{i-1} \rightarrow X_i \leftarrow X_{i+1},X_i$或者其后代<br>
在集合$E$中</li>
<li>路径上的点不在集合$E$中</li>
</ul>
</li>
</ul>
</li>
<li>分离：在贝叶斯网络$G$中，$X$和$Y$在给定$Z$条件下是d分离的$d-sep_G(X;Y|Z)$,如果<strong>不存在</strong>从集合$X$到$Y$的激活路径</li>
</ul>
<p>由此可以通过d分离来找到贝叶斯网络中所有独立性关系：<br>
$$<br>
I(G) = { (\mathbf{X \perp Y |Z}): d-sep_G(\mathbf{X;Y|Z})}<br>
$$<br>
简单总结而言，如果两个节点之间的路径被阻隔，则需要满足以下两个条件其一：</p>
<ul>
<li>若存在V形结构，则冲撞点及其后代不能出现在条件集中</li>
<li>有路径中的点(非碰撞点)出现在条件集合中</li>
</ul>
<h3 id="Forward-VS-Backward">Forward VS Backward</h3>
<p>直观上感觉，贝叶斯网络的反向过程(给出结果，推理原因)要比前向过程困难不少，这是因为子节点会激活其父母之间的路径，而这种相关性会进一步向上传播。<br>
<strong>最小独立映射</strong>：对于一个图$G$和一个分布$P$，我们在图中哪怕仅仅去掉一个边都会导致$G$不再是$P$的一个独立映射，则称$G$是最小独立映射。<br>
<strong>最佳映射</strong>：对于一个图$G$和一个分布$P$，若有$I§ = I(G)$，则称图$G$是$P$的一个最佳映射</p>
]]></content>
      <categories>
        <category>统计学习</category>
      </categories>
  </entry>
  <entry>
    <title>我与历史之缘</title>
    <url>/2020/01/12/blog/</url>
    <content><![CDATA[<blockquote>
<ul>
<li>初识历史</li>
<li>求学之路</li>
<li>心得体会</li>
</ul>
</blockquote>
<h1><font size =5>初识历史</font></h1>
<p> 我开始接触历史应该是从我小学时开始，<a id="more"></a>我的小学时光是在农村度过的，那时候也没有什么娱乐活动，家里有两本老版的三国演义，阅读三国演义便成为了我儿时娱乐活动中重要的一部分。那时候在我的脑海里，蜀国是正义的，为汉室存续而立；曹操是奸贼，挟天子以令诸侯，欲行篡位之事；孙权则是一代枭雄，在我眼里属于中立角色。那时候的我特别喜欢看第一部，因为第一部进本都是蜀迅速发展的故事，而像第二部后面我基本不太会看，关羽、张飞被害，刘备举兵伐吴，陆逊火烧连营，刘备白帝城托孤，这一段我真的看得好气啊，气那些卑鄙小人，气刘备不听诸葛之言，小不忍则乱大谋。到了后面诸葛亮第二次起兵伐魏，无奈内有佞臣，在形势大好之时将诸葛亮召回，真是令我扼腕叹息，叹那“扶不起的阿斗”，为孔明不值。五丈原诸葛镶星那一段简直看的我快要哭出来了，大骂魏延，为孔明叹息。<br>
 虽然后面随着年龄的增长，我也了解到三国演义只是一本小说，其中有很多东西是虚构的，也有很多东西是经过夸张渲染的，但这本书无疑为我打开了一扇门，让我感受到了历史的魅力。小学时期我能够接触的其他历史则是我姐上学时留下来的一些历史教科书，像其他物理、数学、化学我是根本看不懂，也没什么兴趣看，唯独历史教科书可以一读，这或许也正是历史的魅力所在。</p>
<h1><font size =5>求学之路</font></h1>
<p> 在中学时期，我开始正式上历史课，基本就是框架则是一条纵线与一条横线：纵线则是按照时间顺序，从原始人开始，到夏商西周、春秋战国，然后到大一统的秦，中间还有若干朝代，最后则会讲到改革开放后的中国；同时还有着一条横线，则是讲在每一个历史阶段的政治制度、经济制度、文化，同时介绍一些推动了历史发展的人物。这一纵一横共同构成了我们看到的历史的面貌，那时候我开始觉得历史有些枯燥了，要背好多东西，历史那时候在我眼里变成了各种朝代、制度、人物的简单堆砌，变得毫无生气。那时候对于建国以后的那些历史我是最不感兴趣的，究其原因我觉得是多样的，一方面我觉得这段历史离我们太近，本着“得不到的永远在骚动，被偏爱的都有恃无恐”的态度，认为这段历没有古代历史有意思；同时也是因为我需要背的会议太多，那段历史变成了很多会议的简单堆砌，这就让这段历史更加索然无味。初中时候我还是主动去阅读了一些课外历史书籍的，主要是为了和班上同学争论，那时候我也尝试想去啃一些被奉为正史的一些书，但我记得那时候的我是被那晦涩难懂的文言文劝退了。<br>
 高中的时候，我则阅读了一本对我影响挺大的书籍———当年明月的《明朝那些事》，我记得那时候我真的是特别迷这本书，拿到这本书的那天我硬是坚持看到了凌晨三点，才恋恋不舍地把这本书从手中拿开。那是我第一次认识到原来历史也可以如此有趣，历史人物也可以如此鲜活生动。这本书对我产生了挺大的影响，我记得当年明月曾说过历史本来就应该是很有趣的。直到现在，虽然我已经建立起了基本的历史框架，但这其中最鲜活，我最愿意谈的还是明朝那段历史。同时在这段时期我也认识到，历史之所以会变得枯燥，则是因为整个都已经变成了以年份为单位的重大事件的简单累计，这样固然可以用最小的篇幅来传达最重要的信息，但我认为这样恰恰是丢掉了历史最重要的部分———连续性。一个单纯的事件是无聊的，但是事件背后的那些人那些事是生动鲜活的，而能够给人以触动、带给人以启发的恰恰是这些事件背后的故事，单纯的某年某月发生何事是给人带不来任何触动的。</p>
<h1><font size =5>心得体会</font></h1>
<p> 现在回到一个关键性的问题：人为什么要学历史？这个问题我觉得可能就和人为什么要学哲学一样，让很多人感到困惑。但其实在我看来，人是社会性的，都是在与形形色色的人交往中不断构建现实中的自己，历史则为我们提供了这样一个平台，让你可以跨越你所处的层次，跨越你所处的时代，与处在不同时代、不同阶层的人对话，从他们身上学到经验与教训。历史为什么是有趣的，因为人与人之间构成的故事本身就是有趣的，我有时候真的感觉自己很幸运，可以处在这样一个历史时期，回头望去，有希腊文明、有诸子百家、有儒释道、有众多大师，同时处在后工业革命与信息革命时代的我们可以以一个全新的视角去审视过去的这些东西，从历史中学习，从历史中获得启发，读书最快乐的莫过于发现你曾经思考过的问题很多大师也曾经思考过，他们的答案往往会让你有眼前一亮之感。</p>
]]></content>
      <categories>
        <category>闲谈</category>
      </categories>
  </entry>
  <entry>
    <title>python数据处理之时间变换</title>
    <url>/2020/09/19/python-shu-ju-chu-li-zhi-shi-jian-bian-huan/</url>
    <content><![CDATA[<p>本文将就<code>python</code>中对时间标识的处理做一点讲解，在进行数据分析时，有些时候我们需要对时间序列数据进行处理，比如像下面这样一组数据<a id="more"></a>:</p>
<table>
<thead>
<tr>
<th style="text-align:center">Time</th>
<th style="text-align:center">feature1</th>
<th style="text-align:center">…</th>
<th style="text-align:center">feature n</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:center">2013年-01月-01日: 00时00分00秒</td>
<td style="text-align:center">…</td>
<td style="text-align:center">…</td>
<td style="text-align:center">…</td>
</tr>
<tr>
<td style="text-align:center">2013年-01月-01日: 02时00分00秒</td>
<td style="text-align:center">…</td>
<td style="text-align:center">…</td>
<td style="text-align:center">…</td>
</tr>
<tr>
<td style="text-align:center">…</td>
<td style="text-align:center">…</td>
<td style="text-align:center">…</td>
<td style="text-align:center">…</td>
</tr>
<tr>
<td style="text-align:center">2015年-01月-01日: 00时00分00秒</td>
<td style="text-align:center">…</td>
<td style="text-align:center">…</td>
<td style="text-align:center">…</td>
</tr>
</tbody>
</table>
<p>对于这样一段时序数据，采样间隔2小时。当我们拿到这组数据之后，首先要考虑的问题就是这组数据有没有缺失，而判断数据记录有无缺失的关键在于其时间序列<code>Time</code>是否连续。我们一般采用<code>pandas.read_csv()</code>函数来进行<code>csv</code>文件中数据的读取，读取得到的第一列是一个字符串形式，通过这些字符串来判断时间是否连续是比较困难的，需要重新写一个函数来对字符串进行处理。但幸运的是，其实已经有函数可以帮我们完成这样一项工作了，下面介绍下本文的关键对象—<code>unix时间戳</code>：</p>
<blockquote>
<p><strong>unix时间戳:</strong> unix时间戳是一种跟踪时间（以秒为单位）的方式。此计数从1970年1月1日UTC的Unix Epoch开始。因此，unix时间戳仅仅是特定日期与Unix纪元之间的秒数。</p>
</blockquote>
<p>简而言之，unix时间戳就是一个<code>int</code>型的数据，其值代表了某个时间点距离<code>1997-01-01: 00:00:00</code>的秒数，那么如果我们能够将我们的时间字符串转换成unix时间戳，那么后面的数据拆分、数据缺失定位就更加得心应手了。本文按照以下结构进行组织:</p>
<ul>
<li>常见日期时间类型</li>
<li>日期格式转换</li>
<li>总结</li>
</ul>
<h3 id="常见日期时间类型">常见日期时间类型</h3>
<p>在进行时间序列数据处理时，常见的日期时间类型大概有以下四种:</p>
<table>
<thead>
<tr>
<th style="text-align:center">类型</th>
<th style="text-align:left">格式</th>
<th style="text-align:left">示例</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:center">time</td>
<td style="text-align:left">时间格式</td>
<td style="text-align:left">17:00:00</td>
</tr>
<tr>
<td style="text-align:center">date</td>
<td style="text-align:left">日期格式</td>
<td style="text-align:left">2019-09-19</td>
</tr>
<tr>
<td style="text-align:center">datetime</td>
<td style="text-align:left">日期时间格式</td>
<td style="text-align:left">2019-09-19: 17:00:00</td>
</tr>
<tr>
<td style="text-align:center">timestamp</td>
<td style="text-align:left">时间戳格式</td>
<td style="text-align:left">1568912400</td>
</tr>
</tbody>
</table>
<p>实际上以上四种类型可以粗分为两个类型: 字符串类型和时间戳格式，时间字符串直接转时间戳格式并不自然，因此在python中实现两者转化还需借助一个中间变量类型<code>struct_time</code>，时间格式变换流程如下:<br>
$$<br>
str \Leftrightarrow struct , time \Leftrightarrow stamp<br>
$$<br>
字符串格式和时间戳格式通过我上面的讲解大家应该可以有一个基本认识，下面重点介绍下<code>struct_time</code>类型，它是一个具有命名元祖接口的对象:可以通过索引和属性名访问值，存在以下值:</p>
<table>
<thead>
<tr>
<th style="text-align:center">index</th>
<th style="text-align:center">attribute</th>
<th style="text-align:center">values</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:center">0</td>
<td style="text-align:center">tm-year</td>
<td style="text-align:center">年份</td>
</tr>
<tr>
<td style="text-align:center">1</td>
<td style="text-align:center">tm-mon</td>
<td style="text-align:center">月份range(1,12)</td>
</tr>
<tr>
<td style="text-align:center">2</td>
<td style="text-align:center">tm-mday</td>
<td style="text-align:center">天数range(1,31)</td>
</tr>
<tr>
<td style="text-align:center">3</td>
<td style="text-align:center">tm-hour</td>
<td style="text-align:center">小时range(0,23)</td>
</tr>
<tr>
<td style="text-align:center">4</td>
<td style="text-align:center">tm-min</td>
<td style="text-align:center">分钟range(0,59)</td>
</tr>
<tr>
<td style="text-align:center">5</td>
<td style="text-align:center">tm-sec</td>
<td style="text-align:center">秒数range(0,59)</td>
</tr>
<tr>
<td style="text-align:center">6</td>
<td style="text-align:center">tm-wday</td>
<td style="text-align:center">星期range(0,6)</td>
</tr>
<tr>
<td style="text-align:center">7</td>
<td style="text-align:center">tm-yday</td>
<td style="text-align:center">年中的一天range(1,366)</td>
</tr>
</tbody>
</table>
<p>而要实现这样的变换，则就要提到<code>python</code>中两个专门与时间打交道的模块<code>time</code>和<code>datetime</code>。</p>
<h3 id="日期和格式转换">日期和格式转换</h3>
<p>这部份将介绍以下<code>time</code>以及<code>datetime</code>中常用的一些方法。</p>
<h4 id="time模块"><code>time</code>模块</h4>
<p><a href="https://docs.python.org/3/library/time.html"><code>time</code></a>模块是操作系统层面上的， 与<code>os</code>模块类似，主要提供一些系统层面的与时间有关的操作，该模块是其他所有与时间相关模块的基础，我们用到该模块比较多的方法主要有以下几个:</p>
<ul>
<li><code>time.time()</code>: 返回当前时间的时间戳</li>
</ul>
<pre class="line-numbers language-language-python"><code class="language-language-python">>>>: 
print(time.time())

>>>: 
1600502127.7830698
<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span></span></code></pre>
<ul>
<li><code>time.loacltime()</code>: 将一个<code>unix stamp</code>转换为本地时间(东八区)的<code>struct_time</code>类型</li>
</ul>
<pre class="line-numbers language-language-python"><code class="language-language-python">>>>: 
print(time.localtime(time.time())) 

>>>: 
time.struct_time(tm_year=2020, tm_mon=9, tm_mday=19, tm_hour=16, tm_min=2, tm_sec=55, tm_wday=5, tm_yday=263, tm_isdst=0)
<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span></span></code></pre>
<ul>
<li><code>time.gmtime()</code>: 将一个<code>unix stamp</code>转换为格林威治时间的<code>struct_time</code>类型</li>
</ul>
<pre class="line-numbers language-language-python"><code class="language-language-python">>>>: 
print(time.gmtime(time.time()))

>>>: 
time.struct_time(tm_year=2020, tm_mon=9, tm_mday=19, tm_hour=8, tm_min=5, tm_sec=31, tm_wday=5, tm_yday=263, tm_isdst=0)
<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span></span></code></pre>
<ul>
<li><code>time.strftime(format, struct_time)</code>: 将一个<code>struct_time</code>类型输出转换为指定格式的字符串</li>
</ul>
<pre class="line-numbers language-language-python"><code class="language-language-python">>>>: 
format = '%Y-%m-%d: %H:%M:%S' 
struct_time = time.localtime(time.time()) 
print(time.strftime(format, struct_time)) 

>>>: 
2020-09-19: 16:10:42 
<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>
<ul>
<li><code>time.strptime(str, format)</code>: 与<code>strftime</code>作用相反，将一个时间字符串按照<code>format</code>格式解析成<code>struct_time</code>类型</li>
</ul>
<pre class="line-numbers language-language-python"><code class="language-language-python">>>>: 
format = '%Y-%m-%d: %H:%M:%S'  
time_str = '2020-9-19: 16:17:00' 
print(time.strptime(time_str, format))  

>>>: 
time.struct_time(tm_year=2020, tm_mon=9, tm_mday=19, tm_hour=16, tm_min=17, tm_sec=0, tm_wday=5, tm_yday=263, tm_isdst=-1)
<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>
<ul>
<li><code>time.mktime(struct_time)</code>: 将一个<code>struct_time</code>类型数据转化成<code>unxi_stample</code>, 该操作是<code>time.localtime()</code>的反操作，因此其默认输入的<code>struct_time</code>是本地时间</li>
</ul>
<pre class="line-numbers language-language-python"><code class="language-language-python">>>>: 
format = '%Y-%m-%d: %H:%M:%S'  
time_str = '2020-9-19: 16:17:00' 
time_struct = time.strptime(time_str, format) 
print(time_struct)
print(time.mktime(time_struct)) 
print(time.localtime(time.mktime(time_struct)))

>>>:
time.struct_time(tm_year=2020, tm_mon=9, tm_mday=19, tm_hour=16, tm_min=17, tm_sec=0, tm_wday=5, tm_yday=263, tm_isdst=-1)
1600503420.0
time.struct_time(tm_year=2020, tm_mon=9, tm_mday=19, tm_hour=16, tm_min=17, tm_sec=0, tm_wday=5, tm_yday=263, tm_isdst=0)
<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>
<p>上面这些方法实际上就已经完全可以实现我们的功能，大致是下面这样一张图:</p>
<p><img src="https://raw.githubusercontent.com/xuejy19/xuejy19.github.io/source/Img/time.png" alt="转换方法"></p>
<h4 id="datetime模块"><code>datetime</code>模块</h4>
<p><code>datetime</code>模块可以看作是对<code>time</code>模块的一个封装和扩展，上面提到的<code>time</code>中常用方法在<code>datetime</code>中均有对应, 在<code>datetime</code>下主要有以下几个类：</p>
<ul>
<li><code>date</code>: 处理与日期相关的数据，属性: year, month, day</li>
<li><code>time</code>: 处理与某天时间相对应的数据， 属性: hour, minute, second, microsecond, tzinfo</li>
<li><code>datetime</code>: 日期和时间的组合，属性: year, month, day, hour, minute, second, microsecond, tzinfo</li>
<li><code>timedelta</code>: 与持续时间计算有关</li>
<li><code>tzinfo</code>: 时区信息类</li>
</ul>
<p><code>datetime</code>类基本已经可以实现上面<code>time</code>模块所能实现的功能，具体对应大家可以去官网<a href="https://docs.python.org/2/library/datetime.html">datetime</a>查看，我后面如果需要用到也会进行总结。</p>
<h4 id="pandas-to-datetime"><code>pandas.to_datetime()</code></h4>
<p>我们读取数据往往需要使用<code>pandas</code>库，得到<code>Dateframe</code>类型的数据，读取之后往往某一列就是时间序列，若用前面的方法来做转换，需要对每一个时间字符串操作，比较麻烦。为了解决该问题，就需要使用<code>pandas.to_datetime()</code>方法，使用该方法可以将变量变为<code>datetime</code>类型，方便后面进行操作。</p>
<ul>
<li>字符串时间转时间戳</li>
</ul>
<pre class="line-numbers language-language-python"><code class="language-language-python">data.time = pandas.to_datetime(data.time) 
data.time = data.time.apply(lambda x: time.mktime(x.tuple))
<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre>
<ul>
<li>时间戳转字符串</li>
</ul>
<pre class="line-numbers language-language-python"><code class="language-language-python">data.time = pandas.to_datetime(data.time) 
data.time = data.time.apply(lambda x: time.strftime(format, x.tuple))
<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre>
<h3 id="总结">总结</h3>
<p>在进行数据处理的时候我们往往不能够默认数据是“好数据”，因此就必须通过程序来对数据进行验证，而对于时序数据，时间信息的处理是绕不过去的，掌握了<code>python</code>中常用的对时间处理方法，后面处理时序数据可以更加得心应手。</p>
]]></content>
      <categories>
        <category>python学习</category>
      </categories>
      <tags>
        <tag>编程技能， 时序数据</tag>
      </tags>
  </entry>
  <entry>
    <title>EM</title>
    <url>/2020/07/30/em/</url>
    <content><![CDATA[<p>在前面概率密度函数估计中，若概率模型的变量都是可观测变量，那么给定数据，便可以直接用极大似然估计法或者贝叶斯估计法来直接估计模型参数。但是当模型中含有隐变量时，便不能直接使用这些估计方法，<strong>EM算法就是含有隐变量的概率模型参数的极大似然估计法</strong><a id="more"></a>,本文便围绕EM算法展开，主要包含以下几部分：</p>
<ul>
<li>算法引入</li>
<li>算法定义</li>
<li>算法导出</li>
<li>算法收敛性分析</li>
<li>算法应用-高斯混合模型(GMM)</li>
</ul>
<h3 id="算法引入">算法引入</h3>
<p>首先介绍一个使用EM算法的栗子，引出EM算法应用场景:</p>
<blockquote>
<p>三硬币模型： 假设有三枚硬币A,B,C。这三枚硬币出现正面的概率分别为$\pi,p,q$。现进行如下掷币实验：首先掷出硬币A,根据其结果选出硬币B或硬币C，正面选硬币B,反面选硬币C；然后掷选出的硬币，掷硬币的结果，正面记做1，反面记做0,独立的重复$n$次实验，得到实验结果为$(x_1,\dots,x_n)$<br>
<strong>问题</strong>：根据实验结果估计三枚硬币参数(正面朝上概率)</p>
</blockquote>
<p>这个问题的特殊性在于，我们只能够观察到最终实验结果，但是对于中间变量(硬币A的投掷结果)则没有记录，是隐变量，所以该问题是在有隐变量情况下的参数估计问题。下面首先尝试使用极大似然估计方法，做如下标记：</p>
<ul>
<li>待估计参数$\theta = (\pi,p,q)$</li>
<li>观测变量$y$:最终掷硬币结果</li>
<li>隐含变量$z$:每次实验掷硬币A的结果</li>
</ul>
<p>若直接采用最大似然估计，我们希望能够写出似然概率$P(y|\theta)$，但这个概率并不能够直接写出，需要引入隐含变量$z$，因此考虑将似然概率写成<br>
$$<br>
\begin{aligned}<br>
P(y|\theta) &amp;= \sum P(y,z|\theta) = \sum_{z} P(z|\theta) P(y|\theta,z) \\<br>
&amp;= \pi p^y (1-p)^{1-y} + (1-\pi)q^y(1-q)^{1-y}<br>
\end{aligned}<br>
$$<br>
将观测数据记做$Y = (Y_1,\dots, Y_n)^T$,未观测数据记做$Z = (Z_1,\dots, Z_n)^T$,则似然函数可以写做：<br>
$$<br>
L = P(Y|\theta) = \prod_{j=1}^n[\pi p^{y_j} (1-p)^{1-y_j} + (1-\pi)q^{y_j}(1-q)^{1-y_j}]<br>
$$<br>
对数似然函数则是:<br>
$$<br>
logL = \sum_{j=1}^nlog[\pi p^{y_j} (1-p)^{1-y_j} + (1-\pi)q^{y_j}(1-q)^{1-y_j}]<br>
$$<br>
直接极大化对数似然函数并不能得到解析解，该问题只能通过EM算法迭代求解</p>
<h3 id="算法推导">算法推导</h3>
<p>首先给出算法定义：<br>
<strong>EM算法</strong>:</p>
<blockquote>
<p>输入： 观测变量数据$Y$,隐变量数据$Z$，联合分布$P(Y,Z|\theta)$,条件分布$P(Z|Y,\theta$<br>
输出： 模型参数$\theta$</p>
</blockquote>
<ol>
<li>选择参数初始值$\theta^0$,开始迭代</li>
<li>E步：记$\theta^i$为第$i$次迭代参数$\theta$的估计值，在第$i+1$次迭代的E步，计算：</li>
</ol>
<p>$$<br>
\begin{aligned}<br>
Q(\theta,\theta^i) &amp;= E_Z[logP(Y,Z|\theta)|Y,\theta^i] \\<br>
&amp;= \sum_Z log P(Y,Z|\theta) P(Z|Y,\theta^i)<br>
\end{aligned}<br>
$$</p>
<ol start="3">
<li>M步： 求使得$Q(\theta,\theta^i)$极大化的$\theta$,确定第$i+1$次迭代的参数的估计值$\theta^{i+1}$<br>
$$ \theta^{i+1} = argmax_{\theta}Q(\theta,\theta^i)$$</li>
<li>重复第2,3步，直到算法收敛</li>
</ol>
<p>从形式上来看，$Q$函数与极大似然函数其实是一样的，不同之处在于$P(Z|Y,\theta^i)$中使用的是参数迭代估计值，用该迭代估计值代替真值，$Q$函数可以看作是：</p>
<blockquote>
<p>完全数据的对数似然函数$logP(Y,Z|\theta)$关于在给定观测数据$Y$和当前参数$\theta^i$下对未观测数据$Z$的的条件概率分布$P(Z|Y,\theta^i)$的期望。</p>
</blockquote>
<p>我们这样使用$Q$函数来近似优化最大似然函数，需要证明这样的迭代优化是有效的</p>
<h3 id="算法导出">算法导出</h3>
<p>对数似然形式前面已经讨论过，形式为：<br>
$$<br>
\begin{aligned}<br>
L(\theta) &amp;= log P(Y|\theta) = log \sum_Z P(Y,Z|\theta) \<br>
&amp;= log(\sum_Z P(Y|\theta,Z) P(Z|\theta))<br>
\end{aligned}<br>
$$<br>
直接极大化对数似然函数困难之处在于两点：</p>
<ul>
<li>含有隐变量$Z$</li>
<li>对数似然函数是和的对数的形式</li>
</ul>
<p>假设在第$i$次迭代后参数$\theta$估计值为$\theta^i$,新的估计值是$\theta$，我们期望新的估计值$\theta$能够使得对数似然函数增大，因此，考虑计算两者的差：<br>
$$<br>
\begin{aligned}<br>
L(\theta) - L(\theta^i) &amp;= log(\sum_Z P(Y|\theta,Z) P(Z|\theta)) - logP(Y|\theta^i)<br>
\\<br>
&amp;= log(\sum_Z P(Z|Y,\theta^i) \frac{P(Y|Z,\theta)P(Z|\theta)}{P(Z|Y,\theta^i)}) - logP(Y|\theta^i)<br>
\\<br>
&amp;\geq \sum_Z P(Z|Y,\theta^i) log \frac{P(Y|Z,\theta)P(Z|\theta)}{P(Z|Y,\theta^i)} - logP(Y|\theta^i)  (Jensen不等式)<br>
\\<br>
&amp;= \sum_Z P(Z|Y,\theta^i) log \frac{P(Y|Z,\theta)P(Z|\theta)}{P(Z|Y,\theta^i)P(Y|\theta^i)}<br>
\end{aligned}<br>
$$<br>
令<br>
$$<br>
B(\theta,\theta^i) = L(\theta^i) + \sum_Z P(Z|Y,\theta^i) log \frac{P(Y|Z,\theta)P(Z|\theta)}{P(Z|Y,\theta^i)P(Y|\theta^i)}<br>
$$<br>
则有：<br>
$$<br>
L(\theta) \geq B(\theta,\theta^i)<br>
$$<br>
至此，我们找到了对数似然函数的一个下界，同时在点$\theta^i$处，有：<br>
$$<br>
L(\theta^i) = B(\theta^i,\theta^i)<br>
$$<br>
因此若我们可以找到$\theta^{i+1}$,使得$B(\theta^{i+1},\theta^i)&gt;B(\theta^i,<br>
\theta^i)$,则有：<br>
$$<br>
L(\theta^{i+1}) \geq B(\theta^{i+1},\theta^i) &gt; B(\theta^i,\theta^i) = L(\theta^i)<br>
$$<br>
因此我们可以考虑优化$B(\theta,\theta^i)$，从而变相使原始似然函数增大。接下来考虑$B(\theta,\theta^i)$中需要优化的具体是哪一项:<br>
$$<br>
\begin{aligned}<br>
\theta^{i+1} &amp;= argmax(L(\theta^i) +  \sum_Z P(Z|Y,\theta^i) log \frac{P(Y|Z,\theta)P(Z|\theta)}{P(Z|Y,\theta^i)P(Y|\theta^i)})  \\<br>
&amp;= argmax  \sum_Z P(Z|Y,\theta^i) log \frac{P(Y|Z,\theta)P(Z|\theta)}{P(Z|Y,\theta^i)P(Y|\theta^i)} \\<br>
&amp;= argmax \sum_Z P(Z|Y,\theta^i) log(P(Y|Z,\theta) P(Z|\theta)) \\<br>
&amp;= argmax \sum_Z P(Z|Y,\theta^i) log P(Y,Z|\theta) \\<br>
&amp;= argmax Q(\theta,\theta^i)<br>
\end{aligned}<br>
$$</p>
<div align=center>
<p><img src="https://raw.githubusercontent.com/xuejy19/Images/master/EM.png" alt="EM"></p>
</div>
<p>图中下方曲线为$B(\theta,\theta^i)$，上方曲线为$L(\theta)$,在$\theta^n$处，两者相等，当对$B(\theta,\theta^n)$进行优化时，似然函数也得到了优化，同样，这种迭代算法只能够保证每次迭代都会使得似然函数增加，但并不能保证为全局最优。</p>
<h3 id="算法收敛性分析">算法收敛性分析</h3>
<p>该部分只给出一些结论，证明过程略过</p>
<blockquote>
<p><strong>定理1</strong>:设$P(Y|\theta)$为观测数据的似然函数，$\theta^i$为EM算法得到的参数估计序列，$P(Y|\theta^i)$为对应的似然函数序列，则$P(Y|\theta_i)$是单调递增的，即：<br>
$$<br>
P(Y|\theta^{i+1}) \geq P(Y|\theta^i)<br>
$$<br>
<strong>定理2</strong>:设$L(\theta) = log P(Y|\theta)$为观测数据的对数似然函数，$\theta^i$为EM算法得到的参数估计序列，$L(\theta^i)$为对应的对数似然函数序列</p>
<ol>
<li>如果$P(Y|\theta)$有上界,则$L(\theta^i) = log P(Y|\theta^i)$ 收敛到某一值$L^*$</li>
<li>在函数$Q(\theta,\theta^{’})$与$L(\theta)$满足一定条件下，由EM算法得到的参数估计序列$<br>
\theta^i$的收敛点$\theta^*$是$L(\theta)$的稳定点</li>
</ol>
</blockquote>
<h3 id="算法应用-高斯混合模型-GMM">算法应用-高斯混合模型(GMM)</h3>
<p>首先给出高斯混合模型定义:</p>
<blockquote>
<p>高斯混合模型是指具有如下形式的概率分布模型：<br>
$$<br>
P(y|\theta) = \sum_{k=1}^K \alpha_k \phi(y|\theta_k)<br>
$$<br>
其中,$\alpha_k$是系数，$\alpha_k \geq 0$,$\sum \alpha_k = 1$;$\phi(y|\theta_k)$是高斯概率密度函数，$\theta_k = (\mu_k,\sigma_k^2)$,称为第$k$个分模型</p>
</blockquote>
<p>高斯模型是描述数据分布的一种常见模型，但在有时候单一的高斯模型并不足以对数据进行描述，因此便考虑该数据分布有没有可能用多个高斯模型来描述，即任意一个数据点可能来自于某一个分模型。假设观测数据$y_1,y_2,\dots,y_N$由高斯混合模型生成：<br>
$$<br>
P(y|\theta) = \sum_{k=1}^K \alpha_k \phi(y|\theta_k)<br>
$$<br>
其中$\theta = (\alpha_1,\dots,\alpha_K;\theta_1,\dots,\theta_K)$,我们现在期望利用观测数据将这些参数估计出来。</p>
<h4 id="隐变量定义">隐变量定义</h4>
<p>首先来看对于这样一个模型，隐变量是什么，数据是这样产生的：首先依概率$\alpha_k$选择第$k$个高斯分布分模型，然后依第$k$个分模型的概率分布生成观测数据$y$。观测数据$y$是已知的，但是$y$究竟来自于哪个分模型是未知的，记该变量为隐变量$\gamma_{jk}$,其定义如下：<br>
$$<br>
\begin{aligned}<br>
\gamma_{jk} &amp;= \begin{cases}<br>
1,&amp; 第j个观测来自第k个分模型 \<br>
0,&amp; other<br>
\end{cases}<br>
\\<br>
j &amp;= 1,\dots,N;k = 1,\dots,K<br>
\end{aligned}<br>
$$<br>
目前，我们的观测数据为$y_j$,未观测数据是$\gamma_{jk}$,完全数据是：<br>
$$<br>
(y_j,\gamma_{j_1},\dots,\gamma_{jk}),j=1,\dots,N<br>
$$<br>
于是，可以写出完全数据的对数似然函数：</p>
<p>$$<br>
\begin{aligned}<br>
P(y,\gamma|\theta) &amp;= \prod_{j=1}^N P(y_j,\gamma_{j1},\dots,\gamma_{jk}|\theta) \\<br>
&amp;= \prod_{k=1}^K \prod_{j=1}^N [\alpha_k \phi(y_j|\theta_k)]^{\gamma_{jk} } \\<br>
&amp;= \prod_{k=1}^K \alpha_k^{n_k} \prod_{j=1}^N [\alpha_k \phi(y_j|\theta_k)]^{\gamma_{jk} } \\<br>
&amp;= \prod_{k=1}^K \alpha_k^{n_k} \prod_{j=1}^N [\frac{1}{\sqrt{2\pi}\sigma_k} exp(-\frac{(y_j-\mu_k)^2}{2\sigma_k^2})]^{\gamma_{jk}}<br>
\end{aligned}<br>
$$</p>
<p>公式中$n_k = \sum_{j=1}^N \gamma_{jk},\sum_{k=1}^K n_k = N$,由此，便可以写出完全数据的对数似然函数：</p>
<p>$$<br>
logP(y,\gamma|\theta) = \sum_{k=1}^K { n_klog\alpha_k + \sum_{j=1}^N \gamma_{jk}[log(\frac{1}{\sqrt{2\pi}})-log\sigma_k - \frac{1}{2\sigma_k^2} (y_j-\mu_k)^2]}<br>
$$</p>
<p><strong>E步</strong>：其实就是要在已知上次迭代参数估计值$\theta$和观测值$y$的情况下将$\gamma_{jk}$估计出来，然后代回完全数据的对数似然函数，下面就求解下$E(\gamma_{jk}|y,\theta)$:</p>
<p>$$<br>
\begin{aligned}<br>
\hat{\gamma}<em>{jk} &amp;= E(\gamma</em>{jk}|y,\theta) = P(\gamma_{jk}|y,\theta)\\<br>
&amp;= \frac{P(\gamma_{jk = 1,y|\theta})}{\sum_{k=1}^K P(\gamma_{jk}=1,y_j|\theta)} \\<br>
&amp;= \frac{P(y_j|\gamma_{jk}=1,\theta)P(\gamma_{jk}=1|\theta)}{\sum_{k=1}^K P(y_j|\gamma_{jk}=1,\theta)P(\gamma_{jk}=1|\theta)} \\<br>
&amp;= \frac{\alpha_k \phi(y_j|\theta_k)}{\sum_{k=1}^K \alpha_k \phi(y_j|\theta_k)}<br>
\end{aligned}<br>
$$</p>
<p>将该估计值代回完全数据的对数似然函数，便得到了$Q$函数：</p>
<p>$$<br>
Q(\theta,\theta^i) =  logP(y,\gamma|\theta) = \sum_{k=1}^K { n_klog\alpha_k + \sum_{j=1}^N \hat{\gamma}_{jk}[log(\frac{1}{\sqrt{2\pi}})-log\sigma_k - \frac{1}{2\sigma_k^2} (y_j-\mu_k)^2]}<br>
$$</p>
<p><strong>M步</strong>：迭代的M步是求函数$Q(\theta,\theta^i)$对$\theta$的极大值，即求新一轮迭代的模型参数:</p>
<p>$$<br>
\theta^{i+1} = \arg max_{\theta} Q(\theta,\theta^i)<br>
$$</p>
<p>该步只需分别对$\alpha_K,\mu_k,\sigma_k$求偏导，令偏导等于0,注意在求解$\alpha_k$时需要利用$\alpha_k$天然的约束条件$\sum_{k=1}^K \alpha_k = 1$,最终求解结果为:</p>
<p>$$<br>
\begin{aligned}<br>
\hat{\mu}<em>k &amp;= \frac{\sum</em>{j=1}^N \hat{\gamma}<em>{jk}y_j}{\sum</em>{j=1}^N \hat{\gamma}<em>{jk}} \\<br>
\hat{\sigma}<em>k^2 &amp;= \frac{\sum</em>{j=1}^N \hat{\gamma}</em>{jk}(y_j - \mu_k)^2}{\sum_{j=1}^N \hat{\gamma}<em>{jk}} \\<br>
\hat{\alpha}<em>k &amp;= \frac{n_k}{N} = \frac{\sum</em>{j=1}^N \hat{\gamma}</em>{jk}}{N}<br>
\end{aligned}<br>
$$</p>
<h4 id="总结">总结</h4>
<p>该部分就高斯混合模型的EM算法进行总结</p>
<ul>
<li>取参数的初始值开始迭代</li>
<li>E步：根据当前模型参数，计算分模型$k$对观测数据$y_j$的响应度</li>
<li>M步：计算新一轮迭代的模型参数</li>
<li>重复直到算法收敛</li>
</ul>
]]></content>
      <categories>
        <category>统计学习</category>
      </categories>
      <tags>
        <tag>EM算法,隐变量,高斯混合模型</tag>
      </tags>
  </entry>
  <entry>
    <title>logistic回归与最大熵模型</title>
    <url>/2020/08/23/logistic-hui-gui-yu-zui-da-shang-mo-xing/</url>
    <content><![CDATA[<p>在学习李航老师《统计学习》条件随机场章节时，对于学习算法感到有些陌生，后来发现在书中第六章“logistic回归与最大熵模型”有过一些介绍，因此本章节便总结一下相关知识，其中logistic回归模型做简要介绍，重点放在最大熵模型的学习算法上，本文按照如下结构组织：</p>
<ul>
<li>logistic回归</li>
<li>最大熵模型</li>
</ul>
<a id="more"></a>
<h3 id="logistic回归">logistic回归</h3>
<h4 id="logistic分布">logistic分布</h4>
<p>首先介绍下logistic分布：</p>
<blockquote>
<p><strong>logistic分布</strong>:设$X$是连续变量，$X$服从logistic分布是指$X$具有下列分布函数和密度函数：<br>
$$<br>
\begin{aligned}<br>
F(x) &amp;= P(X \leq x) = \frac{1}{1 + e^{-\frac{x-\mu}{\gamma}}} \<br>
f(x) &amp;= F’(x) = \frac{e^{-\frac{x-\mu}{\gamma}}}{\gamma(1+e^{-\frac{x-\mu}{\gamma}})^2}<br>
\end{aligned}<br>
$$<br>
式中，$\mu$为位置参数，$\gamma&gt;0$为形状参数</p>
</blockquote>
<p>对于logistic分布，其概率密度函数和分布函数如下图所示：<br>
<img src="https://raw.githubusercontent.com/xuejy19/xuejy19.github.io/source/Img/logistic.jpeg" alt="logistic"><br>
分布函数的图形是一条$S$形曲线，该曲线以点$(\mu,\frac{1}{2})$为中心对称，即满足:<br>
$$<br>
F(-x+\mu) - \frac{1}{2} = -F(x+\mu) +\frac{1}{2}<br>
$$<br>
曲线在中心增长速度较快，在两端增长速度慢，形状参数$\gamma$的值越小，曲线在中心附近增长得越快。</p>
<h4 id="二项logistic回归模型">二项logistic回归模型</h4>
<p>二项logistic回归模型是一种二分类模型，由条件概率分布$P(Y|X)$表示，形式为参数化的logistic分布，随机变量$X$的取值为实数，随机变量$Y$取值为1或0，下面给出logistic回归模型定义：</p>
<blockquote>
<p><strong>logistic回归模型</strong>：二项logistic回归模型是如下条件概率分布：<br>
$$<br>
\begin{aligned}<br>
P(Y=1|x) &amp;= \frac{exp(w \cdot x + b)}{1 + exp(w \cdot x + b)} \<br>
P(Y=0|x) &amp;= \frac{1}{1 + exp(w \cdot x + b)}<br>
\end{aligned}<br>
$$<br>
其中，$x \in R^n$是输入，$Y \in {0,1}$是输出，$w \in R^n,b \in R$是参数。</p>
</blockquote>
<p>若我们已经有了logistic模型，则判断样本点$x$属于哪一类只需要比较两个概率值的大小，下面给出对数几率的定义:</p>
<blockquote>
<p><strong>对数几率</strong>：一个事件的几率是指该事件发生的概率与该事件不发生的概率的比值，如果一个事件发生的概率是$p$,那么该事件的几率是$\frac{p}{1-p}$,该事件的对数几率或logit函数是:<br>
$$<br>
logit§ = log \frac{p}{1-p}<br>
$$</p>
</blockquote>
<p>对于logistic回归而言，事件$(Y=1|X)$的对数几率为：<br>
$$<br>
log \frac{P(Y=1|x)}{1 - P(Y=1|x)} = w \cdot x<br>
$$<br>
这说明，logistic回归模型可以看做是用线性回归模型去逼近真实标记的对数几率。因此logistic回归模型也被称为对数几率回归，可以看作是经过了如下的映射环节，将$R^n$空间中的$x$映射到了其标记的概率：<br>
$$<br>
x \in R^n  \rightarrow w \cdot x \in R \rightarrow P(Y=1|x) \in [0,1]<br>
$$</p>
<h4 id="模型参数估计">模型参数估计</h4>
<p>对于logistic回归模型，需要估计的参数是$w,b$,可以考虑将它们合并为一个$n+1$维向量:<br>
$$<br>
w = [w;b]<br>
$$<br>
同时，给输入向量加一维，表示为:<br>
$$<br>
x = [x;1]<br>
$$<br>
这样logistic回归模型就可以表示成更加紧凑的形式:<br>
$$<br>
\begin{aligned}<br>
P(Y=1|x) &amp;=  \frac{exp(w \cdot x) }{1+exp(w \cdot x)} \<br>
P(Y=0|x) &amp;=  \frac{1}{1 + exp(w \cdot x)}<br>
\end{aligned}<br>
$$<br>
logistic模型参数学习属于有监督的模型学习问题范畴，利用极大似然法就可以解决，设：<br>
$$<br>
P(Y=1|x) = \pi(x), \quad P(Y=0|x) = 1- \pi(x)<br>
$$<br>
似然函数可以写成:<br>
$$<br>
\prod_{i=1}^N [\pi(x_i)]^{y_i} [1 - \pi(x_i)]^{1-y_i}<br>
$$<br>
对数似然函数为:<br>
$$<br>
\begin{aligned}<br>
L(w) &amp;= \sum_{i=1}^N [y_i log \pi(x_i) + (1-y_i)log(1-\pi(x_i))] \<br>
&amp;= \sum_{i=1}^N [y_i log \frac{\pi(x_i)}{1 - \pi(x_i)} + log(1-\pi(x_i))] \<br>
&amp;= \sum_{i=1}^N [y_i(w \cdot x) - log(1 + exp(w \cdot x))]<br>
\end{aligned}<br>
$$<br>
对$L(w)$求极大值，得到$w$的估计值，常用梯度下降法及拟牛顿法求解(后面专开一章讲解)。</p>
<h4 id="多项logistic回归">多项logistic回归</h4>
<p>上面介绍的是二项logistic模型，适用于二分类，可以将其推广为多项logistic分类模型，用于多类分类。假设离散型随机变量$Y$的取值集合是${1,2,\dots,K}$,那么多项logistic回归的模型是:<br>
$$<br>
\begin{aligned}<br>
P(Y=k|x) &amp;= \frac{\exp(w_k \cdot x)}{1 + \sum_{k=1}^{K-1} \exp(w_k \cdot x)} \<br>
P(Y=K|x) &amp;= \frac{1}{1 + \sum_{k=1}^{K-1} \exp(w_k \cdot x)}<br>
\end{aligned}<br>
$$<br>
这里$x \in R^{n+1},w_k \in R^{n+1}$。二项logistic回归的参数估计法也可以推广到多项logistic回归</p>
<h3 id="最大熵模型">最大熵模型</h3>
<p>最大熵模型，是由最大熵原理推导实现。本部分按照以下结构组织:</p>
<ul>
<li>最大熵原理</li>
<li>最大熵模型推导</li>
<li>最大熵模型学习</li>
</ul>
<h4 id="最大熵原理">最大熵原理</h4>
<p>最大熵原理是概率模型学习的一个准则，最大熵原理认为：</p>
<blockquote>
<p>学习概率模型时，在所有可能的概率模型中，熵最大的模型是最好的模型。通常用约束条件来确定概率模型的集合，所以，最大熵原理也可以表述为在满足约束条件的模型集合中选取熵最大的模型。</p>
</blockquote>
<p>我们首先来复习下信息论中关于熵的定义：</p>
<blockquote>
<p>在信息论与概率统计中，熵是表示<strong>随机变量不确定性</strong>的度量，设$X$是一个取有限个值的离散随机变量，其概率分布为:<br>
$$<br>
P(X = x_i) = p_i, \quad i =1,2,\dots,n<br>
$$<br>
则随机变量$X$的熵定义为:<br>
$$<br>
H(X) = - \sum_{i=1}^n p_i \log p_i<br>
$$</p>
</blockquote>
<p>若$p_i = 0$,则定义$0\log 0 =0$,式中的对数一般取以2为底或者以$e$为底，这时熵的单位被称作比特或纳特，由定义可知，熵只依赖于$X$的分布，而与$X$的取值无关，因此也可将$X$的熵记作$H§$,熵越大，随机变量的不确定性越大。因为$p_i \leq 0$,由$H§$函数性质(凹函数，应用拉格朗日乘子法可求最大值)可知:<br>
$$<br>
0 \leq H§ \leq \log n<br>
$$<br>
当且仅当$X$是均匀分布时右侧等号成立,也就是说，当$X$服从均匀分布时，熵最大，该随机变量的不确定性最大。下面给出条件熵的定义:</p>
<blockquote>
<p>设有随机变量$(X,Y)$,其联合概率分布为：<br>
$$<br>
P(X = x_i,Y = y_i) = p_{ij}\quad i = 1,2,\dots,n;\quad j = 1,2,\dots,m<br>
$$<br>
条件熵$H(Y|X)$表示在已知随机变量$X$条件下随机变量$Y$的不确定性。随机变量$X$给定的条件下随机变量$Y$的条件熵$H(Y|X)$,定义为$X$给定条件下$Y$的条件概率分布的熵对$X$的数学期望：<br>
$$<br>
H(Y|X) = \sum_{i=1}^n p_i H(Y|X = x_i)<br>
$$<br>
当熵和条件熵中的概率由数据估计得到时，所对应的熵与条件熵分别称为经验熵和经验条件熵</p>
</blockquote>
<p>直观的，最大熵原理认为要选择的概率模型首先必须满足已有的事实，即约束条件。在没有更多信息情况下，那些不确定的部分都是“等可能的”,最大熵原理通过熵的最大化来表示等可能性。下面通过一个简单的例子来介绍最大熵原理：</p>
<blockquote>
<p>🌰：假设随机变量$X$有五个取值${A,B,C,D,E }$,要估计取各个值的概率$P(A),P(B),P©,P(D),P(E)$</p>
<ul>
<li>不提供任何额外约束，则只有这些概率的固有约束<br>
$$<br>
P(A) + P(B) + P© + P(D) + P(E) = 1<br>
$$<br>
因为没有任何额外的信息，则根据最大熵原理，只能够认为该概率分布为均匀分布：<br>
$$<br>
P(A) = P(B) = P© = P(D) = P(E) = \frac{1}{5}<br>
$$</li>
<li>从一些先验知识中得到了一些对概率值的约束条件，如$P(A) + P(B) = \frac{3}{10}$,满足约束的概率分布仍旧有无穷多个，在缺少其他信息的情况下，可以认为$A$和$B$是等概率的，$C,D,E$等概率。</li>
</ul>
</blockquote>
<p>实际上，最大熵原理就是为我们提供了一种选择最优概率模型的准则(保守)</p>
<h4 id="最大熵模型的定义">最大熵模型的定义</h4>
<p>最大熵原理是统计学习的一般原理，将它应用到分类得到最大熵模型。<br>
假设分类模型是一个条件概率分布$P(Y|X)$,$X \in \mathcal{X} \subseteq R^n$表示输入，$Y \in \mathcal{Y}$表示输出，$\mathcal{X}$和$\mathcal{Y}$分别是输入和输出的集合，这个模型表示的是对于给定的输入$X$，以条件概率$P(Y|X)$输出$Y$。<br>
给定一个训练数据集:<br>
$$<br>
T = {  (x_1,y_1), (x_2,y_2), \dots, (x_N,y_N)}<br>
$$<br>
学习的目标是应用最大熵模型选择最好的分类模型。首先考虑模型应该满足的条件，即该数据集为模型添加了哪些约束，给定训练数据集后，我们可以确定联合分布$P(X,Y)$的经验分布和边缘分布$P(X)$的经验分布，分别以$\tilde{P}(X,Y)$和$\tilde{P}(X)$表示:<br>
$$<br>
\begin{aligned}<br>
\tilde{P}(X= x,Y=y) &amp;= \frac{\nu(X = x,Y = y)}{N} \<br>
\tilde{P}(X = x) &amp;= \frac{\nu(X = x)}{N}<br>
\end{aligned}<br>
$$<br>
式中$\nu(\cdot)$是频数统计函数，$N$表示训练样本容量。用特征函数$f(x,y)$描述输入$x$和输出$y$之间的某一个事实，其定义是:<br>
$$<br>
f(x,y) = \begin{cases}<br>
1,  &amp;x和y满足某一事实 \<br>
0,  &amp;else<br>
\end{cases}<br>
$$<br>
特征函数$f(x,y)$关于经验分布$\tilde{P}(X,Y)$的期望值，用$E_{\tilde{P}}(f)$表示：<br>
$$<br>
E_{\tilde{P}}(f) = \sum_{x,y} \tilde{P}(x,y) f(x,y)<br>
$$<br>
特征函数$f(x,y)$关于模型$P(Y|X)$i与经验分布$\tilde{P}(X)$的期望值，用$E_P(f)$表示：<br>
$$<br>
E_P(f) = \sum_{x,y} \tilde{P}(x) P(y|x) f(x,y)<br>
$$<br>
如果模型能够获取训练数据中的信息，那么就可以假设这两个期望值相等，即：<br>
$$<br>
E_P(f) = E_{\tilde{P}}(f)<br>
$$<br>
将这作为模型学习的约束条件，假如有$n$个特征函数$f_i(x,y),i=1,2,\dots,n$,那么就有$n$个约束条件，下面给出最大熵模型的定义:</p>
<blockquote>
<p>最大熵模型:假设满足所有约束条件的模型集合为:<br>
$$<br>
C \equiv { P \in \mathcal{P} |E_P(f_i) = E_{\tilde{P}}(f_i), i = 1,2,\dots,n}<br>
$$<br>
定义在条件概率分布$P(Y|X)$上的条件熵为：<br>
$$<br>
H§ = -\sum_{x,y}  \tilde{P}(x)P(y|x) log P(y|x)<br>
$$<br>
则模型集合$C$中条件熵$H§$最大的模型称为最大熵模型</p>
</blockquote>
<p>从定义可以看出，最大熵模型是基于这样一种思想:</p>
<blockquote>
<p>当我们需要对一个事件(变量)的概率分布进行预测时，最大熵原理告诉我们所有的预测应当满足全部已知的条件(约束)，而对未知的情况不要做任何主观假设，保留模型最大的不确定性。</p>
</blockquote>
<h4 id="最大熵模型的学习">最大熵模型的学习</h4>
<p>最大熵模型的学习过程就是求解最大熵模型的过程。最大熵模型学习可以形式化为约束最优化问题。<br>
对于给定的训练数据集$T = { (x_1,y_1), (x_2,y_2),\dots, (x_N,y_N)}$以及特征函数$f_i(x,y),i=1,2,\dots,n$,最大熵模型的学习等价于求解最优化问题:<br>
$$<br>
\begin{aligned}<br>
&amp;\max_{P \in C} \quad H§ = -\sum_{x,y}  \tilde{P}(x)P(y|x) log P(y|x) \<br>
&amp;s.t. \quad E_P(f_i) = E_{\tilde{P}}(f_i),\quad i =1,2,\dots,n \<br>
&amp; \quad \quad \sum_y P(y|x) =1<br>
\end{aligned}<br>
$$<br>
按照最优化问题的习惯，将求解最大值问题改为等价的求最小值问题:<br>
$$<br>
\begin{aligned}<br>
&amp;\min_{P \in C} \quad -H§ = \sum_{x,y}  \tilde{P}(x)P(y|x) log P(y|x) \<br>
&amp;s.t. \quad E_P(f_i) - E_{\tilde{P}}(f_i) = 0,\quad i =1,2,\dots,n \<br>
&amp; \quad \quad \sum_y P(y|x) =1<br>
\end{aligned}<br>
$$<br>
<strong>该优化问题按照李航老师《统计学习》中的说法是满足强对偶性的，但书中只是一带而过，并没有进行详细说明，此处留坑，后面的推导就先默认该优化问题满足强对偶性，后面我搞懂了再来填坑</strong></p>
<hr>
<p><strong>这个问题我又思考了一下，自变量是一个分布看似是一个很虚的东西，但它也不过是一个函数，若$X,Y$都是离散取值变量，那么这个条件概率分布只是一个有限长度的向量，而若$X,Y$取值连续，则该条件概率密度函数可以看作一个无限长的向量，这样来看原始待优化函数其实就是$\boldsymbol{x} \cdot \log \boldsymbol{x}$,这显然是一个凸函数，而约束条件也不过是待优化参数的线性组合，这显然是一个凸优化问题</strong></p>
<hr>
<p>该优化问题共有$n+1$个等式约束，因此考虑引入拉格朗日乘子$w_0,w_1,\dots,w_n$,定义拉格朗日函数$L(P,w)$:<br>
$$<br>
\begin{aligned}<br>
L(P,w) &amp;= -H§ + w_0 (1 - \sum_y P(y|x)) + \sum_{i=1}^n w_i (E_{\tilde{P}}(f_i) - E_P(f_i)) \<br>
&amp;= \sum_{x,y}  \tilde{P}(x)P(y|x) log P(y|x)  + w_0 (1 - \sum_y P(y|x)) \<br>
&amp;+ \sum_{i=1}^n w_i(\sum_{x,y} \tilde{P}(x,y) f(x,y) - \sum_{x,y} \tilde{P}(x) P(y|x) f(x,y))<br>
\end{aligned}<br>
$$<br>
原始优化问题的对偶问题写作：<br>
$$<br>
\max_{w} \min_{P} L(P,w)<br>
$$<br>
首先求解对偶函数$\phi(w)$,因为$L(P,w)$关于$P$是凸函数，因此直接通过求导令导数为0即可：<br>
$$<br>
\begin{aligned}<br>
\frac{ \partial L(P,w)}{\partial P(y|x)} &amp;=  log P(y|x) +1 - w_0 - \sum_{i=1}^n w_i f_i (x,y) = 0<br>
\end{aligned}<br>
$$<br>
在求导这一部分我不太认同李航老师书中的写法，因为$P(y|x)$本身可以看作是一个向量，因此求导相当于对该向量某个分量求导。由此可得$P(y|x)$的解析表达式:<br>
$$<br>
P(y|x) = \exp(\sum_{i=1}^n w_i f_i(x,y) + w_0 -1) = \frac{\exp(\sum_{i=1}^n w_i f_i(x,y))}{\exp(1-w_0)}<br>
$$<br>
又因为有概率密度函数的固有约束$\sum_y P(y|x) = 1$,可得:<br>
$$\exp(1-w_0) = \sum_y \exp(\sum_{i=1}^n w_i f_i(x,y))$$<br>
因此，$P_w(y|x)$的解析表达式写作:<br>
$$<br>
\begin{aligned}<br>
P_w(y|x) &amp;= \frac{1}{Z_w(x)} \exp(\sum_{i=1}^n w_i f_i(x,y)) \<br>
Z_w(x) &amp;= \sum_y \exp(\sum_{i=1}^n w_i f_i(x,y))<br>
\end{aligned}<br>
$$<br>
至此，我们得到了对偶函数$\phi(w)$表达式：<br>
$$<br>
\begin{aligned}<br>
\phi(w) &amp;= \sum_{x,y} \tilde{P}(x,y) \sum_{i=1}^n w_i f_i(x,y) + \sum_{x,y} \tilde{P}(x)P_w(y|x)(\log P_w(y|x) - \sum_{i=1}^n w_i f_i(x,y)) \<br>
&amp;= \sum_{x,y} \tilde{P}(x,y) \sum_{i=1}^n w_i f_i(x,y) - \sum_{x,y} \tilde{P}(x)P_w(y|x) \log Z_w(x) \<br>
&amp;= \sum_{x,y} \tilde{P}(x,y) \sum_{i=1}^n w_i f_i(x,y) - \sum_x \tilde{P}(x) \log Z_w(x)<br>
\end{aligned}<br>
$$<br>
因此接下来便是求解对偶优化问题:<br>
$$<br>
\max \phi(w)<br>
$$<br>
得到$w^\ast$,然后回代$P_w(y|x)$,得到最大熵模型表达式:<br>
$$<br>
P_{w^\ast}(y|x)<br>
$$</p>
<h4 id="极大似然估计">极大似然估计</h4>
<p>这一部分旨在证明一条性质:</p>
<blockquote>
<p>最大熵模型求解过程中，对偶函数的极大化等价于最大熵模型的极大似然估计</p>
</blockquote>
<p>首先写出对数似然函数的形式:<br>
$$<br>
\begin{aligned}<br>
L_{\tilde{P}}(P_w) &amp;= \log \prod_{x,y} P(y|x)^{\tilde{P}(x,y)} = \sum_{x,y} \tilde{P}(x,y) \log P(y|x) \<br>
&amp;= \sum_{x,y} \tilde{P}(x,y) \sum_{i=1}^n w_i f_i(x,)  - \sum_{x,y} \tilde{P}(x,y) \log Z_w(x) \<br>
&amp;= \sum_{x,y} \tilde{P}(x,y) \sum_{i=1}^n w_i f_i(x,y)  - \sum_{x} \tilde{P}(x) \log Z_w(x)<br>
\end{aligned}<br>
$$<br>
与对偶函数$\phi(w)$一致，这说明对对偶函数求最大等价于求最大熵模型的极大似然估计。<br>
最大熵模型与logistic回归模型有类似的形式，它们又称为对数线性模型。模型学习就是对模型进行极大似然估计或者正则化的极大似然估计</p>
]]></content>
      <categories>
        <category>统计学习</category>
      </categories>
      <tags>
        <tag>logistic回归, 最大熵模型, 优化学习</tag>
      </tags>
  </entry>
  <entry>
    <title>SVM</title>
    <url>/2020/08/12/svm/</url>
    <content><![CDATA[<p>这部分将介绍支持向量机(SVM)算法，该部分将按照以下几部分进行组织：</p>
<ul>
<li>算法历史</li>
<li>线性可分支持向量机与硬间隔最大化</li>
<li>线性支持向量机与软间隔最大化</li>
<li>非线性支持向量机与核函数</li>
<li>SVDD</li>
</ul>
<a id="more"></a>
<h3 id="算法历史">算法历史</h3>
<p>支持向量机算法是在机器学习领域非常著名的算法，在神经网络算法得到普及之前，支持向量机算法可以说是统治了机器学习领域的半壁江山，该算法由Vapnik在贝尔实验室开发，根据Vapnik和Chervonekis提出的统计学习框架或VC理论，它提出了一种最可靠的预测方法（ 1974年）和瓦普尼克（Vapnik）（1982年，1995年）。给定一组训练示例，每个训练示例都标记为属于两个类别中的一个或另一个，则SVM训练算法会构建一个模型，该模型将新示例分配给一个类别或另一个类别，使其成为非概率 二进制 线性分类器（尽管方法例如Platt缩放存在以在概率分类设置中使用SVM）。SVM模型是将示例表示为空间中的点，并进行了映射，以使各个类别的示例被尽可能宽的明显间隙分开。然后，将新示例映射到相同的空间，并根据它们落入的间隙的侧面来预测属于一个类别。<br>
除执行线性分类外，SVM还可以使用所谓的内核技巧有效地执行非线性分类，将其输入隐式映射到高维特征空间。</p>
<h3 id="线性可分支持向量机与硬间隔最大化">线性可分支持向量机与硬间隔最大化</h3>
<p>对于线性可分数据，上一部分介绍的感知机算法便可以处理，但需要注意的是，感知机算法学习得到的超平面可能有无穷多个，那么这些超平面中哪个是最优的呢？支持向量机算法便回答了这一问题，该算法认为使两类样本点间隔最大的超平面是最优超平面。首先给出线性可分支持向量机的定义：</p>
<blockquote>
<p><strong>线性可分支持向量机</strong>:给定线性可分训练数据集，通过间隔最大化或者等价地求解相应的凸二次规划问题学习得到的分离超平面为：<br>
$$<br>
\omega^* \cdot x + b^* = 0<br>
$$<br>
以及相应的分类决策函数:<br>
$$ f(x) = sign(\omega^* \cdot x + b^*)$$</p>
</blockquote>
<p>下面就结合下图来对支持向量机的算法思想进行阐述,图中红点与蓝点分别代表两个类别，我们现在期望找到一个超平面，使得两个类别的样本点都能够尽可能离该超平面距离远，也就是希望中间的margin尽可能大。<br>
<img src="https://raw.githubusercontent.com/xuejy19/Images/master/SVM.png" alt="SVM"><br>
这便是支持向量机的思想，下一步需要做的便是将这种想法翻译成一个数学问题，在这里首先引入两个间隔的概念：</p>
<blockquote>
<ul>
<li>几何间隔：所谓几何间隔就是样本点$(x_i,y_i)$到超平面的实际距离，其计算公式为(此时样本点已经被超平面正确分类)：<br>
$$ d = y_i \frac{\omega x_i + b}{||\omega||}$$</li>
<li>函数间隔：不考虑几何间隔中$||\omega||$便得到了函数间隔：<br>
$$ d = y_i(\omega x_i +b)$$</li>
</ul>
</blockquote>
<p>需要注意的是，函数间隔可以表示分类预测的正确性及确信度，在感知机学习中，我们便是使用函数间隔，这是由于数据线性可分，最终目标函数都会收敛到0，使用几何间隔与函数间隔最终效果并无不同(两种间隔符号一致)。但在支持向量机算法中，我们希望间隔最大，这时显然函数间隔是不够的，因为只要成比例的增大$\omega,b$，超平面并没有改变，但是函数间隔却便为两倍，因此在该问题中，我们应当使用几何间隔,至此支持向量机的思想可以写成如下优化问题的形式：<br>
$$<br>
\begin{aligned}<br>
&amp;max_{\omega,b} \quad \gamma  \<br>
&amp;s.t. \quad y_i \frac{\omega x_i +b}{||\omega||} \geq \gamma,\quad i = 1,2,\dots,N<br>
\end{aligned}<br>
$$<br>
上面是优化问题的原始形式，下面尝试对该优化问题进行等价转换以便于求解，首先根据几何间隔与函数间隔的关系，将优化问题转化为：<br>
$$<br>
\begin{aligned}<br>
&amp;max_{\omega,b} \quad \frac{\hat{\gamma}}{||\omega||}  \<br>
&amp;s.t. \quad y_i (\omega x_i +b) \geq \hat{\gamma},\quad i = 1,2,\dots,N<br>
\end{aligned}<br>
$$<br>
此时可以注意到函数间隔$\hat{\gamma}$并不影响该优化问题求解，因此考虑将$\hat{\gamma}$置为1，注意到最大化$\frac{1}{||\omega||}$等价于最小化$\frac{1}{2}||\omega||^2$,因此优化问题最终可以表述为:<br>
$$<br>
\begin{aligned}<br>
&amp;min_{\omega,b} \quad \frac{1}{2} ||\omega||^2 \\<br>
&amp;s.t. \quad y_i (\omega x_i +b) \geq 1,\quad i = 1,2,\dots, N<br>
\end{aligned}<br>
$$<br>
这是一个典型的凸二次规划问题，运用凸优化知识可进行求解。</p>
<p>下面给出一个定理来说明最大间隔分离超平面唯一的定理：</p>
<blockquote>
<p>定理：若训练数据集$T$线性可分，则可将训练数据集中的样本点完全分开的最大间隔分离超平面存在且唯一</p>
</blockquote>
<h4 id="支持向量与间隔边界">支持向量与间隔边界</h4>
<p>首先给出支持向量的定义:</p>
<blockquote>
<p>在线性可分情况下，训练数据集中样本点中与分离超平面距离最近的样本点的实例称为支持向量。</p>
</blockquote>
<p>反映在优化问题中，支持向量满足约束条件的等号,支持向量在超平面:<br>
$$<br>
\begin{aligned}<br>
H_1&amp;: \omega \cdot x +b =1  \<br>
H_2&amp;: \omega \cdot x +b = -1<br>
\end{aligned}<br>
$$<br>
两个支撑超平面之间的距离称为间隔，该间隔距离为：<br>
$$<br>
d = \frac{2}{||\omega||}<br>
$$<br>
在决定分离超平面时只有支持向量起作用，而其他实例点并不起作用。</p>
<h4 id="学习的对偶算法">学习的对偶算法</h4>
<p>在进行求解时，我们往往考虑不直接求解原问题，而是求解原问题的对偶问题, 又因为原问题是一个凸优化问题，同时满足Slater约束品性，因此考虑求解其对偶问题。首先写出Lagrange函数：<br>
$$<br>
L(\omega,b,\alpha) = \frac{1}{2}||\omega^2|| - \sum_{i=1}^N \alpha_i y_i (\omega \cdot x_i +b) + \sum_{i=1}^N \alpha_i<br>
$$<br>
接下来求解对偶函数，对偶函数$g(\alpha)$表示为：<br>
$$<br>
g(\alpha) = \min_{\omega,b} L(\omega,b,\alpha)<br>
$$<br>
因为$L(\omega,b,\alpha)$是凸函数，因此只需要对$\omega,b$求偏导，令偏导等于0即可：<br>
$$<br>
\begin{aligned}<br>
\frac{\partial L(\omega,b,\alpha)}{\partial \omega} &amp;= \omega - \sum_{i=1}^N \alpha_i y_i x_i = 0 \rightarrow \omega =  \sum_{i=1}^N \alpha_i y_i x_i  \\<br>
\frac{\partial L(\omega,b,\alpha)}{\partial b} &amp;= -\sum_{i=1}^N \alpha_i y_i = 0 \rightarrow \sum_{i=1}^N \alpha_i y_i = 0<br>
\end{aligned}<br>
$$<br>
回代拉格朗日函数，可得对偶函数：<br>
$$<br>
\begin{aligned}<br>
g(\alpha) &amp;= \frac{1}{2} \sum_{i=1}^N \sum_{j=1}^N \alpha_i \alpha_j y_i y_i x_i^T x_j - \sum_{i=1}^N \sum_{j=1}^N \alpha_i \alpha_j y_i y_i x_i^T x_j + \sum_{i=1}^N \alpha_i  \\<br>
&amp;= -\frac{1}{2} \sum_{i=1}^N \sum_{j=1}^N \alpha_i \alpha_j y_i y_i x_i^T x_j + \sum_{i=1}^N \alpha_i<br>
\end{aligned}<br>
$$<br>
所以，对偶优化问题可以写做：<br>
$$<br>
\begin{aligned}<br>
&amp;\max_{\alpha} \quad -\frac{1}{2} \sum_{i=1}^N \sum_{j=1}^N \alpha_i \alpha_j y_i y_i x_i^T x_j + \sum_{i=1}^N \alpha_i \\<br>
&amp;s.t. \quad \sum_{i=1}^N \alpha_i y_i =0;\alpha_i \geq 0,i=1,\dots,N<br>
\end{aligned}<br>
$$<br>
对于该问题的求解可以通过SMO(序列最小优化)算法进行求解，因为强对偶性成立,KKT条件满足：<br>
$$<br>
\begin{aligned}<br>
&amp;\nabla_{\omega} L(\omega^<em>,b^</em>,\alpha^<em>) = \omega^</em> - \sum_{i=1}^N \alpha_i^* y_i x_i = 0 \<br>
&amp;\nabla_{b} L(\omega^<em>,b^</em>,\alpha^<em>) = -\sum_{i=1}^N \alpha_i^</em> y_i = 0 \<br>
&amp;\alpha_i^* (y_i(\omega^<em>x_i+b^</em>)-1) = 0 \<br>
&amp;y_i(\omega^* x_i +b^<em>) - 1 \geq 0 \<br>
&amp;\alpha_i^</em> \geq 0<br>
\end{aligned}<br>
$$<br>
由此便可建立对偶问题最优解$\alpha^<em>$与原问题最优解$\omega^</em>,b^<em>$之间的关系，首先至少有一个$\alpha_j^</em>&gt;0$,这是因为若$\alpha^<em>$均为0，则$\omega^</em>$为0,这不是原始最优化问题的解，假设$\alpha_j^<em>&gt;0$,则由互补松弛条件，此时有$y_j(\omega^</em> x_j +b^<em>) = 1$,而$\omega^</em>$表达式是知道的，由此可得$\omega^<em>,b^</em>$表达式为：<br>
$$<br>
\begin{aligned}<br>
\omega^* &amp;= \sum_{i=1}^N \alpha_i^* y_i x_i \<br>
b^* &amp;=  y_j - \sum_{i=1}^N \alpha_i^* y_i(x_i\cdot x_j)<br>
\end{aligned}<br>
$$<br>
至此，我们便通过求解对偶问题得到了原始问题的最优解，得到了超平面方程以及判决方程：<br>
$$<br>
\begin{aligned}<br>
&amp;\omega^* \cdot x + b^* = 0 \<br>
&amp;f(x) = sign(\omega^* \cdot x + b^*)<br>
\end{aligned}<br>
$$<br>
通过引入对偶问题，我们便可以从数学的角度导出支持向量:</p>
<blockquote>
<p>训练数据集中对应于$\alpha_i^* &gt;0$的样本点称为支持向量</p>
</blockquote>
<h3 id="线性支持向量机与软间隔最大化">线性支持向量机与软间隔最大化</h3>
<h4 id="原始问题">原始问题</h4>
<p>前面介绍的算法是针对线性可分数据,那么对于大部分线性可分的数据集，支持向量机能否处理呢？答案是可以，但是需要对原始优化问题做适当变换,对于线性可分的数据，我们要求各个样本点到分类超平面的函数间隔都要大于1，对于不可分数据点，我们直观上的想法是考虑能否将该约束放松，因此我们考虑为每一个样本点引入一个松弛变量$\xi_i$,使得函数间隔加上松弛变量等于1。这样，约束条件变为:<br>
$$<br>
y_i(\omega \cdot x_i +b) \geq 1 - \xi_i<br>
$$<br>
但需要注意的人，该松弛变量的引入是有代价的，需要在目标函数中进行惩罚，因此考虑在目标函数中加入惩罚项$\xi_i$，将目标函数改写为：<br>
$$<br>
\frac{1}{2} ||\omega||^2  + C\sum_{i=1}^N \xi_i<br>
$$<br>
其中$C$是惩罚系数，$C$值大时对误分类惩罚增大。最小化该目标函数包含两层含义：使间隔尽量大；使误分类点尽量少。线性不可分支持向量机的学习问题变为如下凸二次规划问题：<br>
$$<br>
\begin{aligned}<br>
&amp;\min_{\omega,b,\xi} \quad \frac{1}{2} ||\omega||^2 + C \sum_{i=1}^N \xi_i \\<br>
&amp;s.t. \quad y_i(\omega x_i +b) \geq 1- \xi_i,i=1,2,\dots,N \\<br>
&amp;\qquad \xi_i \geq 0,i=1,2,\dots,N<br>
\end{aligned}<br>
$$<br>
对于该原始问题，可以证明$\omega$的解是唯一的，但$b$的解可能不唯一，而是存在一个区间。</p>
<h4 id="对偶问题">对偶问题</h4>
<p>在该部分便不进行对偶问题推导了，步骤与上一部分一致，最终得到的对偶问题形式为：<br>
$$<br>
\begin{aligned}<br>
&amp;\max_{\alpha} \quad -\frac{1}{2} \sum_{i=1}^N \sum_{j=1}^N \alpha_i \alpha_j y_i y_i x_i^T x_j + \sum_{i=1}^N \alpha_i \\<br>
&amp;s.t. \quad \sum_{i=1}^N \alpha_i y_i =0,i=1,\dots,N  \\<br>
&amp; \qquad 0 \leq \alpha_i \leq C \quad i=1,2,\dots,N<br>
\end{aligned}<br>
$$<br>
可以看到引入松弛变量后对偶问题形式基本未发生变化，仅仅约束条件中$\alpha_i$范围变了。需要注意的是将原始问题转化为对偶问题的过程中，引入了两个Lagrange算子$\alpha_i,\mu_i$，但由于存在$<br>
\alpha_i + \mu_i = C $以及$\alpha_i \geq 0, \mu_i \geq 0 $,因此将这些约束条件简化为$0 \leq \alpha_i \leq C$。<br>
下面讨论如何根据对偶问题最优解求解原问题最优解，与硬间隔支持向量机不同，在该问题中，互补松弛条件有两个:<br>
$$<br>
\begin{aligned}<br>
&amp;\alpha_i^* (y_i(\omega^<em>x_i+b^</em>)-1 + \xi_i) = 0  \<br>
&amp;\mu_i \xi_i = 0<br>
\end{aligned}<br>
$$<br>
在求解$b^<em>$时，关键是要将支持向量确定出来，首先支持向量应当在支撑超平面上，这就要求$\alpha_i&gt;0$,同时松弛间隔$\xi_i$应当为0，由第二个互补松弛条件可知$\mu_i$不能够为0，因此$\alpha_i$必须小于$C$,换句话说，$\alpha^</em> = C$所对应的点均在支撑超平面以内，若$\xi_i&lt;1$,则仍能分类正确，若大于1，则就会分类错误。$\omega^<em>$和$b^</em>$表达式可以写做：<br>
$$<br>
\begin{aligned}<br>
\omega^* &amp;= \sum_{i=1}^N \alpha_i^* y_i x_i \<br>
b^* &amp;= y_j - \sum_{i=1}^N y_i \alpha_i^* (x_i \cdot x_j)<br>
\end{aligned}<br>
$$</p>
<h4 id="非线性支持向量机与核函数">非线性支持向量机与核函数</h4>
<p>支持向量机是解决线性分类问题的一种有效方法，但并不能解决非线性分类问题，那么非线性的数据就不能用线性的方法解决么？ 直观上我们有这样一个方法，考虑降维度线性不可分的数据映射到高维度，使映射后的数据在高维度线性可分，由此便引出了本节的核函数。</p>
<h4 id="核函数">核函数</h4>
<p>首先先举一个空间变换的栗子，设原空间为$\mathcal{X} \subset R^2,x = (x^1,x^2)^T$,新空间为$\mathcal{Z} \subset R^2,z = (z^1,z^2)^T$,定义从原空间到新空间的映射：<br>
$$<br>
z = \phi(x) = ((x^1)^2,(x^2)^2)^T<br>
$$<br>
经过这样一个变换原空间中的椭圆：<br>
$$<br>
w_1 (x^1)^2 + w_2 (x^2)^2 +b = 0<br>
$$<br>
变换为了新空间中的直线：<br>
$$<br>
w_1 z^1 + w_2 z^2 + b = 0<br>
$$<br>
这样，原空间的非线性可分问题就变成了新空间的线性可分问题。从该问题出发，我们可以总结出用线性分类方法求解非线性分类问题的步骤：</p>
<ul>
<li>首先使用一个变换将原空间中的数据映射到新空间</li>
<li>在新空间中使用线性分类学习方法从训练数据中学习分类模型</li>
</ul>
<p>而在支持向量机中，达到这一目的的方法应用核技巧，下面首先给出核函数的定义：</p>
<blockquote>
<p>核函数：设$\mathcal{X}$是输入空间，又设$\mathcal{H}$为特征空间(希尔伯特空间),如果存在一个从$\mathcal{X}$到$\mathcal{H}$的映射：<br>
$$<br>
\phi(x):\mathcal{X} \rightarrow \mathcal{H}<br>
$$<br>
使得对所有$x,z \in \mathcal{X}$,函数$K(x,z)$满足条件:<br>
$$<br>
K(x,z) = \phi(x) \cdot \phi(z)<br>
$$<br>
则称$K(x,z)$为核函数，$\phi(x)$为映射函数</p>
</blockquote>
<p>核技巧的想法是，在学习与预测中只定义核函数$K(x,z)$,而不显式的定义映射函数$\phi$。同时，对于一个核$K(x,z)$,特征空间$\mathcal{H}$和映射函数$\phi$的取法并不唯一</p>
<h4 id="非线性支持向量机">非线性支持向量机</h4>
<p>在线性支持向量机的对偶问题中,无论是目标函数还是决策函数中均只包含实例的内积的形式，因此若用$K(x,y)$代替$x\cdot y$,实际上便是在对高维空间中的特征点设计线性分类器。那现在的关键问题便在于如何选择合适的核函数。</p>
<p>关于核函数这部分的理论我后面会另开一个章节进行介绍学习，在这一部分便直接给出一些常用的核函数：</p>
<ul>
<li>多项式核函数:<br>
$$ K(x,z) = (x\cdot z +1)^p$$</li>
<li>高斯核函数(径向基函数)<br>
$$ K(x,z) = exp(- \frac{||x-z||^2}{2\sigma^2})$$</li>
</ul>
<p>需要注意的是，在引入核函数后，又多引入了超参数，比如高斯核函数中的$\sigma$,而这些超参数的选择也会对分类效果产生影响。</p>
<h3 id="一类分类器设计">一类分类器设计</h3>
<p>在前面介绍的支持向量机算法中，针对的都是二分类情况，但当我们做故障检测时，往往就只有大量的正常样本点，现在我们需要根据这些正常样本点来设计一个分类器以对未出现的异常样本点进行检测。在这一部分介绍两种算法：</p>
<ul>
<li>OCSVM</li>
<li>SVDD</li>
</ul>
<h4 id="OCSVM">OCSVM</h4>
<p>在OCSVM中，将坐标原点作为唯一的异常点,最大化最优超平面到远点的距离，若记$\rho$为从原点到超平面的距离，$\xi_i$为松弛变量，则优化问题可以写做：<br>
$$<br>
\begin{aligned}<br>
&amp;\max_{\omega,\rho,\xi_i} \rho - C\sum_{i=1}^N \xi_i \<br>
&amp; s.t. \quad \omega \cdot x_i \geq \rho - \xi_i,\xi_i \geq 0,||\omega|| = 1<br>
\end{aligned}<br>
$$</p>
<h4 id="SVDD">SVDD</h4>
<p>SVDD的基本思想则是，对于$n$维空间中的数据点集，寻找最小超球面，使其成为所有点的边界，其对应优化问题的原问题为:<br>
$$<br>
\begin{aligned}<br>
&amp;\min_{a,R,\xi} \quad R^2 + C\sum_{i=1}^N \xi_i \<br>
&amp;s.t. \quad ||x_i -a||^2 \leq R^2 + \xi_i \<br>
&amp;\qquad \quad \xi_i \geq 0<br>
\end{aligned}<br>
$$</p>
]]></content>
      <categories>
        <category>统计学习</category>
      </categories>
      <tags>
        <tag>支持向量机</tag>
      </tags>
  </entry>
  <entry>
    <title>决策树模型</title>
    <url>/2020/08/30/jue-ce-shu-mo-xing/</url>
    <content><![CDATA[<p>这一部分介绍下决策树算法，决策树是一种基本的分类与回归表示方法，决策树模型呈树形结构，在分类问题中，可以表示为基于特征对实例进行分类的过程。决策树可以认为是&quot;if-then&quot;规则的集合，也可以认为是定义在特征空间与类空间上的概率分分布。本文按照以下四部分进行组织:</p>
<ul>
<li>决策树模型与学习</li>
<li>特征选择</li>
<li>决策树生成算法</li>
<li>决策树剪枝算法</li>
</ul>
<a id="more"></a>
<h3 id="决策树模型与学习">决策树模型与学习</h3>
<h4 id="决策树模型定义">决策树模型定义</h4>
<p>首先给出决策树的定义:</p>
<blockquote>
<p><strong>决策树：</strong> 分类决策树模型是一种描述对实例进行分类的树形结构。决策树由节点和有向边组成，节点由两种类型：内部节点和叶节点。内部节点表示一个特征或属性，叶节点表示一个类。</p>
</blockquote>
<p>一个决策树模型如下图所示：<br>
<img src="https://raw.githubusercontent.com/xuejy19/xuejy19.github.io/source/Img/Decision_tree.png" alt="决策树"><br>
决策树的逻辑其实与人思考的逻辑非常贴合,都是通过一连串的&quot;if-then&quot;语句来进行判断，这也是决策树算法具有较好可解释性的原因。</p>
<h4 id="决策树与-if-then-规则">决策树与&quot;if-then&quot;规则</h4>
<p>一个决策树可以转换成一组&quot;if-then&quot;规则,将决策树转换成&quot;if-then&quot;规则集合的步骤如下:由决策树的根节点到叶节点的每一条路径构建一条规则：路径上内部节点的特征对应着规则的条件，而叶节点的类则对应着规则的结论。<br>
决策树对应的&quot;if-then&quot;规则集合具有一个重要的性质：互斥并且完备，即每一个实例都被一条路径或一条规则所覆盖，而且只被一条规则或一条规则所覆盖。</p>
<h4 id="决策树与条件概率分布">决策树与条件概率分布</h4>
<p>决策树还表示给定特征条件下类的条件概率分布。这一条件概率分布定义在特征空间的一个划分上。将特征空间划分为互不相交的单元或区域，并在每个单元定义一个类的概率分布就构成了一个条件概率分布。决策树的一条路径对应于划分中的一个单元，决策树所表示的条件概率分布由各个单元给定条件下类的条件概率分布组成。<br>
假设$X$为表示特征的随机变量，$Y$为表示类别的随机变量，那么这个条件概率分布可以表示为$P(Y|X)$,$X$取值于给定划分下单元的集合，$Y$取值于类的集合。</p>
<h4 id="决策树学习">决策树学习</h4>
<p>假设给定训练数据集:<br>
$$<br>
D = { (x_1,y_1),(x_2,y_2), \dots, (x_N,y_N)}<br>
$$<br>
其中$x_i \in R^n$为特征向量,$n$为特征个数，$y_i \in {1,2, \dots, K}$为类标记，$N$为样本容量。<br>
<strong>决策树学习的目标是根据给定的训练数据集构建一个决策树模型，使它能够对实例进行正确的分类。</strong></p>
<p>决策树学习本质上是从训练数据集中归纳出一组分类规则，这点与前面章节介绍过的规则学习非常类似，不过决策树所针对的问题更加具体。与训练数据集不相矛盾的决策树可能有多个，也可能一个也没有。我们需要的是一个与训练数据矛盾较小的决策树，同时具有很好的泛化能力。从条件概率分布的角度来看，决策树学习是由训练数据集估计条件概率模型，基于特征空间划分的类的条件概率模型有无穷多个，我们选择的条件概率模型应该不仅对训练数据有很好的拟合，同时也应当对未知的数据有较好的预测。<br>
从所有可能的决策树中选取最优决策树是NP完全问题，所以现实中决策树学习算法通常采用启发式方法，近似求解这一最优问题，这样得到的决策树是次最优的。<br>
决策树学习算法通常是一个递归地选取最优特征，并根据该特征对训练数据进行分割，使得各数据集有一个最好的分类的过程。这一过程对应着特征空间的划分，也对应着决策树的构建。构建流程如下:</p>
<ul>
<li>构建根节点，将所有训练数据都放在跟节点</li>
<li>选择一个最优特征，按照这一特征将训练数据集分割成子集，使得各个子集有一个在当前条件下最好的分类。</li>
<li>判断子集中的实例是否都被正确分类，若都已经被正确分类，则构建叶节点</li>
<li>若子集不能被正确分类，则返回第二步，选取最优特征并划分子集。</li>
</ul>
<p>通过上述流程所构建的决策树往往对训练数据有着良好的分类能力，但对未知的测试数据却未必有很好的分类能力，即可能发生过拟合现象，因此往往需要的生成的决策树自下而上进行剪枝。</p>
<h3 id="特征选择">特征选择</h3>
<p>特征选择的目的在于选取具有分类能力的特征，决策树的每一次分叉都是进行特征选择的过程, 所以有一个亟需回答的问题就是：</p>
<blockquote>
<p><strong>应当以什么样的标准来评判一个特征的分类能力？</strong></p>
</blockquote>
<p>通常评价特征分类能力的指标是信息增益和信息增益比，在前面章节中我已经给出了熵和条件熵的定义：</p>
<blockquote>
<p><strong>熵：</strong> 在信息论与概率统计中，熵是表示<strong>随机变量不确定性</strong>的度量，设$X$是一个取有限个值的离散随机变量，其概率分布为:<br>
$$<br>
P(X = x_i) = p_i, \quad i =1,2,\dots,n<br>
$$<br>
则随机变量$X$的熵定义为:<br>
$$<br>
H(X) = - \sum_{i=1}^n p_i \log p_i<br>
$$</p>
</blockquote>
<blockquote>
<p><strong>条件熵：</strong> 设有随机变量$(X,Y)$,其联合概率分布为：<br>
$$<br>
P(X = x_i,Y = y_i) = p_{ij}\quad i = 1,2,\dots,n;\quad j = 1,2,\dots,m<br>
$$<br>
条件熵$H(Y|X)$表示在已知随机变量$X$条件下随机变量$Y$的不确定性。随机变量$X$给定的条件下随机变量$Y$的条件熵$H(Y|X)$,定义为$X$给定条件下$Y$的条件概率分布的熵对$X$的数学期望：<br>
$$<br>
H(Y|X) = \sum_{i=1}^n p_i H(Y|X = x_i)<br>
$$<br>
当熵和条件熵中的概率由数据估计得到时，所对应的熵与条件熵分别称为经验熵和经验条件熵</p>
</blockquote>
<h4 id="信息增益">信息增益</h4>
<p>下面给出信息增益的定义：</p>
<blockquote>
<p><strong>信息增益：</strong> 特征$A$对训练集$D$的信息增益$g(D,A)$,定义为集合$D$的经验熵$H(D)$与特征$A$给定条件下$D$的经验条件熵$H(D|A)$之差，即：<br>
$$<br>
g(D,A) = H(D) - H(D|A)<br>
$$</p>
</blockquote>
<p>给定数据集$D$和特征$A$,经验熵$H(D)$表示对数据集$D$进行分类的不确定性，而经验条件熵$H(D|A)$则表示在特征$A$给定条件下对数据集$D$进行分类的不确定性。而它们之间的差则表示由于特征$A$而使得对数据集$D$的分类的不确定性减少的程度。由此便可得通过信息增益来选择特征的算法如下:</p>
<blockquote>
<p><strong>信息增益算法</strong><br>
<strong>输入：</strong> 训练数据集$D$及特征$A$<br>
<strong>输出：</strong> 特征$A$对训练数据集$D$的信息增益$g(D,A)$</p>
<ul>
<li>Step1: 计算数据集$D$的经验熵：<br>
$$<br>
H(D) = -\sum_{k=1}^K \frac{|C_k|}{|D|} \log_2 \frac{|C_k|}{|D|}<br>
$$</li>
<li>Step2: 计算特征$A$对数据集$D$的经验条件熵$H(D|A)$<br>
$$<br>
H(D|A) = \sum_{i=1}^n \frac{|D_i|}{|D|} H(D_i) = -\sum_{i=1}^n \frac{|D_i|}{|D|} \sum_{k=1}^K \frac{|D_{ik}|}{|D_i|} \log_2 \frac{|D_{ik}|}{|D_i|}<br>
$$</li>
<li>Step3: 计算信息增益<br>
$$<br>
g(D,A) = H(D) - H(D|A)<br>
$$</li>
</ul>
</blockquote>
<h4 id="信息增益比">信息增益比</h4>
<p>以信息增益作为划分训练数据集的特征，存在偏向于选择取值较多的特征的问题。使用信息增益比则可以对这一问题进行校正，下面给出信息增益比的定义:</p>
<blockquote>
<p><strong>信息增益比：</strong> 特征$A$对训练数据集$D$的信息增益比$g_R(D,A)$定义为其信息增益$g(D,A)$与训练数据集$D$关于特征$A$的值的熵$H_A(D)$之比，即：<br>
$$<br>
g_R(D,A) = \frac{g(D,A)}{H_A(D)}<br>
$$<br>
其中$H_A(D) = -\sum_{i=1}^n \frac{|D_i|}{|D|} \log_2 \frac{|D_i|}{|D|}$,$n$是特征$A$取值的个数</p>
</blockquote>
<h4 id="基尼指数-基尼不纯度">基尼指数(基尼不纯度)</h4>
<p>首先给出基尼指数的定义:</p>
<blockquote>
<p><strong>基尼指数：</strong> 分类问题中，假设有$K$个类，样本属于第$k$类的概率为$p_k$，则概率分布的基尼指数定义为:<br>
$$<br>
Gini§ = \sum_{k=1}^K p_k(1 - p_k) = 1 - \sum_{k=1}^K p_k^2<br>
$$<br>
对于二分类问题，若样本点属于第一个类的概率是$p$,则概率分布的基尼指数为:<br>
$$<br>
Gini§ = 2p(1-p)<br>
$$<br>
对于给定的样本集合$D$，其基尼指数为:<br>
$$<br>
Gini(D) = 1 - \sum_{k=1}^K (\frac{|D_k|}{|D|})^2<br>
$$<br>
在特征$a_i$下将特征空间划分成$n$块，各块的样本数为$D_i$,条件Gini指数可以写做:<br>
$$<br>
Gini(D|a_i) = \sum_{i=1}^n \frac{|D_i|}{|D|} Gini(D_i)<br>
$$<br>
引入特征$a_i$后，集合$D$不确定性降低了:<br>
$$<br>
Gini(D) - Gini(D|a_i)<br>
$$</p>
</blockquote>
<h3 id="决策树的生成">决策树的生成</h3>
<p>决策树生成流程在前面已经介绍过，不同算法的区别主要在于特征选择标准上的差别，该部分主要介绍两种决策树生成算法：</p>
<ul>
<li>ID3算法</li>
<li>C4.5算法</li>
</ul>
<h4 id="ID3算法">ID3算法</h4>
<p>ID3算法相当于用极大似然法进行概率模型的选择,下面给出ID3算法的流程:</p>
<blockquote>
<p><strong>ID3算法</strong><br>
<strong>输入：</strong> 训练数据集$D$, 特征集$A$阈值$\epsilon$<br>
<strong>输出：</strong> 决策树$T$</p>
<ul>
<li>若$D$中所有实例属于同一类$C_k$,则$T$为单节点树，并将$C_k$作为该节点的类标记，返回$T$</li>
<li>若$A = \varnothing$,则$T$为单节点树，并将$D$中实例数最大的类$C_k$作为该节点的类标记，返回$T$</li>
<li>否则，计算$A$中各特征对$D$的信息增益，选择信息最大的特征$A_g$</li>
<li>如果$A_g$的信息增益小于阈值$\epsilon$，则置$T$为单节点树，并将$D$中最大实例数类$C_k$作为类别，返回$T-</li>
<li>否则，按照$A_g$的每一特征值将特征空间划分成若干子集，将$D_i$中实例最大的类作为标记，构建子节点，由节点及其子节点构成树$T$，返回$T$</li>
<li>对第$i$个子节点，以$D_i$为训练集，以$A - {A_g}$为特征集，递归地调用前述步骤，得到子树$T_i$,返回子树</li>
</ul>
</blockquote>
<h4 id="C4-5算法">C4.5算法</h4>
<p>C4.5算法选用信息增益比作为特征选择标准，其流程如下：</p>
<blockquote>
<p><strong>C4.5算法</strong><br>
<strong>输入：</strong> 训练数据集$D$, 特征集$A$阈值$\epsilon$<br>
<strong>输出：</strong> 决策树$T$</p>
<ul>
<li>若$D$中所有实例属于同一类$C_k$,则$T$为单节点树，并将$C_k$作为该节点的类标记，返回$T$</li>
<li>若$A = \varnothing$,则$T$为单节点树，并将$D$中实例数最大的类$C_k$作为该节点的类标记，返回$T$</li>
<li>否则，计算$A$中各特征对$D$的信息增益比，选择信息最大的特征$A_g$</li>
<li>如果$A_g$的信息增益小于阈值$\epsilon$，则置$T$为单节点树，并将$D$中最大实例数类$C_k$作为类别，返回$T-</li>
<li>否则，按照$A_g$的每一特征值将特征空间划分成若干子集，将$D_i$中实例最大的类作为标记，构建子节点，由节点及其子节点构成树$T$，返回$T$</li>
<li>对第$i$个子节点，以$D_i$为训练集，以$A - {A_g}$为特征集，递归地调用前述步骤，得到子树$T_i$,返回子树</li>
</ul>
</blockquote>
<h3 id="决策树的剪枝">决策树的剪枝</h3>
<p>决策树算法非常容易产生过拟合问题，过拟合的原因在于决策树在学习时过多地考虑了如何提高对训练数据分类准确率，从而构建除了过于复杂的决策树，而剪枝就是为了减轻决策树过拟合的一种操作。剪枝操作按照进行的时间段可以分为前剪枝与后剪枝两种：</p>
<ul>
<li>预剪枝：预剪枝操作是在决策树的构建过程中, 即通过对信息增益/信息增益比/Gini指数设定阈值，当分裂前后相应标准小于该阈值时，决策树便不进行分裂操作，又或者是先验地限制决策树的深度或者节点个数。预剪枝得到的决策树尽管更加简单，泛化能力得到了提升，但这样的剪枝操作是盲目的、缺乏指导的，实际应用中很容易导致决策树训练不充分的问题。</li>
<li>后剪枝：后剪枝顾名思义，是在一棵完整的决策树构建完成之后进行剪枝，常用的后剪枝操作有两种:
<ul>
<li>基于带正则项的损失函数进行剪枝</li>
<li>基于验证集错误率进行后剪枝</li>
</ul>
</li>
</ul>
<p>预剪枝的思想非常简单，这里便不做过多介绍，下面以带正则化的损失函数为例介绍下后剪枝的流程</p>
<h4 id="基于正则项的损失函数">基于正则项的损失函数</h4>
<p>首先考虑下对于一棵决策树，其似然函数应当怎么写，假设一棵决策树有$|T|$个叶节点，$t$是树$T$的叶节点，该叶节点上有$N_t$个样本点，其中$k$类的样本点有$N_{tk}$个，$k=1,2,\dots,K$。可以这么考虑，这$|T|$个叶节点将整个特征空间分成了$|T|$部分，其中$t$节点上一个属于$K$类的样本出现的概率(最大似然估计得到)为$\frac{N_{tk}}{N_t}$,而又假设样本之间是彼此独立的，该节点上有$N_{tk}$个该类样本，据此可以写出节点$t$上样本出现的概率:<br>
$$<br>
\prod_{k=1}^K (\frac{N_{tk}}{N_t})^{N_{tk}}<br>
$$<br>
而一共有$|T|$个叶节点，故整个树上的样本出现的概率(似然函数)为：<br>
$$<br>
\prod_{t=1}^{|T|} \prod_{k=1}^K (\frac{N_{tk}}{N_t})^{N_{tk}}<br>
$$<br>
至此便可以写出对数似然函数:<br>
$$<br>
L(T) = \sum_{t=1}^{|T|} \sum_{k=1}^K N_{tk} \log \frac{N_{tk}}{N_t}<br>
$$<br>
我们想要似然函数最大，其实也就是负的对数似然函数最小，由此可得损失函数$C(T)$:<br>
$$<br>
C(T) = - L(T) = -\sum_{t=1}^{|T|} \sum_{k=1}^K N_{tk} \log \frac{N_{tk}}{N_t}<br>
$$<br>
而我们不仅仅希望决策树$T$在训练集上表现良好，同时希望决策树具有良好的泛化性能，因此需要在上面损失函数的基础上添加正则项，正则项需要与决策树的复杂程度有关，这里选用叶节点个数$|T|$作为损失函数,由此可得正则化的损失函数如下:<br>
$$<br>
C_{\alpha}(T) = C(T) + \alpha |T|<br>
$$<br>
其中$\alpha$是权重因子，用来平衡模型复杂度与训练数据拟合效果。将叶节点上的经验熵代入损失函数，可以得到损失函数的另一种表达形式:<br>
$$<br>
C_\alpha(T) = -\sum_{i=1}^{|T|} N_t H_t(T) + \alpha_t<br>
$$<br>
其中$H_t(T)$为叶节点上的经验熵，至此我们便可以写出该后剪枝算法流程:</p>
<blockquote>
<p><strong>基于正则损失函数的后剪枝算法</strong><br>
<strong>输入：</strong> 生成算法产生的整个树$T$,$\alpha$<br>
<strong>输出：</strong> 修建后的子树$T_\alpha$</p>
<ul>
<li>计算当前树各个叶节点上的经验熵</li>
<li>递归地从树的叶节点向上回缩，设一组叶节点回缩到其父节点之前与之后的整体树分别为$T_B$与$T_A$,其对应的损失函数分别是$C_\alpha(T_B)$ 与 $C_\alpha(T_A)$,若：<br>
$$<br>
C_\alpha(T_A) \leq C_\alpha(T_B)<br>
$$<br>
则进行剪枝，将父节点变为新的叶节点</li>
<li>返回第2步，直到不能继续为止，得到损失函数最小的子树$T_\alpha$</li>
</ul>
</blockquote>
<h4 id="基于验证集分类错误率进行后剪枝">基于验证集分类错误率进行后剪枝</h4>
<p>在基于正则损失函数的后剪枝中，所有的数据集都被拿来训练，而在基于验证集分类错误率方法中，首先会将数据集分为训练集和验证集，该方法的大致流程如下:</p>
<ul>
<li>首先利用训练集得到一棵完全生长的决策树</li>
<li>然后测试该决策树在验证集上的错误率$E_1$，同时将某个叶节点向上回缩，即让某个父节点成为新的叶节点，验证新的树在验证集上的错误率$E_2$,若$E_2 \leq E_1$，则考虑将父节点成为新的叶节点</li>
<li>重复第二步，直到剪枝不能提升在验证集上的错误率</li>
</ul>
<p>这种剪枝思路的优点在于是有指导有方向的剪枝，理论上讲在验证集上决策树的性能获得提升，也能够在测试集上获得提升，缺点可能就是有可能会导致模型一定程度上对验证集的过拟合。</p>
]]></content>
      <categories>
        <category>统计学习</category>
      </categories>
      <tags>
        <tag>树模型,categorical变量</tag>
      </tags>
  </entry>
  <entry>
    <title>有感</title>
    <url>/2020/02/22/you-gan/</url>
    <content><![CDATA[<p> 	或许逃避是人的天性，整个博一上学期，似乎整天忙忙碌碌，在若干门课程中来回斡旋，<a id="more"></a>但当闲下来细细一想，似乎也有很多时间自己并没有好好利用，用那些时间在逃避、在咸鱼、在发呆。整个博一上学期自己也读了一些书，比如《历史的教训》、《贫穷的本质》、《禅与摩托车维修技术》、《变革中国》，相比于完整读下来的书，挖的坑自然是更多，《纯粹理性批判》、《世界文明史》、《资本论》…如果你要问我为什么选择这些书，我只能回答机缘巧合吧，这些书或是来源于上课时某位同学的推荐，或是来自于自己查询某些资料时的推荐，不一而足。其实我认为读这些书一方面是一种对自己空余时间的填充，对纯粹理工科学习的一种暂时逃避，也是一个不断寻找答案的过程，寻找一些理工科的书籍里缺乏的一些答案。其实我一直在思考一个问题，一个人的强大究竟是来源于其内心的强大，还是来源于其所凭外物，当你一无所有（指外物）之时，你还能剩下什么？是没有灵魂的一具空壳，还是一个拥有强大生命的种子。<br>
 	读史教会我的第一点是作为历史当中普通人的世事无常，我们所接触的历史往往是王侯将相的历史，是英雄名士的历史，若人类发展的角度来看历史，一个人的历史观总会趋于无情，这种无情我们可以理解为作为现代人的傲慢/优越，如今我们去各地旅游，惊叹于种种历史古迹，惊叹于古人的智慧，但是孰又会在意这些历史古迹下面是当时多少奴隶的血泪。这些遗迹显示了当时高超的建造技术，为后人留下了宝贵遗产，我们或可以说这是历史的进步，然而那些建造他们的奴隶们则消逝于历史的阴影之中。若我们接受这样的历史进步性说法，由此会自然引出下一个结论——历史的发展是必然伴随着牺牲的，作为现代人，我们可以一方面斥责当时的暴政，另一方面又可以欣赏到这些暴政所留下的历史遗迹，每每想到这些，总觉如鲠在喉。或许从这些历史中我们可以得出这样的经验，某些我们认为错误的做法确实促进了当时历史的发展，但是历史发展途径许多，我们应当选择让人民整体更加幸福的发展道路，唉，不过聊以自慰耳。<br>
 行路者需要背负东西，然而不能背负太多。我第一个放入背包的叫做责任，从诞生开始，我就开始与这个世界产生联系，父母、家庭、国家、人类，这些名词限定了我是我，梁启超在《最苦与最乐》中写道：“人生最苦，莫过于身上背负着一种未了的责任；人生最乐，莫过于尽责之时。“责任二字，说来轻巧，实则沉甸甸，也因责任二字，人生少了些凄清，多了些烟火；我第二个放入背包的叫做知识，在我看来，当曾经模糊看不清楚的东西在你眼前变得清晰之时，也是精神愉悦满足之时，文学史哲让你反求诸己，塑造清明的内心世界，理血工学让你更好地认识客观世界，改造客观世界，两种知识于我而言同样重要。吾之一生，有此两者，足矣。</p>
]]></content>
      <categories>
        <category>闲谈</category>
      </categories>
      <tags>
        <tag>无感而发</tag>
      </tags>
  </entry>
  <entry>
    <title>机器学习理论</title>
    <url>/2020/07/06/ji-qi-xue-xi-li-lun/</url>
    <content><![CDATA[<p>笔者近期想做一下<strong>机器学习</strong>相关理论总结<a id="more"></a>，计划划分为以下章节：</p>
<blockquote>
<ul>
<li>贝叶斯理论</li>
<li>概率密度函数估计</li>
<li>EM算法</li>
<li>线性判别——感知器</li>
<li>支持向量机</li>
<li>近邻法</li>
<li>特征提取与特征选择</li>
<li>神经网络</li>
<li>RNN</li>
<li>非监督学习</li>
<li>决策树</li>
<li>聚类</li>
</ul>
</blockquote>
<p>各章节主要包含两部分：</p>
<blockquote>
<ul>
<li>理论介绍</li>
<li>代码实战</li>
</ul>
</blockquote>
]]></content>
      <categories>
        <category>统计学习</category>
      </categories>
  </entry>
  <entry>
    <title>再谈极大似然估计求解</title>
    <url>/2020/08/24/zai-tan-ji-da-si-ran-gu-ji-qiu-jie/</url>
    <content><![CDATA[<p>首先我们思考这样一个问题：</p>
<blockquote>
<p>当我们用最大似然估计进行概率模型参数估计时，为什么基本都是直接求导，一阶导数等于0的点就是我们待求的最优估计?</p>
</blockquote>
<p>问到这个地方的时候，可能有一部分人就不知该如何回答了，因为一阶导数为0显然不是函数最大值点的充要条件，但我相信肯定有的答案是:</p>
<blockquote>
<p>因为碰到的对数似然函数是一个凹函数形式，这样一阶导数为0的点就与函数最大值点互为充要条件了</p>
</blockquote>
<p>这个答案是正确的，但是不够严谨，我相信基本没人会在应用最大似然进行参数估计时首先进行对数似然函数的凸性判定，这篇文章主要就是想通过严谨的数学推导来说明一类概率密度函数的对数似然函数是凹函数，可以直接利用求导等于0这种简单直接的方法寻找最优估计，而若碰到不属于这一类的概率密度函数，则在使用最大似然估计时还是先判断对数似然函数凹凸性为妙，本篇文章按照以下结构组织:</p>
<ul>
<li>指数分布族</li>
<li>指数族函数举例</li>
<li>对数似然函数凹凸性证明</li>
<li>总结</li>
</ul>
<a id="more"></a>
<p></p>
<h3 id="指数分布族">指数分布族</h3>
<p>首先给出指数分布族的定义：</p>
<blockquote>
<p>指数分布族是一类概率分布的总称，这类分布的概率密度函数具有这样的形式：<br>
$$<br>
p_\theta(x) = h(x) \exp(\theta^T \phi(x) - A(\theta))<br>
$$<br>
式中，$x$是密度函数自变量，$x \in \mathcal{X}$;$\phi(x)$是充分统计量，可以看做是原始变量的一个映射:<br>
$$<br>
\phi: \mathcal{X} \rightarrow R^d<br>
$$<br>
$\theta$是模型参数向量，与充分统计量维度相同，$h(x)$是一个只与$x$有关的统计量，$A(\theta)$为配分函数，通过该函数来保证$p_\theta(x)$满足概率密度函数的定义：<br>
$$<br>
\int_{\mathcal{X}} p_{\theta}(x) dx = 1<br>
$$<br>
由此约束条件，我们可以得到$A(\theta)$的解析表达式：<br>
$$<br>
A(\theta) = \log (\int h(x) \exp(\theta^T \phi(x))dx )<br>
$$</p>
</blockquote>
<h3 id="指数分布族函数举例">指数分布族函数举例</h3>
<p>首先给出结论，按照笔者调研，属于指数分布族的分布有：</p>
<ul>
<li>正态分布$N(\mu,\sigma^2)$</li>
<li>伯努利分布(两点分布)$B(1,\pi)$</li>
<li>二项分布$B(n,\pi)$</li>
<li>泊松分布$P(\lambda)$</li>
<li>伽马分布$G(\mu,v)$</li>
<li>…</li>
</ul>
<p>下面就将其中几个分布化成指数分布族的标准形式</p>
<h4 id="伯努利分布">伯努利分布</h4>
<p>对于伯努利分布，我们有$\mathcal{X} = { 0,1 }$,模型参数为$P(x = 1) = \pi,P(x = -1) = 1 - \pi$,因此我们有:<br>
$$<br>
\begin{aligned}<br>
P(X = x)  = \pi^x (1 - \pi)^{1-x} &amp;= \exp(x \log \pi + (1-x) \log(1-\pi) )  \<br>
&amp;= \exp(x \log \frac{\pi}{1-\pi} + \log (1-\pi))<br>
\end{aligned}<br>
$$<br>
因此令$\theta = \log \frac{\pi}{1 - \pi}$, 则$P(X = x)$可以写做:<br>
$$<br>
P(X = x) = \exp(x\theta - \log(1 + e^\theta))<br>
$$<br>
因此对于伯努利分布$h(x) = 1,\phi(x) = x, \theta = log \frac{\pi}{1-\pi}, A(\theta) = log(1 + e^\theta)$</p>
<h4 id="泊松分布">泊松分布</h4>
<p>首先给出泊松分布的形式:<br>
$$<br>
P_\lambda(X = x) = \frac{\lambda^x}{x!} e^{- \lambda}<br>
$$<br>
下面将其转化成指数分布族标准形式:<br>
$$<br>
\begin{aligned}<br>
P_\lambda(X = x) &amp;= \frac{\lambda^x}{x!} e^{- \lambda} \<br>
&amp;= \frac{1}{x!} e^{x\log \lambda - \lambda}<br>
\end{aligned}<br>
$$<br>
因此，对于泊松分布，$h(x) = \frac{1}{x!}, \theta = \log \lambda,\phi(x) = x,A(\theta) = e^\theta$</p>
<h4 id="高斯分布">高斯分布</h4>
<p>高斯分布的概率密度函数形式如下:<br>
$$<br>
p_{\mu,\sigma}(x) = \frac{1}{\sqrt{2 \pi} \sigma} \exp(- \frac{(x-\mu)^2}{2\sigma^2})<br>
$$<br>
下面将其转化成指数分布族标准形式:<br>
$$<br>
\begin{aligned}<br>
p_{\mu,\sigma}(x) &amp;= \frac{1}{\sqrt{2 \pi} \sigma} \exp(- \frac{(x-\mu)^2}{2\sigma^2})  \<br>
&amp;= \frac{1}{\sqrt{2 \pi} \sigma} \exp(\frac{-x^2}{2 \sigma^2} + \frac{x \mu}{\sigma^2} - \frac{\mu^2}{2\sigma^2}) \<br>
&amp;= \frac{1}{\sqrt{2 \pi} } \exp(\frac{-x^2}{2 \sigma^2} + \frac{x \mu}{\sigma^2} - \frac{\mu^2}{2\sigma^2} + \log \sigma)<br>
\end{aligned}<br>
$$<br>
因此，对于高斯分布:<br>
$$<br>
\begin{aligned}<br>
h(x) &amp;= \frac{1}{\sqrt{2 \pi} \sigma}  \<br>
\phi(x) &amp;= [\frac{x^2}{2}, x]^T \<br>
\theta &amp;= [-\frac{1}{\sigma^2}, \frac{\mu}{\sigma^2}]^T \<br>
A(\theta) &amp;= -\frac{1}{2} (log \theta^{(1)} + \theta^{(2)})<br>
\end{aligned}<br>
$$</p>
<h3 id="对数似然函数凹凸性证明">对数似然函数凹凸性证明</h3>
<p>我们关心的是最终对数似然函数的凹凸性，因为最终的对数似然函数是各个样本的概率密度函数取对数后累加的形式，由保凸运算可知，若单个对数概率密度函数的凹凸性可以确定，那么最终对数似然函数的凹凸性也就确定了。对于指数族分布，单个样本的概率密度函数取对数如下:<br>
$$<br>
\begin{aligned}<br>
\log p_{\theta}(x) &amp;= \log h(x) \exp(\theta^T \phi(x) - A(\theta)) \<br>
&amp;= \log h(x) + \theta^T \phi(x) + \log(-A(\theta))<br>
\end{aligned}<br>
$$<br>
第一项与$\theta$无关，第二项为$\theta$的线性组合，不影响函数的凹凸性，所以整个函数的凹凸性是由第三项来确定，对于外层函数是对数函数的对数函数$\log f(x)$而言:<br>
$$<br>
(\log f(x))^{’’} = \frac{f(x) f^{’’}(x) - f^{’}(x)^2}{f(x)^2}<br>
$$<br>
由对数函数性质有$f(x) &gt; 0$,因此，若$f(x)$是凹函数($f^{’’}(x) \leq 0$),则可推出$(\log f(x))^{’’} \leq 0$,即$\log f(x)$为凹函数，若$f(x)$为凸函数则不能有直接的结论，因此我们的重点放在讨论$A(\theta)$的凹凸性上。<br>
下面证明$A(\theta)$为凸函数,从定义出发证明，记$\theta_{\lambda} = \lambda \theta_1 + (1 - \lambda) \theta_2$,其中$\theta_1,\theta_2 \in dom f, \lambda \in [0,1]$,由Hölder不等式，可知:<br>
$$<br>
\begin{aligned}<br>
A(\theta_\lambda) &amp;= \log (\int h(x) \exp(\theta_{\lambda}^T \phi(x))dx ) \<br>
&amp;= \log (\int \exp(\theta_{\lambda}^T \phi(x))d\mu(x)\<br>
&amp;= \log (\int \exp[ (\lambda \theta_1 \phi(x) + (1-\lambda)\theta_2)\phi(x)]d\mu(x) ) \<br>
&amp;= \log (\int \exp(\theta_1 \phi(x)^{\lambda}) \exp (\theta_2 \phi(x))^{1-\lambda} d \mu(x)) \<br>
&amp;\leq \log (\int \exp(\theta_1 \phi(x))^{\frac{\lambda}{\lambda}} d\mu(x) )^\lambda (\int \exp(\theta_2 \phi(x))^{\frac{1-\lambda}{1- \lambda}}d\mu(x))^{1-\lambda}  (Hölder不等式) \<br>
&amp;= \lambda \log (\int \exp(\theta_1 \phi(x)) d\mu(x) ) + (1-\lambda)<br>
\log (\int \exp(\theta_2 \phi(x)) d\mu(x) ) \<br>
&amp;= \lambda A(\theta_1) + (1-\lambda) A(\theta_2)<br>
\end{aligned}<br>
$$<br>
从凸函数定义可知,$A(\theta)$为凸函数，至此我们便得到了对数似然函数的凹凸性:<br>
$$<br>
\begin{aligned}<br>
&amp;A(\theta) 凸 \rightarrow -A(\theta) 凹 \rightarrow \log(-A(\theta)) 凹 \<br>
&amp; log(p_\theta(x)) 凹  \rightarrow 对数似然函数 凹<br>
\end{aligned}<br>
$$</p>
<h3 id="总结">总结</h3>
<p>从前面列出的指数分布族所包含的分布来看，我们日常所碰到的绝大多数概率分布都属于指数族分布，是可以直接通过求导等于0这种方法来得到参数估计，而对于不属于指数分布族的分布，则是有必要对对数似然函数进行分析，而不能直接通过求导来解最优参数估计值。</p>
<h4 id="参考">参考</h4>
<p>本文内容主要参考以下链接：<br>
[1] <a href="https://www.zhihu.com/question/263423642/answer/269353169">为什么极大似然估计求导为 0 就是要求的值呢？</a><br>
[2] <a href="https://web.stanford.edu/class/stats311/Lectures/lec-07.pdf">指数分布族函数</a></p>
]]></content>
      <categories>
        <category>统计学习</category>
      </categories>
      <tags>
        <tag>统计学习，凸优化</tag>
      </tags>
  </entry>
  <entry>
    <title>概率密度函数估计</title>
    <url>/2020/07/27/gai-lu-mi-du-han-shu-gu-ji/</url>
    <content><![CDATA[<p>在使用贝叶斯理论进行决策时，我们需要知道先验概率$P(\omega)$,类条件概率密度函数$p(x|\omega)$,而在这一部分，我们便讨论下如何从样本集中将这些所需的概率密度函数估计出来<a id="more"></a>，本文主要讨论以下几个问题：</p>
<ul>
<li>如何利用样本集估算$p(x|\omega)$和$P(\omega)$</li>
<li>估计量性质</li>
<li>利用样本集估算错误率</li>
</ul>
<p>从样本集推断总体概率分布的方法可以分为以下几种类型:</p>
<ul>
<li>监督参数估计——样本所属的类别以及总体概率密度函数的形式为已知，而概率密度函数的某些参数是未知的</li>
<li>非监督参数估计——已知总体概率密度函数形式，但未知样本所属种类，要求推断出概率密度函数某些参数</li>
<li>非参数估计——已知样本类别，但未知总体概率密度函数形式，要求我们直接推断概率密度函数本身</li>
</ul>
<h3 id="最大似然估计">最大似然估计</h3>
<p>最大似然估计是进行参数估计的常用方法，它有两个假设：</p>
<ul>
<li>待估计的参数$\theta$是一个确定的、未知的量</li>
<li>概率分布函数的形式已知</li>
<li>样本为独立抽取</li>
</ul>
<p>若记$\theta$为待估计参数(比如正态分布中的$\mu,\sigma$),$\mathcal{X}$为样本集合：<br>
$$ \mathcal{X} = { x_1,x_2, \dots, x_n} $$<br>
概率密度函数形式已知，记做$p(x|\theta)$，由于假设样本为独立抽取，则似然函数可以写做：<br>
$$ l(\theta) = p(\mathcal{X}|\theta) = \prod_{i=1}^n p(x_i|\theta)$$<br>
对数似然函数为：<br>
$$  H(\theta) = ln(l(\theta)) = \sum_{i=1}^n lnp(x_i|\theta)$$<br>
首先来看似然函数，它表征了在参数为$\theta$情况下，得到样本集合$\mathcal{X}$的概率，直观来看，这个概率越大，则证明估计得到的参数更好,至于为何使用对数似然函数，这是因为我们需要求$l(\theta)$的最大值，但连乘积求导非常复杂，而取对数运算是单调的，并不改变最值位置，同时计算上大大简化。因此最终最大似然估计值为:</p>
<p>$$  \frac{\partial H(\theta)}{\theta} = 0  $$</p>
<p>实践中常见的概率分布为正态分布，其最大似然估计值为：<br>
$$<br>
\begin{aligned}<br>
\hat{\mu} &amp;= \frac{1}{n} \sum_{i=1}^n x_i \\<br>
\hat{\sigma^2} &amp;= \frac{1}{n} \sum_{i=1}^n (x_i - \hat{\mu})^2<br>
\end{aligned}<br>
$$</p>
<h3 id="贝叶斯估计">贝叶斯估计</h3>
<p>首先回顾下贝叶斯决策：</p>
<ul>
<li>状态空间:$\Omega = { \omega_1, \omega_2, \dots , \omega_c }$</li>
<li>待识别对象: $\mathbf{x} = [x_1,x_2,\dots, x_d]^T$</li>
</ul>
<p>设真实状态为$\omega_j$，而采取决策$a_i$所带来的损失为$\lambda(\omega_j,a_i)$,对于特定$\mathbf{x}$，采取决策$a_i$所带来的平均损失为:<br>
$$  R(a_i|\mathbf{x}) = \sum_{j=1}^c \lambda(a_i,\omega_j) p(\omega_j|x)$$<br>
整体损失则需要再对$\mathbf{x}$进行积分，在进行贝叶斯决策时，若想使得整体损失最小，则只需要对任意的$\mathbf{x}$，$R(a_i|\mathbf{x})$。<br>
下面我们考虑参数估计问题:现在有一个样本集$\mathcal{X}$，要求我们找到最优估计量$\hat{\theta}$(对比决策$a_i$),使得带来的贝叶斯风险最小。由此可见，与贝叶斯决策相比，贝叶斯估计也是立足于使得贝叶斯风险最小，两者对比如下图所示：</p>
<table>
<thead>
<tr>
<th>决策问题</th>
<th style="text-align:right">估计问题</th>
</tr>
</thead>
<tbody>
<tr>
<td>样本$\mathbf{x}$</td>
<td style="text-align:right">样本集$\mathcal{X}$</td>
</tr>
<tr>
<td>决策量$a_i$</td>
<td style="text-align:right">待估计值$\hat{\theta}$</td>
</tr>
<tr>
<td>真实状态$\omega_j$</td>
<td style="text-align:right">真实参数$\theta$</td>
</tr>
<tr>
<td>状态空间$\mathcal{A}$是离散空间</td>
<td style="text-align:right">参数空间$\Theta$是连续空间</td>
</tr>
<tr>
<td>先验概率$P(\omega_j)$</td>
<td style="text-align:right">参数的先验分布$p(\theta)$</td>
</tr>
</tbody>
</table>
<p>对于样本集$\mathcal{X}$，参数估计值为$\hat{\theta}$所带来的平均损失为：<br>
$$<br>
R(\hat{\theta}|\mathcal{X}) = \int_{\Theta} \lambda(\hat{\theta},\theta) p(\hat{\theta}|\mathcal{X}) d\theta<br>
$$<br>
从上式中可以也可以看出贝叶斯决策与贝叶斯估计的区别:</p>
<blockquote>
<p>贝叶斯决策是立足于每一个样本$\mathbf{x}$，只需要保证对每一个样本均做出最优决策，便可保证整体决策最优；而贝叶斯估计则是立足于样本集$\mathcal{X}$</p>
</blockquote>
<p>现在我们要想获得贝叶斯估计，便转化为寻找合适的估计值$\hat{\theta}$,使得$R(\hat{\theta}｜\mathcal{X})$最小，这需要解决两个问题：</p>
<ul>
<li>$p(\hat{\theta}|\mathcal{X})$计算</li>
<li>损失函数$\lambda(\hat{\theta},\theta)$的选择</li>
</ul>
<p>首先考虑$p(\hat{\theta}|\mathcal{X})$的计算，假设$\theta$的先验概率分布为$p(\theta)$,类条件概率密度函数为$p(\mathbf{x}|\theta)$,样本集$\mathcal{X} = { x_1,\dots, x_n }$,似然函数$p(\mathcal{X}|\theta)$可以表示为：<br>
$$<br>
p(\mathcal{X}|\theta) = \prod_{i=1}^n p(\mathbf{x_i}|\theta)<br>
$$<br>
由贝叶斯公式，后验概率可以表示为:<br>
$$<br>
p(\hat{\theta}|\mathcal{X}) = \frac{p(\mathcal{X}|\hat{\theta})p(\hat{\theta})}{ \int p(\mathcal{X}|\theta)p(\theta) d\theta}<br>
$$<br>
损失函数$\lambda(\hat{\theta},\theta)$常见的有三种选择:</p>
<ul>
<li>均方误差函数:$\lambda(\hat{\theta},\theta) = (\theta - \hat{\theta})^2$</li>
<li>绝对值误差函数: $\lambda(\hat{\theta},\theta) = |\theta - \hat{\theta}|$</li>
<li>均匀函数: $\lambda(\hat{\theta}, \theta) = \begin{cases}<br>
0 &amp; x \in [-\frac{\Delta}{2}, \frac{\Delta}{2}] \<br>
1 &amp; other<br>
\end{cases}$</li>
</ul>
<p>在不同损失函数下，可以推导出不同的结果：</p>
<ul>
<li>均方误差函数:<br>
$$ \hat{\theta}<em>{MS} = \int</em>{-\infty}^\infty \theta p(\theta| \mathcal{X})d\theta$$</li>
<li>绝对值误差函数:<br>
$$  \int_{-\infty}^{\hat{\theta_{ABS}}} p(\theta|\mathcal{X}) d\theta = \int_{\hat{\theta_{ABS}}}^{\infty} p(\theta|\mathcal{X}) d\theta $$</li>
<li>均匀误差函数: $\theta_{MAP}$应当取在后验概率密度函数最大之处</li>
</ul>
<p>下面以均方误差函数为例，假设:</p>
<ul>
<li>$p(x|\mu) \sim N(\mu, \sigma^2)$</li>
<li>$p(\mu) \sim N(\mu_0,\sigma_0^2)$</li>
</ul>
<p>则后验概率密度函数$p(\theta|\mathcal{X})$为:<br>
$$<br>
\begin{aligned}<br>
p(\mu|\mathcal{X}) &amp;= \frac{p(\mathcal{X}|\mu)p(\mu)}{\int p(\mathcal{X}|\mu)p(\mu)d\mu}  \\<br>
&amp;= a exp{ -\frac{1}{2}[\sum_{k=1}^N (\frac{\mu - x_k}{\sigma})^2 + (\frac{\mu - \mu_0}{\sigma_0})^2] }<br>
\end{aligned}<br>
$$<br>
由于该概率密度函数是$\mu$的二次函数指数形式，所以仍是一个正态密度，其均值和方差为：<br>
$$<br>
\begin{aligned}<br>
\mu_N &amp;= \frac{N\sigma_0^2}{N\sigma_0^2 + \sigma^2} \frac{1}{N}\sum_{k=1}^N x_k + \frac{\sigma^2}{N\sigma_0^2 + \sigma^2} \mu_0  \\<br>
\sigma_N^2 &amp;= \frac{\sigma_0^2 \sigma^2}{N\sigma_0^2 + \sigma^2}<br>
\end{aligned}<br>
$$<br>
因为误差函数为均方误差函数，则其参数估计值应当是后验概率密度意义上的均值，即$\mu_N$</p>
<h3 id="非参数估计">非参数估计</h3>
<p>在使用最大似然估计或者贝叶斯估计进行参数估计时，需要对类条件概率密度函数$p(\mathbf{x}|\omega)$形式已知，但在某些情况下，我们仅仅拥有样本，但并没有这些先验知识，此时若想要进行概率密度函数的估计，则就用到了非参数估计的方法，该类方法具有以下几点假设：</p>
<ul>
<li>不知道分布函数形式</li>
<li>$N$个样本服从独立同分布</li>
</ul>
<p>假设样本点$\mathbf{x}$落在$d$维空间中，在$d$维空间中有一个超立方体$\mathcal{R}$,点$\mathbf{x}$落到该超立方体中的概率记做$P$:<br>
$$<br>
P = \int_{\mathcal{R}} p(x) dx<br>
$$<br>
若$N$个样本是从密度为$p(x)$的总体中独立抽取的，则$N$个样本中有$k$个落入区域$\mathcal{R}$中的概率符合二项分布，该概率可以写做：<br>
$$<br>
P_k = C_N^k P^k(1-P)^{N-k}<br>
$$<br>
根据二项分布的性质，$k$的期望为：<br>
$$<br>
E(k) = NP<br>
$$<br>
我们的目的是想获得关于$P$的估计值，因此考虑：<br>
$$ \hat{P} = \frac{k}{N}$$<br>
若超立方体$\mathcal{R}$很小，则可以假设在该超球内概率密度函数$p(x)$为定值，此时：<br>
$$<br>
P = \int_{\mathcal{R}} p(x) dx  = p(x)V<br>
$$<br>
由此我们便可以获得$p(x)$的估计值：<br>
$$<br>
\hat{p}(x) = \frac{P}{V} = \frac{k}{NV}<br>
$$<br>
下面讨论下体积$V$的大小及样本数量多少之间的关系。如果把体积$V$固定，样本数取得越来越多，则比值$\frac{k}{N}$将在概率上收敛，最终得到的$p(x)$估计是在超立方体$\mathcal{R}$上的平均估计，而若想要得到$p(x)$的点估计，则需要区间尽可能小，但区间取得过小又会导致一些区间内没有样本点，这种估计也是没有意义的。<br>
下面给出概率密度函数估计值$p_N(x)$收敛于真实概率密度函数$p(x)$的条件：</p>
<ul>
<li>$\lim_{N \rightarrow \infty} V_N = 0$</li>
<li>$\lim_{N \rightarrow \infty} k_N = \infty$</li>
<li>$\lim_{N \rightarrow \infty} \frac{k_N}{N} = 0$</li>
</ul>
<p>第一个条件可以使空间平均$\frac{P}{V}$收敛于$p(x)$,第二个条件可以使频数比在概率意义上收敛于概率$P$。满足以上三个条件的区域序列的选择有两种：</p>
<ul>
<li>Parzen窗法： 使区域序列$V_N$ 以$N$的某个函数的关系不断缩小，但此时需要对$k_N$和$\frac{k_N}{N}$加些限制条件</li>
<li>$k_N$近邻估计，让$k_N$为$N$的某个函数，而$V_N$的选取是使相应的$\mathcal{R_N}$正好包含$x$的$k_N$个近邻。</li>
</ul>
<h4 id="Parzen窗法">Parzen窗法</h4>
<p>假设$\mathcal{R_N}$为一个超立方体，其棱长为$h_N$,则此时超立方体体积$V_N = h_N^d$,定义窗函数$\phi(\mu)$：<br>
$$<br>
\phi(\mu) = \begin{cases}<br>
1, if |\mu_j| \leq \frac{1}{2} \<br>
0, other<br>
\end{cases}<br>
$$<br>
引入窗函数的的目的主要是对落入区间的样本数$k$进行显示表达，当$x_i$落入以$x$为中心，体积为$V_N$的超立方体内时，$\phi(\mu) = \phi(\frac{x-x_i}{h_N}) = 1$，否则为0，此时落入该超立方体内的样本数为：<br>
$$<br>
k_N = \sum_{i=1}^N \phi(\frac{x-x_i}{h_N})<br>
$$<br>
由此便可以得到概率密度函数估计：<br>
$$<br>
p_N(x) = \frac{k_N}{V_NN} = \frac{1}{N} \sum_{i=1}^N \frac{1}{V_N} \phi(\frac{x-x_i}{h_N})<br>
$$<br>
事实上，窗函数的主要作用是内插，反映了每一样本$x_i$对$x$点处的概率密度函数的贡献<br>
下面我们讨论下，若要使得$p_N(x)$满足概率密度函数特性(在整个空间上积分为1),则窗函数应当具有什么特性:<br>
$$<br>
\begin{aligned}<br>
\int p_N(x)dx &amp;= \frac{1}{NV_N} \int \sum_{i=1}^N\phi(\frac{x-x_i}{h_N})dx  \\<br>
&amp;= \frac{1}{N} \sum_{i=1}^N \int \phi(\mu)d\mu \\<br>
&amp; = 1<br>
\end{aligned}<br>
$$<br>
因此考虑到$p_N(x)$非负且积分为1,则窗函数应当具备一般概率密度函数所具有的特征：</p>
<ul>
<li>$\phi(\mu) \geq 0 $</li>
<li>$\int \phi(\mu) d\mu = 1$</li>
</ul>
<p>常见的窗函数有以下三种：</p>
<ul>
<li>方窗函数</li>
<li>正态窗函数:$\phi(\mu) = \frac{1}{\sqrt{2\pi}} exp(-\frac{1}{2}\mu^2)$</li>
<li>指数窗函数:$\phi(\mu) = \frac{1}{2} exp(-|\mu|)$</li>
</ul>
<p>窗宽$h_d$的选择非常重要，若$h_d$选择过大，则会使得估计的分辨力降低，若选的过小，又会导致估计的稳定性下降，反映在估计得到的概率密度函数上就是毛刺很多，一般需要折中考虑。<br>
下面讨论估计量$p_N(x)$的统计性质，可以证明在某些限制条件下，估计量是渐进无偏和平方误差一致的，限制条件为:</p>
<ul>
<li>总体密度函数$p(x)$在$x$点连续</li>
<li>窗函数满足以下条件：<br>
$$ \phi(\mu) \geq 0, \int \phi(\mu) d\mu = 1, sup_{u} \phi(\mu)&lt;\infty$$</li>
<li>窗宽满足: $$ \lim_{N \rightarrow \infty} V_N= 0,\lim_{N \rightarrow \infty} NV_N = \infty $$</li>
</ul>
<h4 id="k-N-近邻估计">$k_N$近邻估计</h4>
<p>parzen窗估计是对体积序列进行限制，而$k_N$近邻估计则是对$k_N$序列做出限制</p>
<h3 id="概率密度函数估计的准确性与分类器性能的关系">概率密度函数估计的准确性与分类器性能的关系</h3>
<p>因为概率密度函数的估计最终还是需要服务于一些下游任务，比如应用贝叶斯决策进行分类，因此这部分主要讨论分类器的误差来源，误差来源主要有以下几种：</p>
<ul>
<li>贝叶斯误差：这种误差是由于不同的类条件概率分布函数之间的相互重叠引起的，这种误差是问题本身所固有的，在分类器设计阶段无法消除</li>
<li>模型误差：指因为选择了不正确的模型导致的误差</li>
<li>估计误差： 由于采用有限样本进行估计所带来的误差</li>
</ul>
]]></content>
      <categories>
        <category>统计学习</category>
      </categories>
  </entry>
  <entry>
    <title>概率分布中的距离度量</title>
    <url>/2020/08/26/gai-lu-fen-bu-zhong-de-ju-chi-du-liang/</url>
    <content><![CDATA[<p>在$k$近邻章节中，我总结了在样本空间中常用的两种距离度量:$L_p$距离以及马氏距离。但在一些机器学习任务中，我们期望能够得到一个分布与目标分布尽可能接近，这也就引出了一个新的问题:</p>
<blockquote>
<p>如何度量两个概率分布之间的距离(差异程度)？</p>
</blockquote>
<p>本文将介绍一些常见的概率分布之间的距离度量，按照以下结构组织:</p>
<ul>
<li>信息量与熵</li>
<li>KL散度(相对熵)</li>
<li>交叉熵</li>
<li>JS散度</li>
<li>推土机理论与Wasserstein距离</li>
</ul>
<a id="more"></a>
<h3 id="信息量与熵">信息量与熵</h3>
<h4 id="信息量">信息量</h4>
<p>任何事件都会承载着一定的信息量，包括已经发生的事件和未发生的事件，只是它们承载的信息量有所不同：</p>
<blockquote>
<p>🌰：对于两个事件：昨天下雨与明天下雨。昨天下雨这个事件，因为已经发生，既定事实，那么它的信息量就为0；而对于明天会下雨这个事件，因为未有发生，那么该事件的信息量就大。</p>
</blockquote>
<p>从上面栗子可以看出信息量是一个与事件发生概率相关的概念，而且可以得出，事件发生的概率越小，其信息量越大。这也很好理解，因为所谓的“搞个大新闻”,也就是发生了不太可能发生的事情，&quot;大新闻&quot;则意味着该事件蕴含着更多的信息。从这个角度出发， 定义信息量:</p>
<blockquote>
<p><strong>信息量:</strong> 假设$X$是一个离散型随机变量，其取值集合为$\mathcal{X}$，其概率分布为:<br>
$$<br>
p(x) = P(X = x), x \in \mathcal{X}<br>
$$<br>
则定义事件$X = x_0$的信息量为:<br>
$$<br>
I(x_0) = -\log(p(x_0))<br>
$$</p>
</blockquote>
<h4 id="熵">熵</h4>
<p>首先给出熵的定义：</p>
<blockquote>
<p>在信息论与概率统计中，熵是表示<strong>随机变量不确定性</strong>的度量，设$X$是一个取有限个值的离散随机变量，其概率分布为:<br>
$$<br>
P(X = x_i) = p_i, \quad i =1,2,\dots,n<br>
$$<br>
则随机变量$X$的熵定义为:<br>
$$<br>
H(X) = - \sum_{i=1}^n p_i \log p_i<br>
$$</p>
</blockquote>
<p>有了信息量的定义后，再看关于熵的定义式可以看出，熵不过是在概率分布$P$下对信息量求的期望，所以说熵实际上就是<strong>事件信息量的期望</strong>。<br>
若$p_i = 0$,则定义$0\log 0 =0$,式中的对数一般取以2为底或者以$e$为底，这时熵的单位被称作比特或纳特，由定义可知，熵只依赖于$X$的分布，而与$X$的取值无关，因此也可将$X$的熵记作$H§$,熵越大，随机变量的不确定性越大。因为$p_i \leq 0$,由$H§$函数性质(凹函数，应用拉格朗日乘子法可求最大值)可知:<br>
$$<br>
0 \leq H§ \leq \log n<br>
$$</p>
<h3 id="KL散度-相对熵">KL散度(相对熵)</h3>
<p>KL散度又称相对熵，如果我们对同一个随机变量有两个独立的概率分布$P(x)$和$Q(x)$，我们可以使用KL散度来衡量这两个分布的差异,下面给出KL散度的定义式:</p>
<blockquote>
<p>在机器学习中，$P$往往用来表示样本的真实分布，$Q$用来表示模型所预测的分布，那么$KL$散度就可以计算两个分布之间的差异，也就是Loss损失值:<br>
$$<br>
\begin{aligned}<br>
D_{KL}(P||Q) &amp;= -\sum_{i=1}^n p(x_i) \log(\frac{q(x_i)}{p(x_i)}) \<br>
&amp;= -\sum_{i=1}^n p(x_i) \log(q(x_i)) - (-\sum_{i=1}^n p(x_i) \log p(x_i))<br>
\end{aligned}<br>
$$<br>
由于$-\log x$为凸函数，由凸函数性质可知:<br>
$$<br>
-\sum_{i=1}^n p(x_i) \log(\frac{q(x_i)}{p(x_i)}) \geq  \log \sum_{i=1}^n q(x_i) = 0<br>
$$<br>
这说明KL散度具有非负的性质，这也说明$P(x)$与其他分布的交叉熵恒大于熵。</p>
</blockquote>
<p>这里需要说明的是，虽然KL散度具有非负性，但其并不满足度量的另外两个性质:</p>
<ul>
<li>对称性</li>
<li>三角不等式</li>
</ul>
<h3 id="交叉熵">交叉熵</h3>
<p>KL散度由两部分组成，第一部分是交叉熵，第二部分则是$P(x)$的熵，$P(x)$因为是真实的分布，所以其熵为固定值。在机器学习任务中，我们需要评估样本固有label与我们算法的预测$predict$之间的差距，在优化过程中，我们只需关注交叉熵，所以在分类任务中，往往采用交叉熵作为loss函数。</p>
<h3 id="JS散度">JS散度</h3>
<p>JS散度只是KL散度的变形，其解决了KL散度的非对称问题，其表达式如下:<br>
$$<br>
JS(P||Q) = \frac{1}{2} KL(P(x)||\frac{P(x) + Q(x)}{2}) + \frac{1}{2}KL(Q(x)||\frac{P(x)+ Q(x)}{2})<br>
$$<br>
若$\log$以2为底，则JS散度的范围为$[0,1]$,若$\log$以$e$为底，则JS散度范围为$[0,\ln 2]$。当两个分布完全相同时取下界，当两个分布完全不同时取上界。<br>
这也就带来了一个问题，若优化时两个分布完全没有交集时,JS散度就是一个常数($1$或$\ln 2$),此时会出现梯度消失问题，导致无法进行优化。</p>
<h3 id="Wasserstein距离">Wasserstein距离</h3>
<p>首先说明下KL散度与JS散度存在的问题:</p>
<blockquote>
<p>若两个分布$P,Q$完全没有重叠，则会导致KL散度无意义($\log 0$),而JS散度是一个常数，也就是说出现了梯度消失问题。</p>
</blockquote>
<p>而Wasserstein距离可以一定程度上解决该问题。</p>
<h4 id="思想">思想</h4>
<p>对于两个分布$P$和$Q$，考虑从将一个分布转换成另一个分布所需的代价入手来定义概率分布的差异。下面以一个栗子来说明该距离的思想:</p>
<blockquote>
<p>🌰: 考虑两个离散分布$P$和$Q$:<br>
$$<br>
\begin{aligned}<br>
P_1 &amp;= 3, P_2 = 2, P_3 = 1, P_4 = 4 \<br>
Q_1 &amp;= 1, Q_2 = 2, Q_3 = 4, Q_4 = 3<br>
\end{aligned}<br>
$$<br>
下面讨论应该如何&quot;移土&quot;，才能够使得两个分布相同:</p>
<ul>
<li>为了让$P_1$与$Q_1$相同，我们需要把$P_1$的3分2到$P_2$去，这样$P_1$和$Q_1$都等于1，此时$P_2$等于4</li>
<li>同理，为了让$P_2$和$Q_2$相同，需要将$P_2$的土填到$P_3$时，这一步将从$P_2$挖2分土填到$P_3$</li>
<li>为了让$P_3,Q_3$相等，最后一步需要将$Q_3$的1分土挖给$Q_4$<br>
每一步的代价计算公式为$\delta_{i+1} = \delta_i + P_i - Q_i$,由此可得:<br>
$$<br>
\begin{aligned}<br>
\delta_0 &amp;= 0 \<br>
\delta_1 &amp;= 0 +3 -1 = 2 \<br>
\delta_2 &amp;= 2 + 2 - 2 = 2 \<br>
\delta_3 &amp;= 2 + 1 -4 = -1 \<br>
\delta_4 &amp;= -1 + 4 - 3 = 0<br>
\end{aligned}<br>
$$<br>
所以最终的总代价，也就是Wasserstein距离为$W = \sum |\delta_i| = 5$。该栗子可以形象的用下图表示:<br>
<img src="https://raw.githubusercontent.com/xuejy19/xuejy19.github.io/source/Img/W_dis.png" alt="推土机距离"></li>
</ul>
</blockquote>
<h4 id="公式">公式</h4>
<p>从上面的离散栗子了解了Wasserstein距离的思想之后，下面给出一般连续分布Wasserstein距离计算公式:</p>
<blockquote>
<p>$$<br>
W(p_r,p_g) = \inf_{\gamma \in \prod(p_r,p_g)} \quad E_{(x,y)\sim \gamma}[||x-y||]<br>
$$<br>
公式中$\prod(p_r,p_g)$指代$p_r$和$p_g$所有可能的联合概率分布$\gamma(x,y)$,直观上来说，$\gamma(x,y)$表示将分布$p_r$转化成$p_g$所需要多少“土”，Wasserstein指代花费最少的运输方案。</p>
</blockquote>
<p>Wasserstein相对于传统的KL散度和JS散而言，当两个分布具有完全不同的支撑集合时，KL散度会是无穷，JS散度会是一个常值，而W距离此时仍能反映两个分布的远近。下面以一个栗子来说明Wasserstein距离的优势所在:</p>
<blockquote>
<p>🌰: 变量$Z \sim U[0,1]$,下面给出两个分布:<br>
$$<br>
\begin{aligned}<br>
&amp;P_0 (x_1), \quad x_1 = (0,Z) \in R^2  \<br>
&amp; P_1(x_2), \quad x_2 = (\theta,z) \in R^2<br>
\end{aligned}<br>
$$<br>
对于这个简单的栗子，不同概率密度距离度量得到的数值如下:<br>
$$<br>
\begin{aligned}<br>
W(P_0,P_1) &amp;= |\theta| \<br>
JS(P_0,P_1) &amp;= \begin{cases}<br>
\log 2  &amp; if \theta \neq 0 \<br>
0  &amp; if \theta = 0<br>
\end{cases} \<br>
KL(P_0||P_1) &amp;= \begin{cases}<br>
+\infty  &amp; if \theta \neq 0 \<br>
0  &amp; if \theta = 0<br>
\end{cases}<br>
\end{aligned}<br>
$$<br>
在Wasserstein度量下，随着$\theta_t \rightarrow 0$,概率分布序列$(P_{\theta_t})_{t \in N}$ 收敛到$P_0$,但在其他度量下并不能收敛，W距离与JS散度在该问题中对比如下图所示:<br>
<img src="https://raw.githubusercontent.com/xuejy19/xuejy19.github.io/source/Img/W.png" alt="$W VS JS$"></p>
</blockquote>
<h4 id="计算">计算</h4>
<p>尽管Wasserstein度量具有很多优点，但其缺点也十分明显：</p>
<blockquote>
<p>计算非常复杂，优化问题不易解</p>
</blockquote>
<p>目前能够显示计算出Wasserstein的只有两种情况：</p>
<blockquote>
<p>一维概率分布<br>
高斯分布</p>
</blockquote>
<p>对于两个离散的一维概率分布，可以直接使用<code>scipy.stats.Wasserstein_distance</code>函数:</p>
<pre class="line-numbers language-language-python"><code class="language-language-python">import numpy as np
from scipy.stats import wasserstein_distance
p = [0,5,9]
q = [2,5,7]
w = wasserstein_distance(q,p)
print(w)
<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>
<p>输出:</p>
<pre class="line-numbers language-language-python"><code class="language-language-python">1.3333333333333335 
<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre>
]]></content>
      <categories>
        <category>统计学习</category>
      </categories>
      <tags>
        <tag>KL散度，JS散度，推土机距离</tag>
      </tags>
  </entry>
  <entry>
    <title>提升算法总结</title>
    <url>/2020/09/01/ti-sheng-suan-fa-zong-jie/</url>
    <content><![CDATA[<p>对于一个特定机器学习问题，我们可能会建立很多模型，这些单个模型可能表现都不是非常好，由此便会引出一个问题:</p>
<blockquote>
<p><strong>问题1: 能否通过一个算法将这些模型组合起来(Ensemble)，产生一个效果更好的组合模型？</strong></p>
</blockquote>
<p>这个问题的答案是肯定的，历史上，Kearns 和 Valiant首先提出了“弱可学习”和“强可学习”的概念：</p>
<blockquote>
<p><strong>强可学习：</strong> 在概率近似正确(PAC)框架下，一个概念(类)，如果存在一个多项式的学习算法能够学习它，并且正确率很高，就称这个概念是强可学习的<br>
<strong>弱可学习：</strong> 一个概念，如果存在一个多项式的算法能够学习它，但学习的正确率仅仅比随机猜测好，则称该概念是弱可学习</p>
</blockquote>
<p>后来Schapire证明了一个重要结论:</p>
<blockquote>
<p><strong>一个概念是强可学习的  $\Leftrightarrow$ 一个概念是弱可学习的</strong></p>
</blockquote>
<p>这样一来，问题1就转换成了另一个问题:</p>
<blockquote>
<p><strong>问题2: 在学习中国，如果已经发现了“弱可学习算法”，那么能否将它提升为强可学习算法？</strong></p>
</blockquote>
<p>该部分介绍以下几类提升算法:</p>
<ul>
<li>Bagging</li>
<li><strong>AdaBoost</strong></li>
<li>随机森林</li>
<li>提升算法解释</li>
<li>总结</li>
</ul>
<a id="more"></a>
<h3 id="Bagging">Bagging</h3>
<p>对于一个训练数据集，如果我们将所有的训练集数据拿来训练，那么我们便只能得到一个模型，因此Bagging的思想非常简单：</p>
<blockquote>
<p>每次从训练数据集中取一部分数据来训练模型，通过这种方式得到多个模型，最后的决策结果则由这些模型投票表决</p>
</blockquote>
<p>算法流程如下:</p>
<blockquote>
<ul>
<li>Step1: 从包含$n$个样本的数据集中取$m$个样本作为新的训练集</li>
<li>Step2: 用挑选出的数据集来训练得到一个模型</li>
<li>Step3: 重复Step1 L次，得到L个模型</li>
<li>Step4: 最终决策结果由这$L$个模型多数投票得到</li>
</ul>
</blockquote>
<h3 id="AdaBoost">AdaBoost</h3>
<h4 id="算法流程">算法流程</h4>
<p>Bagging的思想比较简单粗暴，所有样本都一视同仁，而另一种思路则是改变训练数据的概率分布(训练数据的权值分布)，针对不同的训练数据分布调用弱学习算法学习一系列弱分类器。这样，对于提升算法而言，有两个问题需要回答：</p>
<ul>
<li>在每一轮如何改变训练数据的权值或概率分布</li>
<li>如何将这些弱分类器组合成一个强分类器</li>
</ul>
<p>AdaBoost针对这两个问题的的解决方案是:</p>
<ul>
<li>第1个问题: 提高那些被前一轮弱分类器错误分类样本的权值，而降低那些被正确分类的样本的权值。这样一来，那些没有得到正确分类的数据由于权值加大而受到后一轮弱分类器的更多关注。</li>
<li>第2个问题：AdaBoost采用加权表决的方法,加大分类误差率小的弱分类器的权值。</li>
</ul>
<p>AdaBoost算法流程如下:</p>
<blockquote>
<p><strong>AdaBoost算法</strong><br>
<strong>输入：</strong> 训练数据集$T = { (x_1,y_1), (x_2,y_2),\dots, (x_N,y_N)}$,其中$x_i \in R^n, y_i \in { -1, +1 }$,弱学习算法<br>
<strong>输出：</strong> 最终分类器$G(x)$</p>
<ol>
<li>初始化训练数据的权值分布<br>
$$<br>
D_1 = (w_{11}, \dots, w_{1i}, \dots,w_{1N}), w_{1i} = \frac{1}{N}<br>
$$</li>
<li>对于$m = 1,2,\dots,M$
<ul>
<li>使用具有权值分布$D_m$的训练数据集学习，得到基本分类器<br>
$$<br>
G_m(x): \mathcal{X} \rightarrow { -1, +1 }<br>
$$</li>
<li>计算$G_m(x)$ 在训练数据集上的分类误差率<br>
$$<br>
e_m = \sum_{i=1}^N P(G_m(x_i) \neq y_i) = \sum_{i=1}^N w_{mi} I(G_m(x_i) \neq y_i)<br>
$$</li>
<li>计算$G_m(x)$的系数:<br>
$$<br>
\alpha_m = \frac{1}{2} \log \frac{1 - e_m}{e_m}<br>
$$</li>
</ul>
</li>
</ol>
<ul>
<li>更新训练数据集的权值分布<br>
$$<br>
\begin{aligned}<br>
D_{m+1} &amp;= (w_{m+1,1},\dots, w_{m+1,i}, \dots,w_{m+1,N}) \<br>
w_{m+1,i} &amp;= \frac{w_{m+1,i}}{Z_{m}} \exp(- \alpha_m y_i G_m(x_i)) \<br>
Z_m &amp;= \sum_{i=1}^N w_{mi} \exp(-\alpha_m y_i G_m(x_i))<br>
\end{aligned}<br>
$$</li>
<li>构建基本分类器的线性组合:<br>
$$<br>
f(x) = \sum_{m=1}^M \alpha_m G_m(x)<br>
$$<br>
得到最终分类器：<br>
$$<br>
G(x) = sign(f(x)) = sign(\sum_{m=1}^M \alpha_m G_m(x))<br>
$$</li>
</ul>
</blockquote>
<p>下面对AdaBoost算法说明如下:</p>
<ul>
<li>在最开始假设训练数据具有均匀的权值分布，即每个训练样本在基本分类器中的作用相同，这一假设保证第一步能够在原始数据上学习基本分类器$G_1(x)$</li>
<li>AdaBoost反复学习基本分类器，在每一轮弱分类器训练中，首先使用当前分布$D_m$加权的训练数据集，学习基本分类器$G_m(x)$,使用加权数据进行训练，最终学得的分类器更加关注权重较高的数据，接下来计算该基本分类器$G_m(x)$在加权训练数据集上的分类误差率,然后通过该误差率计算分类器$G_m(x)$的权重，从权重计算公式可以看出，当$e_m \leq 0.5$(弱分类器前提假设)时，$\alpha_m \geq 0$,且随着$e_m$减小，$\alpha_m$会不断增大。</li>
<li>接下来更新训练数据的权值，对于被分类器$G_m(x)$分类正确的样本，增加其权重，反之减少其权重。<strong>不改变所给的训练数据，而是不断改变训练数据权值的分布，使得训练数据在基本分类器的学习中起不同作用。</strong></li>
<li>最终的组合分类器是一个基本分类器的线性组合形式，$f(x)$的符号决定了实例$x$的类，而$f(x)$的绝对值则显示了分类结果的置信度。</li>
</ul>
<h4 id="算法理论说明">算法理论说明</h4>
<p>上一部分从算法思想来对AdaBoost进行理解，这一部分将从理论推导方面证明AdaBoost中的一些关键公式是如何来的，比如：</p>
<ul>
<li>分类器权重$\alpha_m = \frac{1}{2} \log \frac{1-e_m}{e_m}$</li>
<li>样本点权重更新公式：<br>
$$<br>
\begin{aligned}<br>
D_{m+1} &amp;= (w_{m+1,1},\dots, w_{m+1,i}, \dots,w_{m+1,N}) \<br>
w_{m+1,i} &amp;= \frac{w_{m+1,i}}{Z_{m}} \exp(- \alpha_m y_i G_m(x_i)) \<br>
Z_m &amp;= \sum_{i=1}^N w_{mi} \exp(-\alpha_m y_i G_m(x_i))<br>
\end{aligned}<br>
$$</li>
</ul>
<h5 id="AdaBoost算法训练误差界">AdaBoost算法训练误差界</h5>
<p>首先来推导下AdaBoost的训练误差界，样本$x_i$ 在第$t$轮训练时的权重，由样本点权重递推公式可以得到：<br>
$$<br>
w_{t+1i} = w_{ti} \frac{e^{-\alpha_t y_i h_t(i)}}{Z_{t}} = \dots = w_{1i} \frac{e^{-y_i \sum_{j=1}^t \alpha_j h_j(i)}}{\prod_{i=1}^{t} Z_i}<br>
$$<br>
其中$w_{1i} = \frac{1}{N}$,若记：<br>
$$<br>
\boldsymbol{\alpha} = [\alpha_1,\alpha_2,\dots, \alpha_t], \boldsymbol{h(x)} = [h_1(i),h_2(i),\dots, h_t(i)]<br>
$$<br>
$w_{t+1i}$可以写成更加紧凑的形式:<br>
$$<br>
w_{t+1i} = \frac{e^{-y_i &lt;\boldsymbol{\alpha}, \boldsymbol{h(x)}&gt;}}{N \prod_{i=1}^t Z_i}<br>
$$<br>
又因为有$\sum_{i=1}^N w_{t+1i} = 1$,对上式求和后可得：<br>
$$<br>
\prod_{i=1}^t Z_i = \frac{1}{N} \sum_{i=1}^N e^{-y_i &lt;\boldsymbol{\alpha}, \boldsymbol{h(x)}&gt;}<br>
$$<br>
需要注意的是，$&lt;\boldsymbol{\alpha}, \boldsymbol{h(x)}&gt;$刚好是使用AdaBoost最终得到模型的预测结果(未取sign)，下面考虑两种情况：<br>
$$<br>
\begin{aligned}<br>
&amp;If \quad sign(&lt;\boldsymbol{\alpha}, \boldsymbol{h(x_i)}&gt;) = h(x_i) = y_i \Rightarrow &lt;\boldsymbol{\alpha}, \boldsymbol{h(x)}&gt; \cdot y_i = c \geq 0  , 0 \leq e^{-c}  \<br>
&amp;If \quad sign(&lt;\boldsymbol{\alpha}, \boldsymbol{h(x_i)}&gt;) = h(x_i) \neq y_i \Rightarrow &lt;\boldsymbol{\alpha}, \boldsymbol{h(x)}&gt; \cdot y_i = c \leq 0  , 1 \leq e^{-c}<br>
\end{aligned}<br>
$$<br>
而AdaBoost最终训练得到的模型的分类误差为:<br>
$$<br>
\epsilon  = \frac{1}{N}\sum_{i=1}^N \epsilon_i, \epsilon_i = \begin{cases}<br>
1 &amp; h(x_i) \neq y_i \<br>
0 &amp; h(x_i) = y_i<br>
\end{cases}<br>
$$<br>
从前面两种分类讨论可以得到:<br>
$$<br>
\epsilon_i \leq e^{-y_i &lt;\boldsymbol{\alpha}, \boldsymbol{h(x_i)}&gt;}<br>
$$<br>
由此便可以得到AdaBoost分类器的<strong>分类错误率上界</strong>：<br>
$$<br>
\epsilon = \frac{1}{N}\sum_{i=1}^N \epsilon_i \leq \frac{1}{N} \sum_{i=1}^N e^{-y_i &lt;\boldsymbol{\alpha}, \boldsymbol{h(x_i)}&gt;}  = \prod_{i=1}^t Z_i<br>
$$</p>
<h5 id="alpha-t-ast-推导">$\alpha_t^\ast$ 推导</h5>
<p>需要注意的是，我们上面并未指定权重$\alpha$的表达式，那么现在我们考虑这样一个问题:</p>
<blockquote>
<p>$\alpha$取什么值时分类错误率的上界可以尽可能小</p>
</blockquote>
<p>因为$Z_i$的大小依赖于$\alpha_i$，而且它们都是迭代过程中的变量，因此可以考虑将上述问题转化为:</p>
<blockquote>
<p>如何在每一步迭代中选择合适的$\alpha_t$使得$Z_t$尽可能小</p>
</blockquote>
<p>$Z_t$的表达式如下:<br>
$$<br>
Z_t = \sum_{i=1}^N w_{ti} e^{-\alpha_t y_i h_t(x_i)}<br>
$$<br>
这是一个凸函数，因此可以直接求导令导数等于0：<br>
$$<br>
\frac{\partial Z_t}{\partial \alpha_t} = -\sum_{i=1}^N w_{ti} y_i h_t(x_i) e^{-\alpha_t y_i h_t(x_i)} = 0<br>
$$<br>
下面考虑两个集合：<br>
$$<br>
A = y_i h_t(x_i) = 1, \bar{A} = y_i h_t(x_i) = -1<br>
$$<br>
据此，上面的偏导可以写成:<br>
$$<br>
\frac{\partial Z_t}{\partial \alpha_t} = -\sum_{x_i \in A} w_{ti} e^{-\alpha_t} + \sum_{x_i \in \bar{A}} w_{ti} e^{\alpha} = 0<br>
$$<br>
由此可得:<br>
$$<br>
e^{2\alpha_t} = \frac{\sum_{x_i \in A} w_{ti}}{\sum_{x_i \in \bar{A}} w_{ti}} = \frac{1 -\epsilon_t}{\epsilon_t}<br>
$$<br>
求得$\alpha_t$的最优取值为:<br>
$$<br>
\alpha_t^\ast = \frac{1}{2} \log \frac{1 - \epsilon_t}{\epsilon_t}<br>
$$<br>
至此我们便理解了每一步迭代$\alpha_t$表达式的来源，该表达式刚好可以保证最终得到的分类器错误率上界尽可能小(局部最小)。</p>
<h5 id="训练误差界-二分类问题">训练误差界(二分类问题)</h5>
<p>对于一个二分类问题，则可以将$Z_t$的表达式直接写出:<br>
$$<br>
Z_t = 2 \sqrt{e_t(1 - e_t)}<br>
$$<br>
令$e_t = \frac{1}{2} - \gamma_t$,则可得：<br>
$$<br>
Z_t = \sqrt{1 - 4 \gamma_t^2} \leq e^{-2 \gamma_t^2}<br>
$$<br>
由此便可得分类错误率上界为:<br>
$$<br>
\prod_{i=1}^t Z_i \leq e^{-2 \sum_{i=1}^t \gamma_t^2}<br>
$$<br>
据此可以得到推论:</p>
<blockquote>
<p><strong>推论：</strong> 如果存在$\gamma &gt;0$,对所有$i$有，$\gamma_i \geq \gamma$,则有：<br>
$$<br>
\frac{1}{N} \sum_{i=1}^N I(h(x_i) \neq y_i) \leq \exp(-2N\gamma^2)<br>
$$<br>
这说明对于二分类问题，AdaBoost的训练误差是指数速率下降的。</p>
</blockquote>
<p><strong>tips:</strong></p>
<ul>
<li>对于无法接受带权样本的基学习算法，可以通过重采样法来处理，即在每一轮学习中，根据样本分布对训练集重新进行采样，变相实现带权样本，两种做法没有本质区别。</li>
<li>Boosting算法在训练的每一轮都需要检查当前生成的基学习器是否满足条件(比随机猜测要好)，若不满足，则抛弃该基学习器，学习过程停止，若采用&quot;重采样法&quot;，则可获得“重启动机会”，避免算法过早停止。</li>
</ul>
<h3 id="随机森林">随机森林</h3>
<p>随机森林算法其实就是以决策树算法作为基学习算法，选用Bagging策略作为提升策略，最终得到的一个算法，可以这么理解:<br>
$$<br>
随机森林 = 决策树 + Bagging<br>
$$<br>
随机森林算法简单、容易实现、计算开销小，令人惊奇的是，它在很多现实任务中展现了强大的性能，被誉为“代表集成学习技术水平的方法”。<br>
不过传统的Bagging算法仅仅是在训练样本上引入随机性，而随机森林往往考虑在属性选择上引入随机性，具体做法是:原始的决策树算法是从该节点所有属性中选择一个最优属性进行节点分裂，而作为基学习器，为进一步引入随机性，一般会规定从当前节点所有属性中随机选$k$个，然后从这$k$个属性中选择一个最优的来分裂节点，若该节点有$d$个属性，常见的$k$的取值为$k = \log_2 d$</p>
<h3 id="提升算法解释">提升算法解释</h3>
<p>学习器结合可能会从三个方面带来好处:</p>
<ul>
<li>从统计的方面来看，由于学习任务的假设空间往往非常大，此时若使用单学习器可能因误选而导致泛化性能不佳，结合多个学习器会减少这一风险。</li>
<li>从计算角度来看，学习算法往往会陷入局部极小，有的局部极小点所对应的泛化性能可能非常糟糕，而通过多次运行之后进行结合，可降低陷入糟糕极小点的风险。</li>
<li>从表示的方面来看，某些学习任务的真实假设可能不在当前学习算法s所考虑的假设空间中，此时若使用单学习器肯定无效，而通过结合多个学习器，由于相应的假设空间有所扩大，有可能学得更好的近似。</li>
</ul>
<p><img src="https://raw.githubusercontent.com/xuejy19/xuejy19.github.io/source/Img/Ensemble.png" alt="集成学习解释"></p>
<h3 id="总结">总结</h3>
<p>该部分主要总结下两个方面的内容，一个是结合策略的选择，另一个则是引入扰动的思路。</p>
<h4 id="结合策略">结合策略</h4>
<p>结合策略主要有以下几种:</p>
<ul>
<li>平均法,包括简单平均法和加权平均法，一般而言，在个体学习器性能相差较大时，加权平均法表现较好，在个体学习器性能接近时采用简单平均法较好。</li>
<li>投票法,包括绝对多数投票法(设定得票率阈值)，相对多数投票法，加权投票法</li>
<li>学习法，即通过另一个学习器来结合这些基于学习器，该学习器被称为元学习器或者次级学习器，基学习器被称作初级学习器，初级学习器的输出作为次级学习器的输入。</li>
</ul>
<h4 id="引入扰动">引入扰动</h4>
<p>常见的引入扰动的思路有如下几种:</p>
<ul>
<li>数据样本扰动</li>
<li>输入属性扰动</li>
<li>输出表示扰动</li>
<li>算法参数扰动</li>
</ul>
]]></content>
      <categories>
        <category>统计学习</category>
      </categories>
      <tags>
        <tag>Bagging, AdaBoost, 提升树模型</tag>
      </tags>
  </entry>
  <entry>
    <title>疫中居家</title>
    <url>/2020/02/17/yi-zhong-ju-jia/</url>
    <content><![CDATA[<blockquote>
<p>庚子年春，适新冠肆虐，余宅家无所事，聊以为文，以抒心意</p>
</blockquote>
<p> 新冠者，源于蝠属，起于武汉，适逢春运，以至蔓于天下，一时人心浮动，谈冠色变。遥忆己亥岁末，余初闻新冠，以其癣疥之疾，然须臾，武汉闭城，举国百姓皆锁足于室，医者仁心，挽袖前线。<br>
 余宅家，手机为伴，藉网络以察新冠其事，或感动，或痛心，或苦笑。新冠者，镜也，映崇高精神，映无用之官，映复杂人心。余窃以为，此诚危急之秋也，宜上下一下，共度难关，则疫情之终，不日将至矣。<br>
 宅家不可碌碌终日，会余察己日本史所知甚浅，故阅日本史数册以会其意。以吾观之，詹姆斯之《日本史》堪称佳作，然其古史不足，实为大憾；吴廷璆之《日本史》内容翔实，然书中偏见少许，是其瑕疵。纵观日本史，源义经、楠木正成者，皆兵败身殒，然以其悲情命运而传美名于后世；丰臣秀吉者，生前位极人臣，然其后嗣绝于家康之手；德川家康者，韬光养晦，创江户幕府基业，死后被视为神（东照大神），然200余年后，倒幕声起，幕府轰然倒地，故所谓千秋万代者，实为儿戏之言。历史之回响，不过“诸事无常”四字耳。<br>
 愿新冠之终结，医者之凯旋，天下之安康</p>
]]></content>
      <categories>
        <category>闲谈</category>
      </categories>
      <tags>
        <tag>疫中有感</tag>
      </tags>
  </entry>
  <entry>
    <title>线性判别分析</title>
    <url>/2020/09/01/te-zheng-ti-qu-yu-te-zheng-xuan-ze/</url>
    <content><![CDATA[<p>线性判别分析(LDA)是一种经典的线性学习方法，在二分类问题上最早由fisher提出，因此线性判别分析又称Fisher线性判别，该部分按照以下结构组织:</p>
<ul>
<li>LDA算法思想</li>
<li>LDA算法推导</li>
<li>多分类任务</li>
</ul>
<a id="more"></a> 
<h3 id="算法思想">算法思想</h3>
<p>LDA的思想非常朴素：给定训练样例集，设法将样例投影到一条直线上，使得同类样例的投影点尽可能接近、异类样例的投影点尽可能远离，在对新的样本进行分类时，首先将其投影到同样的这条直线上，再根据投影点的位置来确定新样本的类别。<br>
<img src="https://raw.githubusercontent.com/xuejy19/xuejy19.github.io/source/Img/LDA.png" alt="LDA"></p>
<h3 id="理论推导">理论推导</h3>
<h4 id="投影方向求解">投影方向求解</h4>
<p>假设共有$N$个样本点$x_1,x_2,\dots,x_N$,其中有$N_1$个样本属于类别1，$N_2$个样本属于类别2，$N_1 + N_2 = N$, 记第$i$类样本点的均值为$\mu_i$,协方差矩阵为$\Sigma_i$,则有：<br>
$$<br>
\begin{aligned}<br>
\mu_i &amp;= \frac{1}{N_i} \sum_{x_i \in \mathcal{C_i}} x_i \<br>
\Sigma_i &amp;= \frac{1}{N_i} \sum_{x_i \in \mathcal{C_i}} (x_i - \mu_i)(x_i - \mu_i)^T<br>
\end{aligned}<br>
$$<br>
当将这些样本点向$w$投影后，投影后得到的点为$\tilde{x}_i = w^T x$,记此时类别$i$的均值为$\tilde{\mu}<em>i$,方差为$\sigma^2_i$,由均值方差计算公式可得:<br>
$$<br>
\begin{aligned}<br>
\tilde{\mu}<em>i &amp;= \frac{1}{N_i} \sum</em>{x_i \in \mathcal{C_i}} \tilde{x}<em>i = w^T \frac{1}{N} \sum</em>{x_i \in \mathcal{C_i}}  x_i = w^T \mu_i  \<br>
\sigma^2_i &amp;= \frac{1}{N_i} \sum</em>{x_i \in \mathcal{C_i}}(\tilde{x}<em>i - \tilde{\mu<br>
}<em>i)^2 = \frac{1}{N_i} \sum</em>{x_i \in \mathcal{C_i}} (w^T x_i - w^T \mu_i)^2 \<br>
&amp;= w^T [\frac{1}{N_i} \sum</em>{x_i \in \mathcal{C_i}} (x_i - \mu_i)(x_i - \mu_i)^T] w \<br>
&amp;= w^T \Sigma_i w<br>
\end{aligned}<br>
$$<br>
LDA有两个目标，一个是同类的点投影后尽可能接近，另一个目标则是不同类的点尽可能远离：</p>
<ul>
<li>第一个目标我们通过投影后各类点的方差来进行刻画，使同类点的方差尽可能小:<br>
$$<br>
\min \quad w^T \Sigma_1 w + w^T \Sigma_2 w<br>
$$</li>
<li>第二个目标通过不同类的均值尽可能远来进行刻画,使不同类的均值尽可能远:<br>
$$<br>
\max \quad (w^T \mu_1 - w^T \mu_2)^2 = w^T (\mu_1 - \mu_2)(\mu_1 - \mu_2)^T w<br>
$$</li>
</ul>
<p>这两个目标可以通过一个目标函数来刻画：<br>
$$<br>
\max \quad \frac{w^T (\mu_1 - \mu_2)(\mu_1 - \mu_2)^T w}{w^T \Sigma_1 w + w^T \Sigma_2 w }<br>
$$<br>
下面做如下标记,记$S_w = \Sigma_1 + \Sigma_2$为类内离散度矩阵，记$S_b = (\mu_1 - \mu_2)(\mu_1 - \mu_2)^T$ 为类间离散度矩阵，由此便得目标函数形式为:<br>
$$<br>
\max \quad \frac{w^T S_b w}{w^T S_w w}<br>
$$<br>
这是一个广义瑞利熵形式的优化问题，因为分子分母均是$w$的二次项，因此该优化问题的解仅与$w$的方向有关(因为用$cw$ 替代$w$并不影响目标函数的值)，不失一般性，可以限制$w^T S_w w = 1$，上述优化问题可以写为:<br>
$$<br>
\max \quad w^T S_b w  \quad s.t. \quad w^T S_w w = 1<br>
$$<br>
该优化问题可以通过拉格朗日乘子法来进行求解,首先写出拉格朗日函数：<br>
$$<br>
L(w,\lambda) = w^T S_b w - \lambda (w^T S_w w - 1)<br>
$$<br>
上式对$w$求导可得:<br>
$$<br>
\frac{\partial L(w, \lambda)}{\partial w} = S_b w - \lambda S_w w<br>
$$<br>
令偏导等于0，可得$w$应当满足条件:<br>
$$<br>
S_b w = \lambda S_w w<br>
$$<br>
其中$S_b w = (\mu_1 - \mu_2)(\mu_1 - \mu_2)^Tw = c(u_1 - u_2)$,又因为前面讨论过优化目标的取值仅仅与$w$的方向有关，因此不妨取$c = \lambda$,可得:<br>
$$<br>
\mu_1 - \mu_2 = S_w w<br>
$$<br>
因此可得最终$w$最优取值:<br>
$$w^\ast = S_w^{-1}(\mu_1 - \mu_2)$$<br>
这里需要说明一下，因为从前面我们可以推出这样一个表达式$S_w^{-1} S_b w = \lambda w$,这说明$w$应当是矩阵$S_w^{-1} S_b$的特征值，目标函数最大值应当是该矩阵的最大的特征值，最优$w$应当是该最大特征值所对应的特征向量，但后面求解为何没有没有从这个角度入手，这是该问题的特殊性，为解释这一点，首先看$S_b$表达式:<br>
$$<br>
S_b = (\mu_1 - \mu_2)(\mu_1 - \mu_2)^T<br>
$$<br>
这说明矩阵$S_b$的秩等于1($\mu_1 \neq \mu_2$),所以$S_w^{-1}S_b$秩也为1，这说明该矩阵有且仅有一个大于0的特征值，前面的推导相当于是构造性的给出了该最大特征值及对应特征向量表达式,将$w^\ast$代入$S_w^{-1}S_b w$:<br>
$$<br>
\begin{aligned}<br>
S_w^{-1} S_b w^\ast &amp;= S_w^{-1} (\mu_1 - \mu_2)(\mu_1 - \mu_2)^T S_w^{-1}(\mu_1 - \mu_2) \<br>
&amp;= C S_w^{-1}(\mu_1 -\mu_2)<br>
\end{aligned}<br>
$$<br>
其中$C = (\mu_1 - \mu_2)^TS_w^{-1}(\mu_1 - \mu_2) &gt;0 $($S_w$对称且可逆，推出其正定)，这说明$S_w^{-1}S_b$的最大特征值就是$C$,对应的特征向量刚好是$w^\ast$!<br>
考虑到数值的稳定性，在实践中往往会首先对$S_w$进行特征值分解$S_w = U \Sigma U^T$,然后再求逆。</p>
<h4 id="在线分类">在线分类</h4>
<p>在得到最佳投影向量后，需要设定一个策略，来对新来的样本进行分类，该策略选择通常有以下几种:</p>
<ul>
<li>设定阈值，阈值选择通常有以下两种:
<ul>
<li>均值取平均<br>
$$t = \frac{\tilde{\mu}_1 + \tilde{\mu}_2}{2}$$</li>
<li>根据样本点个数进行加权<br>
$$ t = \frac{N_2 \tilde{\mu}_1 + N_1 \tilde{\mu}_2}{N_1 + N_2}$$</li>
</ul>
</li>
<li>在一维上估计概率密度函数，用Bayes决策方法分类</li>
</ul>
<h3 id="多分类任务">多分类任务</h3>
<p>LDA 也可以推广到多分类任务中，假定存在$N$类，且第$i$类的样本数为$m_i$,首先定义全局散度矩阵:<br>
$$<br>
S_t = S_w + S_b = \sum_{i=1}^m(x_i - \mu)(x_i - \mu)^T<br>
$$<br>
其中$\mu$ 是所有样本点的均值，将类内离散度矩阵定义为每个类别的离散度矩阵之和:<br>
$$<br>
S_w = \sum_{i=1}^N S_{w_i}, \quad S_{w_i} = \sum_{x \in \mathcal{X_i}} (x - \mu_i)(x - \mu_i)^T<br>
$$<br>
然后求出类间离散度矩阵表达式:<br>
$$<br>
S_b = S_t -S_w = \sum_{i=1}^N m_i(\mu_i - \mu)(\mu_i - \mu)^T<br>
$$<br>
优化目标函数可以写成:<br>
$$<br>
\max_{W} \frac{tr(W^T S_b W)}{tr(W^T S_w W)}<br>
$$<br>
该问题的求导与前面一样，同样根据$W$的尺度不变性将上述优化问题转化为：<br>
$$<br>
\max_{W} \quad tr(W^T S_b W) \quad s.t. tr(W^T S_w W) = 1<br>
$$<br>
应用拉格朗日乘子法，并对$W$求导令导数为0可得:<br>
$$<br>
S_b W = \lambda S_w W<br>
$$<br>
$W$的闭式解则是$S_w^{-1} S_b$的$d’$个最大非零广义特征值所对应的特征向量组成的矩阵，需要注意的是最终降的维数应当满足$d’ \leq N-1$,这是因为矩阵$S_b$本身最大只有$N-1$维，因此$S_w^{-1}S_b$至多有$N-1$个非零特征值。<br>
若将$W$视为一个投影矩阵，则多分类LDA相当于将样本投影到$d’$空间，$d’$通常远小于数据原有的属性数$d$,于是可以通过这个投影来达到讲维的目的，同时这个过程中也应用了类别的信息，因此$LDA$也常被视为一种经典的监督降维技术。</p>
]]></content>
      <categories>
        <category>统计学习</category>
      </categories>
      <tags>
        <tag>线性判别分析</tag>
      </tags>
  </entry>
  <entry>
    <title>规则学习</title>
    <url>/2020/08/27/gui-ze-xue-xi/</url>
    <content><![CDATA[<p>首先讲一个有意思的现象，早期的主流人工智能专注于以逻辑为基础来进行形式化和推理，但这样很难定量地对不确定事件进行表达和处理，后来随着机器学习算法的井喷，大家都更加关注于对定量的数据进行处理。但现在，很多人发现在解决某些任务的时候，加入一些行业中的常识知识可能会让任务解决地更好，而现在定性知识与定量数据之间似乎有着一道鸿沟，唯一的联系就是进行算法设计的人可能会依赖于他的领域知识来选择合适的算法并对算法进行调参。基于学习下定性知识如何表达的想法，这部分介绍下周志华老师🍉书的倒数第二章——规则学习，这部分内容安扎以下结构组织:</p>
<ul>
<li>基本概念</li>
<li>序贯覆盖</li>
<li>剪枝优化</li>
<li>一阶规则学习</li>
<li>归纳逻辑程序设计</li>
</ul>
<a id="more"></a>
<h3 id="基本概念">基本概念</h3>
<p>首先给出机器学习中规则和规则学习的概念：</p>
<blockquote>
<p><strong>规则:</strong> 在机器学习领域中，“规则”通常是指语义明确、能描述数据分布所隐含的客观规律或领域概念，可以写成&quot;若…,则…&quot;形式的逻辑规则。<br>
<strong>规则学习:</strong> “规则学习”是从训练数据中学习出一组能用于对未见示例进行判别的规则。</p>
</blockquote>
<p>形式化来看，一条规则形如：<br>
$$<br>
\oplus \leftarrow f_1 \land f_2 \dots \land f_L<br>
$$<br>
公式中$\leftarrow$右侧部分称为&quot;规则体&quot;，表示该条规则的前提，左边部分称为&quot;规则头&quot;,表示该条规则的结果。规则体是由逻辑文字$f_k$所组成的合取式(conjuction)，每个文字$f_k$都是对示例属性进行检验的布尔表达式，例如&quot;(色泽 = 乌黑)“或者”$\neg$(根蒂 = 硬挺)&quot;。$L$是规则体中逻辑文字的个数,称为规则的长度,规则头的&quot;$\oplus$&quot;,同样是逻辑文字，一般用来表示所判定的目标的类别或概念，比如&quot;好瓜&quot;，这样的逻辑规则也被称为<strong>if-then规则</strong>。<br>
与神经网络、支持向量机这样的&quot;黑箱模型&quot;相比，规则学习具有以下几个优点:</p>
<ul>
<li>可解释性较强</li>
<li>很强的表达能力，绝大多数的人类知识都能够通过数理逻辑进行简洁的刻画和表达，例如&quot;父亲的父亲是爷爷&quot;这样的知识不易用函数式表达，而用一阶逻辑则可以方便地写为：<br>
$$<br>
爷爷(X,Y) \leftarrow 父亲(X,Z) \land 父亲(Z,Y)<br>
$$</li>
<li>逻辑规则的抽象描述能力在处理一些高度复杂的AI任务时具有显著的优势，例如在问答系统中有时可能遇到非常多、甚至无穷种可能的答案，此时若能基于逻辑规则进行抽象表述或者推理，则将带来极大的便利。</li>
</ul>
<p>假设我们从西瓜数据集中学得规则集合$\mathcal{R}$:<br>
$$<br>
\begin{aligned}<br>
&amp;规则1: 好瓜 \leftarrow (根蒂 = 蜷缩) \land (脐部 = 凹陷) \<br>
&amp;规则2: \neg好瓜 \leftarrow (纹理 = 模糊)<br>
\end{aligned}<br>
$$<br>
规则集合中的每条规则都可看作一个子模型，规则集合是这些子模型的一个集成。当同一个示例被判别结果不同的多条规则覆盖时，则称发生了“冲突”，解决冲突的办法则称为&quot;冲突消解&quot;，常用的冲突消解的方法有投票法、排序法、元规则法，投票法就是常用的少数服从多数策略，排序法则是指定规则优先级，出现冲突时使用排序靠前的规则。所谓元规则，其实就是规则的规则，例如&quot;发生冲突时使用长度最小的规则“，然后根据元规则的指导来使用规则集。<br>
此外，从训练集学得的规则集合也许并不能覆盖所有可能的未见示例，此时规则学习算法通常会设置一条默认规则，由它来处理规则集合未覆盖的样本；例如为$\mathcal{R}$增加一条默认规则：“未被规则1,2覆盖的都不是好瓜”。<br>
从形式语言表达能力而言，规则可分为两类：“命题规则”和“一阶规则”：</p>
<blockquote>
<ul>
<li>命题规则是由“原子命题(propositional atom)”和逻辑连接词“与($\land$)”、&quot;(或($\lor$))&quot;、“非($\neg$)”和“蕴含”($\leftarrow$)构成的简单陈述句；例如规则集$\mathcal{R}$就是一个命题规则集，“根蒂 = 蜷缩”，“脐部 = 凹陷”都是元子命题。</li>
<li>一阶规则的基本成分是能表述事物的属性或关系的&quot;原子公式&quot;，例如表达父子关系的谓词(predict)“父亲$(X,Y)$”就是原子公式，再如表示加1操作&quot;$\sigma(X) = X+1$“的函数$\sigma(X)$也是原子公式。如果进一步使用谓词“自然数$(X)$”表示$X$是自然数，“$\forall X$”表示对任意$X$成立，”$\exists Y$“表示&quot;存在$Y$使之成立”,那么&quot;所有自然数加1都是自然数&quot;就可以写做:<br>
$$<br>
\forall X, \exists Y  \leftarrow 自然数(X) \land (Y = \sigma(X))<br>
$$<br>
或者表示成更加简洁的形式:<br>
$$<br>
\forall X, 自然数(\sigma(X)) \leftarrow 自然数(X)<br>
$$<br>
这样的规则就是一阶规则，其中$X$和$Y$称为逻辑变量，$\forall,\exists$用于限定变量的取值范围，称为&quot;量词&quot;，显然，一阶规则可以表达复杂的关系，因此也被称为“关系型规则”。若我们简单地把属性当作谓词来定义示例与属性值之间的关系，则命题规则集$\mathcal{R}$可以改写为一阶规则集$R’$:<br>
$$<br>
\begin{aligned}<br>
&amp;规则1: 好瓜(X) \leftarrow 根蒂(X,蜷缩) \land 脐部(X,凹陷) \<br>
&amp;规则2: \neg 好瓜(X) \leftarrow 纹理(X,模糊)<br>
\end{aligned}<br>
$$<br>
从形式语言系统角度来看，命题规则是一阶规则的特例，因此一阶规则的学习比命题规则要复杂得多。</li>
</ul>
</blockquote>
<h3 id="序贯覆盖">序贯覆盖</h3>
<p>规则学习的目标是产生一个可以覆盖尽可能多样例的规则集，最直接的做法就是“序贯覆盖”，即逐条归纳:在训练集上每学到一条规则，就将该规则覆盖的训练样例去除，然后以剩下的训练样例组成训练集重复上述过程。由于每次只处理一部分数据，因此也被称为&quot;分治&quot;策略。<br>
序贯覆盖法的关键是如何从训练集学出单条规则，显然，对规则学习目标$\oplus$ 产生一条规则就是寻找最优的一组逻辑文字构成规则体，这是一个搜索问题。形式化上来说，给定正例集合与反例集合，学习任务是基于候选文字集合$\mathcal{F} = { f_k }$来生成最优规则$r$。在命题规则学习中，候选文字是形如&quot;$R(属性_i,属性值_{ij})$“的布尔表达式，$R(x,y)$则是判断$x,y$是否满足关系$R$的二元布尔函数。<br>
而具体的学习方法则是从空规则$”\oplus \leftarrow&quot;$开始，将正例类别作为规则头，再逐个遍历训练集中的每个属性及取值，尝试将其作为逻辑文字增加到规则体中，若能使当前规则体仅覆盖正例，由此产生一条规则，然后去除已被覆盖的正例并基于剩余样本生成下一个规则：通俗来说，这种方法的思想便是挑选出所有仅覆盖正例的特征和仅覆盖反例的特征。<br>
而原始的穷尽搜索的做法在属性和候选值较多时会由于组合爆炸而不可行,现实任务中一般有两种策略来产生规则:</p>
<ul>
<li>自顶向下:从比较一般的规则开始，逐渐添加新文字以缩小规则覆盖范围，直到满足预定条件为止，该策略也称为“生成测试”法，是规则逐渐特化的过程。</li>
<li>自底向上:从比较特殊的规则开始,逐渐删除文字以扩大规则覆盖范围，直到满足条件为止，该种策略是规则逐渐泛化的过程。</li>
</ul>
<h3 id="剪枝优化">剪枝优化</h3>
<p>规则生成本质上是一个贪心搜索的过程，需有一定的机制来缓解过拟合的风险，最常见的做法是剪枝。与决策树相似，剪枝可以分为预剪枝与后剪枝。在实际操作时通常是基于某种性能度量指标来评估增/删逻辑文字前后的规则性能，或增/删规则集前后的规则集性能，从而判断是否需要进行剪枝。<br>
经常使用的预剪枝算法为$CN2$算法，使用似然率统计量作为指标；后剪枝常用策略为“减错剪枝”，即将样例集进行划分，划分为训练集和验证集，根据在验证集上的表现优劣来对从训练集中学出的模型进行剪枝。、</p>
<h3 id="一阶规则学习">一阶规则学习</h3>
<p>命题逻辑的基本单元是“原子命题”，这也就导致命题规则学习很难处理对象之间的&quot;关系&quot;，而关系信息在很多任务中非常重要。例如，当我们在瓜摊挑瓜时,通常很难把水果摊上所有西瓜的特征用属性值表示出来，因为我们很难判断:色泽看起来多深才叫&quot;色泽青绿&quot;？敲起来声音多低才叫&quot;敲声沉闷&quot;？比较现实的做法是将西瓜进行比较，例如，“瓜1的颜色比瓜2更深，并且瓜1的根蒂比瓜2更蜷”，因此“瓜1比瓜2更好”。然而，这已经超越了命题逻辑的表达能力，需要用一阶逻辑来表达，并且要使用一阶规则进行学习。</p>
<p>首先我们需要将命题逻辑数据转换为关系数据，比如对于瓜的色泽，我们可以定义:<br>
$$<br>
色泽深度：乌黑&gt;青绿&gt;浅白<br>
$$<br>
那么“瓜1色泽乌黑，是好瓜；瓜2色泽青绿，不是好瓜”，就可以表示为:<br>
$$<br>
色泽更深(瓜1，瓜2)，更好(瓜1，瓜2)<br>
$$<br>
这样的数据直接描述了样例之间的关系，称为“关系数据”，其中由原样本属性转化而来的“色泽更深”、“根蒂更蜷”等原子公式称为“背景知识”，而由样本类别转化而来的关于“更好”，“$\neg$更好”等原子数据称为关系数据样例，下面给出一个一阶规则示例：<br>
$$<br>
(\forall X,\forall Y)(更好(X,Y)) \leftarrow 根蒂更蜷(X,Y) \land 脐部更凹(X,Y)<br>
$$<br>
与命题规则相比，一阶规则的规则头、规则体都是一阶逻辑表达式，个体对象&quot;瓜1，瓜2&quot;被逻辑变量$X,Y$替换，全称量词$\forall$ 表示该规则对所有个体对象均成立；通常，在一阶规则中所有出现的变量都被全称量词限定，所以有时会将全称量词省去,一阶规则有着强大的表达能力，例如它能简洁地表达递归概念，如:<br>
$$<br>
更好(X,Y) \leftarrow 更好(X,Z) \land 更好(Z,Y)<br>
$$<br>
一阶规则学习能够容易地引入领域知识，这是它相对于命题规则学习的另一大优势。在命题规则学习乃至一般的统计学习中，若欲引入领域知识，通常有两种做法:<strong>在现有属性的基础上基于领域知识构造出新属性，或基于领域知识设计某种函数机制(如正则化)来对假设。</strong> 然而现实任务中并非所有的领域知识都能够容易地通过属性重构和函数约束来表达。例如，假定我们获得包含了某未知元素的化合物$X$，欲通过实验来发现它与已知化合物$Y$的化学表达式,我们可以重复多次实验，测出每次结果中化合物的组份含量，虽然我们对反应中的未知元素的性质一无所知，但知道一些普遍成立的化学原理，例如金属原子一般产生离子键、氢原子之间一般都是共价键，并且也了解已知元素间可能发生反应，这些领域知识非常重要，但往往很难在基于命题表示的学习中加以利用。<br>
下面介绍一种著名的一阶规则学习算法</p>
<h4 id="First-Order-inductive-Learner">First-Order inductive Learner</h4>
<p>FOIL是著名的一阶规则学习算法，它遵循序贯覆盖框架且采用自顶向下的规则归纳策略，由于有逻辑变量的存在，FOIL在规则生成时需考虑不同的变量组合。这也是和命题规则所不同的地方,在命题规则的学习中只需要考虑原子命题之间的组合，而在一阶逻辑规则学习中，除了要考虑原子公式的组合，还应当考虑公式中变量的组合。<br>
FOIL使用“FOIL增益”来选择文字:<br>
$$<br>
F_{Gain} = \hat{m}<em>+ \times (\log_2 \frac{\hat{m}</em>+}{\hat{m}<em>+ + \hat{m}</em>-}-\log_2 \frac{m_+}{m_+ + m_-})<br>
$$</p>
<h3 id="总结">总结</h3>
<p>规则学习是&quot;符号主义学习&quot;，是最早开始研究的机器学习技术之一。序贯覆盖是规则学习的基本框架，CN2采用集述搜索，是最早考虑过拟合问题的规则学习算法，现实出后剪枝在缓解规则学习过拟合中的优势。<br>
由于命题规则的表述能力有限，一阶逻辑学习开始发展，FOIL通过变量替换等操作把命题规则学习转化为一阶规则学习。<br>
近年来随着机器学习技术进入更多应用领域，在富含结构信息和领域知识的任务中，逻辑表达的重要性逐渐凸显出来，因此出现了一些将规则学习与统计学习相结合的努力，例如给贝叶斯网中的节点赋予逻辑意义的“关系贝叶斯网络”、贝叶斯逻辑程序、马尔可夫逻辑网络，统称为&quot;统计关系学习&quot;。</p>
]]></content>
      <categories>
        <category>统计学习</category>
      </categories>
      <tags>
        <tag>规则学习，定性关联</tag>
      </tags>
  </entry>
  <entry>
    <title>线性判别函数</title>
    <url>/2020/08/11/xian-xing-pan-bie-han-shu/</url>
    <content><![CDATA[<p>在这一部分将总结一下关于感知机的一些相关知识点，不过首先我想要介绍下统计学习相关算法的一种分类标准。该部分内容将按照以下几部分进行组织：</p>
<blockquote>
<ul>
<li>模型分类-生成式模型与判别式模型</li>
<li>线性判别-感知机</li>
</ul>
</blockquote>
<a id="more"></a>
<h3 id="模型分类-生成式模型与判别模型">模型分类-生成式模型与判别模型</h3>
<p>监督学习的任务便是学习一个模型，应用这一模型，对给定的输入$X$预测相应的输出$Y$，该模型一般具有两种形式：</p>
<ul>
<li>$Y = f(X)$</li>
<li>$P = (Y|X)$</li>
</ul>
<p>第一种模型我们称为判别模型，是函数形式；后一种我们称为生成模型，为条件概率分布的形式。下面列一个表格来对常见机器学习算法按照该标准进行分类：</p>
<table>
<thead>
<tr>
<th>生成式模型</th>
<th>判别式模型</th>
</tr>
</thead>
<tbody>
<tr>
<td>朴素贝叶斯模型,HMM,GMM</td>
<td>感知机,SVM,k近邻,AdaBoost</td>
</tr>
</tbody>
</table>
<h3 id="线性判别-感知机">线性判别-感知机</h3>
<p>感知机模型是一种非常简单的判别式模型，该算法出发点有以下几点：</p>
<ul>
<li>二分类问题</li>
<li>假设存在一个超平面，可以将两类样本分开，即要求模型线性可分</li>
</ul>
<p>对于一个判别式模型，我们需要解决三个问题：</p>
<ol>
<li>明确模型类</li>
<li>确定准则函数</li>
<li>利用准则函数，结合数据进行模型学习</li>
</ol>
<h4 id="模型类">模型类</h4>
<p>首先给出感知机函数定义：</p>
<blockquote>
<p>假设输入空间(特征空间)是$\mathcal{X} \subseteq R^n$,输出空间是$\mathcal{Y} = {-1,+1}$。输入$x\in \mathcal{X}$表示实例的特征向量，对应于输入空间(特征空间)的点；输出$y \in \mathcal{Y}$表示实例的类别。由输入空间到输出空间的函数可表示为:<br>
$$<br>
f(x) = sign(\omega x+ b)<br>
$$</p>
</blockquote>
<p>感知机是一种线性分类模型，属于判别模型。感知机模型的假设空间是定义在特征空间中的所有线性分类模型或线性分类起，即函数集合${ f|f(x) = \omega x + b}$。</p>
<h4 id="准则函数-损失函数">准则函数(损失函数)</h4>
<p>损失函数的一个自然选择是误分类点个数。但是这样的损失函数不是参数$\omega,b$的连续可导函数，不易优化。损失函数的另一个选择则是误分类点到超平面的总距离，为此先写出输入空间$R^n$中任意一点$x_0$到超平面$S$的距离：<br>
$$<br>
d= \frac{|\omega x_0 +b|}{|| \omega ||}<br>
$$<br>
其次，对于误分类的数据$(x_i,y_i)$来说:<br>
$$<br>
-y_i(\omega \cdot x_i + b) &gt;0<br>
$$<br>
因此，误分类点$x_i$到超平面的距离为：<br>
$$<br>
\frac{-y_i(\omega x_i +b)}{|| \omega ||}<br>
$$<br>
因此，若假设超平面$S$的误分类点集合为$M$,那么所有误分类点到超平面$S$的距离为：<br>
$$<br>
L = -\frac{1}{||\omega||} \sum_{x_i \in M} y_i (\omega x_i + b)<br>
$$</p>
<p>由此可见，在该目标函数下，若所有样本均被正确分类，则损失函数为0,因此不管是带范数的几何间隔，还是去掉范数的函数间隔，最终都将收敛到0，为计算方便，因此考虑将损失函数中的$||\omega||$去掉，写出目标函数如下：<br>
$$<br>
L(\omega,b) = -\sum_{x_i \in M} y_i (\omega x_i + b)<br>
$$</p>
<h4 id="优化算法">优化算法</h4>
<p>在目标函数确定的情况下，对于该问题的优化考虑采用随机梯度下降法(SGD),首先求的目标函数对$\omega, b$的偏导：<br>
$$<br>
\begin{aligned}<br>
\nabla_{\omega} L(\omega,b) &amp;= -\sum_{x_i \in M}y_i x_i \\<br>
\nabla_{b} L(\omega,b) &amp;= -\sum_{x_i \in M} y_i<br>
\end{aligned}<br>
$$<br>
接下来对参数进行更新:<br>
$$<br>
\begin{aligned}<br>
\omega &amp;\leftarrow \omega + \eta y_i x_i  \\<br>
b &amp;\leftarrow b + \eta y_i<br>
\end{aligned}<br>
$$</p>
<h4 id="感知机算法收敛性">感知机算法收敛性</h4>
<p>在该部分将给出定理，但不加证明：</p>
<blockquote>
<p>设训练数据集是线性可分的，则：</p>
<ul>
<li>存在满足条件$||\omega|| = 1$的超平面将训练数据集完全分开，且存在$\gamma &gt;0$,对所有$i=1,\dots,N$:<br>
$$<br>
y_i\omega x_i \geq \gamma<br>
$$</li>
<li>令$R = max ||x_i||$,则感知机算法在训练集上的误分类次数$k$满足不等式:<br>
$$<br>
k \leq (\frac{R}{\gamma})^2<br>
$$</li>
</ul>
</blockquote>
<p>需要注意的是，当数据线性不可分时，感知机算法便不会收敛，当训练集线性可分时，感知机算法存在无穷多个解，其解由于不同的初值或者迭代顺序而可能有所不同</p>
]]></content>
      <categories>
        <category>统计学习</category>
      </categories>
      <tags>
        <tag>线性判别,感知机</tag>
      </tags>
  </entry>
  <entry>
    <title>近邻法</title>
    <url>/2020/08/25/jin-lin-fa/</url>
    <content><![CDATA[<p>这一部分介绍一下$k$近邻算法，该算法于1968年由Cover和Hart提出，$k$近邻法是一种基本分类与回归方法，本文结构如下:</p>
<ul>
<li>$k$近邻算法</li>
<li>压缩近邻法</li>
<li>$k$近邻实现：$kd$树</li>
</ul>
<a id="more"></a>
<h3 id="k-近邻算法">$k$近邻算法</h3>
<h4 id="算法流程">算法流程</h4>
<p>$k$近邻的算法非常简单朴素:</p>
<blockquote>
<p>给定一个数据集，对新的输入实例，在训练数据集中找到与该实例<strong>最邻近</strong>的$k$个实例，这$k$个实例中的多数属于某个类，就把该输入实例分为这个类。</p>
</blockquote>
<p>$k$近邻的算法思想可以说是非常符合人的直觉，正如俗语所说:&quot;物以类聚，人以群分“，对于分类问题就是，想要看这个样本点是什么类别，只需要看其周围的点都是啥类别就可以了。 下面给出一般$k$近邻算法的定义:</p>
<blockquote>
<p><strong>$k$近邻法</strong><br>
<strong>输入</strong>: 训练数据集<br>
$$<br>
T = {(x_1,y_1), (x_2,y_2),\dots, (x_N,y_N) }<br>
$$<br>
其中，$x_i \in \mathcal{X} \subseteq R^n$为实例的特征向量，$y_i \in \mathcal{Y} = { c_1, c_2, \dots, c_K}$为实例的类别<br>
<strong>输出</strong>: 实例$x$所属的类$y$<br>
<strong>算法流程</strong>:</p>
<ol>
<li>根据给定的距离度量，在训练集中找出与$x$最邻近的$k$个点，涵盖这$k$个点的$x$的邻域记做$N_k(x)$</li>
<li>在$N_k(x)$中根据分类决策规则(如多数表决)决定$x$的类别$y$:<br>
$$<br>
y = \arg \max_{c_j} \sum_{x_i \in N_k(x)} I(y_i = c_j),\quad i = 1,2,\dots,N;j=1,2,\dots,K<br>
$$</li>
</ol>
</blockquote>
<p>若$k=1$，则此时$k$近邻算法被称为最近邻算法，$k$近邻算法没有显示的学习过程</p>
<h4 id="距离度量">距离度量</h4>
<h5 id="L-p距离">$L_p距离$</h5>
<p>使用$k$近邻算法的一个关键问题是需要明确如何定义<strong>最近</strong>，即选择什么样的距离度量，下面介绍下一般通用的距离度量$L_p$距离：</p>
<blockquote>
<p>设特征空间$\mathcal{X}$是$n$维实数向量空间$R^n$,$x_i,x_j \in \mathcal{X}$,$x_i = (x_i^1,x_i^2,\dots,x_i^n),x_j = (x_j^1,x_j^2,\dots,x_j^n)$,则$x_i,x_j$之间的$L_p$距离定义为:<br>
$$<br>
L_p(x_i,x_j) = (\sum_{i=1}^n |x_i^{(l)} - x_j^{(l)}|^p)^{\frac{1}{p}}<br>
$$</p>
</blockquote>
<p>这一类距离度量都是由范数导出的：</p>
<ul>
<li>$p = 1$,对应的是由1范数导出的曼哈顿距离</li>
<li>$p = 2$,对应的是由2范数导出的欧几里得距离</li>
<li>$p = \infty$,对应的是由无穷范数导出的切比雪夫距离<br>
$$<br>
L_\infty(x_i,x_j) = \max_l |x_i^{(l)} - x_j^{(l)}|<br>
$$<br>
<img src="https://raw.githubusercontent.com/xuejy19/xuejy19.github.io/source/Img/Lp.png" alt="$L_p$距离"><br>
上图显示的是在不同的$p$下距离原点距离为1的点所组成的等高线。</li>
</ul>
<h5 id="马氏距离">马氏距离</h5>
<p>马氏距离是另一种常用的距离度量,首先给出维基百科中关于马氏距离的定义:</p>
<blockquote>
<p>马氏距离(Mahalanobis distance)是由印度统计学家马哈拉诺比斯(P. C. Mahalanobis)提出的，表示数据的协方差距离。它是一种有效的计算两个未知样本集的相似度的方法，与欧式距离不同的是，它考虑到各种特性之间的联系(如体重与身高是相关联的)，并且是尺度无关的，即马氏距离独立于测量尺度。对于一个均值为$\mu$,协方差矩阵为$\Sigma$的变量，其马氏距离为:<br>
$$<br>
\sqrt{(x - \mu)^T \Sigma^{-1} (x - \mu)}<br>
$$<br>
马氏距离也可以定义为两个服从同一分布并且其协方差矩阵为$\Sigma$的随机变量$x$与$y$的差异程度:<br>
$$<br>
d_M(x,y) = \sqrt{(x - y)^T \Sigma^{-1} (x - y)}<br>
$$<br>
若协方差矩阵为单位阵，则此时马氏距离便退化为欧式距离，若协方差矩阵为对角阵，此时马氏距离称为正规化的马氏距离:<br>
$$<br>
d(x,y) = \sqrt{\sum_{i=1}^p \frac{(x_i - y_i)^2}{\sigma_i^2}}<br>
$$</p>
</blockquote>
<p>下面我对马氏距离做一个简要的说明，首先协方差矩阵是一个对称半正定矩阵，可以进行特征值分解:<br>
$$<br>
\Sigma = U \Lambda U^T<br>
$$<br>
其中,$U$是酉矩阵，满足$U^T = U^{-1}$,是由矩阵$\Sigma$的特征向量组成(列向量)，而$\Lambda$是一个对角矩阵，对角元素是相应特征向量所对应的特征值。下面我们考虑将协方差矩阵写成下面的形式:<br>
$$<br>
\begin{aligned}<br>
\Sigma &amp;= U \Lambda^{\frac{1}{2}} \Lambda^{\frac{1}{2}} U^T \<br>
&amp;=  U \Lambda^{\frac{1}{2}} (U \Lambda^{\frac{1}{2}})^T \<br>
&amp;= AA^T<br>
\end{aligned}<br>
$$<br>
其中$A = U \Lambda^{\frac{1}{2}}$,所以马氏距离计算公式中的$\Sigma^{-1}$可以写做:<br>
$$<br>
\Sigma^{-1} = (A^T)^{-1} A^{-1}<br>
$$<br>
下面计算样本点$x,y$的马氏距离的平方:<br>
$$<br>
\begin{aligned}<br>
d_M(x,y)^2 &amp;= (x-y)^T \Sigma^{-1} (x - y) \<br>
&amp;= (x-y)^T (A^T)^{-1} A^{-1} (x-y) \<br>
&amp;= [A^{-1}x - A^{-1}y]^T[A^{-1}x - A^{-1} y]<br>
\end{aligned}<br>
$$<br>
若记$A^{-1}x = x’,A^{-1}y = y’$,则$d_M(x,y)^2$可以写做：<br>
$$<br>
d_M(x,y)^2 = || x’ - y’||^2<br>
$$<br>
这也就是说在原始空间计算$x,y$之间的马氏距离相当于计算变换后空间中对应点的欧式距离！最后我们看一下这个变换做了什么事情：<br>
$$<br>
A^{-1} = \Lambda^{-\frac{1}{2}} U^T<br>
$$<br>
$U^T$是一个旋转操作，相当于按照主成分方向旋转，消除各个维度之间的联系，而$\Lambda^{-\frac{1}{2}}$是一个scale操作，将变换后的各个维度的点再除以对应分布的标准差以消除由于分布不同带来的距离度量的差异。<br>
最后总结下马氏距离与欧式距离的区别:</p>
<ol>
<li>马氏距离的计算是建立在总体样本的基础上的，也就是说，如果拿同样的两个样本，放入两个不同的总体当中，最后计算得出的两个样本的马氏距离通常是不相同的。</li>
<li>在计算马氏距离过程中，要求总样本数大于样本的维数，否则协方差矩阵不满秩，无法求逆</li>
</ol>
<p>马氏距离的优缺点:</p>
<ul>
<li>优点：它不受量纲的影响，两点之间的马氏距离与原始数据的测量单位无关，由标准化数据和中心化数据计算出的两点之间的马氏距离相同。马氏距离还可以排除变量之间相关性的干扰</li>
<li>缺点: 夸大了变化微小的变量的作用</li>
</ul>
<h4 id="k-值选取">$k$值选取</h4>
<p>$k$是近邻法中一个重要的超参数，下面首先说明两种极端情况:</p>
<ul>
<li>$k=1$,此时整个样本空间相当于被分割成了$N$块($N$为训练样本个数)，对于一个新的样本，需要判断它落在那一块，便将它归入对应中心点的类别，此时模型非常复杂。</li>
<li>$k =N$,这也就意味着对于一个新的样本点，直接将它归为整个训练实例中最多的类，此时模型又过于简单。</li>
</ul>
<p>总结来看，若选择较小的$k$值，对与近邻实例点非常敏感，如果原始数据噪声较大，则会导致错分，$k$值的减小就意味着整体模型变得更加复杂，容易发生过拟合；若$k$值较大，就相当于用较大邻域中的训练数据进行预测，这一定程度上减轻了数据噪声对预测结果的影响，但因为将邻域扩大，导致一些不相似的样本点也在邻域中，这可能会导致分类错误。<br>
下图是使用$k$近邻方法在MNIST数据集上的结果，距离度量选用的是欧几里得距离，图中横坐标表示训练样本规模，纵坐标则是分类错误率，从图中可以看出，当样本数量较少时，选取较小的$k$得到分类效果较好，但随着训练样本数的增加，不同$k$值的分类效果开始接近。<br>
<img src="https://raw.githubusercontent.com/xuejy19/xuejy19.github.io/source/Img/k.png" alt="$k$值选取"></p>
<h4 id="算法复杂度分析">算法复杂度分析</h4>
<p>需要注意的是，$k$近邻法的时间复杂度非常高，假设特征空间维数为$n$，样本集容量为$N$，则每来一个样本，首先要计算该样本点$x$到样本集中各点的距离($O(N*n$)),然后需要对这$N$个距离进行排序($O(N\log N)$),因此对单个样本进行类别划分的复杂度便已经是:<br>
$$<br>
O(N * n) + O(N \log N)<br>
$$<br>
对于高维的数据，直接应用$k$近邻法的时间开销是非常大的，因此在应用中往往首先考虑对高维数据进行将维。</p>
<h3 id="压缩近邻法">压缩近邻法</h3>
<p>由于$k$近邻法计算复杂度过高，因此有很多旨在降低近邻法复杂度的算法被提出，压缩近邻法是其中一种，下面给出其算法流程:</p>
<blockquote>
<p><strong>压缩近邻法</strong>：<br>
Step1: 从Grabbag中选择一个样本放入Store中<br>
Step2: 用store中样本以近邻法测试Grabbag中样本。如果分错，则将该样本放入store<br>
Step3: 重复上面方法直到Grabbag中没有样本再转入到Store中，或Grabbag为空则停止<br>
Step4: 用Store中样本作为近邻法设计集合</p>
</blockquote>
<p>压缩近邻法的思想也很简单，原始样本数据集中并不是所有的数据点都对分类有帮助，因此考虑挑选出一些具有代表性的点来组成新的训练集，该集合比之前的集合要小，当新来一个数据时，便用前面挑选出的那些点应用近邻法判断数据点类别。</p>
<h3 id="kd-树">$kd$树</h3>
<h4 id="kd-树定义">$kd$树定义</h4>
<p>$kd$树是一种对$k$维空间中的实例点进行存储以便对其进行快速检索的树形数据结构。$kd$树是二叉树，表示对$k$维空间的一个划分。构造$kd$树相当于不断地用垂直于坐标轴的超平面将$k$维空间切分，构成一系列的$k$维超矩形区域，$kd$树的每一个节点对应于一个$k$维超矩形区域。</p>
<h4 id="kd-树构造">$kd$树构造</h4>
<p>$kd$树的构造流程如下:</p>
<blockquote>
<p><strong>构造平衡$kd$树</strong><br>
<strong>输入:</strong> $k$维空间数据集$T = { x_1,x_2,\dots,x_N }$,其中$x_i = (x_i^1,x_i^2,\dots,x_i^k)^T$<br>
<strong>输出:</strong> $kd$树</p>
<ul>
<li>开始：构造根节点，根节点对应于包含$T$的$k$维空间的超矩形区域。选择$x^1$为坐标轴，以$T$中所有实例的$x^1$坐标的中位数为切分点，将根节点对应的超矩形区域切分为两个子区域。由根节点生成深度为$1$的左、右子节点：左子节点对应于坐标$x^1$小于切分点的子区域，右子节点对应于坐标$x^1$大于切分点的子区域。将落在切分超平面上的实例点保存在该节点。</li>
<li>重复：对深度为$j$的节点，选择$x^l$为切分的坐标轴，$l = j(mod k) +1$,以该节点的区域中所有实例的$x^l$坐标的中位数为切分点，将对应的超矩形区域再次分割为两个子区域。</li>
<li>直到两个子区域没有实例存在时停止，从而形成$kd$树的区域划分。</li>
</ul>
</blockquote>
<h4 id="kd-树搜索">$kd$树搜索</h4>
<p>下面给出$kd$树的最近邻搜索算法：</p>
<blockquote>
<p><strong>kd树搜索算法</strong><br>
<strong>输入:</strong> 已构造的$kd$树，目标点$x$<br>
<strong>输出:</strong> $x$的最近邻</p>
<ul>
<li>在$kd$树中找到包含目标点$x$的叶节点：从根节点出发，递归向下搜索即可</li>
<li>以此叶节点为&quot;当前最近点&quot;</li>
<li>递归地向上回退，在每个节点进行以下操作:
<ul>
<li>如果该节点保存的实例点比当前最近点距离目标点更近，则以该实例点为当前最近点</li>
<li>检查另一子节点对应的区域是否与以目标点为球心、以目标点与“当前最近点”间的距离为半径的超球体相交。如果相交，则可能在另一个子节点对应的区域内存在距目标点更近的点，移动到另一个子节点。接着递归地进行最近邻搜索</li>
</ul>
</li>
<li>当回退到跟节点时，搜索结束。最后的&quot;当前最近点&quot;即为$x$的最近邻点。</li>
</ul>
</blockquote>
<p>如果实例点是随机分布的，$kd$树搜索的平均计算复杂度是$O(\log N)$,$kd$树更适用于训练实例数远大于空间维数时的$k$近邻搜索，当空间维数接近训练实例数时，它的效率会迅速下降，接近线性扫描。</p>
]]></content>
      <categories>
        <category>统计学习</category>
      </categories>
      <tags>
        <tag>分类，k近邻</tag>
      </tags>
  </entry>
  <entry>
    <title>论时间驱动与事件驱动</title>
    <url>/2020/08/24/lun-shi-jian-qu-dong-yu-shi-jian-qu-dong/</url>
    <content><![CDATA[]]></content>
  </entry>
  <entry>
    <title>非监督学习</title>
    <url>/2020/09/11/fei-jian-du-xue-xi/</url>
    <content><![CDATA[<p>这部分总结下传统统计学习的最后一部分内容——非监督学习，非监督学习又称无监督学习，我们前面讲的大部分算法都是需要样本的标签的，通过标签来构造损失函数，进而进行模型学习，但在有的情况下我们并没有数据的标签，这种情况下我们就什么都不能做了么？当然不是，对于无标签数据，我们仍旧可以开展一部分工作，比如：</p>
<ul>
<li>概率模型估计</li>
<li>数据降维</li>
<li>数据聚类</li>
</ul>
<p>由于概率模型估计算法在前面已经讲过了(<a href="https://xuejy19.github.io/2020/07/30/EM/">EM算法</a>、<a href="https://xuejy19.github.io/2020/07/27/%E6%A6%82%E7%8E%87%E5%AF%86%E5%BA%A6%E5%87%BD%E6%95%B0%E4%BC%B0%E8%AE%A1/#more">概率密度函数估计</a>)，这里就不再赘述，本文按照以下结构组织:</p>
<ul>
<li>无监督学习思想</li>
<li>数据降维</li>
<li>数据聚类</li>
</ul>
<a id="more"></a>
<h3 id="无监督学习思想">无监督学习思想</h3>
<p>无监督学习是从无标注的数据中学习数据的统计规律或者说内在结构的机器学习，主要包括聚类、降维、概率估计，<strong>无监督学习可以用于数据分析或者监督学习预处理</strong>。<br>
无监督学习使用无标注数据$U = { x_1, x_2, \dots, x_N}$学习或训练，其中$x_i，i = 1,2,\dots,N$, 是样本，由特征向量组成。无监督学习的模型是函数$z = g_{\theta}(x)$ , 条件概率分布$P_{\theta}(z|x)$，或条件概率分布$P_{\theta}(x|z)$。其中$x \in X$是输入，表示样本； $z \in Z$是输出，表示对样本的分析结果，可以是类别、转换、概率；$\theta$是参数。<br>
无监督学习是一个困难的任务，因为数据没有标注，也就是没有人为的引导，机器需要自己从数据中找出规律。模型的输入$x$在数据中可以观测，而输出$z$隐藏在数据中。无监督学习通常需要大量的数据，因为对数据隐藏的规律的发现需要足够的观测。<br>
<img src="https://raw.githubusercontent.com/xuejy19/xuejy19.github.io/source/Img/Unsupervised.jpg" alt="机器学习算法划分"></p>
<h3 id="数据降维">数据降维</h3>
<h4 id="问题定义">问题定义</h4>
<p>降维是将训练数据中的样本(实例)从高维空间转换到低维空间。假设样本原本存在于低维空间，或者近似地存在于低维空间，通过降维则可以更好地表示样本数据的结构，即更好地表示样本之间的关系。高维空间通常是高维的欧式空间，而低维空间则是低维的欧式空间或者流形(manifold)。低维空间不是事先给定，而是从数据中自动发现，其维数通常是事先给定的。从高维到低维的降维中，要保证样本的信息损失最少。<br>
<img src="https://raw.githubusercontent.com/xuejy19/xuejy19.github.io/source/Img/dimension.png" alt="降维"></p>
<p>本文主要介绍四种降维方法：</p>
<ul>
<li>主成分分析法(PCA)</li>
<li>多维缩放(MDS)</li>
<li>等距特征映射(ISOMAP)</li>
<li>局部线性</li>
</ul>
<h3 id="数据聚类">数据聚类</h3>
<h4 id="问题定义-2">问题定义</h4>
<p>聚类是将样本集合中相似的样本(实例)分配到相同的类，不相似的样本分配到不同的类。聚类时，样本通常是欧式空间中的向量，类别不是事先给定，而是从数据中自动发现，但<strong>类别的个数</strong>通常要预先给定。样本之间的相似度或距离由<strong>度量</strong>决定。如果一个样本只能属于一个类，则称为硬聚类；如果一个样本可以属于多个类，则称为软聚类。<br>
<img src="https://raw.githubusercontent.com/xuejy19/xuejy19.github.io/source/Img/clustering.png" alt="聚类"></p>
]]></content>
      <categories>
        <category>统计学习</category>
      </categories>
      <tags>
        <tag>PCA, MDS, ISOMAP</tag>
      </tags>
  </entry>
  <entry>
    <title>隐马尔可夫模型</title>
    <url>/2020/08/17/hmm/</url>
    <content><![CDATA[<p>在这一部分，我将对隐马尔可夫模型(HMM)做简要介绍,该章节分为以下几个部分进行组织:</p>
<ul>
<li>基本概念</li>
<li>概率计算算法</li>
<li>学习算法</li>
<li>预测算法</li>
</ul>
<a id="more"></a>
<h3 id="基本概念">基本概念</h3>
<p>首先给出隐马尔可夫模型的定义：</p>
<blockquote>
<p><strong>隐马尔可夫模型</strong>:隐马尔可夫模型是关于时序的概率模型，描述由一个隐藏的马尔可夫链随机生成不可观测的随机状态序列,再由各个状态生成一个观测从而产生观测随机序列的过程。隐藏的马尔可夫链随机生成的状态序列称为状态序列；每个状态生成一个观测，而由此产生的观测的随机序列，称为观测序列，序列的每一个位置又可以看作是一个时刻。</p>
</blockquote>
<p>隐马尔可夫模型的形式定义如下:设$Q$是所有可能的状态的集合,$V$是所有可能的观测的集合:</p>
<p>$$<br>
Q = (q_1,q_1,\dots,q_N),V = (v_1,v_2,\dots,v_M)<br>
$$</p>
<p>其中,$N$是可能的状态数，$M$是可能的观测数。$I$是长度为$T$的状态序列,$O$是对应的观测序列:</p>
<p>$$<br>
I = (i_1,i_2,\dots,i_T),O = (o_1,o_2,\dots,o_T)<br>
$$</p>
<p>$A$是状态转移概率矩阵:</p>
<p>$$<br>
A = [a_{ij}]_{N \times N}<br>
$$</p>
<p>其中:</p>
<p>$$<br>
a_{ij} = P(i_{t+1} = q_j | i_t = q_i),i=1,\dots,N;j=1,\dots,N<br>
$$</p>
<p>指代在时刻$t$处于状态$q_i$条件下,在时刻$t+1$转移到$q_j$的概率，$B$是观测概率矩阵:</p>
<p>$$<br>
B = [b_j(k)]_{N\times M}<br>
$$</p>
<p>其中:</p>
<p>$$<br>
b_j(k) = P(o_t = v_k|i_t = q_j),\quad k=1,2,\dots,M;\quad,j=1,2,\dots,N<br>
$$</p>
<p>指代在时刻$t$处于状态$q_j$条件下生成观测$v_k$的概率。$\pi$是初始是初始状态概率向量:</p>
<p>$$<br>
\pi = (\pi_i)<br>
$$</p>
<p>其中:</p>
<p>$$<br>
\pi_i = P(i_1 = q_i),\quad i =1,2,\dots,N<br>
$$</p>
<p>指代初始时刻处于状态$q_i$的概率。隐马尔可夫模型由初始状态概率向量$\pi$,状态转移概率矩阵$A$和观测概率矩阵$B$决定。$\pi$和$A$决定状态序列,$B$决定观测序列。因此，隐马尔可夫模型可以用三元符号表示:</p>
<p>$$<br>
\lambda = (\pi,A,B)<br>
$$</p>
<p>状态转移矩阵$A$与初始状态向量$\pi$确定了隐藏的马尔可夫链，生成不可观测的状态序列。观测概率矩阵$B$确定了如何从状态生成观测，与状态序列综合确定了如何产生观测序列。从定义来看，隐马尔可夫模型做了两个基本假设：</p>
<ol>
<li>齐次马尔可夫性假设，即假设隐藏的马尔可夫链在任意时刻$t$的状态只依赖于前一时刻的状态，与其他时刻的状态及观测无关，也与$t$无关：</li>
</ol>
<p>$$<br>
P(i_t| i_{t-1},o_{t-1},\dots,i_1,t_1) = P(i_t|i_{t-1}),\quad t = 1,2,\dots,T<br>
$$</p>
<ol start="2">
<li>观测独立性假设,即假设在任意时刻的观测只依赖于该时刻的马尔可夫链的状态，与其他观测及状态无关:</li>
</ol>
<p>$$<br>
P(o_t|i_t,i_{t-1},o_{t-1},\dots,i_1,t_1) = P(o_t|i_t)<br>
$$</p>
<p>隐马尔可夫模型可用于标注,这时状态对应着标记。标注问题是给定观测的序列预测其对应的标记序列。可以假设标注问题的数据是由隐马尔可夫模型生成的。这样我们可以利用隐马尔可夫模型的学习与预测算法进行标注。</p>
<h4 id="观测序列的生成过程">观测序列的生成过程</h4>
<p>根据隐马尔可夫模型定义,可以将一个长度为$T$的观测序列$O = (o_1,o_2,\dots,o_T)$的生成过程描述如下:</p>
<blockquote>
<p>输入： 隐马尔可夫模型$\lambda = (A,B,\pi)$,观测序列长度$T$<br>
输出： 观测序列$O = (o_1,o_2,\dots,o_T)$</p>
<ol>
<li>按照初始状态分布$\pi$产生状态$i_1$</li>
<li>令$t=1$</li>
<li>按照状态$i_t$的观测概率分布$b_{i_t}(k)$生成$o_t$</li>
<li>按照状态$i_t$的状态转移概率分布${a_{i_t,i_{t+1}}}$产生状态$i_{t+1}$</li>
<li>令$t = t+1$,如果$t&lt;T$,转步(3),否则，终止</li>
</ol>
</blockquote>
<h4 id="隐马尔可夫模型的3个基本问题">隐马尔可夫模型的3个基本问题</h4>
<p>隐马尔可夫模型有3个基本问题:</p>
<ul>
<li>概率计算问题,给定模型$\lambda = (A,B,\pi)$和观测序列$O = (o_1,o_2,\dots,o_T)$,计算在模型$\lambda$下观测序列$O$出现的概率$P(O|\lambda)$</li>
<li>学习问题，已知观测序列$O = (o_1,o_2,\dots,o_T)$,估计模型$\lambda = (A,B,\pi)$参数，使得在该模型下观测序列概率$P(O|\lambda)$。即用极大似然估计的方法估计参数。</li>
<li>预测问题，也称为解码问题。已知模型$\lambda = (A,B,\pi)$和观测序列$O = (o_1,o_2,\dots,o_T)$,求对给定观测序列条件概率$P(I|O)$最大的状态序列$I = (i_1,i_2,\dots,i_T)$。即给定观测序列，求最有可能的对应的状态序列。</li>
</ul>
<h3 id="概率计算算法">概率计算算法</h3>
<h4 id="直接计算法">直接计算法</h4>
<p>首先介绍最直观的直接计算法，给定模型和观测序列后，计算观测序列出现的概率的直接方法便是按照概率公式直接计算。通过列举所有可能的长度为$T$的状态序列，求各个状态序列$I$与观测序列$O$的联合概率$P(I,O|\lambda)$,然后对所有可能的状态序列求和，得到$P(O|\lambda)$。</p>
<p>状态序列$I = (i_1,i_2,\dots,i_T)$的概率是:</p>
<p>$$<br>
P(I|\lambda) = \pi_{i1}a_{i_1 i_2} a_{i_2 i_3} \dots a_{i_{T-1} i_T}<br>
$$</p>
<p>对固定的状态序列$I = (i_1,i_2,\dots,i_T)$,观测序列$O = (o_1,o_2,\dots,o_T)$的概率是:</p>
<p>$$<br>
P(O|I,\lambda) = b_{i_1}(o_1) b_{i_2}(o_2) \dots b_{i_T}(o_T)<br>
$$</p>
<p>$O$和$I$同时出现的联合概率为:</p>
<p>$$<br>
\begin{aligned}<br>
P(O,I|\lambda) &amp;= P(O|I,\lambda) P(I|\lambda) \\<br>
&amp;= \pi_{i_1}b_{i_1}(o_1) a_{i_1 i_2} b_{i_2}(o_2) \dots a_{i_{T-1}i_T} b_{i_T}(o_T)<br>
\end{aligned}<br>
$$</p>
<p>然后，对所有可能的状态序列$I$求和，得到观测序列$O$的概率$P(O|\lambda)$，即:</p>
<p>$$<br>
\begin{aligned}<br>
P(O,I|\lambda) &amp;= \sum_{I} P(O|I,\lambda) P(I|\lambda) \\<br>
&amp;= \sum_{i_1,i_2,\dots,i_T} \pi_{i_1}b_{i_1}(o_1) a_{i_1 i_2} b_{i_2}(o_2) \dots a_{i_{T-1}i_T} b_{i_T}(o_T)<br>
\end{aligned}<br>
$$</p>
<p>这种直接计算的复杂度是$O(TN^T)$,在实际中并不可行。</p>
<h4 id="前向算法">前向算法</h4>
<p>首先定义前向概率：</p>
<blockquote>
<p>前向概率:给定隐马尔可夫模型$\lambda$,定义到时刻$t$部分观测序列为$o_1,o_2,\dots,o_t$且状态为$q_i$的概率为前向概率，记做：<br>
$$<br>
\alpha_t(i) = P(o_1,o_2,\dots,o_t,i_t = q_i|\lambda)<br>
$$<br>
由此便可以递推求得前向概率$\alpha_t(i)$和观测序列概率$P(O|\lambda)$</p>
</blockquote>
<blockquote>
<p><strong>观测序列概率的前向算法</strong><br>
输入：隐马尔可夫模型$\lambda$,观测序列$O$<br>
输出: 观测序列概率$P(O|\lambda)$</p>
<ul>
<li>初值：<br>
$$<br>
\alpha_1(i) = \pi_i b_i(o_1),\quad i = 1,2,\dots, N<br>
$$</li>
<li>递推：对$t=1,2,\dots,T-1$<br>
$$<br>
\alpha_{t+1}(i) = [\sum_{j=1}^N \alpha_t(j) a_{ji}] b_i(o_{t+1}),\quad i= 1,2,\dots,N<br>
$$</li>
<li>终止:<br>
$$<br>
P(O|\lambda) = \sum_{i=1}^N \alpha_T(i)<br>
$$</li>
</ul>
</blockquote>
<p>前向算法实际上是基于“状态序列的路径结构”递推计算$P(O|\lambda)$的算法。前向算法高效的关键在于其局部计算前向概率，然后利用路径结构将前向概率递推到全局,得到$P(O|\lambda)$。</p>
<h4 id="后向算法">后向算法</h4>
<p>首先给出后向概率的定义:</p>
<blockquote>
<p><strong>后向概率</strong>：给定隐马尔可夫模型$\Lambda$,定义在时刻$t$状态为$q_i$的条件下，从$t+1$到$T$的部分观测序列为$o_{t+1},o_{t+2},\dots,o_T$的概率为后向概率，记做：<br>
$$<br>
\beta_t(i) = P(o_{t+1},o_{t+2},\dots,o_T|i_t = q_i,\lambda)<br>
$$</p>
</blockquote>
<p>与前向概率计算类似,后向概率的计算也可以采用递推算法:</p>
<blockquote>
<p><strong>观测序列概率的后向算法</strong><br>
输入：隐马尔可夫模型$\lambda$,观测序列$O$<br>
输出：观测序列概率$P(O|\lambda)$</p>
<ul>
<li>初始化:<br>
$$<br>
\beta_T(i) = 1,\quad i =1,2,\dots,N<br>
$$</li>
<li>对$t = T-1,T-2,\dots,1$<br>
$$<br>
\beta_t(i) = \sum_{j=1}^N a_{ij} b_j(o_{t+1})\beta_{t+1}(j),<br>
\quad i=1,2,\dots,N<br>
$$</li>
<li>终止：<br>
$$<br>
P(O|\lambda) = \sum_{i=1}^N \pi_i b_i(o_1)\beta_1(i)<br>
$$</li>
</ul>
</blockquote>
<p>利用前向概率与后向概率的定义可以将观测序列概率统一写成:</p>
<p>$$<br>
P(O|\lambda) =\sum_{i=1}^N \alpha_t(i) \beta_t(i) =\sum_{i=1}^N \sum_{j=1}^N \alpha_t(i)<br>
a_{ij} b_j(o_{t+1}) \beta_{t+1}(j)<br>
$$</p>
<p>该联合概率公式告诉我们，若我们知道了某一个时刻所有状态下的前向概率，同时知道下一个时刻所有状态下的后向概率，则可以直接计算出观测序列概率。前面介绍的三个计算观测序列概率的公式实际上为我们提供了三种递推思路:</p>
<ul>
<li>前向概率递推，即从前向后递推</li>
<li>后向概率递推，即从后向前递推</li>
<li>前向后向概率递推，即从两个方向开始递推</li>
</ul>
<p>这三种方法的计算复杂度均为$O(TN^2)$级别，对比直接计算的$O(TN^T)$级别，计算复杂度大大降低。</p>
<h4 id="一些概率与期望值的计算">一些概率与期望值的计算</h4>
<p>利用前向概率和后向概率，可以得到关于单个状态和两个状态概率的计算公式。</p>
<ol>
<li>给定模型$\lambda$和观测$O$,在时刻$t$处于状态$q_i$的概率,记：<br>
$$<br>
\gamma_t(i) = P(i_t = q_i|O,\lambda)<br>
$$<br>
可以通过前向后向概率计算,由贝叶斯公式可得:<br>
$$<br>
\gamma_t(i) = P(i_t = q_i|O,\lambda) = \frac{P(i_t = q_i,O|\lambda)}{P(O|\lambda)}<br>
$$<br>
由此可以得到：<br>
$$<br>
\gamma_t(i) = \frac{\alpha_t(i) \beta_t(i)}{\sum_{j=1}^N \alpha_t(j)\beta_t(j)}<br>
$$</li>
<li>给定模型$\lambda$和观测$O$,在时刻$t$处于状态$q_i$且在时刻$t+1$处于状态$q_j$的概率为：<br>
$$<br>
\xi_t(i,j) = P(i_t = q_i, i_{t+1} = q_j | O,\lambda)<br>
$$<br>
由贝叶斯公式可得：<br>
$$<br>
\begin{aligned}<br>
\xi_t(i,j)  &amp;= \frac{P(i_t = q_i,i_{t+1} = q_j, O|\lambda)}{P(O|\lambda)} \\<br>
&amp;= \frac{P(i_t = q_i,i_{t+1} = q_j, O|\lambda)}{\sum_{i=1}^N \sum_{j=1}^N P(i_t = q_i,i_{t+1} = q_j, O|\lambda)}<br>
\end{aligned}<br>
$$<br>
公式中$P(i_t = q_i,i_{t+1} = q_j, O|\lambda)$计算公式为:<br>
$$<br>
P(i_t = q_i,i_{t+1} = q_j, O|\lambda) = \alpha_t(i) a_{ij} b_j (o_{t+1}) \beta_{t+1}(j)<br>
$$</li>
<li>将$\gamma_t(i)$和$\xi_t(i,j)$对各个时刻$t$求和，可以得到一些期望值：</li>
</ol>
<ul>
<li>在观测$O$下状态$i$出现的期望值：<br>
$$<br>
\sum_{t=1}^T \gamma_t(i)<br>
$$</li>
<li>在观测$O$下由状态$i$转移的期望值:<br>
$$<br>
\sum_{t=1}^{T-1} \gamma_t(i)<br>
$$</li>
<li>在观测$O$下由状态$i$转移到状态$j$的期望值：<br>
$$<br>
\sum_{t=1}^{T-1} \xi_t(i,j)<br>
$$</li>
</ul>
<h3 id="学习算法">学习算法</h3>
<p>隐马尔可夫的学习算法可以根据数据类型分为两种：</p>
<ul>
<li>训练数据包含观测序列和状态序列, 此时可采用监督学习算法</li>
<li>训练数据仅有观测序列，此时需采用无监督学习算法</li>
</ul>
<h4 id="监督学习方法">监督学习方法</h4>
<p>假设已给训练数据包含$s$个长度相同的观测序列和对应的状态序列${ (O_1,I_1),\dots,<br>
(O_S,I_S)}$,那么可以直接采用极大似然法来估计马尔可夫参数,具体方法如下:</p>
<ol>
<li>转移概率$a_{ij}$的估计：设样本中时刻$t$处于状态$i$,而时刻$t+1$处于状态$j$出现<br>
的频数为$A_{ij}$,那么状态转移概率$a_{ij}$的估计为：<br>
$$<br>
\hat{a}<em>{ij} = \frac{A</em>{ij} }{\sum_{j=1}^N A_{ij} }<br>
$$<br>
这其实就是一个伯努利分布的最大似然估计问题，计算比较简单。</li>
<li>观测概率$b_j(k)$的估计,设样本中状态为$j$并且观测为$k$的频数是$B_{jk}$,那么状态为$j$观测为$k$的估计是：<br>
$$<br>
\hat{b}<em>j(k) = \frac{B</em>{jk}}{\sum_{k=1}^M B_{jk}}<br>
$$</li>
<li>初始状态概率$\pi_i$的极大似然估计为$S$个样本中初始状态为$q_i$的频率</li>
</ol>
<h3 id="无监督学习方法-BW算法">无监督学习方法-BW算法</h3>
<p>假设训练数据只包含$S$个长度为$T$的观测序列${ O_1,O_2,\dots, O_S}$<br>
而没有相应的状态序列，学习的目标是学习隐马尔可夫模型的参数。我们将观测序列<br>
看做观测数据$O$,状态序列数据看做不可观测的隐数据$I$,那么隐马尔可夫实际上是<br>
一个含有隐变量概率模型：</p>
<p>$$<br>
P(O|\lambda) = \sum_{I} P(O|I,\lambda)P(I|\lambda)<br>
$$</p>
<p>这是在EM算法解决框架下，可以按照EM算法步骤进行计算</p>
<ul>
<li>确定完全数据的对数似然函数，所有的观测数据写成$O = (o_1,o_2,\dots,o_T)$,<br>
所有隐数据写成$I = (i_1,i_2,\dots,i_T)$。完全数据的对数似然函数是<br>
$log P(O,I|\lambda)$</li>
<li>EM算法的E步:求$Q$函数$Q(\lambda,\bar{\lambda})$<br>
$$<br>
\begin{aligned}<br>
Q(\lambda,\bar{\lambda}) &amp;= E_I[logP(O,I|\lambda)P(O,I|\bar{\lambda})] \\<br>
&amp;= \sum_I log P(O,I|\lambda) P(I|O,\bar{\lambda}) \\<br>
&amp;= \sum_I log P(O,I|\lambda) \frac{P(I,O|\bar{\lambda})}{P(O|\bar{\lambda})}<br>
\end{aligned}<br>
$$<br>
又因为$P(O|\bar{\lambda})$与$\lambda$无关，而在接下来M步我们要做的是找到合适的<br>
$\lambda$极大化$Q$函数，因此可以考虑将该项略去，将$Q$函数写做:<br>
$$<br>
Q(\lambda,\bar{\lambda}) = \sum_I log P(O,I|\lambda) P(I,O|\bar{\lambda})<br>
$$<br>
其中,$\bar{\lambda}$是隐马尔可夫模型的当前参数估计值,$\lambda$是要极大化的隐马尔<br>
可夫模型参数。<br>
$$<br>
P(O,I|\lambda) = \pi_{i_1} b_{i_1}(o_1) a_{i_1,i_2} b_{i_2}(o_2)<br>
\dots a_{i_{T-1}i_T} b_{i_T}(o_T)<br>
$$<br>
于是$Q$函数可以写做:<br>
$$<br>
\begin{aligned}<br>
Q(\lambda,\bar{\lambda}) &amp;= \sum_I log \pi_{i_1} P(O,I|\bar{\lambda}) + \sum_I (\sum_{t=1}^{T-1} log a_{i_t i_{t+1}})<br>
P(O,I|\bar{\lambda})\ &amp;+ \sum_{I}(\sum_{t=1}^T log b_{i_t}(o_t))P(O,I|\bar{\lambda})<br>
\end{aligned}<br>
$$</li>
<li>EM算法M步：极大化$Q$函数$Q(\lambda,\bar{\lambda})$求模型参数$A,B,\pi$<br>
由最终$Q$函数的写法可以看出，要极大化参数单独出现在三项中，因此只需对各项<br>
分别极大化</li>
</ul>
<ol>
<li>求$\pi_{i_1}$估计值，第一项可以重写为：<br>
$$<br>
\sum_I log \pi_{i_1} P(O,I|\bar{\lambda}) = \sum_{i=1}^N log \pi_{i} P(O,i_1 = i|\bar{\lambda})<br>
$$<br>
同时需要注意到关于$\pi_i$的天然约束$\sum_{i=1}^N \pi_i = 1$,该优化问题可以通过<br>
Lagrange乘子法进行求解，最终求解值为:<br>
$$<br>
\pi_i = \frac{P(O,i_1 = i|\bar{\lambda})}{P(O|\bar{\lambda})}<br>
= \gamma_1(i)<br>
$$</li>
<li>求$a_{ij}$估计值，第二项可以重写为：<br>
$$<br>
\sum_I (\sum_{t=1}^{T-1} log a_{i_t i_{t+1}})P(O,I|\bar{\lambda}) =<br>
\sum_{i=1}^N \sum_{j=1}^N \sum_{t=1}^{T-1} log a_{i j} P(O,i_t = i,i_{t+1} = j|\bar{\lambda})<br>
$$<br>
注意存在约束条件$\sum_{j=1}^N a_{ij} = 1$,应用Lagrange乘子法可得:<br>
$$<br>
a_{ij} = \frac{\sum_{t=1}^{T-1} P(O,i_t = i,i_{t+1} = j|\bar{\lambda})}{\sum_{t=1}^{T-1} P(O,i_t = i|\bar{\lambda})}<br>
= \frac{\sum_{t=1}^{T-1} \xi_t(i,j)}{\sum_{t=1}^{T-1} \gamma_t(i)}<br>
$$</li>
<li>求$b_j(k)$的估计值，首先将第三项重写为:<br>
$$<br>
\sum_{I}(\sum_{t=1}^T log b_{i_t}(o_t))P(O,I|\bar{\lambda}) =<br>
\sum_{j=1}^N \sum_{t=1}^{T} log b_j(o_t) P(O,i_t = j|\bar{\lambda})<br>
$$<br>
约束条件是$\sum_{k=1}^M b_j(k) = 1$。只有在$o_t = v_k$时,$b_j(o_t)$对$b_j(k)$的<br>
偏导数才不为0,以$I(o_t = v_k)$表示,求得:<br>
$$<br>
b_j(k) = \frac{\sum_{t=1}^T P(O,i_t =j|\bar{\lambda})I(o_t = v_k)}{\sum_{t=1}^T P(O,i_t = j|\bar{\lambda})}<br>
= \frac{\sum_{t=1,o_t = v_k}^T \gamma_t(j)}{\sum_{t=1}^T \gamma_t(j)}<br>
$$</li>
</ol>
<blockquote>
<p>Baum-Welch算法：<br>
输入：观测数据$O = (o_1,o_2,\dots,o_T)$<br>
输出：隐马尔可夫模型参数</p>
<ul>
<li>初始化:对$n=0$,选取$a_{ij}^{(0)}, b_j(k)^{(0)},\pi_i^{(0)}$，得到模型<br>
$\lambda^{(0)} = (A^{(0)},B^{(0)},\pi^{(0)})$</li>
<li>根据上面给出的公式进行递推</li>
<li>重复E步和M步。直到算法收敛</li>
</ul>
</blockquote>
<h3 id="预测算法">预测算法</h3>
<p>下面介绍两种隐马尔可夫模型的预测算法：近似算法与维特比算法。</p>
<h4 id="近似算法">近似算法</h4>
<p>近似算法的想法是，在每个时刻$t$选择在该时刻最有可能出现的状态$i_t^<em>$,从而<br>
得到一个状态序列$I^</em> = (i_1^<em>,i_2^</em>,\dots,i_T^*)$,将该序列作为预测的结果。<br>
给定隐马尔可夫模型$\lambda$和观测序列$O$,在时刻$t$处于状态$q_i$的概率<br>
$\gamma_t(i)$是：</p>
<p>$$<br>
\gamma_t(i) = \frac{\alpha_t(i)\beta_t(i)}{P(O|\lambda)} =<br>
\frac{\alpha_t(i)\beta_t(i)}{\sum_{j=1}^N \alpha_t(j)\beta_t(j)}<br>
$$</p>
<p>在每一时刻$t$最有可能的状态$i_t^*$是:</p>
<p>$$<br>
i_t^* = argmax_{1 \leq i \leq N} [\gamma_t(i)],\quad t = 1,2,\dots,T<br>
$$</p>
<p>进而得到状态序列</p>
<p>$$<br>
I^* = (i_1^<em>,i_2^</em>,\dots,i_T^*)<br>
$$</p>
<p>近似算法优点是计算简单，其缺点是不能够保证预测的序列整体是最有可能的状态序列，这是因为预测的状态序列可能有实际不发生的部分。</p>
<h4 id="维特比算法">维特比算法</h4>
<p>维特比算法实际上是用动态规划来解隐马尔可夫模型的预测问题，即用动态规划来求概率最大路径(最优路径)。这时一条路径对应着一个最优序列。<br>
首先导入两个变量$\delta,\varPhi$,定义在时刻$t$状态为$i$的所有单个路径$(i_1,i_2,\dots,i_t)$中概率最大值为:<br>
$$<br>
\delta_t(i) = \max_{i_1,i_2,\dots,i_{t-1}} P(i_t = i,i_{t-1},\dots,i_1,o_t,\dots,o_1|\lambda)<br>
$$<br>
由定义可知变量$\delta$的递推公式：<br>
$$<br>
\begin{aligned}<br>
\delta_{t+1}(i) &amp;= \max_{i_1,i_2,\dots,i_{t}} P(i_{t+1} = i,i_{t},\dots,i_1,o_{t+1},\dots,o_1|\lambda) \<br>
&amp;= max_{1 \leq j \leq N} [\delta_t(j) a_{ji}]b_i(o_{t+1})<br>
\end{aligned}<br>
$$<br>
定义在时刻$t$状态为$i$的所有单个路径$(i_1,i_2,\dots,i_{t-1},i)$中概率最大的路径的第<br>
$t-1$个结点为：<br>
$$<br>
\varPhi_t(i) = argmax_{1 \leq j \leq N}[\delta_{t-1}(j) a_{ji}],\quad i=1,\dots,N<br>
$$<br>
下面给出维特比算法的流程:</p>
<blockquote>
<p><strong>维特比算法：</strong></p>
<ul>
<li>输入： 模型$\lambda = (A,B,\pi)$和观测$O = (o_1,o_2,\dots,o_T)$</li>
<li>输出： 最优路径$I^\ast -= (i_1^\ast,i_2^\ast,\dots,i_T^\ast)$</li>
</ul>
<ol>
<li>初始化<br>
$$<br>
\delta_1(i) = \pi_i b_i(o_1),\varPhi_1(i) = 0<br>
$$</li>
<li>递推：对$t = 2,3,\dots,T$<br>
$$<br>
\begin{aligned}<br>
\delta_{t+1}(i) &amp;= \max_{1 \leq j \leq N} [\delta_t(j) a_{ji}]b_i(o_{t+1}) \<br>
\varPhi_t(i) &amp;= argmax_{1 \leq j \leq N}[\delta_{t-1}(j) a_{ji}]<br>
\end{aligned}<br>
$$</li>
<li>终止<br>
$$<br>
P^* = \max_{1 \leq i \leq N} \delta_T(i), i_T^* = argmax_{1 \leq i \leq N}<br>
[\delta_T(i)]<br>
$$</li>
<li>最优路径回溯。对$t = T-1,T-2,\dots,1$<br>
$$<br>
i_t^* = \varPhi_{t+1}(i_{t+1}^*)<br>
$$<br>
求得最优路径$I^{\ast} = (i_1^\ast,i_2^\ast,\dots,i_T^\ast)$</li>
</ol>
</blockquote>
]]></content>
      <categories>
        <category>统计学习</category>
      </categories>
      <tags>
        <tag>概率模型，HMM</tag>
      </tags>
  </entry>
  <entry>
    <title>pytorch学习</title>
    <url>/2020/09/06/chu-shi-pytorch/</url>
    <content><![CDATA[<p>在前面介绍深度学习的理论知识时，相信大家可以感受到，神经网络的实现主要有以下两个难题：</p>
<ul>
<li>当网络结构复杂起来时，手写一个神经网络是非常困难(尤其是进行反向误差传播时)，也是十分费时的。</li>
<li>一个神经网络有着大量的参数，对计算机的计算能力要求非常高，而GPU是计算机中有着大量计算资源的部分，如何将这部分计算资源调动起来完成一个深度神经网络的训练是另一个难题。</li>
</ul>
<a id="more"></a>
<p>为了解决上述两个问题，深度学习框架便诞生了，目前主流的深度学习框架主要下图所示的这些:<br>
<img src="https://raw.githubusercontent.com/xuejy19/xuejy19.github.io/source/Img/framework.jpeg" alt="主流框架"><br>
这些框架比较如下图所示:<br>
<img src="https://raw.githubusercontent.com/xuejy19/xuejy19.github.io/source/Img/framework1.png" alt="深度学习框架"><br>
目前在科研机构中应用较多的是Tensorflow和pytorch,本文便以pytorch为例来进行讲解，本文内容主要参考官方教程：<a href="https://pytorch.org/tutorials/beginner/blitz/tensor_tutorial.html#sphx-glr-beginner-blitz-tensor-tutorial-py">pytorch官方教程</a>。建议英文阅读能力较强的人还是去阅读原版教程，如果对于算法的理论理解已经到位的话，整个教程还是非常简洁明了的。本文按照以下结构组织:</p>
<ul>
<li>pytorch简介</li>
<li>张量和计算图</li>
<li>低级API和高级API</li>
</ul>
<h3 id="pytorch简介">pytorch简介</h3>
<p>pytorch 是一个开源的python 机器学习库，基于Torch，底层由C++实现，应用于人工智能领域，如自然语言处理(nlp)。它最初由Facebook的人工智能研究团队开发，并且被用于Uber的概率编程软件Pyro.<br>
Pytorch 具有以下两大特征:</p>
<blockquote>
<ul>
<li>提供类似于Numpy的张量计算，可使用gpu加速</li>
<li>基于自动微分系统的深度神经网络</li>
</ul>
</blockquote>
<h3 id="张量和计算图">张量和计算图</h3>
<p>从caffee以来，基本上所有的深度学习框架都包含这两个概念，其实这两个关键概念也正是针对本文一开始提出的两个问题。</p>
<h4 id="张量">张量</h4>
<blockquote>
<p><strong>张量：</strong> 张量可以看作gpu下 numpy的替代品，在应用传统机器学习算法时，和我们打交道最多python库就是numpy，但遗憾的是numpy并不能够在gpu下进行运算。</p>
</blockquote>
<p>输入:</p>
<pre class="line-numbers language-language-python"><code class="language-language-python">torch.randn(2,3)
<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre>
<p>输出:</p>
<pre class="line-numbers language-language-python"><code class="language-language-python">tensor([[ 1.9335,  0.8795, -0.6964],
        [ 0.1573,  1.5692, -0.2320]])
<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre>
<p>tensor的大部分操作与numpy是差不多的，具体属性和方法参考<a href="https://pytorch.org/docs/stable/tensors.html?highlight=torch%20tensor#torch.Tensor">torch.Tensor</a></p>
<h4 id="计算图">计算图</h4>
<p>现代的神经网络架构可以拥有上百万个参数，从计算的角度看，训练神经网络包含两个阶段:</p>
<ul>
<li>用于计算损失函数值的正向传递</li>
<li>向后传递以计算可学习参数的梯度</li>
</ul>
<p>正向传递计算是非常简单的，前一层神经网络的输出是下一层神经网络的输入。后向传递有些复杂，因为这要求我们应用求导的链式法则来计算损失函数的梯度。</p>
<p>当一个神经网络的规模比较小(层数,各层神经元数)时，手算损失函数对各个参数的梯度是可行的，但随着神经网络规模的扩大，梯度的计算变得越来越困难，出于深度学习的这种现实需要，计算图便诞生了：<br>
计算图属于有向无环图，计算图中的节点有两种，一种是基本数学运算符，另一种则是我们所定义的变量。比如下图就可以表示一个简单的神经网络:<br>
$$<br>
\begin{aligned}<br>
b &amp;= w_1 * a \<br>
c &amp;= w_2 * a \<br>
d &amp;= w_3 * b  + w_4 * c \<br>
L &amp; = 10 - d<br>
\end{aligned}<br>
$$<br>
在图中，变量$b,c$和$d$都是数学运算的结果，而变量$a ,w_1,w_2,w_3,w_4$ 由用户自己初始化，由于它们不是由任何数学运算符创建的，因此与其创建相对应的节点由其名称本身表示。</p>
<p><img src="https://raw.githubusercontent.com/xuejy19/xuejy19.github.io/source/Img/computation_graph.png" alt="计算图"></p>
<p>下面来介绍如何使用计算图来进行梯度计算，首先我们计算在图中相连接的节点之间的梯度,将对应的梯度值写到边上，如下图所示:<br>
<img src="https://raw.githubusercontent.com/xuejy19/xuejy19.github.io/source/Img/full_graph.png" alt="梯度计算图"><br>
要计算损失$L$到某个变量$w$的梯度只需要按照以下步骤:</p>
<ol>
<li>找到从$L$到$w$所有可能的路径</li>
<li>对于各个路径，将沿路径所有边相乘</li>
<li>将各个路径计算结果相加</li>
</ol>
<h3 id="低阶API和高阶API">低阶API和高阶API</h3>
<p>我们在使用pytorch时主要接触到的pytorch的包的架构如下图所示:<br>
<img src="https://raw.githubusercontent.com/xuejy19/xuejy19.github.io/source/Img/Pytorch-package-hierarchy.jpg" alt="pytorch主要包"><br>
对于一个三层的全连结网络，我们可以仅仅使用tensor来实现，不过这种情况下需要手写梯度，下面我就一步步介绍如何用api来使你的代码变得简洁高效:</p>
<h4 id="tensor">tensor</h4>
<pre class="line-numbers language-language-python"><code class="language-language-python"># -*- coding: utf-8 -*-

import torch


dtype = torch.float
device = torch.device("cpu")
# device = torch.device("cuda:0") # Uncomment this to run on GPU

# N is batch size; D_in is input dimension;
# H is hidden dimension; D_out is output dimension.
N, D_in, H, D_out = 64, 1000, 100, 10

# Create random input and output data
x = torch.randn(N, D_in, device=device, dtype=dtype)
y = torch.randn(N, D_out, device=device, dtype=dtype)

# Randomly initialize weights
w1 = torch.randn(D_in, H, device=device, dtype=dtype)
w2 = torch.randn(H, D_out, device=device, dtype=dtype)

learning_rate = 1e-6
for t in range(500):
    # Forward pass: compute predicted y
    h = x.mm(w1)
    h_relu = h.clamp(min=0)
    y_pred = h_relu.mm(w2)

    # Compute and print loss
    loss = (y_pred - y).pow(2).sum().item()
    if t % 100 == 99:
        print(t, loss)

    # Backprop to compute gradients of w1 and w2 with respect to loss
    grad_y_pred = 2.0 * (y_pred - y)
    grad_w2 = h_relu.t().mm(grad_y_pred)
    grad_h_relu = grad_y_pred.mm(w2.t())
    grad_h = grad_h_relu.clone()
    grad_h[h < 0] = 0
    grad_w1 = x.t().mm(grad_h)

    # Update weights using gradient descent
    w1 -= learning_rate * grad_w1
    w2 -= learning_rate * grad_w2
<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>
<h4 id="autograd">autograd</h4>
<p>这种情况下其实是没有显示的模型的概念的，只有自己定义的一个个参数。但当神经网络复杂之后，手算梯度变得不再现实，pytorch为解决这个问题提供了<code>autograd</code>这样一个好用的工具，如果我们将需要计算梯度的参数设置为<code>requires_grad = True</code>,那么在进行前向计算过程中，会定义一个计算图，同时会保存对应参数的梯度信息，也就是前面介绍的计算图中边上的梯度信息，通过<code>loss.backward()</code>操作，可以将损失对各个参数的梯度计算出来，由此可以重写上面的代码如下:</p>
<pre class="line-numbers language-language-python"><code class="language-language-python"># -*- coding: utf-8 -*-
# -*- coding: utf-8 -*-
import torch

dtype = torch.float
device = torch.device("cpu")
# device = torch.device("cuda:0") # Uncomment this to run on GPU

# N is batch size; D_in is input dimension;
# H is hidden dimension; D_out is output dimension.
N, D_in, H, D_out = 64, 1000, 100, 10

# Create random Tensors to hold input and outputs.
# Setting requires_grad=False indicates that we do not need to compute gradients
# with respect to these Tensors during the backward pass.
x = torch.randn(N, D_in, device=device, dtype=dtype)
y = torch.randn(N, D_out, device=device, dtype=dtype)

# Create random Tensors for weights.
# Setting requires_grad=True indicates that we want to compute gradients with
# respect to these Tensors during the backward pass.
w1 = torch.randn(D_in, H, device=device, dtype=dtype, requires_grad=True)
w2 = torch.randn(H, D_out, device=device, dtype=dtype, requires_grad=True)

learning_rate = 1e-6
for t in range(500):
    # Forward pass: compute predicted y using operations on Tensors; these
    # are exactly the same operations we used to compute the forward pass using
    # Tensors, but we do not need to keep references to intermediate values since
    # we are not implementing the backward pass by hand.
    y_pred = x.mm(w1).clamp(min=0).mm(w2)

    # Compute and print loss using operations on Tensors.
    # Now loss is a Tensor of shape (1,)
    # loss.item() gets the scalar value held in the loss.
    loss = (y_pred - y).pow(2).sum()
    if t % 100 == 99:
        print(t, loss.item())

    # Use autograd to compute the backward pass. This call will compute the
    # gradient of loss with respect to all Tensors with requires_grad=True.
    # After this call w1.grad and w2.grad will be Tensors holding the gradient
    # of the loss with respect to w1 and w2 respectively.
    loss.backward()

    # Manually update weights using gradient descent. Wrap in torch.no_grad()
    # because weights have requires_grad=True, but we don't need to track this
    # in autograd.
    # An alternative way is to operate on weight.data and weight.grad.data.
    # Recall that tensor.data gives a tensor that shares the storage with
    # tensor, but doesn't track history.
    # You can also use torch.optim.SGD to achieve this.
    with torch.no_grad():
        w1 -= learning_rate * w1.grad
        w2 -= learning_rate * w2.grad

        # Manually zero the gradients after updating weights
        w1.grad.zero_()
        w2.grad.zero_()
<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>
<h4 id="Extend-autograd-function">Extend autograd function</h4>
<p>可以看到，有了<code>autograd</code>这个工具后，复杂网络的反向梯度计算也很容易解决。虽然<code>autograd.Function</code>中已经基本包含了我们所经常用到的运算函数(自动微分算子)，但若我们希望将一个其中没有的函数也加入到计算图中，则需要对<code>autograd.Function</code>进行扩展，比如我们可以重写<code>ReLu</code>函数，然后将其纳入自动微分:</p>
<pre class="line-numbers language-language-python"><code class="language-language-python"># -*- coding: utf-8 -*-
import torch


class MyReLU(torch.autograd.Function):
    """
    We can implement our own custom autograd Functions by subclassing
    torch.autograd.Function and implementing the forward and backward passes
    which operate on Tensors.
    """

    @staticmethod
    def forward(ctx, input):
        """
        In the forward pass we receive a Tensor containing the input and return
        a Tensor containing the output. ctx is a context object that can be used
        to stash information for backward computation. You can cache arbitrary
        objects for use in the backward pass using the ctx.save_for_backward method.
        """
        ctx.save_for_backward(input)
        return input.clamp(min=0)

    @staticmethod
    def backward(ctx, grad_output):
        """
        In the backward pass we receive a Tensor containing the gradient of the loss
        with respect to the output, and we need to compute the gradient of the loss
        with respect to the input.
        """
        input, = ctx.saved_tensors
        grad_input = grad_output.clone()
        grad_input[input < 0] = 0
        return grad_input


dtype = torch.float
device = torch.device("cpu")
# device = torch.device("cuda:0") # Uncomment this to run on GPU

# N is batch size; D_in is input dimension;
# H is hidden dimension; D_out is output dimension.
N, D_in, H, D_out = 64, 1000, 100, 10

# Create random Tensors to hold input and outputs.
x = torch.randn(N, D_in, device=device, dtype=dtype)
y = torch.randn(N, D_out, device=device, dtype=dtype)

# Create random Tensors for weights.
w1 = torch.randn(D_in, H, device=device, dtype=dtype, requires_grad=True)
w2 = torch.randn(H, D_out, device=device, dtype=dtype, requires_grad=True)

learning_rate = 1e-6
for t in range(500):
    # To apply our Function, we use Function.apply method. We alias this as 'relu'.
    relu = MyReLU.apply

    # Forward pass: compute predicted y using operations; we compute
    # ReLU using our custom autograd operation.
    y_pred = relu(x.mm(w1)).mm(w2)

    # Compute and print loss
    loss = (y_pred - y).pow(2).sum()
    if t % 100 == 99:
        print(t, loss.item())

    # Use autograd to compute the backward pass.
    loss.backward()

    # Update weights using gradient descent
    with torch.no_grad():
        w1 -= learning_rate * w1.grad
        w2 -= learning_rate * w2.grad

        # Manually zero the gradients after updating weights
        w1.grad.zero_()
        w2.grad.zero_()
<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>
<h4 id="nn-Module">nn.Module</h4>
<p>如果我们要构建一个深层神经网络，固然可以用这些基本的自动微分算子来构建，但这样做显得有些<code>low-level</code>，因为神经网络在进行理论推导时实际上就是<code>层的堆砌</code>，那么在进行编程实现时，可否沿用这个思想，将一些微分算子封装成神经网络层的形式，pytorch中实现这个功能是通过包<code>nn</code>，<code>nn</code>中除了定义了一系列模块<code>Modules</code>，同时还有大量的损失函数，下面我们就用<code>torch.nn.Sequential</code>来重新实现这样一个三层神经网络:</p>
<pre class="line-numbers language-language-python"><code class="language-language-python">import torch

# N is batch size; D_in is input dimension;
# H is hidden dimension; D_out is output dimension.
N, D_in, H, D_out = 64, 1000, 100, 10

# Create random Tensors to hold inputs and outputs
x = torch.randn(N, D_in)
y = torch.randn(N, D_out)

# Use the nn package to define our model as a sequence of layers. nn.Sequential
# is a Module which contains other Modules, and applies them in sequence to
# produce its output. Each Linear Module computes output from input using a
# linear function, and holds internal Tensors for its weight and bias.
model = torch.nn.Sequential(
    torch.nn.Linear(D_in, H),
    torch.nn.ReLU(),
    torch.nn.Linear(H, D_out),
)

# The nn package also contains definitions of popular loss functions; in this
# case we will use Mean Squared Error (MSE) as our loss function.
loss_fn = torch.nn.MSELoss(reduction='sum')

learning_rate = 1e-4
for t in range(500):
    # Forward pass: compute predicted y by passing x to the model. Module objects
    # override the __call__ operator so you can call them like functions. When
    # doing so you pass a Tensor of input data to the Module and it produces
    # a Tensor of output data.
    y_pred = model(x)

    # Compute and print loss. We pass Tensors containing the predicted and true
    # values of y, and the loss function returns a Tensor containing the
    # loss.
    loss = loss_fn(y_pred, y)
    if t % 100 == 99:
        print(t, loss.item())

    # Zero the gradients before running the backward pass.
    model.zero_grad()

    # Backward pass: compute gradient of the loss with respect to all the learnable
    # parameters of the model. Internally, the parameters of each Module are stored
    # in Tensors with requires_grad=True, so this call will compute gradients for
    # all learnable parameters in the model.
    loss.backward()

    # Update the weights using gradient descent. Each parameter is a Tensor, so
    # we can access its gradients like we did before.
    with torch.no_grad():
        for param in model.parameters():
            param -= learning_rate * param.grad
<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>
<p>可以看到，与不使用<code>nn</code>包相比，上面的代码实现一方面利用<code>nn.Module.Sequential</code>实现了对神经网络层的封装，另一方面应用<code>nn.MSELoss</code>来替代原始的手写损失函数，使得代码的可读性和简洁性进一步提升。同时需要注意的是，在进行反向传播之前通过<code>model.zero_grad()</code>将存储的梯度归0，在进行参数更新时必须要使用<code>with torch.no_grad()</code>上下文管理语句来避免在进行参数更新时仍记录梯度，提高计算速度。</p>
<h4 id="optim">optim</h4>
<p>在实际进行优化时，我们不仅会用到<code>SGD</code>这种常规的优化算法，同时也会用到一些复杂的优化算法如<code>AdaGrad</code>,<code>RMSprop</code>, <code>Adam</code>等，<code>torch.optim</code>提供了大部分常用的优化算法，同时应用优化器后也不需要再使用<code>with torch.no_grad()</code>语句，可以直接使用<code>optimizer.step()</code>来实现参数更新，重写这个三层神经网络:</p>
<pre class="line-numbers language-language-python"><code class="language-language-python"># -*- coding: utf-8 -*-
import torch

# N is batch size; D_in is input dimension;
# H is hidden dimension; D_out is output dimension.
N, D_in, H, D_out = 64, 1000, 100, 10

# Create random Tensors to hold inputs and outputs
x = torch.randn(N, D_in)
y = torch.randn(N, D_out)

# Use the nn package to define our model and loss function.
model = torch.nn.Sequential(
    torch.nn.Linear(D_in, H),
    torch.nn.ReLU(),
    torch.nn.Linear(H, D_out),
)
loss_fn = torch.nn.MSELoss(reduction='sum')

# Use the optim package to define an Optimizer that will update the weights of
# the model for us. Here we will use Adam; the optim package contains many other
# optimization algorithms. The first argument to the Adam constructor tells the
# optimizer which Tensors it should update.
learning_rate = 1e-4
optimizer = torch.optim.Adam(model.parameters(), lr=learning_rate)
for t in range(500):
    # Forward pass: compute predicted y by passing x to the model.
    y_pred = model(x)

    # Compute and print loss.
    loss = loss_fn(y_pred, y)
    if t % 100 == 99:
        print(t, loss.item())

    # Before the backward pass, use the optimizer object to zero all of the
    # gradients for the variables it will update (which are the learnable
    # weights of the model). This is because by default, gradients are
    # accumulated in buffers( i.e, not overwritten) whenever .backward()
    # is called. Checkout docs of torch.autograd.backward for more details.
    optimizer.zero_grad()

    # Backward pass: compute gradient of the loss with respect to model
    # parameters
    loss.backward()

    # Calling the step function on an Optimizer makes an update to its
    # parameters
    optimizer.step()
<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>
<h4 id="Custom-nn-Modules">Custom nn Modules</h4>
<p>在前面定义这个神经网络是通过<code>torch.nn.Sequential</code>将各层函数封装成一个模型，有时可能需要定义一个更加复杂的模型，而这个模型中的有些模块需要自己手动实现，此时可以通过定义一个继承<code>torch.nn.Module</code>的子类来实现这个功能：</p>
<pre class="line-numbers language-language-python"><code class="language-language-python"># -*- coding: utf-8 -*-
import torch


class TwoLayerNet(torch.nn.Module):
    def __init__(self, D_in, H, D_out):
        """
        In the constructor we instantiate two nn.Linear modules and assign them as
        member variables.
        """
        super(TwoLayerNet, self).__init__()
        self.linear1 = torch.nn.Linear(D_in, H)
        self.linear2 = torch.nn.Linear(H, D_out)

    def forward(self, x):
        """
        In the forward function we accept a Tensor of input data and we must return
        a Tensor of output data. We can use Modules defined in the constructor as
        well as arbitrary operators on Tensors.
        """
        h_relu = self.linear1(x).clamp(min=0)
        y_pred = self.linear2(h_relu)
        return y_pred


# N is batch size; D_in is input dimension;
# H is hidden dimension; D_out is output dimension.
N, D_in, H, D_out = 64, 1000, 100, 10

# Create random Tensors to hold inputs and outputs
x = torch.randn(N, D_in)
y = torch.randn(N, D_out)

# Construct our model by instantiating the class defined above
model = TwoLayerNet(D_in, H, D_out)

# Construct our loss function and an Optimizer. The call to model.parameters()
# in the SGD constructor will contain the learnable parameters of the two
# nn.Linear modules which are members of the model.
criterion = torch.nn.MSELoss(reduction='sum')
optimizer = torch.optim.SGD(model.parameters(), lr=1e-4)
for t in range(500):
    # Forward pass: Compute predicted y by passing x to the model
    y_pred = model(x)

    # Compute and print loss
    loss = criterion(y_pred, y)
    if t % 100 == 99:
        print(t, loss.item())

    # Zero gradients, perform a backward pass, and update the weights.
    optimizer.zero_grad()
    loss.backward()
    optimizer.step()
<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>
<p>可以看到，这样做其实与直接使用<code>torch.nn.Sequential</code>来定义模型在有些情况下是差不多的，但通过继承<code>nn.Module</code>一方面可以使得自己的模型更具扩展性，同时代码的可读性也进一步加强，因为你自己的神经网络模型是以一个类的形式存在的。</p>
<h4 id="TensorDataset">TensorDataset</h4>
<p>在进行数据处理时，我们的数据基本都是<code>x_train,y_train,x_test,y_test</code>的形式，在进行迭代时可能必须要通过下标来进行迭代，而为了让迭代操作更加方便，<code>pytorch</code>引入了<code>torch.utils.data</code>来辅助进行数据处理，而其中<code>TensorDataset()</code>方法相当于是对<code>x,y</code>做了一个<code>wrap</code>操作，在进行迭代时可以一起迭代而不需要分别迭代：</p>
<blockquote>
<p>应用<code>TensorDataset()</code>方法:</p>
<pre class="line-numbers language-language-python"><code class="language-language-python">train_ds = TensorDataset(x_train, y_train)
<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre>
<p>应用前迭代：</p>
<pre class="line-numbers language-language-python"><code class="language-language-python">xb = x_train[start_i:end_i]
yb = y_train[start_i:end_i]
<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre>
<p>应用后迭代：</p>
<pre class="line-numbers language-language-python"><code class="language-language-python">xb,yb = train_ds[i*bs : i*bs+bs]
<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre>
</blockquote>
<h4 id="DataLoader">DataLoader</h4>
<p>虽然使用<code>TensorDataset</code>使得迭代运算变得简单，但我们还是按照<code>batch_size</code>的大小来对迭代下标进行维护，为了能够更方便地应用数据集进行训练，<code>torch.utils.data</code>又为我们提供了另一个好用的工具<code>DataLoader</code>,它可以将<code>warp</code>之后的数据集按照<code>batch_size</code>大小进行分割，然后在迭代时每次取一个<code>batch</code>的数据。</p>
<blockquote>
<p>应用<code>DataLoader</code>对数据集进行处理：</p>
<pre class="line-numbers language-language-python"><code class="language-language-python">train_ds = TensorDataset(x_train, y_train)
train_dl = DataLoader(train_ds, batch_size=bs)
<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre>
<p>对处理后的数据进行迭代：</p>
<pre class="line-numbers language-language-python"><code class="language-language-python">for xb,yb in train_dl:
   pred = model(xb)
<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre>
</blockquote>
<h4 id="MNIST数据集CNN代码">MNIST数据集CNN代码</h4>
<p>有了应用上面的那些封装好的api，在MNIST数据集上实现一个CNN代码便不再困难，下面给出代码：</p>
<pre class="line-numbers language-language-python"><code class="language-language-python">from pathlib import Path
from matplotlib import pyplot as plt
from IPython.core.debugger import set_trace
from torch import nn
from torch import optim
from torch.utils.data import TensorDataset
from torch.utils.data import DataLoader
import torch.nn.functional as F
import numpy as np
import requests
import math
import pickle
import gzip
import torch

'''
class Mnist_logistic(nn.Module):
    def __init__(self):
        super().__init__()
        self.weights = nn.Parameter(torch.randn(784,10)/math.sqrt(784))
        self.bias = nn.Parameter(torch.zeros(10))
    def forward(self, xb):
        return xb @ self.weights + self.bias
'''
'''
class Mnist_logistic(nn.Module):
    def __init__(self):
        super().__init__()
        self.linear = nn.Linear(784,10)
    def forward(self, x):
        return self.linear(x)
'''
def get_model():
    model = Mnist_CNN().to(device)
    opt = optim.SGD(model.parameters(), lr = 0.5)
    return model, opt
def preprocess(x, y):
    return x.view(-1,1,28,28).to(device), y.to(device)
class WrappedDataLoader:
    def __init__(self, d1, func, batch_size):
        self.d1 = d1
        self.func = func 
        self.batch_size = 64
    def __len__(self):
        return len(self.d1)
    def __iter__(self):
        batches = iter(self.d1)
        for b in batches:
            yield (self.func(*b))
class Mnist_CNN(nn.Module):
    def __init__(self):
        super().__init__()
        self.conv1 = nn.Conv2d(1, 16, kernel_size=3, stride=2, padding=1)
        self.conv2 = nn.Conv2d(16, 16, kernel_size=3, stride=2, padding=1)
        self.conv3 = nn.Conv2d(16, 10, kernel_size=3, stride=2, padding=1)
        self.relu = nn.ReLU()
        self.pooling = nn.AdaptiveAvgPool2d(1)
    def forward(self, xb):
        xb = self.relu(self.conv1(xb))
        xb = self.relu(self.conv2(xb))
        xb = self.relu(self.conv3(xb))
        xb = self.pooling(xb)
        return xb.view(-1,xb.size(1))
def loss_batch(moedl, loss_func, xb, yb, opt = None):
    loss = loss_func(model(xb), yb)
    if opt is not None:
        loss.backward()
        opt.step()
        opt.zero_grad()
    return loss.item(), len(xb)

def fit(epoches, model, loss_func, opt, train_dl, valid_dl):
    for epoch in range(epoches):
        model.train()
        for xb, yb in train_dl:
            loss_batch(model, loss_func, xb, yb, opt)
        model.eval()
        with torch.no_grad():
            losses, nums = zip(*[loss_batch(model, loss_func, xb, yb) for xb, yb in valid_dl])
            val_loss = np.sum(np.multiply(losses, nums)/np.sum(nums))
            if epoch % 10 == 0:
                train_aur, valid_aur = cal_acur(model, train_dl, valid_dl)    
                print('epoch:',epoch,'验证集损失:',np.str(val_loss),'训练集准确率:',np.str(train_aur),'验证集准确率:',np.str(valid_aur))

def get_data(train_ds, valid_ds, bs):
    train_dl = DataLoader(train_ds, batch_size = bs, shuffle = True)
    valid_dl = DataLoader(valid_ds, batch_size = bs, shuffle = True)
    return train_dl, valid_dl
def batch_acu(y_pred, y):
    batch_pred = torch.argmax(y_pred, dim = 1)
    return (batch_pred == y).sum()

def cal_acur(model, train_dl, valid_dl):
    train_pred_right = 0
    valid_pred_right = 0
    for xb, yb in train_dl:
        y_pred = model(xb) 
        train_pred_right += batch_acu(y_pred, yb)
    for xb, yb in valid_dl:
        y_pred = model(xb)
        valid_pred_right += batch_acu(y_pred, yb)
    train_acur = train_pred_right.float()/(len(train_dl)*train_dl.batch_size) 
    valid_acur = valid_pred_right.float()/(len(valid_dl)*valid_dl.batch_size)
    return train_acur.item(), valid_acur.item()
        

		

if __name__ == '__main__':
    device = torch.device('cuda:0' if torch.cuda.is_available() else 'cpu')
    batch_size = 64
    FILENAME = "data/mnist/mnist.pkl.gz"
    with gzip.open(FILENAME, "rb") as f:
        ((x_train, y_train), (x_valid, y_valid), _) = pickle.load(f, encoding="latin-1")
    x_train, y_train, x_valid, y_valid = map(torch.tensor, (x_train, y_train, x_valid, y_valid))
    print(torch.cuda.get_device_name(0))
    train_ds = TensorDataset(x_train, y_train)
    valid_ds = TensorDataset(x_valid, y_valid)
    loss_func = F.cross_entropy
    epoches = 100
    # train and valid
    train_dl, valid_dl = get_data(train_ds, valid_ds, batch_size)
    train_dl = WrappedDataLoader(train_dl, preprocess, batch_size)
    valid_dl = WrappedDataLoader(valid_dl, preprocess, batch_size)
    model, opt = get_model()
    fit(epoches, model, loss_func, opt, train_dl, valid_dl)

<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>
<h3 id="总结">总结</h3>
<p>最后总结一下<code>pytorch</code>中常用的包：</p>
<ul>
<li><code>torch.nn:</code>包含搭建神经网络层的模块(Modules)和一系列<code>loss</code>函数</li>
<li><code>torch.nn.functional:</code>提供常用的激活函数，这算是一个比较低级的api，大部分激活函数在<code>nn</code>下都有封装</li>
<li><code>torch.autograd:</code> 提供所有张量操作的自动求导方法</li>
<li><code>torch.optim:</code> 提供各种参数优化方法，如<code>SGD</code>、<code>Adam</code>、<code>AdaGrad</code>等</li>
<li><code>torch.utils.data:</code> 用于加载数据，比如上面介绍的<code>TensorDataset()</code>和<code>DataLoader()</code>方法</li>
<li><code>torchvision.datasets:</code>加载图像处理领域常用数据集，如<code>MNIST</code>、<code>coco</code>、<code>CIFAR10</code>、<code>Imagenet</code>等</li>
<li><code>torchvision.modules:</code> 图像处理领域常用模型，如<code>AlexNet</code>、<code>VGG</code>、<code>ResNet</code>、<code>DenseNet</code>等</li>
<li><code>torchvision.transforms:</code> 图片相关处理操作，如裁剪、尺寸缩放、归一化等</li>
</ul>
<p><code>pytorch</code>的学习其实并不是孤立的，其实是与其他知识相耦合的:</p>
<ul>
<li><code>python</code>语法知识</li>
<li>算法理论知识</li>
</ul>
<p>如果你本身<code>python</code>用的非常熟练，同时算法的理论基础也比较扎实，那么<code>pytorch</code>对你来说可能就是一个非常容易上手的工具，但如果本身这两方面的知识不够牢固，在学习<code>pytorch</code>的过程中，这两方面的能力也能得到很大的提高。</p>
]]></content>
      <categories>
        <category>统计学习</category>
      </categories>
      <tags>
        <tag>pytorch，深度学习框架</tag>
      </tags>
  </entry>
  <entry>
    <title>条件随机场</title>
    <url>/2020/08/21/tiao-jian-sui-ji-chang/</url>
    <content><![CDATA[<p>另一种常用模型为条件随机场(CRF)模型,条件随机场是给定一组输入随机变量条件下另一组输出随机变量的条件概率分布模型，其特点是假设输出随机变量构成马尔可夫随机场。本章节按照以下几部分组织：</p>
<ul>
<li>概率无向图模型</li>
<li>条件随机场定义与形式</li>
<li>条件随机场概率计算</li>
<li>条件随机场学习算法</li>
<li>条件随机场预测算法</li>
</ul>
<a id="more"></a>
<h3 id="概率无向图模型">概率无向图模型</h3>
<h4 id="概率无向图模型定义">概率无向图模型定义</h4>
<p>图是由节点和及连接的边组成的集合，表示为$G = (V,E)$，若连接的边无方向，则称为无向图，反之则称为有向图。<br>
概率图模型是由图表示的概率分布，图中的节点表示随机变量，节点之间的边表示变量之间的概率依赖关系。<br>
给定一个联合概率分布$P(Y)$和表示它的无向图$G$,首先定义无向图表示的随机变量之间存在的成对马尔可夫性、局部马尔可夫性和全局马尔可夫性。</p>
<blockquote>
<ul>
<li>成对马尔可夫性：设$\mu$和$v$是无向图$G$中任意两个没有边连接的节点，两个节点分别对应随机变量$Y_{\mu},Y_v$。其他所有节点的集合记做$O$,对应随机变量组是$Y_O$。成对马尔可夫性是指给定随机变量组$Y_O$的条件下，随机变量$Y_{\mu}$和$Y_v$是条件独立的,即：<br>
$$<br>
P(Y_{\mu},Y_v|Y_O) = P(Y_{\mu}|Y_O)P(Y_{v}|Y_O)<br>
$$</li>
<li>局部马尔可夫性：设$v \in V$是无向图$G$中任意一个节点，$W$是与$v$有边连接的所有节点，$O$是$v$和$W$以外的其他所有节点。$v,W,O$对应的随机变量(组)记做$Y_v,Y_W,Y_O$,局部马尔可夫性是指在给定随机变量组$Y_W$条件下，随机变量$Y_v$与随机变量组$Y_O$是相互独立的:<br>
$$<br>
P(Y_{O},Y_v|Y_W) = P(Y_{v}|Y_W)P(Y_{O}|Y_W)<br>
$$<br>
在$P(Y_O|Y_W)&gt;0$情况下，有：<br>
$$<br>
P(Y_v|Y_W) = P(Y_v|Y_W,Y_O)<br>
$$</li>
<li>全局马尔可夫性：设结点集合$A,B$是在无向图$G$中被节点集合$C$分开的任意节点集合，它们所对应的变量组记做$Y_A,Y_B,Y_C$。全局马尔可夫性是指给定随机变量组$Y_C$条件下随机变量组$Y_A，Y_B$是条件独立的，即：<br>
$$<br>
P(Y_A,Y_B|Y_C) = P(Y_A|Y_C)P(Y_B|Y_C)<br>
$$</li>
</ul>
</blockquote>
<p>下面定义概率无向图模型:</p>
<blockquote>
<p><strong>概率无向图模型:</strong> 设有联合概率分布$P(Y)$，由无向图$G = (V,E)$表示，在图$G$中，节点表示随机变量，边表示随机变量之间的概率依赖关系。若联合概率分布$P(Y)$满足<strong>成对、局部或全局马尔可夫性</strong>,就称此概率分布为概率无向图模型，或马尔可夫随机场。</p>
</blockquote>
<h4 id="概率无向图模型的因子分解">概率无向图模型的因子分解</h4>
<p>能够使用图模型来对联合概率分布进行编码，主要出发点便是变量之间的马尔可夫性假设，这让我们可以以更加紧凑的形式来表示联合概率分布。<br>
首先给出无向图中团与最大团的定义:</p>
<blockquote>
<p><strong>团与最大团</strong>:无向图中任何两个节点均有边连接的子集称为团(clique)。若$C$是无向图$G$的一个团，并且不能够再加入任何一个$G$的节点使其成为一个更大的团，则称此$C$为最大团。</p>
</blockquote>
<p>将概率无向图模型的联合概率分布表示为其最大团上的随机变量的函数的乘积的操作，称为概率无向图模型的因子分解。<br>
给定概率图模型，设其无向图为$G$，$C$为$G$上的最大团，$Y_C$表示$C$对应的随机变量，那么联合概率分布$P(Y)$可以表示成图中所有最大团$C$上函数$\varPhi_C(Y_C)$的<strong>连乘形式</strong>:<br>
$$<br>
P(Y) = \frac{1}{Z} \prod_{C} \varPhi_C(Y_C)<br>
$$<br>
公式中$Z$是一个归一化因子$Z = \sum_Y \prod_{C} \varPhi_C(Y_C)$。函数$\varPhi_C(Y_C)$称为势函数。势函数通常定义为指数函数:<br>
$$<br>
\varPhi_C(Y_C) = exp{ -E(Y_C)}<br>
$$<br>
概率无向图的因子分解由Hammersley-Clifford定理保证:</p>
<blockquote>
<p>Hammersley-Clifford定理: 概率无向图模型的联合概率分布$P(Y)$可以表示为如下形式:<br>
$$<br>
P(Y) = \frac{1}{Z} \prod_C \varPhi_C(Y_C)<br>
$$<br>
其中，$C$是无向图的最大团，$Y_C$是$C$的节点对应的随机变量，$\varPhi_C(Y_C)$是$C$上定义的严格正函数，乘积是在无向图所有的最大团上进行的，关于该定理的证明参考链接<a href="https://www.jianshu.com/p/dd27249b8c4a">Hammersley-Clifford定理证明</a></p>
</blockquote>
<h3 id="条件随机场的定义与形式">条件随机场的定义与形式</h3>
<h4 id="条件随机场的定义">条件随机场的定义</h4>
<p>条件随机场是给定随机变量$X$条件下，随机变量$Y$的马尔可夫随机场。本文主要介绍线性链条件随机场，该模型可用于标注等问题。这时，在条件概率模型$P(Y|X)$中，$Y$是输出变量，表示标记序列(状态序列)，$X$是输入变量，表示需要标注的观测序列。学习时，利用训练数据集通过极大似然估计或正则化的极大似然估计得到条件概率模型$\hat{P}(Y|X)$;预测时，对于给定的输入序列$x$,求出条件概率$\hat{P}(y|x)$最大的输出序列$\hat{y}$。<br>
首先给出一般的条件随机场的定义:</p>
<blockquote>
<p><strong>条件随机场</strong>：设$X$和$Y$是随机变量，$P(Y|X)$是在给定$X$条件下$Y$的条件概率分布。若随机变量$Y$构成一个由无向图$G = (V,E)$表示的马尔可夫随机场，即：<br>
$$<br>
P(Y_v|X,Y_w,w \neq v) = P(Y_v|X,Y_w,w\sim v)<br>
$$<br>
对任意节点$v$成立，则称条件概率分布$P(Y|X)$为条件随机场。公式中$w \sim v$表示在图$G = (V,E)$中与节点$v$有边连接的所有节点$w$,$w \neq v$表示节点$v$以外的所有节点，$Y_v,Y_w,Y_{\mu}$为节点$v,\mu,w$所对应的随机变量。</p>
</blockquote>
<p>虽然在定义中并没有要求$X$和$Y$具有相同的结构，但在现实中，一般假设$X$和$Y$具有相同的结构，而对于线性链，则是图$G = (V,E)$可以表示成：<br>
$$<br>
G = (V = { 1,2,\dots,n}, E = {(i,i+1)}),\quad i = 1,2,\dots,n-1<br>
$$<br>
<img src="https://raw.githubusercontent.com/xuejy19/xuejy19.github.io/source/Img/SRF.png" alt="线性链条件随机场"><br>
下面给出线性链条件随机场的定义：</p>
<blockquote>
<p><strong>线性链条件随机场</strong>：设$X = (X_1,X_2,\dots,X_n),Y= (Y_1,Y_2,\dots,Y_n)$均为线性链表示的随机变量序列，若在给定随机变量序列$X$的条件下，随机变量序列$Y$的条件概率分布$P(Y|X)$构成条件随机场，即满足马尔可夫性：<br>
$$<br>
P(Y_i|X,Y_1,\dots,Y_{i-1},Y_{i+1},\dots,Y_n) = P(Y_i|X,Y_{i-1},Y_{i+1}),i =1,\dots,n<br>
$$<br>
则称$P(Y|X)$为线性链条件随机场。在标注问题中，$X$表示输入观测序列，$Y$表示对应的输出标记序列或状态序列。</p>
</blockquote>
<h4 id="条件随机场的参数化形式">条件随机场的参数化形式</h4>
<p>下面给出条件随机场的参数化形式：</p>
<blockquote>
<p><strong>线性链条件随机场参数化形式</strong>：设$P(Y|X)$为线性链条件随机场，则在随机变量$X$取值为$x$条件下，随机变量$Y$取值为$y$的条件概率具有如下形式:<br>
$$<br>
P(y|x) = \frac{1}{Z(x)} exp(\sum_{i,k} \lambda_k t_k(y_{i-1},y_i,x,i) + \sum_{i,l} \mu_l s_l(y_i,x,i) )<br>
$$<br>
其中$Z(x)$为归一化因子，保证$P(y|x)$满足概率形式，$t_k$和$s_l$是特征函数，$\lambda_k$和$\mu_l$是对应的权值。</p>
</blockquote>
<p>定义中公式表示给定输入序列$x$，对输出序列$y$预测的条件概率。$t_k$是定义在边上的特征函数，称为转移特征，依赖于当前和前一个位置；$s_l$是定义在节点上的特征函数，称为状态特征，依赖于当前位置。$t_k$和$s_l$都依赖于位置，是局部特征函数。通常，特征函数$t_k$和$s_l$取值为0或1，当满足特征条件时取值为1，否则为0。条件随机场完全由特征函数$t_k,s_l$和对应的权值$\lambda_k,\mu_l$确定。<br>
为便于理解，下面给出一个线性链条件随机场的栗子：</p>
<blockquote>
<p><strong>🌰：</strong> 设有一标注问题：输入观测序列为$X = (X_1,X_2,X_3)$,输出标记序列为$Y = (Y_1,Y_2,Y_3)$,输出标记取之于$\mathcal{Y} = { 1,2 }$<br>
假设特征$t_k,s_l$和对应的权值$\lambda_k,\mu_l$如下：<br>
$$<br>
\begin{aligned}<br>
t_1 &amp;=  t_1(y_{i-1} = 1,y_i = 2,x,i) \quad i =2,3, \lambda_1 = 1 \<br>
t_2 &amp;= t_2(y_1 = 1,y_2 = 1,x,2),\quad \lambda_2 = 0.6 \<br>
t_3 &amp;= t_3(y_2 = 2,y_3 =1,x,3), \quad \lambda_3 = 1 \<br>
t_4 &amp;= t_4(y_1 = 2,y_2 = 1,x,2), \quad \lambda_4 = 1 \<br>
t_5 &amp;= t_5(y_2 = 2,y_3 =2,x,3),\quad \lambda_5 = 0.2 \<br>
s_1 &amp;= s_1(y_1 = 1,x,1), \qquad \qquad \mu_1 = 1 \<br>
s_2 &amp;= s_2(y_i =2,x,i),i =1,2 \quad \mu_2 = 0.5 \<br>
s_3 &amp;= s_3(y_i =1,x,i),i =1,2 \quad \mu_2 = 0.8 \<br>
s_4 &amp;= s_4(y_3 =2,x,3),i =1,2 \quad \mu_2 = 0.5<br>
\end{aligned}<br>
$$<br>
对于给定的观测序列，求标记序列为$y = (y_1,y_2,y_3) = (1,2,2)$的非规范条件概率(不进行归一化)<br>
<strong>解</strong>：由线性链条件随机场的参数化形式可得:<br>
$$<br>
P(y_1 = 1,y_2 = 2,y_3 =2|x) \propto exp(3.2)<br>
$$</p>
</blockquote>
<h4 id="条件随机场的简化形式">条件随机场的简化形式</h4>
<p>注意到条件随机场的参数化形式中同一特征在各个位置都有定义，因此可以考虑对同一个特征在各个位置求和，将局部特征函数转化成一个全局特征函数，这样就可以将条件随机场写成权值向量和特征向量的内积形式，即条件随机场的简化形式。<br>
假设有$K_1$个转移特征，$K_2$个状态特征，$K = K_1 + K_2$,记：<br>
$$<br>
f_k(y_{i-1},y_i,x,i) = \begin{cases}<br>
t_k(y_{i-1},y_i,x,i), &amp; k =1,2,\dots,K_1 \<br>
s_l(y_i,x,i), &amp; k = K_1 + l; l = 1,2,\dots,K_2<br>
\end{cases}<br>
$$<br>
然后对转移与状态特征在各个位置$i$求和，记做：<br>
$$<br>
f_k(y,x) = \sum_{i=1}^n f_k(y_{i-1},y_i, x,i),\quad k = 1,2,\dots,K<br>
$$<br>
用$w_k$表示特征$f_k(y,x)$的权值，即：<br>
$$<br>
w_k = \begin{cases}<br>
\lambda_k, &amp; k =1,2,\dots, K_1 \<br>
\mu_l , &amp; k = K_1 + l; l= 1,2,\dots,K_2<br>
\end{cases}<br>
$$<br>
于是条件随机场参数化可以写成更加紧凑的形式：<br>
$$<br>
\begin{aligned}<br>
P(y|x) &amp;= \frac{1}{Z(x)} exp \sum_{k=1}^K w_k f_k(y,x)  \<br>
Z(x) &amp;= \sum_y exp \sum_{k=1}^K w_k f_k(y,x)<br>
\end{aligned}<br>
$$<br>
若以$\boldsymbol{w}$表示权值向量，即：<br>
$$<br>
\boldsymbol{w} = (w_1,w_2,\dots,w_K)^T<br>
$$<br>
以$\boldsymbol{F(y,x)}$表示全局特征向量:<br>
$$<br>
\boldsymbol{F(y,x)} = (f_1(y,x),f_2(y,x),\dots, f_K(y,x))^T<br>
$$<br>
则条件随机场可以以两个向量内积的形式来表示:<br>
$$<br>
P_w(y|x) = \frac{exp(\boldsymbol{w \cdot F(y,x)})}{Z_w(x)}<br>
$$</p>
<h4 id="条件随机场的矩阵形式">条件随机场的矩阵形式</h4>
<p>假设$P_w(y|x)$是线性链条件随机场，表示对给定观测序列，相应的标记序列$y$的条件概率。对每个标记序列引入特殊的起点和终点状态标记$y_0 = start$和$y_{n+1} = stop$,这时标注序列的概率可以通过矩阵形式表示并有效计算。<br>
对观测序列$x$的每一个位置$i =1,2,\dots,n+1$,由于$y_{i-1}$和$y_i$在$m$个标记中取值，因此可以定义一个$m$阶矩阵随机变量：<br>
$$<br>
M_i(x) = [M_i(y_{i-1},y_i|x)]<br>
$$<br>
矩阵随机变量元素为:<br>
$$<br>
\begin{aligned}<br>
M_i(y_{i-1},y_i|x) &amp;= exp(W_i(y_{i-1},y_i|x)) \<br>
W_i(y_{i-1},y_i|x) &amp;= \sum_{k=1}^K w_k f_k(y_{i-1},y_i,x,i)<br>
\end{aligned}<br>
$$<br>
这样，给定观测序列$x$,相应的标记序列$y$的非规范概率可以通过该序列$n+1$个矩阵的适当元素的乘积<br>
$\prod_{i=1}^{n+1} M_i(y_{i-1},y_i|x)$,于是，条件概率$P_w(y|x)$可以表示为：<br>
$$<br>
P_w(y|x) = \frac{1}{Z_w(x)} \prod_{i=1}^{n+1} M_i(y_{i-1},y_i|x)<br>
$$<br>
其中$Z_w(x) = \sum_y \prod_{i=1}^{n+1} M_i(y_{i-1},y_i|x)$,可以证明，该归一化因子也可以表示成$n+1$个矩阵连乘积的矩阵的元素。<br>
下面的部分依次介绍条件随机场的三个经典问题：</p>
<ul>
<li>概率计算问题，即给点条件随机场$P(Y|X)$,输入序列$x$和输出序列$y$,计算相应条件概率的问题</li>
<li>学习问题，即在给定训练数据集情况下估计条件随机场模型参数的问题</li>
<li>预测问题，即在给定条件随机场$P(Y|X)$和输入序列$x$,求条件概率最大的输出序列$y^*$,即对观测序列进行标注。</li>
</ul>
<p>概率计算问题比较简单，也是后两个问题的基础，而在实际进行应用中，我们首先需要明确线性链条件随机场是否适用于待解决的问题，然后利用已有的训练数据来进行模型学习，最终再用学习得到的模型进行预测(标注)。</p>
<h3 id="条件随机场概率计算问题">条件随机场概率计算问题</h3>
<p>与隐马尔可夫模型的概率计算类似，对于这种有明显前后关联的概率模型， 往往都是采用递推方式来进行概率计算，通过引入前向-后向向量，递归地进行概率计算，该算法也被称为前向-后向算法。</p>
<h4 id="前向-后向算法">前向-后向算法</h4>
<p>对每个指标$ i =0,1,\dots,n+1$,定义前向向量$\alpha_i(x)$:<br>
$$<br>
\alpha_0(y|x) = \begin{cases}<br>
1, &amp;y = start \<br>
0, &amp; other<br>
\end{cases}<br>
$$<br>
递推公式为:<br>
$$<br>
\alpha_i^T(y_i|x) = \alpha_{i-1}^T(y_{i-1}|x)[M_i(y_{i-1},y_i|x)],\quad i = 1,2,\dots,n+1<br>
$$<br>
也可表示为：<br>
$$<br>
\alpha_i^T(x) = \alpha_{i-1}^T(x) M_i(x)<br>
$$<br>
$\alpha_i(y_i|x)$表示在位置$i$的标记是$y_i$，并且从1到$i$的前部分标记序列的非规范化概率，$y_i$可以取的值有$m$个，在上述公式中，$\alpha_{i-1}(x)$是一个$m$维列向量，而$M_i(x)$是一个$m$阶矩阵，实际上$y_i$的任意取值均可能由$y_{n-1}$的$m$种状态转移过来，因此上述的计算实际上是对各种转移可能性的求和。<br>
同样，对每个指标$i = 0,1,\dots,n+1$,定义后向向量$\beta_i(x)$:<br>
$$<br>
\begin{aligned}<br>
\beta_{n+1}(y_{n+1|x}) &amp;= \begin{cases}<br>
1, &amp; y_{n+1} = stop \<br>
0, &amp; other<br>
\end{cases} \<br>
\beta_i(y_i|x) &amp;= [M_{i+1}(y_i,y_{i+1}|x)] \beta_{i+1}(y_{i+1}|x)<br>
\end{aligned}<br>
$$<br>
又可表示为：<br>
$$<br>
\beta_i(x) = M_{i+1}(x) \beta_{i+1}(x)<br>
$$<br>
$\beta_i(y_i|x)$表示在位置$i$的标记为$y_i$并且从$i+1$到$n$后半部分标记序列的非规范化序列。</p>
<h4 id="概率计算">概率计算</h4>
<p>按照前向后向概率的定义，很容易计算标记序列在位置$i$是标记$y_i$的条件概率和在位置$i-1$和$i$是标记$y_{i-1}$和$y_i$的条件概率：<br>
$$<br>
\begin{aligned}<br>
P(Y_i = y_i|x) &amp;= \frac{\alpha_i^T(y_i|x) \beta_i(y_i|x)}{Z(x)} \<br>
P(Y_{i-1} = y_{i-1},Y_i = y_i|x) &amp;= \frac{\alpha_{i-1}^T(y_{i-1}|x)M_i(y_{i-1},y_i|x) \beta_i(y_i|x)}{Z(x)}<br>
\end{aligned}<br>
$$<br>
其中，<br>
$$<br>
Z(x) = \alpha_n^T(x) \boldsymbol{1} = \boldsymbol{1} \beta_1(x)<br>
$$<br>
$\boldsymbol{1}$是元素全为1的$m$维列向量</p>
<h4 id="期望值计算">期望值计算</h4>
<p>利用前向、后向向量，可以计算特征函数关于联合分布$P(X,Y)$和条件分布$P(Y|X)$的数学期望。<br>
特征函数$f_k$关于条件分布$P(Y|X)$的数学期望是:<br>
$$<br>
\begin{aligned}<br>
E_{P(Y|X)}[f_k] &amp;= \sum_y P(y|x) f_k(y,x)   \<br>
&amp;= \sum_{i=1}^{n+1} \sum_{y_{i-1},y_i} f_k(y_{i-1},y_i,x,i)  P(Y_{i-1} = y_{i-1},Y_i = y_i|x) \<br>
&amp;\qquad k = 1,2,\dots,K<br>
\end{aligned}<br>
$$<br>
能够这样进行展开，实际上还是从$f_k(y,x)$的定义出发：<br>
$$<br>
f_k(y,x) = \sum_{i=1}^{n+1} f_k(y_{i-1},y_i, x,i),\quad k = 1,2,\dots,K<br>
$$<br>
展开之后便是上述形式，这中写法也显示了在概率图中，联合概率分布可以表示成一系列局部概率分布的乘积形式。<br>
假设经验分布为$\tilde{P}(X)$,则特征函数关于联合分布$P(X,Y)$的数学期望可以表示为：<br>
$$<br>
\begin{aligned}<br>
E_{P(X,Y)}[f_k] &amp;= \sum_{x,y}P(x,y) \sum_{i=1}^{n+1} f_k(y_{i-1},y_i,x,i)  \<br>
&amp;= \sum_{x} \tilde{P}(x) \sum_{i=1}^{n+1} \sum_{y_{i-1},y_i} f_k(y_{i-1},y_i,x,i)  P(Y_{i-1} = y_{i-1},Y_i = y_i|x) \<br>
&amp;\quad, i =1,2,\dots,K<br>
\end{aligned}<br>
$$<br>
有了前向、后向算法，对于给定的观测序列$x$与标记序列$y$，可以通过一次前向扫描计算$\alpha_i$及$Z(x)$,通过一次后向扫描计算$\beta_i$,从而计算所有的概率和特征的期望。</p>
<h3 id="条件随机场的学习算法">条件随机场的学习算法</h3>
<p><strong>条件随机场模型实际上是定义在时序数据上的对数线性模型</strong>，其学习方法包括极大似然估计和正则化的极大似然估计，具体的优化实现算法有改进的迭代尺度法IIS、梯度下降法及拟牛顿法。</p>
<h4 id="改进的迭代尺度法">改进的迭代尺度法</h4>
<p>已知训练数据集，由此可知经验概率分布$\tilde{P}(X,Y)$，可以通过极大化训练数据的对数似然函数来求模型参数。<br>
训练数据的对数似然函数为:<br>
$$<br>
\begin{aligned}<br>
L(w) &amp;= L_{\tilde{P}}(P_w)  = \log \prod_{x,y} P_w(y|x)^{\tilde{P}(x,y)} = \sum_{x,y} \tilde{P}(x,y) \log P_w(y|x) \<br>
&amp;= \sum_{x,y} [\tilde{P}(x,y) \sum_{k=1}^K w_k f_k(y|x) -\tilde{P}(x,y) \log Z_w(x)] \<br>
&amp;= \sum_{j=1}^N \sum_{k=1}^K w_k f_k(y_j,x_j) - \sum_{j=1}^N \log Z_w(x_j)<br>
\end{aligned}<br>
$$<br>
这一部分先挖坑，等我补一下相关知识再来填坑…</p>
<h3 id="条件随机场预测算法">条件随机场预测算法</h3>
<p>条件随机场的预测问题是指给定条件随机场$P(Y|X)$和输入序列(观测序列)$x$,求条件概率最大的输出序列$y^<em>$,即对观测序列进行标注。与HMM模型中的预测问题类似，该问题可以通过维特比算法进行解决。<br>
由条件随机场的简化形式:<br>
$$<br>
P_w(y|x) = \frac{exp(\boldsymbol{w \cdot F(y,x)})}{Z_w(x)}<br>
$$<br>
可知：<br>
$$<br>
\begin{aligned}<br>
y^</em> &amp;= \arg \max_{y} P_w(y|x) \<br>
&amp;= \arg \max_{y} \frac{exp(\boldsymbol{w \cdot F(y,x)})}{Z_w(x)} \<br>
&amp;= \arg \max_{y} exp(\boldsymbol{w \cdot F(y,x)}) \<br>
&amp;= \arg \max_{y}   \boldsymbol{w \cdot F(y,x)}<br>
\end{aligned}<br>
$$<br>
于是，条件随机场的预测问题成为求非规范概率最大的最优路径问题：<br>
$$<br>
\max_y (\boldsymbol{w \cdot F(y,x)})<br>
$$<br>
这里路径表示标记序列，其中:<br>
$$<br>
\begin{aligned}<br>
\boldsymbol{w} &amp;= (w_1,w_2,\dots,w_K)^T \<br>
\boldsymbol{F(y,x)} &amp;= (f_1(y,x),f_2(y,x),\dots,f_K(y,x))^T  \<br>
f_k(y,x) &amp;= \sum_{i=1}^n f_k(y_{i-1},y_i,x,i),\quad k = 1,2,\dots,K<br>
\end{aligned}<br>
$$<br>
为了求解方便，考虑将优化问题写成如下形式：<br>
$$<br>
\max_y \sum_{i=1}^n \boldsymbol{w} \cdot F_i(y_{i-1},y_i,x)<br>
$$<br>
其中:<br>
$$<br>
F_i(y_{i-1},y_i,x) = (f_1(y_{i-1},y_i,x,i),f_2(y_{i-1},y_i,x,i),\dots,f_K(y_{i-1},y_i,x,i))<br>
$$<br>
是局部特征向量。下面给出维特比算法流程:</p>
<blockquote>
<p><strong>维特比算法</strong>：<br>
输入:模型特征向量$\boldsymbol{F(y,x)}$,权值向量$\boldsymbol{w}$,观测序列$\boldsymbol{x} = (x_1,x_2,\dots,x_n)$<br>
输出:最优路径$\boldsymbol{y^\ast = (y_1^\ast,y_2^\ast,\dots,y_n^\ast)}$</p>
<ol>
<li>初始化：<br>
$$<br>
\delta_1(j) = \boldsymbol{w} \cdot F_1(y_0 = start,y_1=j,x), \quad j = 1,2,\dots,m<br>
$$</li>
<li>递推：对$i = 2,3,\dots,n$<br>
$$<br>
\begin{aligned}<br>
\delta_i(l) &amp;= \max_{1 \le j \le m}{ \delta_{i-1}(j) + \boldsymbol{w} \cdot F_i(y_{i-1} = j,y_i = l,x)},\quad l = 1,2,\dots,m  \<br>
\varphi_i(l) &amp;= \arg \max_{1 \leq j \leq m}{ \delta_{i-1}(j) + \boldsymbol{w} \cdot F_i(y_{i-1} = j,y_i = l,x)},\quad l = 1,2,\dots,m<br>
\end{aligned}<br>
$$</li>
<li>终止:<br>
$$<br>
\begin{aligned}<br>
\max_y(\boldsymbol{w} \cdot \boldsymbol{F(x,y)}) &amp;= \max_{1 \leq j \leq m} \delta_n(j) \<br>
y_n^\ast &amp;= \arg \max_{1 \leq j \leq m} \delta_n(j)<br>
\end{aligned}<br>
$$</li>
<li>回溯，得最优路径:<br>
$$<br>
y_i^\ast = \varphi_{i+1}(y_{i+1}^\ast),\quad i = n-1,n-2,\dots,1<br>
$$<br>
求得最优路径$\boldsymbol{y^\ast} = (y_1^\ast,y_2^\ast,\dots,y_n^\ast)$</li>
</ol>
</blockquote>
]]></content>
      <categories>
        <category>统计学习</category>
      </categories>
      <tags>
        <tag>概率图，条件随机场</tag>
      </tags>
  </entry>
  <entry>
    <title>深度学习基础</title>
    <url>/2020/09/02/shen-du-xue-xi-ji-chu/</url>
    <content><![CDATA[<p>近些年来，神经网络算法家族蓬勃发展，本部分主要介绍一下这些算法的一些通用原理基础，该部分按照以下结构组织:</p>
<ul>
<li>神经元模型</li>
<li>多层神经网络</li>
<li><strong>反向传播算法(BP)</strong></li>
<li>网络训练中常见问题</li>
<li>常见网络模型介绍</li>
</ul>
<a id="more"></a>
<h3 id="神经元模型">神经元模型</h3>
<p>时至今日，神经网络已经是一个相当大的、多学科交叉的学科领域，下面给出神经网络的定义：</p>
<blockquote>
<p><strong>神经网络:</strong> 神经网络是由具有适应性的简单单元所组成的广泛并进行互联的网络，它的组织能够模拟生物神经系统对真实世界物体所做出的交互反应。</p>
</blockquote>
<p>神经网络的基本组成成分是神经元模型，即神经网络定义中的“简单单元”。在生物神经网络中，每个神经元与其他神经元相连，当它“兴奋”时，就会向相连的神经元发送化学物质，从而改变这些神经元内的电位；如果某神经元的电位超过了一个“阈值”，那么它就会被激活，向其他神经元发送化学物质。</p>
<p><img src="https://raw.githubusercontent.com/xuejy19/xuejy19.github.io/source/Img/Neuro-model1.png" alt="神经元"><br>
将生物学概念中的神经元模型可以抽象成如下图所示的数学模型，这就是一直沿用至今的“M-P神经元模型”，在这个模型中，神经元接受到来自$n$个其他神经元传递过来的输入信号，这些输入信号通过带权重的连接进行传递，神经元接收到的总输入值将与神经元的阈值做比较，然后同过“激活函数”处理以产生神经元的输出。<br>
<img src="https://raw.githubusercontent.com/xuejy19/xuejy19.github.io/source/Img/Neuro-model.jpg" alt="神经元模型"><br>
理想的激活函数是阶跃函数，它将输入值映射为输出值0或1，1对应着神经元兴奋，0对应着神经元抑制，然而阶跃函数具有不连续、不光滑等性质，因此实际常用sigmoid函数作为激活函数:<br>
$$<br>
sigmoid(x) = \frac{1}{1 + e^{-x}}<br>
$$<br>
sigmoid函数能够将在较大范围内变化的输入值压缩到$(0,1)$输出范围内，因此有时也被称为“挤压函数”。<br>
<img src="https://raw.githubusercontent.com/xuejy19/xuejy19.github.io/source/Img/act_function.png" alt="激活函数"><br>
把许多这样的神经元按照一定的层次结构连接起来，就得到了一个神经网络，尽管神经网络类型有很多，但其组成基本单元几乎都是“M-P神经元模型”。从计算机科学的角度看，我们可以先不考虑神经网络是否真的模拟了神经网络，只需要将一个神经网络视为包含了许多参数的数学模型，这个模型是若干函数，例如$y_j = f(\sum_i w_i x_i - \theta_j)$相互嵌套代入而得，有效的神经网络算法大多以数学证明为支撑。</p>
<h3 id="多层感知机">多层感知机</h3>
<h4 id="定义">定义</h4>
<p>在前面已经介绍过感知机模型，感知机模型也可以看作一个以符号函数为激活函数的神经网络，只有两层，一个是输入层，一个是输出层,如下图所示<br>
<img src="https://raw.githubusercontent.com/xuejy19/xuejy19.github.io/source/Img/Perceptron.jpg" alt="Perceptron"><br>
但是这只是一个线性模型，只能够处理线性可分数据，其学习能力非常有限，甚至对于简单的异或问题都无法解决，更一般的，常见的神经网络是多层结构，如下图所示<br>
<img src="https://raw.githubusercontent.com/xuejy19/xuejy19.github.io/source/Img/MLP.png" alt="多层感知机"><br>
在多层感知机中，每层神经元与下一层神经元全互连，神经元之间不存在同层连接，也不存在跨层连接，这样的神经网络结构通常称为“多层前馈神经网络”，其中输入层神经元接收外界输出，隐层与输出层神经元对信号进行加工，最终结果由输出层神经网络输出；换言之，输入层神经元仅是接受输入，不进行函数处理，隐层与输出层包含功能神经元，一个神经网络只要包含隐层，就可以称为多层网络。神经网络的学习过程，就是根据训练数据来调整神经元之间的连接权以及每个功能神经元的阈值；换言之，神经网络学到的东西，蕴含在连接权与阈值之中。</p>
<h4 id="多层感知机逼近能力">多层感知机逼近能力</h4>
<p>多层感知机的逼近能力由如下定理保证:</p>
<blockquote>
<p><strong>定理：</strong> 令$\phi(\cdot)$ 为有界、非常量单调递增连续函数，$I_p$表示$p$维单位超立方体$[0,1]^p$,$C(I_p)$表示定义在$I_p$上的连续函数构成的集合，则给定任意函数$f \in C(I_p)$和$\epsilon &gt;0$ ,存在整数$M$和一组实常数$\alpha_i,\theta_i,w_{ij}$,其中$i=1,2,\dots,M;j=1,2,\dots,p$,使得网络输出:<br>
$$<br>
F(x_1,x_2,\dots,x_p) = \sum_{i=1}^M \alpha_i \phi(\sum_{j=1}^p w_{ij}x_j - \theta_i)<br>
$$<br>
可任意逼近$f(\cdot)$,即：<br>
$$<br>
|F(x_1,x_2,\dots,x_p) - f(x_1,x_2,\dots,x_p)| &lt; \epsilon , \forall (x_1,\dots,x_p) \in I<br>
$$</p>
</blockquote>
<h3 id="反向传播算法">反向传播算法</h3>
<p>多层网络的学习能力比单层感知机强得多，欲训练多层网络，则需要更加强大的学习算法，误差逆传播(BP)算法就是其中最杰出的代表，它是迄今为止最成功的神经网络学习算法，现实任务中在使用神经网络时，大多是使用BP算法进行训练。</p>
<p>下面对BP算法做简单推导，给定训练集$D = { (\boldsymbol{x_1,y_1}),(\boldsymbol{x_2,y_2}),\dots , (\boldsymbol{x_N,y_N})}$ ,其中$\boldsymbol{x_i} \in \mathbb{R}^d,\boldsymbol{y_i} \in \mathbb{R}^l$,即输入是由$d$个属性描述，输出$l$维实值向量，下面以一个三层前馈网络来进行反向传播算法推导，输入层有$d$个神经元，隐藏层有$q$个神经元，输出层有$l$个神经元。输出层第$j$个神经元的阈值用$\theta_j$表示，隐层第$h$个神经元的阈值用$\gamma_h$表示，输入层第$i$个神经元与隐层第$h$个神经元之间的连接权重为$v_{ih}$,隐层第$h$个神经元与输出层第$j$个神经元之间的连接权重为$w_{hj}$,记隐层第$h$个神经元接收到的输入为$\alpha_h = \sum_{i=1}^d v_{ih} x_i$,输出层第$j$个神经元输入为$\beta_j = \sum_{h=1}^q w_{hj} b_h $,其中$b_h$为隐层第$h$个神经元的输出。假设隐层和输出层神经元都是用Sigmoid函数作为激活函数。<br>
对训练例$(\boldsymbol{x_k,y_k})$,假定网络输出为$\hat{\boldsymbol{y}}<em>k = (\hat{y}<em>1^k, \hat{y}<em>2^k, \dots, \hat{y}<em>l^k)$,即：<br>
$$<br>
\hat{y}<em>j^k = f(\beta_j - \theta_j)<br>
$$<br>
则网络在$(\boldsymbol{x_k,y_k})$用方差和来定义:<br>
$$<br>
E_k = \frac{1}{2} \sum</em>{j=1}^l (\hat{y_j}^k - y_j^k)^2<br>
$$<br>
首先来考虑对于这样一个三层网络，有多少个参数需要确定，权重参数有$d \times q + q \times l$个,阈值参数有$ q + l$个，因此总共需要学习的参数个数为$(d+l+1)q +l$个，BP是一个迭代学习算法，在每一轮中采用广义的感知机策略(链式法则求梯度)进行参数学习，任意参数$v$的更新估计式为:<br>
$$<br>
v \leftarrow v + \Delta v<br>
$$<br>
具体参数的求解其实就是一个链式法则求导的过程，比如以隐藏层到输出层的权重$w</em>{hj}$为例，其实只需要找到从$w</em>{hj}$到误差函数中间有哪些中间变量，然后反向传播时便将误差按照变量路径传递回来即可，对于$w</em>{hj}$,我们可以写出一条变量路径:<br>
$$<br>
w</em>{hj} \rightarrow  \beta_j \rightarrow \hat{y}<em>j^k \rightarrow E_k<br>
$$<br>
因此由链式法则，可得:<br>
$$<br>
\frac{\partial E_k}{\partial w</em>{hj}} = \frac{\partial E_k}{\partial \hat{y}_j^k} \frac{ \partial \hat{y}<em>j^k}{\partial \beta_j} \frac{\partial \beta_j}{\partial w</em>{hj}} = (\hat{y}<em>j^k - y_j^k) \hat{y}<em>j^k (1 - \hat{y}<em>j^k) b_h<br>
$$<br>
$\frac{ \partial \hat{y}<em>j^k}{\partial \beta_j} $应用了Sigmoid函数$f’(x) = f(x)(1 - f(x))$的性质，由此梯度下降算法原理，可得：<br>
$$<br>
\Delta w</em>{hj} = -\eta \frac{\partial E_k}{\partial w</em>{hj}}<br>
$$<br>
下面给出$v</em>{ih}$的前向变量传递路径：<br>
$$<br>
v</em>{ih} \rightarrow \alpha_h \rightarrow b_h \rightarrow \beta_1,\dots,\beta_l \rightarrow \hat{y}<em>1^k, \hat{y}<em>2^k \dots \hat{y}<em>l^k \rightarrow E_k<br>
$$<br>
此时前向路径共有$l$条，因此计算梯度时需要分别极计算$l$部分，计算结果为:<br>
$$<br>
\begin{aligned}<br>
\Delta v</em>{ih} &amp;= -\eta \frac{\partial E_k}{b_h} \frac{\partial b_h}{\partial \alpha_h} \frac{\partial \alpha_h}{\partial v</em>{ih}}  \<br>
&amp;= -\eta [\sum</em>{j=1}^l \frac{\partial E_k}{\partial \beta_j} \frac{\partial \beta_j}{\partial b_h}] f’(\alpha_h - \gamma_h) x_i<br>
\end{aligned}<br>
$$<br>
其他参数更新也都是同样思路，在这里便不做过多赘述，其实BP算法就只是应用链式求导法则反向计算梯度(更新权重)的方法。与随机梯度下降以及批梯度下降相对应，BP算法也可以分为标准BP算法(一次更新一个实例)，累积BP算法(一次更新所有样例)，在实际中训练时往往采取折中的策略，每次取一批数据进行权重更新。</p>
<h3 id="网络训练中常见问题">网络训练中常见问题</h3>
<p>对于这样一个简单BP网络，在训练中也会面临很多问题，这些问题是神经网络算法所固有的一些问题，很多研究也是以解决这些问题为出发点，这部分主要介绍三个问题:</p>
<ul>
<li>过拟合问题</li>
<li>梯度消失与梯度爆炸</li>
<li>局部极小值问题</li>
</ul>
<h4 id="过拟合问题">过拟合问题</h4>
<p>正是由于BP网络具有强大的表示能力，BP神经网络经常遭遇过拟合，即随着训练的进行，训练误差进一步降低，但测试误差却可能上升。有两种策略可以缓解过拟合问题：</p>
<ul>
<li>早停：将数据划分为训练集和验证集，训练集用来计算梯度、更新连接权和阈值，验证集用来估计误差，若训练集误差降低但验证集误差升高，则停止训练，并返回具有最小验证集误差的连接权和阈值。</li>
<li>正则化： 在目标误差函数中增加一个用于描述网络复杂度的部分，例如连接权和阈值的平方和，仍令$E_k$表示第$k$个训练样例上的误差，$w_i$表示连接权和阈值，则对于批反向传播的误差函数可以写为:<br>
$$<br>
E = \lambda \frac{1}{m} \sum_{k=1}^m E_k + (1 - \lambda) \sum_i w_i^2<br>
$$<br>
其中$\lambda$用于对经验误差和网络复杂度进行折中，常通过交叉验证法进行估计。</li>
</ul>
<h4 id="梯度消失与梯度爆炸">梯度消失与梯度爆炸</h4>
<p>我们在反向计算梯度时实际上是一个链式求导的过程，假设有一个层数为5的神经网络(包含输入层和输出层)，假设我们欲更新输入层到第一隐层的一个权重$w$，中间需要经过若干变量($w_1,\dots,w_k$)才能到达损失函数,则进行反向传播时，损失函数$L$对$w$求偏导是一个连乘积的形式：<br>
$$<br>
\frac{\partial L}{\partial w} = \frac{\partial L}{\partial w_k} \cdot \frac{\partial w_k}{\partial w_{k-1}} \dots \frac{\partial w_1}{\partial w}<br>
$$<br>
若这些连乘积每一项都大于1，则就出现了梯度爆炸情况，导致参数更新不稳定，若连乘积每一项都小于1，则连乘之后就会是一个非常小的值，也就是说出现了梯度消失。<br>
梯度在反向传播过程中，没经过一层就会经过一次对激活函数求导的过程，加入选的是Sigmoid函数作为激活函数，其导数$f’(x) \leq 0.25$,因此随着网络的变深，使用Sigmoid函数作为激活函数会不可避免地导致出现梯度消失。<br>
<img src="https://raw.githubusercontent.com/xuejy19/xuejy19.github.io/source/Img/sigmoid.png" alt="sigmoid"><br>
下面介绍三个解决梯度消失/爆炸问题的思路。</p>
<h5 id="Relu激活函数">Relu激活函数</h5>
<p>为解决使用Sigmoid激活函数导致梯度消失的问题，在深层神经网络的训练中人们往往采用另一种激活函数——Relu以及它的变形Leakage-Relu，函数图像如下图所示:<br>
<img src="https://raw.githubusercontent.com/xuejy19/xuejy19.github.io/source/Img/relu.jpg" alt="relu"><br>
可以看到，Relu函数在被激活后($&gt;0$),梯度恒为1，既不会导致梯度消失也不会导致梯度爆炸，但relu函数有一个明显的缺点:当relu的输入值为负时，输出始终为0，这样会导致神经元不能够更新参数，也就是该神经元无法学习了，为了解决relu函数这个问题，在relu的负半区引入了一个泄漏值，也就是说当输入小于0时仍旧有一个小的梯度，该泄漏值可以指定也可以作为一个参数与神经网络其他参数一同被学习。</p>
<h5 id="Batchnorm">Batchnorm</h5>
<p>机器学习领域有一个非常重要的假设:IID，即独立同分布假设， 假设训练数据和测试数据是满足相同分布的，这也是通过训练数据获得的模型能够在测试集获得好的效果的一个基本保障，而：</p>
<blockquote>
<p><strong>Batchnorm就是在深度神经网络训练过程中使每一层神经网络的输入保持相同分布。</strong></p>
</blockquote>
<p>由于深度神经网络往往具有复杂的网络结构，在进行训练过程中，各层神经元的参数都在变化，导致各层神经元的输入分布不断发生偏移，BN的思想相当直观，之所以训练收敛慢，一般是整体分布逐渐往非线性函数取值区间的上下限两端端靠近，这就导致向后传播时低层神经网络出现梯度消失现象，这是训练神经网络收敛越来越慢的本质原因，而BN就是通过一定规范化手段，把每层神经网络任意神经元的输入值强行拉回到均值为0，方差为1的正态分布，这样使得激活函数的输入落在非线性函数对输入较为敏感的区域(以Sigmoid函数为例，落在0附近区域)，这样进行梯度反向传播时求导得到的梯度值就会较大，一定程度上缓和了梯度消失现象。</p>
<p>但是很明显，从Sigmoid函数图像可以看出，0附近区域其实是接近线性的，如果把所有的输入都拉到Sigmoid函数的线性区域内，那么不就相当于是激活函数是线性函数了么，而我们知道多层线性网络与一层线性网络其实是等价的，所以BN为了保持激活函数的非线性，对norm之后的输入又做了一步变换:<br>
$$<br>
y_i = \gamma \hat{x}_i +    \beta<br>
$$<br>
每个神经元增加了两个参数：尺度参数$\gamma$，偏移参数$\beta$,这等价于让输入从线性区域向非线性区域移动了一点，引入这两个参数本质上是想找到一个线性和非线性的平衡点，使得网络既具有非线性的强表达能力，又在一定程度上减轻了梯度消失问题。<br>
下面给出Batchnorm的具体流程:<br>
<img src="https://raw.githubusercontent.com/xuejy19/xuejy19.github.io/source/Img/BN.png" alt="Batchnorm"></p>
<h5 id="残差结构">残差结构</h5>
<p>首先来谈一谈深层网络的退化问题，理论上来说，深层网络的性能应当不会比浅层网络性能更差，因为深层网络可以考虑对浅层网络进行拷贝，然后剩下来的层作为一个恒等映射，这样最起码深层网络的性能应当与浅层网络性能一致，然而事实却并非这样，实验表明，深层网络表现并不总是优于浅层网络。<br>
<img src="https://raw.githubusercontent.com/xuejy19/xuejy19.github.io/source/Img/deep.png" alt="网络性能对比"></p>
<p>深度网络的退化说明深度网络不易训练，前面讨论到深层网络往往并不能够通过建立恒等映射的形式来达到与浅层网络相同的性能，因此何凯明等考虑显式的引入该恒等映射，而让中间的网络学习一个残差，假设我们希望一个堆叠非线性层(stacked nonlinear layers)学习的映射是$\mathcal{H(x)}$,我们现在让该堆叠非线性层学习另一个映射$\mathcal{F(x)} = \mathcal{H(x)} - x$，通过$\mathcal{F(x)}+x$来重构期望映射， 而在网络中实现这个目的的操作则是“跳连”(shortcut connection)，如下图所示：<br>
<img src="https://raw.githubusercontent.com/xuejy19/xuejy19.github.io/source/Img/res.png" alt="resnet"></p>
<p>上图所示的结构一般被称为一个残差单元，下面我们来仔细讨论下这样的结构为什么可以解决梯度消失问题，假设上述残差单元的输入为$x$,则残差单元的输出可以表示为:<br>
$$<br>
\boldsymbol{y} = \mathcal{F(\boldsymbol{x},{(\boldsymbol{W_i})})} + \boldsymbol{x}，i=1,2,\dots,k<br>
$$<br>
$k$表示该残差单元包含几个权重层，需要说明的是，一个残差单元至少应该包含两个层，若只有一个层，则会出现:<br>
$$<br>
\boldsymbol{y} = \boldsymbol{W_1^T x + x} = \boldsymbol{(W_1^T + 1)x}<br>
$$<br>
而若包含两层，则残差单元的输出写做:<br>
$$<br>
\boldsymbol{y} = \boldsymbol{W_2^T \sigma(W_1^Tx) + \boldsymbol{x}}<br>
$$<br>
其中,假设$\boldsymbol{x} \in \mathbb{R}^d$,残差单元第一层有$n_1$个神经元，则上式中$\boldsymbol{W_1} \in \mathbb{R}^{d \times n_1}, \boldsymbol{W_2} \in \mathbb{R}^{n_1 \times d}$,损失函数到$\boldsymbol{y}$的梯度为$\frac{\partial L}{\partial \boldsymbol{y}}$,那么我们可以用该梯度表示$\frac{\partial L}{\partial \boldsymbol{x}}$:<br>
$$<br>
\frac{\partial L}{\partial \boldsymbol{x}} = \frac{\partial L}{\partial \boldsymbol{y}} \frac{\partial \boldsymbol{y}}{\partial \boldsymbol{x}} = \frac{\partial L}{\partial \boldsymbol{y}} (\boldsymbol{I} +  \boldsymbol{(W_1 W_2)^T})<br>
$$<br>
其中激活函数取得Relu激活函数，可以看到，在进行反向传播过程中，到$\boldsymbol{y}$的梯度会无损再传递到其前层的$\boldsymbol{x}$，哪怕是残差梯度等于0或者是一个大于$-1$的负值，也可以保证有梯度传递到前面的层，这使得Resnet相对于普通的神经网络更不容易发生梯度消失现象。<br>
何凯明博士这篇提出Resnet的论文也因为其突出贡献获得了2016年CVPR最佳论文奖，整篇论文非常简洁易懂，非常建议大家去读一下，这里给出论文链接<a href="https://openaccess.thecvf.com/content_cvpr_2016/papers/He_Deep_Residual_Learning_CVPR_2016_paper.pdf">Deep Residual Learning for Image Recognition</a></p>
<h4 id="局部极小值问题">局部极小值问题</h4>
<p>神经网络训练过程其实就是一个参数寻优的过程，反向传播算法可以看作是在神经网络这种架构下梯度下降法的实现形式，因此最终收敛的点往往具有梯度为0的特点，这是一个局部极小点，若优化问题是一个凸问题，则局部极小点同时也是全局极小值，而神经网络的优化大多数情况都是一个非凸优化问题，这也就意味着最终训练收敛的点大概率只是一个“局部极小值点”，而非“全局极小值”点,如下图所示:<br>
<img src="https://raw.githubusercontent.com/xuejy19/xuejy19.github.io/source/Img/min.png" alt="局部极小与全局极小"><br>
在现实任务中，人们常采用以下策略试图跳出局部极小，到达全局极小：</p>
<ul>
<li>以多组不同参数值初始化多个网络，按标准方法训练后，取其中误差最小的解作为最终参数，这相当于从多个不同的初始点开始搜索，这样就可能陷入不同的局部极小，从中进行选择有可能获得更接近全局最小的结果。</li>
<li>使用“模拟退火”策略，模拟退火在每一步都以一定概率接受比当前解更差的结果，从而有助于跳出局部极小，在迭代过程中，接受“次优解“的概率要随着时间推移而逐渐降低，从而保证算法稳定。</li>
<li>使用随机梯度下降(SGD)算法，即便陷入了局部极小点，所计算出的梯度仍可能不为0，这样就有机会跳出局部极小继续搜索。</li>
</ul>
<h3 id="常见网络模型介绍">常见网络模型介绍</h3>
<p>神经网络家族成员庞杂，除了前面介绍的BP神经网络外，还有很多类型的神经网络，这部分就一些常见的做简要介绍，后面用到的话再补充。</p>
<h4 id="卷积神经网络">卷积神经网络</h4>
<p>卷积神经网络在图像处理领域有着广泛应用，一个完整的卷积神经网络其实包含两个部分:</p>
<ul>
<li><strong>特征提取</strong></li>
<li>分类</li>
</ul>
<p>后面分类所用的网络其实就是前面所讨论的全连接神经网络或者它的一些变形，卷积神经网络名字的由来则主要是在特征提取阶段，下面就以图片处理为例重点讲解一下特征提取过程。<br>
<img src="https://raw.githubusercontent.com/xuejy19/xuejy19.github.io/source/Img/CNN.png" alt="CNN"><br>
其实我们不论做什么样的机器学习任务，都可以分为特征提取和算法学习两个阶段，而CNN则将这两个阶段结合了起来，然后一起优化，是一种“end2end”的算法，对于一张$n \times n$像素的黑白图像，其实该图像所有信息都包含在$n \times n$个值当中，若要完成一个分类任务，最简单粗暴的方法可能是直接将这$n \times n$个值直接作为特征输入到到全连接层来进行分类，但是这样做有以下两个缺点:</p>
<ul>
<li>输入特征维度非常高，因此网络的规模会很大，参数量会非常大，计算开销会非常大，训练困难。</li>
<li>单个像素值本身并不具备什么意义，最终神经网络结果很有可能并不理想。</li>
</ul>
<p>卷积神经网络的出发点也是为了解决这样一个问题，特征提取部分主要包含三种操作:</p>
<ul>
<li>卷积层</li>
<li>ReLu层</li>
<li>池化层</li>
</ul>
<h4 id="卷积层">卷积层</h4>
<p>一个图像中的单个像素点并不具备什么含义，但是一簇像素点(比如一幅图像中的眼睛区域)可能就具备某种含义了，而卷积层的出发点就是希望能够提取图像中某部分区域的信息，而具体操作则是通过卷积核与图像区域进行加权求和，这其实本质上就是一个线性映射，如下图所示，图像处理中的卷积与信号处理中的卷积不同之处在于其仅仅只有加权求和操作，而并没有翻转操作。<br>
<img src="https://raw.githubusercontent.com/xuejy19/xuejy19.github.io/source/Img/conv.png" alt="图像卷积"><br>
上图中所示的是一个$3 \times 3$的卷积核，可以看出该卷积核对于一个区域左上角像素与有下降像素更加关注，而一正一负操作则表明该卷积核更关注的是左上角像素值与由下角像素值的差。选用不同的卷积核可以从原始图像中提取不同的特征，因此在实际应用中，一般会选用多个大小不一的卷积核以全面捕捉不同大小的区域所包含的信息。<br>
在进行卷积操作时，有时会对图像周围区域进行填充，以提高卷积操作对图像边缘特征提取的能力。</p>
<h4 id="ReLu层">ReLu层</h4>
<p>经过Relu层则主要是为了实现非线性变换，以提高网络的表示能力，比如上图中的-8经过ReLu层之后就变成了0。</p>
<h4 id="池化层-下采样层">池化层(下采样层)</h4>
<p>经过卷积操作之后，我们得到了一张张有着不同值的特征图(feature map)，尽管数据量比原图少了很多，但还是过于庞大，而池化层的作用就是进一步减少数据量,扩大特征图的感受野。<br>
<img src="https://raw.githubusercontent.com/xuejy19/xuejy19.github.io/source/Img/Pooling.png" alt="池化"><br>
池化一般有两种操作，一种是Max pooling,一种是Average pooling,前一种就是取一个区域的最大值，而后一种则是取一个区域的平均值。</p>
<h4 id="总结">总结</h4>
<p>以上这三种操作就是就是卷积神经网络特征提取部分的三个基本单元，对这三种基本单元进行组合便可以组成特征提取部分，需要说明的是卷积神经网络中一个非常重要的操作就是<strong>共享权重</strong>：</p>
<blockquote>
<p>假设输入图像为$32 \times 32$,用两个$5 \times 5$卷积核，则一幅图片经过卷积操作之后变为$28 \times 28$的特征图，该特征图中每个神经元还有一个偏置，因此连接数为:<br>
$$<br>
(5 \times 5 + 1) \times 28 \times 28 \times 2<br>
$$<br>
但在卷积神经网络中，一个特征图共用一套权值，因此实际需要更新的连接权数为:<br>
$$<br>
(5 \times 5 +1 )\times 2<br>
$$</p>
</blockquote>
<p>最后给出经过卷积和池化操作后特征图尺寸变化计算公式:<br>
$$<br>
\begin{aligned}<br>
&amp;卷积： Size_{out} = (Size_{in} - Kernel_{conv} + 2*Padding)/Stride + 1 \<br>
&amp;池化： Size_{out} = (Size_{in} - Kernel_{pooling}) /stride + 1<br>
\end{aligned}<br>
$$</p>
<h3 id="循环神经网络-RNN">循环神经网络(RNN)</h3>
<h4 id="标准循环神经网络">标准循环神经网络</h4>
<h5 id="直观理解">直观理解</h5>
<p>在实际应用中，我们常常会碰到序列数据，这类数据的特点是相邻数据点之间有着较强的联系，比如语句数据、车辆轨迹数据、财经数据等，传统的神经网络已经有着很强的表示能力，但传统的神经网络只能够单独地处理一个接一个输入，前一个输入与后一个输入并没有任何关系，但是对于序列数据，很明显我们对于后一个数据点的预测是依赖于前面的数据的，下面举一个简单的🌰：</p>
<blockquote>
<p><strong>词性标注任务：</strong><br>
<strong>任务输入：</strong> 我/吃/苹果<br>
<strong>任务输出：</strong> 我/nn 吃/v 苹果/nn<br>
对于这样一个任务，我们当然可以直接用普通的神经网络来做，输入为单独的单词，输出为词性标注好的单词。<strong>但是很明显，一个句子中，前一个单词其实对当前单词的词性预测有很大的影响，比如预测“苹果”词性的时候，由于前面的“吃”是一个动词，那么“苹果”作为名次的概率就会远大于动词的概率，因为动词后面接名次很常见，而动词后面接动词不常见</strong></p>
</blockquote>
<p>为了解决这样类似的问题，更好地处理序列信息，循环神经网络就诞生了，下图就是一个简单的RNN的示意图。<br>
<img src="https://raw.githubusercontent.com/xuejy19/xuejy19.github.io/source/Img/RNN.png" alt="RNN"><br>
如果将上图中$h^{(t-1)} \rightarrow h^{(t)}$的连线断开，那么一个RNN就被分解成了若干独立的三层神经网络，现在加入了隐层之间的连接，就使得时刻靠后的神经网络的输出会依赖于前面时刻神经网络的值，以$O^{(t        )}$为例，往前回推，可以写出一条变量链如下:<br>
$$<br>
O^{(t)} \rightarrow h^{(t)} \rightarrow x^{(t)},h^{(t-1)} \rightarrow x^{(t)}, x^{(t-1)},h^{(t-2)} \rightarrow \dots<br>
$$<br>
从该变量链可以看出，$t$时刻的预测输出是依赖于其之前所有的输入。</p>
<h5 id="权重共享">权重共享</h5>
<p>在RNN中也沿用了权重共享的思想，可以看到，不同时刻的神经网络共享输入权重矩阵$\boldsymbol{U}$，输出权重矩阵$\boldsymbol{V}$以及循环权重矩阵$\boldsymbol{W}$,下面简单分析下这样做的好处：</p>
<ul>
<li>运算量减少，模型训练加快</li>
<li>序列长度可以不固定，模型更具泛化能力</li>
</ul>
<p>在进行BP的时候，由于权重共享，因此误差传递每个时刻都会有，假设序列长度为$T$，要对$\boldsymbol{U}$进行权重更新，可以写出误差传递链为：<br>
$$<br>
E \rightarrow E_1, E_2,\dots E_T \rightarrow O^{(1)},O^{(2)},\dots,O^{(T)} \rightarrow  \rightarrow h^{(1)}, h^{(2)}, \dots, h^{(T)} \rightarrow x^{(1)}, x^{(2)},\dots,x^{(T)}<br>
$$<br>
当然，在反向传播的过程中，$h^{t}$到$h^{(t-1)},\dots,h^{(1)}$都是有着传播链条的， 是一个连乘积求和的形式,下面给出$\frac{\partial L_t}{\partial U}$的公式:<br>
$$<br>
\frac{\partial L_t}{\partial U} = \sum_{k=0}^t \frac{\partial L_t}{\partial O^{(t)}}  \frac{\partial O^{(t)}}{\partial h^{(t)}}(\prod_{j=k+1}^t \frac{\partial h^{(j)}}{\partial h^{(j-1)}}) \frac{\partial h^{(k)}}{U}<br>
$$</p>
<h5 id="RNN梯度消失">RNN梯度消失</h5>
<p>从上面的梯度表达式可以看出，$L_t$对输入矩阵$U$的梯度由它$t$项组成，由于有着激活函数存在，因此对靠前时刻的$U$求导是连乘积形成，因此随着序列的增长，累加项中距离当前时刻较远的时刻的项会出现梯度消失或者梯度爆炸，这就导致梯度被近距离梯度主导，模型难以学得远距离的依赖关系。</p>
<h4 id="LSTM">LSTM</h4>
<p>前面讲到普通的RNN由于存在对远距离依赖的梯度消失或者爆炸现象，因此对于长序列表现并不是很好，为解决这一问题，LSTM便诞生了，首先给出LSTM的结构图。<br>
<img src="https://raw.githubusercontent.com/xuejy19/xuejy19.github.io/source/Img/LSTM.png" alt="LSTM"><br>
与标准RNN相比，LSTM增加了三个门控组建，输入门控制当前时刻$t$的计算输出，<strong>与普通RNN相比，LSTM还另外又用遗忘门来控制之前时刻的细胞状态相连接，这个操作类似于前面介绍的Resnet的跳连(skip connection)操作，通过这样的跳连操作，使得若遗忘门初始化较大(接近1)，则在误差反向传播时，可以通过该通路无损地传递到前面时刻，这在一定程度上缓解了RNN中的梯度消失问题，使得LSTM可以处理较长序列</strong>。下面重点讨论下为何引入这三个门控：</p>
<ul>
<li>输入门$i_t$：输入门控制当前词$\boldsymbol{x_t}$融入细胞状态$\boldsymbol{c_t}$, 在做句子理解任务时，当前词$\boldsymbol{x_t}$可能对整句话的意思很重要，也可能并不重要，要不要将它加入输出中则是由输入门来进行控制。</li>
<li>遗忘门$f_t$：$f_t$控制上一时刻的细胞状态$\boldsymbol{c_{t-1}}$融入当前细胞状态$\boldsymbol{c_t}$。在理解一句话时，当前词$\boldsymbol{x_t}$可能继续延续上文的意思进行叙述，也可能从当前词$\boldsymbol{x_t}$开始叙述新的内容，与上文无关。</li>
<li>输出们$o_t$：输出门的目的是从细胞状态$\boldsymbol{c_t}$产生隐层单元$\boldsymbol{h_t}$,细胞状态中的信息并非都与输出隐层状态有关，其中可能包含了很多对$\boldsymbol{h_t}$无用的信息，通过输出门可以对细胞状态中的信息进行筛选。</li>
</ul>
<p>总结而言，门机制的引入带来了两点好处：</p>
<ul>
<li>门机制极大地减轻了梯度消失问题，降低了调参难度</li>
<li>门机制引入了特征过滤，将有用的特征保存，没用的特征丢弃，这极大地丰富了我们向量的表示信息。<br>
同样RNN的参数共享思想在LSTM中也得到了继承，但由于引入了三个门控组件，LSTM参数量相较于RNN增多，训练速度较RNN要慢一些。</li>
</ul>
<h4 id="GRU">GRU</h4>
<p>GRU的全称是 Gated Recurrent Unit(门控循环单位)，与LSTM相比，GRU仅仅只有两个门控组件，一个是重置门$r_t$,一个是更新门$z_t$。<br>
<img src="https://raw.githubusercontent.com/xuejy19/xuejy19.github.io/source/Img/gru.png" alt="GRU"></p>
<p>从上图可以看出，gru实际上就是LSTM的变形，取消了LSTM中的细胞状态(cell state)，只用隐藏状态(hidden state)，并使用更新门来代替LSTM中的输入门与遗忘门，取消了输出门，加入了重置门。实验证明，gru效果与LSTM差不多，但是参数更少，训练计算开销小，训练速度更快。</p>
]]></content>
      <categories>
        <category>统计学习</category>
      </categories>
      <tags>
        <tag>BP算法， 深度学习， CNN</tag>
      </tags>
  </entry>
  <entry>
    <title>生成对抗网络GAN</title>
    <url>/2020/09/09/sheng-cheng-dui-kang-wang-luo-gan/</url>
    <content><![CDATA[<p>这一部分介绍一种特殊的神经网路模型——生成对抗网络(GAN)，生成对抗网络由<a href="https://en.wikipedia.org/wiki/Ian_Goodfellow">Lan Goodfellow</a>于2014年提出，该算法在形式上表现为两个神经网络的彼此对抗，对于生成对抗网络，我们可以从以下几个角度来对其进行限定：</p>
<ul>
<li><strong>本质：</strong> 学习训练数据集的分布</li>
<li><strong>作用：</strong> 产生新的样本，对于小样本任务可以起到数据增强的作用</li>
<li><strong>实现形式：</strong> 两个神经网络彼此对抗</li>
</ul>
<p>本文按照以下结构进行组织:</p>
<ul>
<li>GAN算法思想</li>
<li>GAN背后数学推导</li>
<li>GAN<code>pytorch</code>实现</li>
</ul>
<a id="more"></a>
<h3 id="GAN-算法思想">GAN 算法思想</h3>
<p><img src="https://raw.githubusercontent.com/xuejy19/xuejy19.github.io/source/Img/GAN.jpg" alt="GAN"><br>
考虑这样一个场景，一个小镇里有一个造假钞的人，同时有一个警察，它们两个各自的目标分别是：</p>
<ul>
<li><strong>罪犯：</strong> 不断提高自己的造假钞技术，使得自己的假钞足以以假乱真，让警察鉴别不出来</li>
<li><strong>警察：</strong> 不断提高自己的鉴别水平，能够准确地识别小偷制造的假钞</li>
</ul>
<p>从博弈论的角度来看，这其实是一个零和博弈：</p>
<blockquote>
<p>小偷的造假技术越高超，则警察鉴别起来就越困难;反之，警察的鉴别技术越高超，则小偷造出能欺骗过警察的假钞就更加困难，在对抗中，两个人都在变强。</p>
</blockquote>
<p>接下来我们分析构想一个场景，我们有一组数据，但是数据的量不足，我们期望能够找到这样一个&quot;造假数据&quot;的人帮助我们产生一些新的图像，以达到数据增强的目的。但在这个&quot;造假数据&quot;的人造出一个假数据之后，我们需要判断一下这个数据到底合不合格，因此我们就需要另外一个鉴别&quot;假数据&quot;的&quot;警察&quot;。</p>
<p>其实我们的本质目的是为了<strong>造假数据</strong>,但这个任务若无监督是很难进行下去的，因此我们需要为其提供一个&quot;警察&quot;对其进行监督，因此我们就需要有两个学习器，一个学习如何生成&quot;假数据&quot;，另一个则需要学习如何判别出这些&quot;假数据&quot;，在这两个学习器博弈的过程中我们最终得到了一个&quot;造假数据&quot;比较高超学习器。</p>
<h3 id="GAN-背后数学推导">GAN 背后数学推导</h3>
<blockquote>
<p><strong>沃兹基</strong>曾经说过：算法思想也就图一乐，真涨姿势还是要看数学推导</p>
</blockquote>
<h4 id="从最大似然估计谈起">从最大似然估计谈起</h4>
<p>因为我们本质上是希望能够得到一个学习器，使得其产生的数据的能够与训练集数据同分布，那么数学推导就从分布入手，假设训练集数据概率分布为$p(x|\theta)$,其中$\theta$为该概率分布所依赖的参数，当我们得到一组数据$X = (x_1,x_2,\dots, x_N)$时，我们想要对参数进行估计，那么此时就要祭出已经老生常谈的<strong>最大似然估计</strong>，假设样本独立同分布，则写出似然函数如下:<br>
$$<br>
L(X|\theta) = \prod_{i=1}^N p(x_i | \theta)<br>
$$<br>
而$\theta$的求解其实就是一个优化问题:<br>
$$<br>
\begin{aligned}<br>
\theta^* &amp;= argmax L(X|\theta) \<br>
&amp;= argmax \ln L(X|\theta) \<br>
&amp;= argmax \frac{1}{N} L(X|\theta) \<br>
&amp;= argmax \frac{1}{N} \sum_{i=1}^N \ln p(x_i|\theta) \<br>
&amp;= argmax E_{x \sim p(x)} \ln p(x|\theta)  \<br>
&amp;= argmax \int_{x} p(x) \ln p(x|\theta) dx<br>
\end{aligned}<br>
$$<br>
这里需要特别说明一下这一步：<br>
$$<br>
argmax \frac{1}{N} \sum_{i=1}^N \ln p(x_i|\theta) \Leftrightarrow argmax E_{x \sim p(x)} \ln p(x|\theta)<br>
$$<br>
当样本数量足够大时，空间中每个样本点都被覆盖，而具体空间中某一个点$x_i$会落入多少样本点则取决于数据分布$p(x)$在该点概率密度函数的大小，换句话说，如果从采样的角度，要对某点的概率密度函数进行估计，那么只需要原始数据进行采样，采样$N$个点，若最终有$n_{x_i}$个点落在了$x_i$处，那么该点的概率密度函数就可以估计为:<br>
$$<br>
p(x_i) = \frac{n_{x_i}}{N}<br>
$$<br>
于是上面的等价性也是可以按照这种思想推导出来：<br>
$$<br>
\begin{aligned}<br>
\lim_{N \rightarrow \infty} \frac{1}{N} \ln p(x_i|\theta) &amp;= \sum_{x} \frac{n_{x}}{N} \ln p(x_i|\theta)  \<br>
&amp;= \int_x p(x) \ln p(x|\theta)  dx  \<br>
&amp;= E_{x \sim p(x)} \ln p(x|\theta)<br>
\end{aligned}<br>
$$<br>
最大似然函数估计的目的是找到一组最合适的参数$\theta$使得分布$p(x|\theta)$更加符合数据分布，但是能够应用最大似然估计的场合一般是对数据分布有一个假设，这样才有参数可以优化，搜索空间限制在$\mathcal{p(x|\theta)}$,现在我们将这个约束去掉，在所有可能分布中找到一个分布$q(x)$,使得$q(x)$能够尽可能接近原始数据分布$p_{data}(x)$。 至此我们证明了：<br>
$$<br>
\theta^* = argmax L(X|\theta)  \Leftrightarrow  argmax E_{x \sim p(x)} \ln q(x)<br>
$$</p>
<h4 id="最大似然估计与KL散度">最大似然估计与KL散度</h4>
<p>紧接上文，我们来看下$E_{x \sim p(x)} \ln q(x)$:<br>
$$<br>
\begin{aligned}<br>
argmax_{q(x)}    E_{x \sim p(x)} \ln q(x) &amp;= argmax_{q(x)}\int_x p(x) \ln q(x) dx  \<br>
&amp;= argmin_{q(x)} -\int_x p(x) \ln q(x) dx  \<br>
&amp;= argmin_{q(x)} -\int_x p(x) \ln q(x) dx + \int_x p(x) \ln p(x) dx \<br>
&amp;= argmin_{q(x)} D_{KL}(p(x)||q(x))<br>
\end{aligned}<br>
$$<br>
这说明我们要找到一个分布$q(x)$使得其对于原始分布$p(x)$产生数据的极大似然函数最大就等价于找到一个分布$q(x)$使得两个分布之间的KL散度最小(不知道啥是KL散度的，来<a href="https://xuejy19.github.io/2020/08/26/%E6%A6%82%E7%8E%87%E5%88%86%E5%B8%83%E4%B8%AD%E7%9A%84%E8%B7%9D%E7%A6%BB%E5%BA%A6%E9%87%8F/#more">这里</a>补课)。同时中间结果则是也等价于最小化交叉熵，所以我们又得到了一个有用的结论:</p>
<blockquote>
<p>最大化似然函数 $\Leftrightarrow$ 最小化交叉熵 $\Leftrightarrow$ 最小化KL散度</p>
</blockquote>
<p>然后为了解决KL散度的不对称性，又引入了JS散度：<br>
$$<br>
JS(p(x)||q(x)) = \frac{1}{2} D_{KL}(p(x)||\frac{p(x)+ q(x)}{2}) + \frac{1}{2} D_{KL} (q(x) || \frac{p(x) + q(x)}{2})<br>
$$<br>
本来想推导下优化KL散度等价于优化JS散度，但没推出来，只有一个不等关系:<br>
$$<br>
JS(p||q) \leq \frac{1}{4} D_{KL}(p||q) + \frac{1}{4} D_{KL}(q||p)<br>
$$<br>
本身KL散度的取值范围是$[0, +\infty]$,而JS散度的取值范围是$[0,1]$($\log$以2为底)，但是KL散度与JS散度取最小值的条件是一样的，即两个分布完全相同，因此使用JS散度或者KL散度进行优化，应当是同样的优化效果。 因此可以继续将结论补充：</p>
<blockquote>
<p>最大化似然函数 $\Leftrightarrow$ 最小化交叉熵 $\Leftrightarrow$ 最小化KL散度$\Leftrightarrow$ 最小化JS散度</p>
</blockquote>
<h4 id="生成对抗网络">生成对抗网络</h4>
<p>之前进行分布拟合，一般通过假设分布是一个高斯分布(或混合高斯分布)，然后对参数进行优化，使得对数据有一个较好的拟合效果，但高斯分布假设很多时候并不满足，因此需要一个更大的模型搜索空间。随着神经网络科学的发展，人们开始思考，能否使用神经网络来将一个高斯分布映射为我们期望的分布$p_G$，但由于$p_{data}$是未知的，我们并不知道应该如何衡量两个分布之间的差异，因此就需要有一个判别器。GAN的目标函数如下:<br>
$$<br>
V(D, G) = E_{x \sim data} \log D(x) + E_{x \sim p_G} \log (1 - D(x))<br>
$$<br>
前一项中的$D(x)$表示判别器对来自训练集数据的评分，后一项中的$D(x)$表示判别器对来自生成器数据的评分，如果想提高判别器的性能，则要求判别器对来自训练数据集的数据评分较高，对来自生成器生成数据评分较低，因为$D(x) \in [0,1]$,因此$D(x)$也可以认为是一个样本是来自原始分布的概率。下面我们将$V(D,G)$写开:<br>
$$<br>
\begin{aligned}<br>
V(D,G) &amp;=  E_{x \sim data} \log D(x) + E_{x \sim p_G} \log (1 - D(x)) \<br>
&amp;= \int_x p_{data}(x) \log D(x) dx + \int_x p_G(x) \log(1-D(x)) dx \<br>
&amp;= \int_x [p_{data}(x) \log D(x) + p_G(x) \log(1 - D(x))] dx<br>
\end{aligned}<br>
$$<br>
判别器希望最大化$V(D,G)$,即对于来自于真实样本分布的样本的打分尽可能多，对来自于生成器的样本的打分尽可能低，想要优化上式，即低于任意一个输入$x$，判别器应当:<br>
$$<br>
\max_{D} v(D) = p_{data}(x) \log D(x) + p_G(x) \log(1 - D(x))<br>
$$<br>
该函数是一个凹函数，可以直接通过求导等于0求得$D(x)$最优解：<br>
$$<br>
\frac{\partial v(D)}{\partial D(x)} = \frac{p_{data}(x)}{D(x)} - \frac{p_G(x)}{1 - D(x)} = 0<br>
$$<br>
可以求得：<br>
$$<br>
D^<em>(x) = \frac{p_{data}(x)}{p_{data}(x) + p_{G}(x)}<br>
$$<br>
将求得的$D(x)$回代$V(D,G)$:<br>
$$<br>
\begin{aligned}<br>
V(D^</em>,G) &amp;= \int_x p_{data}(x) \log \frac{p_{data}(x)}{p_{data}(x) + p_{G}(x)} dx + \int_x p_G(x) \frac{p_{G}(x)}{p_{data}(x) + p_{G}(x)}  dx \<br>
&amp;= -2\log2 + 2JS(p_{data}(x) || p_G(x))<br>
\end{aligned}<br>
$$<br>
所以说我们接下来对生成器G的优化，其实就是:<br>
$$<br>
G^* = \arg \min_G V(D^*,G) = \arg \min_{G} JS(p_{data}(x)||p_G(x))<br>
$$<br>
这说明我们对生成器进行优化，实质上就是期望能够找到一个分布，使得$p_G(x)$与原始数据分布$p_{data}(x)$的JS散度最小，由前面的等价性，其实就是找到一个分布使得对训练数据的最大似然函数最大。 因此整个优化问题可以写做:<br>
$$<br>
\min_{G} \max_{D} V(D,G)<br>
$$</p>
<h4 id="GAN-Pytorch实现">GAN <code>Pytorch</code>实现</h4>
<p>在实现的时候需要关注的问题我觉得主要有以下几个：</p>
<ul>
<li>损失函数选择</li>
<li>网络结构设计</li>
<li>优化顺序</li>
</ul>
<h4 id="损失函数选择">损失函数选择</h4>
<p>首先讨论下损失函数选择，对于判别器而言，其面临的其实就是一个二分类问题，我们看下$V(D,G)$:<br>
$$<br>
\begin{aligned}<br>
V(D,G) &amp;= E_{x \sim data} \log D(x) + E_{x \sim p_G} \log (1 - D(x))<br>
\end{aligned}<br>
$$<br>
从采样的角度来看，$V(D,G)$可以写做:<br>
$$<br>
\begin{aligned}<br>
V(D,G) = \sum_{i=1, x \sim p_{data}(x)}^{batch_size} \log D(x) +<br>
\sum_{i=1, x \sim p_{G}(x)}^{batch_size} \log (1 - D(x))<br>
\end{aligned}<br>
$$<br>
若设置来自于真实数据集的数据的label为1，来自于生成器的数据的label为0，则上式其实可以看作两个二分类交叉熵的和，二分类交叉熵计算公式为：<br>
$$<br>
BCE(x_n,y_n) = y_n \log x_n + (1-y_n) \log (1 - x_n)<br>
$$<br>
对于label为1的样本，保留前一项；而对于label为0的样本，保留后一项，刚好就是$V(D,G)$。当然也可以通过直接就两个类别做one-hot，然后使用普通的交叉熵损失函数也可以。</p>
<p>在对生成器进行优化的时候，因为我们期望造出的假样本能够尽可能真，因此只需要将假样本的label设置为1，然后进行反向传播优化生成器即可。</p>
<h4 id="网络结构设计">网络结构设计</h4>
<p>生成对抗网络是出了名的难训练，因此合理的网络结构非常重要，对于新入门的人来说就只能参考一些现成的网络结构,同时对于不同的任务也应当选用不同的网络结构。目前生成对抗网络在图像处理领域较多，因此往往采用卷积神经网络。<br>
<img src="https://raw.githubusercontent.com/xuejy19/xuejy19.github.io/source/Img/generative-adversarial-network.png" alt="GAN"></p>
<h4 id="优化顺序">优化顺序</h4>
<p>生成对抗网络的优化是一个交替优化的过程，一般是先对判别器进行优化，接下来再对生成器进行优化，两者进行交替优化，互相博弈，下面简单地写下计算图：</p>
<blockquote>
<p><strong>判别器：</strong><br>
真实样本、虚假样本(生成器生成) $\rightarrow$ 判别器网络D $\rightarrow$  predict $\rightarrow$ Loss_D<br>
<strong>生成器：</strong><br>
虚假样本 $\rightarrow$ 生成器网络 $\rightarrow$ 虚假样本 $\rightarrow$ 判别器 $\rightarrow$ predict $\rightarrow$ Loss_G</p>
</blockquote>
<h4 id="pytorch-代码实现"><code>pytorch</code> 代码实现</h4>
<p>数据集采用的是<a href="http://mmlab.ie.cuhk.edu.hk/projects/CelebA.html">celeb-A faces Dataset</a>,代码实现如下:</p>
<pre class="line-numbers language-language-python"><code class="language-language-python">import argparse
import os
import random
import numpy as np
import matplotlib.pyplot as plt
import matplotlib.animation
from tqdm import tqdm   
from IPython.display import HTML

import torch 
import torch.nn as nn
import torch.nn.parallel
import torch.backends.cudnn as cudnn
import torch.optim as optim
import torch.utils.data 
import torchvision.datasets as datasets
import torchvision.transforms as transforms
import torchvision.utils as vision_utils
def matplotlib_imshow(img , one_channel = False):
    if one_channel:
        img = img.mean(dim = 0)
    img = img/2 + 0.5
    npimg = img.numpy()
    if one_channel:
        plt.imshow(img, cmap = 'Greys') 
    else:
        npimg  = np.transpose(npimg, (1,2,0)) 
        plt.imshow(npimg)
    plt.show()
def load_data(dataroot, image_size, batch_size, num_workers):
    transform = transforms.Compose([
                   transforms.Resize(image_size) ,
                   transforms.CenterCrop(image_size),
                   transforms.ToTensor(),
                   transforms.Normalize((0.5,0.5,0.5),(0.5,0.5,0.5))])
    dataset = datasets.ImageFolder(root = dataroot, transform = transform) 
    # Create the dataloader
    dataloader = torch.utils.data.DataLoader(dataset, batch_size = batch_size, 
                                        shuffle = True, num_workers = num_workers)
    # plot some training images 
    # real_batch, _ = iter(dataloader).next()
    
    #batch_image = vision_utils.make_grid(real_batch, padding = 2) 
    #matplotlib_imshow(batch_image)
    return dataloader
def weights_init(model):
    classname = model.__class__.__name__
    if classname.find('Conv') != -1:
        nn.init.normal_(model.weight.data, 0.0, 0.02) 
    elif classname.find('BatchNorm') != -1:
        nn.init.normal_(model.weight.data, 1.0, 0.02)
        nn.init.constant_(model.bias.data, 0)

class Generator(nn.Module):
    def __init__(self, nz, ngf, nc):
        super(Generator, self).__init__() 
        # nz = 100 H_out = (H_in - 1)*stride - 2*padding + kernel_size
        self.conv1 = nn.Sequential(
            nn.ConvTranspose2d(nz, ngf*8, 4, 1, 0, bias = False),
            nn.BatchNorm2d(ngf*8),
            nn.ReLU())  
        # state size: (ngf*8)*4*4
        self.conv2 = nn.Sequential(
            nn.ConvTranspose2d(ngf*8, ngf*4, 4, 2, 1, bias = False),
            nn.BatchNorm2d(ngf*4),
            nn.ReLU())
        # state size: (ngf*4)*8*8 
        self.conv3 = nn.Sequential(
            nn.ConvTranspose2d(ngf*4, ngf*2, 4, 2, 1, bias = False),
            nn.BatchNorm2d(ngf*2),
            nn.ReLU())
        # state size: (ngf*2)*16*16 
        self.conv4 = nn.Sequential(
            nn.ConvTranspose2d(ngf*2, ngf, 4, 2, 1, bias = False),
            nn.BatchNorm2d(ngf),
            nn.ReLU()) 
        # state size: ngf*32*32 
        self.conv5 = nn.Sequential(
            nn.ConvTranspose2d(ngf, nc, 4, 2, 1, bias = False),
            nn.Tanh())
    def forward(self, x):
        x = self.conv1(x)
        x = self.conv2(x)
        x = self.conv3(x)
        x = self.conv4(x)
        return self.conv5(x)
class Discriminator(nn.Module):
    def __init__(self, nc, ndf):
        super(Discriminator,self).__init__()
        # input size: nc*64*64 
        self.conv1 = nn.Sequential(
            nn.Conv2d(nc, ndf, 4, 2, 1, bias = False), 
            nn.LeakyReLU(0.2)) 
        # state size: ndf*32*32 
        self.conv2 = nn.Sequential(
            nn.Conv2d(ndf, 2*ndf, 4, 2, 1, bias = False),
            nn.BatchNorm2d(2*ndf),
            nn.LeakyReLU(0.2))  
        # state size: (ndf*2)*16*16 
        self.conv3 = nn.Sequential(
            nn.Conv2d(ndf*2, ndf*4, 4, 2, 1, bias = False),
            nn.BatchNorm2d(ndf*4),
            nn.LeakyReLU(0.2)) 
        # state size: (ndf*4)*8*8 
        self.conv4 = nn.Sequential(
            nn.Conv2d(ndf*4, ndf*8, 4, 2, 1, bias = False),
            nn.BatchNorm2d(ndf*8),
            nn.LeakyReLU(0.2)) 
        # state size: (ndf*8)*4*4
        self.conv5 = nn.Sequential(
            nn.Conv2d(ndf*8,1, 4,1,0,bias = False),
            nn.Sigmoid()) 
        # state size: 1*1 [0,1] 
    def forward(self, x):
        x = self.conv1(x) 
        x = self.conv2(x) 
        x = self.conv3(x)
        x = self.conv4(x)
        return self.conv5(x)
def get_model():
    device = args.device
    netG = Generator(args.nz, args.ngf, args.channels).to(device)
    netG.apply(weights_init)
    netD = Discriminator(args.channels, args.ndf).to(device) 
    netD.apply(weights_init)
    optimizerD = optim.Adam(netD.parameters(), lr=args.lr, betas = (args.beta1,0.999))
    optimizerG = optim.Adam(netG.parameters(), lr=args.lr, betas = (args.beta1,0.999))
    criterion = nn.BCELoss() 
    return netG, netD, optimizerD, optimizerG, criterion
def plot_figure(G_loss, D_loss, Dx, D_z1, D_z2, img_list):
    plt.figure(figsize = (10,5)) 
    plt.title('Generator and Discriminator Loss During Training') 
    plt.plot(G_loss, label = 'G') 
    plt.plot(D_loss, label = 'D') 
    plt.xlabel('iterations') 
    plt.ylabel('Loss') 
    plt.legend()
    plt.savefig('curve_folder/Loss.png')
    plt.figure(figsize = (10, 5)) 
    plt.title('Dx and Dz') 
    plt.plot(Dx, label = 'Dx') 
    plt.plot(D_z1, label = 'Dz1')
    plt.plot(D_z2, label = 'Dz2') 
    plt.xlabel('iterations')
    plt.ylabel('P') 
    plt.legend() 
    plt.savefig('curve_folder/D.png') 
    for i in range(len(img_list)):
        fig = plt.figure(figsize = (8,8)) 
        plt.axis('off') 
        plt.imshow(np.transpose(img_list[i], (1,2,0))) 
        plt.savefig('fake_image/fake_img' + str(i) + '.png')
def fit(epoches, dataloader, device, netG, netD, optimizerG, optimizerD):
    # Lists to keep track of progress 
    img_list = [] 
    G_losses = [] 
    D_losses = []
    Dx_list = [] 
    Dz1_list = []
    Dz2_list = []
    iters = 0
    real_label = 1.0
    fake_label = 0.0
    fixed_noise = torch.randn(64, args.nz, 1, 1, device = device)
    print('Starting Training Loop...') 

    for epoch in range(epoches):
        for i, data in enumerate(dataloader, 0):
            # Update D network: maximize log(D(x)) + log(1-D(G(z))) 
            ## Train with all real batch
            netD.zero_grad() 
            image_batch = data[0].to(device) 
            batch_size = image_batch.shape[0] 
            label = torch.full((batch_size,), real_label, dtype = torch.float,device = device) 
            #Forward pass real batch through D 
            output = netD(image_batch).view(-1) 
            errD_real = criterion(output, label)
            #Calculate gradients for D in backward pass 
            errD_real.backward() 
            D_x = output.mean().item() 

            ## Train with all-fake batch 
            # Generate batch of latent vectors  
            noise = torch.randn(batch_size,args.nz, 1, 1, device = device)  
            # Generate fake image batch with G 
            fake = netG(noise) 
            label.fill_(fake_label)
            # Classify all fake batch with D 
            # use detach() to make sure only change netD params 
            output = netD(fake.detach()).view(-1)  
            errD_fake = criterion(output, label) 
            errD_fake.backward() 
            D_G_z1 = output.mean().item() 
            errD = errD_real + errD_fake
            # update D 
            optimizerD.step() 

            #############################
            #(2) Update G network: maximize log(D(G(z))) 
            ############################# 
            netG.zero_grad() 
            label.fill_(real_label) # fake labels are real for generator cost
            # Since we just updated D, perform another forward pass of all fake
            # batch through D
            output = netD(fake).view(-1) 
            # Calculate G's loss based on this output 
            errG = criterion(output, label) 
            # Calculate gradients for  G
            errG.backward() 
            D_G_z2 = output.mean().item() 
            # Update G 
            optimizerG.step() 

            # Output training stats 
            if i%50 == 0:
                print('[%d/%d][%d/%d]\tLoss_D: %.4f\t Loss_G: %.4f\tD(x):%.4f\tD(G(z)): %.4f/%.4f' % (epoch, epoches, i,
                    len(dataloader), errD.item(), errG.item(), D_x, D_G_z1, D_G_z2))
                # Save Losses for plotting later
                G_losses.append(errG.item()) 
                D_losses.append(errD.item()) 
                Dx_list.append(D_x) 
                Dz1_list.append(D_G_z1)
                Dz2_list.append(D_G_z2)
                # Check how the generator is doing by saving G's output on
                # fixed noise
                if iters % 50 == 0:
                    with torch.no_grad():
                        fake = netG(fixed_noise).detach().cpu()
                    img_list.append(vision_utils.make_grid(fake[:64], padding=2,
                        normalize = True)) 
                iters += 1
                    
    plot_figure(G_losses, D_losses, Dx_list, Dz1_list, Dz2_list, img_list)

if __name__ == '__main__':
    manualSeed = 999
   # print('Random Seed: ', manualSeed) 
    random.seed(manualSeed)
    torch.manual_seed(manualSeed)
    
    # argparse 
    parser = argparse.ArgumentParser(description = 'Train DCGAN') 
    parser.add_argument('--root', type = str, default = 'data', 
                        help = 'Root directory for dataset') 
    parser.add_argument('--device', type = str, default= 'cuda:0',
                        help = 'Choose the training device')
    parser.add_argument('--workers', type = int, default = 2, 
                        help = 'Number of workers for dataloader')
    parser.add_argument('--batch_size', type = int, default = 128, 
                        help = 'Batch size during training')
    parser.add_argument('--image_size', type = int, default = 64,
                        help = 'Spatial size of training images')
    parser.add_argument('--channels', type = int, default = 3,
                        help = 'Number of channels in the training images')
    parser.add_argument('--nz', type = int, default = 100,
                        help = 'Size of generator input')
    parser.add_argument('--ngf', type = int, default = 64,
                        help = 'Size of feature maps in generator') 
    parser.add_argument('--ndf', type = int, default = 64,
                        help = 'Size of feature maps in discriminator') 
    parser.add_argument('--epoches', type = int, default = 10,
                        help = 'Number of training epoches') 
    parser.add_argument('--lr', type = float, default = 2e-4,
                        help = 'Learning rate for optimizer') 
    parser.add_argument('--beta1', type = float, default = 0.5,
                        help = 'Beta1 hyperparam for Adam optimizer') 
    args = parser.parse_args() 
    dataloader = load_data(args.root, args.image_size, args.batch_size,
                           args.workers)
    netG, netD, optimizerD, optimizerG, criterion = get_model()
    
    fit(args.epoches, dataloader,args.device, netG, netD, optimizerG, optimizerD)
<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>
<p>最后附上一些结果图：<br>
<strong>celeb-A faces Dataset</strong><br>
<img src="https://raw.githubusercontent.com/xuejy19/xuejy19.github.io/source/Img/Loss.png" alt="Loss"><br>
<img src="https://raw.githubusercontent.com/xuejy19/xuejy19.github.io/source/Img/fake_image.png" alt="fake_image"></p>
<p><strong>MNIST</strong><br>
<img src="https://raw.githubusercontent.com/xuejy19/xuejy19.github.io/source/Img/Loss_mn.png" alt="Loss"><br>
<img src="https://raw.githubusercontent.com/xuejy19/xuejy19.github.io/source/Img/mnist_gan.png" alt="fake_msist"></p>
<p>网络结构比较简单，真正在使用时应当需要根据任务来进行调整，参数初始化以及部分超参数选择，<strong>沃兹基</strong>曾经说过：</p>
<blockquote>
<p>深度神经网络理论学习是一门学问，代码实现又是一门学问，网络调参更是一门学问，想把神经网络搞掂，三种学问缺一不可。</p>
</blockquote>
<p>GAN目前还面临很多问题，比如训练困难，在图像处理之外的领域效果并不理想，同时在理论支撑方面也不够扎实，如果想进一步学习GAN，可以阅读下面几篇文章：</p>
<ul>
<li><a href="https://zhuanlan.zhihu.com/p/56943597">这份攻略帮你「稳住」反复无常的 GAN</a></li>
<li><a href="https://zhuanlan.zhihu.com/p/75789936">海量案例！生成对抗网络（GAN）的18个绝妙应用</a></li>
</ul>
]]></content>
      <categories>
        <category>统计学习</category>
      </categories>
      <tags>
        <tag>GAN</tag>
      </tags>
  </entry>
</search>
