<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>贝叶斯理论</title>
    <url>/2020/07/23/bayes/</url>
    <content><![CDATA[<table style="width:8%;">
<colgroup>
<col style="width: 8%" />
</colgroup>
<thead>
<tr class="header">
<th>该部分按照以下层次进行组织： &gt; * 从贝叶斯公式谈起 &gt; * 贝叶斯决策 <a id="more"></a></th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>## 贝叶斯公式 <strong>概念介绍</strong>(From 维基百科)： &gt; * <strong>条件概率</strong>：条件概率就是事件A在事件B发生条件下的概率，记做<span class="math inline">\(P(A|B)\)</span>. &gt; * <strong>先验概率</strong>：先验概率是指在考虑“观测数据”之前，对某一不确定量的估计,如<span class="math inline">\(P(A)\)</span> &gt; * <strong>后验概率</strong>：在贝叶斯统计中，一个随机事件或者一个不确定事件的后验概率是在考虑和给出相关证据或数据后所得到的条件概率，“后验”指代考虑了被测试事件的相关证据，如<span class="math inline">\(P(B|A)\)</span>可以看作事件B的后验概率</td>
</tr>
<tr class="even">
<td>贝叶斯公式形式如下： <span class="math display">\[ P(A|B) = \frac{P(A)P(A|B)}{P(B)} \]</span> 再根据全概率公式： <span class="math display">\[P(B) = \sum_j P(B|A_j)P(A_j) \]</span> 可以得到贝叶斯公式另一种形式： <span class="math display">\[P(A_i|B) = \frac{P(A_i)P(B|A_i)}{\sum_j P(B|A_j)P(A_j) }\]</span> &gt; 公式分析：在现实世界中，我们往往期望通过一些观察到的事件来对一些不可以直接观测的事件进行推断，公式中的<span class="math inline">\(B\)</span>变量指代可以直接观测到的事件，而我们的目的则是希望通过<span class="math inline">\(B\)</span>事件与<span class="math inline">\(A\)</span>事件之间的关联(因果性,相关性)来间接对A事件进行观测，贝叶斯公式便为我们搭起了这样一做桥梁。</td>
</tr>
<tr class="odd">
<td>从公式角度出发，可以看出这样进行推理是有代价的，或者是必须拥有某些知识：</td>
</tr>
<tr class="even">
<td>&gt; - 先验概率<span class="math inline">\(P(A)\)</span> &gt; - 类条件概率<span class="math inline">\(P(B|A)\)</span></td>
</tr>
</tbody>
</table>
<h2 id="贝叶斯决策">贝叶斯决策</h2>
<p>这部分主要介绍三种基于贝叶斯理论的决策策略： &gt; - 基于最小错误率的贝叶斯决策 &gt; - 基于最小风险的贝叶斯决策 &gt; - 在限定一类错误率条件下使另一类错误率为最小的两类别决策 --- ### 最小错误率贝叶斯决策 从贝叶斯公式出发，通过比较后验概率大小，选择后验概率大的类别作为label。以而分类问题为例，<span class="math inline">\(\omega_i(i=1,2)\)</span>是类别标签，<span class="math inline">\(x\)</span>为特征相量，由贝叶斯公式，可得<span class="math inline">\(P(\omega_i|x)\)</span>： <span class="math display">\[ P(\omega_i|x) = \frac{P(x|\omega_i)P(\omega_i)}{\sum_{j=1}^2 P(x|\omega_j)P(\omega_j)}\]</span> 最小错误率下等价的决策规则有： &gt; - 若<span class="math inline">\(P(\omega_i|x) = max_{j=1,2}P(\omega_j|x)\)</span>,则<span class="math inline">\(x \in \omega_i\)</span> &gt; - 若<span class="math inline">\(P(x|\omega_i)P(\omega_i) = max_{j=1,2}P(x|\omega_j)P(\omega_j)\)</span>,则<span class="math inline">\(x \in \omega_i\)</span> &gt; - 对于二分类问题，若<span class="math inline">\(l(x) = \frac{P(x|\omega_1)}{P(x|\omega_2)} &gt; \frac{P(\omega_2)}{P(\omega_1)}\)</span>,则<span class="math inline">\(x \in \omega_1\)</span></p>
<p>下面证明该种决策策略为最小错误率，首先给出平均错误率公式： <span class="math display">\[ P(e) = \int_{-\infty}^{\infty}P(e,x)dx = \int_{-\infty}^{\infty} P(e|x)p(x)dx\]</span> 由上面给出的决策规则可知： <span class="math display">\[ P(e|x) = \begin{cases} P(\omega_1|x),if P(\omega_2|x) &gt; P(\omega_1|x) \\ 
P(\omega_2|x),if P(\omega_1|x) &gt; P(\omega_2|x) \end{cases}\]</span> 若<span class="math inline">\(t\)</span>为两类的决策分界面，在该分界面上<span class="math inline">\(P(\omega_1|x) = P(\omega_2|x)\)</span>，<span class="math inline">\(t\)</span>将整个决策空间分为了两部分<span class="math inline">\(\mathcal{R_1}\)</span>和<span class="math inline">\(\mathcal{R_2}\)</span>,分别对应将<span class="math inline">\(x\)</span>归为<span class="math inline">\(\omega_1\)</span>和<span class="math inline">\(\omega_2\)</span>,据此平均错误率可以写为： <span class="math display">\[
\begin{aligned}
P(e) &amp;= P(x \in \mathcal{R_1},\omega_2) + P(x \in \mathcal{R_2},\omega_1) \\
&amp;= P(x\in \mathcal{R_1}|\omega_2) P(\omega_2) + P(x \in \mathcal{R_2}|\omega_1) P(\omega_1) \\
&amp;= P(\omega_2) \int_{\mathcal{R_1}} p(x|\omega_2)dx + P(\omega_1)\int_{\mathcal{R_2}}p(x|\omega_1)dx \\
&amp;= P(\omega_2) P_2(e) + P(\omega_1) P_1(e)
\end{aligned}
\]</span> <img src="https://raw.githubusercontent.com/xuejy19/Images/master/bayes2.png" alt="最小错误率" /></p>
<p>图中斜线部分代表<span class="math inline">\(P(e)\)</span>的第一项，纹线部分代表第二项，而从最小错误率公式来看，若要使平均错误率<span class="math inline">\(P(e)\)</span>最小，只需要对于任意的<span class="math inline">\(x\)</span>，保证<span class="math inline">\(P(e|x)\)</span>最小，而这恰恰是贝叶斯最小错误率决策规则。</p>
<h3 id="最小风险贝叶斯决策">最小风险贝叶斯决策</h3>
<p>最小风险贝叶斯决策，或者称为最小损失贝叶斯决策，这种决策思想也是非常朴素： &gt; 同样是决策错误，但不同的错误带来的损失并不相同，有时甚至天差地别，比如去医院看病，对于早起癌症，漏警要比虚警严重的多，可能会让患者失去早期治疗的时机。</p>
<p>最小风险错误率准则正是考虑各种错误造成损失不同而提出的一种决策规则，若将决策空间记做<span class="math inline">\(\mathcal{A}\)</span>,而每个决策都将带来一定的损失，它通常是决策和自然状态的函数，比如<span class="math inline">\(\lambda(a_i,\omega_j)\)</span>则指代在<span class="math inline">\(x\)</span>处于状态<span class="math inline">\(\omega_j\)</span>而做出<span class="math inline">\(a_i\)</span>决策时的损失，下面给出数学符号定义： - 观测向量<span class="math inline">\(x\)</span>为<span class="math inline">\(d\)</span>维随机向量 <span class="math display">\[ x = [x_1, x_2 , \dots, x_d]^T\]</span> - 状态空间<span class="math inline">\(\Omega\)</span>由<span class="math inline">\(c\)</span>个自然状态(c类)组成 <span class="math display">\[ \Omega = \{ \omega_1, \dots, \omega_c \}\]</span> - 决策空间由<span class="math inline">\(n\)</span>个决策组成 <span class="math display">\[ \mathcal{A} = \{ a_1, \dots, a_n \} \]</span> - 决策表，决策表中第<span class="math inline">\(i\)</span>行<span class="math inline">\(j\)</span>列为相应损失值<span class="math inline">\(\lambda(a_i,\omega_j)\)</span></p>
<p>由于引入了损失的概念，在进行决策时便不能够只根据后验概率大小来做决策，必须考虑所采取的决策是否使得损失最小。对于给定的<span class="math inline">\(x\)</span>，如果我们采取决策<span class="math inline">\(a_i\)</span>,其对应<span class="math inline">\(c\)</span>种可能的损失<span class="math inline">\(\lambda(a_i,\omega_j)\)</span>,每种损失出现的概率为<span class="math inline">\(P(\omega_i|x)\)</span>,由此可以计算条件期望损失<span class="math inline">\(R(a_i|x)\)</span>: <span class="math display">\[
    R(a_i|x) = E(\lambda(a_i,\omega_j)) = \sum_{j=1}^c \lambda(a_i,\omega_j) P(\omega_j|x), i=1,\dots,n
\]</span> 在决策论种<span class="math inline">\(R(a_i|x)\)</span>被称作条件风险，由于<span class="math inline">\(x\)</span>是随机向量的观察值，因此对于不同的<span class="math inline">\(x\)</span>，采取决策<span class="math inline">\(a_i\)</span>时，其条件风险是不同的，所以最终采取何种决策应当与<span class="math inline">\(x\)</span>有关，在这种意义下，可以将决策<span class="math inline">\(a\)</span>看作是<span class="math inline">\(x\)</span>的函数，记做<span class="math inline">\(a(x)\)</span>,可以定义期望风险<span class="math inline">\(R\)</span>为： <span class="math display">\[
    R = \int R(a(x)|x) p(x)dx
\]</span> 若想使得期望风险<span class="math inline">\(R\)</span>最小，只需要保证对于任意的<span class="math inline">\(x\)</span>，<span class="math inline">\(R(a(x)|x)\)</span>最小，因此最小损失贝叶斯决策规则为： <span class="math display">\[
    if R(a_k|x) = \min \limits_{i=1,\dots,n}R(a_i|x) \Rightarrow a = a_k
\]</span> ### 在限定一类错误率条件下，使得另一类错误率最小 在两类别决策问题中，有两种错误分类的可能性,分别对应两个错误率<span class="math inline">\(P_1(e)\)</span>和<span class="math inline">\(P_2(e)\)</span>,而在实际应用中，我们往往期望某一个错误率不大于某个常数，而使另一类错误率尽可能小。比如在癌症诊断中，我们认识到将异常判断为正常非常严重，我们期望这类错误率为一个很小的值<span class="math inline">\(P_2(e) = \epsilon_0\)</span>,在满足该条件下，期望另一类错误率<span class="math inline">\(P_1(e)\)</span>尽可能低，这其实是一个带有等式约束的优化问题，可以用拉格朗日乘子法进行求解，拉格朗日函数<span class="math inline">\(L\)</span>为: <span class="math display">\[
    L = P_1(e) + \lambda(P_2(e) - \epsilon_0)
\]</span> 其中<span class="math inline">\(P_1(e)\)</span>为： <span class="math display">\[
    P_1(e) = \int_{\mathcal{R_2}} p(x|\omega_1)dx 
\]</span> <span class="math inline">\(P_2(e)\)</span>类似，因此拉格朗日函数可以写做： <span class="math display">\[
\begin{aligned}
    L &amp;= \int_{\mathcal{R_2}} p(x|\omega_1) + \lambda(\int_{\mathcal{R_1}} p(x|\omega_2)dx - \epsilon_0)  \\
    &amp;= 1-\lambda \epsilon_0 + \int_{\mathcal{R_1}} [\lambda  p(x|\omega_2) - p(x|\omega_1)dx]
\end{aligned}
\]</span> <span class="math inline">\(L\)</span>分别对分界点<span class="math inline">\(t\)</span>和<span class="math inline">\(\lambda\)</span>求导，可得： <span class="math display">\[
 \begin{cases}
    \frac{\partial L}{\partial t} = 0 \Rightarrow \lambda = \frac{p(t|\omega_1)}{p(t|\omega_2)} \\
    \frac{\partial L}{\partial \lambda} = 0 \Rightarrow P_2(e) = \epsilon_0
 \end{cases} 
\]</span> 由此便可得到Neyman-Pearson决策准则： <span class="math display">\[
    if \frac{p(x|\omega_1)}{p(x|omega_2)} &gt; \lambda \Rightarrow x \in \omega_1 
\]</span></p>
]]></content>
      <categories>
        <category>统计学习</category>
      </categories>
  </entry>
  <entry>
    <title>EM</title>
    <url>/2020/07/30/em/</url>
    <content><![CDATA[<p>在前面概率密度函数估计中，若概率模型的变量都是可观测变量，那么给定数据，便可以直接用极大似然估计法或者贝叶斯估计法来直接估计模型参数。但是当模型中含有隐变量时，便不能直接使用这些估计方法，<strong>EM算法就是含有隐变量的概率模型参数的极大似然估计法</strong><a id="more"></a>,本文便围绕EM算法展开，主要包含以下几部分： - 算法引入 - 算法定义 - 算法导出 - 算法收敛性分析 - 算法应用-高斯混合模型(GMM)</p>
<h3 id="算法引入">算法引入</h3>
<p>首先介绍一个使用EM算法的栗子，引出EM算法应用场景: &gt; 三硬币模型： 假设有三枚硬币A,B,C。这三枚硬币出现正面的概率分别为<span class="math inline">\(\pi,p,q\)</span>。现进行如下掷币实验：首先掷出硬币A,根据其结果选出硬币B或硬币C，正面选硬币B,反面选硬币C；然后掷选出的硬币，掷硬币的结果，正面记做1，反面记做0,独立的重复<span class="math inline">\(n\)</span>次实验，得到实验结果为<span class="math inline">\((x_1,\dots,x_n)\)</span> &gt; <strong>问题</strong>：根据实验结果估计三枚硬币参数(正面朝上概率)</p>
<p>这个问题的特殊性在于，我们只能够观察到最终实验结果，但是对于中间变量(硬币A的投掷结果)则没有记录，是隐变量，所以该问题是在有隐变量情况下的参数估计问题。下面首先尝试使用极大似然估计方法，做如下标记： - 待估计参数<span class="math inline">\(\theta = (\pi,p,q)\)</span> - 观测变量<span class="math inline">\(y\)</span>:最终掷硬币结果 - 隐含变量<span class="math inline">\(z\)</span>:每次实验掷硬币A的结果</p>
<p>若直接采用最大似然估计，我们希望能够写出似然概率<span class="math inline">\(P(y|\theta)\)</span>，但这个概率并不能够直接写出，需要引入隐含变量<span class="math inline">\(z\)</span>，因此考虑将似然概率写成 <span class="math display">\[
    \begin{aligned}
         P(y|\theta) &amp;= \sum P(y,z|\theta) = \sum_{z} P(z|\theta) P(y|\theta,z) \\\\
         &amp;= \pi p^y (1-p)^{1-y} + (1-\pi)q^y(1-q)^{1-y}
    \end{aligned}
\]</span> 将观测数据记做<span class="math inline">\(Y = (Y_1,\dots, Y_n)^T\)</span>,未观测数据记做<span class="math inline">\(Z = (Z_1,\dots, Z_n)^T\)</span>,则似然函数可以写做： <span class="math display">\[
    L = P(Y|\theta) = \prod_{j=1}^n[\pi p^{y_j} (1-p)^{1-y_j} + (1-\pi)q^{y_j}(1-q)^{1-y_j}]
\]</span> 对数似然函数则是: <span class="math display">\[
    logL = \sum_{j=1}^nlog[\pi p^{y_j} (1-p)^{1-y_j} + (1-\pi)q^{y_j}(1-q)^{1-y_j}]
\]</span> 直接极大化对数似然函数并不能得到解析解，该问题只能通过EM算法迭代求解</p>
<h3 id="算法推导">算法推导</h3>
<p>首先给出算法定义： <strong>EM算法</strong>: &gt; 输入： 观测变量数据<span class="math inline">\(Y\)</span>,隐变量数据<span class="math inline">\(Z\)</span>，联合分布<span class="math inline">\(P(Y,Z|\theta)\)</span>,条件分布<span class="math inline">\(P(Z|Y,\theta\)</span> &gt; 输出： 模型参数<span class="math inline">\(\theta\)</span> 1. 选择参数初始值<span class="math inline">\(\theta^0\)</span>,开始迭代 2. E步：记<span class="math inline">\(\theta^i\)</span>为第<span class="math inline">\(i\)</span>次迭代参数<span class="math inline">\(\theta\)</span>的估计值，在第<span class="math inline">\(i+1\)</span>次迭代的E步，计算：</p>
<p><span class="math display">\[
    \begin{aligned}
        Q(\theta,\theta^i) &amp;= E_Z[logP(Y,Z|\theta)|Y,\theta^i] \\\\
            &amp;= \sum_Z log P(Y,Z|\theta) P(Z|Y,\theta^i)
    \end{aligned}
\]</span></p>
<ol start="3" type="1">
<li>M步： 求使得<span class="math inline">\(Q(\theta,\theta^i)\)</span>极大化的<span class="math inline">\(\theta\)</span>,确定第<span class="math inline">\(i+1\)</span>次迭代的参数的估计值<span class="math inline">\(\theta^{i+1}\)</span> <span class="math display">\[ \theta^{i+1} = argmax_{\theta}Q(\theta,\theta^i)\]</span></li>
<li>重复第2,3步，直到算法收敛</li>
</ol>
<p>从形式上来看，<span class="math inline">\(Q\)</span>函数与极大似然函数其实是一样的，不同之处在于<span class="math inline">\(P(Z|Y,\theta^i)\)</span>中使用的是参数迭代估计值，用该迭代估计值代替真值，<span class="math inline">\(Q\)</span>函数可以看作是： &gt; 完全数据的对数似然函数<span class="math inline">\(logP(Y,Z|\theta)\)</span>关于在给定观测数据<span class="math inline">\(Y\)</span>和当前参数<span class="math inline">\(\theta^i\)</span>下对未观测数据<span class="math inline">\(Z\)</span>的的条件概率分布<span class="math inline">\(P(Z|Y,\theta^i)\)</span>的期望。</p>
<p>我们这样使用<span class="math inline">\(Q\)</span>函数来近似优化最大似然函数，需要证明这样的迭代优化是有效的</p>
<h3 id="算法导出">算法导出</h3>
<p>对数似然形式前面已经讨论过，形式为： <span class="math display">\[
    \begin{aligned}
        L(\theta) &amp;= log P(Y|\theta) = log \sum_Z P(Y,Z|\theta) \\
        &amp;= log(\sum_Z P(Y|\theta,Z) P(Z|\theta))
    \end{aligned}
\]</span> 直接极大化对数似然函数困难之处在于两点： - 含有隐变量<span class="math inline">\(Z\)</span> - 对数似然函数是和的对数的形式</p>
假设在第<span class="math inline">\(i\)</span>次迭代后参数<span class="math inline">\(\theta\)</span>估计值为<span class="math inline">\(\theta^i\)</span>,新的估计值是<span class="math inline">\(\theta\)</span>，我们期望新的估计值<span class="math inline">\(\theta\)</span>能够使得对数似然函数增大，因此，考虑计算两者的差： <span class="math display">\[
    \begin{aligned}
        L(\theta) - L(\theta^i) &amp;= log(\sum_Z P(Y|\theta,Z) P(Z|\theta)) - logP(Y|\theta^i) 
        \\\\
        &amp;= log(\sum_Z P(Z|Y,\theta^i) \frac{P(Y|Z,\theta)P(Z|\theta)}{P(Z|Y,\theta^i)}) - logP(Y|\theta^i) 
        \\\\
        &amp;\geq \sum_Z P(Z|Y,\theta^i) log \frac{P(Y|Z,\theta)P(Z|\theta)}{P(Z|Y,\theta^i)} - logP(Y|\theta^i)  (Jensen不等式)  
        \\\\
        &amp;= \sum_Z P(Z|Y,\theta^i) log \frac{P(Y|Z,\theta)P(Z|\theta)}{P(Z|Y,\theta^i)P(Y|\theta^i)}
    \end{aligned}
\]</span> 令 <span class="math display">\[
    B(\theta,\theta^i) = L(\theta^i) + \sum_Z P(Z|Y,\theta^i) log \frac{P(Y|Z,\theta)P(Z|\theta)}{P(Z|Y,\theta^i)P(Y|\theta^i)}
\]</span> 则有： <span class="math display">\[
    L(\theta) \geq B(\theta,\theta^i)
\]</span> 至此，我们找到了对数似然函数的一个下界，同时在点<span class="math inline">\(\theta^i\)</span>处，有： <span class="math display">\[
    L(\theta^i) = B(\theta^i,\theta^i)
\]</span> 因此若我们可以找到<span class="math inline">\(\theta^{i+1}\)</span>,使得<span class="math inline">\(B(\theta^{i+1},\theta^i)&gt;B(\theta^i, \theta^i)\)</span>,则有： <span class="math display">\[
    L(\theta^{i+1}) \geq B(\theta^{i+1},\theta^i) &gt; B(\theta^i,\theta^i) = L(\theta^i)
\]</span> 因此我们可以考虑优化<span class="math inline">\(B(\theta,\theta^i)\)</span>，从而变相使原始似然函数增大。接下来考虑<span class="math inline">\(B(\theta,\theta^i)\)</span>中需要优化的具体是哪一项: <span class="math display">\[
    \begin{aligned}
        \theta^{i+1} &amp;= argmax(L(\theta^i) +  \sum_Z P(Z|Y,\theta^i) log \frac{P(Y|Z,\theta)P(Z|\theta)}{P(Z|Y,\theta^i)P(Y|\theta^i)})  \\\\
        &amp;= argmax  \sum_Z P(Z|Y,\theta^i) log \frac{P(Y|Z,\theta)P(Z|\theta)}{P(Z|Y,\theta^i)P(Y|\theta^i)} \\\\
        &amp;= argmax \sum_Z P(Z|Y,\theta^i) log(P(Y|Z,\theta) P(Z|\theta)) \\\\
        &amp;= argmax \sum_Z P(Z|Y,\theta^i) log P(Y,Z|\theta) \\\\
        &amp;= argmax Q(\theta,\theta^i)
    \end{aligned}
\]</span>
<div data-align="center">
<figure>
<img src="https://raw.githubusercontent.com/xuejy19/Images/master/EM.png" alt="EM" /><figcaption>EM</figcaption>
</figure>
</div>
<p>图中下方曲线为<span class="math inline">\(B(\theta,\theta^i)\)</span>，上方曲线为<span class="math inline">\(L(\theta)\)</span>,在<span class="math inline">\(\theta^n\)</span>处，两者相等，当对<span class="math inline">\(B(\theta,\theta^n)\)</span>进行优化时，似然函数也得到了优化，同样，这种迭代算法只能够保证每次迭代都会使得似然函数增加，但并不能保证为全局最优。 ### 算法收敛性分析 该部分只给出一些结论，证明过程略过 &gt; <strong>定理1</strong>:设<span class="math inline">\(P(Y|\theta)\)</span>为观测数据的似然函数，<span class="math inline">\(\theta^i\)</span>为EM算法得到的参数估计序列，<span class="math inline">\(P(Y|\theta^i)\)</span>为对应的似然函数序列，则<span class="math inline">\(P(Y|\theta_i)\)</span>是单调递增的，即： <span class="math display">\[    
    P(Y|\theta^{i+1}) \geq P(Y|\theta^i) 
    \]</span> &gt; <strong>定理2</strong>:设<span class="math inline">\(L(\theta) = log P(Y|\theta)\)</span>为观测数据的对数似然函数，<span class="math inline">\(\theta^i\)</span>为EM算法得到的参数估计序列，<span class="math inline">\(L(\theta^i)\)</span>为对应的对数似然函数序列 &gt;1. 如果<span class="math inline">\(P(Y|\theta)\)</span>有上界,则<span class="math inline">\(L(\theta^i) = log P(Y|\theta^i)\)</span> 收敛到某一值<span class="math inline">\(L^*\)</span> &gt;2. 在函数<span class="math inline">\(Q(\theta,\theta^{&#39;})\)</span>与<span class="math inline">\(L(\theta)\)</span>满足一定条件下，由EM算法得到的参数估计序列$ <sup>i<span class="math inline">\(的收敛点\)</span></sup>*<span class="math inline">\(是\)</span>L()$的稳定点</p>
<h3 id="算法应用-高斯混合模型gmm">算法应用-高斯混合模型(GMM)</h3>
<p>首先给出高斯混合模型定义: &gt; 高斯混合模型是指具有如下形式的概率分布模型： <span class="math display">\[
    P(y|\theta) = \sum_{k=1}^K \alpha_k \phi(y|\theta_k)
\]</span> 其中,<span class="math inline">\(\alpha_k\)</span>是系数，<span class="math inline">\(\alpha_k \geq 0\)</span>,<span class="math inline">\(\sum \alpha_k = 1\)</span>;<span class="math inline">\(\phi(y|\theta_k)\)</span>是高斯概率密度函数，<span class="math inline">\(\theta_k = (\mu_k,\sigma_k^2)\)</span>,称为第<span class="math inline">\(k\)</span>个分模型</p>
<p>高斯模型是描述数据分布的一种常见模型，但在有时候单一的高斯模型并不足以对数据进行描述，因此便考虑该数据分布有没有可能用多个高斯模型来描述，即任意一个数据点可能来自于某一个分模型。假设观测数据<span class="math inline">\(y_1,y_2,\dots,y_N\)</span>由高斯混合模型生成： <span class="math display">\[
      P(y|\theta) = \sum_{k=1}^K \alpha_k \phi(y|\theta_k)
\]</span> 其中<span class="math inline">\(\theta = (\alpha_1,\dots,\alpha_K;\theta_1,\dots,\theta_K)\)</span>,我们现在期望利用观测数据将这些参数估计出来。</p>
<h4 id="隐变量定义">隐变量定义</h4>
<p>首先来看对于这样一个模型，隐变量是什么，数据是这样产生的：首先依概率<span class="math inline">\(\alpha_k\)</span>选择第<span class="math inline">\(k\)</span>个高斯分布分模型，然后依第<span class="math inline">\(k\)</span>个分模型的概率分布生成观测数据<span class="math inline">\(y\)</span>。观测数据<span class="math inline">\(y\)</span>是已知的，但是<span class="math inline">\(y\)</span>究竟来自于哪个分模型是未知的，记该变量为隐变量<span class="math inline">\(\gamma_{jk}\)</span>,其定义如下： <span class="math display">\[
    \begin{aligned}
        \gamma_{jk} &amp;= \begin{cases}
            1,&amp; 第j个观测来自第k个分模型 \\
            0,&amp; other
        \end{cases}   
        \\\\
        j &amp;= 1,\dots,N;k = 1,\dots,K
    \end{aligned}
\]</span> 目前，我们的观测数据为<span class="math inline">\(y_j\)</span>,未观测数据是<span class="math inline">\(\gamma_{jk}\)</span>,完全数据是： <span class="math display">\[
    (y_j,\gamma_{j_1},\dots,\gamma_{jk}),j=1,\dots,N
\]</span> 于是，可以写出完全数据的对数似然函数：</p>
<p><span class="math display">\[
    \begin{aligned}
         P(y,\gamma|\theta) &amp;= \prod_{j=1}^N P(y_j,\gamma_{j1},\dots,\gamma_{jk}|\theta) \\\\
         &amp;= \prod_{k=1}^K \prod_{j=1}^N [\alpha_k \phi(y_j|\theta_k)]^{\gamma_{jk} } \\\\
         &amp;= \prod_{k=1}^K \alpha_k^{n_k} \prod_{j=1}^N [\alpha_k \phi(y_j|\theta_k)]^{\gamma_{jk} } \\\\
         &amp;= \prod_{k=1}^K \alpha_k^{n_k} \prod_{j=1}^N [\frac{1}{\sqrt{2\pi}\sigma_k} exp(-\frac{(y_j-\mu_k)^2}{2\sigma_k^2})]^{\gamma_{jk}}
    \end{aligned}
\]</span></p>
<p>公式中<span class="math inline">\(n_k = \sum_{j=1}^N \gamma_{jk},\sum_{k=1}^K n_k = N\)</span>,由此，便可以写出完全数据的对数似然函数：</p>
<p><span class="math display">\[
 logP(y,\gamma|\theta) = \sum_{k=1}^K \{ n_klog\alpha_k + \sum_{j=1}^N \gamma_{jk}[log(\frac{1}{\sqrt{2\pi}})-log\sigma_k - \frac{1}{2\sigma_k^2} (y_j-\mu_k)^2]\}
\]</span></p>
<p><strong>E步</strong>：其实就是要在已知上次迭代参数估计值<span class="math inline">\(\theta\)</span>和观测值<span class="math inline">\(y\)</span>的情况下将<span class="math inline">\(\gamma_{jk}\)</span>估计出来，然后代回完全数据的对数似然函数，下面就求解下<span class="math inline">\(E(\gamma_{jk}|y,\theta)\)</span>:</p>
<p><span class="math display">\[
\begin{aligned}
    \hat{\gamma}_{jk} &amp;= E(\gamma_{jk}|y,\theta) = P(\gamma_{jk}|y,\theta)\\\\
    &amp;= \frac{P(\gamma_{jk = 1,y|\theta})}{\sum_{k=1}^K P(\gamma_{jk}=1,y_j|\theta)} \\\\
    &amp;= \frac{P(y_j|\gamma_{jk}=1,\theta)P(\gamma_{jk}=1|\theta)}{\sum_{k=1}^K P(y_j|\gamma_{jk}=1,\theta)P(\gamma_{jk}=1|\theta)} \\\\
    &amp;= \frac{\alpha_k \phi(y_j|\theta_k)}{\sum_{k=1}^K \alpha_k \phi(y_j|\theta_k)}
\end{aligned}
\]</span></p>
<p>将该估计值代回完全数据的对数似然函数，便得到了<span class="math inline">\(Q\)</span>函数：</p>
<p><span class="math display">\[
    Q(\theta,\theta^i) =  logP(y,\gamma|\theta) = \sum_{k=1}^K \{ n_klog\alpha_k + \sum_{j=1}^N \hat{\gamma}_{jk}[log(\frac{1}{\sqrt{2\pi}})-log\sigma_k - \frac{1}{2\sigma_k^2} (y_j-\mu_k)^2]\}
\]</span></p>
<p><strong>M步</strong>：迭代的M步是求函数<span class="math inline">\(Q(\theta,\theta^i)\)</span>对<span class="math inline">\(\theta\)</span>的极大值，即求新一轮迭代的模型参数:</p>
<p><span class="math display">\[
    \theta^{i+1} = \arg max_{\theta} Q(\theta,\theta^i)
\]</span></p>
<p>该步只需分别对<span class="math inline">\(\alpha_K,\mu_k,\sigma_k\)</span>求偏导，令偏导等于0,注意在求解<span class="math inline">\(\alpha_k\)</span>时需要利用<span class="math inline">\(\alpha_k\)</span>天然的约束条件<span class="math inline">\(\sum_{k=1}^K \alpha_k = 1\)</span>,最终求解结果为:</p>
<p><span class="math display">\[
\begin{aligned}
    \hat{\mu}_k &amp;= \frac{\sum_{j=1}^N \hat{\gamma}_{jk}y_j}{\sum_{j=1}^N \hat{\gamma}_{jk}} \\\\
    \hat{\sigma}_k^2 &amp;= \frac{\sum_{j=1}^N \hat{\gamma}_{jk}(y_j - \mu_k)^2}{\sum_{j=1}^N \hat{\gamma}_{jk}} \\\\
    \hat{\alpha}_k &amp;= \frac{n_k}{N} = \frac{\sum_{j=1}^N \hat{\gamma}_{jk}}{N}
\end{aligned}
\]</span> #### 总结 该部分就高斯混合模型的EM算法进行总结 - 取参数的初始值开始迭代 - E步：根据当前模型参数，计算分模型<span class="math inline">\(k\)</span>对观测数据<span class="math inline">\(y_j\)</span>的响应度 - M步：计算新一轮迭代的模型参数 - 重复直到算法收敛</p>
]]></content>
      <categories>
        <category>统计学习</category>
      </categories>
      <tags>
        <tag>EM算法,隐变量,高斯混合模型</tag>
      </tags>
  </entry>
  <entry>
    <title>马尔可夫网络</title>
    <url>/2020/07/25/markov-network/</url>
    <content><![CDATA[<p>在这一部分我将介绍另一种常见的概率图模型:马尔可夫网络，或称马尔可夫随机场。接下来分为以下几部分进行组织<a id="more"></a>： - 马尔可夫网络 - 独立性假设 - 势函数</p>
<h3 id="马尔可夫网络">马尔可夫网络</h3>
<p>首先给出维基百科上关于马尔可夫网络的定义： &gt; 马尔可夫网络是一组有马尔可夫性质随机变量<span class="math inline">\(X\)</span>的全联合概率分布模型。马尔可夫网络类似贝叶斯网络用于表示依赖关系。但是，一方面它可以表示贝叶斯网络无法表示的一些关系，如循环依赖；另一方面，它不能表示贝叶斯网络能够表示的某些关系，如推导关系。</p>
<p>马尔可夫网络具有以下特征： - 无向图<span class="math inline">\(H = (V,E)\)</span> - 每个顶点<span class="math inline">\(v \in V\)</span>表示一个在集合<span class="math inline">\(X\)</span>的随机变量，每条边<span class="math inline">\(\{u,v\}\in E\)</span> 表示随机变量<span class="math inline">\(u\)</span>和<span class="math inline">\(v\)</span>之间的依赖关系</p>
<h3 id="独立性假设">独立性假设</h3>
<p>马尔可夫网络与贝叶斯网络类似，以图的形式对独立性假设进行编码，首先给出图中激活路径的概念： - 激活路径：一个路径<span class="math inline">\(X_1,X_2,\dots,X_n\)</span>是激活的 <span class="math inline">\(\Leftrightarrow\)</span> 没有路径上的变量被观测到</p>
<p>有了激活路径概念之后，便可以讨论图中的条件独立关系： - 在图<span class="math inline">\(H\)</span>中，节点集<span class="math inline">\(X\)</span>与节点集<span class="math inline">\(Y\)</span>相对于是节点集<span class="math inline">\(Z\)</span>是分离的 <span class="math inline">\(\Leftrightarrow\)</span> <span class="math inline">\(X\)</span>与<span class="math inline">\(Y\)</span>之间不存在激活路径 - 表示为<span class="math inline">\(sep_H(X;Y|Z)\)</span>,栗子如下图所示。</p>
<figure>
<img src="https://raw.githubusercontent.com/xuejy19/Images/master/mav1.png" alt="栗子" /><figcaption>栗子</figcaption>
</figure>
<h3 id="势函数">势函数</h3>
<p>在贝叶斯网络中，节点之间的关系通过条件概率进行表征，而在马尔可夫网络中，节点之间的关系通过势函数来表征，势函数定义如下： &gt; 一个函数集合<span class="math inline">\(f_k\)</span>(也称为因子或者团因子有时也称为特征)，每一个<span class="math inline">\(f_k\)</span>的定义域是图<span class="math inline">\(G\)</span>的团或子团<span class="math inline">\(k\)</span>,每一个<span class="math inline">\(f_k\)</span>是从可能的特定联合的指派(到元素<span class="math inline">\(k\)</span>)到非负实数的映射。</p>
<p>总结而言，势函数具有以下特征： &gt; - 非负 &gt; - 与条件概率不同，其求和不必为1</p>
<h3 id="吉布斯分布">吉布斯分布</h3>
<p>我们引入该图模型的目的在于：<strong>通过马尔可夫网络来对变量之间的独立性关系进行编码，然后获得对于联合概率分布更加紧凑的表示形式</strong>，而吉布斯分布便是将联合概率与势函数相连接的桥梁。 一个概率分布<span class="math inline">\(P\)</span>可以分解为图<span class="math inline">\(H\)</span>： &gt; - <span class="math inline">\(H\)</span>可以分为若干子团<span class="math inline">\(D_i\)</span> &gt; - 在每个子团上相应定义了势函数<span class="math inline">\(\pi_i\)</span></p>
<p>由此便可以写出吉布斯分布形式： <span class="math display">\[
\begin{aligned}
     P(X_1,X_2,\dots,X_n) = \frac{1}{Z} f(X_1,X_2,\dots,X_n) 
\end{aligned}  
\]</span></p>
<p>其中: &gt; - <span class="math inline">\(f(X_1,X_2,\dots,X_n) = \prod_{i=1}^m \pi_i(D_i)\)</span> &gt; - <span class="math inline">\(Z = \sum_{X_1,\dots,X_n} f(X_1,\dots,X_n)\)</span></p>
<p>每一个势函数定义了一个映射:<span class="math inline">\(Sub(X) \rightarrow \mathcal{R^+}\)</span> ,一个势函数的定义域相当于是一个集合，而这个集合中的变量彼此之间是直接依赖关系，从这个视角来看，条件概率可以看作是势函数的特例(仅仅表征两个变量之间依赖关系)。</p>
<p>下面我们讨论下一个势函数(因子)应该定义在一个多大的集合上，首先考虑如果在每条边上均定义一个势函数，那么马尔可夫网络能否表征联合概率分布？答案是并不行，为此我以一个7个节点(均为二值变量)的全连接网络为例，分别讨论两种情况下具有的独立参数的数量： - 联合概率分布：<span class="math inline">\(2^7 - 1 = 127\)</span> - 边上势函数：<span class="math inline">\(4*C_7^2 = 84\)</span></p>
<p>显然对于全连接网络，若给予每个边一个势函数并不足以表征联合概率分布，在前面已经说明一个势函数定义域中的变量应当是两两直接相关，因此势函数的定义域应当是一个团(clique)。 <span class="math inline">\(\Rightarrow\)</span> <strong>马尔可夫网络中的局部结构是团</strong></p>
<h3 id="马尔可夫网络分解">马尔可夫网络分解</h3>
<p>首先给出<span class="math inline">\(I-Map\)</span>的概念:<span class="math inline">\(H\)</span>为一个马尔可夫网络，<span class="math inline">\(P\)</span>为联合概率分布，若有<span class="math inline">\(I(H) \subseteq I(P)\)</span>，则称马尔可夫网络<span class="math inline">\(H\)</span>为分布<span class="math inline">\(P\)</span>的一个<span class="math inline">\(I-Map\)</span>。</p>
<p>记<span class="math inline">\(D_i\)</span>为马尔可夫网络<span class="math inline">\(H\)</span>中的子团，Hammersley-Clifford定理如下： &gt; 对于一个无向图<span class="math inline">\(H\)</span>和分布<span class="math inline">\(P\)</span>,<span class="math inline">\(P\)</span>可以被分解成<span class="math inline">\(P(X) = \frac{1}{Z}\prod \pi_i (D_i) \Leftrightarrow\)</span> <span class="math inline">\(H\)</span>是<span class="math inline">\(P\)</span>的<span class="math inline">\(I-Map\)</span></p>
<p>在实际进行分解时一般都是在最大团上定义势函数</p>
<h3 id="对数表示">对数表示</h3>
<p>在马尔可夫网络中，联合分布除了可以利用吉布斯分布来进行表示，也可以采取对数表示的方法，将原始的势函数<span class="math inline">\(\pi_i(D_i)\)</span> 表示成<span class="math inline">\(exp(-\epsilon(D_i))\)</span>的形式，其中<span class="math inline">\(\epsilon(D_i) = -log(\pi_i(D_i))\)</span>,据此，联合概率分布可以表示成： <span class="math display">\[
    P(X_1,X_2,\dots,X_n) = \frac{1}{Z} exp(-\sum_{i=1}^m \epsilon(D_i))
\]</span> 这种表示方法将连乘变成了连家，计算更加方便，同时有定理证明，若无向图<span class="math inline">\(H\)</span>是分布<span class="math inline">\(P\)</span>的一个独立映射，那么<span class="math inline">\(\epsilon(D_i) = \omega_i \phi_i(D_i)\)</span>,此时<span class="math inline">\(\phi\)</span>被称作特征函数，与势函数关系为： <span class="math display">\[
    \pi_i(D_i) = exp(\omega_i \phi_i(D_i))
\]</span></p>
]]></content>
      <categories>
        <category>统计学习</category>
      </categories>
  </entry>
  <entry>
    <title>SVM</title>
    <url>/2020/08/12/svm/</url>
    <content><![CDATA[<p>这部分将介绍支持向量机(SVM)算法，该部分将按照以下几部分进行组织： - 算法历史 - 线性可分支持向量机与硬间隔最大化 - 线性支持向量机与软间隔最大化 - 非线性支持向量机与核函数 - SVDD <a id="more"></a></p>
<h3 id="算法历史">算法历史</h3>
<p>支持向量机算法是在机器学习领域非常著名的算法，在神经网络算法得到普及之前，支持向量机算法可以说是统治了机器学习领域的半壁江山，该算法由Vapnik在贝尔实验室开发，根据Vapnik和Chervonekis提出的统计学习框架或VC理论，它提出了一种最可靠的预测方法（ 1974年）和瓦普尼克（Vapnik）（1982年，1995年）。给定一组训练示例，每个训练示例都标记为属于两个类别中的一个或另一个，则SVM训练算法会构建一个模型，该模型将新示例分配给一个类别或另一个类别，使其成为非概率 二进制 线性分类器（尽管方法例如Platt缩放存在以在概率分类设置中使用SVM）。SVM模型是将示例表示为空间中的点，并进行了映射，以使各个类别的示例被尽可能宽的明显间隙分开。然后，将新示例映射到相同的空间，并根据它们落入的间隙的侧面来预测属于一个类别。 除执行线性分类外，SVM还可以使用所谓的内核技巧有效地执行非线性分类，将其输入隐式映射到高维特征空间。</p>
<h3 id="线性可分支持向量机与硬间隔最大化">线性可分支持向量机与硬间隔最大化</h3>
<p>对于线性可分数据，上一部分介绍的感知机算法便可以处理，但需要注意的是，感知机算法学习得到的超平面可能有无穷多个，那么这些超平面中哪个是最优的呢？支持向量机算法便回答了这一问题，该算法认为使两类样本点间隔最大的超平面是最优超平面。首先给出线性可分支持向量机的定义： &gt; <strong>线性可分支持向量机</strong>:给定线性可分训练数据集，通过间隔最大化或者等价地求解相应的凸二次规划问题学习得到的分离超平面为： <span class="math display">\[
    \omega^* \cdot x + b^* = 0
\]</span> 以及相应的分类决策函数: <span class="math display">\[ f(x) = sign(\omega^* \cdot x + b^*)\]</span></p>
<p>下面就结合下图来对支持向量机的算法思想进行阐述,图中红点与蓝点分别代表两个类别，我们现在期望找到一个超平面，使得两个类别的样本点都能够尽可能离该超平面距离远，也就是希望中间的margin尽可能大。 <img src="https://raw.githubusercontent.com/xuejy19/Images/master/SVM.png" alt="SVM" /> 这便是支持向量机的思想，下一步需要做的便是将这种想法翻译成一个数学问题，在这里首先引入两个间隔的概念： &gt; - 几何间隔：所谓几何间隔就是样本点<span class="math inline">\((x_i,y_i)\)</span>到超平面的实际距离，其计算公式为(此时样本点已经被超平面正确分类)： <span class="math display">\[ d = y_i \frac{\omega x_i + b}{||\omega||}\]</span> &gt; - 函数间隔：不考虑几何间隔中<span class="math inline">\(||\omega||\)</span>便得到了函数间隔： <span class="math display">\[ d = y_i(\omega x_i +b)\]</span></p>
<p>需要注意的是，函数间隔可以表示分类预测的正确性及确信度，在感知机学习中，我们便是使用函数间隔，这是由于数据线性可分，最终目标函数都会收敛到0，使用几何间隔与函数间隔最终效果并无不同(两种间隔符号一致)。但在支持向量机算法中，我们希望间隔最大，这时显然函数间隔是不够的，因为只要成比例的增大<span class="math inline">\(\omega,b\)</span>，超平面并没有改变，但是函数间隔却便为两倍，因此在该问题中，我们应当使用几何间隔,至此支持向量机的思想可以写成如下优化问题的形式： <span class="math display">\[
    \begin{aligned}
        &amp;max_{\omega,b} \quad \gamma  \\
        &amp;s.t. \quad y_i \frac{\omega x_i +b}{||\omega||} \geq \gamma,\quad i = 1,2,\dots,N
    \end{aligned}
\]</span> 上面是优化问题的原始形式，下面尝试对该优化问题进行等价转换以便于求解，首先根据几何间隔与函数间隔的关系，将优化问题转化为： <span class="math display">\[
    \begin{aligned}
         &amp;max_{\omega,b} \quad \frac{\hat{\gamma}}{||\omega||}  \\
        &amp;s.t. \quad y_i (\omega x_i +b) \geq \hat{\gamma},\quad i = 1,2,\dots,N
    \end{aligned}
\]</span> 此时可以注意到函数间隔<span class="math inline">\(\hat{\gamma}\)</span>并不影响该优化问题求解，因此考虑将<span class="math inline">\(\hat{\gamma}\)</span>置为1，注意到最大化<span class="math inline">\(\frac{1}{||\omega||}\)</span>等价于最小化<span class="math inline">\(\frac{1}{2}||\omega||^2\)</span>,因此优化问题最终可以表述为: <span class="math display">\[
    \begin{aligned}
        &amp;min_{\omega,b} \quad \frac{1}{2} ||\omega||^2 \\\\
        &amp;s.t. \quad y_i (\omega x_i +b) \geq 1,\quad i = 1,2,\dots, N
    \end{aligned}
\]</span> 这是一个典型的凸二次规划问题，运用凸优化知识可进行求解。</p>
<p>下面给出一个定理来说明最大间隔分离超平面唯一的定理： &gt; 定理：若训练数据集<span class="math inline">\(T\)</span>线性可分，则可将训练数据集中的样本点完全分开的最大间隔分离超平面存在且唯一</p>
<h4 id="支持向量与间隔边界">支持向量与间隔边界</h4>
<p>首先给出支持向量的定义: &gt; 在线性可分情况下，训练数据集中样本点中与分离超平面距离最近的样本点的实例称为支持向量。</p>
<p>反映在优化问题中，支持向量满足约束条件的等号,支持向量在超平面: <span class="math display">\[
    \begin{aligned}
         H_1&amp;: \omega \cdot x +b =1  \\
         H_2&amp;: \omega \cdot x +b = -1
    \end{aligned}
\]</span> 两个支撑超平面之间的距离称为间隔，该间隔距离为： <span class="math display">\[
    d = \frac{2}{||\omega||}
\]</span> 在决定分离超平面时只有支持向量起作用，而其他实例点并不起作用。</p>
<h4 id="学习的对偶算法">学习的对偶算法</h4>
<p>在进行求解时，我们往往考虑不直接求解原问题，而是求解原问题的对偶问题, 又因为原问题是一个凸优化问题，同时满足Slater约束品性，因此考虑求解其对偶问题。首先写出Lagrange函数： <span class="math display">\[
    L(\omega,b,\alpha) = \frac{1}{2}||\omega^2|| - \sum_{i=1}^N \alpha_i y_i (\omega \cdot x_i +b) + \sum_{i=1}^N \alpha_i
\]</span> 接下来求解对偶函数，对偶函数<span class="math inline">\(g(\alpha)\)</span>表示为： <span class="math display">\[
    g(\alpha) = \min_{\omega,b} L(\omega,b,\alpha)
\]</span> 因为<span class="math inline">\(L(\omega,b,\alpha)\)</span>是凸函数，因此只需要对<span class="math inline">\(\omega,b\)</span>求偏导，令偏导等于0即可： <span class="math display">\[
    \begin{aligned}
        \frac{\partial L(\omega,b,\alpha)}{\partial \omega} &amp;= \omega - \sum_{i=1}^N \alpha_i y_i x_i = 0 \rightarrow \omega =  \sum_{i=1}^N \alpha_i y_i x_i  \\\\
        \frac{\partial L(\omega,b,\alpha)}{\partial b} &amp;= -\sum_{i=1}^N \alpha_i y_i = 0 \rightarrow \sum_{i=1}^N \alpha_i y_i = 0
    \end{aligned}
\]</span> 回代拉格朗日函数，可得对偶函数： <span class="math display">\[
    \begin{aligned}
          g(\alpha) &amp;= \frac{1}{2} \sum_{i=1}^N \sum_{j=1}^N \alpha_i \alpha_j y_i y_i x_i^T x_j - \sum_{i=1}^N \sum_{j=1}^N \alpha_i \alpha_j y_i y_i x_i^T x_j + \sum_{i=1}^N \alpha_i  \\\\
          &amp;= -\frac{1}{2} \sum_{i=1}^N \sum_{j=1}^N \alpha_i \alpha_j y_i y_i x_i^T x_j + \sum_{i=1}^N \alpha_i
    \end{aligned}
\]</span> 所以，对偶优化问题可以写做： <span class="math display">\[
    \begin{aligned}
        &amp;\max_{\alpha} \quad -\frac{1}{2} \sum_{i=1}^N \sum_{j=1}^N \alpha_i \alpha_j y_i y_i x_i^T x_j + \sum_{i=1}^N \alpha_i \\\\
        &amp;s.t. \quad \sum_{i=1}^N \alpha_i y_i =0;\alpha_i \geq 0,i=1,\dots,N
    \end{aligned}
\]</span> 对于该问题的求解可以通过SMO(序列最小优化)算法进行求解，因为强对偶性成立,KKT条件满足： <span class="math display">\[
    \begin{aligned}
        &amp;\nabla_{\omega} L(\omega^*,b^*,\alpha^*) = \omega^* - \sum_{i=1}^N \alpha_i^* y_i x_i = 0 \\
        &amp;\nabla_{b} L(\omega^*,b^*,\alpha^*) = -\sum_{i=1}^N \alpha_i^* y_i = 0 \\
        &amp;\alpha_i^* (y_i(\omega^*x_i+b^*)-1) = 0 \\
       &amp;y_i(\omega^* x_i +b^*) - 1 \geq 0 \\
        &amp;\alpha_i^* \geq 0 
    \end{aligned}
\]</span> 由此便可建立对偶问题最优解<span class="math inline">\(\alpha^*\)</span>与原问题最优解<span class="math inline">\(\omega^*,b^*\)</span>之间的关系，首先至少有一个<span class="math inline">\(\alpha_j^*&gt;0\)</span>,这是因为若<span class="math inline">\(\alpha^*\)</span>均为0，则<span class="math inline">\(\omega^*\)</span>为0,这不是原始最优化问题的解，假设<span class="math inline">\(\alpha_j^*&gt;0\)</span>,则由互补松弛条件，此时有<span class="math inline">\(y_j(\omega^* x_j +b^*) = 1\)</span>,而<span class="math inline">\(\omega^*\)</span>表达式是知道的，由此可得<span class="math inline">\(\omega^*,b^*\)</span>表达式为： <span class="math display">\[
    \begin{aligned}
        \omega^* &amp;= \sum_{i=1}^N \alpha_i^* y_i x_i \\
        b^* &amp;=  y_j - \sum_{i=1}^N \alpha_i^* y_i(x_i\cdot x_j)
    \end{aligned}
\]</span> 至此，我们便通过求解对偶问题得到了原始问题的最优解，得到了超平面方程以及判决方程： <span class="math display">\[
\begin{aligned}
    &amp;\omega^* \cdot x + b^* = 0 \\
    &amp;f(x) = sign(\omega^* \cdot x + b^*)
\end{aligned}
\]</span> 通过引入对偶问题，我们便可以从数学的角度导出支持向量: &gt; 训练数据集中对应于<span class="math inline">\(\alpha_i^* &gt;0\)</span>的样本点称为支持向量</p>
<h3 id="线性支持向量机与软间隔最大化">线性支持向量机与软间隔最大化</h3>
<h4 id="原始问题">原始问题</h4>
<p>前面介绍的算法是针对线性可分数据,那么对于大部分线性可分的数据集，支持向量机能否处理呢？答案是可以，但是需要对原始优化问题做适当变换,对于线性可分的数据，我们要求各个样本点到分类超平面的函数间隔都要大于1，对于不可分数据点，我们直观上的想法是考虑能否将该约束放松，因此我们考虑为每一个样本点引入一个松弛变量<span class="math inline">\(\xi_i\)</span>,使得函数间隔加上松弛变量等于1。这样，约束条件变为: <span class="math display">\[
    y_i(\omega \cdot x_i +b) \geq 1 - \xi_i
\]</span> 但需要注意的人，该松弛变量的引入是有代价的，需要在目标函数中进行惩罚，因此考虑在目标函数中加入惩罚项<span class="math inline">\(\xi_i\)</span>，将目标函数改写为： <span class="math display">\[
    \frac{1}{2} ||\omega||^2  + C\sum_{i=1}^N \xi_i
\]</span> 其中<span class="math inline">\(C\)</span>是惩罚系数，<span class="math inline">\(C\)</span>值大时对误分类惩罚增大。最小化该目标函数包含两层含义：使间隔尽量大；使误分类点尽量少。线性不可分支持向量机的学习问题变为如下凸二次规划问题： <span class="math display">\[
    \begin{aligned}
        &amp;\min_{\omega,b,\xi} \quad \frac{1}{2} ||\omega||^2 + C \sum_{i=1}^N \xi_i \\\\
        &amp;s.t. \quad y_i(\omega x_i +b) \geq 1- \xi_i,i=1,2,\dots,N \\\\
        &amp;\qquad \xi_i \geq 0,i=1,2,\dots,N
    \end{aligned}
\]</span> 对于该原始问题，可以证明<span class="math inline">\(\omega\)</span>的解是唯一的，但<span class="math inline">\(b\)</span>的解可能不唯一，而是存在一个区间。</p>
<h4 id="对偶问题">对偶问题</h4>
<p>在该部分便不进行对偶问题推导了，步骤与上一部分一致，最终得到的对偶问题形式为： <span class="math display">\[
    \begin{aligned}
        &amp;\max_{\alpha} \quad -\frac{1}{2} \sum_{i=1}^N \sum_{j=1}^N \alpha_i \alpha_j y_i y_i x_i^T x_j + \sum_{i=1}^N \alpha_i \\\\
        &amp;s.t. \quad \sum_{i=1}^N \alpha_i y_i =0,i=1,\dots,N  \\\\
        &amp; \qquad 0 \leq \alpha_i \leq C \quad i=1,2,\dots,N
    \end{aligned}
\]</span> 可以看到引入松弛变量后对偶问题形式基本未发生变化，仅仅约束条件中<span class="math inline">\(\alpha_i\)</span>范围变了。需要注意的是将原始问题转化为对偶问题的过程中，引入了两个Lagrange算子<span class="math inline">\(\alpha_i,\mu_i\)</span>，但由于存在$ _i + _i = C <span class="math inline">\(以及\)</span>_i 0, _i 0 $,因此将这些约束条件简化为<span class="math inline">\(0 \leq \alpha_i \leq C\)</span>。 下面讨论如何根据对偶问题最优解求解原问题最优解，与硬间隔支持向量机不同，在该问题中，互补松弛条件有两个: <span class="math display">\[
    \begin{aligned}
        &amp;\alpha_i^* (y_i(\omega^*x_i+b^*)-1 + \xi_i) = 0  \\
        &amp;\mu_i \xi_i = 0 
    \end{aligned}
\]</span> 在求解<span class="math inline">\(b^*\)</span>时，关键是要将支持向量确定出来，首先支持向量应当在支撑超平面上，这就要求<span class="math inline">\(\alpha_i&gt;0\)</span>,同时松弛间隔<span class="math inline">\(\xi_i\)</span>应当为0，由第二个互补松弛条件可知<span class="math inline">\(\mu_i\)</span>不能够为0，因此<span class="math inline">\(\alpha_i\)</span>必须小于<span class="math inline">\(C\)</span>,换句话说，<span class="math inline">\(\alpha^* = C\)</span>所对应的点均在支撑超平面以内，若<span class="math inline">\(\xi_i&lt;1\)</span>,则仍能分类正确，若大于1，则就会分类错误。<span class="math inline">\(\omega^*\)</span>和<span class="math inline">\(b^*\)</span>表达式可以写做： <span class="math display">\[
    \begin{aligned}
        \omega^* &amp;= \sum_{i=1}^N \alpha_i^* y_i x_i \\
        b^* &amp;= y_j - \sum_{i=1}^N y_i \alpha_i^* (x_i \cdot x_j)
    \end{aligned}
\]</span></p>
<h4 id="非线性支持向量机与核函数">非线性支持向量机与核函数</h4>
<p>支持向量机是解决线性分类问题的一种有效方法，但并不能解决非线性分类问题，那么非线性的数据就不能用线性的方法解决么？ 直观上我们有这样一个方法，考虑降维度线性不可分的数据映射到高维度，使映射后的数据在高维度线性可分，由此便引出了本节的核函数。 #### 核函数 首先先举一个空间变换的栗子，设原空间为<span class="math inline">\(\mathcal{X} \subset R^2,x = (x^1,x^2)^T\)</span>,新空间为<span class="math inline">\(\mathcal{Z} \subset R^2,z = (z^1,z^2)^T\)</span>,定义从原空间到新空间的映射： <span class="math display">\[
    z = \phi(x) = ((x^1)^2,(x^2)^2)^T
\]</span> 经过这样一个变换原空间中的椭圆： <span class="math display">\[
    w_1 (x^1)^2 + w_2 (x^2)^2 +b = 0
\]</span> 变换为了新空间中的直线： <span class="math display">\[
    w_1 z^1 + w_2 z^2 + b = 0
\]</span> 这样，原空间的非线性可分问题就变成了新空间的线性可分问题。从该问题出发，我们可以总结出用线性分类方法求解非线性分类问题的步骤： - 首先使用一个变换将原空间中的数据映射到新空间 - 在新空间中使用线性分类学习方法从训练数据中学习分类模型</p>
<p>而在支持向量机中，达到这一目的的方法应用核技巧，下面首先给出核函数的定义： &gt; 核函数：设<span class="math inline">\(\mathcal{X}\)</span>是输入空间，又设<span class="math inline">\(\mathcal{H}\)</span>为特征空间(希尔伯特空间),如果存在一个从<span class="math inline">\(\mathcal{X}\)</span>到<span class="math inline">\(\mathcal{H}\)</span>的映射： <span class="math display">\[
    \phi(x):\mathcal{X} \rightarrow \mathcal{H}
\]</span> 使得对所有<span class="math inline">\(x,z \in \mathcal{X}\)</span>,函数<span class="math inline">\(K(x,z)\)</span>满足条件: <span class="math display">\[
    K(x,z) = \phi(x) \cdot \phi(z)
\]</span> 则称<span class="math inline">\(K(x,z)\)</span>为核函数，<span class="math inline">\(\phi(x)\)</span>为映射函数</p>
<p>核技巧的想法是，在学习与预测中只定义核函数<span class="math inline">\(K(x,z)\)</span>,而不显式的定义映射函数<span class="math inline">\(\phi\)</span>。同时，对于一个核<span class="math inline">\(K(x,z)\)</span>,特征空间<span class="math inline">\(\mathcal{H}\)</span>和映射函数<span class="math inline">\(\phi\)</span>的取法并不唯一</p>
<h4 id="非线性支持向量机">非线性支持向量机</h4>
<p>在线性支持向量机的对偶问题中,无论是目标函数还是决策函数中均只包含实例的内积的形式，因此若用<span class="math inline">\(K(x,y)\)</span>代替<span class="math inline">\(x\cdot y\)</span>,实际上便是在对高维空间中的特征点设计线性分类器。那现在的关键问题便在于如何选择合适的核函数。</p>
<p>关于核函数这部分的理论我后面会另开一个章节进行介绍学习，在这一部分便直接给出一些常用的核函数： - 多项式核函数: <span class="math display">\[ K(x,z) = (x\cdot z +1)^p\]</span> - 高斯核函数(径向基函数) <span class="math display">\[ K(x,z) = exp(- \frac{||x-z||^2}{2\sigma^2})\]</span></p>
<p>需要注意的是，在引入核函数后，又多引入了超参数，比如高斯核函数中的<span class="math inline">\(\sigma\)</span>,而这些超参数的选择也会对分类效果产生影响。</p>
<h3 id="一类分类器设计">一类分类器设计</h3>
<p>在前面介绍的支持向量机算法中，针对的都是二分类情况，但当我们做故障检测时，往往就只有大量的正常样本点，现在我们需要根据这些正常样本点来设计一个分类器以对未出现的异常样本点进行检测。在这一部分介绍两种算法： - OCSVM - SVDD</p>
<h4 id="ocsvm">OCSVM</h4>
<p>在OCSVM中，将坐标原点作为唯一的异常点,最大化最优超平面到远点的距离，若记<span class="math inline">\(\rho\)</span>为从原点到超平面的距离，<span class="math inline">\(\xi_i\)</span>为松弛变量，则优化问题可以写做： <span class="math display">\[
    \begin{aligned}
        &amp;\max_{\omega,\rho,\xi_i} \rho - C\sum_{i=1}^N \xi_i \\
        &amp; s.t. \quad \omega \cdot x_i \geq \rho - \xi_i,\xi_i \geq 0,||\omega|| = 1
    \end{aligned}
\]</span></p>
<h4 id="svdd">SVDD</h4>
<p>SVDD的基本思想则是，对于<span class="math inline">\(n\)</span>维空间中的数据点集，寻找最小超球面，使其成为所有点的边界，其对应优化问题的原问题为: <span class="math display">\[
    \begin{aligned}
         &amp;\min_{a,R,\xi} \quad R^2 + C\sum_{i=1}^N \xi_i \\
         &amp;s.t. \quad ||x_i -a||^2 \leq R^2 + \xi_i \\
         &amp;\qquad \quad \xi_i \geq 0 
    \end{aligned}
\]</span></p>
]]></content>
      <categories>
        <category>统计学习</category>
      </categories>
      <tags>
        <tag>支持向量机</tag>
      </tags>
  </entry>
  <entry>
    <title>贝叶斯网络</title>
    <url>/2020/07/24/bayes-network/</url>
    <content><![CDATA[<p>在该部分对贝叶斯网络进行介绍，按照递进关系总结以下知识点：<a id="more"></a> - 条件独立性 - 条件参数化 - 朴素贝叶斯模型 - <strong>贝叶斯网络</strong></p>
<hr />
<h3 id="问题来源">问题来源</h3>
<p>首先探讨下为什么我们需要利用变量之间的条件独立性性质，假设我们要描述的对象为<span class="math inline">\(n\)</span>个二值变量：<span class="math inline">\(X_1,\dots,X_n\)</span>,这些变量的联合分布为<span class="math inline">\(P\)</span>,那么我们需要<span class="math inline">\(\bf{2^n - 1}\)</span>个参数来描述联合分布<span class="math inline">\(P\)</span>，所需参数量是<span class="math inline">\(O(2^n)\)</span>级别，我们便考虑能否利用变量之间的某种性质使得<span class="math inline">\(P\)</span>可以用更少的参数来描述，由此便引出了变量之间的独立性性质。</p>
<h3 id="独立性">独立性</h3>
<p>下面给出一些独立性等价的定义:</p>
<blockquote>
<ul>
<li>若<span class="math inline">\(P(X = x|Y = y) = P(X=x)\)</span>对于任意的<span class="math inline">\(x,y\)</span>均成立 <span class="math inline">\(\Rightarrow\)</span> 变量<span class="math inline">\(X\)</span>与<span class="math inline">\(Y\)</span>相互独立</li>
<li>若<span class="math inline">\(P(X,Y) = P(X|Y)P(Y) = P(X)P(Y)\)</span> <span class="math inline">\(\Rightarrow\)</span> 变量<span class="math inline">\(X\)</span>与<span class="math inline">\(Y\)</span>相互独立</li>
</ul>
</blockquote>
<p>若问题来源中的<span class="math inline">\(n\)</span>个变量相互独立，则联合分布: <span class="math display">\[
    P(X_1,X_2,\dots,X_n) = P(X_1) P(X_2) \dots P(X_n)
\]</span> 此时仅需<span class="math inline">\(O(n)\)</span>个变量来对联合分布进行描述，参数花销大大减少，但在实际中，独立性是一个非常强的假设，往往不能够得到满足，由此便引出了条件独立性性质。</p>
<h3 id="条件独立性">条件独立性</h3>
<blockquote>
<p>定义： 两个变量<span class="math inline">\(X\)</span>和<span class="math inline">\(Y\)</span>关于变量<span class="math inline">\(Z\)</span>是条件独立的 <span class="math inline">\(\Leftrightarrow\)</span> 对于<span class="math inline">\(\forall x,y,z\)</span>,<span class="math inline">\(P(X = x|Y = y, Z = z) = P(X = x|Z = z)\)</span> <span class="math inline">\(\Leftrightarrow\)</span> <span class="math inline">\(P(X = x,Y = y|Z = z) = P(X = x|Z = z) \cdot P(Y=y|Z= z)\)</span></p>
</blockquote>
<p>这里需要特别说明的是条件独立性并不能推出独立性，独立性也并不能推出条件独立性，下面给出两个反例： - 独立性 <span class="math inline">\(\nRightarrow\)</span> 条件独立性: 有两枚正反概率均为<span class="math inline">\(50\%\)</span>的硬币,设事件A为第一枚硬币为正面，事件B为第二枚硬币为正面，事件C为两枚硬币同面，若不考虑事件C，事件A与事件B显然独立;但若事件C已经发生，则此时A与B便不再独立，即A与B关于C不条件独立。 - 条件独立性 <span class="math inline">\(\nRightarrow\)</span> 独立性: 有两枚硬币，一枚正面概率为<span class="math inline">\(90\%\)</span>,另一枚反面概率为<span class="math inline">\(90\%\)</span>,随机拿出一枚抛掷硬币两次, 事件A表示第一次为正面，事件B表示第二次为正面，事件C表示选择的是第一枚硬币，则抛开事件C，事件A与事件B并不独立(<span class="math inline">\(0.5 = P(B) \neq P(B|A) = 0.9802\)</span>),但若事件C发生，则相当于进行了两次独立重复实验，此时事件A与事件B独立。</p>
<p>条件独立性符号表示： - <span class="math inline">\(Ind(X;Y|Z)\)</span> or <span class="math inline">\((X \perp Y|Z)\)</span></p>
<h3 id="条件参数化">条件参数化</h3>
<p>在了解了变量条件独立性后，下面以一个例子分析如何利用变量之间的条件独立性性质来减少描述联合分布所需的参数。</p>
<div data-align="center">
<figure>
<img src="https://raw.githubusercontent.com/xuejy19/Images/master/Ind.png" alt="栗子" /><figcaption>栗子</figcaption>
</figure>
</div>
<p>图中大写字母均代表随机变量，含义如下： - <span class="math inline">\(D:\)</span>课程难度，<span class="math inline">\(Val(D) = \{d^0, d^1\}\)</span> - <span class="math inline">\(I:\)</span>智力，<span class="math inline">\(Val(I) = \{i^0,i^1\}\)</span> - <span class="math inline">\(S:\)</span>SAT分数，<span class="math inline">\(Val(S) = \{s^0,s^1\}\)</span> - <span class="math inline">\(G:\)</span>课程成绩，<span class="math inline">\(Val(G) = \{g^0,g^1, g^2\}\)</span> - <span class="math inline">\(L:\)</span>推荐信，<span class="math inline">\(Val(L) = \{l^0,l^1\}\)</span></p>
<p>假设<span class="math inline">\(G\)</span>和<span class="math inline">\(S\)</span>关于<span class="math inline">\(I\)</span>条件独立，下面我们分析两种情况的条件参数化</p>
<p>首先以<span class="math inline">\(I\)</span>和<span class="math inline">\(S\)</span>两个变量为例，我们既可以通过联合分布<span class="math inline">\(P(I,S)\)</span>来进行描述，也可以通过<span class="math inline">\(P(I)\)</span>和<span class="math inline">\(P(S|I)\)</span>进行描述，两种描述均是三个独立参数，如下图所示。 <img src="https://raw.githubusercontent.com/xuejy19/Images/master/eg2.png" /></p>
<p>接下来分析<span class="math inline">\(I、D、G\)</span>三个变量的条件参数化，其中<span class="math inline">\(I\)</span>与<span class="math inline">\(D\)</span>相互独立，下面分析两种表示方法所需的参数个数： - 联合分布: <span class="math inline">\(P(I,D,G)\)</span>,所需参数个数为<span class="math inline">\(2 \times 2 \times 3 - 1=11\)</span> - 利用独立性条件: <span class="math inline">\(P(I,D,G) = P(I)P(D)P(G|I,D)\)</span>,所需独立参数为$1+1+22 (3-1) =10 $ <span class="math inline">\(\Rightarrow\)</span> <strong>利用独立性可以减少表示联合分布所需的参数个数</strong></p>
<h3 id="朴素贝叶斯模型">朴素贝叶斯模型</h3>
<p>在这一部分对朴素贝叶斯模型做简要介绍，首先我们有两组变量： - <span class="math inline">\(C\)</span>:类别变量，<span class="math inline">\(C = \{c_1,\dots, c_k\}\)</span> - 观测变量：<span class="math inline">\(X_1,X_2,\dots, X_d\)</span></p>
<p>在朴素贝叶斯模型中，我们的目标便是利用观测变量来对类别进行预测/推理。所有这些变量其实可以看做一个联合概率分布<span class="math inline">\(P(C,X_1,\dots,X_d)\)</span>,然而该概率分布很难计算，所需的参数量是指数级别，因此在朴素贝叶斯模型中有一个很强的假设： &gt; 观测变量彼此之间关于类别变量<span class="math inline">\(C\)</span>是相互独立的，据此联合概率分布便很容易计算： <span class="math display">\[   P(C,X_1,\dots,X_d) = P(X_1,\dots,X_d|C) P(C) = P(X_1|C)\dots P(X_d|C) P(C)  \]</span></p>
<p>这是一个很强的假设，在真实世界中往往不能满足，同时因为没有充分利用变量之间的相关性性质，也造成了计算上的浪费。</p>
<h3 id="贝叶斯网络">贝叶斯网络</h3>
<blockquote>
<p>贝叶斯网路定义：贝叶斯网络，又称信念网络，是一种概率图模型，记做<span class="math inline">\((G,P)\)</span>,借由有向无环图表示一组随机变量及它们之间的条件概率分布。</p>
</blockquote>
<p>贝叶斯网络是一种以有向无环图方式对独立性假设进行编码的手段，在贝叶斯网络中，任意变量与它的非后代变量对于其父节点变量是条件独立的： <span class="math display">\[
    (X_i \perp Nondesc(X_i)|Pa(X_i))
\]</span> <strong>独立性映射(I-Maps):</strong> 记<span class="math inline">\(P\)</span>为一组变量<span class="math inline">\(\mathbf{X}\)</span>的联合概率分布，<span class="math inline">\(I(P)\)</span>为表征<span class="math inline">\(P\)</span>中独立性的集合 &gt; 定义：<span class="math inline">\(G\)</span>为贝叶斯网路，若<span class="math inline">\(I(G) \subset I(P)\)</span>,则称<span class="math inline">\(G\)</span>为<span class="math inline">\(P\)</span>的一个独立映射</p>
<blockquote>
<p>分解定理：若<span class="math inline">\(G\)</span>是<span class="math inline">\(P\)</span>的独立映射 <span class="math inline">\(\Leftrightarrow\)</span> <span class="math inline">\(P(X_1,\dots,X_d) = \prod \limits_{i=1}^d P(X_i|Pa(X_i))\)</span>,通过该分解定理可以将全局的概率分布用局部的条件概率来表示</p>
</blockquote>
<p>下面分析下与联合概率分布相比，贝叶斯网络所需参数,以<span class="math inline">\(n\)</span>个二值变量为例： - 联合概率分布：<span class="math inline">\(2^n\)</span> - 贝叶斯网络(限制各个节点入度为<span class="math inline">\(k\)</span>):<span class="math inline">\(n2^k\)</span></p>
<h3 id="d-separation有向分离">d-Separation(有向分离)</h3>
<p>在前面我们已经讨论了贝叶斯网络<span class="math inline">\(G\)</span>编码了局部独立性假设，现在我们希望能够了解<span class="math inline">\(G\)</span>是否还编码了其他独立性假设，因此我们需要一个方法来挖掘出<span class="math inline">\(G\)</span>中所有的独立性假设<br />
<span class="math inline">\(\Rightarrow\)</span> <strong>d-Separation</strong> 下面分别讨论不分离两种情况： - 不分离 - 两个节点直接相连：此时不存在一个节点使得这两个节点关于该节点条件独立 <span class="math inline">\(e.g. X \rightarrow Y\)</span> - 两个节点间接相连：head2tail,head2head,tail2tail - 一般情况：对于图<span class="math inline">\(G\)</span>中的路径<span class="math inline">\(X_1 \leftrightarrow \dots \leftrightarrow X_n\)</span>关于节点集合<span class="math inline">\(E\)</span>是激活的如果 - 对于每个V状结构<span class="math inline">\(X_{i-1} \rightarrow X_i \leftarrow X_{i+1},X_i\)</span>或者其后代 在集合<span class="math inline">\(E\)</span>中 - 路径上的点不在集合<span class="math inline">\(E\)</span>中 - 分离：在贝叶斯网络<span class="math inline">\(G\)</span>中，<span class="math inline">\(X\)</span>和<span class="math inline">\(Y\)</span>在给定<span class="math inline">\(Z\)</span>条件下是d分离的<span class="math inline">\(d-sep_G(X;Y|Z)\)</span>,如果<strong>不存在</strong>从集合<span class="math inline">\(X\)</span>到<span class="math inline">\(Y\)</span>的激活路径</p>
<p>由此可以通过d分离来找到贝叶斯网络中所有独立性关系： <span class="math display">\[
    I(G) = \{ (\mathbf{X \perp Y |Z}): d-sep_G(\mathbf{X;Y|Z})\}
\]</span> 简单总结而言，如果两个节点之间的路径被阻隔，则需要满足以下两个条件其一： - 若存在V形结构，则冲撞点及其后代不能出现在条件集中 - 有路径中的点(非碰撞点)出现在条件集合中</p>
<h3 id="forward-vs-backward">Forward VS Backward</h3>
<p>直观上感觉，贝叶斯网络的反向过程(给出结果，推理原因)要比前向过程困难不少，这是因为子节点会激活其父母之间的路径，而这种相关性会进一步向上传播。 <strong>最小独立映射</strong>：对于一个图<span class="math inline">\(G\)</span>和一个分布<span class="math inline">\(P\)</span>，我们在图中哪怕仅仅去掉一个边都会导致<span class="math inline">\(G\)</span>不再是<span class="math inline">\(P\)</span>的一个独立映射，则称<span class="math inline">\(G\)</span>是最小独立映射。 <strong>最佳映射</strong>：对于一个图<span class="math inline">\(G\)</span>和一个分布<span class="math inline">\(P\)</span>，若有<span class="math inline">\(I(P) = I(G)\)</span>，则称图<span class="math inline">\(G\)</span>是<span class="math inline">\(P\)</span>的一个最佳映射</p>
]]></content>
      <categories>
        <category>统计学习</category>
      </categories>
  </entry>
  <entry>
    <title>我与历史之缘</title>
    <url>/2020/01/12/blog/</url>
    <content><![CDATA[<blockquote>
<ul>
<li>初识历史</li>
<li>求学之路</li>
<li>心得体会 # <font size =5>初识历史</font>  我开始接触历史应该是从我小学时开始，<a id="more"></a>我的小学时光是在农村度过的，那时候也没有什么娱乐活动，家里有两本老版的三国演义，阅读三国演义便成为了我儿时娱乐活动中重要的一部分。那时候在我的脑海里，蜀国是正义的，为汉室存续而立；曹操是奸贼，挟天子以令诸侯，欲行篡位之事；孙权则是一代枭雄，在我眼里属于中立角色。那时候的我特别喜欢看第一部，因为第一部进本都是蜀迅速发展的故事，而像第二部后面我基本不太会看，关羽、张飞被害，刘备举兵伐吴，陆逊火烧连营，刘备白帝城托孤，这一段我真的看得好气啊，气那些卑鄙小人，气刘备不听诸葛之言，小不忍则乱大谋。到了后面诸葛亮第二次起兵伐魏，无奈内有佞臣，在形势大好之时将诸葛亮召回，真是令我扼腕叹息，叹那“扶不起的阿斗”，为孔明不值。五丈原诸葛镶星那一段简直看的我快要哭出来了，大骂魏延，为孔明叹息。  虽然后面随着年龄的增长，我也了解到三国演义只是一本小说，其中有很多东西是虚构的，也有很多东西是经过夸张渲染的，但这本书无疑为我打开了一扇门，让我感受到了历史的魅力。小学时期我能够接触的其他历史则是我姐上学时留下来的一些历史教科书，像其他物理、数学、化学我是根本看不懂，也没什么兴趣看，唯独历史教科书可以一读，这或许也正是历史的魅力所在。 # <font size =5>求学之路</font>  在中学时期，我开始正式上历史课，基本就是框架则是一条纵线与一条横线：纵线则是按照时间顺序，从原始人开始，到夏商西周、春秋战国，然后到大一统的秦，中间还有若干朝代，最后则会讲到改革开放后的中国；同时还有着一条横线，则是讲在每一个历史阶段的政治制度、经济制度、文化，同时介绍一些推动了历史发展的人物。这一纵一横共同构成了我们看到的历史的面貌，那时候我开始觉得历史有些枯燥了，要背好多东西，历史那时候在我眼里变成了各种朝代、制度、人物的简单堆砌，变得毫无生气。那时候对于建国以后的那些历史我是最不感兴趣的，究其原因我觉得是多样的，一方面我觉得这段历史离我们太近，本着“得不到的永远在骚动，被偏爱的都有恃无恐”的态度，认为这段历没有古代历史有意思；同时也是因为我需要背的会议太多，那段历史变成了很多会议的简单堆砌，这就让这段历史更加索然无味。初中时候我还是主动去阅读了一些课外历史书籍的，主要是为了和班上同学争论，那时候我也尝试想去啃一些被奉为正史的一些书，但我记得那时候的我是被那晦涩难懂的文言文劝退了。  高中的时候，我则阅读了一本对我影响挺大的书籍———当年明月的《明朝那些事》，我记得那时候我真的是特别迷这本书，拿到这本书的那天我硬是坚持看到了凌晨三点，才恋恋不舍地把这本书从手中拿开。那是我第一次认识到原来历史也可以如此有趣，历史人物也可以如此鲜活生动。这本书对我产生了挺大的影响，我记得当年明月曾说过历史本来就应该是很有趣的。直到现在，虽然我已经建立起了基本的历史框架，但这其中最鲜活，我最愿意谈的还是明朝那段历史。同时在这段时期我也认识到，历史之所以会变得枯燥，则是因为整个都已经变成了以年份为单位的重大事件的简单累计，这样固然可以用最小的篇幅来传达最重要的信息，但我认为这样恰恰是丢掉了历史最重要的部分———连续性。一个单纯的事件是无聊的，但是事件背后的那些人那些事是生动鲜活的，而能够给人以触动、带给人以启发的恰恰是这些事件背后的故事，单纯的某年某月发生何事是给人带不来任何触动的。 # <font size =5>心得体会</font>  现在回到一个关键性的问题：人为什么要学历史？这个问题我觉得可能就和人为什么要学哲学一样，让很多人感到困惑。但其实在我看来，人是社会性的，都是在与形形色色的人交往中不断构建现实中的自己，历史则为我们提供了这样一个平台，让你可以跨越你所处的层次，跨越你所处的时代，与处在不同时代、不同阶层的人对话，从他们身上学到经验与教训。历史为什么是有趣的，因为人与人之间构成的故事本身就是有趣的，我有时候真的感觉自己很幸运，可以处在这样一个历史时期，回头望去，有希腊文明、有诸子百家、有儒释道、有众多大师，同时处在后工业革命与信息革命时代的我们可以以一个全新的视角去审视过去的这些东西，从历史中学习，从历史中获得启发，读书最快乐的莫过于发现你曾经思考过的问题很多大师也曾经思考过，他们的答案往往会让你有眼前一亮之感。</li>
</ul>
</blockquote>
]]></content>
      <categories>
        <category>闲谈</category>
      </categories>
  </entry>
  <entry>
    <title>logistic回归与最大熵模型</title>
    <url>/2020/08/23/logistic-hui-gui-yu-zui-da-shang-mo-xing/</url>
    <content><![CDATA[<p>在学习李航老师《统计学习》条件随机场章节时，对于学习算法感到有些陌生，后来发现在书中第六章“logistic回归与最大熵模型”有过一些介绍，因此本章节便总结一下相关知识，其中logistic回归模型做简要介绍，重点放在最大熵模型的学习算法上，本文按照如下结构组织： - logistic回归 - 最大熵模型 <a id="more"></a> ### logistic回归 #### logistic分布 首先介绍下logistic分布： &gt; <strong>logistic分布</strong>:设<span class="math inline">\(X\)</span>是连续变量，<span class="math inline">\(X\)</span>服从logistic分布是指<span class="math inline">\(X\)</span>具有下列分布函数和密度函数： &gt; <span class="math display">\[
    \begin{aligned}
        F(x) &amp;= P(X \leq x) = \frac{1}{1 + e^{-\frac{x-\mu}{\gamma}}} \\
        f(x) &amp;= F&#39;(x) = \frac{e^{-\frac{x-\mu}{\gamma}}}{\gamma(1+e^{-\frac{x-\mu}{\gamma}})^2}
    \end{aligned}
&gt; \]</span> &gt; 式中，<span class="math inline">\(\mu\)</span>为位置参数，<span class="math inline">\(\gamma&gt;0\)</span>为形状参数</p>
<p>对于logistic分布，其概率密度函数和分布函数如下图所示： <img src="https://raw.githubusercontent.com/xuejy19/xuejy19.github.io/source/Img/logistic.jpeg" alt="logistic" /> 分布函数的图形是一条<span class="math inline">\(S\)</span>形曲线，该曲线以点<span class="math inline">\((\mu,\frac{1}{2})\)</span>为中心对称，即满足: <span class="math display">\[
    F(-x+\mu) - \frac{1}{2} = -F(x+\mu) +\frac{1}{2}
\]</span> 曲线在中心增长速度较快，在两端增长速度慢，形状参数<span class="math inline">\(\gamma\)</span>的值越小，曲线在中心附近增长得越快。 #### 二项logistic回归模型 二项logistic回归模型是一种二分类模型，由条件概率分布<span class="math inline">\(P(Y|X)\)</span>表示，形式为参数化的logistic分布，随机变量<span class="math inline">\(X\)</span>的取值为实数，随机变量<span class="math inline">\(Y\)</span>取值为1或0，下面给出logistic回归模型定义： &gt; <strong>logistic回归模型</strong>：二项logistic回归模型是如下条件概率分布： &gt; <span class="math display">\[
    \begin{aligned}
        P(Y=1|x) &amp;= \frac{exp(w \cdot x + b)}{1 + exp(w \cdot x + b)} \\
        P(Y=0|x) &amp;= \frac{1}{1 + exp(w \cdot x + b)} 
    \end{aligned}
&gt; \]</span> &gt; 其中，<span class="math inline">\(x \in R^n\)</span>是输入，<span class="math inline">\(Y \in \{0,1\}\)</span>是输出，<span class="math inline">\(w \in R^n,b \in R\)</span>是参数。</p>
<p>若我们已经有了logistic模型，则判断样本点<span class="math inline">\(x\)</span>属于哪一类只需要比较两个概率值的大小，下面给出对数几率的定义: &gt; <strong>对数几率</strong>：一个事件的几率是指该事件发生的概率与该事件不发生的概率的比值，如果一个事件发生的概率是<span class="math inline">\(p\)</span>,那么该事件的几率是<span class="math inline">\(\frac{p}{1-p}\)</span>,该事件的对数几率或logit函数是: &gt; <span class="math display">\[
    logit(p) = log \frac{p}{1-p}
&gt; \]</span></p>
<p>对于logistic回归而言，事件<span class="math inline">\((Y=1|X)\)</span>的对数几率为： <span class="math display">\[
    log \frac{P(Y=1|x)}{1 - P(Y=1|x)} = w \cdot x
\]</span> 这说明，logistic回归模型可以看做是用线性回归模型去逼近真实标记的对数几率。因此logistic回归模型也被称为对数几率回归，可以看作是经过了如下的映射环节，将<span class="math inline">\(R^n\)</span>空间中的<span class="math inline">\(x\)</span>映射到了其标记的概率： <span class="math display">\[
    x \in R^n  \rightarrow w \cdot x \in R \rightarrow P(Y=1|x) \in [0,1]
\]</span></p>
<h4 id="模型参数估计">模型参数估计</h4>
<p>对于logistic回归模型，需要估计的参数是<span class="math inline">\(w,b\)</span>,可以考虑将它们合并为一个<span class="math inline">\(n+1\)</span>维向量: <span class="math display">\[
    w = [w;b]
\]</span> 同时，给输入向量加一维，表示为: <span class="math display">\[
    x = [x;1]
\]</span> 这样logistic回归模型就可以表示成更加紧凑的形式: <span class="math display">\[
    \begin{aligned}
        P(Y=1|x) &amp;=  \frac{exp(w \cdot x) }{1+exp(w \cdot x)} \\
        P(Y=0|x) &amp;=  \frac{1}{1 + exp(w \cdot x)}
    \end{aligned}
\]</span> logistic模型参数学习属于有监督的模型学习问题范畴，利用极大似然法就可以解决，设： <span class="math display">\[
    P(Y=1|x) = \pi(x), \quad P(Y=0|x) = 1- \pi(x)
\]</span> 似然函数可以写成: <span class="math display">\[
    \prod_{i=1}^N [\pi(x_i)]^{y_i} [1 - \pi(x_i)]^{1-y_i}
\]</span> 对数似然函数为: <span class="math display">\[
    \begin{aligned}
        L(w) &amp;= \sum_{i=1}^N [y_i log \pi(x_i) + (1-y_i)log(1-\pi(x_i))] \\
            &amp;= \sum_{i=1}^N [y_i log \frac{\pi(x_i)}{1 - \pi(x_i)} + log(1-\pi(x_i))] \\
            &amp;= \sum_{i=1}^N [y_i(w \cdot x) - log(1 + exp(w \cdot x))]
    \end{aligned}
\]</span> 对<span class="math inline">\(L(w)\)</span>求极大值，得到<span class="math inline">\(w\)</span>的估计值，常用梯度下降法及拟牛顿法求解(后面专开一章讲解)。 #### 多项logistic回归 上面介绍的是二项logistic模型，适用于二分类，可以将其推广为多项logistic分类模型，用于多类分类。假设离散型随机变量<span class="math inline">\(Y\)</span>的取值集合是<span class="math inline">\(\{1,2,\dots,K\}\)</span>,那么多项logistic回归的模型是: <span class="math display">\[
    \begin{aligned}
        P(Y=k|x) &amp;= \frac{\exp(w_k \cdot x)}{1 + \sum_{k=1}^{K-1} \exp(w_k \cdot x)} \\
        P(Y=K|x) &amp;= \frac{1}{1 + \sum_{k=1}^{K-1} \exp(w_k \cdot x)}
    \end{aligned}
\]</span> 这里<span class="math inline">\(x \in R^{n+1},w_k \in R^{n+1}\)</span>。二项logistic回归的参数估计法也可以推广到多项logistic回归</p>
<h3 id="最大熵模型">最大熵模型</h3>
<p>最大熵模型，是由最大熵原理推导实现。本部分按照以下结构组织: - 最大熵原理 - 最大熵模型推导 - 最大熵模型学习</p>
<h4 id="最大熵原理">最大熵原理</h4>
<p>最大熵原理是概率模型学习的一个准则，最大熵原理认为： &gt; 学习概率模型时，在所有可能的概率模型中，熵最大的模型是最好的模型。通常用约束条件来确定概率模型的集合，所以，最大熵原理也可以表述为在满足约束条件的模型集合中选取熵最大的模型。</p>
<p>我们首先来复习下信息论中关于熵的定义： &gt; 在信息论与概率统计中，熵是表示<strong>随机变量不确定性</strong>的度量，设<span class="math inline">\(X\)</span>是一个取有限个值的离散随机变量，其概率分布为: &gt; <span class="math display">\[
    P(X = x_i) = p_i, \quad i =1,2,\dots,n
&gt; \]</span> 则随机变量<span class="math inline">\(X\)</span>的熵定义为: <span class="math display">\[
    H(X) = - \sum_{i=1}^n p_i \log p_i 
\]</span></p>
<p>若<span class="math inline">\(p_i = 0\)</span>,则定义<span class="math inline">\(0\log 0 =0\)</span>,式中的对数一般取以2为底或者以<span class="math inline">\(e\)</span>为底，这时熵的单位被称作比特或纳特，由定义可知，熵只依赖于<span class="math inline">\(X\)</span>的分布，而与<span class="math inline">\(X\)</span>的取值无关，因此也可将<span class="math inline">\(X\)</span>的熵记作<span class="math inline">\(H(p)\)</span>,熵越大，随机变量的不确定性越大。因为<span class="math inline">\(p_i \leq 0\)</span>,由<span class="math inline">\(H(p)\)</span>函数性质(凹函数，应用拉格朗日乘子法可求最大值)可知: <span class="math display">\[
    0 \leq H(p) \leq \log n
\]</span> 当且仅当<span class="math inline">\(X\)</span>是均匀分布时右侧等号成立,也就是说，当<span class="math inline">\(X\)</span>服从均匀分布时，熵最大，该随机变量的不确定性最大。下面给出条件熵的定义: &gt; 设有随机变量<span class="math inline">\((X,Y)\)</span>,其联合概率分布为： <span class="math display">\[
    P(X = x_i,Y = y_i) = p_{ij}\quad i = 1,2,\dots,n;\quad j = 1,2,\dots,m
\]</span> 条件熵<span class="math inline">\(H(Y|X)\)</span>表示在已知随机变量<span class="math inline">\(X\)</span>条件下随机变量<span class="math inline">\(Y\)</span>的不确定性。随机变量<span class="math inline">\(X\)</span>给定的条件下随机变量<span class="math inline">\(Y\)</span>的条件熵<span class="math inline">\(H(Y|X)\)</span>,定义为<span class="math inline">\(X\)</span>给定条件下<span class="math inline">\(Y\)</span>的条件概率分布的熵对<span class="math inline">\(X\)</span>的数学期望： <span class="math display">\[
    H(Y|X) = \sum_{i=1}^n p_i H(Y|X = x_i)
\]</span> 当熵和条件熵中的概率由数据估计得到时，所对应的熵与条件熵分别称为经验熵和经验条件熵</p>
<p>直观的，最大熵原理认为要选择的概率模型首先必须满足已有的事实，即约束条件。在没有更多信息情况下，那些不确定的部分都是“等可能的”,最大熵原理通过熵的最大化来表示等可能性。下面通过一个简单的例子来介绍最大熵原理： &gt; 🌰：假设随机变量<span class="math inline">\(X\)</span>有五个取值<span class="math inline">\(\{A,B,C,D,E \}\)</span>,要估计取各个值的概率<span class="math inline">\(P(A),P(B),P(C),P(D),P(E)\)</span> &gt; - 不提供任何额外约束，则只有这些概率的固有约束 &gt; <span class="math display">\[
    P(A) + P(B) + P(C) + P(D) + P(E) = 1
&gt; \]</span> &gt; 因为没有任何额外的信息，则根据最大熵原理，只能够认为该概率分布为均匀分布： &gt; <span class="math display">\[
    P(A) = P(B) = P(C) = P(D) = P(E) = \frac{1}{5}
&gt; \]</span> &gt; - 从一些先验知识中得到了一些对概率值的约束条件，如<span class="math inline">\(P(A) + P(B) = \frac{3}{10}\)</span>,满足约束的概率分布仍旧有无穷多个，在缺少其他信息的情况下，可以认为<span class="math inline">\(A\)</span>和<span class="math inline">\(B\)</span>是等概率的，<span class="math inline">\(C,D,E\)</span>等概率。</p>
<p>实际上，最大熵原理就是为我们提供了一种选择最优概率模型的准则(保守)</p>
<h4 id="最大熵模型的定义">最大熵模型的定义</h4>
<p>最大熵原理是统计学习的一般原理，将它应用到分类得到最大熵模型。 假设分类模型是一个条件概率分布<span class="math inline">\(P(Y|X)\)</span>,<span class="math inline">\(X \in \mathcal{X} \subseteq R^n\)</span>表示输入，<span class="math inline">\(Y \in \mathcal{Y}\)</span>表示输出，<span class="math inline">\(\mathcal{X}\)</span>和<span class="math inline">\(\mathcal{Y}\)</span>分别是输入和输出的集合，这个模型表示的是对于给定的输入<span class="math inline">\(X\)</span>，以条件概率<span class="math inline">\(P(Y|X)\)</span>输出<span class="math inline">\(Y\)</span>。 给定一个训练数据集: <span class="math display">\[
    T = \{  (x_1,y_1), (x_2,y_2), \dots, (x_N,y_N)\}
\]</span> 学习的目标是应用最大熵模型选择最好的分类模型。首先考虑模型应该满足的条件，即该数据集为模型添加了哪些约束，给定训练数据集后，我们可以确定联合分布<span class="math inline">\(P(X,Y)\)</span>的经验分布和边缘分布<span class="math inline">\(P(X)\)</span>的经验分布，分别以<span class="math inline">\(\tilde{P}(X,Y)\)</span>和<span class="math inline">\(\tilde{P}(X)\)</span>表示: <span class="math display">\[
    \begin{aligned}
        \tilde{P}(X= x,Y=y) &amp;= \frac{\nu(X = x,Y = y)}{N} \\
        \tilde{P}(X = x) &amp;= \frac{\nu(X = x)}{N} 
    \end{aligned}
\]</span> 式中<span class="math inline">\(\nu(\cdot)\)</span>是频数统计函数，<span class="math inline">\(N\)</span>表示训练样本容量。用特征函数<span class="math inline">\(f(x,y)\)</span>描述输入<span class="math inline">\(x\)</span>和输出<span class="math inline">\(y\)</span>之间的某一个事实，其定义是: <span class="math display">\[
    f(x,y) = \begin{cases}
        1,  &amp;x和y满足某一事实 \\
        0,  &amp;else 
    \end{cases}
\]</span> 特征函数<span class="math inline">\(f(x,y)\)</span>关于经验分布<span class="math inline">\(\tilde{P}(X,Y)\)</span>的期望值，用<span class="math inline">\(E_{\tilde{P}}(f)\)</span>表示： <span class="math display">\[
    E_{\tilde{P}}(f) = \sum_{x,y} \tilde{P}(x,y) f(x,y)
\]</span> 特征函数<span class="math inline">\(f(x,y)\)</span>关于模型<span class="math inline">\(P(Y|X)\)</span>i与经验分布<span class="math inline">\(\tilde{P}(X)\)</span>的期望值，用<span class="math inline">\(E_P(f)\)</span>表示： <span class="math display">\[
    E_P(f) = \sum_{x,y} \tilde{P}(x) P(y|x) f(x,y)
\]</span> 如果模型能够获取训练数据中的信息，那么就可以假设这两个期望值相等，即： <span class="math display">\[
    E_P(f) = E_{\tilde{P}}(f)
\]</span> 将这作为模型学习的约束条件，假如有<span class="math inline">\(n\)</span>个特征函数<span class="math inline">\(f_i(x,y),i=1,2,\dots,n\)</span>,那么就有<span class="math inline">\(n\)</span>个约束条件，下面给出最大熵模型的定义: &gt; 最大熵模型:假设满足所有约束条件的模型集合为: &gt; <span class="math display">\[
    C \equiv \{ P \in \mathcal{P} |E_P(f_i) = E_{\tilde{P}}(f_i), i = 1,2,\dots,n\} 
&gt; \]</span> &gt; 定义在条件概率分布<span class="math inline">\(P(Y|X)\)</span>上的条件熵为： &gt; <span class="math display">\[
    H(P) = -\sum_{x,y}  \tilde{P}(x)P(y|x) log P(y|x)
&gt; \]</span> &gt; 则模型集合<span class="math inline">\(C\)</span>中条件熵<span class="math inline">\(H(P)\)</span>最大的模型称为最大熵模型</p>
<p>从定义可以看出，最大熵模型是基于这样一种思想: &gt; 当我们需要对一个事件(变量)的概率分布进行预测时，最大熵原理告诉我们所有的预测应当满足全部已知的条件(约束)，而对未知的情况不要做任何主观假设，保留模型最大的不确定性。</p>
<h4 id="最大熵模型的学习">最大熵模型的学习</h4>
<p>最大熵模型的学习过程就是求解最大熵模型的过程。最大熵模型学习可以形式化为约束最优化问题。 对于给定的训练数据集<span class="math inline">\(T = \{ (x_1,y_1), (x_2,y_2),\dots, (x_N,y_N)\}\)</span>以及特征函数<span class="math inline">\(f_i(x,y),i=1,2,\dots,n\)</span>,最大熵模型的学习等价于求解最优化问题: <span class="math display">\[
    \begin{aligned}
        &amp;\max_{P \in C} \quad H(P) = -\sum_{x,y}  \tilde{P}(x)P(y|x) log P(y|x) \\
        &amp;s.t. \quad E_P(f_i) = E_{\tilde{P}}(f_i),\quad i =1,2,\dots,n \\  
        &amp; \quad \quad \sum_y P(y|x) =1
    \end{aligned}
\]</span> 按照最优化问题的习惯，将求解最大值问题改为等价的求最小值问题: <span class="math display">\[
    \begin{aligned}
        &amp;\min_{P \in C} \quad -H(P) = \sum_{x,y}  \tilde{P}(x)P(y|x) log P(y|x) \\
        &amp;s.t. \quad E_P(f_i) - E_{\tilde{P}}(f_i) = 0,\quad i =1,2,\dots,n \\  
        &amp; \quad \quad \sum_y P(y|x) =1
    \end{aligned}
\]</span> <strong>该优化问题按照李航老师《统计学习》中的说法是满足强对偶性的，但书中只是一带而过，并没有进行详细说明，此处留坑，后面的推导就先默认该优化问题满足强对偶性，后面我搞懂了再来填坑</strong> <em><strong> </strong>这个问题我又思考了一下，自变量是一个分布看似是一个很虚的东西，但它也不过是一个函数，若<span class="math inline">\(X,Y\)</span>都是离散取值变量，那么这个条件概率分布只是一个有限长度的向量，而若<span class="math inline">\(X,Y\)</span>取值连续，则该条件概率密度函数可以看作一个无限长的向量，这样来看原始待优化函数其实就是<span class="math inline">\(\boldsymbol{x} \cdot \log \boldsymbol{x}\)</span>,这显然是一个凸函数，而约束条件也不过是待优化参数的线性组合，这显然是一个凸优化问题<strong> </strong></em> 该优化问题共有<span class="math inline">\(n+1\)</span>个等式约束，因此考虑引入拉格朗日乘子<span class="math inline">\(w_0,w_1,\dots,w_n\)</span>,定义拉格朗日函数<span class="math inline">\(L(P,w)\)</span>: <span class="math display">\[
    \begin{aligned}
        L(P,w) &amp;= -H(P) + w_0 (1 - \sum_y P(y|x)) + \sum_{i=1}^n w_i (E_{\tilde{P}}(f_i) - E_P(f_i)) \\
        &amp;= \sum_{x,y}  \tilde{P}(x)P(y|x) log P(y|x)  + w_0 (1 - \sum_y P(y|x)) \\
        &amp;+ \sum_{i=1}^n w_i(\sum_{x,y} \tilde{P}(x,y) f(x,y) - \sum_{x,y} \tilde{P}(x) P(y|x) f(x,y))
    \end{aligned}
\]</span> 原始优化问题的对偶问题写作： <span class="math display">\[
    \max_{w} \min_{P} L(P,w)
\]</span> 首先求解对偶函数<span class="math inline">\(\phi(w)\)</span>,因为<span class="math inline">\(L(P,w)\)</span>关于<span class="math inline">\(P\)</span>是凸函数，因此直接通过求导令导数为0即可： <span class="math display">\[
    \begin{aligned}
        \frac{ \partial L(P,w)}{\partial P(y|x)} &amp;=  log P(y|x) +1 - w_0 - \sum_{i=1}^n w_i f_i (x,y) = 0 
    \end{aligned}
\]</span> 在求导这一部分我不太认同李航老师书中的写法，因为<span class="math inline">\(P(y|x)\)</span>本身可以看作是一个向量，因此求导相当于对该向量某个分量求导。由此可得<span class="math inline">\(P(y|x)\)</span>的解析表达式: <span class="math display">\[
    P(y|x) = \exp(\sum_{i=1}^n w_i f_i(x,y) + w_0 -1) = \frac{\exp(\sum_{i=1}^n w_i f_i(x,y))}{\exp(1-w_0)}
\]</span> 又因为有概率密度函数的固有约束<span class="math inline">\(\sum_y P(y|x) = 1\)</span>,可得: <span class="math display">\[\exp(1-w_0) = \sum_y \exp(\sum_{i=1}^n w_i f_i(x,y))\]</span> 因此，<span class="math inline">\(P_w(y|x)\)</span>的解析表达式写作: <span class="math display">\[
    \begin{aligned}
        P_w(y|x) &amp;= \frac{1}{Z_w(x)} \exp(\sum_{i=1}^n w_i f_i(x,y)) \\
        Z_w(x) &amp;= \sum_y \exp(\sum_{i=1}^n w_i f_i(x,y))
    \end{aligned}
\]</span> 至此，我们得到了对偶函数<span class="math inline">\(\phi(w)\)</span>表达式： <span class="math display">\[
    \begin{aligned}
        \phi(w) &amp;= \sum_{x,y} \tilde{P}(x,y) \sum_{i=1}^n w_i f_i(x,y) + \sum_{x,y} \tilde{P}(x)P_w(y|x)(\log P_w(y|x) - \sum_{i=1}^n w_i f_i(x,y)) \\
        &amp;= \sum_{x,y} \tilde{P}(x,y) \sum_{i=1}^n w_i f_i(x,y) - \sum_{x,y} \tilde{P}(x)P_w(y|x) \log Z_w(x) \\
        &amp;= \sum_{x,y} \tilde{P}(x,y) \sum_{i=1}^n w_i f_i(x,y) - \sum_x \tilde{P}(x) \log Z_w(x)
    \end{aligned}
\]</span> 因此接下来便是求解对偶优化问题: <span class="math display">\[
    \max \phi(w)
\]</span> 得到<span class="math inline">\(w^\ast\)</span>,然后回代<span class="math inline">\(P_w(y|x)\)</span>,得到最大熵模型表达式: <span class="math display">\[
    P_{w^\ast}(y|x)
\]</span> #### 极大似然估计 这一部分旨在证明一条性质: &gt; 最大熵模型求解过程中，对偶函数的极大化等价于最大熵模型的极大似然估计</p>
<p>首先写出对数似然函数的形式: <span class="math display">\[
    \begin{aligned}
         L_{\tilde{P}}(P_w) &amp;= \log \prod_{x,y} P(y|x)^{\tilde{P}(x,y)} = \sum_{x,y} \tilde{P}(x,y) \log P(y|x) \\
         &amp;= \sum_{x,y} \tilde{P}(x,y) \sum_{i=1}^n w_i f_i(x,)  - \sum_{x,y} \tilde{P}(x,y) \log Z_w(x) \\
         &amp;= \sum_{x,y} \tilde{P}(x,y) \sum_{i=1}^n w_i f_i(x,y)  - \sum_{x} \tilde{P}(x) \log Z_w(x)
    \end{aligned}
\]</span> 与对偶函数<span class="math inline">\(\phi(w)\)</span>一致，这说明对对偶函数求最大等价于求最大熵模型的极大似然估计。 最大熵模型与logistic回归模型有类似的形式，它们又称为对数线性模型。模型学习就是对模型进行极大似然估计或者正则化的极大似然估计</p>
]]></content>
      <categories>
        <category>统计学习</category>
      </categories>
      <tags>
        <tag>logistic回归, 最大熵模型, 优化学习</tag>
      </tags>
  </entry>
  <entry>
    <title>python数据处理之时间变换</title>
    <url>/2020/09/19/python-shu-ju-chu-li-zhi-shi-jian-bian-huan/</url>
    <content><![CDATA[<p>本文将就<code>python</code>中对时间标识的处理做一点讲解，在进行数据分析时，有些时候我们需要对时间序列数据进行处理，比如像下面这样一组数据<a id="more"></a>:</p>
<table>
<thead>
<tr class="header">
<th style="text-align: center;">Time</th>
<th style="text-align: center;">feature1</th>
<th style="text-align: center;">...</th>
<th style="text-align: center;">feature n</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td style="text-align: center;">2013年-01月-01日: 00时00分00秒</td>
<td style="text-align: center;">...</td>
<td style="text-align: center;">...</td>
<td style="text-align: center;">...</td>
</tr>
<tr class="even">
<td style="text-align: center;">2013年-01月-01日: 02时00分00秒</td>
<td style="text-align: center;">...</td>
<td style="text-align: center;">...</td>
<td style="text-align: center;">...</td>
</tr>
<tr class="odd">
<td style="text-align: center;">...</td>
<td style="text-align: center;">...</td>
<td style="text-align: center;">...</td>
<td style="text-align: center;">...</td>
</tr>
<tr class="even">
<td style="text-align: center;">2015年-01月-01日: 00时00分00秒</td>
<td style="text-align: center;">...</td>
<td style="text-align: center;">...</td>
<td style="text-align: center;">...</td>
</tr>
</tbody>
</table>
<p>对于这样一段时序数据，采样间隔2小时。当我们拿到这组数据之后，首先要考虑的问题就是这组数据有没有缺失，而判断数据记录有无缺失的关键在于其时间序列<code>Time</code>是否连续。我们一般采用<code>pandas.read_csv()</code>函数来进行<code>csv</code>文件中数据的读取，读取得到的第一列是一个字符串形式，通过这些字符串来判断时间是否连续是比较困难的，需要重新写一个函数来对字符串进行处理。但幸运的是，其实已经有函数可以帮我们完成这样一项工作了，下面介绍下本文的关键对象---<code>unix时间戳</code>： &gt; <strong>unix时间戳:</strong> unix时间戳是一种跟踪时间（以秒为单位）的方式。此计数从1970年1月1日UTC的Unix Epoch开始。因此，unix时间戳仅仅是特定日期与Unix纪元之间的秒数。</p>
<p>简而言之，unix时间戳就是一个<code>int</code>型的数据，其值代表了某个时间点距离<code>1997-01-01: 00:00:00</code>的秒数，那么如果我们能够将我们的时间字符串转换成unix时间戳，那么后面的数据拆分、数据缺失定位就更加得心应手了。本文按照以下结构进行组织: - 常见日期时间类型 - 日期格式转换 - 总结</p>
<h3 id="常见日期时间类型">常见日期时间类型</h3>
<p>在进行时间序列数据处理时，常见的日期时间类型大概有以下四种: | 类型| 格式 | 示例 | | :-----:| :---- |:----| | time | 时间格式 | 17:00:00| | date | 日期格式 | 2019-09-19 | | datetime | 日期时间格式 | 2019-09-19: 17:00:00 | | timestamp | 时间戳格式 | 1568912400 |</p>
<p>实际上以上四种类型可以粗分为两个类型: 字符串类型和时间戳格式，时间字符串直接转时间戳格式并不自然，因此在python中实现两者转化还需借助一个中间变量类型<code>struct_time</code>，时间格式变换流程如下: <span class="math display">\[
    str \Leftrightarrow struct \, time \Leftrightarrow stamp 
\]</span> 字符串格式和时间戳格式通过我上面的讲解大家应该可以有一个基本认识，下面重点介绍下<code>struct_time</code>类型，它是一个具有命名元祖接口的对象:可以通过索引和属性名访问值，存在以下值: | index | attribute | values | | :-----:| :----: |:----:| | 0 | tm-year | 年份 | | 1 | tm-mon | 月份range(1,12) | | 2 | tm-mday | 天数range(1,31) | | 3 | tm-hour | 小时range(0,23) | | 4 | tm-min | 分钟range(0,59) | | 5 | tm-sec | 秒数range(0,59) | | 6 | tm-wday | 星期range(0,6) | | 7 | tm-yday | 年中的一天range(1,366) | 而要实现这样的变换，则就要提到<code>python</code>中两个专门与时间打交道的模块<code>time</code>和<code>datetime</code>。</p>
<h3 id="日期和格式转换">日期和格式转换</h3>
<p>这部份将介绍以下<code>time</code>以及<code>datetime</code>中常用的一些方法。 #### <code>time</code>模块 <a href="https://docs.python.org/3/library/time.html"><code>time</code></a>模块是操作系统层面上的， 与<code>os</code>模块类似，主要提供一些系统层面的与时间有关的操作，该模块是其他所有与时间相关模块的基础，我们用到该模块比较多的方法主要有以下几个: - <code>time.time()</code>: 返回当前时间的时间戳</p>
<div class="sourceCode" id="cb1"><pre class="sourceCode python"><code class="sourceCode python"><a class="sourceLine" id="cb1-1" data-line-number="1"><span class="op">&gt;&gt;&gt;</span>: </a>
<a class="sourceLine" id="cb1-2" data-line-number="2"><span class="bu">print</span>(time.time())</a>
<a class="sourceLine" id="cb1-3" data-line-number="3"></a>
<a class="sourceLine" id="cb1-4" data-line-number="4"><span class="op">&gt;&gt;&gt;</span>: </a>
<a class="sourceLine" id="cb1-5" data-line-number="5"><span class="fl">1600502127.7830698</span></a></code></pre></div>
<ul>
<li><code>time.loacltime()</code>: 将一个<code>unix stamp</code>转换为本地时间(东八区)的<code>struct_time</code>类型</li>
</ul>
<div class="sourceCode" id="cb2"><pre class="sourceCode python"><code class="sourceCode python"><a class="sourceLine" id="cb2-1" data-line-number="1"><span class="op">&gt;&gt;&gt;</span>: </a>
<a class="sourceLine" id="cb2-2" data-line-number="2"><span class="bu">print</span>(time.localtime(time.time())) </a>
<a class="sourceLine" id="cb2-3" data-line-number="3"></a>
<a class="sourceLine" id="cb2-4" data-line-number="4"><span class="op">&gt;&gt;&gt;</span>: </a>
<a class="sourceLine" id="cb2-5" data-line-number="5">time.struct_time(tm_year<span class="op">=</span><span class="dv">2020</span>, tm_mon<span class="op">=</span><span class="dv">9</span>, tm_mday<span class="op">=</span><span class="dv">19</span>, tm_hour<span class="op">=</span><span class="dv">16</span>, tm_min<span class="op">=</span><span class="dv">2</span>, tm_sec<span class="op">=</span><span class="dv">55</span>, tm_wday<span class="op">=</span><span class="dv">5</span>, tm_yday<span class="op">=</span><span class="dv">263</span>, tm_isdst<span class="op">=</span><span class="dv">0</span>)</a></code></pre></div>
<ul>
<li><code>time.gmtime()</code>: 将一个<code>unix stamp</code>转换为格林威治时间的<code>struct_time</code>类型</li>
</ul>
<div class="sourceCode" id="cb3"><pre class="sourceCode python"><code class="sourceCode python"><a class="sourceLine" id="cb3-1" data-line-number="1"><span class="op">&gt;&gt;&gt;</span>: </a>
<a class="sourceLine" id="cb3-2" data-line-number="2"><span class="bu">print</span>(time.gmtime(time.time()))</a>
<a class="sourceLine" id="cb3-3" data-line-number="3"></a>
<a class="sourceLine" id="cb3-4" data-line-number="4"><span class="op">&gt;&gt;&gt;</span>: </a>
<a class="sourceLine" id="cb3-5" data-line-number="5">time.struct_time(tm_year<span class="op">=</span><span class="dv">2020</span>, tm_mon<span class="op">=</span><span class="dv">9</span>, tm_mday<span class="op">=</span><span class="dv">19</span>, tm_hour<span class="op">=</span><span class="dv">8</span>, tm_min<span class="op">=</span><span class="dv">5</span>, tm_sec<span class="op">=</span><span class="dv">31</span>, tm_wday<span class="op">=</span><span class="dv">5</span>, tm_yday<span class="op">=</span><span class="dv">263</span>, tm_isdst<span class="op">=</span><span class="dv">0</span>)</a></code></pre></div>
<ul>
<li><code>time.strftime(format, struct_time)</code>: 将一个<code>struct_time</code>类型输出转换为指定格式的字符串<br />
</li>
</ul>
<div class="sourceCode" id="cb4"><pre class="sourceCode python"><code class="sourceCode python"><a class="sourceLine" id="cb4-1" data-line-number="1"><span class="op">&gt;&gt;&gt;</span>: </a>
<a class="sourceLine" id="cb4-2" data-line-number="2"><span class="bu">format</span> <span class="op">=</span> <span class="st">&#39;%Y-%m-</span><span class="sc">%d</span><span class="st">: %H:%M:%S&#39;</span> </a>
<a class="sourceLine" id="cb4-3" data-line-number="3">struct_time <span class="op">=</span> time.localtime(time.time()) </a>
<a class="sourceLine" id="cb4-4" data-line-number="4"><span class="bu">print</span>(time.strftime(<span class="bu">format</span>, struct_time)) </a>
<a class="sourceLine" id="cb4-5" data-line-number="5"></a>
<a class="sourceLine" id="cb4-6" data-line-number="6"><span class="op">&gt;&gt;&gt;</span>: </a>
<a class="sourceLine" id="cb4-7" data-line-number="7"><span class="dv">2020-09-19</span>: <span class="dv">16</span>:<span class="dv">10</span>:<span class="dv">42</span> </a></code></pre></div>
<ul>
<li><code>time.strptime(str, format)</code>: 与<code>strftime</code>作用相反，将一个时间字符串按照<code>format</code>格式解析成<code>struct_time</code>类型</li>
</ul>
<div class="sourceCode" id="cb5"><pre class="sourceCode python"><code class="sourceCode python"><a class="sourceLine" id="cb5-1" data-line-number="1"><span class="op">&gt;&gt;&gt;</span>: </a>
<a class="sourceLine" id="cb5-2" data-line-number="2"><span class="bu">format</span> <span class="op">=</span> <span class="st">&#39;%Y-%m-</span><span class="sc">%d</span><span class="st">: %H:%M:%S&#39;</span>  </a>
<a class="sourceLine" id="cb5-3" data-line-number="3">time_str <span class="op">=</span> <span class="st">&#39;2020-9-19: 16:17:00&#39;</span> </a>
<a class="sourceLine" id="cb5-4" data-line-number="4"><span class="bu">print</span>(time.strptime(time_str, <span class="bu">format</span>))  </a>
<a class="sourceLine" id="cb5-5" data-line-number="5"></a>
<a class="sourceLine" id="cb5-6" data-line-number="6"><span class="op">&gt;&gt;&gt;</span>: </a>
<a class="sourceLine" id="cb5-7" data-line-number="7">time.struct_time(tm_year<span class="op">=</span><span class="dv">2020</span>, tm_mon<span class="op">=</span><span class="dv">9</span>, tm_mday<span class="op">=</span><span class="dv">19</span>, tm_hour<span class="op">=</span><span class="dv">16</span>, tm_min<span class="op">=</span><span class="dv">17</span>, tm_sec<span class="op">=</span><span class="dv">0</span>, tm_wday<span class="op">=</span><span class="dv">5</span>, tm_yday<span class="op">=</span><span class="dv">263</span>, tm_isdst<span class="op">=-</span><span class="dv">1</span>)</a></code></pre></div>
<ul>
<li><code>time.mktime(struct_time)</code>: 将一个<code>struct_time</code>类型数据转化成<code>unxi_stample</code>, 该操作是<code>time.localtime()</code>的反操作，因此其默认输入的<code>struct_time</code>是本地时间</li>
</ul>
<div class="sourceCode" id="cb6"><pre class="sourceCode python"><code class="sourceCode python"><a class="sourceLine" id="cb6-1" data-line-number="1"><span class="op">&gt;&gt;&gt;</span>: </a>
<a class="sourceLine" id="cb6-2" data-line-number="2"><span class="bu">format</span> <span class="op">=</span> <span class="st">&#39;%Y-%m-</span><span class="sc">%d</span><span class="st">: %H:%M:%S&#39;</span>  </a>
<a class="sourceLine" id="cb6-3" data-line-number="3">time_str <span class="op">=</span> <span class="st">&#39;2020-9-19: 16:17:00&#39;</span> </a>
<a class="sourceLine" id="cb6-4" data-line-number="4">time_struct <span class="op">=</span> time.strptime(time_str, <span class="bu">format</span>) </a>
<a class="sourceLine" id="cb6-5" data-line-number="5"><span class="bu">print</span>(time_struct)</a>
<a class="sourceLine" id="cb6-6" data-line-number="6"><span class="bu">print</span>(time.mktime(time_struct)) </a>
<a class="sourceLine" id="cb6-7" data-line-number="7"><span class="bu">print</span>(time.localtime(time.mktime(time_struct)))</a>
<a class="sourceLine" id="cb6-8" data-line-number="8"></a>
<a class="sourceLine" id="cb6-9" data-line-number="9"><span class="op">&gt;&gt;&gt;</span>:</a>
<a class="sourceLine" id="cb6-10" data-line-number="10">time.struct_time(tm_year<span class="op">=</span><span class="dv">2020</span>, tm_mon<span class="op">=</span><span class="dv">9</span>, tm_mday<span class="op">=</span><span class="dv">19</span>, tm_hour<span class="op">=</span><span class="dv">16</span>, tm_min<span class="op">=</span><span class="dv">17</span>, tm_sec<span class="op">=</span><span class="dv">0</span>, tm_wday<span class="op">=</span><span class="dv">5</span>, tm_yday<span class="op">=</span><span class="dv">263</span>, tm_isdst<span class="op">=-</span><span class="dv">1</span>)</a>
<a class="sourceLine" id="cb6-11" data-line-number="11"><span class="fl">1600503420.0</span></a>
<a class="sourceLine" id="cb6-12" data-line-number="12">time.struct_time(tm_year<span class="op">=</span><span class="dv">2020</span>, tm_mon<span class="op">=</span><span class="dv">9</span>, tm_mday<span class="op">=</span><span class="dv">19</span>, tm_hour<span class="op">=</span><span class="dv">16</span>, tm_min<span class="op">=</span><span class="dv">17</span>, tm_sec<span class="op">=</span><span class="dv">0</span>, tm_wday<span class="op">=</span><span class="dv">5</span>, tm_yday<span class="op">=</span><span class="dv">263</span>, tm_isdst<span class="op">=</span><span class="dv">0</span>)</a></code></pre></div>
<p>上面这些方法实际上就已经完全可以实现我们的功能，大致是下面这样一张图:</p>
<figure>
<img src="https://raw.githubusercontent.com/xuejy19/xuejy19.github.io/source/Img/time.png" alt="转换方法" /><figcaption>转换方法</figcaption>
</figure>
<h4 id="datetime模块"><code>datetime</code>模块</h4>
<p><code>datetime</code>模块可以看作是对<code>time</code>模块的一个封装和扩展，上面提到的<code>time</code>中常用方法在<code>datetime</code>中均有对应, 在<code>datetime</code>下主要有以下几个类： - <code>date</code>: 处理与日期相关的数据，属性: year, month, day - <code>time</code>: 处理与某天时间相对应的数据， 属性: hour, minute, second, microsecond, tzinfo - <code>datetime</code>: 日期和时间的组合，属性: year, month, day, hour, minute, second, microsecond, tzinfo - <code>timedelta</code>: 与持续时间计算有关<br />
- <code>tzinfo</code>: 时区信息类</p>
<p><code>datetime</code>类基本已经可以实现上面<code>time</code>模块所能实现的功能，具体对应大家可以去官网<a href="https://docs.python.org/2/library/datetime.html">datetime</a>查看，我后面如果需要用到也会进行总结。</p>
<h4 id="pandas.to_datetime"><code>pandas.to_datetime()</code></h4>
<p>我们读取数据往往需要使用<code>pandas</code>库，得到<code>Dateframe</code>类型的数据，读取之后往往某一列就是时间序列，若用前面的方法来做转换，需要对每一个时间字符串操作，比较麻烦。为了解决该问题，就需要使用<code>pandas.to_datetime()</code>方法，使用该方法可以将变量变为<code>datetime</code>类型，方便后面进行操作。 - 字符串时间转时间戳</p>
<div class="sourceCode" id="cb7"><pre class="sourceCode python"><code class="sourceCode python"><a class="sourceLine" id="cb7-1" data-line-number="1">data.time <span class="op">=</span> pandas.to_datetime(data.time) </a>
<a class="sourceLine" id="cb7-2" data-line-number="2">data.time <span class="op">=</span> data.time.<span class="bu">apply</span>(<span class="kw">lambda</span> x: time.mktime(x.<span class="bu">tuple</span>))</a></code></pre></div>
<ul>
<li>时间戳转字符串</li>
</ul>
<div class="sourceCode" id="cb8"><pre class="sourceCode python"><code class="sourceCode python"><a class="sourceLine" id="cb8-1" data-line-number="1">data.time <span class="op">=</span> pandas.to_datetime(data.time) </a>
<a class="sourceLine" id="cb8-2" data-line-number="2">data.time <span class="op">=</span> data.time.<span class="bu">apply</span>(<span class="kw">lambda</span> x: time.strftime(<span class="bu">format</span>, x.<span class="bu">tuple</span>))</a></code></pre></div>
<h3 id="总结">总结</h3>
<p>在进行数据处理的时候我们往往不能够默认数据是“好数据”，因此就必须通过程序来对数据进行验证，而对于时序数据，时间信息的处理是绕不过去的，掌握了<code>python</code>中常用的对时间处理方法，后面处理时序数据可以更加得心应手。</p>
]]></content>
      <categories>
        <category>python学习</category>
      </categories>
      <tags>
        <tag>编程技能， 时序数据</tag>
      </tags>
  </entry>
  <entry>
    <title>再谈极大似然估计求解</title>
    <url>/2020/08/24/zai-tan-ji-da-si-ran-gu-ji-qiu-jie/</url>
    <content><![CDATA[<p>首先我们思考这样一个问题： &gt; 当我们用最大似然估计进行概率模型参数估计时，为什么基本都是直接求导，一阶导数等于0的点就是我们待求的最优估计?</p>
<p>问到这个地方的时候，可能有一部分人就不知该如何回答了，因为一阶导数为0显然不是函数最大值点的充要条件，但我相信肯定有的答案是: &gt; 因为碰到的对数似然函数是一个凹函数形式，这样一阶导数为0的点就与函数最大值点互为充要条件了</p>
<p>这个答案是正确的，但是不够严谨，我相信基本没人会在应用最大似然进行参数估计时首先进行对数似然函数的凸性判定，这篇文章主要就是想通过严谨的数学推导来说明一类概率密度函数的对数似然函数是凹函数，可以直接利用求导等于0这种简单直接的方法寻找最优估计，而若碰到不属于这一类的概率密度函数，则在使用最大似然估计时还是先判断对数似然函数凹凸性为妙，本篇文章按照以下结构组织: - 指数分布族 - 指数族函数举例 - 对数似然函数凹凸性证明 - 总结 <a id="more"></a>  ### 指数分布族 首先给出指数分布族的定义： &gt; 指数分布族是一类概率分布的总称，这类分布的概率密度函数具有这样的形式： &gt; <span class="math display">\[
    p_\theta(x) = h(x) \exp(\theta^T \phi(x) - A(\theta))
&gt; \]</span> &gt; 式中，<span class="math inline">\(x\)</span>是密度函数自变量，<span class="math inline">\(x \in \mathcal{X}\)</span>;<span class="math inline">\(\phi(x)\)</span>是充分统计量，可以看做是原始变量的一个映射: &gt; <span class="math display">\[
    \phi: \mathcal{X} \rightarrow R^d
&gt; \]</span> &gt; <span class="math inline">\(\theta\)</span>是模型参数向量，与充分统计量维度相同，<span class="math inline">\(h(x)\)</span>是一个只与<span class="math inline">\(x\)</span>有关的统计量，<span class="math inline">\(A(\theta)\)</span>为配分函数，通过该函数来保证<span class="math inline">\(p_\theta(x)\)</span>满足概率密度函数的定义： &gt; <span class="math display">\[
     \int_{\mathcal{X}} p_{\theta}(x) dx = 1
&gt; \]</span> &gt; 由此约束条件，我们可以得到<span class="math inline">\(A(\theta)\)</span>的解析表达式： &gt; <span class="math display">\[
    A(\theta) = \log (\int h(x) \exp(\theta^T \phi(x))dx )
&gt; \]</span></p>
<h3 id="指数分布族函数举例">指数分布族函数举例</h3>
<p>首先给出结论，按照笔者调研，属于指数分布族的分布有： - 正态分布<span class="math inline">\(N(\mu,\sigma^2)\)</span> - 伯努利分布(两点分布)<span class="math inline">\(B(1,\pi)\)</span> - 二项分布<span class="math inline">\(B(n,\pi)\)</span> - 泊松分布<span class="math inline">\(P(\lambda)\)</span> - 伽马分布<span class="math inline">\(G(\mu,v)\)</span> - .......</p>
<p>下面就将其中几个分布化成指数分布族的标准形式</p>
<h4 id="伯努利分布">伯努利分布</h4>
<p>对于伯努利分布，我们有<span class="math inline">\(\mathcal{X} = \{ 0,1 \}\)</span>,模型参数为<span class="math inline">\(P(x = 1) = \pi,P(x = -1) = 1 - \pi\)</span>,因此我们有: <span class="math display">\[
    \begin{aligned}
        P(X = x)  = \pi^x (1 - \pi)^{1-x} &amp;= \exp(x \log \pi + (1-x) \log(1-\pi) )  \\
        &amp;= \exp(x \log \frac{\pi}{1-\pi} + \log (1-\pi)) 
    \end{aligned}
\]</span> 因此令<span class="math inline">\(\theta = \log \frac{\pi}{1 - \pi}\)</span>, 则<span class="math inline">\(P(X = x)\)</span>可以写做: <span class="math display">\[
    P(X = x) = \exp(x\theta - \log(1 + e^\theta))
\]</span> 因此对于伯努利分布<span class="math inline">\(h(x) = 1,\phi(x) = x, \theta = log \frac{\pi}{1-\pi}, A(\theta) = log(1 + e^\theta)\)</span></p>
<h4 id="泊松分布">泊松分布</h4>
<p>首先给出泊松分布的形式: <span class="math display">\[
    P_\lambda(X = x) = \frac{\lambda^x}{x!} e^{- \lambda}
\]</span> 下面将其转化成指数分布族标准形式: <span class="math display">\[
    \begin{aligned}
        P_\lambda(X = x) &amp;= \frac{\lambda^x}{x!} e^{- \lambda} \\
        &amp;= \frac{1}{x!} e^{x\log \lambda - \lambda}
    \end{aligned}
\]</span> 因此，对于泊松分布，<span class="math inline">\(h(x) = \frac{1}{x!}, \theta = \log \lambda,\phi(x) = x,A(\theta) = e^\theta\)</span></p>
<h4 id="高斯分布">高斯分布</h4>
<p>高斯分布的概率密度函数形式如下: <span class="math display">\[
    p_{\mu,\sigma}(x) = \frac{1}{\sqrt{2 \pi} \sigma} \exp(- \frac{(x-\mu)^2}{2\sigma^2})
\]</span> 下面将其转化成指数分布族标准形式: <span class="math display">\[
    \begin{aligned}
         p_{\mu,\sigma}(x) &amp;= \frac{1}{\sqrt{2 \pi} \sigma} \exp(- \frac{(x-\mu)^2}{2\sigma^2})  \\
         &amp;= \frac{1}{\sqrt{2 \pi} \sigma} \exp(\frac{-x^2}{2 \sigma^2} + \frac{x \mu}{\sigma^2} - \frac{\mu^2}{2\sigma^2}) \\
         &amp;= \frac{1}{\sqrt{2 \pi} } \exp(\frac{-x^2}{2 \sigma^2} + \frac{x \mu}{\sigma^2} - \frac{\mu^2}{2\sigma^2} + \log \sigma)
    \end{aligned}
\]</span> 因此，对于高斯分布: <span class="math display">\[
    \begin{aligned}
        h(x) &amp;= \frac{1}{\sqrt{2 \pi} \sigma}  \\
        \phi(x) &amp;= [\frac{x^2}{2}, x]^T \\
        \theta &amp;= [-\frac{1}{\sigma^2}, \frac{\mu}{\sigma^2}]^T \\
        A(\theta) &amp;= -\frac{1}{2} (log \theta^{(1)} + \theta^{(2)})
    \end{aligned}
\]</span></p>
<h3 id="对数似然函数凹凸性证明">对数似然函数凹凸性证明</h3>
<p>我们关心的是最终对数似然函数的凹凸性，因为最终的对数似然函数是各个样本的概率密度函数取对数后累加的形式，由保凸运算可知，若单个对数概率密度函数的凹凸性可以确定，那么最终对数似然函数的凹凸性也就确定了。对于指数族分布，单个样本的概率密度函数取对数如下: <span class="math display">\[
    \begin{aligned}
        \log p_{\theta}(x) &amp;= \log h(x) \exp(\theta^T \phi(x) - A(\theta)) \\
        &amp;= \log h(x) + \theta^T \phi(x) + \log(-A(\theta))
    \end{aligned}
\]</span> 第一项与<span class="math inline">\(\theta\)</span>无关，第二项为<span class="math inline">\(\theta\)</span>的线性组合，不影响函数的凹凸性，所以整个函数的凹凸性是由第三项来确定，对于外层函数是对数函数的对数函数<span class="math inline">\(\log f(x)\)</span>而言: <span class="math display">\[
    (\log f(x))^{&#39;&#39;} = \frac{f(x) f^{&#39;&#39;}(x) - f^{&#39;}(x)^2}{f(x)^2}
\]</span> 由对数函数性质有<span class="math inline">\(f(x) &gt; 0\)</span>,因此，若<span class="math inline">\(f(x)\)</span>是凹函数(<span class="math inline">\(f^{&#39;&#39;}(x) \leq 0\)</span>),则可推出<span class="math inline">\((\log f(x))^{&#39;&#39;} \leq 0\)</span>,即<span class="math inline">\(\log f(x)\)</span>为凹函数，若<span class="math inline">\(f(x)\)</span>为凸函数则不能有直接的结论，因此我们的重点放在讨论<span class="math inline">\(A(\theta)\)</span>的凹凸性上。 下面证明<span class="math inline">\(A(\theta)\)</span>为凸函数,从定义出发证明，记<span class="math inline">\(\theta_{\lambda} = \lambda \theta_1 + (1 - \lambda) \theta_2\)</span>,其中<span class="math inline">\(\theta_1,\theta_2 \in dom f, \lambda \in [0,1]\)</span>,由Hölder不等式，可知: <span class="math display">\[
    \begin{aligned}
        A(\theta_\lambda) &amp;= \log (\int h(x) \exp(\theta_{\lambda}^T \phi(x))dx ) \\ 
        &amp;= \log (\int \exp(\theta_{\lambda}^T \phi(x))d\mu(x)\\
        &amp;= \log (\int \exp[ (\lambda \theta_1 \phi(x) + (1-\lambda)\theta_2)\phi(x)]d\mu(x) ) \\
        &amp;= \log (\int \exp(\theta_1 \phi(x)^{\lambda}) \exp (\theta_2 \phi(x))^{1-\lambda} d \mu(x)) \\
        &amp;\leq \log (\int \exp(\theta_1 \phi(x))^{\frac{\lambda}{\lambda}} d\mu(x) )^\lambda (\int \exp(\theta_2 \phi(x))^{\frac{1-\lambda}{1- \lambda}}d\mu(x))^{1-\lambda}  (Hölder不等式) \\
        &amp;= \lambda \log (\int \exp(\theta_1 \phi(x)) d\mu(x) ) + (1-\lambda)
        \log (\int \exp(\theta_2 \phi(x)) d\mu(x) ) \\
        &amp;= \lambda A(\theta_1) + (1-\lambda) A(\theta_2)
    \end{aligned}
\]</span> 从凸函数定义可知,<span class="math inline">\(A(\theta)\)</span>为凸函数，至此我们便得到了对数似然函数的凹凸性: <span class="math display">\[
    \begin{aligned}
        &amp;A(\theta) 凸 \rightarrow -A(\theta) 凹 \rightarrow \log(-A(\theta)) 凹 \\
        &amp; log(p_\theta(x)) 凹  \rightarrow 对数似然函数 凹
    \end{aligned}
\]</span></p>
<h3 id="总结">总结</h3>
<p>从前面列出的指数分布族所包含的分布来看，我们日常所碰到的绝大多数概率分布都属于指数族分布，是可以直接通过求导等于0这种方法来得到参数估计，而对于不属于指数分布族的分布，则是有必要对对数似然函数进行分析，而不能直接通过求导来解最优参数估计值。</p>
<h4 id="参考">参考</h4>
<p>本文内容主要参考以下链接： [1] <a href="https://www.zhihu.com/question/263423642/answer/269353169">为什么极大似然估计求导为 0 就是要求的值呢？</a> [2] <a href="https://web.stanford.edu/class/stats311/Lectures/lec-07.pdf">指数分布族函数</a></p>
]]></content>
      <categories>
        <category>统计学习</category>
      </categories>
      <tags>
        <tag>统计学习，凸优化</tag>
      </tags>
  </entry>
  <entry>
    <title>决策树模型</title>
    <url>/2020/08/30/jue-ce-shu-mo-xing/</url>
    <content><![CDATA[<p>这一部分介绍下决策树算法，决策树是一种基本的分类与回归表示方法，决策树模型呈树形结构，在分类问题中，可以表示为基于特征对实例进行分类的过程。决策树可以认为是&quot;if-then&quot;规则的集合，也可以认为是定义在特征空间与类空间上的概率分分布。本文按照以下四部分进行组织: - 决策树模型与学习 - 特征选择 - 决策树生成算法 - 决策树剪枝算法</p>
<a id="more"></a>
<h3 id="决策树模型与学习">决策树模型与学习</h3>
<h4 id="决策树模型定义">决策树模型定义</h4>
<p>首先给出决策树的定义: &gt; <strong>决策树：</strong> 分类决策树模型是一种描述对实例进行分类的树形结构。决策树由节点和有向边组成，节点由两种类型：内部节点和叶节点。内部节点表示一个特征或属性，叶节点表示一个类。</p>
<p>一个决策树模型如下图所示： <img src="https://raw.githubusercontent.com/xuejy19/xuejy19.github.io/source/Img/Decision_tree.png" alt="决策树" /> 决策树的逻辑其实与人思考的逻辑非常贴合,都是通过一连串的&quot;if-then&quot;语句来进行判断，这也是决策树算法具有较好可解释性的原因。 #### 决策树与&quot;if-then&quot;规则 一个决策树可以转换成一组&quot;if-then&quot;规则,将决策树转换成&quot;if-then&quot;规则集合的步骤如下:由决策树的根节点到叶节点的每一条路径构建一条规则：路径上内部节点的特征对应着规则的条件，而叶节点的类则对应着规则的结论。 决策树对应的&quot;if-then&quot;规则集合具有一个重要的性质：互斥并且完备，即每一个实例都被一条路径或一条规则所覆盖，而且只被一条规则或一条规则所覆盖。</p>
<h4 id="决策树与条件概率分布">决策树与条件概率分布</h4>
<p>决策树还表示给定特征条件下类的条件概率分布。这一条件概率分布定义在特征空间的一个划分上。将特征空间划分为互不相交的单元或区域，并在每个单元定义一个类的概率分布就构成了一个条件概率分布。决策树的一条路径对应于划分中的一个单元，决策树所表示的条件概率分布由各个单元给定条件下类的条件概率分布组成。 假设<span class="math inline">\(X\)</span>为表示特征的随机变量，<span class="math inline">\(Y\)</span>为表示类别的随机变量，那么这个条件概率分布可以表示为<span class="math inline">\(P(Y|X)\)</span>,<span class="math inline">\(X\)</span>取值于给定划分下单元的集合，<span class="math inline">\(Y\)</span>取值于类的集合。</p>
<h4 id="决策树学习">决策树学习</h4>
<p>假设给定训练数据集: <span class="math display">\[
    D = \{ (x_1,y_1),(x_2,y_2), \dots, (x_N,y_N)\}
\]</span> 其中<span class="math inline">\(x_i \in R^n\)</span>为特征向量,<span class="math inline">\(n\)</span>为特征个数，<span class="math inline">\(y_i \in \{1,2, \dots, K\}\)</span>为类标记，<span class="math inline">\(N\)</span>为样本容量。 <strong>决策树学习的目标是根据给定的训练数据集构建一个决策树模型，使它能够对实例进行正确的分类。</strong></p>
<p>决策树学习本质上是从训练数据集中归纳出一组分类规则，这点与前面章节介绍过的规则学习非常类似，不过决策树所针对的问题更加具体。与训练数据集不相矛盾的决策树可能有多个，也可能一个也没有。我们需要的是一个与训练数据矛盾较小的决策树，同时具有很好的泛化能力。从条件概率分布的角度来看，决策树学习是由训练数据集估计条件概率模型，基于特征空间划分的类的条件概率模型有无穷多个，我们选择的条件概率模型应该不仅对训练数据有很好的拟合，同时也应当对未知的数据有较好的预测。 从所有可能的决策树中选取最优决策树是NP完全问题，所以现实中决策树学习算法通常采用启发式方法，近似求解这一最优问题，这样得到的决策树是次最优的。 决策树学习算法通常是一个递归地选取最优特征，并根据该特征对训练数据进行分割，使得各数据集有一个最好的分类的过程。这一过程对应着特征空间的划分，也对应着决策树的构建。构建流程如下: - 构建根节点，将所有训练数据都放在跟节点 - 选择一个最优特征，按照这一特征将训练数据集分割成子集，使得各个子集有一个在当前条件下最好的分类。 - 判断子集中的实例是否都被正确分类，若都已经被正确分类，则构建叶节点 - 若子集不能被正确分类，则返回第二步，选取最优特征并划分子集。</p>
<p>通过上述流程所构建的决策树往往对训练数据有着良好的分类能力，但对未知的测试数据却未必有很好的分类能力，即可能发生过拟合现象，因此往往需要的生成的决策树自下而上进行剪枝。</p>
<h3 id="特征选择">特征选择</h3>
<p>特征选择的目的在于选取具有分类能力的特征，决策树的每一次分叉都是进行特征选择的过程, 所以有一个亟需回答的问题就是： &gt; <strong>应当以什么样的标准来评判一个特征的分类能力？</strong></p>
<p>通常评价特征分类能力的指标是信息增益和信息增益比，在前面章节中我已经给出了熵和条件熵的定义： &gt; <strong>熵：</strong> 在信息论与概率统计中，熵是表示<strong>随机变量不确定性</strong>的度量，设<span class="math inline">\(X\)</span>是一个取有限个值的离散随机变量，其概率分布为: &gt; <span class="math display">\[
    P(X = x_i) = p_i, \quad i =1,2,\dots,n
&gt; \]</span> 则随机变量<span class="math inline">\(X\)</span>的熵定义为: <span class="math display">\[
    H(X) = - \sum_{i=1}^n p_i \log p_i 
\]</span></p>
<blockquote>
<p><strong>条件熵：</strong> 设有随机变量<span class="math inline">\((X,Y)\)</span>,其联合概率分布为： <span class="math display">\[
P(X = x_i,Y = y_i) = p_{ij}\quad i = 1,2,\dots,n;\quad j = 1,2,\dots,m
\]</span> 条件熵<span class="math inline">\(H(Y|X)\)</span>表示在已知随机变量<span class="math inline">\(X\)</span>条件下随机变量<span class="math inline">\(Y\)</span>的不确定性。随机变量<span class="math inline">\(X\)</span>给定的条件下随机变量<span class="math inline">\(Y\)</span>的条件熵<span class="math inline">\(H(Y|X)\)</span>,定义为<span class="math inline">\(X\)</span>给定条件下<span class="math inline">\(Y\)</span>的条件概率分布的熵对<span class="math inline">\(X\)</span>的数学期望： <span class="math display">\[
H(Y|X) = \sum_{i=1}^n p_i H(Y|X = x_i)
\]</span> 当熵和条件熵中的概率由数据估计得到时，所对应的熵与条件熵分别称为经验熵和经验条件熵</p>
</blockquote>
<h4 id="信息增益">信息增益</h4>
<p>下面给出信息增益的定义： &gt; <strong>信息增益：</strong> 特征<span class="math inline">\(A\)</span>对训练集<span class="math inline">\(D\)</span>的信息增益<span class="math inline">\(g(D,A)\)</span>,定义为集合<span class="math inline">\(D\)</span>的经验熵<span class="math inline">\(H(D)\)</span>与特征<span class="math inline">\(A\)</span>给定条件下<span class="math inline">\(D\)</span>的经验条件熵<span class="math inline">\(H(D|A)\)</span>之差，即： &gt;<span class="math display">\[
    g(D,A) = H(D) - H(D|A)
&gt;\]</span></p>
<p>给定数据集<span class="math inline">\(D\)</span>和特征<span class="math inline">\(A\)</span>,经验熵<span class="math inline">\(H(D)\)</span>表示对数据集<span class="math inline">\(D\)</span>进行分类的不确定性，而经验条件熵<span class="math inline">\(H(D|A)\)</span>则表示在特征<span class="math inline">\(A\)</span>给定条件下对数据集<span class="math inline">\(D\)</span>进行分类的不确定性。而它们之间的差则表示由于特征<span class="math inline">\(A\)</span>而使得对数据集<span class="math inline">\(D\)</span>的分类的不确定性减少的程度。由此便可得通过信息增益来选择特征的算法如下: &gt; <strong>信息增益算法</strong> &gt; <strong>输入：</strong> 训练数据集<span class="math inline">\(D\)</span>及特征<span class="math inline">\(A\)</span> &gt; <strong>输出：</strong> 特征<span class="math inline">\(A\)</span>对训练数据集<span class="math inline">\(D\)</span>的信息增益<span class="math inline">\(g(D,A)\)</span> &gt; - Step1: 计算数据集<span class="math inline">\(D\)</span>的经验熵： &gt; <span class="math display">\[
    H(D) = -\sum_{k=1}^K \frac{|C_k|}{|D|} \log_2 \frac{|C_k|}{|D|}
&gt; \]</span> &gt; - Step2: 计算特征<span class="math inline">\(A\)</span>对数据集<span class="math inline">\(D\)</span>的经验条件熵<span class="math inline">\(H(D|A)\)</span> <span class="math display">\[
    H(D|A) = \sum_{i=1}^n \frac{|D_i|}{|D|} H(D_i) = -\sum_{i=1}^n \frac{|D_i|}{|D|} \sum_{k=1}^K \frac{|D_{ik}|}{|D_i|} \log_2 \frac{|D_{ik}|}{|D_i|}
\]</span> &gt; - Step3: 计算信息增益 &gt; <span class="math display">\[
    g(D,A) = H(D) - H(D|A)
&gt; \]</span></p>
<h4 id="信息增益比">信息增益比</h4>
<p>以信息增益作为划分训练数据集的特征，存在偏向于选择取值较多的特征的问题。使用信息增益比则可以对这一问题进行校正，下面给出信息增益比的定义: &gt; <strong>信息增益比：</strong> 特征<span class="math inline">\(A\)</span>对训练数据集<span class="math inline">\(D\)</span>的信息增益比<span class="math inline">\(g_R(D,A)\)</span>定义为其信息增益<span class="math inline">\(g(D,A)\)</span>与训练数据集<span class="math inline">\(D\)</span>关于特征<span class="math inline">\(A\)</span>的值的熵<span class="math inline">\(H_A(D)\)</span>之比，即： &gt; <span class="math display">\[
    g_R(D,A) = \frac{g(D,A)}{H_A(D)}
&gt; \]</span> &gt; 其中<span class="math inline">\(H_A(D) = -\sum_{i=1}^n \frac{|D_i|}{|D|} \log_2 \frac{|D_i|}{|D|}\)</span>,<span class="math inline">\(n\)</span>是特征<span class="math inline">\(A\)</span>取值的个数</p>
<h4 id="基尼指数基尼不纯度">基尼指数(基尼不纯度)</h4>
<p>首先给出基尼指数的定义: &gt; <strong>基尼指数：</strong> 分类问题中，假设有<span class="math inline">\(K\)</span>个类，样本属于第<span class="math inline">\(k\)</span>类的概率为<span class="math inline">\(p_k\)</span>，则概率分布的基尼指数定义为: &gt; <span class="math display">\[
    Gini(p) = \sum_{k=1}^K p_k(1 - p_k) = 1 - \sum_{k=1}^K p_k^2
&gt; \]</span> &gt; 对于二分类问题，若样本点属于第一个类的概率是<span class="math inline">\(p\)</span>,则概率分布的基尼指数为: &gt; <span class="math display">\[
    Gini(p) = 2p(1-p)
&gt; \]</span> &gt; 对于给定的样本集合<span class="math inline">\(D\)</span>，其基尼指数为: &gt; <span class="math display">\[
    Gini(D) = 1 - \sum_{k=1}^K (\frac{|D_k|}{|D|})^2
&gt; \]</span> &gt; 在特征<span class="math inline">\(a_i\)</span>下将特征空间划分成<span class="math inline">\(n\)</span>块，各块的样本数为<span class="math inline">\(D_i\)</span>,条件Gini指数可以写做: &gt; <span class="math display">\[
    Gini(D|a_i) = \sum_{i=1}^n \frac{|D_i|}{|D|} Gini(D_i)
&gt; \]</span> &gt; 引入特征<span class="math inline">\(a_i\)</span>后，集合<span class="math inline">\(D\)</span>不确定性降低了: &gt; <span class="math display">\[
    Gini(D) - Gini(D|a_i)
&gt; \]</span> ### 决策树的生成 决策树生成流程在前面已经介绍过，不同算法的区别主要在于特征选择标准上的差别，该部分主要介绍两种决策树生成算法： - ID3算法 - C4.5算法</p>
<h4 id="id3算法">ID3算法</h4>
<p>ID3算法相当于用极大似然法进行概率模型的选择,下面给出ID3算法的流程: &gt; <strong>ID3算法</strong> &gt; <strong>输入：</strong> 训练数据集<span class="math inline">\(D\)</span>, 特征集<span class="math inline">\(A\)</span>阈值<span class="math inline">\(\epsilon\)</span> &gt; <strong>输出：</strong> 决策树<span class="math inline">\(T\)</span> &gt; - 若<span class="math inline">\(D\)</span>中所有实例属于同一类<span class="math inline">\(C_k\)</span>,则<span class="math inline">\(T\)</span>为单节点树，并将<span class="math inline">\(C_k\)</span>作为该节点的类标记，返回<span class="math inline">\(T\)</span> &gt; - 若<span class="math inline">\(A = \varnothing\)</span>,则<span class="math inline">\(T\)</span>为单节点树，并将<span class="math inline">\(D\)</span>中实例数最大的类<span class="math inline">\(C_k\)</span>作为该节点的类标记，返回<span class="math inline">\(T\)</span> &gt; - 否则，计算<span class="math inline">\(A\)</span>中各特征对<span class="math inline">\(D\)</span>的信息增益，选择信息最大的特征<span class="math inline">\(A_g\)</span> &gt; - 如果<span class="math inline">\(A_g\)</span>的信息增益小于阈值<span class="math inline">\(\epsilon\)</span>，则置<span class="math inline">\(T\)</span>为单节点树，并将<span class="math inline">\(D\)</span>中最大实例数类<span class="math inline">\(C_k\)</span>作为类别，返回<span class="math inline">\(T- &gt; - 否则，按照\)</span>A_g<span class="math inline">\(的每一特征值将特征空间划分成若干子集，将\)</span>D_i<span class="math inline">\(中实例最大的类作为标记，构建子节点，由节点及其子节点构成树\)</span>T<span class="math inline">\(，返回\)</span>T$ &gt; - 对第<span class="math inline">\(i\)</span>个子节点，以<span class="math inline">\(D_i\)</span>为训练集，以<span class="math inline">\(A - {A_g}\)</span>为特征集，递归地调用前述步骤，得到子树<span class="math inline">\(T_i\)</span>,返回子树</p>
<h4 id="c4.5算法">C4.5算法</h4>
<p>C4.5算法选用信息增益比作为特征选择标准，其流程如下： &gt; <strong>C4.5算法</strong> &gt; <strong>输入：</strong> 训练数据集<span class="math inline">\(D\)</span>, 特征集<span class="math inline">\(A\)</span>阈值<span class="math inline">\(\epsilon\)</span> &gt; <strong>输出：</strong> 决策树<span class="math inline">\(T\)</span> &gt; - 若<span class="math inline">\(D\)</span>中所有实例属于同一类<span class="math inline">\(C_k\)</span>,则<span class="math inline">\(T\)</span>为单节点树，并将<span class="math inline">\(C_k\)</span>作为该节点的类标记，返回<span class="math inline">\(T\)</span> &gt; - 若<span class="math inline">\(A = \varnothing\)</span>,则<span class="math inline">\(T\)</span>为单节点树，并将<span class="math inline">\(D\)</span>中实例数最大的类<span class="math inline">\(C_k\)</span>作为该节点的类标记，返回<span class="math inline">\(T\)</span> &gt; - 否则，计算<span class="math inline">\(A\)</span>中各特征对<span class="math inline">\(D\)</span>的信息增益比，选择信息最大的特征<span class="math inline">\(A_g\)</span> &gt; - 如果<span class="math inline">\(A_g\)</span>的信息增益小于阈值<span class="math inline">\(\epsilon\)</span>，则置<span class="math inline">\(T\)</span>为单节点树，并将<span class="math inline">\(D\)</span>中最大实例数类<span class="math inline">\(C_k\)</span>作为类别，返回<span class="math inline">\(T- &gt; - 否则，按照\)</span>A_g<span class="math inline">\(的每一特征值将特征空间划分成若干子集，将\)</span>D_i<span class="math inline">\(中实例最大的类作为标记，构建子节点，由节点及其子节点构成树\)</span>T<span class="math inline">\(，返回\)</span>T$ &gt; - 对第<span class="math inline">\(i\)</span>个子节点，以<span class="math inline">\(D_i\)</span>为训练集，以<span class="math inline">\(A - {A_g}\)</span>为特征集，递归地调用前述步骤，得到子树<span class="math inline">\(T_i\)</span>,返回子树</p>
<h3 id="决策树的剪枝">决策树的剪枝</h3>
<p>决策树算法非常容易产生过拟合问题，过拟合的原因在于决策树在学习时过多地考虑了如何提高对训练数据分类准确率，从而构建除了过于复杂的决策树，而剪枝就是为了减轻决策树过拟合的一种操作。剪枝操作按照进行的时间段可以分为前剪枝与后剪枝两种： - 预剪枝：预剪枝操作是在决策树的构建过程中, 即通过对信息增益/信息增益比/Gini指数设定阈值，当分裂前后相应标准小于该阈值时，决策树便不进行分裂操作，又或者是先验地限制决策树的深度或者节点个数。预剪枝得到的决策树尽管更加简单，泛化能力得到了提升，但这样的剪枝操作是盲目的、缺乏指导的，实际应用中很容易导致决策树训练不充分的问题。 - 后剪枝：后剪枝顾名思义，是在一棵完整的决策树构建完成之后进行剪枝，常用的后剪枝操作有两种: - 基于带正则项的损失函数进行剪枝 - 基于验证集错误率进行后剪枝</p>
<p>预剪枝的思想非常简单，这里便不做过多介绍，下面以带正则化的损失函数为例介绍下后剪枝的流程 #### 基于正则项的损失函数 首先考虑下对于一棵决策树，其似然函数应当怎么写，假设一棵决策树有<span class="math inline">\(|T|\)</span>个叶节点，<span class="math inline">\(t\)</span>是树<span class="math inline">\(T\)</span>的叶节点，该叶节点上有<span class="math inline">\(N_t\)</span>个样本点，其中<span class="math inline">\(k\)</span>类的样本点有<span class="math inline">\(N_{tk}\)</span>个，<span class="math inline">\(k=1,2,\dots,K\)</span>。可以这么考虑，这<span class="math inline">\(|T|\)</span>个叶节点将整个特征空间分成了<span class="math inline">\(|T|\)</span>部分，其中<span class="math inline">\(t\)</span>节点上一个属于<span class="math inline">\(K\)</span>类的样本出现的概率(最大似然估计得到)为<span class="math inline">\(\frac{N_{tk}}{N_t}\)</span>,而又假设样本之间是彼此独立的，该节点上有<span class="math inline">\(N_{tk}\)</span>个该类样本，据此可以写出节点<span class="math inline">\(t\)</span>上样本出现的概率: <span class="math display">\[
     \prod_{k=1}^K (\frac{N_{tk}}{N_t})^{N_{tk}}
\]</span> 而一共有<span class="math inline">\(|T|\)</span>个叶节点，故整个树上的样本出现的概率(似然函数)为： <span class="math display">\[
    \prod_{t=1}^{|T|} \prod_{k=1}^K (\frac{N_{tk}}{N_t})^{N_{tk}}
\]</span> 至此便可以写出对数似然函数: <span class="math display">\[
    L(T) = \sum_{t=1}^{|T|} \sum_{k=1}^K N_{tk} \log \frac{N_{tk}}{N_t}
\]</span> 我们想要似然函数最大，其实也就是负的对数似然函数最小，由此可得损失函数<span class="math inline">\(C(T)\)</span>: <span class="math display">\[
    C(T) = - L(T) = -\sum_{t=1}^{|T|} \sum_{k=1}^K N_{tk} \log \frac{N_{tk}}{N_t}
\]</span> 而我们不仅仅希望决策树<span class="math inline">\(T\)</span>在训练集上表现良好，同时希望决策树具有良好的泛化性能，因此需要在上面损失函数的基础上添加正则项，正则项需要与决策树的复杂程度有关，这里选用叶节点个数<span class="math inline">\(|T|\)</span>作为损失函数,由此可得正则化的损失函数如下: <span class="math display">\[
    C_{\alpha}(T) = C(T) + \alpha |T|
\]</span> 其中<span class="math inline">\(\alpha\)</span>是权重因子，用来平衡模型复杂度与训练数据拟合效果。将叶节点上的经验熵代入损失函数，可以得到损失函数的另一种表达形式: <span class="math display">\[
    C_\alpha(T) = -\sum_{i=1}^{|T|} N_t H_t(T) + \alpha_t
\]</span> 其中<span class="math inline">\(H_t(T)\)</span>为叶节点上的经验熵，至此我们便可以写出该后剪枝算法流程: &gt; <strong>基于正则损失函数的后剪枝算法</strong> &gt; <strong>输入：</strong> 生成算法产生的整个树<span class="math inline">\(T\)</span>,<span class="math inline">\(\alpha\)</span> &gt; <strong>输出：</strong> 修建后的子树<span class="math inline">\(T_\alpha\)</span> &gt; - 计算当前树各个叶节点上的经验熵 &gt; - 递归地从树的叶节点向上回缩，设一组叶节点回缩到其父节点之前与之后的整体树分别为<span class="math inline">\(T_B\)</span>与<span class="math inline">\(T_A\)</span>,其对应的损失函数分别是<span class="math inline">\(C_\alpha(T_B)\)</span> 与 <span class="math inline">\(C_\alpha(T_A)\)</span>,若： <span class="math display">\[
    C_\alpha(T_A) \leq C_\alpha(T_B)
\]</span> 则进行剪枝，将父节点变为新的叶节点 &gt; - 返回第2步，直到不能继续为止，得到损失函数最小的子树<span class="math inline">\(T_\alpha\)</span></p>
<h4 id="基于验证集分类错误率进行后剪枝">基于验证集分类错误率进行后剪枝</h4>
<p>在基于正则损失函数的后剪枝中，所有的数据集都被拿来训练，而在基于验证集分类错误率方法中，首先会将数据集分为训练集和验证集，该方法的大致流程如下: - 首先利用训练集得到一棵完全生长的决策树 - 然后测试该决策树在验证集上的错误率<span class="math inline">\(E_1\)</span>，同时将某个叶节点向上回缩，即让某个父节点成为新的叶节点，验证新的树在验证集上的错误率<span class="math inline">\(E_2\)</span>,若<span class="math inline">\(E_2 \leq E_1\)</span>，则考虑将父节点成为新的叶节点 - 重复第二步，直到剪枝不能提升在验证集上的错误率</p>
<p>这种剪枝思路的优点在于是有指导有方向的剪枝，理论上讲在验证集上决策树的性能获得提升，也能够在测试集上获得提升，缺点可能就是有可能会导致模型一定程度上对验证集的过拟合。</p>
]]></content>
      <categories>
        <category>统计学习</category>
      </categories>
      <tags>
        <tag>树模型,categorical变量</tag>
      </tags>
  </entry>
  <entry>
    <title>提升算法总结</title>
    <url>/2020/09/01/ti-sheng-suan-fa-zong-jie/</url>
    <content><![CDATA[<p>对于一个特定机器学习问题，我们可能会建立很多模型，这些单个模型可能表现都不是非常好，由此便会引出一个问题: &gt; <strong>问题1: 能否通过一个算法将这些模型组合起来(Ensemble)，产生一个效果更好的组合模型？</strong></p>
<p>这个问题的答案是肯定的，历史上，Kearns 和 Valiant首先提出了“弱可学习”和“强可学习”的概念： &gt; <strong>强可学习：</strong> 在概率近似正确(PAC)框架下，一个概念(类)，如果存在一个多项式的学习算法能够学习它，并且正确率很高，就称这个概念是强可学习的 &gt; <strong>弱可学习：</strong> 一个概念，如果存在一个多项式的算法能够学习它，但学习的正确率仅仅比随机猜测好，则称该概念是弱可学习</p>
<p>后来Schapire证明了一个重要结论: &gt; <strong>一个概念是强可学习的 <span class="math inline">\(\Leftrightarrow\)</span> 一个概念是弱可学习的</strong></p>
<p>这样一来，问题1就转换成了另一个问题: &gt; <strong>问题2: 在学习中国，如果已经发现了“弱可学习算法”，那么能否将它提升为强可学习算法？</strong></p>
<p>该部分介绍以下几类提升算法: - Bagging - <strong>AdaBoost</strong> - 随机森林 - 提升算法解释 - 总结</p>
<a id="more"></a>
<h3 id="bagging">Bagging</h3>
<p>对于一个训练数据集，如果我们将所有的训练集数据拿来训练，那么我们便只能得到一个模型，因此Bagging的思想非常简单： &gt; 每次从训练数据集中取一部分数据来训练模型，通过这种方式得到多个模型，最后的决策结果则由这些模型投票表决</p>
<p>算法流程如下: &gt; - Step1: 从包含<span class="math inline">\(n\)</span>个样本的数据集中取<span class="math inline">\(m\)</span>个样本作为新的训练集 &gt; - Step2: 用挑选出的数据集来训练得到一个模型 &gt; - Step3: 重复Step1 L次，得到L个模型 &gt; - Step4: 最终决策结果由这<span class="math inline">\(L\)</span>个模型多数投票得到</p>
<h3 id="adaboost">AdaBoost</h3>
<h4 id="算法流程">算法流程</h4>
<p>Bagging的思想比较简单粗暴，所有样本都一视同仁，而另一种思路则是改变训练数据的概率分布(训练数据的权值分布)，针对不同的训练数据分布调用弱学习算法学习一系列弱分类器。这样，对于提升算法而言，有两个问题需要回答： - 在每一轮如何改变训练数据的权值或概率分布<br />
- 如何将这些弱分类器组合成一个强分类器</p>
<p>AdaBoost针对这两个问题的的解决方案是: - 第1个问题: 提高那些被前一轮弱分类器错误分类样本的权值，而降低那些被正确分类的样本的权值。这样一来，那些没有得到正确分类的数据由于权值加大而受到后一轮弱分类器的更多关注。 - 第2个问题：AdaBoost采用加权表决的方法,加大分类误差率小的弱分类器的权值。</p>
<p>AdaBoost算法流程如下: &gt; <strong>AdaBoost算法</strong> &gt; <strong>输入：</strong> 训练数据集<span class="math inline">\(T = \{ (x_1,y_1), (x_2,y_2),\dots, (x_N,y_N)\}\)</span>,其中<span class="math inline">\(x_i \in R^n, y_i \in \{ -1, +1 \}\)</span>,弱学习算法 &gt; <strong>输出：</strong> 最终分类器<span class="math inline">\(G(x)\)</span> &gt; 1. 初始化训练数据的权值分布 &gt; <span class="math display">\[
    D_1 = (w_{11}, \dots, w_{1i}, \dots,w_{1N}), w_{1i} = \frac{1}{N}
&gt; \]</span> &gt; 2. 对于<span class="math inline">\(m = 1,2,\dots,M\)</span> &gt; - 使用具有权值分布<span class="math inline">\(D_m\)</span>的训练数据集学习，得到基本分类器 &gt; <span class="math display">\[
        G_m(x): \mathcal{X} \rightarrow \{ -1, +1 \}
&gt;    \]</span> &gt; - 计算<span class="math inline">\(G_m(x)\)</span> 在训练数据集上的分类误差率 &gt; <span class="math display">\[
        e_m = \sum_{i=1}^N P(G_m(x_i) \neq y_i) = \sum_{i=1}^N w_{mi} I(G_m(x_i) \neq y_i)
&gt;    \]</span> &gt; - 计算<span class="math inline">\(G_m(x)\)</span>的系数: &gt; <span class="math display">\[
        \alpha_m = \frac{1}{2} \log \frac{1 - e_m}{e_m}
&gt;    \]</span> &gt; - 更新训练数据集的权值分布 &gt; <span class="math display">\[
    \begin{aligned}
        D_{m+1} &amp;= (w_{m+1,1},\dots, w_{m+1,i}, \dots,w_{m+1,N}) \\
        w_{m+1,i} &amp;= \frac{w_{m+1,i}}{Z_{m}} \exp(- \alpha_m y_i G_m(x_i)) \\
        Z_m &amp;= \sum_{i=1}^N w_{mi} \exp(-\alpha_m y_i G_m(x_i))
    \end{aligned}
&gt;   \]</span> &gt; - 构建基本分类器的线性组合: &gt; <span class="math display">\[
         f(x) = \sum_{m=1}^M \alpha_m G_m(x) 
&gt;     \]</span> &gt; 得到最终分类器： &gt; <span class="math display">\[
        G(x) = sign(f(x)) = sign(\sum_{m=1}^M \alpha_m G_m(x))
&gt;   \]</span></p>
<p>下面对AdaBoost算法说明如下: - 在最开始假设训练数据具有均匀的权值分布，即每个训练样本在基本分类器中的作用相同，这一假设保证第一步能够在原始数据上学习基本分类器<span class="math inline">\(G_1(x)\)</span> - AdaBoost反复学习基本分类器，在每一轮弱分类器训练中，首先使用当前分布<span class="math inline">\(D_m\)</span>加权的训练数据集，学习基本分类器<span class="math inline">\(G_m(x)\)</span>,使用加权数据进行训练，最终学得的分类器更加关注权重较高的数据，接下来计算该基本分类器<span class="math inline">\(G_m(x)\)</span>在加权训练数据集上的分类误差率,然后通过该误差率计算分类器<span class="math inline">\(G_m(x)\)</span>的权重，从权重计算公式可以看出，当<span class="math inline">\(e_m \leq 0.5\)</span>(弱分类器前提假设)时，<span class="math inline">\(\alpha_m \geq 0\)</span>,且随着<span class="math inline">\(e_m\)</span>减小，<span class="math inline">\(\alpha_m\)</span>会不断增大。 - 接下来更新训练数据的权值，对于被分类器<span class="math inline">\(G_m(x)\)</span>分类正确的样本，增加其权重，反之减少其权重。<strong>不改变所给的训练数据，而是不断改变训练数据权值的分布，使得训练数据在基本分类器的学习中起不同作用。</strong> - 最终的组合分类器是一个基本分类器的线性组合形式，<span class="math inline">\(f(x)\)</span>的符号决定了实例<span class="math inline">\(x\)</span>的类，而<span class="math inline">\(f(x)\)</span>的绝对值则显示了分类结果的置信度。</p>
<h4 id="算法理论说明">算法理论说明</h4>
<p>上一部分从算法思想来对AdaBoost进行理解，这一部分将从理论推导方面证明AdaBoost中的一些关键公式是如何来的，比如： - 分类器权重<span class="math inline">\(\alpha_m = \frac{1}{2} \log \frac{1-e_m}{e_m}\)</span> - 样本点权重更新公式： <span class="math display">\[
\begin{aligned}
    D_{m+1} &amp;= (w_{m+1,1},\dots, w_{m+1,i}, \dots,w_{m+1,N}) \\
    w_{m+1,i} &amp;= \frac{w_{m+1,i}}{Z_{m}} \exp(- \alpha_m y_i G_m(x_i)) \\
    Z_m &amp;= \sum_{i=1}^N w_{mi} \exp(-\alpha_m y_i G_m(x_i))
\end{aligned}
\]</span></p>
<h5 id="adaboost算法训练误差界">AdaBoost算法训练误差界</h5>
<p>首先来推导下AdaBoost的训练误差界，样本<span class="math inline">\(x_i\)</span> 在第<span class="math inline">\(t\)</span>轮训练时的权重，由样本点权重递推公式可以得到： <span class="math display">\[
    w_{t+1i} = w_{ti} \frac{e^{-\alpha_t y_i h_t(i)}}{Z_{t}} = \dots = w_{1i} \frac{e^{-y_i \sum_{j=1}^t \alpha_j h_j(i)}}{\prod_{i=1}^{t} Z_i}
\]</span> 其中<span class="math inline">\(w_{1i} = \frac{1}{N}\)</span>,若记： <span class="math display">\[
    \boldsymbol{\alpha} = [\alpha_1,\alpha_2,\dots, \alpha_t], \boldsymbol{h(x)} = [h_1(i),h_2(i),\dots, h_t(i)] 
\]</span> <span class="math inline">\(w_{t+1i}\)</span>可以写成更加紧凑的形式: <span class="math display">\[
    w_{t+1i} = \frac{e^{-y_i &lt;\boldsymbol{\alpha}, \boldsymbol{h(x)}&gt;}}{N \prod_{i=1}^t Z_i}
\]</span> 又因为有<span class="math inline">\(\sum_{i=1}^N w_{t+1i} = 1\)</span>,对上式求和后可得： <span class="math display">\[
    \prod_{i=1}^t Z_i = \frac{1}{N} \sum_{i=1}^N e^{-y_i &lt;\boldsymbol{\alpha}, \boldsymbol{h(x)}&gt;}
\]</span> 需要注意的是，<span class="math inline">\(&lt;\boldsymbol{\alpha}, \boldsymbol{h(x)}&gt;\)</span>刚好是使用AdaBoost最终得到模型的预测结果(未取sign)，下面考虑两种情况： <span class="math display">\[
    \begin{aligned}
        &amp;If \quad sign(&lt;\boldsymbol{\alpha}, \boldsymbol{h(x_i)}&gt;) = h(x_i) = y_i \Rightarrow &lt;\boldsymbol{\alpha}, \boldsymbol{h(x)}&gt; \cdot y_i = c \geq 0  , 0 \leq e^{-c}  \\
        &amp;If \quad sign(&lt;\boldsymbol{\alpha}, \boldsymbol{h(x_i)}&gt;) = h(x_i) \neq y_i \Rightarrow &lt;\boldsymbol{\alpha}, \boldsymbol{h(x)}&gt; \cdot y_i = c \leq 0  , 1 \leq e^{-c} 
    \end{aligned}
\]</span> 而AdaBoost最终训练得到的模型的分类误差为: <span class="math display">\[
    \epsilon  = \frac{1}{N}\sum_{i=1}^N \epsilon_i, \epsilon_i = \begin{cases}
        1 &amp; h(x_i) \neq y_i \\
        0 &amp; h(x_i) = y_i
    \end{cases}
\]</span> 从前面两种分类讨论可以得到: <span class="math display">\[
    \epsilon_i \leq e^{-y_i &lt;\boldsymbol{\alpha}, \boldsymbol{h(x_i)}&gt;} 
\]</span> 由此便可以得到AdaBoost分类器的<strong>分类错误率上界</strong>： <span class="math display">\[
 \epsilon = \frac{1}{N}\sum_{i=1}^N \epsilon_i \leq \frac{1}{N} \sum_{i=1}^N e^{-y_i &lt;\boldsymbol{\alpha}, \boldsymbol{h(x_i)}&gt;}  = \prod_{i=1}^t Z_i
\]</span></p>
<h5 id="alpha_tast-推导"><span class="math inline">\(\alpha_t^\ast\)</span> 推导</h5>
<p>需要注意的是，我们上面并未指定权重<span class="math inline">\(\alpha\)</span>的表达式，那么现在我们考虑这样一个问题: &gt; <span class="math inline">\(\alpha\)</span>取什么值时分类错误率的上界可以尽可能小</p>
<p>因为<span class="math inline">\(Z_i\)</span>的大小依赖于<span class="math inline">\(\alpha_i\)</span>，而且它们都是迭代过程中的变量，因此可以考虑将上述问题转化为: &gt; 如何在每一步迭代中选择合适的<span class="math inline">\(\alpha_t\)</span>使得<span class="math inline">\(Z_t\)</span>尽可能小</p>
<p><span class="math inline">\(Z_t\)</span>的表达式如下: <span class="math display">\[
    Z_t = \sum_{i=1}^N w_{ti} e^{-\alpha_t y_i h_t(x_i)}
\]</span> 这是一个凸函数，因此可以直接求导令导数等于0： <span class="math display">\[
    \frac{\partial Z_t}{\partial \alpha_t} = -\sum_{i=1}^N w_{ti} y_i h_t(x_i) e^{-\alpha_t y_i h_t(x_i)} = 0 
\]</span> 下面考虑两个集合： <span class="math display">\[
    A = y_i h_t(x_i) = 1, \bar{A} = y_i h_t(x_i) = -1
\]</span> 据此，上面的偏导可以写成: <span class="math display">\[
    \frac{\partial Z_t}{\partial \alpha_t} = -\sum_{x_i \in A} w_{ti} e^{-\alpha_t} + \sum_{x_i \in \bar{A}} w_{ti} e^{\alpha} = 0 
\]</span> 由此可得: <span class="math display">\[
    e^{2\alpha_t} = \frac{\sum_{x_i \in A} w_{ti}}{\sum_{x_i \in \bar{A}} w_{ti}} = \frac{1 -\epsilon_t}{\epsilon_t}
\]</span> 求得<span class="math inline">\(\alpha_t\)</span>的最优取值为: <span class="math display">\[
    \alpha_t^\ast = \frac{1}{2} \log \frac{1 - \epsilon_t}{\epsilon_t}
\]</span> 至此我们便理解了每一步迭代<span class="math inline">\(\alpha_t\)</span>表达式的来源，该表达式刚好可以保证最终得到的分类器错误率上界尽可能小(局部最小)。 ##### 训练误差界(二分类问题) 对于一个二分类问题，则可以将<span class="math inline">\(Z_t\)</span>的表达式直接写出: <span class="math display">\[
    Z_t = 2 \sqrt{e_t(1 - e_t)}
\]</span> 令<span class="math inline">\(e_t = \frac{1}{2} - \gamma_t\)</span>,则可得： <span class="math display">\[
    Z_t = \sqrt{1 - 4 \gamma_t^2} \leq e^{-2 \gamma_t^2}
\]</span> 由此便可得分类错误率上界为: <span class="math display">\[
    \prod_{i=1}^t Z_i \leq e^{-2 \sum_{i=1}^t \gamma_t^2}
 \]</span> 据此可以得到推论: &gt;<strong>推论：</strong> 如果存在<span class="math inline">\(\gamma &gt;0\)</span>,对所有<span class="math inline">\(i\)</span>有，<span class="math inline">\(\gamma_i \geq \gamma\)</span>,则有： <span class="math display">\[
    \frac{1}{N} \sum_{i=1}^N I(h(x_i) \neq y_i) \leq \exp(-2N\gamma^2)
\]</span> 这说明对于二分类问题，AdaBoost的训练误差是指数速率下降的。</p>
<p><strong>tips:</strong> - 对于无法接受带权样本的基学习算法，可以通过重采样法来处理，即在每一轮学习中，根据样本分布对训练集重新进行采样，变相实现带权样本，两种做法没有本质区别。 - Boosting算法在训练的每一轮都需要检查当前生成的基学习器是否满足条件(比随机猜测要好)，若不满足，则抛弃该基学习器，学习过程停止，若采用&quot;重采样法&quot;，则可获得“重启动机会”，避免算法过早停止。</p>
<h3 id="随机森林">随机森林</h3>
<p>随机森林算法其实就是以决策树算法作为基学习算法，选用Bagging策略作为提升策略，最终得到的一个算法，可以这么理解: <span class="math display">\[
    随机森林 = 决策树 + Bagging
\]</span> 随机森林算法简单、容易实现、计算开销小，令人惊奇的是，它在很多现实任务中展现了强大的性能，被誉为“代表集成学习技术水平的方法”。 不过传统的Bagging算法仅仅是在训练样本上引入随机性，而随机森林往往考虑在属性选择上引入随机性，具体做法是:原始的决策树算法是从该节点所有属性中选择一个最优属性进行节点分裂，而作为基学习器，为进一步引入随机性，一般会规定从当前节点所有属性中随机选<span class="math inline">\(k\)</span>个，然后从这<span class="math inline">\(k\)</span>个属性中选择一个最优的来分裂节点，若该节点有<span class="math inline">\(d\)</span>个属性，常见的<span class="math inline">\(k\)</span>的取值为<span class="math inline">\(k = \log_2 d\)</span></p>
<h3 id="提升算法解释">提升算法解释</h3>
<p>学习器结合可能会从三个方面带来好处: - 从统计的方面来看，由于学习任务的假设空间往往非常大，此时若使用单学习器可能因误选而导致泛化性能不佳，结合多个学习器会减少这一风险。 - 从计算角度来看，学习算法往往会陷入局部极小，有的局部极小点所对应的泛化性能可能非常糟糕，而通过多次运行之后进行结合，可降低陷入糟糕极小点的风险。 - 从表示的方面来看，某些学习任务的真实假设可能不在当前学习算法s所考虑的假设空间中，此时若使用单学习器肯定无效，而通过结合多个学习器，由于相应的假设空间有所扩大，有可能学得更好的近似。</p>
<figure>
<img src="https://raw.githubusercontent.com/xuejy19/xuejy19.github.io/source/Img/Ensemble.png" alt="集成学习解释" /><figcaption>集成学习解释</figcaption>
</figure>
<h3 id="总结">总结</h3>
<p>该部分主要总结下两个方面的内容，一个是结合策略的选择，另一个则是引入扰动的思路。 #### 结合策略 结合策略主要有以下几种: - 平均法,包括简单平均法和加权平均法，一般而言，在个体学习器性能相差较大时，加权平均法表现较好，在个体学习器性能接近时采用简单平均法较好。 - 投票法,包括绝对多数投票法(设定得票率阈值)，相对多数投票法，加权投票法 - 学习法，即通过另一个学习器来结合这些基于学习器，该学习器被称为元学习器或者次级学习器，基学习器被称作初级学习器，初级学习器的输出作为次级学习器的输入。</p>
<h4 id="引入扰动">引入扰动</h4>
<p>常见的引入扰动的思路有如下几种: - 数据样本扰动 - 输入属性扰动 - 输出表示扰动 - 算法参数扰动</p>
]]></content>
      <categories>
        <category>统计学习</category>
      </categories>
      <tags>
        <tag>Bagging, AdaBoost, 提升树模型</tag>
      </tags>
  </entry>
  <entry>
    <title>有感</title>
    <url>/2020/02/22/you-gan/</url>
    <content><![CDATA[<p>  或许逃避是人的天性，整个博一上学期，似乎整天忙忙碌碌，在若干门课程中来回斡旋，<a id="more"></a>但当闲下来细细一想，似乎也有很多时间自己并没有好好利用，用那些时间在逃避、在咸鱼、在发呆。整个博一上学期自己也读了一些书，比如《历史的教训》、《贫穷的本质》、《禅与摩托车维修技术》、《变革中国》，相比于完整读下来的书，挖的坑自然是更多，《纯粹理性批判》、《世界文明史》、《资本论》......如果你要问我为什么选择这些书，我只能回答机缘巧合吧，这些书或是来源于上课时某位同学的推荐，或是来自于自己查询某些资料时的推荐，不一而足。其实我认为读这些书一方面是一种对自己空余时间的填充，对纯粹理工科学习的一种暂时逃避，也是一个不断寻找答案的过程，寻找一些理工科的书籍里缺乏的一些答案。其实我一直在思考一个问题，一个人的强大究竟是来源于其内心的强大，还是来源于其所凭外物，当你一无所有（指外物）之时，你还能剩下什么？是没有灵魂的一具空壳，还是一个拥有强大生命的种子。   读史教会我的第一点是作为历史当中普通人的世事无常，我们所接触的历史往往是王侯将相的历史，是英雄名士的历史，若人类发展的角度来看历史，一个人的历史观总会趋于无情，这种无情我们可以理解为作为现代人的傲慢/优越，如今我们去各地旅游，惊叹于种种历史古迹，惊叹于古人的智慧，但是孰又会在意这些历史古迹下面是当时多少奴隶的血泪。这些遗迹显示了当时高超的建造技术，为后人留下了宝贵遗产，我们或可以说这是历史的进步，然而那些建造他们的奴隶们则消逝于历史的阴影之中。若我们接受这样的历史进步性说法，由此会自然引出下一个结论——历史的发展是必然伴随着牺牲的，作为现代人，我们可以一方面斥责当时的暴政，另一方面又可以欣赏到这些暴政所留下的历史遗迹，每每想到这些，总觉如鲠在喉。或许从这些历史中我们可以得出这样的经验，某些我们认为错误的做法确实促进了当时历史的发展，但是历史发展途径许多，我们应当选择让人民整体更加幸福的发展道路，唉，不过聊以自慰耳。  行路者需要背负东西，然而不能背负太多。我第一个放入背包的叫做责任，从诞生开始，我就开始与这个世界产生联系，父母、家庭、国家、人类，这些名词限定了我是我，梁启超在《最苦与最乐》中写道：“人生最苦，莫过于身上背负着一种未了的责任；人生最乐，莫过于尽责之时。“责任二字，说来轻巧，实则沉甸甸，也因责任二字，人生少了些凄清，多了些烟火；我第二个放入背包的叫做知识，在我看来，当曾经模糊看不清楚的东西在你眼前变得清晰之时，也是精神愉悦满足之时，文学史哲让你反求诸己，塑造清明的内心世界，理血工学让你更好地认识客观世界，改造客观世界，两种知识于我而言同样重要。吾之一生，有此两者，足矣。</p>
]]></content>
      <categories>
        <category>闲谈</category>
      </categories>
      <tags>
        <tag>无感而发</tag>
      </tags>
  </entry>
  <entry>
    <title>机器学习理论</title>
    <url>/2020/07/06/ji-qi-xue-xi-li-lun/</url>
    <content><![CDATA[<p>笔者近期想做一下<strong>机器学习</strong>相关理论总结<a id="more"></a>，计划划分为以下章节： &gt; * 贝叶斯理论 &gt; * 概率密度函数估计 &gt; * EM算法 &gt; * 线性判别——感知器 &gt; * 支持向量机 &gt; * 近邻法 &gt; * 特征提取与特征选择 &gt; * 神经网络 &gt; * RNN &gt; * 非监督学习 &gt; * 决策树 &gt; * 聚类</p>
<p>各章节主要包含两部分： &gt; * 理论介绍 &gt; * 代码实战</p>
]]></content>
      <categories>
        <category>统计学习</category>
      </categories>
  </entry>
  <entry>
    <title>概率分布中的距离度量</title>
    <url>/2020/08/26/gai-lu-fen-bu-zhong-de-ju-chi-du-liang/</url>
    <content><![CDATA[<p>在<span class="math inline">\(k\)</span>近邻章节中，我总结了在样本空间中常用的两种距离度量:<span class="math inline">\(L_p\)</span>距离以及马氏距离。但在一些机器学习任务中，我们期望能够得到一个分布与目标分布尽可能接近，这也就引出了一个新的问题: &gt; 如何度量两个概率分布之间的距离(差异程度)？</p>
<p>本文将介绍一些常见的概率分布之间的距离度量，按照以下结构组织: - 信息量与熵 - KL散度(相对熵) - 交叉熵 - JS散度 - 推土机理论与Wasserstein距离 <a id="more"></a></p>
<h3 id="信息量与熵">信息量与熵</h3>
<h4 id="信息量">信息量</h4>
<p>任何事件都会承载着一定的信息量，包括已经发生的事件和未发生的事件，只是它们承载的信息量有所不同： &gt; 🌰：对于两个事件：昨天下雨与明天下雨。昨天下雨这个事件，因为已经发生，既定事实，那么它的信息量就为0；而对于明天会下雨这个事件，因为未有发生，那么该事件的信息量就大。</p>
<p>从上面栗子可以看出信息量是一个与事件发生概率相关的概念，而且可以得出，事件发生的概率越小，其信息量越大。这也很好理解，因为所谓的“搞个大新闻”,也就是发生了不太可能发生的事情，&quot;大新闻&quot;则意味着该事件蕴含着更多的信息。从这个角度出发， 定义信息量: &gt; <strong>信息量:</strong> 假设<span class="math inline">\(X\)</span>是一个离散型随机变量，其取值集合为<span class="math inline">\(\mathcal{X}\)</span>，其概率分布为: &gt; <span class="math display">\[
    p(x) = P(X = x), x \in \mathcal{X}
&gt; \]</span> &gt; 则定义事件<span class="math inline">\(X = x_0\)</span>的信息量为: &gt; <span class="math display">\[
    I(x_0) = -\log(p(x_0))
&gt; \]</span></p>
<h4 id="熵">熵</h4>
<p>首先给出熵的定义： &gt; 在信息论与概率统计中，熵是表示<strong>随机变量不确定性</strong>的度量，设<span class="math inline">\(X\)</span>是一个取有限个值的离散随机变量，其概率分布为: &gt; <span class="math display">\[
    P(X = x_i) = p_i, \quad i =1,2,\dots,n
&gt; \]</span> 则随机变量<span class="math inline">\(X\)</span>的熵定义为: <span class="math display">\[
    H(X) = - \sum_{i=1}^n p_i \log p_i 
\]</span></p>
<p>有了信息量的定义后，再看关于熵的定义式可以看出，熵不过是在概率分布<span class="math inline">\(P\)</span>下对信息量求的期望，所以说熵实际上就是<strong>事件信息量的期望</strong>。 若<span class="math inline">\(p_i = 0\)</span>,则定义<span class="math inline">\(0\log 0 =0\)</span>,式中的对数一般取以2为底或者以<span class="math inline">\(e\)</span>为底，这时熵的单位被称作比特或纳特，由定义可知，熵只依赖于<span class="math inline">\(X\)</span>的分布，而与<span class="math inline">\(X\)</span>的取值无关，因此也可将<span class="math inline">\(X\)</span>的熵记作<span class="math inline">\(H(p)\)</span>,熵越大，随机变量的不确定性越大。因为<span class="math inline">\(p_i \leq 0\)</span>,由<span class="math inline">\(H(p)\)</span>函数性质(凹函数，应用拉格朗日乘子法可求最大值)可知: <span class="math display">\[
    0 \leq H(p) \leq \log n
\]</span></p>
<h3 id="kl散度相对熵">KL散度(相对熵)</h3>
<p>KL散度又称相对熵，如果我们对同一个随机变量有两个独立的概率分布<span class="math inline">\(P(x)\)</span>和<span class="math inline">\(Q(x)\)</span>，我们可以使用KL散度来衡量这两个分布的差异,下面给出KL散度的定义式: &gt; 在机器学习中，<span class="math inline">\(P\)</span>往往用来表示样本的真实分布，<span class="math inline">\(Q\)</span>用来表示模型所预测的分布，那么<span class="math inline">\(KL\)</span>散度就可以计算两个分布之间的差异，也就是Loss损失值: &gt; <span class="math display">\[
    \begin{aligned}
         D_{KL}(P||Q) &amp;= -\sum_{i=1}^n p(x_i) \log(\frac{q(x_i)}{p(x_i)}) \\
            &amp;= -\sum_{i=1}^n p(x_i) \log(q(x_i)) - (-\sum_{i=1}^n p(x_i) \log p(x_i))
    \end{aligned}
&gt; \]</span> &gt; 由于<span class="math inline">\(-\log x\)</span>为凸函数，由凸函数性质可知: &gt; <span class="math display">\[
     -\sum_{i=1}^n p(x_i) \log(\frac{q(x_i)}{p(x_i)}) \geq  \log \sum_{i=1}^n q(x_i) = 0 
&gt; \]</span> &gt; 这说明KL散度具有非负的性质，这也说明<span class="math inline">\(P(x)\)</span>与其他分布的交叉熵恒大于熵。</p>
<p>这里需要说明的是，虽然KL散度具有非负性，但其并不满足度量的另外两个性质: - 对称性 - 三角不等式</p>
<h3 id="交叉熵">交叉熵</h3>
<p>KL散度由两部分组成，第一部分是交叉熵，第二部分则是<span class="math inline">\(P(x)\)</span>的熵，<span class="math inline">\(P(x)\)</span>因为是真实的分布，所以其熵为固定值。在机器学习任务中，我们需要评估样本固有label与我们算法的预测<span class="math inline">\(predict\)</span>之间的差距，在优化过程中，我们只需关注交叉熵，所以在分类任务中，往往采用交叉熵作为loss函数。</p>
<h3 id="js散度">JS散度</h3>
<p>JS散度只是KL散度的变形，其解决了KL散度的非对称问题，其表达式如下: <span class="math display">\[
    JS(P||Q) = \frac{1}{2} KL(P(x)||\frac{P(x) + Q(x)}{2}) + \frac{1}{2}KL(Q(x)||\frac{P(x)+ Q(x)}{2})
\]</span> 若<span class="math inline">\(\log\)</span>以2为底，则JS散度的范围为<span class="math inline">\([0,1]\)</span>,若<span class="math inline">\(\log\)</span>以<span class="math inline">\(e\)</span>为底，则JS散度范围为<span class="math inline">\([0,\ln 2]\)</span>。当两个分布完全相同时取下界，当两个分布完全不同时取上界。 这也就带来了一个问题，若优化时两个分布完全没有交集时,JS散度就是一个常数(<span class="math inline">\(1\)</span>或<span class="math inline">\(\ln 2\)</span>),此时会出现梯度消失问题，导致无法进行优化。</p>
<h3 id="wasserstein距离">Wasserstein距离</h3>
<p>首先说明下KL散度与JS散度存在的问题: &gt; 若两个分布<span class="math inline">\(P,Q\)</span>完全没有重叠，则会导致KL散度无意义(<span class="math inline">\(\log 0\)</span>),而JS散度是一个常数，也就是说出现了梯度消失问题。</p>
<p>而Wasserstein距离可以一定程度上解决该问题。 #### 思想 对于两个分布<span class="math inline">\(P\)</span>和<span class="math inline">\(Q\)</span>，考虑从将一个分布转换成另一个分布所需的代价入手来定义概率分布的差异。下面以一个栗子来说明该距离的思想: &gt; 🌰: 考虑两个离散分布<span class="math inline">\(P\)</span>和<span class="math inline">\(Q\)</span>: &gt; <span class="math display">\[
    \begin{aligned}
        P_1 &amp;= 3, P_2 = 2, P_3 = 1, P_4 = 4 \\
        Q_1 &amp;= 1, Q_2 = 2, Q_3 = 4, Q_4 = 3 
    \end{aligned}
&gt; \]</span> &gt; 下面讨论应该如何&quot;移土&quot;，才能够使得两个分布相同: &gt; - 为了让<span class="math inline">\(P_1\)</span>与<span class="math inline">\(Q_1\)</span>相同，我们需要把<span class="math inline">\(P_1\)</span>的3分2到<span class="math inline">\(P_2\)</span>去，这样<span class="math inline">\(P_1\)</span>和<span class="math inline">\(Q_1\)</span>都等于1，此时<span class="math inline">\(P_2\)</span>等于4 &gt; - 同理，为了让<span class="math inline">\(P_2\)</span>和<span class="math inline">\(Q_2\)</span>相同，需要将<span class="math inline">\(P_2\)</span>的土填到<span class="math inline">\(P_3\)</span>时，这一步将从<span class="math inline">\(P_2\)</span>挖2分土填到<span class="math inline">\(P_3\)</span> &gt; - 为了让<span class="math inline">\(P_3,Q_3\)</span>相等，最后一步需要将<span class="math inline">\(Q_3\)</span>的1分土挖给<span class="math inline">\(Q_4\)</span> &gt; 每一步的代价计算公式为<span class="math inline">\(\delta_{i+1} = \delta_i + P_i - Q_i\)</span>,由此可得: &gt; <span class="math display">\[
    \begin{aligned}
        \delta_0 &amp;= 0 \\
        \delta_1 &amp;= 0 +3 -1 = 2 \\
        \delta_2 &amp;= 2 + 2 - 2 = 2 \\
        \delta_3 &amp;= 2 + 1 -4 = -1 \\
        \delta_4 &amp;= -1 + 4 - 3 = 0 
    \end{aligned}
&gt; \]</span> &gt; 所以最终的总代价，也就是Wasserstein距离为<span class="math inline">\(W = \sum |\delta_i| = 5\)</span>。该栗子可以形象的用下图表示: &gt; <img src="https://raw.githubusercontent.com/xuejy19/xuejy19.github.io/source/Img/W_dis.png" alt="推土机距离" /></p>
<h4 id="公式">公式</h4>
<p>从上面的离散栗子了解了Wasserstein距离的思想之后，下面给出一般连续分布Wasserstein距离计算公式: &gt; <span class="math display">\[
    W(p_r,p_g) = \inf_{\gamma \in \prod(p_r,p_g)} \quad E_{(x,y)\sim \gamma}[||x-y||]
&gt; \]</span> &gt; 公式中<span class="math inline">\(\prod(p_r,p_g)\)</span>指代<span class="math inline">\(p_r\)</span>和<span class="math inline">\(p_g\)</span>所有可能的联合概率分布<span class="math inline">\(\gamma(x,y)\)</span>,直观上来说，<span class="math inline">\(\gamma(x,y)\)</span>表示将分布<span class="math inline">\(p_r\)</span>转化成<span class="math inline">\(p_g\)</span>所需要多少“土”，Wasserstein指代花费最少的运输方案。</p>
<p>Wasserstein相对于传统的KL散度和JS散而言，当两个分布具有完全不同的支撑集合时，KL散度会是无穷，JS散度会是一个常值，而W距离此时仍能反映两个分布的远近。下面以一个栗子来说明Wasserstein距离的优势所在: &gt; 🌰: 变量<span class="math inline">\(Z \sim U[0,1]\)</span>,下面给出两个分布: &gt; <span class="math display">\[
    \begin{aligned}
        &amp;P_0 (x_1), \quad x_1 = (0,Z) \in R^2  \\
        &amp; P_1(x_2), \quad x_2 = (\theta,z) \in R^2
    \end{aligned}
&gt; \]</span> &gt; 对于这个简单的栗子，不同概率密度距离度量得到的数值如下: &gt; <span class="math display">\[
    \begin{aligned}
        W(P_0,P_1) &amp;= |\theta| \\ 
        JS(P_0,P_1) &amp;= \begin{cases}
             \log 2  &amp; if \theta \neq 0 \\
             0  &amp; if \theta = 0  
        \end{cases} \\
        KL(P_0||P_1) &amp;= \begin{cases}
             +\infty  &amp; if \theta \neq 0 \\
             0  &amp; if \theta = 0  
        \end{cases}
    \end{aligned}
&gt; \]</span> &gt; 在Wasserstein度量下，随着<span class="math inline">\(\theta_t \rightarrow 0\)</span>,概率分布序列<span class="math inline">\((P_{\theta_t})_{t \in N}\)</span> 收敛到<span class="math inline">\(P_0\)</span>,但在其他度量下并不能收敛，W距离与JS散度在该问题中对比如下图所示: &gt; <img src="https://raw.githubusercontent.com/xuejy19/xuejy19.github.io/source/Img/W.png" alt="W VS JS" /></p>
<h4 id="计算">计算</h4>
<p>尽管Wasserstein度量具有很多优点，但其缺点也十分明显： &gt; 计算非常复杂，优化问题不易解</p>
<p>目前能够显示计算出Wasserstein的只有两种情况： &gt; 一维概率分布 &gt; 高斯分布</p>
<p>对于两个离散的一维概率分布，可以直接使用<code>scipy.stats.Wasserstein_distance</code>函数:</p>
<div class="sourceCode" id="cb1"><pre class="sourceCode python"><code class="sourceCode python"><a class="sourceLine" id="cb1-1" data-line-number="1"><span class="im">import</span> numpy <span class="im">as</span> np</a>
<a class="sourceLine" id="cb1-2" data-line-number="2"><span class="im">from</span> scipy.stats <span class="im">import</span> wasserstein_distance</a>
<a class="sourceLine" id="cb1-3" data-line-number="3">p <span class="op">=</span> [<span class="dv">0</span>,<span class="dv">5</span>,<span class="dv">9</span>]</a>
<a class="sourceLine" id="cb1-4" data-line-number="4">q <span class="op">=</span> [<span class="dv">2</span>,<span class="dv">5</span>,<span class="dv">7</span>]</a>
<a class="sourceLine" id="cb1-5" data-line-number="5">w <span class="op">=</span> wasserstein_distance(q,p)</a>
<a class="sourceLine" id="cb1-6" data-line-number="6"><span class="bu">print</span>(w)</a></code></pre></div>
<p>输出:</p>
<div class="sourceCode" id="cb2"><pre class="sourceCode python"><code class="sourceCode python"><a class="sourceLine" id="cb2-1" data-line-number="1"><span class="fl">1.3333333333333335</span> </a></code></pre></div>
]]></content>
      <categories>
        <category>统计学习</category>
      </categories>
      <tags>
        <tag>KL散度，JS散度，推土机距离</tag>
      </tags>
  </entry>
  <entry>
    <title>概率密度函数估计</title>
    <url>/2020/07/27/gai-lu-mi-du-han-shu-gu-ji/</url>
    <content><![CDATA[<p>在使用贝叶斯理论进行决策时，我们需要知道先验概率<span class="math inline">\(P(\omega)\)</span>,类条件概率密度函数<span class="math inline">\(p(x|\omega)\)</span>,而在这一部分，我们便讨论下如何从样本集中将这些所需的概率密度函数估计出来<a id="more"></a>，本文主要讨论以下几个问题： - 如何利用样本集估算<span class="math inline">\(p(x|\omega)\)</span>和<span class="math inline">\(P(\omega)\)</span> - 估计量性质 - 利用样本集估算错误率</p>
<p>从样本集推断总体概率分布的方法可以分为以下几种类型: - 监督参数估计——样本所属的类别以及总体概率密度函数的形式为已知，而概率密度函数的某些参数是未知的 - 非监督参数估计——已知总体概率密度函数形式，但未知样本所属种类，要求推断出概率密度函数某些参数 - 非参数估计——已知样本类别，但未知总体概率密度函数形式，要求我们直接推断概率密度函数本身</p>
<h3 id="最大似然估计">最大似然估计</h3>
<p>最大似然估计是进行参数估计的常用方法，它有两个假设： - 待估计的参数<span class="math inline">\(\theta\)</span>是一个确定的、未知的量 - 概率分布函数的形式已知 - 样本为独立抽取</p>
<p>若记<span class="math inline">\(\theta\)</span>为待估计参数(比如正态分布中的<span class="math inline">\(\mu,\sigma\)</span>),<span class="math inline">\(\mathcal{X}\)</span>为样本集合： <span class="math display">\[ \mathcal{X} = \{ x_1,x_2, \dots, x_n\} \]</span> 概率密度函数形式已知，记做<span class="math inline">\(p(x|\theta)\)</span>，由于假设样本为独立抽取，则似然函数可以写做： <span class="math display">\[ l(\theta) = p(\mathcal{X}|\theta) = \prod_{i=1}^n p(x_i|\theta)\]</span> 对数似然函数为： <span class="math display">\[  H(\theta) = ln(l(\theta)) = \sum_{i=1}^n lnp(x_i|\theta)\]</span> 首先来看似然函数，它表征了在参数为<span class="math inline">\(\theta\)</span>情况下，得到样本集合<span class="math inline">\(\mathcal{X}\)</span>的概率，直观来看，这个概率越大，则证明估计得到的参数更好,至于为何使用对数似然函数，这是因为我们需要求<span class="math inline">\(l(\theta)\)</span>的最大值，但连乘积求导非常复杂，而取对数运算是单调的，并不改变最值位置，同时计算上大大简化。因此最终最大似然估计值为:</p>
<p><span class="math display">\[  \frac{\partial H(\theta)}{\theta} = 0  \]</span></p>
<p>实践中常见的概率分布为正态分布，其最大似然估计值为： <span class="math display">\[
\begin{aligned}
    \hat{\mu} &amp;= \frac{1}{n} \sum_{i=1}^n x_i \\\\
    \hat{\sigma^2} &amp;= \frac{1}{n} \sum_{i=1}^n (x_i - \hat{\mu})^2
\end{aligned}
\]</span> ### 贝叶斯估计 首先回顾下贝叶斯决策： - 状态空间:<span class="math inline">\(\Omega = \{ \omega_1, \omega_2, \dots , \omega_c \}\)</span> - 待识别对象: <span class="math inline">\(\mathbf{x} = [x_1,x_2,\dots, x_d]^T\)</span></p>
<p>设真实状态为<span class="math inline">\(\omega_j\)</span>，而采取决策<span class="math inline">\(a_i\)</span>所带来的损失为<span class="math inline">\(\lambda(\omega_j,a_i)\)</span>,对于特定<span class="math inline">\(\mathbf{x}\)</span>，采取决策<span class="math inline">\(a_i\)</span>所带来的平均损失为: <span class="math display">\[  R(a_i|\mathbf{x}) = \sum_{j=1}^c \lambda(a_i,\omega_j) p(\omega_j|x)\]</span> 整体损失则需要再对<span class="math inline">\(\mathbf{x}\)</span>进行积分，在进行贝叶斯决策时，若想使得整体损失最小，则只需要对任意的<span class="math inline">\(\mathbf{x}\)</span>，<span class="math inline">\(R(a_i|\mathbf{x})\)</span>。 下面我们考虑参数估计问题:现在有一个样本集<span class="math inline">\(\mathcal{X}\)</span>，要求我们找到最优估计量<span class="math inline">\(\hat{\theta}\)</span>(对比决策<span class="math inline">\(a_i\)</span>),使得带来的贝叶斯风险最小。由此可见，与贝叶斯决策相比，贝叶斯估计也是立足于使得贝叶斯风险最小，两者对比如下图所示：</p>
<table>
<thead>
<tr class="header">
<th>决策问题</th>
<th style="text-align: right;">估计问题</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>样本<span class="math inline">\(\mathbf{x}\)</span></td>
<td style="text-align: right;">样本集<span class="math inline">\(\mathcal{X}\)</span></td>
</tr>
<tr class="even">
<td>决策量<span class="math inline">\(a_i\)</span></td>
<td style="text-align: right;">待估计值<span class="math inline">\(\hat{\theta}\)</span></td>
</tr>
<tr class="odd">
<td>真实状态<span class="math inline">\(\omega_j\)</span></td>
<td style="text-align: right;">真实参数<span class="math inline">\(\theta\)</span></td>
</tr>
<tr class="even">
<td>状态空间<span class="math inline">\(\mathcal{A}\)</span>是离散空间</td>
<td style="text-align: right;">参数空间<span class="math inline">\(\Theta\)</span>是连续空间</td>
</tr>
<tr class="odd">
<td>先验概率<span class="math inline">\(P(\omega_j)\)</span></td>
<td style="text-align: right;">参数的先验分布<span class="math inline">\(p(\theta)\)</span></td>
</tr>
</tbody>
</table>
<p>对于样本集<span class="math inline">\(\mathcal{X}\)</span>，参数估计值为<span class="math inline">\(\hat{\theta}\)</span>所带来的平均损失为： <span class="math display">\[
    R(\hat{\theta}|\mathcal{X}) = \int_{\Theta} \lambda(\hat{\theta},\theta) p(\hat{\theta}|\mathcal{X}) d\theta
\]</span> 从上式中可以也可以看出贝叶斯决策与贝叶斯估计的区别: &gt; 贝叶斯决策是立足于每一个样本<span class="math inline">\(\mathbf{x}\)</span>，只需要保证对每一个样本均做出最优决策，便可保证整体决策最优；而贝叶斯估计则是立足于样本集<span class="math inline">\(\mathcal{X}\)</span></p>
<p>现在我们要想获得贝叶斯估计，便转化为寻找合适的估计值<span class="math inline">\(\hat{\theta}\)</span>,使得<span class="math inline">\(R(\hat{\theta}｜\mathcal{X})\)</span>最小，这需要解决两个问题： - <span class="math inline">\(p(\hat{\theta}|\mathcal{X})\)</span>计算 - 损失函数<span class="math inline">\(\lambda(\hat{\theta},\theta)\)</span>的选择</p>
<p>首先考虑<span class="math inline">\(p(\hat{\theta}|\mathcal{X})\)</span>的计算，假设<span class="math inline">\(\theta\)</span>的先验概率分布为<span class="math inline">\(p(\theta)\)</span>,类条件概率密度函数为<span class="math inline">\(p(\mathbf{x}|\theta)\)</span>,样本集<span class="math inline">\(\mathcal{X} = \{ x_1,\dots, x_n \}\)</span>,似然函数<span class="math inline">\(p(\mathcal{X}|\theta)\)</span>可以表示为： <span class="math display">\[
    p(\mathcal{X}|\theta) = \prod_{i=1}^n p(\mathbf{x_i}|\theta)
\]</span> 由贝叶斯公式，后验概率可以表示为: <span class="math display">\[
    p(\hat{\theta}|\mathcal{X}) = \frac{p(\mathcal{X}|\hat{\theta})p(\hat{\theta})}{ \int p(\mathcal{X}|\theta)p(\theta) d\theta}
\]</span> 损失函数<span class="math inline">\(\lambda(\hat{\theta},\theta)\)</span>常见的有三种选择: - 均方误差函数:<span class="math inline">\(\lambda(\hat{\theta},\theta) = (\theta - \hat{\theta})^2\)</span> - 绝对值误差函数: <span class="math inline">\(\lambda(\hat{\theta},\theta) = |\theta - \hat{\theta}|\)</span> - 均匀函数: <span class="math inline">\(\lambda(\hat{\theta}, \theta) = \begin{cases}  0 &amp; x \in [-\frac{\Delta}{2}, \frac{\Delta}{2}] \\  1 &amp; other \end{cases}\)</span></p>
<p>在不同损失函数下，可以推导出不同的结果： - 均方误差函数:<br />
<span class="math display">\[ \hat{\theta}_{MS} = \int_{-\infty}^\infty \theta p(\theta| \mathcal{X})d\theta\]</span> - 绝对值误差函数: <span class="math display">\[  \int_{-\infty}^{\hat{\theta_{ABS}}} p(\theta|\mathcal{X}) d\theta = \int_{\hat{\theta_{ABS}}}^{\infty} p(\theta|\mathcal{X}) d\theta \]</span> - 均匀误差函数: <span class="math inline">\(\theta_{MAP}\)</span>应当取在后验概率密度函数最大之处</p>
<p>下面以均方误差函数为例，假设: - <span class="math inline">\(p(x|\mu) \sim N(\mu, \sigma^2)\)</span> - <span class="math inline">\(p(\mu) \sim N(\mu_0,\sigma_0^2)\)</span></p>
<p>则后验概率密度函数<span class="math inline">\(p(\theta|\mathcal{X})\)</span>为: <span class="math display">\[
\begin{aligned}
    p(\mu|\mathcal{X}) &amp;= \frac{p(\mathcal{X}|\mu)p(\mu)}{\int p(\mathcal{X}|\mu)p(\mu)d\mu}  \\\\
    &amp;= a exp\{ -\frac{1}{2}[\sum_{k=1}^N (\frac{\mu - x_k}{\sigma})^2 + (\frac{\mu - \mu_0}{\sigma_0})^2] \}
\end{aligned}
\]</span> 由于该概率密度函数是<span class="math inline">\(\mu\)</span>的二次函数指数形式，所以仍是一个正态密度，其均值和方差为： <span class="math display">\[
\begin{aligned}
    \mu_N &amp;= \frac{N\sigma_0^2}{N\sigma_0^2 + \sigma^2} \frac{1}{N}\sum_{k=1}^N x_k + \frac{\sigma^2}{N\sigma_0^2 + \sigma^2} \mu_0  \\\\
    \sigma_N^2 &amp;= \frac{\sigma_0^2 \sigma^2}{N\sigma_0^2 + \sigma^2}
\end{aligned}
\]</span> 因为误差函数为均方误差函数，则其参数估计值应当是后验概率密度意义上的均值，即<span class="math inline">\(\mu_N\)</span> ### 非参数估计 在使用最大似然估计或者贝叶斯估计进行参数估计时，需要对类条件概率密度函数<span class="math inline">\(p(\mathbf{x}|\omega)\)</span>形式已知，但在某些情况下，我们仅仅拥有样本，但并没有这些先验知识，此时若想要进行概率密度函数的估计，则就用到了非参数估计的方法，该类方法具有以下几点假设： - 不知道分布函数形式 - <span class="math inline">\(N\)</span>个样本服从独立同分布</p>
<p>假设样本点<span class="math inline">\(\mathbf{x}\)</span>落在<span class="math inline">\(d\)</span>维空间中，在<span class="math inline">\(d\)</span>维空间中有一个超立方体<span class="math inline">\(\mathcal{R}\)</span>,点<span class="math inline">\(\mathbf{x}\)</span>落到该超立方体中的概率记做<span class="math inline">\(P\)</span>: <span class="math display">\[
    P = \int_{\mathcal{R}} p(x) dx 
\]</span> 若<span class="math inline">\(N\)</span>个样本是从密度为<span class="math inline">\(p(x)\)</span>的总体中独立抽取的，则<span class="math inline">\(N\)</span>个样本中有<span class="math inline">\(k\)</span>个落入区域<span class="math inline">\(\mathcal{R}\)</span>中的概率符合二项分布，该概率可以写做： <span class="math display">\[
    P_k = C_N^k P^k(1-P)^{N-k}
\]</span> 根据二项分布的性质，<span class="math inline">\(k\)</span>的期望为： <span class="math display">\[
    E(k) = NP 
\]</span> 我们的目的是想获得关于<span class="math inline">\(P\)</span>的估计值，因此考虑： <span class="math display">\[ \hat{P} = \frac{k}{N}\]</span> 若超立方体<span class="math inline">\(\mathcal{R}\)</span>很小，则可以假设在该超球内概率密度函数<span class="math inline">\(p(x)\)</span>为定值，此时： <span class="math display">\[
    P = \int_{\mathcal{R}} p(x) dx  = p(x)V 
\]</span> 由此我们便可以获得<span class="math inline">\(p(x)\)</span>的估计值： <span class="math display">\[
    \hat{p}(x) = \frac{P}{V} = \frac{k}{NV}
\]</span> 下面讨论下体积<span class="math inline">\(V\)</span>的大小及样本数量多少之间的关系。如果把体积<span class="math inline">\(V\)</span>固定，样本数取得越来越多，则比值<span class="math inline">\(\frac{k}{N}\)</span>将在概率上收敛，最终得到的<span class="math inline">\(p(x)\)</span>估计是在超立方体<span class="math inline">\(\mathcal{R}\)</span>上的平均估计，而若想要得到<span class="math inline">\(p(x)\)</span>的点估计，则需要区间尽可能小，但区间取得过小又会导致一些区间内没有样本点，这种估计也是没有意义的。 下面给出概率密度函数估计值<span class="math inline">\(p_N(x)\)</span>收敛于真实概率密度函数<span class="math inline">\(p(x)\)</span>的条件： - <span class="math inline">\(\lim_{N \rightarrow \infty} V_N = 0\)</span> - <span class="math inline">\(\lim_{N \rightarrow \infty} k_N = \infty\)</span> - <span class="math inline">\(\lim_{N \rightarrow \infty} \frac{k_N}{N} = 0\)</span></p>
<p>第一个条件可以使空间平均<span class="math inline">\(\frac{P}{V}\)</span>收敛于<span class="math inline">\(p(x)\)</span>,第二个条件可以使频数比在概率意义上收敛于概率<span class="math inline">\(P\)</span>。满足以上三个条件的区域序列的选择有两种： - Parzen窗法： 使区域序列<span class="math inline">\(V_N\)</span> 以<span class="math inline">\(N\)</span>的某个函数的关系不断缩小，但此时需要对<span class="math inline">\(k_N\)</span>和<span class="math inline">\(\frac{k_N}{N}\)</span>加些限制条件 - <span class="math inline">\(k_N\)</span>近邻估计，让<span class="math inline">\(k_N\)</span>为<span class="math inline">\(N\)</span>的某个函数，而<span class="math inline">\(V_N\)</span>的选取是使相应的<span class="math inline">\(\mathcal{R_N}\)</span>正好包含<span class="math inline">\(x\)</span>的<span class="math inline">\(k_N\)</span>个近邻。</p>
<h4 id="parzen窗法">Parzen窗法</h4>
<p>假设<span class="math inline">\(\mathcal{R_N}\)</span>为一个超立方体，其棱长为<span class="math inline">\(h_N\)</span>,则此时超立方体体积<span class="math inline">\(V_N = h_N^d\)</span>,定义窗函数<span class="math inline">\(\phi(\mu)\)</span>： <span class="math display">\[
    \phi(\mu) = \begin{cases}
        1, if |\mu_j| \leq \frac{1}{2} \\
        0, other
    \end{cases}
\]</span> 引入窗函数的的目的主要是对落入区间的样本数<span class="math inline">\(k\)</span>进行显示表达，当<span class="math inline">\(x_i\)</span>落入以<span class="math inline">\(x\)</span>为中心，体积为<span class="math inline">\(V_N\)</span>的超立方体内时，<span class="math inline">\(\phi(\mu) = \phi(\frac{x-x_i}{h_N}) = 1\)</span>，否则为0，此时落入该超立方体内的样本数为： <span class="math display">\[
    k_N = \sum_{i=1}^N \phi(\frac{x-x_i}{h_N})
\]</span> 由此便可以得到概率密度函数估计： <span class="math display">\[
    p_N(x) = \frac{k_N}{V_NN} = \frac{1}{N} \sum_{i=1}^N \frac{1}{V_N} \phi(\frac{x-x_i}{h_N})
\]</span> 事实上，窗函数的主要作用是内插，反映了每一样本<span class="math inline">\(x_i\)</span>对<span class="math inline">\(x\)</span>点处的概率密度函数的贡献 下面我们讨论下，若要使得<span class="math inline">\(p_N(x)\)</span>满足概率密度函数特性(在整个空间上积分为1),则窗函数应当具有什么特性: <span class="math display">\[
    \begin{aligned}
          \int p_N(x)dx &amp;= \frac{1}{NV_N} \int \sum_{i=1}^N\phi(\frac{x-x_i}{h_N})dx  \\\\
          &amp;= \frac{1}{N} \sum_{i=1}^N \int \phi(\mu)d\mu \\\\
          &amp; = 1
    \end{aligned}
\]</span> 因此考虑到<span class="math inline">\(p_N(x)\)</span>非负且积分为1,则窗函数应当具备一般概率密度函数所具有的特征： - $() 0 $ - <span class="math inline">\(\int \phi(\mu) d\mu = 1\)</span></p>
<p>常见的窗函数有以下三种： - 方窗函数 - 正态窗函数:<span class="math inline">\(\phi(\mu) = \frac{1}{\sqrt{2\pi}} exp(-\frac{1}{2}\mu^2)\)</span> - 指数窗函数:<span class="math inline">\(\phi(\mu) = \frac{1}{2} exp(-|\mu|)\)</span></p>
<p>窗宽<span class="math inline">\(h_d\)</span>的选择非常重要，若<span class="math inline">\(h_d\)</span>选择过大，则会使得估计的分辨力降低，若选的过小，又会导致估计的稳定性下降，反映在估计得到的概率密度函数上就是毛刺很多，一般需要折中考虑。 下面讨论估计量<span class="math inline">\(p_N(x)\)</span>的统计性质，可以证明在某些限制条件下，估计量是渐进无偏和平方误差一致的，限制条件为: - 总体密度函数<span class="math inline">\(p(x)\)</span>在<span class="math inline">\(x\)</span>点连续 - 窗函数满足以下条件： <span class="math display">\[ \phi(\mu) \geq 0, \int \phi(\mu) d\mu = 1, sup_{u} \phi(\mu)&lt;\infty\]</span> - 窗宽满足: <span class="math display">\[ \lim_{N \rightarrow \infty} V_N= 0,\lim_{N \rightarrow \infty} NV_N = \infty \]</span></p>
<h4 id="k_n近邻估计"><span class="math inline">\(k_N\)</span>近邻估计</h4>
<p>parzen窗估计是对体积序列进行限制，而<span class="math inline">\(k_N\)</span>近邻估计则是对<span class="math inline">\(k_N\)</span>序列做出限制</p>
<h3 id="概率密度函数估计的准确性与分类器性能的关系">概率密度函数估计的准确性与分类器性能的关系</h3>
<p>因为概率密度函数的估计最终还是需要服务于一些下游任务，比如应用贝叶斯决策进行分类，因此这部分主要讨论分类器的误差来源，误差来源主要有以下几种： - 贝叶斯误差：这种误差是由于不同的类条件概率分布函数之间的相互重叠引起的，这种误差是问题本身所固有的，在分类器设计阶段无法消除 - 模型误差：指因为选择了不正确的模型导致的误差 - 估计误差： 由于采用有限样本进行估计所带来的误差</p>
]]></content>
      <categories>
        <category>统计学习</category>
      </categories>
  </entry>
  <entry>
    <title>线性判别分析</title>
    <url>/2020/09/01/te-zheng-ti-qu-yu-te-zheng-xuan-ze/</url>
    <content><![CDATA[<p>线性判别分析(LDA)是一种经典的线性学习方法，在二分类问题上最早由fisher提出，因此线性判别分析又称Fisher线性判别，该部分按照以下结构组织: - LDA算法思想 - LDA算法推导 - 多分类任务</p>
<a id="more"></a>
<h3 id="算法思想">算法思想</h3>
<p>LDA的思想非常朴素：给定训练样例集，设法将样例投影到一条直线上，使得同类样例的投影点尽可能接近、异类样例的投影点尽可能远离，在对新的样本进行分类时，首先将其投影到同样的这条直线上，再根据投影点的位置来确定新样本的类别。 <img src="https://raw.githubusercontent.com/xuejy19/xuejy19.github.io/source/Img/LDA.png" alt="LDA" /></p>
<h3 id="理论推导">理论推导</h3>
<h4 id="投影方向求解">投影方向求解</h4>
<p>假设共有<span class="math inline">\(N\)</span>个样本点<span class="math inline">\(x_1,x_2,\dots,x_N\)</span>,其中有<span class="math inline">\(N_1\)</span>个样本属于类别1，<span class="math inline">\(N_2\)</span>个样本属于类别2，<span class="math inline">\(N_1 + N_2 = N\)</span>, 记第<span class="math inline">\(i\)</span>类样本点的均值为<span class="math inline">\(\mu_i\)</span>,协方差矩阵为<span class="math inline">\(\Sigma_i\)</span>,则有： <span class="math display">\[
    \begin{aligned}
        \mu_i &amp;= \frac{1}{N_i} \sum_{x_i \in \mathcal{C_i}} x_i \\
        \Sigma_i &amp;= \frac{1}{N_i} \sum_{x_i \in \mathcal{C_i}} (x_i - \mu_i)(x_i - \mu_i)^T
    \end{aligned} 
\]</span> 当将这些样本点向<span class="math inline">\(w\)</span>投影后，投影后得到的点为<span class="math inline">\(\tilde{x}_i = w^T x\)</span>,记此时类别<span class="math inline">\(i\)</span>的均值为<span class="math inline">\(\tilde{\mu}_i\)</span>,方差为<span class="math inline">\(\sigma^2_i\)</span>,由均值方差计算公式可得: <span class="math display">\[
    \begin{aligned}
        \tilde{\mu}_i &amp;= \frac{1}{N_i} \sum_{x_i \in \mathcal{C_i}} \tilde{x}_i = w^T \frac{1}{N} \sum_{x_i \in \mathcal{C_i}}  x_i = w^T \mu_i  \\
        \sigma^2_i &amp;= \frac{1}{N_i} \sum_{x_i \in \mathcal{C_i}}(\tilde{x}_i - \tilde{\mu
        }_i)^2 = \frac{1}{N_i} \sum_{x_i \in \mathcal{C_i}} (w^T x_i - w^T \mu_i)^2 \\
                &amp;= w^T [\frac{1}{N_i} \sum_{x_i \in \mathcal{C_i}} (x_i - \mu_i)(x_i - \mu_i)^T] w \\
             &amp;= w^T \Sigma_i w
    \end{aligned}
\]</span> LDA有两个目标，一个是同类的点投影后尽可能接近，另一个目标则是不同类的点尽可能远离： - 第一个目标我们通过投影后各类点的方差来进行刻画，使同类点的方差尽可能小: <span class="math display">\[
    \min \quad w^T \Sigma_1 w + w^T \Sigma_2 w
\]</span> - 第二个目标通过不同类的均值尽可能远来进行刻画,使不同类的均值尽可能远: <span class="math display">\[
    \max \quad (w^T \mu_1 - w^T \mu_2)^2 = w^T (\mu_1 - \mu_2)(\mu_1 - \mu_2)^T w
\]</span></p>
<p>这两个目标可以通过一个目标函数来刻画： <span class="math display">\[
    \max \quad \frac{w^T (\mu_1 - \mu_2)(\mu_1 - \mu_2)^T w}{w^T \Sigma_1 w + w^T \Sigma_2 w }
\]</span> 下面做如下标记,记<span class="math inline">\(S_w = \Sigma_1 + \Sigma_2\)</span>为类内离散度矩阵，记<span class="math inline">\(S_b = (\mu_1 - \mu_2)(\mu_1 - \mu_2)^T\)</span> 为类间离散度矩阵，由此便得目标函数形式为: <span class="math display">\[
    \max \quad \frac{w^T S_b w}{w^T S_w w}
\]</span> 这是一个广义瑞利熵形式的优化问题，因为分子分母均是<span class="math inline">\(w\)</span>的二次项，因此该优化问题的解仅与<span class="math inline">\(w\)</span>的方向有关(因为用<span class="math inline">\(cw\)</span> 替代<span class="math inline">\(w\)</span>并不影响目标函数的值)，不失一般性，可以限制<span class="math inline">\(w^T S_w w = 1\)</span>，上述优化问题可以写为: <span class="math display">\[
    \max \quad w^T S_b w  \quad s.t. \quad w^T S_w w = 1
\]</span> 该优化问题可以通过拉格朗日乘子法来进行求解,首先写出拉格朗日函数： <span class="math display">\[
    L(w,\lambda) = w^T S_b w - \lambda (w^T S_w w - 1)
\]</span> 上式对<span class="math inline">\(w\)</span>求导可得: <span class="math display">\[
    \frac{\partial L(w, \lambda)}{\partial w} = S_b w - \lambda S_w w
\]</span> 令偏导等于0，可得<span class="math inline">\(w\)</span>应当满足条件: <span class="math display">\[
    S_b w = \lambda S_w w
\]</span> 其中<span class="math inline">\(S_b w = (\mu_1 - \mu_2)(\mu_1 - \mu_2)^Tw = c(u_1 - u_2)\)</span>,又因为前面讨论过优化目标的取值仅仅与<span class="math inline">\(w\)</span>的方向有关，因此不妨取<span class="math inline">\(c = \lambda\)</span>,可得: <span class="math display">\[
    \mu_1 - \mu_2 = S_w w
\]</span><br />
因此可得最终<span class="math inline">\(w\)</span>最优取值: <span class="math display">\[w^\ast = S_w^{-1}(\mu_1 - \mu_2)\]</span> 这里需要说明一下，因为从前面我们可以推出这样一个表达式<span class="math inline">\(S_w^{-1} S_b w = \lambda w\)</span>,这说明<span class="math inline">\(w\)</span>应当是矩阵<span class="math inline">\(S_w^{-1} S_b\)</span>的特征值，目标函数最大值应当是该矩阵的最大的特征值，最优<span class="math inline">\(w\)</span>应当是该最大特征值所对应的特征向量，但后面求解为何没有没有从这个角度入手，这是该问题的特殊性，为解释这一点，首先看<span class="math inline">\(S_b\)</span>表达式: <span class="math display">\[
    S_b = (\mu_1 - \mu_2)(\mu_1 - \mu_2)^T 
\]</span> 这说明矩阵<span class="math inline">\(S_b\)</span>的秩等于1(<span class="math inline">\(\mu_1 \neq \mu_2\)</span>),所以<span class="math inline">\(S_w^{-1}S_b\)</span>秩也为1，这说明该矩阵有且仅有一个大于0的特征值，前面的推导相当于是构造性的给出了该最大特征值及对应特征向量表达式,将<span class="math inline">\(w^\ast\)</span>代入<span class="math inline">\(S_w^{-1}S_b w\)</span>: <span class="math display">\[
    \begin{aligned}
         S_w^{-1} S_b w^\ast &amp;= S_w^{-1} (\mu_1 - \mu_2)(\mu_1 - \mu_2)^T S_w^{-1}(\mu_1 - \mu_2) \\
         &amp;= C S_w^{-1}(\mu_1 -\mu_2)
    \end{aligned}
\]</span> 其中$C = (_1 - _2)<sup>TS_w</sup>{-1}(_1 - _2) &gt;0 <span class="math inline">\((\)</span>S_w<span class="math inline">\(对称且可逆，推出其正定)，这说明\)</span>S_w<sup>{-1}S_b<span class="math inline">\(的最大特征值就是\)</span>C<span class="math inline">\(,对应的特征向量刚好是\)</span>w</sup><span class="math inline">\(! 考虑到数值的稳定性，在实践中往往会首先对\)</span>S_w<span class="math inline">\(进行特征值分解\)</span>S_w = U U^T$,然后再求逆。</p>
<h4 id="在线分类">在线分类</h4>
<p>在得到最佳投影向量后，需要设定一个策略，来对新来的样本进行分类，该策略选择通常有以下几种: - 设定阈值，阈值选择通常有以下两种: - 均值取平均 <span class="math display">\[t = \frac{\tilde{\mu}_1 + \tilde{\mu}_2}{2}\]</span> - 根据样本点个数进行加权 <span class="math display">\[ t = \frac{N_2 \tilde{\mu}_1 + N_1 \tilde{\mu}_2}{N_1 + N_2}\]</span> - 在一维上估计概率密度函数，用Bayes决策方法分类 ### 多分类任务 LDA 也可以推广到多分类任务中，假定存在<span class="math inline">\(N\)</span>类，且第<span class="math inline">\(i\)</span>类的样本数为<span class="math inline">\(m_i\)</span>,首先定义全局散度矩阵: <span class="math display">\[
    S_t = S_w + S_b = \sum_{i=1}^m(x_i - \mu)(x_i - \mu)^T 
\]</span> 其中<span class="math inline">\(\mu\)</span> 是所有样本点的均值，将类内离散度矩阵定义为每个类别的离散度矩阵之和: <span class="math display">\[
    S_w = \sum_{i=1}^N S_{w_i}, \quad S_{w_i} = \sum_{x \in \mathcal{X_i}} (x - \mu_i)(x - \mu_i)^T
\]</span> 然后求出类间离散度矩阵表达式: <span class="math display">\[
    S_b = S_t -S_w = \sum_{i=1}^N m_i(\mu_i - \mu)(\mu_i - \mu)^T
\]</span> 优化目标函数可以写成: <span class="math display">\[
    \max_{W} \frac{tr(W^T S_b W)}{tr(W^T S_w W)}
\]</span> 该问题的求导与前面一样，同样根据<span class="math inline">\(W\)</span>的尺度不变性将上述优化问题转化为： <span class="math display">\[
    \max_{W} \quad tr(W^T S_b W) \quad s.t. tr(W^T S_w W) = 1
\]</span> 应用拉格朗日乘子法，并对<span class="math inline">\(W\)</span>求导令导数为0可得: <span class="math display">\[
    S_b W = \lambda S_w W
\]</span> <span class="math inline">\(W\)</span>的闭式解则是<span class="math inline">\(S_w^{-1} S_b\)</span>的<span class="math inline">\(d&#39;\)</span>个最大非零广义特征值所对应的特征向量组成的矩阵，需要注意的是最终降的维数应当满足<span class="math inline">\(d&#39; \leq N-1\)</span>,这是因为矩阵<span class="math inline">\(S_b\)</span>本身最大只有<span class="math inline">\(N-1\)</span>维，因此<span class="math inline">\(S_w^{-1}S_b\)</span>至多有<span class="math inline">\(N-1\)</span>个非零特征值。 若将<span class="math inline">\(W\)</span>视为一个投影矩阵，则多分类LDA相当于将样本投影到<span class="math inline">\(d&#39;\)</span>空间，<span class="math inline">\(d&#39;\)</span>通常远小于数据原有的属性数<span class="math inline">\(d\)</span>,于是可以通过这个投影来达到讲维的目的，同时这个过程中也应用了类别的信息，因此<span class="math inline">\(LDA\)</span>也常被视为一种经典的监督降维技术。</p>
]]></content>
      <categories>
        <category>统计学习</category>
      </categories>
      <tags>
        <tag>线性判别分析</tag>
      </tags>
  </entry>
  <entry>
    <title>疫中居家</title>
    <url>/2020/02/17/yi-zhong-ju-jia/</url>
    <content><![CDATA[<blockquote>
<p>庚子年春，适新冠肆虐，余宅家无所事，聊以为文，以抒心意</p>
</blockquote>
<p> 新冠者，源于蝠属，起于武汉，适逢春运，以至蔓于天下，一时人心浮动，谈冠色变。遥忆己亥岁末，余初闻新冠，以其癣疥之疾，然须臾，武汉闭城，举国百姓皆锁足于室，医者仁心，挽袖前线。  余宅家，手机为伴，藉网络以察新冠其事，或感动，或痛心，或苦笑。新冠者，镜也，映崇高精神，映无用之官，映复杂人心。余窃以为，此诚危急之秋也，宜上下一下，共度难关，则疫情之终，不日将至矣。  宅家不可碌碌终日，会余察己日本史所知甚浅，故阅日本史数册以会其意。以吾观之，詹姆斯之《日本史》堪称佳作，然其古史不足，实为大憾；吴廷璆之《日本史》内容翔实，然书中偏见少许，是其瑕疵。纵观日本史，源义经、楠木正成者，皆兵败身殒，然以其悲情命运而传美名于后世；丰臣秀吉者，生前位极人臣，然其后嗣绝于家康之手；德川家康者，韬光养晦，创江户幕府基业，死后被视为神（东照大神），然200余年后，倒幕声起，幕府轰然倒地，故所谓千秋万代者，实为儿戏之言。历史之回响，不过“诸事无常”四字耳。  愿新冠之终结，医者之凯旋，天下之安康</p>
]]></content>
      <categories>
        <category>闲谈</category>
      </categories>
      <tags>
        <tag>疫中有感</tag>
      </tags>
  </entry>
  <entry>
    <title>线性判别函数</title>
    <url>/2020/08/11/xian-xing-pan-bie-han-shu/</url>
    <content><![CDATA[<p>在这一部分将总结一下关于感知机的一些相关知识点，不过首先我想要介绍下统计学习相关算法的一种分类标准。该部分内容将按照以下几部分进行组织： &gt; - 模型分类-生成式模型与判别式模型 &gt; - 线性判别-感知机</p>
<a id="more"></a>
<h3 id="模型分类-生成式模型与判别模型">模型分类-生成式模型与判别模型</h3>
<p>监督学习的任务便是学习一个模型，应用这一模型，对给定的输入<span class="math inline">\(X\)</span>预测相应的输出<span class="math inline">\(Y\)</span>，该模型一般具有两种形式： - <span class="math inline">\(Y = f(X)\)</span> - <span class="math inline">\(P = (Y|X)\)</span></p>
<p>第一种模型我们称为判别模型，是函数形式；后一种我们称为生成模型，为条件概率分布的形式。下面列一个表格来对常见机器学习算法按照该标准进行分类：</p>
<table>
<thead>
<tr class="header">
<th>生成式模型</th>
<th>判别式模型</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>朴素贝叶斯模型,HMM,GMM</td>
<td>感知机,SVM,k近邻,AdaBoost</td>
</tr>
</tbody>
</table>
<h3 id="线性判别-感知机">线性判别-感知机</h3>
<p>感知机模型是一种非常简单的判别式模型，该算法出发点有以下几点： - 二分类问题 - 假设存在一个超平面，可以将两类样本分开，即要求模型线性可分</p>
<p>对于一个判别式模型，我们需要解决三个问题： 1. 明确模型类 2. 确定准则函数 3. 利用准则函数，结合数据进行模型学习</p>
<h4 id="模型类">模型类</h4>
<p>首先给出感知机函数定义： &gt; 假设输入空间(特征空间)是<span class="math inline">\(\mathcal{X} \subseteq R^n\)</span>,输出空间是<span class="math inline">\(\mathcal{Y} = \{-1,+1\}\)</span>。输入<span class="math inline">\(x\in \mathcal{X}\)</span>表示实例的特征向量，对应于输入空间(特征空间)的点；输出<span class="math inline">\(y \in \mathcal{Y}\)</span>表示实例的类别。由输入空间到输出空间的函数可表示为: <span class="math display">\[
    f(x) = sign(\omega x+ b)
\]</span></p>
<p>感知机是一种线性分类模型，属于判别模型。感知机模型的假设空间是定义在特征空间中的所有线性分类模型或线性分类起，即函数集合<span class="math inline">\(\{ f|f(x) = \omega x + b\}\)</span>。</p>
<h4 id="准则函数损失函数">准则函数(损失函数)</h4>
<p>损失函数的一个自然选择是误分类点个数。但是这样的损失函数不是参数<span class="math inline">\(\omega,b\)</span>的连续可导函数，不易优化。损失函数的另一个选择则是误分类点到超平面的总距离，为此先写出输入空间<span class="math inline">\(R^n\)</span>中任意一点<span class="math inline">\(x_0\)</span>到超平面<span class="math inline">\(S\)</span>的距离： <span class="math display">\[
    d= \frac{|\omega x_0 +b|}{|| \omega ||}
\]</span> 其次，对于误分类的数据<span class="math inline">\((x_i,y_i)\)</span>来说: <span class="math display">\[
    -y_i(\omega \cdot x_i + b) &gt;0 
\]</span> 因此，误分类点<span class="math inline">\(x_i\)</span>到超平面的距离为： <span class="math display">\[
    \frac{-y_i(\omega x_i +b)}{|| \omega ||}
\]</span> 因此，若假设超平面<span class="math inline">\(S\)</span>的误分类点集合为<span class="math inline">\(M\)</span>,那么所有误分类点到超平面<span class="math inline">\(S\)</span>的距离为： <span class="math display">\[
    L = -\frac{1}{||\omega||} \sum_{x_i \in M} y_i (\omega x_i + b)
\]</span></p>
<p>由此可见，在该目标函数下，若所有样本均被正确分类，则损失函数为0,因此不管是带范数的几何间隔，还是去掉范数的函数间隔，最终都将收敛到0，为计算方便，因此考虑将损失函数中的<span class="math inline">\(||\omega||\)</span>去掉，写出目标函数如下： <span class="math display">\[
    L(\omega,b) = -\sum_{x_i \in M} y_i (\omega x_i + b)
\]</span></p>
<h4 id="优化算法">优化算法</h4>
<p>在目标函数确定的情况下，对于该问题的优化考虑采用随机梯度下降法(SGD),首先求的目标函数对<span class="math inline">\(\omega, b\)</span>的偏导： <span class="math display">\[
    \begin{aligned}
        \nabla_{\omega} L(\omega,b) &amp;= -\sum_{x_i \in M}y_i x_i \\\\
        \nabla_{b} L(\omega,b) &amp;= -\sum_{x_i \in M} y_i
    \end{aligned}
\]</span> 接下来对参数进行更新: <span class="math display">\[
    \begin{aligned}
         \omega &amp;\leftarrow \omega + \eta y_i x_i  \\\\
         b &amp;\leftarrow b + \eta y_i
    \end{aligned} 
\]</span></p>
<h4 id="感知机算法收敛性">感知机算法收敛性</h4>
<p>在该部分将给出定理，但不加证明： &gt; 设训练数据集是线性可分的，则： &gt; - 存在满足条件<span class="math inline">\(||\omega|| = 1\)</span>的超平面将训练数据集完全分开，且存在<span class="math inline">\(\gamma &gt;0\)</span>,对所有<span class="math inline">\(i=1,\dots,N\)</span>: <span class="math display">\[
    y_i\omega x_i \geq \gamma
\]</span> &gt; - 令<span class="math inline">\(R = max ||x_i||\)</span>,则感知机算法在训练集上的误分类次数<span class="math inline">\(k\)</span>满足不等式: <span class="math display">\[
    k \leq (\frac{R}{\gamma})^2
\]</span></p>
<p>需要注意的是，当数据线性不可分时，感知机算法便不会收敛，当训练集线性可分时，感知机算法存在无穷多个解，其解由于不同的初值或者迭代顺序而可能有所不同</p>
]]></content>
      <categories>
        <category>统计学习</category>
      </categories>
      <tags>
        <tag>线性判别,感知机</tag>
      </tags>
  </entry>
  <entry>
    <title>规则学习</title>
    <url>/2020/08/27/gui-ze-xue-xi/</url>
    <content><![CDATA[<p>首先讲一个有意思的现象，早期的主流人工智能专注于以逻辑为基础来进行形式化和推理，但这样很难定量地对不确定事件进行表达和处理，后来随着机器学习算法的井喷，大家都更加关注于对定量的数据进行处理。但现在，很多人发现在解决某些任务的时候，加入一些行业中的常识知识可能会让任务解决地更好，而现在定性知识与定量数据之间似乎有着一道鸿沟，唯一的联系就是进行算法设计的人可能会依赖于他的领域知识来选择合适的算法并对算法进行调参。基于学习下定性知识如何表达的想法，这部分介绍下周志华老师🍉书的倒数第二章——规则学习，这部分内容安扎以下结构组织: - 基本概念 - 序贯覆盖 - 剪枝优化 - 一阶规则学习 - 归纳逻辑程序设计</p>
<a id="more"></a>
<h3 id="基本概念">基本概念</h3>
<p>首先给出机器学习中规则和规则学习的概念： &gt; <strong>规则:</strong> 在机器学习领域中，“规则”通常是指语义明确、能描述数据分布所隐含的客观规律或领域概念，可以写成&quot;若....,则....&quot;形式的逻辑规则。 &gt; <strong>规则学习:</strong> “规则学习”是从训练数据中学习出一组能用于对未见示例进行判别的规则。</p>
<p>形式化来看，一条规则形如： <span class="math display">\[
    \oplus \leftarrow f_1 \land f_2 \dots \land f_L
\]</span> 公式中<span class="math inline">\(\leftarrow\)</span>右侧部分称为&quot;规则体&quot;，表示该条规则的前提，左边部分称为&quot;规则头&quot;,表示该条规则的结果。规则体是由逻辑文字<span class="math inline">\(f_k\)</span>所组成的合取式(conjuction)，每个文字<span class="math inline">\(f_k\)</span>都是对示例属性进行检验的布尔表达式，例如&quot;(色泽 = 乌黑)&quot;或者&quot;<span class="math inline">\(\neg\)</span>(根蒂 = 硬挺)&quot;。<span class="math inline">\(L\)</span>是规则体中逻辑文字的个数,称为规则的长度,规则头的&quot;<span class="math inline">\(\oplus\)</span>&quot;,同样是逻辑文字，一般用来表示所判定的目标的类别或概念，比如&quot;好瓜&quot;，这样的逻辑规则也被称为<strong>if-then规则</strong>。 与神经网络、支持向量机这样的&quot;黑箱模型&quot;相比，规则学习具有以下几个优点: - 可解释性较强 - 很强的表达能力，绝大多数的人类知识都能够通过数理逻辑进行简洁的刻画和表达，例如&quot;父亲的父亲是爷爷&quot;这样的知识不易用函数式表达，而用一阶逻辑则可以方便地写为： <span class="math display">\[
    爷爷(X,Y) \leftarrow 父亲(X,Z) \land 父亲(Z,Y)
\]</span> - 逻辑规则的抽象描述能力在处理一些高度复杂的AI任务时具有显著的优势，例如在问答系统中有时可能遇到非常多、甚至无穷种可能的答案，此时若能基于逻辑规则进行抽象表述或者推理，则将带来极大的便利。</p>
<p>假设我们从西瓜数据集中学得规则集合<span class="math inline">\(\mathcal{R}\)</span>: <span class="math display">\[
    \begin{aligned}
        &amp;规则1: 好瓜 \leftarrow (根蒂 = 蜷缩) \land (脐部 = 凹陷) \\
        &amp;规则2: \neg好瓜 \leftarrow (纹理 = 模糊)
    \end{aligned}
\]</span> 规则集合中的每条规则都可看作一个子模型，规则集合是这些子模型的一个集成。当同一个示例被判别结果不同的多条规则覆盖时，则称发生了“冲突”，解决冲突的办法则称为&quot;冲突消解&quot;，常用的冲突消解的方法有投票法、排序法、元规则法，投票法就是常用的少数服从多数策略，排序法则是指定规则优先级，出现冲突时使用排序靠前的规则。所谓元规则，其实就是规则的规则，例如&quot;发生冲突时使用长度最小的规则“，然后根据元规则的指导来使用规则集。 此外，从训练集学得的规则集合也许并不能覆盖所有可能的未见示例，此时规则学习算法通常会设置一条默认规则，由它来处理规则集合未覆盖的样本；例如为<span class="math inline">\(\mathcal{R}\)</span>增加一条默认规则：“未被规则1,2覆盖的都不是好瓜”。 从形式语言表达能力而言，规则可分为两类：“命题规则”和“一阶规则”： &gt; - 命题规则是由“原子命题(propositional atom)”和逻辑连接词“与(<span class="math inline">\(\land\)</span>)”、&quot;(或(<span class="math inline">\(\lor\)</span>))&quot;、“非(<span class="math inline">\(\neg\)</span>)”和“蕴含”(<span class="math inline">\(\leftarrow\)</span>)构成的简单陈述句；例如规则集<span class="math inline">\(\mathcal{R}\)</span>就是一个命题规则集，&quot;根蒂 = 蜷缩&quot;，“脐部 = 凹陷”都是元子命题。 &gt; - 一阶规则的基本成分是能表述事物的属性或关系的&quot;原子公式&quot;，例如表达父子关系的谓词(predict)“父亲<span class="math inline">\((X,Y)\)</span>”就是原子公式，再如表示加1操作&quot;<span class="math inline">\(\sigma(X) = X+1\)</span>&quot;的函数<span class="math inline">\(\sigma(X)\)</span>也是原子公式。如果进一步使用谓词“自然数<span class="math inline">\((X)\)</span>”表示<span class="math inline">\(X\)</span>是自然数，“<span class="math inline">\(\forall X\)</span>”表示对任意<span class="math inline">\(X\)</span>成立，&quot;<span class="math inline">\(\exists Y\)</span>&quot;表示&quot;存在<span class="math inline">\(Y\)</span>使之成立&quot;,那么&quot;所有自然数加1都是自然数&quot;就可以写做: &gt; <span class="math display">\[
    \forall X, \exists Y  \leftarrow 自然数(X) \land (Y = \sigma(X))
&gt; \]</span> &gt; 或者表示成更加简洁的形式: &gt; <span class="math display">\[
    \forall X, 自然数(\sigma(X)) \leftarrow 自然数(X)
&gt; \]</span> &gt; 这样的规则就是一阶规则，其中<span class="math inline">\(X\)</span>和<span class="math inline">\(Y\)</span>称为逻辑变量，<span class="math inline">\(\forall,\exists\)</span>用于限定变量的取值范围，称为&quot;量词&quot;，显然，一阶规则可以表达复杂的关系，因此也被称为“关系型规则”。若我们简单地把属性当作谓词来定义示例与属性值之间的关系，则命题规则集<span class="math inline">\(\mathcal{R}\)</span>可以改写为一阶规则集<span class="math inline">\(R&#39;\)</span>: &gt;<span class="math display">\[
    \begin{aligned}
        &amp;规则1: 好瓜(X) \leftarrow 根蒂(X,蜷缩) \land 脐部(X,凹陷) \\
        &amp;规则2: \neg 好瓜(X) \leftarrow 纹理(X,模糊)
    \end{aligned}
&gt;\]</span> &gt; 从形式语言系统角度来看，命题规则是一阶规则的特例，因此一阶规则的学习比命题规则要复杂得多。</p>
<h3 id="序贯覆盖">序贯覆盖</h3>
<p>规则学习的目标是产生一个可以覆盖尽可能多样例的规则集，最直接的做法就是“序贯覆盖”，即逐条归纳:在训练集上每学到一条规则，就将该规则覆盖的训练样例去除，然后以剩下的训练样例组成训练集重复上述过程。由于每次只处理一部分数据，因此也被称为&quot;分治&quot;策略。 序贯覆盖法的关键是如何从训练集学出单条规则，显然，对规则学习目标<span class="math inline">\(\oplus\)</span> 产生一条规则就是寻找最优的一组逻辑文字构成规则体，这是一个搜索问题。形式化上来说，给定正例集合与反例集合，学习任务是基于候选文字集合<span class="math inline">\(\mathcal{F} = \{ f_k \}\)</span>来生成最优规则<span class="math inline">\(r\)</span>。在命题规则学习中，候选文字是形如&quot;<span class="math inline">\(R(属性_i,属性值_{ij})\)</span>&quot;的布尔表达式，<span class="math inline">\(R(x,y)\)</span>则是判断<span class="math inline">\(x,y\)</span>是否满足关系<span class="math inline">\(R\)</span>的二元布尔函数。 而具体的学习方法则是从空规则<span class="math inline">\(&quot;\oplus \leftarrow&quot;\)</span>开始，将正例类别作为规则头，再逐个遍历训练集中的每个属性及取值，尝试将其作为逻辑文字增加到规则体中，若能使当前规则体仅覆盖正例，由此产生一条规则，然后去除已被覆盖的正例并基于剩余样本生成下一个规则：通俗来说，这种方法的思想便是挑选出所有仅覆盖正例的特征和仅覆盖反例的特征。 而原始的穷尽搜索的做法在属性和候选值较多时会由于组合爆炸而不可行,现实任务中一般有两种策略来产生规则: - 自顶向下:从比较一般的规则开始，逐渐添加新文字以缩小规则覆盖范围，直到满足预定条件为止，该策略也称为“生成测试”法，是规则逐渐特化的过程。 - 自底向上:从比较特殊的规则开始,逐渐删除文字以扩大规则覆盖范围，直到满足条件为止，该种策略是规则逐渐泛化的过程。</p>
<h3 id="剪枝优化">剪枝优化</h3>
<p>规则生成本质上是一个贪心搜索的过程，需有一定的机制来缓解过拟合的风险，最常见的做法是剪枝。与决策树相似，剪枝可以分为预剪枝与后剪枝。在实际操作时通常是基于某种性能度量指标来评估增/删逻辑文字前后的规则性能，或增/删规则集前后的规则集性能，从而判断是否需要进行剪枝。 经常使用的预剪枝算法为<span class="math inline">\(CN2\)</span>算法，使用似然率统计量作为指标；后剪枝常用策略为“减错剪枝”，即将样例集进行划分，划分为训练集和验证集，根据在验证集上的表现优劣来对从训练集中学出的模型进行剪枝。、</p>
<h3 id="一阶规则学习">一阶规则学习</h3>
<p>命题逻辑的基本单元是“原子命题”，这也就导致命题规则学习很难处理对象之间的&quot;关系&quot;，而关系信息在很多任务中非常重要。例如，当我们在瓜摊挑瓜时,通常很难把水果摊上所有西瓜的特征用属性值表示出来，因为我们很难判断:色泽看起来多深才叫&quot;色泽青绿&quot;？敲起来声音多低才叫&quot;敲声沉闷&quot;？比较现实的做法是将西瓜进行比较，例如，&quot;瓜1的颜色比瓜2更深，并且瓜1的根蒂比瓜2更蜷&quot;，因此“瓜1比瓜2更好”。然而，这已经超越了命题逻辑的表达能力，需要用一阶逻辑来表达，并且要使用一阶规则进行学习。</p>
<p>首先我们需要将命题逻辑数据转换为关系数据，比如对于瓜的色泽，我们可以定义: <span class="math display">\[
    色泽深度：乌黑&gt;青绿&gt;浅白
\]</span> 那么“瓜1色泽乌黑，是好瓜；瓜2色泽青绿，不是好瓜”，就可以表示为: <span class="math display">\[
    色泽更深(瓜1，瓜2)，更好(瓜1，瓜2)
\]</span> 这样的数据直接描述了样例之间的关系，称为“关系数据”，其中由原样本属性转化而来的“色泽更深”、“根蒂更蜷”等原子公式称为“背景知识”，而由样本类别转化而来的关于“更好”，“<span class="math inline">\(\neg\)</span>更好”等原子数据称为关系数据样例，下面给出一个一阶规则示例： <span class="math display">\[
    (\forall X,\forall Y)(更好(X,Y)) \leftarrow 根蒂更蜷(X,Y) \land 脐部更凹(X,Y)
\]</span> 与命题规则相比，一阶规则的规则头、规则体都是一阶逻辑表达式，个体对象&quot;瓜1，瓜2&quot;被逻辑变量<span class="math inline">\(X,Y\)</span>替换，全称量词<span class="math inline">\(\forall\)</span> 表示该规则对所有个体对象均成立；通常，在一阶规则中所有出现的变量都被全称量词限定，所以有时会将全称量词省去,一阶规则有着强大的表达能力，例如它能简洁地表达递归概念，如: <span class="math display">\[
    更好(X,Y) \leftarrow 更好(X,Z) \land 更好(Z,Y)
\]</span> 一阶规则学习能够容易地引入领域知识，这是它相对于命题规则学习的另一大优势。在命题规则学习乃至一般的统计学习中，若欲引入领域知识，通常有两种做法:<strong>在现有属性的基础上基于领域知识构造出新属性，或基于领域知识设计某种函数机制(如正则化)来对假设。</strong> 然而现实任务中并非所有的领域知识都能够容易地通过属性重构和函数约束来表达。例如，假定我们获得包含了某未知元素的化合物<span class="math inline">\(X\)</span>，欲通过实验来发现它与已知化合物<span class="math inline">\(Y\)</span>的化学表达式,我们可以重复多次实验，测出每次结果中化合物的组份含量，虽然我们对反应中的未知元素的性质一无所知，但知道一些普遍成立的化学原理，例如金属原子一般产生离子键、氢原子之间一般都是共价键，并且也了解已知元素间可能发生反应，这些领域知识非常重要，但往往很难在基于命题表示的学习中加以利用。 下面介绍一种著名的一阶规则学习算法 #### First-Order inductive Learner FOIL是著名的一阶规则学习算法，它遵循序贯覆盖框架且采用自顶向下的规则归纳策略，由于有逻辑变量的存在，FOIL在规则生成时需考虑不同的变量组合。这也是和命题规则所不同的地方,在命题规则的学习中只需要考虑原子命题之间的组合，而在一阶逻辑规则学习中，除了要考虑原子公式的组合，还应当考虑公式中变量的组合。 FOIL使用“FOIL增益”来选择文字: <span class="math display">\[
    F_{Gain} = \hat{m}_+ \times (\log_2 \frac{\hat{m}_+}{\hat{m}_+ + \hat{m}_-}-\log_2 \frac{m_+}{m_+ + m_-})
\]</span> ### 总结 规则学习是&quot;符号主义学习&quot;，是最早开始研究的机器学习技术之一。序贯覆盖是规则学习的基本框架，CN2采用集述搜索，是最早考虑过拟合问题的规则学习算法，现实出后剪枝在缓解规则学习过拟合中的优势。 由于命题规则的表述能力有限，一阶逻辑学习开始发展，FOIL通过变量替换等操作把命题规则学习转化为一阶规则学习。 近年来随着机器学习技术进入更多应用领域，在富含结构信息和领域知识的任务中，逻辑表达的重要性逐渐凸显出来，因此出现了一些将规则学习与统计学习相结合的努力，例如给贝叶斯网中的节点赋予逻辑意义的“关系贝叶斯网络”、贝叶斯逻辑程序、马尔可夫逻辑网络，统称为&quot;统计关系学习&quot;。</p>
]]></content>
      <categories>
        <category>统计学习</category>
      </categories>
      <tags>
        <tag>规则学习，定性关联</tag>
      </tags>
  </entry>
  <entry>
    <title>论时间驱动与事件驱动</title>
    <url>/2020/08/24/lun-shi-jian-qu-dong-yu-shi-jian-qu-dong/</url>
    <content><![CDATA[
]]></content>
  </entry>
  <entry>
    <title>近邻法</title>
    <url>/2020/08/25/jin-lin-fa/</url>
    <content><![CDATA[<p>这一部分介绍一下<span class="math inline">\(k\)</span>近邻算法，该算法于1968年由Cover和Hart提出，<span class="math inline">\(k\)</span>近邻法是一种基本分类与回归方法，本文结构如下: - <span class="math inline">\(k\)</span>近邻算法 - 压缩近邻法 - <span class="math inline">\(k\)</span>近邻实现：<span class="math inline">\(kd\)</span>树 <a id="more"></a> ### <span class="math inline">\(k\)</span>近邻算法</p>
<h4 id="算法流程">算法流程</h4>
<p><span class="math inline">\(k\)</span>近邻的算法非常简单朴素: &gt; 给定一个数据集，对新的输入实例，在训练数据集中找到与该实例<strong>最邻近</strong>的<span class="math inline">\(k\)</span>个实例，这<span class="math inline">\(k\)</span>个实例中的多数属于某个类，就把该输入实例分为这个类。</p>
<p><span class="math inline">\(k\)</span>近邻的算法思想可以说是非常符合人的直觉，正如俗语所说:&quot;物以类聚，人以群分“，对于分类问题就是，想要看这个样本点是什么类别，只需要看其周围的点都是啥类别就可以了。 下面给出一般<span class="math inline">\(k\)</span>近邻算法的定义: &gt; <strong><span class="math inline">\(k\)</span>近邻法</strong> &gt; <strong>输入</strong>: 训练数据集 &gt; <span class="math display">\[
    T = \{(x_1,y_1), (x_2,y_2),\dots, (x_N,y_N) \} 
&gt; \]</span> &gt; 其中，<span class="math inline">\(x_i \in \mathcal{X} \subseteq R^n\)</span>为实例的特征向量，<span class="math inline">\(y_i \in \mathcal{Y} = \{ c_1, c_2, \dots, c_K\}\)</span>为实例的类别 &gt; <strong>输出</strong>: 实例<span class="math inline">\(x\)</span>所属的类<span class="math inline">\(y\)</span> &gt; <strong>算法流程</strong>: &gt; 1. 根据给定的距离度量，在训练集中找出与<span class="math inline">\(x\)</span>最邻近的<span class="math inline">\(k\)</span>个点，涵盖这<span class="math inline">\(k\)</span>个点的<span class="math inline">\(x\)</span>的邻域记做<span class="math inline">\(N_k(x)\)</span> &gt; 2. 在<span class="math inline">\(N_k(x)\)</span>中根据分类决策规则(如多数表决)决定<span class="math inline">\(x\)</span>的类别<span class="math inline">\(y\)</span>: &gt; <span class="math display">\[
    y = \arg \max_{c_j} \sum_{x_i \in N_k(x)} I(y_i = c_j),\quad i = 1,2,\dots,N;j=1,2,\dots,K
&gt; \]</span></p>
<p>若<span class="math inline">\(k=1\)</span>，则此时<span class="math inline">\(k\)</span>近邻算法被称为最近邻算法，<span class="math inline">\(k\)</span>近邻算法没有显示的学习过程 #### 距离度量 ##### <span class="math inline">\(L_p距离\)</span> 使用<span class="math inline">\(k\)</span>近邻算法的一个关键问题是需要明确如何定义<strong>最近</strong>，即选择什么样的距离度量，下面介绍下一般通用的距离度量<span class="math inline">\(L_p\)</span>距离： &gt; 设特征空间<span class="math inline">\(\mathcal{X}\)</span>是<span class="math inline">\(n\)</span>维实数向量空间<span class="math inline">\(R^n\)</span>,<span class="math inline">\(x_i,x_j \in \mathcal{X}\)</span>,<span class="math inline">\(x_i = (x_i^1,x_i^2,\dots,x_i^n),x_j = (x_j^1,x_j^2,\dots,x_j^n)\)</span>,则<span class="math inline">\(x_i,x_j\)</span>之间的<span class="math inline">\(L_p\)</span>距离定义为: &gt; <span class="math display">\[
    L_p(x_i,x_j) = (\sum_{i=1}^n |x_i^{(l)} - x_j^{(l)}|^p)^{\frac{1}{p}}
&gt; \]</span></p>
<p>这一类距离度量都是由范数导出的： - <span class="math inline">\(p = 1\)</span>,对应的是由1范数导出的曼哈顿距离 - <span class="math inline">\(p = 2\)</span>,对应的是由2范数导出的欧几里得距离 - <span class="math inline">\(p = \infty\)</span>,对应的是由无穷范数导出的切比雪夫距离 <span class="math display">\[
    L_\infty(x_i,x_j) = \max_l |x_i^{(l)} - x_j^{(l)}|
\]</span> <img src="https://raw.githubusercontent.com/xuejy19/xuejy19.github.io/source/Img/Lp.png" alt="L_p距离" /> 上图显示的是在不同的<span class="math inline">\(p\)</span>下距离原点距离为1的点所组成的等高线。</p>
<h5 id="马氏距离">马氏距离</h5>
<p>马氏距离是另一种常用的距离度量,首先给出维基百科中关于马氏距离的定义: &gt; 马氏距离(Mahalanobis distance)是由印度统计学家马哈拉诺比斯(P. C. Mahalanobis)提出的，表示数据的协方差距离。它是一种有效的计算两个未知样本集的相似度的方法，与欧式距离不同的是，它考虑到各种特性之间的联系(如体重与身高是相关联的)，并且是尺度无关的，即马氏距离独立于测量尺度。对于一个均值为<span class="math inline">\(\mu\)</span>,协方差矩阵为<span class="math inline">\(\Sigma\)</span>的变量，其马氏距离为: &gt; <span class="math display">\[
    \sqrt{(x - \mu)^T \Sigma^{-1} (x - \mu)}
&gt; \]</span> &gt; 马氏距离也可以定义为两个服从同一分布并且其协方差矩阵为<span class="math inline">\(\Sigma\)</span>的随机变量<span class="math inline">\(x\)</span>与<span class="math inline">\(y\)</span>的差异程度: &gt; <span class="math display">\[
    d_M(x,y) = \sqrt{(x - y)^T \Sigma^{-1} (x - y)}
&gt; \]</span> &gt; 若协方差矩阵为单位阵，则此时马氏距离便退化为欧式距离，若协方差矩阵为对角阵，此时马氏距离称为正规化的马氏距离: &gt; <span class="math display">\[
    d(x,y) = \sqrt{\sum_{i=1}^p \frac{(x_i - y_i)^2}{\sigma_i^2}}
&gt; \]</span></p>
<p>下面我对马氏距离做一个简要的说明，首先协方差矩阵是一个对称半正定矩阵，可以进行特征值分解: <span class="math display">\[
    \Sigma = U \Lambda U^T
\]</span> 其中,<span class="math inline">\(U\)</span>是酉矩阵，满足<span class="math inline">\(U^T = U^{-1}\)</span>,是由矩阵<span class="math inline">\(\Sigma\)</span>的特征向量组成(列向量)，而<span class="math inline">\(\Lambda\)</span>是一个对角矩阵，对角元素是相应特征向量所对应的特征值。下面我们考虑将协方差矩阵写成下面的形式: <span class="math display">\[
    \begin{aligned}
        \Sigma &amp;= U \Lambda^{\frac{1}{2}} \Lambda^{\frac{1}{2}} U^T \\
        &amp;=  U \Lambda^{\frac{1}{2}} (U \Lambda^{\frac{1}{2}})^T \\
        &amp;= AA^T
    \end{aligned}
\]</span> 其中<span class="math inline">\(A = U \Lambda^{\frac{1}{2}}\)</span>,所以马氏距离计算公式中的<span class="math inline">\(\Sigma^{-1}\)</span>可以写做: <span class="math display">\[
    \Sigma^{-1} = (A^T)^{-1} A^{-1}
\]</span> 下面计算样本点<span class="math inline">\(x,y\)</span>的马氏距离的平方: <span class="math display">\[
    \begin{aligned}
        d_M(x,y)^2 &amp;= (x-y)^T \Sigma^{-1} (x - y) \\
            &amp;= (x-y)^T (A^T)^{-1} A^{-1} (x-y) \\
            &amp;= [A^{-1}x - A^{-1}y]^T[A^{-1}x - A^{-1} y] 
    \end{aligned}
\]</span> 若记<span class="math inline">\(A^{-1}x = x&#39;,A^{-1}y = y&#39;\)</span>,则<span class="math inline">\(d_M(x,y)^2\)</span>可以写做： <span class="math display">\[
    d_M(x,y)^2 = || x&#39; - y&#39;||^2 
\]</span> 这也就是说在原始空间计算<span class="math inline">\(x,y\)</span>之间的马氏距离相当于计算变换后空间中对应点的欧式距离！最后我们看一下这个变换做了什么事情： <span class="math display">\[
    A^{-1} = \Lambda^{-\frac{1}{2}} U^T
\]</span> <span class="math inline">\(U^T\)</span>是一个旋转操作，相当于按照主成分方向旋转，消除各个维度之间的联系，而<span class="math inline">\(\Lambda^{-\frac{1}{2}}\)</span>是一个scale操作，将变换后的各个维度的点再除以对应分布的标准差以消除由于分布不同带来的距离度量的差异。 最后总结下马氏距离与欧式距离的区别: 1. 马氏距离的计算是建立在总体样本的基础上的，也就是说，如果拿同样的两个样本，放入两个不同的总体当中，最后计算得出的两个样本的马氏距离通常是不相同的。 2. 在计算马氏距离过程中，要求总样本数大于样本的维数，否则协方差矩阵不满秩，无法求逆</p>
<p>马氏距离的优缺点: - 优点：它不受量纲的影响，两点之间的马氏距离与原始数据的测量单位无关，由标准化数据和中心化数据计算出的两点之间的马氏距离相同。马氏距离还可以排除变量之间相关性的干扰 - 缺点: 夸大了变化微小的变量的作用</p>
<h4 id="k值选取"><span class="math inline">\(k\)</span>值选取</h4>
<p><span class="math inline">\(k\)</span>是近邻法中一个重要的超参数，下面首先说明两种极端情况: - <span class="math inline">\(k=1\)</span>,此时整个样本空间相当于被分割成了<span class="math inline">\(N\)</span>块(<span class="math inline">\(N\)</span>为训练样本个数)，对于一个新的样本，需要判断它落在那一块，便将它归入对应中心点的类别，此时模型非常复杂。 - <span class="math inline">\(k =N\)</span>,这也就意味着对于一个新的样本点，直接将它归为整个训练实例中最多的类，此时模型又过于简单。</p>
<p>总结来看，若选择较小的<span class="math inline">\(k\)</span>值，对与近邻实例点非常敏感，如果原始数据噪声较大，则会导致错分，<span class="math inline">\(k\)</span>值的减小就意味着整体模型变得更加复杂，容易发生过拟合；若<span class="math inline">\(k\)</span>值较大，就相当于用较大邻域中的训练数据进行预测，这一定程度上减轻了数据噪声对预测结果的影响，但因为将邻域扩大，导致一些不相似的样本点也在邻域中，这可能会导致分类错误。 下图是使用<span class="math inline">\(k\)</span>近邻方法在MNIST数据集上的结果，距离度量选用的是欧几里得距离，图中横坐标表示训练样本规模，纵坐标则是分类错误率，从图中可以看出，当样本数量较少时，选取较小的<span class="math inline">\(k\)</span>得到分类效果较好，但随着训练样本数的增加，不同<span class="math inline">\(k\)</span>值的分类效果开始接近。 <img src="https://raw.githubusercontent.com/xuejy19/xuejy19.github.io/source/Img/k.png" alt="k值选取" /> #### 算法复杂度分析 需要注意的是，<span class="math inline">\(k\)</span>近邻法的时间复杂度非常高，假设特征空间维数为<span class="math inline">\(n\)</span>，样本集容量为<span class="math inline">\(N\)</span>，则每来一个样本，首先要计算该样本点<span class="math inline">\(x\)</span>到样本集中各点的距离(<span class="math inline">\(O(N*n\)</span>)),然后需要对这<span class="math inline">\(N\)</span>个距离进行排序(<span class="math inline">\(O(N\log N)\)</span>),因此对单个样本进行类别划分的复杂度便已经是: <span class="math display">\[
    O(N * n) + O(N \log N)
\]</span> 对于高维的数据，直接应用<span class="math inline">\(k\)</span>近邻法的时间开销是非常大的，因此在应用中往往首先考虑对高维数据进行将维。</p>
<h3 id="压缩近邻法">压缩近邻法</h3>
<p>由于<span class="math inline">\(k\)</span>近邻法计算复杂度过高，因此有很多旨在降低近邻法复杂度的算法被提出，压缩近邻法是其中一种，下面给出其算法流程: &gt; <strong>压缩近邻法</strong>： &gt; Step1: 从Grabbag中选择一个样本放入Store中 &gt; Step2: 用store中样本以近邻法测试Grabbag中样本。如果分错，则将该样本放入store &gt; Step3: 重复上面方法直到Grabbag中没有样本再转入到Store中，或Grabbag为空则停止 &gt; Step4: 用Store中样本作为近邻法设计集合</p>
<p>压缩近邻法的思想也很简单，原始样本数据集中并不是所有的数据点都对分类有帮助，因此考虑挑选出一些具有代表性的点来组成新的训练集，该集合比之前的集合要小，当新来一个数据时，便用前面挑选出的那些点应用近邻法判断数据点类别。</p>
<h3 id="kd树"><span class="math inline">\(kd\)</span>树</h3>
<h4 id="kd树定义"><span class="math inline">\(kd\)</span>树定义</h4>
<p><span class="math inline">\(kd\)</span>树是一种对<span class="math inline">\(k\)</span>维空间中的实例点进行存储以便对其进行快速检索的树形数据结构。<span class="math inline">\(kd\)</span>树是二叉树，表示对<span class="math inline">\(k\)</span>维空间的一个划分。构造<span class="math inline">\(kd\)</span>树相当于不断地用垂直于坐标轴的超平面将<span class="math inline">\(k\)</span>维空间切分，构成一系列的<span class="math inline">\(k\)</span>维超矩形区域，<span class="math inline">\(kd\)</span>树的每一个节点对应于一个<span class="math inline">\(k\)</span>维超矩形区域。</p>
<h4 id="kd树构造"><span class="math inline">\(kd\)</span>树构造</h4>
<p><span class="math inline">\(kd\)</span>树的构造流程如下: &gt; <strong>构造平衡<span class="math inline">\(kd\)</span>树</strong> &gt; <strong>输入:</strong> <span class="math inline">\(k\)</span>维空间数据集<span class="math inline">\(T = \{ x_1,x_2,\dots,x_N \}\)</span>,其中<span class="math inline">\(x_i = (x_i^1,x_i^2,\dots,x_i^k)^T\)</span> &gt; <strong>输出:</strong> <span class="math inline">\(kd\)</span>树 &gt; - 开始：构造根节点，根节点对应于包含<span class="math inline">\(T\)</span>的<span class="math inline">\(k\)</span>维空间的超矩形区域。选择<span class="math inline">\(x^1\)</span>为坐标轴，以<span class="math inline">\(T\)</span>中所有实例的<span class="math inline">\(x^1\)</span>坐标的中位数为切分点，将根节点对应的超矩形区域切分为两个子区域。由根节点生成深度为<span class="math inline">\(1\)</span>的左、右子节点：左子节点对应于坐标<span class="math inline">\(x^1\)</span>小于切分点的子区域，右子节点对应于坐标<span class="math inline">\(x^1\)</span>大于切分点的子区域。将落在切分超平面上的实例点保存在该节点。 &gt; - 重复：对深度为<span class="math inline">\(j\)</span>的节点，选择<span class="math inline">\(x^l\)</span>为切分的坐标轴，<span class="math inline">\(l = j(mod k) +1\)</span>,以该节点的区域中所有实例的<span class="math inline">\(x^l\)</span>坐标的中位数为切分点，将对应的超矩形区域再次分割为两个子区域。 &gt; - 直到两个子区域没有实例存在时停止，从而形成<span class="math inline">\(kd\)</span>树的区域划分。</p>
<h4 id="kd树搜索"><span class="math inline">\(kd\)</span>树搜索</h4>
<p>下面给出<span class="math inline">\(kd\)</span>树的最近邻搜索算法： &gt; <strong>kd树搜索算法</strong> &gt; <strong>输入:</strong> 已构造的<span class="math inline">\(kd\)</span>树，目标点<span class="math inline">\(x\)</span> &gt; <strong>输出:</strong> <span class="math inline">\(x\)</span>的最近邻 &gt; - 在<span class="math inline">\(kd\)</span>树中找到包含目标点<span class="math inline">\(x\)</span>的叶节点：从根节点出发，递归向下搜索即可 &gt; - 以此叶节点为&quot;当前最近点&quot; &gt; - 递归地向上回退，在每个节点进行以下操作: &gt; - 如果该节点保存的实例点比当前最近点距离目标点更近，则以该实例点为当前最近点 &gt; - 检查另一子节点对应的区域是否与以目标点为球心、以目标点与“当前最近点”间的距离为半径的超球体相交。如果相交，则可能在另一个子节点对应的区域内存在距目标点更近的点，移动到另一个子节点。接着递归地进行最近邻搜索 &gt; - 当回退到跟节点时，搜索结束。最后的&quot;当前最近点&quot;即为<span class="math inline">\(x\)</span>的最近邻点。</p>
<p>如果实例点是随机分布的，<span class="math inline">\(kd\)</span>树搜索的平均计算复杂度是<span class="math inline">\(O(\log N)\)</span>,<span class="math inline">\(kd\)</span>树更适用于训练实例数远大于空间维数时的<span class="math inline">\(k\)</span>近邻搜索，当空间维数接近训练实例数时，它的效率会迅速下降，接近线性扫描。</p>
]]></content>
      <categories>
        <category>统计学习</category>
      </categories>
      <tags>
        <tag>分类，k近邻</tag>
      </tags>
  </entry>
  <entry>
    <title>非监督学习</title>
    <url>/2020/09/11/fei-jian-du-xue-xi/</url>
    <content><![CDATA[<p>这部分总结下传统统计学习的最后一部分内容——非监督学习，非监督学习又称无监督学习，我们前面讲的大部分算法都是需要样本的标签的，通过标签来构造损失函数，进而进行模型学习，但在有的情况下我们并没有数据的标签，这种情况下我们就什么都不能做了么？当然不是，对于无标签数据，我们仍旧可以开展一部分工作，比如： - 概率模型估计 - 数据降维 - 数据聚类</p>
<p>由于概率模型估计算法在前面已经讲过了(<a href="https://xuejy19.github.io/2020/07/30/EM/">EM算法</a>、<a href="https://xuejy19.github.io/2020/07/27/概率密度函数估计/#more">概率密度函数估计</a>)，这里就不再赘述，本文按照以下结构组织: - 无监督学习思想 - 数据降维 - 数据聚类</p>
<a id="more"></a>
<h3 id="无监督学习思想">无监督学习思想</h3>
<p>无监督学习是从无标注的数据中学习数据的统计规律或者说内在结构的机器学习，主要包括聚类、降维、概率估计，<strong>无监督学习可以用于数据分析或者监督学习预处理</strong>。 无监督学习使用无标注数据<span class="math inline">\(U = \{ x_1, x_2, \dots, x_N\}\)</span>学习或训练，其中<span class="math inline">\(x_i，i = 1,2,\dots,N\)</span>, 是样本，由特征向量组成。无监督学习的模型是函数<span class="math inline">\(z = g_{\theta}(x)\)</span> , 条件概率分布<span class="math inline">\(P_{\theta}(z|x)\)</span>，或条件概率分布<span class="math inline">\(P_{\theta}(x|z)\)</span>。其中<span class="math inline">\(x \in X\)</span>是输入，表示样本； <span class="math inline">\(z \in Z\)</span>是输出，表示对样本的分析结果，可以是类别、转换、概率；<span class="math inline">\(\theta\)</span>是参数。 无监督学习是一个困难的任务，因为数据没有标注，也就是没有人为的引导，机器需要自己从数据中找出规律。模型的输入<span class="math inline">\(x\)</span>在数据中可以观测，而输出<span class="math inline">\(z\)</span>隐藏在数据中。无监督学习通常需要大量的数据，因为对数据隐藏的规律的发现需要足够的观测。 <img src="https://raw.githubusercontent.com/xuejy19/xuejy19.github.io/source/Img/Unsupervised.jpg" alt="机器学习算法划分" /></p>
<h3 id="数据降维">数据降维</h3>
<h4 id="问题定义">问题定义</h4>
<p>降维是将训练数据中的样本(实例)从高维空间转换到低维空间。假设样本原本存在于低维空间，或者近似地存在于低维空间，通过降维则可以更好地表示样本数据的结构，即更好地表示样本之间的关系。高维空间通常是高维的欧式空间，而低维空间则是低维的欧式空间或者流形(manifold)。低维空间不是事先给定，而是从数据中自动发现，其维数通常是事先给定的。从高维到低维的降维中，要保证样本的信息损失最少。 <img src="https://raw.githubusercontent.com/xuejy19/xuejy19.github.io/source/Img/dimension.png" alt="降维" /></p>
<p>本文主要介绍四种降维方法： - 主成分分析法(PCA) - 多维缩放(MDS) - 等距特征映射(ISOMAP) - 局部线性 ### 数据聚类 #### 问题定义 聚类是将样本集合中相似的样本(实例)分配到相同的类，不相似的样本分配到不同的类。聚类时，样本通常是欧式空间中的向量，类别不是事先给定，而是从数据中自动发现，但<strong>类别的个数</strong>通常要预先给定。样本之间的相似度或距离由<strong>度量</strong>决定。如果一个样本只能属于一个类，则称为硬聚类；如果一个样本可以属于多个类，则称为软聚类。 <img src="https://raw.githubusercontent.com/xuejy19/xuejy19.github.io/source/Img/clustering.png" alt="聚类" /></p>
]]></content>
      <categories>
        <category>统计学习</category>
      </categories>
      <tags>
        <tag>PCA, MDS, ISOMAP</tag>
      </tags>
  </entry>
  <entry>
    <title>隐马尔可夫模型</title>
    <url>/2020/08/17/hmm/</url>
    <content><![CDATA[<p>在这一部分，我将对隐马尔可夫模型(HMM)做简要介绍,该章节分为以下几个部分进行组织:</p>
<ul>
<li>基本概念</li>
<li>概率计算算法</li>
<li>学习算法</li>
<li>预测算法</li>
</ul>
<a id="more"></a>
<h3 id="基本概念">基本概念</h3>
<p>首先给出隐马尔可夫模型的定义：</p>
<blockquote>
<p><strong>隐马尔可夫模型</strong>:隐马尔可夫模型是关于时序的概率模型，描述由一个隐藏的马尔可夫链随机生成不可观测的随机状态序列,再由各个状态生成一个观测从而产生观测随机序列的过程。隐藏的马尔可夫链随机生成的状态序列称为状态序列；每个状态生成一个观测，而由此产生的观测的随机序列，称为观测序列，序列的每一个位置又可以看作是一个时刻。</p>
</blockquote>
<p>隐马尔可夫模型的形式定义如下:设<span class="math inline">\(Q\)</span>是所有可能的状态的集合,<span class="math inline">\(V\)</span>是所有可能的观测的集合:</p>
<p><span class="math display">\[
Q = (q_1,q_1,\dots,q_N),V = (v_1,v_2,\dots,v_M)
\]</span></p>
<p>其中,<span class="math inline">\(N\)</span>是可能的状态数，<span class="math inline">\(M\)</span>是可能的观测数。<span class="math inline">\(I\)</span>是长度为<span class="math inline">\(T\)</span>的状态序列,<span class="math inline">\(O\)</span>是对应的观测序列:</p>
<p><span class="math display">\[
I = (i_1,i_2,\dots,i_T),O = (o_1,o_2,\dots,o_T)
\]</span></p>
<p><span class="math inline">\(A\)</span>是状态转移概率矩阵:</p>
<p><span class="math display">\[
A = [a_{ij}]_{N \times N}
\]</span></p>
<p>其中:</p>
<p><span class="math display">\[
a_{ij} = P(i_{t+1} = q_j | i_t = q_i),i=1,\dots,N;j=1,\dots,N
\]</span></p>
<p>指代在时刻<span class="math inline">\(t\)</span>处于状态<span class="math inline">\(q_i\)</span>条件下,在时刻<span class="math inline">\(t+1\)</span>转移到<span class="math inline">\(q_j\)</span>的概率，<span class="math inline">\(B\)</span>是观测概率矩阵:</p>
<p><span class="math display">\[
B = [b_j(k)]_{N\times M}
\]</span></p>
<p>其中:</p>
<p><span class="math display">\[
b_j(k) = P(o_t = v_k|i_t = q_j),\quad k=1,2,\dots,M;\quad,j=1,2,\dots,N
\]</span></p>
<p>指代在时刻<span class="math inline">\(t\)</span>处于状态<span class="math inline">\(q_j\)</span>条件下生成观测<span class="math inline">\(v_k\)</span>的概率。<span class="math inline">\(\pi\)</span>是初始是初始状态概率向量:</p>
<p><span class="math display">\[
\pi = (\pi_i)
\]</span></p>
<p>其中:</p>
<p><span class="math display">\[
\pi_i = P(i_1 = q_i),\quad i =1,2,\dots,N
\]</span></p>
<p>指代初始时刻处于状态<span class="math inline">\(q_i\)</span>的概率。隐马尔可夫模型由初始状态概率向量<span class="math inline">\(\pi\)</span>,状态转移概率矩阵<span class="math inline">\(A\)</span>和观测概率矩阵<span class="math inline">\(B\)</span>决定。<span class="math inline">\(\pi\)</span>和<span class="math inline">\(A\)</span>决定状态序列,<span class="math inline">\(B\)</span>决定观测序列。因此，隐马尔可夫模型可以用三元符号表示:</p>
<p><span class="math display">\[
\lambda = (\pi,A,B)
\]</span></p>
<p>状态转移矩阵<span class="math inline">\(A\)</span>与初始状态向量<span class="math inline">\(\pi\)</span>确定了隐藏的马尔可夫链，生成不可观测的状态序列。观测概率矩阵<span class="math inline">\(B\)</span>确定了如何从状态生成观测，与状态序列综合确定了如何产生观测序列。从定义来看，隐马尔可夫模型做了两个基本假设：</p>
<ol type="1">
<li>齐次马尔可夫性假设，即假设隐藏的马尔可夫链在任意时刻<span class="math inline">\(t\)</span>的状态只依赖于前一时刻的状态，与其他时刻的状态及观测无关，也与<span class="math inline">\(t\)</span>无关：</li>
</ol>
<p><span class="math display">\[
P(i_t| i_{t-1},o_{t-1},\dots,i_1,t_1) = P(i_t|i_{t-1}),\quad t = 1,2,\dots,T
\]</span></p>
<ol start="2" type="1">
<li>观测独立性假设,即假设在任意时刻的观测只依赖于该时刻的马尔可夫链的状态，与其他观测及状态无关:</li>
</ol>
<p><span class="math display">\[
P(o_t|i_t,i_{t-1},o_{t-1},\dots,i_1,t_1) = P(o_t|i_t)
\]</span></p>
<p>隐马尔可夫模型可用于标注,这时状态对应着标记。标注问题是给定观测的序列预测其对应的标记序列。可以假设标注问题的数据是由隐马尔可夫模型生成的。这样我们可以利用隐马尔可夫模型的学习与预测算法进行标注。</p>
<h4 id="观测序列的生成过程">观测序列的生成过程</h4>
<p>根据隐马尔可夫模型定义,可以将一个长度为<span class="math inline">\(T\)</span>的观测序列<span class="math inline">\(O = (o_1,o_2,\dots,o_T)\)</span>的生成过程描述如下:</p>
<blockquote>
<p>输入： 隐马尔可夫模型<span class="math inline">\(\lambda = (A,B,\pi)\)</span>,观测序列长度<span class="math inline">\(T\)</span> 输出： 观测序列<span class="math inline">\(O = (o_1,o_2,\dots,o_T)\)</span></p>
<ol type="1">
<li>按照初始状态分布<span class="math inline">\(\pi\)</span>产生状态<span class="math inline">\(i_1\)</span></li>
<li>令<span class="math inline">\(t=1\)</span></li>
<li>按照状态<span class="math inline">\(i_t\)</span>的观测概率分布<span class="math inline">\(b_{i_t}(k)\)</span>生成<span class="math inline">\(o_t\)</span></li>
<li>按照状态<span class="math inline">\(i_t\)</span>的状态转移概率分布<span class="math inline">\(\{a_{i_t,i_{t+1}}\}\)</span>产生状态<span class="math inline">\(i_{t+1}\)</span></li>
<li>令<span class="math inline">\(t = t+1\)</span>,如果<span class="math inline">\(t&lt;T\)</span>,转步(3),否则，终止</li>
</ol>
</blockquote>
<h4 id="隐马尔可夫模型的3个基本问题">隐马尔可夫模型的3个基本问题</h4>
<p>隐马尔可夫模型有3个基本问题:</p>
<ul>
<li>概率计算问题,给定模型<span class="math inline">\(\lambda = (A,B,\pi)\)</span>和观测序列<span class="math inline">\(O = (o_1,o_2,\dots,o_T)\)</span>,计算在模型<span class="math inline">\(\lambda\)</span>下观测序列<span class="math inline">\(O\)</span>出现的概率<span class="math inline">\(P(O|\lambda)\)</span></li>
<li>学习问题，已知观测序列<span class="math inline">\(O = (o_1,o_2,\dots,o_T)\)</span>,估计模型<span class="math inline">\(\lambda = (A,B,\pi)\)</span>参数，使得在该模型下观测序列概率<span class="math inline">\(P(O|\lambda)\)</span>。即用极大似然估计的方法估计参数。</li>
<li>预测问题，也称为解码问题。已知模型<span class="math inline">\(\lambda = (A,B,\pi)\)</span>和观测序列<span class="math inline">\(O = (o_1,o_2,\dots,o_T)\)</span>,求对给定观测序列条件概率<span class="math inline">\(P(I|O)\)</span>最大的状态序列<span class="math inline">\(I = (i_1,i_2,\dots,i_T)\)</span>。即给定观测序列，求最有可能的对应的状态序列。</li>
</ul>
<h3 id="概率计算算法">概率计算算法</h3>
<h4 id="直接计算法">直接计算法</h4>
<p>首先介绍最直观的直接计算法，给定模型和观测序列后，计算观测序列出现的概率的直接方法便是按照概率公式直接计算。通过列举所有可能的长度为<span class="math inline">\(T\)</span>的状态序列，求各个状态序列<span class="math inline">\(I\)</span>与观测序列<span class="math inline">\(O\)</span>的联合概率<span class="math inline">\(P(I,O|\lambda)\)</span>,然后对所有可能的状态序列求和，得到<span class="math inline">\(P(O|\lambda)\)</span>。</p>
<p>状态序列<span class="math inline">\(I = (i_1,i_2,\dots,i_T)\)</span>的概率是:</p>
<p><span class="math display">\[
P(I|\lambda) = \pi_{i1}a_{i_1 i_2} a_{i_2 i_3} \dots a_{i_{T-1} i_T}
\]</span></p>
<p>对固定的状态序列<span class="math inline">\(I = (i_1,i_2,\dots,i_T)\)</span>,观测序列<span class="math inline">\(O = (o_1,o_2,\dots,o_T)\)</span>的概率是:</p>
<p><span class="math display">\[
P(O|I,\lambda) = b_{i_1}(o_1) b_{i_2}(o_2) \dots b_{i_T}(o_T)
\]</span></p>
<p><span class="math inline">\(O\)</span>和<span class="math inline">\(I\)</span>同时出现的联合概率为:</p>
<p><span class="math display">\[
\begin{aligned}
         P(O,I|\lambda) &amp;= P(O|I,\lambda) P(I|\lambda) \\\\
            &amp;= \pi_{i_1}b_{i_1}(o_1) a_{i_1 i_2} b_{i_2}(o_2) \dots a_{i_{T-1}i_T} b_{i_T}(o_T)
    \end{aligned}
\]</span></p>
<p>然后，对所有可能的状态序列<span class="math inline">\(I\)</span>求和，得到观测序列<span class="math inline">\(O\)</span>的概率<span class="math inline">\(P(O|\lambda)\)</span>，即:</p>
<p><span class="math display">\[
\begin{aligned}
         P(O,I|\lambda) &amp;= \sum_{I} P(O|I,\lambda) P(I|\lambda) \\\\
            &amp;= \sum_{i_1,i_2,\dots,i_T} \pi_{i_1}b_{i_1}(o_1) a_{i_1 i_2} b_{i_2}(o_2) \dots a_{i_{T-1}i_T} b_{i_T}(o_T)
    \end{aligned}
\]</span></p>
<p>这种直接计算的复杂度是<span class="math inline">\(O(TN^T)\)</span>,在实际中并不可行。</p>
<h4 id="前向算法">前向算法</h4>
<p>首先定义前向概率：</p>
<blockquote>
<p>前向概率:给定隐马尔可夫模型<span class="math inline">\(\lambda\)</span>,定义到时刻<span class="math inline">\(t\)</span>部分观测序列为<span class="math inline">\(o_1,o_2,\dots,o_t\)</span>且状态为<span class="math inline">\(q_i\)</span>的概率为前向概率，记做： <span class="math display">\[
\alpha_t(i) = P(o_1,o_2,\dots,o_t,i_t = q_i|\lambda)
\]</span> 由此便可以递推求得前向概率<span class="math inline">\(\alpha_t(i)\)</span>和观测序列概率<span class="math inline">\(P(O|\lambda)\)</span></p>
</blockquote>
<blockquote>
<p><strong>观测序列概率的前向算法</strong> 输入：隐马尔可夫模型<span class="math inline">\(\lambda\)</span>,观测序列<span class="math inline">\(O\)</span> 输出: 观测序列概率<span class="math inline">\(P(O|\lambda)\)</span></p>
<ul>
<li>初值： <span class="math display">\[
\alpha_1(i) = \pi_i b_i(o_1),\quad i = 1,2,\dots, N
\]</span></li>
<li>递推：对<span class="math inline">\(t=1,2,\dots,T-1\)</span> <span class="math display">\[
\alpha_{t+1}(i) = [\sum_{j=1}^N \alpha_t(j) a_{ji}] b_i(o_{t+1}),\quad i= 1,2,\dots,N
\]</span></li>
<li>终止: <span class="math display">\[
P(O|\lambda) = \sum_{i=1}^N \alpha_T(i)
\]</span></li>
</ul>
</blockquote>
<p>前向算法实际上是基于“状态序列的路径结构”递推计算<span class="math inline">\(P(O|\lambda)\)</span>的算法。前向算法高效的关键在于其局部计算前向概率，然后利用路径结构将前向概率递推到全局,得到<span class="math inline">\(P(O|\lambda)\)</span>。</p>
<h4 id="后向算法">后向算法</h4>
<p>首先给出后向概率的定义:</p>
<blockquote>
<p><strong>后向概率</strong>：给定隐马尔可夫模型<span class="math inline">\(\Lambda\)</span>,定义在时刻<span class="math inline">\(t\)</span>状态为<span class="math inline">\(q_i\)</span>的条件下，从<span class="math inline">\(t+1\)</span>到<span class="math inline">\(T\)</span>的部分观测序列为<span class="math inline">\(o_{t+1},o_{t+2},\dots,o_T\)</span>的概率为后向概率，记做： <span class="math display">\[
\beta_t(i) = P(o_{t+1},o_{t+2},\dots,o_T|i_t = q_i,\lambda)
\]</span></p>
</blockquote>
<p>与前向概率计算类似,后向概率的计算也可以采用递推算法:</p>
<blockquote>
<p><strong>观测序列概率的后向算法</strong> 输入：隐马尔可夫模型<span class="math inline">\(\lambda\)</span>,观测序列<span class="math inline">\(O\)</span> 输出：观测序列概率<span class="math inline">\(P(O|\lambda)\)</span></p>
<ul>
<li>初始化: <span class="math display">\[
\beta_T(i) = 1,\quad i =1,2,\dots,N
\]</span></li>
<li>对<span class="math inline">\(t = T-1,T-2,\dots,1\)</span> <span class="math display">\[
\beta_t(i) = \sum_{j=1}^N a_{ij} b_j(o_{t+1})\beta_{t+1}(j),
\quad i=1,2,\dots,N
\]</span></li>
<li>终止： <span class="math display">\[
P(O|\lambda) = \sum_{i=1}^N \pi_i b_i(o_1)\beta_1(i)
\]</span></li>
</ul>
</blockquote>
<p>利用前向概率与后向概率的定义可以将观测序列概率统一写成:</p>
<p><span class="math display">\[
P(O|\lambda) =\sum_{i=1}^N \alpha_t(i) \beta_t(i) =\sum_{i=1}^N \sum_{j=1}^N \alpha_t(i)
    a_{ij} b_j(o_{t+1}) \beta_{t+1}(j)
\]</span></p>
<p>该联合概率公式告诉我们，若我们知道了某一个时刻所有状态下的前向概率，同时知道下一个时刻所有状态下的后向概率，则可以直接计算出观测序列概率。前面介绍的三个计算观测序列概率的公式实际上为我们提供了三种递推思路:</p>
<ul>
<li>前向概率递推，即从前向后递推</li>
<li>后向概率递推，即从后向前递推</li>
<li>前向后向概率递推，即从两个方向开始递推</li>
</ul>
<p>这三种方法的计算复杂度均为<span class="math inline">\(O(TN^2)\)</span>级别，对比直接计算的<span class="math inline">\(O(TN^T)\)</span>级别，计算复杂度大大降低。</p>
<h4 id="一些概率与期望值的计算">一些概率与期望值的计算</h4>
<p>利用前向概率和后向概率，可以得到关于单个状态和两个状态概率的计算公式。 1. 给定模型<span class="math inline">\(\lambda\)</span>和观测<span class="math inline">\(O\)</span>,在时刻<span class="math inline">\(t\)</span>处于状态<span class="math inline">\(q_i\)</span>的概率,记： <span class="math display">\[
\gamma_t(i) = P(i_t = q_i|O,\lambda)
\]</span> 可以通过前向后向概率计算,由贝叶斯公式可得: <span class="math display">\[
\gamma_t(i) = P(i_t = q_i|O,\lambda) = \frac{P(i_t = q_i,O|\lambda)}{P(O|\lambda)}
\]</span> 由此可以得到： <span class="math display">\[
\gamma_t(i) = \frac{\alpha_t(i) \beta_t(i)}{\sum_{j=1}^N \alpha_t(j)\beta_t(j)}
\]</span> 2. 给定模型<span class="math inline">\(\lambda\)</span>和观测<span class="math inline">\(O\)</span>,在时刻<span class="math inline">\(t\)</span>处于状态<span class="math inline">\(q_i\)</span>且在时刻<span class="math inline">\(t+1\)</span>处于状态<span class="math inline">\(q_j\)</span>的概率为： <span class="math display">\[
\xi_t(i,j) = P(i_t = q_i, i_{t+1} = q_j | O,\lambda)
\]</span> 由贝叶斯公式可得： <span class="math display">\[
\begin{aligned}
     \xi_t(i,j)  &amp;= \frac{P(i_t = q_i,i_{t+1} = q_j, O|\lambda)}{P(O|\lambda)} \\\\
     &amp;= \frac{P(i_t = q_i,i_{t+1} = q_j, O|\lambda)}{\sum_{i=1}^N \sum_{j=1}^N P(i_t = q_i,i_{t+1} = q_j, O|\lambda)}
\end{aligned}
\]</span> 公式中<span class="math inline">\(P(i_t = q_i,i_{t+1} = q_j, O|\lambda)\)</span>计算公式为: <span class="math display">\[
P(i_t = q_i,i_{t+1} = q_j, O|\lambda) = \alpha_t(i) a_{ij} b_j (o_{t+1}) \beta_{t+1}(j)
\]</span> 3. 将<span class="math inline">\(\gamma_t(i)\)</span>和<span class="math inline">\(\xi_t(i,j)\)</span>对各个时刻<span class="math inline">\(t\)</span>求和，可以得到一些期望值： - 在观测<span class="math inline">\(O\)</span>下状态<span class="math inline">\(i\)</span>出现的期望值： <span class="math display">\[
\sum_{t=1}^T \gamma_t(i)
\]</span> - 在观测<span class="math inline">\(O\)</span>下由状态<span class="math inline">\(i\)</span>转移的期望值: <span class="math display">\[
\sum_{t=1}^{T-1} \gamma_t(i)
\]</span> - 在观测<span class="math inline">\(O\)</span>下由状态<span class="math inline">\(i\)</span>转移到状态<span class="math inline">\(j\)</span>的期望值： <span class="math display">\[
\sum_{t=1}^{T-1} \xi_t(i,j)
\]</span></p>
<h3 id="学习算法">学习算法</h3>
<p>隐马尔可夫的学习算法可以根据数据类型分为两种：</p>
<ul>
<li>训练数据包含观测序列和状态序列, 此时可采用监督学习算法</li>
<li>训练数据仅有观测序列，此时需采用无监督学习算法</li>
</ul>
<h4 id="监督学习方法">监督学习方法</h4>
<p>假设已给训练数据包含<span class="math inline">\(s\)</span>个长度相同的观测序列和对应的状态序列<span class="math inline">\(\{ (O_1,I_1),\dots, (O_S,I_S)\}\)</span>,那么可以直接采用极大似然法来估计马尔可夫参数,具体方法如下: 1. 转移概率<span class="math inline">\(a_{ij}\)</span>的估计：设样本中时刻<span class="math inline">\(t\)</span>处于状态<span class="math inline">\(i\)</span>,而时刻<span class="math inline">\(t+1\)</span>处于状态<span class="math inline">\(j\)</span>出现 的频数为<span class="math inline">\(A_{ij}\)</span>,那么状态转移概率<span class="math inline">\(a_{ij}\)</span>的估计为： <span class="math display">\[
\hat{a}_{ij} = \frac{A_{ij} }{\sum_{j=1}^N A_{ij} }
\]</span> 这其实就是一个伯努利分布的最大似然估计问题，计算比较简单。 1. 观测概率<span class="math inline">\(b_j(k)\)</span>的估计,设样本中状态为<span class="math inline">\(j\)</span>并且观测为<span class="math inline">\(k\)</span>的频数是<span class="math inline">\(B_{jk}\)</span>,那么状态为<span class="math inline">\(j\)</span>观测为<span class="math inline">\(k\)</span>的估计是： <span class="math display">\[
\hat{b}_j(k) = \frac{B_{jk}}{\sum_{k=1}^M B_{jk}}
\]</span> 3. 初始状态概率<span class="math inline">\(\pi_i\)</span>的极大似然估计为<span class="math inline">\(S\)</span>个样本中初始状态为<span class="math inline">\(q_i\)</span>的频率</p>
<h3 id="无监督学习方法-bw算法">无监督学习方法-BW算法</h3>
<p>假设训练数据只包含<span class="math inline">\(S\)</span>个长度为<span class="math inline">\(T\)</span>的观测序列<span class="math inline">\(\{ O_1,O_2,\dots, O_S\}\)</span> 而没有相应的状态序列，学习的目标是学习隐马尔可夫模型的参数。我们将观测序列 看做观测数据<span class="math inline">\(O\)</span>,状态序列数据看做不可观测的隐数据<span class="math inline">\(I\)</span>,那么隐马尔可夫实际上是 一个含有隐变量概率模型：</p>
<p><span class="math display">\[
P(O|\lambda) = \sum_{I} P(O|I,\lambda)P(I|\lambda)
\]</span></p>
<p>这是在EM算法解决框架下，可以按照EM算法步骤进行计算</p>
<ul>
<li>确定完全数据的对数似然函数，所有的观测数据写成<span class="math inline">\(O = (o_1,o_2,\dots,o_T)\)</span>, 所有隐数据写成<span class="math inline">\(I = (i_1,i_2,\dots,i_T)\)</span>。完全数据的对数似然函数是 <span class="math inline">\(log P(O,I|\lambda)\)</span></li>
<li>EM算法的E步:求<span class="math inline">\(Q\)</span>函数<span class="math inline">\(Q(\lambda,\bar{\lambda})\)</span> <span class="math display">\[
\begin{aligned}
      Q(\lambda,\bar{\lambda}) &amp;= E_I[logP(O,I|\lambda)P(O,I|\bar{\lambda})] \\\\
      &amp;= \sum_I log P(O,I|\lambda) P(I|O,\bar{\lambda}) \\\\
      &amp;= \sum_I log P(O,I|\lambda) \frac{P(I,O|\bar{\lambda})}{P(O|\bar{\lambda})}
\end{aligned}
\]</span> 又因为<span class="math inline">\(P(O|\bar{\lambda})\)</span>与<span class="math inline">\(\lambda\)</span>无关，而在接下来M步我们要做的是找到合适的 <span class="math inline">\(\lambda\)</span>极大化<span class="math inline">\(Q\)</span>函数，因此可以考虑将该项略去，将<span class="math inline">\(Q\)</span>函数写做: <span class="math display">\[
Q(\lambda,\bar{\lambda}) = \sum_I log P(O,I|\lambda) P(I,O|\bar{\lambda})
\]</span> 其中,<span class="math inline">\(\bar{\lambda}\)</span>是隐马尔可夫模型的当前参数估计值,<span class="math inline">\(\lambda\)</span>是要极大化的隐马尔 可夫模型参数。 <span class="math display">\[
P(O,I|\lambda) = \pi_{i_1} b_{i_1}(o_1) a_{i_1,i_2} b_{i_2}(o_2)
  \dots a_{i_{T-1}i_T} b_{i_T}(o_T)
\]</span> 于是<span class="math inline">\(Q\)</span>函数可以写做: <span class="math display">\[
\begin{aligned}
      Q(\lambda,\bar{\lambda}) &amp;= \sum_I log \pi_{i_1} P(O,I|\bar{\lambda}) + \sum_I (\sum_{t=1}^{T-1} log a_{i_t i_{t+1}})
      P(O,I|\bar{\lambda})\\ &amp;+ \sum_{I}(\sum_{t=1}^T log b_{i_t}(o_t))P(O,I|\bar{\lambda})
  \end{aligned}
\]</span></li>
<li>EM算法M步：极大化<span class="math inline">\(Q\)</span>函数<span class="math inline">\(Q(\lambda,\bar{\lambda})\)</span>求模型参数<span class="math inline">\(A,B,\pi\)</span> 由最终<span class="math inline">\(Q\)</span>函数的写法可以看出，要极大化参数单独出现在三项中，因此只需对各项 分别极大化</li>
</ul>
<ol type="1">
<li>求<span class="math inline">\(\pi_{i_1}\)</span>估计值，第一项可以重写为： <span class="math display">\[
\sum_I log \pi_{i_1} P(O,I|\bar{\lambda}) = \sum_{i=1}^N log \pi_{i} P(O,i_1 = i|\bar{\lambda})
\]</span> 同时需要注意到关于<span class="math inline">\(\pi_i\)</span>的天然约束<span class="math inline">\(\sum_{i=1}^N \pi_i = 1\)</span>,该优化问题可以通过 Lagrange乘子法进行求解，最终求解值为: <span class="math display">\[
\pi_i = \frac{P(O,i_1 = i|\bar{\lambda})}{P(O|\bar{\lambda})}
 = \gamma_1(i)
\]</span></li>
<li>求<span class="math inline">\(a_{ij}\)</span>估计值，第二项可以重写为： <span class="math display">\[
\sum_I (\sum_{t=1}^{T-1} log a_{i_t i_{t+1}})P(O,I|\bar{\lambda}) = 
 \sum_{i=1}^N \sum_{j=1}^N \sum_{t=1}^{T-1} log a_{i j} P(O,i_t = i,i_{t+1} = j|\bar{\lambda})
\]</span> 注意存在约束条件<span class="math inline">\(\sum_{j=1}^N a_{ij} = 1\)</span>,应用Lagrange乘子法可得: <span class="math display">\[
a_{ij} = \frac{\sum_{t=1}^{T-1} P(O,i_t = i,i_{t+1} = j|\bar{\lambda})}{\sum_{t=1}^{T-1} P(O,i_t = i|\bar{\lambda})}
  = \frac{\sum_{t=1}^{T-1} \xi_t(i,j)}{\sum_{t=1}^{T-1} \gamma_t(i)}
\]</span></li>
<li>求<span class="math inline">\(b_j(k)\)</span>的估计值，首先将第三项重写为: <span class="math display">\[
\sum_{I}(\sum_{t=1}^T log b_{i_t}(o_t))P(O,I|\bar{\lambda}) = 
 \sum_{j=1}^N \sum_{t=1}^{T} log b_j(o_t) P(O,i_t = j|\bar{\lambda})
\]</span> 约束条件是<span class="math inline">\(\sum_{k=1}^M b_j(k) = 1\)</span>。只有在<span class="math inline">\(o_t = v_k\)</span>时,<span class="math inline">\(b_j(o_t)\)</span>对<span class="math inline">\(b_j(k)\)</span>的 偏导数才不为0,以<span class="math inline">\(I(o_t = v_k)\)</span>表示,求得: <span class="math display">\[
b_j(k) = \frac{\sum_{t=1}^T P(O,i_t =j|\bar{\lambda})I(o_t = v_k)}{\sum_{t=1}^T P(O,i_t = j|\bar{\lambda})}
 = \frac{\sum_{t=1,o_t = v_k}^T \gamma_t(j)}{\sum_{t=1}^T \gamma_t(j)}
\]</span> &gt; Baum-Welch算法： &gt; 输入：观测数据<span class="math inline">\(O = (o_1,o_2,\dots,o_T)\)</span> &gt; 输出：隐马尔可夫模型参数 &gt; - 初始化:对<span class="math inline">\(n=0\)</span>,选取<span class="math inline">\(a_{ij}^{(0)}, b_j(k)^{(0)},\pi_i^{(0)}\)</span>，得到模型 &gt; <span class="math inline">\(\lambda^{(0)} = (A^{(0)},B^{(0)},\pi^{(0)})\)</span> &gt; - 根据上面给出的公式进行递推 &gt; - 重复E步和M步。直到算法收敛</li>
</ol>
<h3 id="预测算法">预测算法</h3>
<p>下面介绍两种隐马尔可夫模型的预测算法：近似算法与维特比算法。</p>
<h4 id="近似算法">近似算法</h4>
<p>近似算法的想法是，在每个时刻<span class="math inline">\(t\)</span>选择在该时刻最有可能出现的状态<span class="math inline">\(i_t^*\)</span>,从而 得到一个状态序列<span class="math inline">\(I^* = (i_1^*,i_2^*,\dots,i_T^*)\)</span>,将该序列作为预测的结果。 给定隐马尔可夫模型<span class="math inline">\(\lambda\)</span>和观测序列<span class="math inline">\(O\)</span>,在时刻<span class="math inline">\(t\)</span>处于状态<span class="math inline">\(q_i\)</span>的概率 <span class="math inline">\(\gamma_t(i)\)</span>是：</p>
<p><span class="math display">\[
\gamma_t(i) = \frac{\alpha_t(i)\beta_t(i)}{P(O|\lambda)} = 
 \frac{\alpha_t(i)\beta_t(i)}{\sum_{j=1}^N \alpha_t(j)\beta_t(j)}
\]</span></p>
<p>在每一时刻<span class="math inline">\(t\)</span>最有可能的状态<span class="math inline">\(i_t^*\)</span>是:</p>
<p><span class="math display">\[
i_t^* = argmax_{1 \leq i \leq N} [\gamma_t(i)],\quad t = 1,2,\dots,T
\]</span></p>
<p>进而得到状态序列</p>
<p><span class="math display">\[
I^* = (i_1^*,i_2^*,\dots,i_T^*)
\]</span></p>
<p>近似算法优点是计算简单，其缺点是不能够保证预测的序列整体是最有可能的状态序列，这是因为预测的状态序列可能有实际不发生的部分。</p>
<h4 id="维特比算法">维特比算法</h4>
<p>维特比算法实际上是用动态规划来解隐马尔可夫模型的预测问题，即用动态规划来求概率最大路径(最优路径)。这时一条路径对应着一个最优序列。 首先导入两个变量<span class="math inline">\(\delta,\varPhi\)</span>,定义在时刻<span class="math inline">\(t\)</span>状态为<span class="math inline">\(i\)</span>的所有单个路径<span class="math inline">\((i_1,i_2,\dots,i_t)\)</span>中概率最大值为: <span class="math display">\[
    \delta_t(i) = \max_{i_1,i_2,\dots,i_{t-1}} P(i_t = i,i_{t-1},\dots,i_1,o_t,\dots,o_1|\lambda)
\]</span> 由定义可知变量<span class="math inline">\(\delta\)</span>的递推公式： <span class="math display">\[
    \begin{aligned}
     \delta_{t+1}(i) &amp;= \max_{i_1,i_2,\dots,i_{t}} P(i_{t+1} = i,i_{t},\dots,i_1,o_{t+1},\dots,o_1|\lambda) \\
    &amp;= max_{1 \leq j \leq N} [\delta_t(j) a_{ji}]b_i(o_{t+1})
    \end{aligned}
\]</span> 定义在时刻<span class="math inline">\(t\)</span>状态为<span class="math inline">\(i\)</span>的所有单个路径<span class="math inline">\((i_1,i_2,\dots,i_{t-1},i)\)</span>中概率最大的路径的第 <span class="math inline">\(t-1\)</span>个结点为： <span class="math display">\[
    \varPhi_t(i) = argmax_{1 \leq j \leq N}[\delta_{t-1}(j) a_{ji}],\quad i=1,\dots,N 
\]</span> 下面给出维特比算法的流程: &gt; <strong>维特比算法：</strong> &gt; - 输入： 模型<span class="math inline">\(\lambda = (A,B,\pi)\)</span>和观测<span class="math inline">\(O = (o_1,o_2,\dots,o_T)\)</span> &gt; - 输出： 最优路径<span class="math inline">\(I^\ast -= (i_1^\ast,i_2^\ast,\dots,i_T^\ast)\)</span> &gt; 1. 初始化 &gt; <span class="math display">\[
&gt;   \delta_1(i) = \pi_i b_i(o_1),\varPhi_1(i) = 0
&gt; \]</span> &gt; 2. 递推：对<span class="math inline">\(t = 2,3,\dots,T\)</span> &gt; <span class="math display">\[
&gt; \begin{aligned}
&gt;   \delta_{t+1}(i) &amp;= \max_{1 \leq j \leq N} [\delta_t(j) a_{ji}]b_i(o_{t+1}) \\
&gt; \varPhi_t(i) &amp;= argmax_{1 \leq j \leq N}[\delta_{t-1}(j) a_{ji}]
&gt; \end{aligned}
&gt; \]</span> &gt; 3. 终止 &gt; <span class="math display">\[
&gt;   P^* = \max_{1 \leq i \leq N} \delta_T(i), i_T^* = argmax_{1 \leq i \leq N}
&gt; [\delta_T(i)]
&gt; \]</span> &gt; 4. 最优路径回溯。对<span class="math inline">\(t = T-1,T-2,\dots,1\)</span> &gt; <span class="math display">\[
&gt;   i_t^* = \varPhi_{t+1}(i_{t+1}^*)
&gt; \]</span> &gt; 求得最优路径<span class="math inline">\(I^{\ast} = (i_1^\ast,i_2^\ast,\dots,i_T^\ast)\)</span></p>
]]></content>
      <categories>
        <category>统计学习</category>
      </categories>
      <tags>
        <tag>概率模型，HMM</tag>
      </tags>
  </entry>
  <entry>
    <title>pytorch学习</title>
    <url>/2020/09/06/chu-shi-pytorch/</url>
    <content><![CDATA[<p>在前面介绍深度学习的理论知识时，相信大家可以感受到，神经网络的实现主要有以下两个难题： - 当网络结构复杂起来时，手写一个神经网络是非常困难(尤其是进行反向误差传播时)，也是十分费时的。 - 一个神经网络有着大量的参数，对计算机的计算能力要求非常高，而GPU是计算机中有着大量计算资源的部分，如何将这部分计算资源调动起来完成一个深度神经网络的训练是另一个难题。 <a id="more"></a></p>
<p>为了解决上述两个问题，深度学习框架便诞生了，目前主流的深度学习框架主要下图所示的这些: <img src="https://raw.githubusercontent.com/xuejy19/xuejy19.github.io/source/Img/framework.jpeg" alt="主流框架" /> 这些框架比较如下图所示: <img src="https://raw.githubusercontent.com/xuejy19/xuejy19.github.io/source/Img/framework1.png" alt="深度学习框架" /> 目前在科研机构中应用较多的是Tensorflow和pytorch,本文便以pytorch为例来进行讲解，本文内容主要参考官方教程：<a href="https://pytorch.org/tutorials/beginner/blitz/tensor_tutorial.html#sphx-glr-beginner-blitz-tensor-tutorial-py">pytorch官方教程</a>。建议英文阅读能力较强的人还是去阅读原版教程，如果对于算法的理论理解已经到位的话，整个教程还是非常简洁明了的。本文按照以下结构组织: - pytorch简介 - 张量和计算图 - 低级API和高级API</p>
<h3 id="pytorch简介">pytorch简介</h3>
<p>pytorch 是一个开源的python 机器学习库，基于Torch，底层由C++实现，应用于人工智能领域，如自然语言处理(nlp)。它最初由Facebook的人工智能研究团队开发，并且被用于Uber的概率编程软件Pyro. Pytorch 具有以下两大特征: &gt; - 提供类似于Numpy的张量计算，可使用gpu加速 &gt; - 基于自动微分系统的深度神经网络</p>
<h3 id="张量和计算图">张量和计算图</h3>
<p>从caffee以来，基本上所有的深度学习框架都包含这两个概念，其实这两个关键概念也正是针对本文一开始提出的两个问题。 #### 张量 &gt; <strong>张量：</strong> 张量可以看作gpu下 numpy的替代品，在应用传统机器学习算法时，和我们打交道最多python库就是numpy，但遗憾的是numpy并不能够在gpu下进行运算。</p>
<p>输入:</p>
<div class="sourceCode" id="cb1"><pre class="sourceCode python"><code class="sourceCode python"><a class="sourceLine" id="cb1-1" data-line-number="1">torch.randn(<span class="dv">2</span>,<span class="dv">3</span>)</a></code></pre></div>
<p>输出:</p>
<div class="sourceCode" id="cb2"><pre class="sourceCode python"><code class="sourceCode python"><a class="sourceLine" id="cb2-1" data-line-number="1">tensor([[ <span class="fl">1.9335</span>,  <span class="fl">0.8795</span>, <span class="fl">-0.6964</span>],</a>
<a class="sourceLine" id="cb2-2" data-line-number="2">        [ <span class="fl">0.1573</span>,  <span class="fl">1.5692</span>, <span class="fl">-0.2320</span>]])</a></code></pre></div>
<p>tensor的大部分操作与numpy是差不多的，具体属性和方法参考<a href="https://pytorch.org/docs/stable/tensors.html?highlight=torch%20tensor#torch.Tensor">torch.Tensor</a> #### 计算图 现代的神经网络架构可以拥有上百万个参数，从计算的角度看，训练神经网络包含两个阶段: - 用于计算损失函数值的正向传递 - 向后传递以计算可学习参数的梯度</p>
<p>正向传递计算是非常简单的，前一层神经网络的输出是下一层神经网络的输入。后向传递有些复杂，因为这要求我们应用求导的链式法则来计算损失函数的梯度。</p>
<p>当一个神经网络的规模比较小(层数,各层神经元数)时，手算损失函数对各个参数的梯度是可行的，但随着神经网络规模的扩大，梯度的计算变得越来越困难，出于深度学习的这种现实需要，计算图便诞生了： 计算图属于有向无环图，计算图中的节点有两种，一种是基本数学运算符，另一种则是我们所定义的变量。比如下图就可以表示一个简单的神经网络: <span class="math display">\[
    \begin{aligned}
        b &amp;= w_1 * a \\
        c &amp;= w_2 * a \\
        d &amp;= w_3 * b  + w_4 * c \\
        L &amp; = 10 - d
    \end{aligned}
\]</span> 在图中，变量<span class="math inline">\(b,c\)</span>和<span class="math inline">\(d\)</span>都是数学运算的结果，而变量<span class="math inline">\(a ,w_1,w_2,w_3,w_4\)</span> 由用户自己初始化，由于它们不是由任何数学运算符创建的，因此与其创建相对应的节点由其名称本身表示。</p>
<figure>
<img src="https://raw.githubusercontent.com/xuejy19/xuejy19.github.io/source/Img/computation_graph.png" alt="计算图" /><figcaption>计算图</figcaption>
</figure>
<p>下面来介绍如何使用计算图来进行梯度计算，首先我们计算在图中相连接的节点之间的梯度,将对应的梯度值写到边上，如下图所示: <img src="https://raw.githubusercontent.com/xuejy19/xuejy19.github.io/source/Img/full_graph.png" alt="梯度计算图" /> 要计算损失<span class="math inline">\(L\)</span>到某个变量<span class="math inline">\(w\)</span>的梯度只需要按照以下步骤: 1. 找到从<span class="math inline">\(L\)</span>到<span class="math inline">\(w\)</span>所有可能的路径 2. 对于各个路径，将沿路径所有边相乘 3. 将各个路径计算结果相加</p>
<h3 id="低阶api和高阶api">低阶API和高阶API</h3>
<p>我们在使用pytorch时主要接触到的pytorch的包的架构如下图所示: <img src="https://raw.githubusercontent.com/xuejy19/xuejy19.github.io/source/Img/Pytorch-package-hierarchy.jpg" alt="pytorch主要包" /> 对于一个三层的全连结网络，我们可以仅仅使用tensor来实现，不过这种情况下需要手写梯度，下面我就一步步介绍如何用api来使你的代码变得简洁高效: #### tensor</p>
<div class="sourceCode" id="cb3"><pre class="sourceCode python"><code class="sourceCode python"><a class="sourceLine" id="cb3-1" data-line-number="1"><span class="co"># -*- coding: utf-8 -*-</span></a>
<a class="sourceLine" id="cb3-2" data-line-number="2"></a>
<a class="sourceLine" id="cb3-3" data-line-number="3"><span class="im">import</span> torch</a>
<a class="sourceLine" id="cb3-4" data-line-number="4"></a>
<a class="sourceLine" id="cb3-5" data-line-number="5"></a>
<a class="sourceLine" id="cb3-6" data-line-number="6">dtype <span class="op">=</span> torch.<span class="bu">float</span></a>
<a class="sourceLine" id="cb3-7" data-line-number="7">device <span class="op">=</span> torch.device(<span class="st">&quot;cpu&quot;</span>)</a>
<a class="sourceLine" id="cb3-8" data-line-number="8"><span class="co"># device = torch.device(&quot;cuda:0&quot;) # Uncomment this to run on GPU</span></a>
<a class="sourceLine" id="cb3-9" data-line-number="9"></a>
<a class="sourceLine" id="cb3-10" data-line-number="10"><span class="co"># N is batch size; D_in is input dimension;</span></a>
<a class="sourceLine" id="cb3-11" data-line-number="11"><span class="co"># H is hidden dimension; D_out is output dimension.</span></a>
<a class="sourceLine" id="cb3-12" data-line-number="12">N, D_in, H, D_out <span class="op">=</span> <span class="dv">64</span>, <span class="dv">1000</span>, <span class="dv">100</span>, <span class="dv">10</span></a>
<a class="sourceLine" id="cb3-13" data-line-number="13"></a>
<a class="sourceLine" id="cb3-14" data-line-number="14"><span class="co"># Create random input and output data</span></a>
<a class="sourceLine" id="cb3-15" data-line-number="15">x <span class="op">=</span> torch.randn(N, D_in, device<span class="op">=</span>device, dtype<span class="op">=</span>dtype)</a>
<a class="sourceLine" id="cb3-16" data-line-number="16">y <span class="op">=</span> torch.randn(N, D_out, device<span class="op">=</span>device, dtype<span class="op">=</span>dtype)</a>
<a class="sourceLine" id="cb3-17" data-line-number="17"></a>
<a class="sourceLine" id="cb3-18" data-line-number="18"><span class="co"># Randomly initialize weights</span></a>
<a class="sourceLine" id="cb3-19" data-line-number="19">w1 <span class="op">=</span> torch.randn(D_in, H, device<span class="op">=</span>device, dtype<span class="op">=</span>dtype)</a>
<a class="sourceLine" id="cb3-20" data-line-number="20">w2 <span class="op">=</span> torch.randn(H, D_out, device<span class="op">=</span>device, dtype<span class="op">=</span>dtype)</a>
<a class="sourceLine" id="cb3-21" data-line-number="21"></a>
<a class="sourceLine" id="cb3-22" data-line-number="22">learning_rate <span class="op">=</span> <span class="fl">1e-6</span></a>
<a class="sourceLine" id="cb3-23" data-line-number="23"><span class="cf">for</span> t <span class="kw">in</span> <span class="bu">range</span>(<span class="dv">500</span>):</a>
<a class="sourceLine" id="cb3-24" data-line-number="24">    <span class="co"># Forward pass: compute predicted y</span></a>
<a class="sourceLine" id="cb3-25" data-line-number="25">    h <span class="op">=</span> x.mm(w1)</a>
<a class="sourceLine" id="cb3-26" data-line-number="26">    h_relu <span class="op">=</span> h.clamp(<span class="bu">min</span><span class="op">=</span><span class="dv">0</span>)</a>
<a class="sourceLine" id="cb3-27" data-line-number="27">    y_pred <span class="op">=</span> h_relu.mm(w2)</a>
<a class="sourceLine" id="cb3-28" data-line-number="28"></a>
<a class="sourceLine" id="cb3-29" data-line-number="29">    <span class="co"># Compute and print loss</span></a>
<a class="sourceLine" id="cb3-30" data-line-number="30">    loss <span class="op">=</span> (y_pred <span class="op">-</span> y).<span class="bu">pow</span>(<span class="dv">2</span>).<span class="bu">sum</span>().item()</a>
<a class="sourceLine" id="cb3-31" data-line-number="31">    <span class="cf">if</span> t <span class="op">%</span> <span class="dv">100</span> <span class="op">==</span> <span class="dv">99</span>:</a>
<a class="sourceLine" id="cb3-32" data-line-number="32">        <span class="bu">print</span>(t, loss)</a>
<a class="sourceLine" id="cb3-33" data-line-number="33"></a>
<a class="sourceLine" id="cb3-34" data-line-number="34">    <span class="co"># Backprop to compute gradients of w1 and w2 with respect to loss</span></a>
<a class="sourceLine" id="cb3-35" data-line-number="35">    grad_y_pred <span class="op">=</span> <span class="fl">2.0</span> <span class="op">*</span> (y_pred <span class="op">-</span> y)</a>
<a class="sourceLine" id="cb3-36" data-line-number="36">    grad_w2 <span class="op">=</span> h_relu.t().mm(grad_y_pred)</a>
<a class="sourceLine" id="cb3-37" data-line-number="37">    grad_h_relu <span class="op">=</span> grad_y_pred.mm(w2.t())</a>
<a class="sourceLine" id="cb3-38" data-line-number="38">    grad_h <span class="op">=</span> grad_h_relu.clone()</a>
<a class="sourceLine" id="cb3-39" data-line-number="39">    grad_h[h <span class="op">&lt;</span> <span class="dv">0</span>] <span class="op">=</span> <span class="dv">0</span></a>
<a class="sourceLine" id="cb3-40" data-line-number="40">    grad_w1 <span class="op">=</span> x.t().mm(grad_h)</a>
<a class="sourceLine" id="cb3-41" data-line-number="41"></a>
<a class="sourceLine" id="cb3-42" data-line-number="42">    <span class="co"># Update weights using gradient descent</span></a>
<a class="sourceLine" id="cb3-43" data-line-number="43">    w1 <span class="op">-=</span> learning_rate <span class="op">*</span> grad_w1</a>
<a class="sourceLine" id="cb3-44" data-line-number="44">    w2 <span class="op">-=</span> learning_rate <span class="op">*</span> grad_w2</a></code></pre></div>
<h4 id="autograd">autograd</h4>
<p>这种情况下其实是没有显示的模型的概念的，只有自己定义的一个个参数。但当神经网络复杂之后，手算梯度变得不再现实，pytorch为解决这个问题提供了<code>autograd</code>这样一个好用的工具，如果我们将需要计算梯度的参数设置为<code>requires_grad = True</code>,那么在进行前向计算过程中，会定义一个计算图，同时会保存对应参数的梯度信息，也就是前面介绍的计算图中边上的梯度信息，通过<code>loss.backward()</code>操作，可以将损失对各个参数的梯度计算出来，由此可以重写上面的代码如下:</p>
<div class="sourceCode" id="cb4"><pre class="sourceCode python"><code class="sourceCode python"><a class="sourceLine" id="cb4-1" data-line-number="1"><span class="co"># -*- coding: utf-8 -*-</span></a>
<a class="sourceLine" id="cb4-2" data-line-number="2"><span class="co"># -*- coding: utf-8 -*-</span></a>
<a class="sourceLine" id="cb4-3" data-line-number="3"><span class="im">import</span> torch</a>
<a class="sourceLine" id="cb4-4" data-line-number="4"></a>
<a class="sourceLine" id="cb4-5" data-line-number="5">dtype <span class="op">=</span> torch.<span class="bu">float</span></a>
<a class="sourceLine" id="cb4-6" data-line-number="6">device <span class="op">=</span> torch.device(<span class="st">&quot;cpu&quot;</span>)</a>
<a class="sourceLine" id="cb4-7" data-line-number="7"><span class="co"># device = torch.device(&quot;cuda:0&quot;) # Uncomment this to run on GPU</span></a>
<a class="sourceLine" id="cb4-8" data-line-number="8"></a>
<a class="sourceLine" id="cb4-9" data-line-number="9"><span class="co"># N is batch size; D_in is input dimension;</span></a>
<a class="sourceLine" id="cb4-10" data-line-number="10"><span class="co"># H is hidden dimension; D_out is output dimension.</span></a>
<a class="sourceLine" id="cb4-11" data-line-number="11">N, D_in, H, D_out <span class="op">=</span> <span class="dv">64</span>, <span class="dv">1000</span>, <span class="dv">100</span>, <span class="dv">10</span></a>
<a class="sourceLine" id="cb4-12" data-line-number="12"></a>
<a class="sourceLine" id="cb4-13" data-line-number="13"><span class="co"># Create random Tensors to hold input and outputs.</span></a>
<a class="sourceLine" id="cb4-14" data-line-number="14"><span class="co"># Setting requires_grad=False indicates that we do not need to compute gradients</span></a>
<a class="sourceLine" id="cb4-15" data-line-number="15"><span class="co"># with respect to these Tensors during the backward pass.</span></a>
<a class="sourceLine" id="cb4-16" data-line-number="16">x <span class="op">=</span> torch.randn(N, D_in, device<span class="op">=</span>device, dtype<span class="op">=</span>dtype)</a>
<a class="sourceLine" id="cb4-17" data-line-number="17">y <span class="op">=</span> torch.randn(N, D_out, device<span class="op">=</span>device, dtype<span class="op">=</span>dtype)</a>
<a class="sourceLine" id="cb4-18" data-line-number="18"></a>
<a class="sourceLine" id="cb4-19" data-line-number="19"><span class="co"># Create random Tensors for weights.</span></a>
<a class="sourceLine" id="cb4-20" data-line-number="20"><span class="co"># Setting requires_grad=True indicates that we want to compute gradients with</span></a>
<a class="sourceLine" id="cb4-21" data-line-number="21"><span class="co"># respect to these Tensors during the backward pass.</span></a>
<a class="sourceLine" id="cb4-22" data-line-number="22">w1 <span class="op">=</span> torch.randn(D_in, H, device<span class="op">=</span>device, dtype<span class="op">=</span>dtype, requires_grad<span class="op">=</span><span class="va">True</span>)</a>
<a class="sourceLine" id="cb4-23" data-line-number="23">w2 <span class="op">=</span> torch.randn(H, D_out, device<span class="op">=</span>device, dtype<span class="op">=</span>dtype, requires_grad<span class="op">=</span><span class="va">True</span>)</a>
<a class="sourceLine" id="cb4-24" data-line-number="24"></a>
<a class="sourceLine" id="cb4-25" data-line-number="25">learning_rate <span class="op">=</span> <span class="fl">1e-6</span></a>
<a class="sourceLine" id="cb4-26" data-line-number="26"><span class="cf">for</span> t <span class="kw">in</span> <span class="bu">range</span>(<span class="dv">500</span>):</a>
<a class="sourceLine" id="cb4-27" data-line-number="27">    <span class="co"># Forward pass: compute predicted y using operations on Tensors; these</span></a>
<a class="sourceLine" id="cb4-28" data-line-number="28">    <span class="co"># are exactly the same operations we used to compute the forward pass using</span></a>
<a class="sourceLine" id="cb4-29" data-line-number="29">    <span class="co"># Tensors, but we do not need to keep references to intermediate values since</span></a>
<a class="sourceLine" id="cb4-30" data-line-number="30">    <span class="co"># we are not implementing the backward pass by hand.</span></a>
<a class="sourceLine" id="cb4-31" data-line-number="31">    y_pred <span class="op">=</span> x.mm(w1).clamp(<span class="bu">min</span><span class="op">=</span><span class="dv">0</span>).mm(w2)</a>
<a class="sourceLine" id="cb4-32" data-line-number="32"></a>
<a class="sourceLine" id="cb4-33" data-line-number="33">    <span class="co"># Compute and print loss using operations on Tensors.</span></a>
<a class="sourceLine" id="cb4-34" data-line-number="34">    <span class="co"># Now loss is a Tensor of shape (1,)</span></a>
<a class="sourceLine" id="cb4-35" data-line-number="35">    <span class="co"># loss.item() gets the scalar value held in the loss.</span></a>
<a class="sourceLine" id="cb4-36" data-line-number="36">    loss <span class="op">=</span> (y_pred <span class="op">-</span> y).<span class="bu">pow</span>(<span class="dv">2</span>).<span class="bu">sum</span>()</a>
<a class="sourceLine" id="cb4-37" data-line-number="37">    <span class="cf">if</span> t <span class="op">%</span> <span class="dv">100</span> <span class="op">==</span> <span class="dv">99</span>:</a>
<a class="sourceLine" id="cb4-38" data-line-number="38">        <span class="bu">print</span>(t, loss.item())</a>
<a class="sourceLine" id="cb4-39" data-line-number="39"></a>
<a class="sourceLine" id="cb4-40" data-line-number="40">    <span class="co"># Use autograd to compute the backward pass. This call will compute the</span></a>
<a class="sourceLine" id="cb4-41" data-line-number="41">    <span class="co"># gradient of loss with respect to all Tensors with requires_grad=True.</span></a>
<a class="sourceLine" id="cb4-42" data-line-number="42">    <span class="co"># After this call w1.grad and w2.grad will be Tensors holding the gradient</span></a>
<a class="sourceLine" id="cb4-43" data-line-number="43">    <span class="co"># of the loss with respect to w1 and w2 respectively.</span></a>
<a class="sourceLine" id="cb4-44" data-line-number="44">    loss.backward()</a>
<a class="sourceLine" id="cb4-45" data-line-number="45"></a>
<a class="sourceLine" id="cb4-46" data-line-number="46">    <span class="co"># Manually update weights using gradient descent. Wrap in torch.no_grad()</span></a>
<a class="sourceLine" id="cb4-47" data-line-number="47">    <span class="co"># because weights have requires_grad=True, but we don&#39;t need to track this</span></a>
<a class="sourceLine" id="cb4-48" data-line-number="48">    <span class="co"># in autograd.</span></a>
<a class="sourceLine" id="cb4-49" data-line-number="49">    <span class="co"># An alternative way is to operate on weight.data and weight.grad.data.</span></a>
<a class="sourceLine" id="cb4-50" data-line-number="50">    <span class="co"># Recall that tensor.data gives a tensor that shares the storage with</span></a>
<a class="sourceLine" id="cb4-51" data-line-number="51">    <span class="co"># tensor, but doesn&#39;t track history.</span></a>
<a class="sourceLine" id="cb4-52" data-line-number="52">    <span class="co"># You can also use torch.optim.SGD to achieve this.</span></a>
<a class="sourceLine" id="cb4-53" data-line-number="53">    <span class="cf">with</span> torch.no_grad():</a>
<a class="sourceLine" id="cb4-54" data-line-number="54">        w1 <span class="op">-=</span> learning_rate <span class="op">*</span> w1.grad</a>
<a class="sourceLine" id="cb4-55" data-line-number="55">        w2 <span class="op">-=</span> learning_rate <span class="op">*</span> w2.grad</a>
<a class="sourceLine" id="cb4-56" data-line-number="56"></a>
<a class="sourceLine" id="cb4-57" data-line-number="57">        <span class="co"># Manually zero the gradients after updating weights</span></a>
<a class="sourceLine" id="cb4-58" data-line-number="58">        w1.grad.zero_()</a>
<a class="sourceLine" id="cb4-59" data-line-number="59">        w2.grad.zero_()</a></code></pre></div>
<h4 id="extend-autograd-function">Extend autograd function</h4>
<p>可以看到，有了<code>autograd</code>这个工具后，复杂网络的反向梯度计算也很容易解决。虽然<code>autograd.Function</code>中已经基本包含了我们所经常用到的运算函数(自动微分算子)，但若我们希望将一个其中没有的函数也加入到计算图中，则需要对<code>autograd.Function</code>进行扩展，比如我们可以重写<code>ReLu</code>函数，然后将其纳入自动微分:</p>
<div class="sourceCode" id="cb5"><pre class="sourceCode python"><code class="sourceCode python"><a class="sourceLine" id="cb5-1" data-line-number="1"><span class="co"># -*- coding: utf-8 -*-</span></a>
<a class="sourceLine" id="cb5-2" data-line-number="2"><span class="im">import</span> torch</a>
<a class="sourceLine" id="cb5-3" data-line-number="3"></a>
<a class="sourceLine" id="cb5-4" data-line-number="4"></a>
<a class="sourceLine" id="cb5-5" data-line-number="5"><span class="kw">class</span> MyReLU(torch.autograd.Function):</a>
<a class="sourceLine" id="cb5-6" data-line-number="6">    <span class="co">&quot;&quot;&quot;</span></a>
<a class="sourceLine" id="cb5-7" data-line-number="7"><span class="co">    We can implement our own custom autograd Functions by subclassing</span></a>
<a class="sourceLine" id="cb5-8" data-line-number="8"><span class="co">    torch.autograd.Function and implementing the forward and backward passes</span></a>
<a class="sourceLine" id="cb5-9" data-line-number="9"><span class="co">    which operate on Tensors.</span></a>
<a class="sourceLine" id="cb5-10" data-line-number="10"><span class="co">    &quot;&quot;&quot;</span></a>
<a class="sourceLine" id="cb5-11" data-line-number="11"></a>
<a class="sourceLine" id="cb5-12" data-line-number="12">    <span class="at">@staticmethod</span></a>
<a class="sourceLine" id="cb5-13" data-line-number="13">    <span class="kw">def</span> forward(ctx, <span class="bu">input</span>):</a>
<a class="sourceLine" id="cb5-14" data-line-number="14">        <span class="co">&quot;&quot;&quot;</span></a>
<a class="sourceLine" id="cb5-15" data-line-number="15"><span class="co">        In the forward pass we receive a Tensor containing the input and return</span></a>
<a class="sourceLine" id="cb5-16" data-line-number="16"><span class="co">        a Tensor containing the output. ctx is a context object that can be used</span></a>
<a class="sourceLine" id="cb5-17" data-line-number="17"><span class="co">        to stash information for backward computation. You can cache arbitrary</span></a>
<a class="sourceLine" id="cb5-18" data-line-number="18"><span class="co">        objects for use in the backward pass using the ctx.save_for_backward method.</span></a>
<a class="sourceLine" id="cb5-19" data-line-number="19"><span class="co">        &quot;&quot;&quot;</span></a>
<a class="sourceLine" id="cb5-20" data-line-number="20">        ctx.save_for_backward(<span class="bu">input</span>)</a>
<a class="sourceLine" id="cb5-21" data-line-number="21">        <span class="cf">return</span> <span class="bu">input</span>.clamp(<span class="bu">min</span><span class="op">=</span><span class="dv">0</span>)</a>
<a class="sourceLine" id="cb5-22" data-line-number="22"></a>
<a class="sourceLine" id="cb5-23" data-line-number="23">    <span class="at">@staticmethod</span></a>
<a class="sourceLine" id="cb5-24" data-line-number="24">    <span class="kw">def</span> backward(ctx, grad_output):</a>
<a class="sourceLine" id="cb5-25" data-line-number="25">        <span class="co">&quot;&quot;&quot;</span></a>
<a class="sourceLine" id="cb5-26" data-line-number="26"><span class="co">        In the backward pass we receive a Tensor containing the gradient of the loss</span></a>
<a class="sourceLine" id="cb5-27" data-line-number="27"><span class="co">        with respect to the output, and we need to compute the gradient of the loss</span></a>
<a class="sourceLine" id="cb5-28" data-line-number="28"><span class="co">        with respect to the input.</span></a>
<a class="sourceLine" id="cb5-29" data-line-number="29"><span class="co">        &quot;&quot;&quot;</span></a>
<a class="sourceLine" id="cb5-30" data-line-number="30">        <span class="bu">input</span>, <span class="op">=</span> ctx.saved_tensors</a>
<a class="sourceLine" id="cb5-31" data-line-number="31">        grad_input <span class="op">=</span> grad_output.clone()</a>
<a class="sourceLine" id="cb5-32" data-line-number="32">        grad_input[<span class="bu">input</span> <span class="op">&lt;</span> <span class="dv">0</span>] <span class="op">=</span> <span class="dv">0</span></a>
<a class="sourceLine" id="cb5-33" data-line-number="33">        <span class="cf">return</span> grad_input</a>
<a class="sourceLine" id="cb5-34" data-line-number="34"></a>
<a class="sourceLine" id="cb5-35" data-line-number="35"></a>
<a class="sourceLine" id="cb5-36" data-line-number="36">dtype <span class="op">=</span> torch.<span class="bu">float</span></a>
<a class="sourceLine" id="cb5-37" data-line-number="37">device <span class="op">=</span> torch.device(<span class="st">&quot;cpu&quot;</span>)</a>
<a class="sourceLine" id="cb5-38" data-line-number="38"><span class="co"># device = torch.device(&quot;cuda:0&quot;) # Uncomment this to run on GPU</span></a>
<a class="sourceLine" id="cb5-39" data-line-number="39"></a>
<a class="sourceLine" id="cb5-40" data-line-number="40"><span class="co"># N is batch size; D_in is input dimension;</span></a>
<a class="sourceLine" id="cb5-41" data-line-number="41"><span class="co"># H is hidden dimension; D_out is output dimension.</span></a>
<a class="sourceLine" id="cb5-42" data-line-number="42">N, D_in, H, D_out <span class="op">=</span> <span class="dv">64</span>, <span class="dv">1000</span>, <span class="dv">100</span>, <span class="dv">10</span></a>
<a class="sourceLine" id="cb5-43" data-line-number="43"></a>
<a class="sourceLine" id="cb5-44" data-line-number="44"><span class="co"># Create random Tensors to hold input and outputs.</span></a>
<a class="sourceLine" id="cb5-45" data-line-number="45">x <span class="op">=</span> torch.randn(N, D_in, device<span class="op">=</span>device, dtype<span class="op">=</span>dtype)</a>
<a class="sourceLine" id="cb5-46" data-line-number="46">y <span class="op">=</span> torch.randn(N, D_out, device<span class="op">=</span>device, dtype<span class="op">=</span>dtype)</a>
<a class="sourceLine" id="cb5-47" data-line-number="47"></a>
<a class="sourceLine" id="cb5-48" data-line-number="48"><span class="co"># Create random Tensors for weights.</span></a>
<a class="sourceLine" id="cb5-49" data-line-number="49">w1 <span class="op">=</span> torch.randn(D_in, H, device<span class="op">=</span>device, dtype<span class="op">=</span>dtype, requires_grad<span class="op">=</span><span class="va">True</span>)</a>
<a class="sourceLine" id="cb5-50" data-line-number="50">w2 <span class="op">=</span> torch.randn(H, D_out, device<span class="op">=</span>device, dtype<span class="op">=</span>dtype, requires_grad<span class="op">=</span><span class="va">True</span>)</a>
<a class="sourceLine" id="cb5-51" data-line-number="51"></a>
<a class="sourceLine" id="cb5-52" data-line-number="52">learning_rate <span class="op">=</span> <span class="fl">1e-6</span></a>
<a class="sourceLine" id="cb5-53" data-line-number="53"><span class="cf">for</span> t <span class="kw">in</span> <span class="bu">range</span>(<span class="dv">500</span>):</a>
<a class="sourceLine" id="cb5-54" data-line-number="54">    <span class="co"># To apply our Function, we use Function.apply method. We alias this as &#39;relu&#39;.</span></a>
<a class="sourceLine" id="cb5-55" data-line-number="55">    relu <span class="op">=</span> MyReLU.<span class="bu">apply</span></a>
<a class="sourceLine" id="cb5-56" data-line-number="56"></a>
<a class="sourceLine" id="cb5-57" data-line-number="57">    <span class="co"># Forward pass: compute predicted y using operations; we compute</span></a>
<a class="sourceLine" id="cb5-58" data-line-number="58">    <span class="co"># ReLU using our custom autograd operation.</span></a>
<a class="sourceLine" id="cb5-59" data-line-number="59">    y_pred <span class="op">=</span> relu(x.mm(w1)).mm(w2)</a>
<a class="sourceLine" id="cb5-60" data-line-number="60"></a>
<a class="sourceLine" id="cb5-61" data-line-number="61">    <span class="co"># Compute and print loss</span></a>
<a class="sourceLine" id="cb5-62" data-line-number="62">    loss <span class="op">=</span> (y_pred <span class="op">-</span> y).<span class="bu">pow</span>(<span class="dv">2</span>).<span class="bu">sum</span>()</a>
<a class="sourceLine" id="cb5-63" data-line-number="63">    <span class="cf">if</span> t <span class="op">%</span> <span class="dv">100</span> <span class="op">==</span> <span class="dv">99</span>:</a>
<a class="sourceLine" id="cb5-64" data-line-number="64">        <span class="bu">print</span>(t, loss.item())</a>
<a class="sourceLine" id="cb5-65" data-line-number="65"></a>
<a class="sourceLine" id="cb5-66" data-line-number="66">    <span class="co"># Use autograd to compute the backward pass.</span></a>
<a class="sourceLine" id="cb5-67" data-line-number="67">    loss.backward()</a>
<a class="sourceLine" id="cb5-68" data-line-number="68"></a>
<a class="sourceLine" id="cb5-69" data-line-number="69">    <span class="co"># Update weights using gradient descent</span></a>
<a class="sourceLine" id="cb5-70" data-line-number="70">    <span class="cf">with</span> torch.no_grad():</a>
<a class="sourceLine" id="cb5-71" data-line-number="71">        w1 <span class="op">-=</span> learning_rate <span class="op">*</span> w1.grad</a>
<a class="sourceLine" id="cb5-72" data-line-number="72">        w2 <span class="op">-=</span> learning_rate <span class="op">*</span> w2.grad</a>
<a class="sourceLine" id="cb5-73" data-line-number="73"></a>
<a class="sourceLine" id="cb5-74" data-line-number="74">        <span class="co"># Manually zero the gradients after updating weights</span></a>
<a class="sourceLine" id="cb5-75" data-line-number="75">        w1.grad.zero_()</a>
<a class="sourceLine" id="cb5-76" data-line-number="76">        w2.grad.zero_()</a></code></pre></div>
<h4 id="nn.module">nn.Module</h4>
<p>如果我们要构建一个深层神经网络，固然可以用这些基本的自动微分算子来构建，但这样做显得有些<code>low-level</code>，因为神经网络在进行理论推导时实际上就是<code>层的堆砌</code>，那么在进行编程实现时，可否沿用这个思想，将一些微分算子封装成神经网络层的形式，pytorch中实现这个功能是通过包<code>nn</code>，<code>nn</code>中除了定义了一系列模块<code>Modules</code>，同时还有大量的损失函数，下面我们就用<code>torch.nn.Sequential</code>来重新实现这样一个三层神经网络:</p>
<div class="sourceCode" id="cb6"><pre class="sourceCode python"><code class="sourceCode python"><a class="sourceLine" id="cb6-1" data-line-number="1"><span class="im">import</span> torch</a>
<a class="sourceLine" id="cb6-2" data-line-number="2"></a>
<a class="sourceLine" id="cb6-3" data-line-number="3"><span class="co"># N is batch size; D_in is input dimension;</span></a>
<a class="sourceLine" id="cb6-4" data-line-number="4"><span class="co"># H is hidden dimension; D_out is output dimension.</span></a>
<a class="sourceLine" id="cb6-5" data-line-number="5">N, D_in, H, D_out <span class="op">=</span> <span class="dv">64</span>, <span class="dv">1000</span>, <span class="dv">100</span>, <span class="dv">10</span></a>
<a class="sourceLine" id="cb6-6" data-line-number="6"></a>
<a class="sourceLine" id="cb6-7" data-line-number="7"><span class="co"># Create random Tensors to hold inputs and outputs</span></a>
<a class="sourceLine" id="cb6-8" data-line-number="8">x <span class="op">=</span> torch.randn(N, D_in)</a>
<a class="sourceLine" id="cb6-9" data-line-number="9">y <span class="op">=</span> torch.randn(N, D_out)</a>
<a class="sourceLine" id="cb6-10" data-line-number="10"></a>
<a class="sourceLine" id="cb6-11" data-line-number="11"><span class="co"># Use the nn package to define our model as a sequence of layers. nn.Sequential</span></a>
<a class="sourceLine" id="cb6-12" data-line-number="12"><span class="co"># is a Module which contains other Modules, and applies them in sequence to</span></a>
<a class="sourceLine" id="cb6-13" data-line-number="13"><span class="co"># produce its output. Each Linear Module computes output from input using a</span></a>
<a class="sourceLine" id="cb6-14" data-line-number="14"><span class="co"># linear function, and holds internal Tensors for its weight and bias.</span></a>
<a class="sourceLine" id="cb6-15" data-line-number="15">model <span class="op">=</span> torch.nn.Sequential(</a>
<a class="sourceLine" id="cb6-16" data-line-number="16">    torch.nn.Linear(D_in, H),</a>
<a class="sourceLine" id="cb6-17" data-line-number="17">    torch.nn.ReLU(),</a>
<a class="sourceLine" id="cb6-18" data-line-number="18">    torch.nn.Linear(H, D_out),</a>
<a class="sourceLine" id="cb6-19" data-line-number="19">)</a>
<a class="sourceLine" id="cb6-20" data-line-number="20"></a>
<a class="sourceLine" id="cb6-21" data-line-number="21"><span class="co"># The nn package also contains definitions of popular loss functions; in this</span></a>
<a class="sourceLine" id="cb6-22" data-line-number="22"><span class="co"># case we will use Mean Squared Error (MSE) as our loss function.</span></a>
<a class="sourceLine" id="cb6-23" data-line-number="23">loss_fn <span class="op">=</span> torch.nn.MSELoss(reduction<span class="op">=</span><span class="st">&#39;sum&#39;</span>)</a>
<a class="sourceLine" id="cb6-24" data-line-number="24"></a>
<a class="sourceLine" id="cb6-25" data-line-number="25">learning_rate <span class="op">=</span> <span class="fl">1e-4</span></a>
<a class="sourceLine" id="cb6-26" data-line-number="26"><span class="cf">for</span> t <span class="kw">in</span> <span class="bu">range</span>(<span class="dv">500</span>):</a>
<a class="sourceLine" id="cb6-27" data-line-number="27">    <span class="co"># Forward pass: compute predicted y by passing x to the model. Module objects</span></a>
<a class="sourceLine" id="cb6-28" data-line-number="28">    <span class="co"># override the __call__ operator so you can call them like functions. When</span></a>
<a class="sourceLine" id="cb6-29" data-line-number="29">    <span class="co"># doing so you pass a Tensor of input data to the Module and it produces</span></a>
<a class="sourceLine" id="cb6-30" data-line-number="30">    <span class="co"># a Tensor of output data.</span></a>
<a class="sourceLine" id="cb6-31" data-line-number="31">    y_pred <span class="op">=</span> model(x)</a>
<a class="sourceLine" id="cb6-32" data-line-number="32"></a>
<a class="sourceLine" id="cb6-33" data-line-number="33">    <span class="co"># Compute and print loss. We pass Tensors containing the predicted and true</span></a>
<a class="sourceLine" id="cb6-34" data-line-number="34">    <span class="co"># values of y, and the loss function returns a Tensor containing the</span></a>
<a class="sourceLine" id="cb6-35" data-line-number="35">    <span class="co"># loss.</span></a>
<a class="sourceLine" id="cb6-36" data-line-number="36">    loss <span class="op">=</span> loss_fn(y_pred, y)</a>
<a class="sourceLine" id="cb6-37" data-line-number="37">    <span class="cf">if</span> t <span class="op">%</span> <span class="dv">100</span> <span class="op">==</span> <span class="dv">99</span>:</a>
<a class="sourceLine" id="cb6-38" data-line-number="38">        <span class="bu">print</span>(t, loss.item())</a>
<a class="sourceLine" id="cb6-39" data-line-number="39"></a>
<a class="sourceLine" id="cb6-40" data-line-number="40">    <span class="co"># Zero the gradients before running the backward pass.</span></a>
<a class="sourceLine" id="cb6-41" data-line-number="41">    model.zero_grad()</a>
<a class="sourceLine" id="cb6-42" data-line-number="42"></a>
<a class="sourceLine" id="cb6-43" data-line-number="43">    <span class="co"># Backward pass: compute gradient of the loss with respect to all the learnable</span></a>
<a class="sourceLine" id="cb6-44" data-line-number="44">    <span class="co"># parameters of the model. Internally, the parameters of each Module are stored</span></a>
<a class="sourceLine" id="cb6-45" data-line-number="45">    <span class="co"># in Tensors with requires_grad=True, so this call will compute gradients for</span></a>
<a class="sourceLine" id="cb6-46" data-line-number="46">    <span class="co"># all learnable parameters in the model.</span></a>
<a class="sourceLine" id="cb6-47" data-line-number="47">    loss.backward()</a>
<a class="sourceLine" id="cb6-48" data-line-number="48"></a>
<a class="sourceLine" id="cb6-49" data-line-number="49">    <span class="co"># Update the weights using gradient descent. Each parameter is a Tensor, so</span></a>
<a class="sourceLine" id="cb6-50" data-line-number="50">    <span class="co"># we can access its gradients like we did before.</span></a>
<a class="sourceLine" id="cb6-51" data-line-number="51">    <span class="cf">with</span> torch.no_grad():</a>
<a class="sourceLine" id="cb6-52" data-line-number="52">        <span class="cf">for</span> param <span class="kw">in</span> model.parameters():</a>
<a class="sourceLine" id="cb6-53" data-line-number="53">            param <span class="op">-=</span> learning_rate <span class="op">*</span> param.grad</a></code></pre></div>
<p>可以看到，与不使用<code>nn</code>包相比，上面的代码实现一方面利用<code>nn.Module.Sequential</code>实现了对神经网络层的封装，另一方面应用<code>nn.MSELoss</code>来替代原始的手写损失函数，使得代码的可读性和简洁性进一步提升。同时需要注意的是，在进行反向传播之前通过<code>model.zero_grad()</code>将存储的梯度归0，在进行参数更新时必须要使用<code>with torch.no_grad()</code>上下文管理语句来避免在进行参数更新时仍记录梯度，提高计算速度。</p>
<h4 id="optim">optim</h4>
<p>在实际进行优化时，我们不仅会用到<code>SGD</code>这种常规的优化算法，同时也会用到一些复杂的优化算法如<code>AdaGrad</code>,<code>RMSprop</code>, <code>Adam</code>等，<code>torch.optim</code>提供了大部分常用的优化算法，同时应用优化器后也不需要再使用<code>with torch.no_grad()</code>语句，可以直接使用<code>optimizer.step()</code>来实现参数更新，重写这个三层神经网络:</p>
<div class="sourceCode" id="cb7"><pre class="sourceCode python"><code class="sourceCode python"><a class="sourceLine" id="cb7-1" data-line-number="1"><span class="co"># -*- coding: utf-8 -*-</span></a>
<a class="sourceLine" id="cb7-2" data-line-number="2"><span class="im">import</span> torch</a>
<a class="sourceLine" id="cb7-3" data-line-number="3"></a>
<a class="sourceLine" id="cb7-4" data-line-number="4"><span class="co"># N is batch size; D_in is input dimension;</span></a>
<a class="sourceLine" id="cb7-5" data-line-number="5"><span class="co"># H is hidden dimension; D_out is output dimension.</span></a>
<a class="sourceLine" id="cb7-6" data-line-number="6">N, D_in, H, D_out <span class="op">=</span> <span class="dv">64</span>, <span class="dv">1000</span>, <span class="dv">100</span>, <span class="dv">10</span></a>
<a class="sourceLine" id="cb7-7" data-line-number="7"></a>
<a class="sourceLine" id="cb7-8" data-line-number="8"><span class="co"># Create random Tensors to hold inputs and outputs</span></a>
<a class="sourceLine" id="cb7-9" data-line-number="9">x <span class="op">=</span> torch.randn(N, D_in)</a>
<a class="sourceLine" id="cb7-10" data-line-number="10">y <span class="op">=</span> torch.randn(N, D_out)</a>
<a class="sourceLine" id="cb7-11" data-line-number="11"></a>
<a class="sourceLine" id="cb7-12" data-line-number="12"><span class="co"># Use the nn package to define our model and loss function.</span></a>
<a class="sourceLine" id="cb7-13" data-line-number="13">model <span class="op">=</span> torch.nn.Sequential(</a>
<a class="sourceLine" id="cb7-14" data-line-number="14">    torch.nn.Linear(D_in, H),</a>
<a class="sourceLine" id="cb7-15" data-line-number="15">    torch.nn.ReLU(),</a>
<a class="sourceLine" id="cb7-16" data-line-number="16">    torch.nn.Linear(H, D_out),</a>
<a class="sourceLine" id="cb7-17" data-line-number="17">)</a>
<a class="sourceLine" id="cb7-18" data-line-number="18">loss_fn <span class="op">=</span> torch.nn.MSELoss(reduction<span class="op">=</span><span class="st">&#39;sum&#39;</span>)</a>
<a class="sourceLine" id="cb7-19" data-line-number="19"></a>
<a class="sourceLine" id="cb7-20" data-line-number="20"><span class="co"># Use the optim package to define an Optimizer that will update the weights of</span></a>
<a class="sourceLine" id="cb7-21" data-line-number="21"><span class="co"># the model for us. Here we will use Adam; the optim package contains many other</span></a>
<a class="sourceLine" id="cb7-22" data-line-number="22"><span class="co"># optimization algorithms. The first argument to the Adam constructor tells the</span></a>
<a class="sourceLine" id="cb7-23" data-line-number="23"><span class="co"># optimizer which Tensors it should update.</span></a>
<a class="sourceLine" id="cb7-24" data-line-number="24">learning_rate <span class="op">=</span> <span class="fl">1e-4</span></a>
<a class="sourceLine" id="cb7-25" data-line-number="25">optimizer <span class="op">=</span> torch.optim.Adam(model.parameters(), lr<span class="op">=</span>learning_rate)</a>
<a class="sourceLine" id="cb7-26" data-line-number="26"><span class="cf">for</span> t <span class="kw">in</span> <span class="bu">range</span>(<span class="dv">500</span>):</a>
<a class="sourceLine" id="cb7-27" data-line-number="27">    <span class="co"># Forward pass: compute predicted y by passing x to the model.</span></a>
<a class="sourceLine" id="cb7-28" data-line-number="28">    y_pred <span class="op">=</span> model(x)</a>
<a class="sourceLine" id="cb7-29" data-line-number="29"></a>
<a class="sourceLine" id="cb7-30" data-line-number="30">    <span class="co"># Compute and print loss.</span></a>
<a class="sourceLine" id="cb7-31" data-line-number="31">    loss <span class="op">=</span> loss_fn(y_pred, y)</a>
<a class="sourceLine" id="cb7-32" data-line-number="32">    <span class="cf">if</span> t <span class="op">%</span> <span class="dv">100</span> <span class="op">==</span> <span class="dv">99</span>:</a>
<a class="sourceLine" id="cb7-33" data-line-number="33">        <span class="bu">print</span>(t, loss.item())</a>
<a class="sourceLine" id="cb7-34" data-line-number="34"></a>
<a class="sourceLine" id="cb7-35" data-line-number="35">    <span class="co"># Before the backward pass, use the optimizer object to zero all of the</span></a>
<a class="sourceLine" id="cb7-36" data-line-number="36">    <span class="co"># gradients for the variables it will update (which are the learnable</span></a>
<a class="sourceLine" id="cb7-37" data-line-number="37">    <span class="co"># weights of the model). This is because by default, gradients are</span></a>
<a class="sourceLine" id="cb7-38" data-line-number="38">    <span class="co"># accumulated in buffers( i.e, not overwritten) whenever .backward()</span></a>
<a class="sourceLine" id="cb7-39" data-line-number="39">    <span class="co"># is called. Checkout docs of torch.autograd.backward for more details.</span></a>
<a class="sourceLine" id="cb7-40" data-line-number="40">    optimizer.zero_grad()</a>
<a class="sourceLine" id="cb7-41" data-line-number="41"></a>
<a class="sourceLine" id="cb7-42" data-line-number="42">    <span class="co"># Backward pass: compute gradient of the loss with respect to model</span></a>
<a class="sourceLine" id="cb7-43" data-line-number="43">    <span class="co"># parameters</span></a>
<a class="sourceLine" id="cb7-44" data-line-number="44">    loss.backward()</a>
<a class="sourceLine" id="cb7-45" data-line-number="45"></a>
<a class="sourceLine" id="cb7-46" data-line-number="46">    <span class="co"># Calling the step function on an Optimizer makes an update to its</span></a>
<a class="sourceLine" id="cb7-47" data-line-number="47">    <span class="co"># parameters</span></a>
<a class="sourceLine" id="cb7-48" data-line-number="48">    optimizer.step()</a></code></pre></div>
<h4 id="custom-nn-modules">Custom nn Modules</h4>
<p>在前面定义这个神经网络是通过<code>torch.nn.Sequential</code>将各层函数封装成一个模型，有时可能需要定义一个更加复杂的模型，而这个模型中的有些模块需要自己手动实现，此时可以通过定义一个继承<code>torch.nn.Module</code>的子类来实现这个功能：</p>
<div class="sourceCode" id="cb8"><pre class="sourceCode python"><code class="sourceCode python"><a class="sourceLine" id="cb8-1" data-line-number="1"><span class="co"># -*- coding: utf-8 -*-</span></a>
<a class="sourceLine" id="cb8-2" data-line-number="2"><span class="im">import</span> torch</a>
<a class="sourceLine" id="cb8-3" data-line-number="3"></a>
<a class="sourceLine" id="cb8-4" data-line-number="4"></a>
<a class="sourceLine" id="cb8-5" data-line-number="5"><span class="kw">class</span> TwoLayerNet(torch.nn.Module):</a>
<a class="sourceLine" id="cb8-6" data-line-number="6">    <span class="kw">def</span> <span class="fu">__init__</span>(<span class="va">self</span>, D_in, H, D_out):</a>
<a class="sourceLine" id="cb8-7" data-line-number="7">        <span class="co">&quot;&quot;&quot;</span></a>
<a class="sourceLine" id="cb8-8" data-line-number="8"><span class="co">        In the constructor we instantiate two nn.Linear modules and assign them as</span></a>
<a class="sourceLine" id="cb8-9" data-line-number="9"><span class="co">        member variables.</span></a>
<a class="sourceLine" id="cb8-10" data-line-number="10"><span class="co">        &quot;&quot;&quot;</span></a>
<a class="sourceLine" id="cb8-11" data-line-number="11">        <span class="bu">super</span>(TwoLayerNet, <span class="va">self</span>).<span class="fu">__init__</span>()</a>
<a class="sourceLine" id="cb8-12" data-line-number="12">        <span class="va">self</span>.linear1 <span class="op">=</span> torch.nn.Linear(D_in, H)</a>
<a class="sourceLine" id="cb8-13" data-line-number="13">        <span class="va">self</span>.linear2 <span class="op">=</span> torch.nn.Linear(H, D_out)</a>
<a class="sourceLine" id="cb8-14" data-line-number="14"></a>
<a class="sourceLine" id="cb8-15" data-line-number="15">    <span class="kw">def</span> forward(<span class="va">self</span>, x):</a>
<a class="sourceLine" id="cb8-16" data-line-number="16">        <span class="co">&quot;&quot;&quot;</span></a>
<a class="sourceLine" id="cb8-17" data-line-number="17"><span class="co">        In the forward function we accept a Tensor of input data and we must return</span></a>
<a class="sourceLine" id="cb8-18" data-line-number="18"><span class="co">        a Tensor of output data. We can use Modules defined in the constructor as</span></a>
<a class="sourceLine" id="cb8-19" data-line-number="19"><span class="co">        well as arbitrary operators on Tensors.</span></a>
<a class="sourceLine" id="cb8-20" data-line-number="20"><span class="co">        &quot;&quot;&quot;</span></a>
<a class="sourceLine" id="cb8-21" data-line-number="21">        h_relu <span class="op">=</span> <span class="va">self</span>.linear1(x).clamp(<span class="bu">min</span><span class="op">=</span><span class="dv">0</span>)</a>
<a class="sourceLine" id="cb8-22" data-line-number="22">        y_pred <span class="op">=</span> <span class="va">self</span>.linear2(h_relu)</a>
<a class="sourceLine" id="cb8-23" data-line-number="23">        <span class="cf">return</span> y_pred</a>
<a class="sourceLine" id="cb8-24" data-line-number="24"></a>
<a class="sourceLine" id="cb8-25" data-line-number="25"></a>
<a class="sourceLine" id="cb8-26" data-line-number="26"><span class="co"># N is batch size; D_in is input dimension;</span></a>
<a class="sourceLine" id="cb8-27" data-line-number="27"><span class="co"># H is hidden dimension; D_out is output dimension.</span></a>
<a class="sourceLine" id="cb8-28" data-line-number="28">N, D_in, H, D_out <span class="op">=</span> <span class="dv">64</span>, <span class="dv">1000</span>, <span class="dv">100</span>, <span class="dv">10</span></a>
<a class="sourceLine" id="cb8-29" data-line-number="29"></a>
<a class="sourceLine" id="cb8-30" data-line-number="30"><span class="co"># Create random Tensors to hold inputs and outputs</span></a>
<a class="sourceLine" id="cb8-31" data-line-number="31">x <span class="op">=</span> torch.randn(N, D_in)</a>
<a class="sourceLine" id="cb8-32" data-line-number="32">y <span class="op">=</span> torch.randn(N, D_out)</a>
<a class="sourceLine" id="cb8-33" data-line-number="33"></a>
<a class="sourceLine" id="cb8-34" data-line-number="34"><span class="co"># Construct our model by instantiating the class defined above</span></a>
<a class="sourceLine" id="cb8-35" data-line-number="35">model <span class="op">=</span> TwoLayerNet(D_in, H, D_out)</a>
<a class="sourceLine" id="cb8-36" data-line-number="36"></a>
<a class="sourceLine" id="cb8-37" data-line-number="37"><span class="co"># Construct our loss function and an Optimizer. The call to model.parameters()</span></a>
<a class="sourceLine" id="cb8-38" data-line-number="38"><span class="co"># in the SGD constructor will contain the learnable parameters of the two</span></a>
<a class="sourceLine" id="cb8-39" data-line-number="39"><span class="co"># nn.Linear modules which are members of the model.</span></a>
<a class="sourceLine" id="cb8-40" data-line-number="40">criterion <span class="op">=</span> torch.nn.MSELoss(reduction<span class="op">=</span><span class="st">&#39;sum&#39;</span>)</a>
<a class="sourceLine" id="cb8-41" data-line-number="41">optimizer <span class="op">=</span> torch.optim.SGD(model.parameters(), lr<span class="op">=</span><span class="fl">1e-4</span>)</a>
<a class="sourceLine" id="cb8-42" data-line-number="42"><span class="cf">for</span> t <span class="kw">in</span> <span class="bu">range</span>(<span class="dv">500</span>):</a>
<a class="sourceLine" id="cb8-43" data-line-number="43">    <span class="co"># Forward pass: Compute predicted y by passing x to the model</span></a>
<a class="sourceLine" id="cb8-44" data-line-number="44">    y_pred <span class="op">=</span> model(x)</a>
<a class="sourceLine" id="cb8-45" data-line-number="45"></a>
<a class="sourceLine" id="cb8-46" data-line-number="46">    <span class="co"># Compute and print loss</span></a>
<a class="sourceLine" id="cb8-47" data-line-number="47">    loss <span class="op">=</span> criterion(y_pred, y)</a>
<a class="sourceLine" id="cb8-48" data-line-number="48">    <span class="cf">if</span> t <span class="op">%</span> <span class="dv">100</span> <span class="op">==</span> <span class="dv">99</span>:</a>
<a class="sourceLine" id="cb8-49" data-line-number="49">        <span class="bu">print</span>(t, loss.item())</a>
<a class="sourceLine" id="cb8-50" data-line-number="50"></a>
<a class="sourceLine" id="cb8-51" data-line-number="51">    <span class="co"># Zero gradients, perform a backward pass, and update the weights.</span></a>
<a class="sourceLine" id="cb8-52" data-line-number="52">    optimizer.zero_grad()</a>
<a class="sourceLine" id="cb8-53" data-line-number="53">    loss.backward()</a>
<a class="sourceLine" id="cb8-54" data-line-number="54">    optimizer.step()</a></code></pre></div>
<p>可以看到，这样做其实与直接使用<code>torch.nn.Sequential</code>来定义模型在有些情况下是差不多的，但通过继承<code>nn.Module</code>一方面可以使得自己的模型更具扩展性，同时代码的可读性也进一步加强，因为你自己的神经网络模型是以一个类的形式存在的。</p>
<h4 id="tensordataset">TensorDataset</h4>
<p>在进行数据处理时，我们的数据基本都是<code>x_train,y_train,x_test,y_test</code>的形式，在进行迭代时可能必须要通过下标来进行迭代，而为了让迭代操作更加方便，<code>pytorch</code>引入了<code>torch.utils.data</code>来辅助进行数据处理，而其中<code>TensorDataset()</code>方法相当于是对<code>x,y</code>做了一个<code>wrap</code>操作，在进行迭代时可以一起迭代而不需要分别迭代： &gt;应用<code>TensorDataset()</code>方法: &gt;<code>python &gt;train_ds = TensorDataset(x_train, y_train) &gt;</code> &gt; 应用前迭代： &gt; <code>python &gt; xb = x_train[start_i:end_i] &gt; yb = y_train[start_i:end_i] &gt;</code> &gt; 应用后迭代： &gt; <code>python &gt; xb,yb = train_ds[i*bs : i*bs+bs] &gt;</code></p>
<h4 id="dataloader">DataLoader</h4>
<p>虽然使用<code>TensorDataset</code>使得迭代运算变得简单，但我们还是按照<code>batch_size</code>的大小来对迭代下标进行维护，为了能够更方便地应用数据集进行训练，<code>torch.utils.data</code>又为我们提供了另一个好用的工具<code>DataLoader</code>,它可以将<code>warp</code>之后的数据集按照<code>batch_size</code>大小进行分割，然后在迭代时每次取一个<code>batch</code>的数据。 &gt; 应用<code>DataLoader</code>对数据集进行处理： &gt; <code>python &gt; train_ds = TensorDataset(x_train, y_train) &gt; train_dl = DataLoader(train_ds, batch_size=bs) &gt;</code> &gt; 对处理后的数据进行迭代： &gt; <code>python &gt; for xb,yb in train_dl: &gt;    pred = model(xb) &gt;</code></p>
<h4 id="mnist数据集cnn代码">MNIST数据集CNN代码</h4>
<p>有了应用上面的那些封装好的api，在MNIST数据集上实现一个CNN代码便不再困难，下面给出代码：</p>
<div class="sourceCode" id="cb9"><pre class="sourceCode python"><code class="sourceCode python"><a class="sourceLine" id="cb9-1" data-line-number="1"><span class="im">from</span> pathlib <span class="im">import</span> Path</a>
<a class="sourceLine" id="cb9-2" data-line-number="2"><span class="im">from</span> matplotlib <span class="im">import</span> pyplot <span class="im">as</span> plt</a>
<a class="sourceLine" id="cb9-3" data-line-number="3"><span class="im">from</span> IPython.core.debugger <span class="im">import</span> set_trace</a>
<a class="sourceLine" id="cb9-4" data-line-number="4"><span class="im">from</span> torch <span class="im">import</span> nn</a>
<a class="sourceLine" id="cb9-5" data-line-number="5"><span class="im">from</span> torch <span class="im">import</span> optim</a>
<a class="sourceLine" id="cb9-6" data-line-number="6"><span class="im">from</span> torch.utils.data <span class="im">import</span> TensorDataset</a>
<a class="sourceLine" id="cb9-7" data-line-number="7"><span class="im">from</span> torch.utils.data <span class="im">import</span> DataLoader</a>
<a class="sourceLine" id="cb9-8" data-line-number="8"><span class="im">import</span> torch.nn.functional <span class="im">as</span> F</a>
<a class="sourceLine" id="cb9-9" data-line-number="9"><span class="im">import</span> numpy <span class="im">as</span> np</a>
<a class="sourceLine" id="cb9-10" data-line-number="10"><span class="im">import</span> requests</a>
<a class="sourceLine" id="cb9-11" data-line-number="11"><span class="im">import</span> math</a>
<a class="sourceLine" id="cb9-12" data-line-number="12"><span class="im">import</span> pickle</a>
<a class="sourceLine" id="cb9-13" data-line-number="13"><span class="im">import</span> gzip</a>
<a class="sourceLine" id="cb9-14" data-line-number="14"><span class="im">import</span> torch</a>
<a class="sourceLine" id="cb9-15" data-line-number="15"></a>
<a class="sourceLine" id="cb9-16" data-line-number="16"><span class="co">&#39;&#39;&#39;</span></a>
<a class="sourceLine" id="cb9-17" data-line-number="17"><span class="co">class Mnist_logistic(nn.Module):</span></a>
<a class="sourceLine" id="cb9-18" data-line-number="18"><span class="co">    def __init__(self):</span></a>
<a class="sourceLine" id="cb9-19" data-line-number="19"><span class="co">        super().__init__()</span></a>
<a class="sourceLine" id="cb9-20" data-line-number="20"><span class="co">        self.weights = nn.Parameter(torch.randn(784,10)/math.sqrt(784))</span></a>
<a class="sourceLine" id="cb9-21" data-line-number="21"><span class="co">        self.bias = nn.Parameter(torch.zeros(10))</span></a>
<a class="sourceLine" id="cb9-22" data-line-number="22"><span class="co">    def forward(self, xb):</span></a>
<a class="sourceLine" id="cb9-23" data-line-number="23"><span class="co">        return xb @ self.weights + self.bias</span></a>
<a class="sourceLine" id="cb9-24" data-line-number="24"><span class="co">&#39;&#39;&#39;</span></a>
<a class="sourceLine" id="cb9-25" data-line-number="25"><span class="co">&#39;&#39;&#39;</span></a>
<a class="sourceLine" id="cb9-26" data-line-number="26"><span class="co">class Mnist_logistic(nn.Module):</span></a>
<a class="sourceLine" id="cb9-27" data-line-number="27"><span class="co">    def __init__(self):</span></a>
<a class="sourceLine" id="cb9-28" data-line-number="28"><span class="co">        super().__init__()</span></a>
<a class="sourceLine" id="cb9-29" data-line-number="29"><span class="co">        self.linear = nn.Linear(784,10)</span></a>
<a class="sourceLine" id="cb9-30" data-line-number="30"><span class="co">    def forward(self, x):</span></a>
<a class="sourceLine" id="cb9-31" data-line-number="31"><span class="co">        return self.linear(x)</span></a>
<a class="sourceLine" id="cb9-32" data-line-number="32"><span class="co">&#39;&#39;&#39;</span></a>
<a class="sourceLine" id="cb9-33" data-line-number="33"><span class="kw">def</span> get_model():</a>
<a class="sourceLine" id="cb9-34" data-line-number="34">    model <span class="op">=</span> Mnist_CNN().to(device)</a>
<a class="sourceLine" id="cb9-35" data-line-number="35">    opt <span class="op">=</span> optim.SGD(model.parameters(), lr <span class="op">=</span> <span class="fl">0.5</span>)</a>
<a class="sourceLine" id="cb9-36" data-line-number="36">    <span class="cf">return</span> model, opt</a>
<a class="sourceLine" id="cb9-37" data-line-number="37"><span class="kw">def</span> preprocess(x, y):</a>
<a class="sourceLine" id="cb9-38" data-line-number="38">    <span class="cf">return</span> x.view(<span class="op">-</span><span class="dv">1</span>,<span class="dv">1</span>,<span class="dv">28</span>,<span class="dv">28</span>).to(device), y.to(device)</a>
<a class="sourceLine" id="cb9-39" data-line-number="39"><span class="kw">class</span> WrappedDataLoader:</a>
<a class="sourceLine" id="cb9-40" data-line-number="40">    <span class="kw">def</span> <span class="fu">__init__</span>(<span class="va">self</span>, d1, func, batch_size):</a>
<a class="sourceLine" id="cb9-41" data-line-number="41">        <span class="va">self</span>.d1 <span class="op">=</span> d1</a>
<a class="sourceLine" id="cb9-42" data-line-number="42">        <span class="va">self</span>.func <span class="op">=</span> func </a>
<a class="sourceLine" id="cb9-43" data-line-number="43">        <span class="va">self</span>.batch_size <span class="op">=</span> <span class="dv">64</span></a>
<a class="sourceLine" id="cb9-44" data-line-number="44">    <span class="kw">def</span> <span class="fu">__len__</span>(<span class="va">self</span>):</a>
<a class="sourceLine" id="cb9-45" data-line-number="45">        <span class="cf">return</span> <span class="bu">len</span>(<span class="va">self</span>.d1)</a>
<a class="sourceLine" id="cb9-46" data-line-number="46">    <span class="kw">def</span> <span class="fu">__iter__</span>(<span class="va">self</span>):</a>
<a class="sourceLine" id="cb9-47" data-line-number="47">        batches <span class="op">=</span> <span class="bu">iter</span>(<span class="va">self</span>.d1)</a>
<a class="sourceLine" id="cb9-48" data-line-number="48">        <span class="cf">for</span> b <span class="kw">in</span> batches:</a>
<a class="sourceLine" id="cb9-49" data-line-number="49">            <span class="cf">yield</span> (<span class="va">self</span>.func(<span class="op">*</span>b))</a>
<a class="sourceLine" id="cb9-50" data-line-number="50"><span class="kw">class</span> Mnist_CNN(nn.Module):</a>
<a class="sourceLine" id="cb9-51" data-line-number="51">    <span class="kw">def</span> <span class="fu">__init__</span>(<span class="va">self</span>):</a>
<a class="sourceLine" id="cb9-52" data-line-number="52">        <span class="bu">super</span>().<span class="fu">__init__</span>()</a>
<a class="sourceLine" id="cb9-53" data-line-number="53">        <span class="va">self</span>.conv1 <span class="op">=</span> nn.Conv2d(<span class="dv">1</span>, <span class="dv">16</span>, kernel_size<span class="op">=</span><span class="dv">3</span>, stride<span class="op">=</span><span class="dv">2</span>, padding<span class="op">=</span><span class="dv">1</span>)</a>
<a class="sourceLine" id="cb9-54" data-line-number="54">        <span class="va">self</span>.conv2 <span class="op">=</span> nn.Conv2d(<span class="dv">16</span>, <span class="dv">16</span>, kernel_size<span class="op">=</span><span class="dv">3</span>, stride<span class="op">=</span><span class="dv">2</span>, padding<span class="op">=</span><span class="dv">1</span>)</a>
<a class="sourceLine" id="cb9-55" data-line-number="55">        <span class="va">self</span>.conv3 <span class="op">=</span> nn.Conv2d(<span class="dv">16</span>, <span class="dv">10</span>, kernel_size<span class="op">=</span><span class="dv">3</span>, stride<span class="op">=</span><span class="dv">2</span>, padding<span class="op">=</span><span class="dv">1</span>)</a>
<a class="sourceLine" id="cb9-56" data-line-number="56">        <span class="va">self</span>.relu <span class="op">=</span> nn.ReLU()</a>
<a class="sourceLine" id="cb9-57" data-line-number="57">        <span class="va">self</span>.pooling <span class="op">=</span> nn.AdaptiveAvgPool2d(<span class="dv">1</span>)</a>
<a class="sourceLine" id="cb9-58" data-line-number="58">    <span class="kw">def</span> forward(<span class="va">self</span>, xb):</a>
<a class="sourceLine" id="cb9-59" data-line-number="59">        xb <span class="op">=</span> <span class="va">self</span>.relu(<span class="va">self</span>.conv1(xb))</a>
<a class="sourceLine" id="cb9-60" data-line-number="60">        xb <span class="op">=</span> <span class="va">self</span>.relu(<span class="va">self</span>.conv2(xb))</a>
<a class="sourceLine" id="cb9-61" data-line-number="61">        xb <span class="op">=</span> <span class="va">self</span>.relu(<span class="va">self</span>.conv3(xb))</a>
<a class="sourceLine" id="cb9-62" data-line-number="62">        xb <span class="op">=</span> <span class="va">self</span>.pooling(xb)</a>
<a class="sourceLine" id="cb9-63" data-line-number="63">        <span class="cf">return</span> xb.view(<span class="op">-</span><span class="dv">1</span>,xb.size(<span class="dv">1</span>))</a>
<a class="sourceLine" id="cb9-64" data-line-number="64"><span class="kw">def</span> loss_batch(moedl, loss_func, xb, yb, opt <span class="op">=</span> <span class="va">None</span>):</a>
<a class="sourceLine" id="cb9-65" data-line-number="65">    loss <span class="op">=</span> loss_func(model(xb), yb)</a>
<a class="sourceLine" id="cb9-66" data-line-number="66">    <span class="cf">if</span> opt <span class="kw">is</span> <span class="kw">not</span> <span class="va">None</span>:</a>
<a class="sourceLine" id="cb9-67" data-line-number="67">        loss.backward()</a>
<a class="sourceLine" id="cb9-68" data-line-number="68">        opt.step()</a>
<a class="sourceLine" id="cb9-69" data-line-number="69">        opt.zero_grad()</a>
<a class="sourceLine" id="cb9-70" data-line-number="70">    <span class="cf">return</span> loss.item(), <span class="bu">len</span>(xb)</a>
<a class="sourceLine" id="cb9-71" data-line-number="71"></a>
<a class="sourceLine" id="cb9-72" data-line-number="72"><span class="kw">def</span> fit(epoches, model, loss_func, opt, train_dl, valid_dl):</a>
<a class="sourceLine" id="cb9-73" data-line-number="73">    <span class="cf">for</span> epoch <span class="kw">in</span> <span class="bu">range</span>(epoches):</a>
<a class="sourceLine" id="cb9-74" data-line-number="74">        model.train()</a>
<a class="sourceLine" id="cb9-75" data-line-number="75">        <span class="cf">for</span> xb, yb <span class="kw">in</span> train_dl:</a>
<a class="sourceLine" id="cb9-76" data-line-number="76">            loss_batch(model, loss_func, xb, yb, opt)</a>
<a class="sourceLine" id="cb9-77" data-line-number="77">        model.<span class="bu">eval</span>()</a>
<a class="sourceLine" id="cb9-78" data-line-number="78">        <span class="cf">with</span> torch.no_grad():</a>
<a class="sourceLine" id="cb9-79" data-line-number="79">            losses, nums <span class="op">=</span> <span class="bu">zip</span>(<span class="op">*</span>[loss_batch(model, loss_func, xb, yb) <span class="cf">for</span> xb, yb <span class="kw">in</span> valid_dl])</a>
<a class="sourceLine" id="cb9-80" data-line-number="80">            val_loss <span class="op">=</span> np.<span class="bu">sum</span>(np.multiply(losses, nums)<span class="op">/</span>np.<span class="bu">sum</span>(nums))</a>
<a class="sourceLine" id="cb9-81" data-line-number="81">            <span class="cf">if</span> epoch <span class="op">%</span> <span class="dv">10</span> <span class="op">==</span> <span class="dv">0</span>:</a>
<a class="sourceLine" id="cb9-82" data-line-number="82">                train_aur, valid_aur <span class="op">=</span> cal_acur(model, train_dl, valid_dl)    </a>
<a class="sourceLine" id="cb9-83" data-line-number="83">                <span class="bu">print</span>(<span class="st">&#39;epoch:&#39;</span>,epoch,<span class="st">&#39;验证集损失:&#39;</span>,np.<span class="bu">str</span>(val_loss),<span class="st">&#39;训练集准确率:&#39;</span>,np.<span class="bu">str</span>(train_aur),<span class="st">&#39;验证集准确率:&#39;</span>,np.<span class="bu">str</span>(valid_aur))</a>
<a class="sourceLine" id="cb9-84" data-line-number="84"></a>
<a class="sourceLine" id="cb9-85" data-line-number="85"><span class="kw">def</span> get_data(train_ds, valid_ds, bs):</a>
<a class="sourceLine" id="cb9-86" data-line-number="86">    train_dl <span class="op">=</span> DataLoader(train_ds, batch_size <span class="op">=</span> bs, shuffle <span class="op">=</span> <span class="va">True</span>)</a>
<a class="sourceLine" id="cb9-87" data-line-number="87">    valid_dl <span class="op">=</span> DataLoader(valid_ds, batch_size <span class="op">=</span> bs, shuffle <span class="op">=</span> <span class="va">True</span>)</a>
<a class="sourceLine" id="cb9-88" data-line-number="88">    <span class="cf">return</span> train_dl, valid_dl</a>
<a class="sourceLine" id="cb9-89" data-line-number="89"><span class="kw">def</span> batch_acu(y_pred, y):</a>
<a class="sourceLine" id="cb9-90" data-line-number="90">    batch_pred <span class="op">=</span> torch.argmax(y_pred, dim <span class="op">=</span> <span class="dv">1</span>)</a>
<a class="sourceLine" id="cb9-91" data-line-number="91">    <span class="cf">return</span> (batch_pred <span class="op">==</span> y).<span class="bu">sum</span>()</a>
<a class="sourceLine" id="cb9-92" data-line-number="92"></a>
<a class="sourceLine" id="cb9-93" data-line-number="93"><span class="kw">def</span> cal_acur(model, train_dl, valid_dl):</a>
<a class="sourceLine" id="cb9-94" data-line-number="94">    train_pred_right <span class="op">=</span> <span class="dv">0</span></a>
<a class="sourceLine" id="cb9-95" data-line-number="95">    valid_pred_right <span class="op">=</span> <span class="dv">0</span></a>
<a class="sourceLine" id="cb9-96" data-line-number="96">    <span class="cf">for</span> xb, yb <span class="kw">in</span> train_dl:</a>
<a class="sourceLine" id="cb9-97" data-line-number="97">        y_pred <span class="op">=</span> model(xb) </a>
<a class="sourceLine" id="cb9-98" data-line-number="98">        train_pred_right <span class="op">+=</span> batch_acu(y_pred, yb)</a>
<a class="sourceLine" id="cb9-99" data-line-number="99">    <span class="cf">for</span> xb, yb <span class="kw">in</span> valid_dl:</a>
<a class="sourceLine" id="cb9-100" data-line-number="100">        y_pred <span class="op">=</span> model(xb)</a>
<a class="sourceLine" id="cb9-101" data-line-number="101">        valid_pred_right <span class="op">+=</span> batch_acu(y_pred, yb)</a>
<a class="sourceLine" id="cb9-102" data-line-number="102">    train_acur <span class="op">=</span> train_pred_right.<span class="bu">float</span>()<span class="op">/</span>(<span class="bu">len</span>(train_dl)<span class="op">*</span>train_dl.batch_size) </a>
<a class="sourceLine" id="cb9-103" data-line-number="103">    valid_acur <span class="op">=</span> valid_pred_right.<span class="bu">float</span>()<span class="op">/</span>(<span class="bu">len</span>(valid_dl)<span class="op">*</span>valid_dl.batch_size)</a>
<a class="sourceLine" id="cb9-104" data-line-number="104">    <span class="cf">return</span> train_acur.item(), valid_acur.item()</a>
<a class="sourceLine" id="cb9-105" data-line-number="105">        </a>
<a class="sourceLine" id="cb9-106" data-line-number="106"></a>
<a class="sourceLine" id="cb9-107" data-line-number="107">        </a>
<a class="sourceLine" id="cb9-108" data-line-number="108"></a>
<a class="sourceLine" id="cb9-109" data-line-number="109"><span class="cf">if</span> <span class="va">__name__</span> <span class="op">==</span> <span class="st">&#39;__main__&#39;</span>:</a>
<a class="sourceLine" id="cb9-110" data-line-number="110">    device <span class="op">=</span> torch.device(<span class="st">&#39;cuda:0&#39;</span> <span class="cf">if</span> torch.cuda.is_available() <span class="cf">else</span> <span class="st">&#39;cpu&#39;</span>)</a>
<a class="sourceLine" id="cb9-111" data-line-number="111">    batch_size <span class="op">=</span> <span class="dv">64</span></a>
<a class="sourceLine" id="cb9-112" data-line-number="112">    FILENAME <span class="op">=</span> <span class="st">&quot;data/mnist/mnist.pkl.gz&quot;</span></a>
<a class="sourceLine" id="cb9-113" data-line-number="113">    <span class="cf">with</span> gzip.<span class="bu">open</span>(FILENAME, <span class="st">&quot;rb&quot;</span>) <span class="im">as</span> f:</a>
<a class="sourceLine" id="cb9-114" data-line-number="114">        ((x_train, y_train), (x_valid, y_valid), _) <span class="op">=</span> pickle.load(f, encoding<span class="op">=</span><span class="st">&quot;latin-1&quot;</span>)</a>
<a class="sourceLine" id="cb9-115" data-line-number="115">    x_train, y_train, x_valid, y_valid <span class="op">=</span> <span class="bu">map</span>(torch.tensor, (x_train, y_train, x_valid, y_valid))</a>
<a class="sourceLine" id="cb9-116" data-line-number="116">    <span class="bu">print</span>(torch.cuda.get_device_name(<span class="dv">0</span>))</a>
<a class="sourceLine" id="cb9-117" data-line-number="117">    train_ds <span class="op">=</span> TensorDataset(x_train, y_train)</a>
<a class="sourceLine" id="cb9-118" data-line-number="118">    valid_ds <span class="op">=</span> TensorDataset(x_valid, y_valid)</a>
<a class="sourceLine" id="cb9-119" data-line-number="119">    loss_func <span class="op">=</span> F.cross_entropy</a>
<a class="sourceLine" id="cb9-120" data-line-number="120">    epoches <span class="op">=</span> <span class="dv">100</span></a>
<a class="sourceLine" id="cb9-121" data-line-number="121">    <span class="co"># train and valid</span></a>
<a class="sourceLine" id="cb9-122" data-line-number="122">    train_dl, valid_dl <span class="op">=</span> get_data(train_ds, valid_ds, batch_size)</a>
<a class="sourceLine" id="cb9-123" data-line-number="123">    train_dl <span class="op">=</span> WrappedDataLoader(train_dl, preprocess, batch_size)</a>
<a class="sourceLine" id="cb9-124" data-line-number="124">    valid_dl <span class="op">=</span> WrappedDataLoader(valid_dl, preprocess, batch_size)</a>
<a class="sourceLine" id="cb9-125" data-line-number="125">    model, opt <span class="op">=</span> get_model()</a>
<a class="sourceLine" id="cb9-126" data-line-number="126">    fit(epoches, model, loss_func, opt, train_dl, valid_dl)</a></code></pre></div>
<h3 id="总结">总结</h3>
<p>最后总结一下<code>pytorch</code>中常用的包： - <code>torch.nn:</code>包含搭建神经网络层的模块(Modules)和一系列<code>loss</code>函数 - <code>torch.nn.functional:</code>提供常用的激活函数，这算是一个比较低级的api，大部分激活函数在<code>nn</code>下都有封装 - <code>torch.autograd:</code> 提供所有张量操作的自动求导方法 - <code>torch.optim:</code> 提供各种参数优化方法，如<code>SGD</code>、<code>Adam</code>、<code>AdaGrad</code>等 - <code>torch.utils.data:</code> 用于加载数据，比如上面介绍的<code>TensorDataset()</code>和<code>DataLoader()</code>方法 - <code>torchvision.datasets:</code>加载图像处理领域常用数据集，如<code>MNIST</code>、<code>coco</code>、<code>CIFAR10</code>、<code>Imagenet</code>等 - <code>torchvision.modules:</code> 图像处理领域常用模型，如<code>AlexNet</code>、<code>VGG</code>、<code>ResNet</code>、<code>DenseNet</code>等 - <code>torchvision.transforms:</code> 图片相关处理操作，如裁剪、尺寸缩放、归一化等</p>
<p><code>pytorch</code>的学习其实并不是孤立的，其实是与其他知识相耦合的: - <code>python</code>语法知识 - 算法理论知识</p>
<p>如果你本身<code>python</code>用的非常熟练，同时算法的理论基础也比较扎实，那么<code>pytorch</code>对你来说可能就是一个非常容易上手的工具，但如果本身这两方面的知识不够牢固，在学习<code>pytorch</code>的过程中，这两方面的能力也能得到很大的提高。</p>
]]></content>
      <categories>
        <category>统计学习</category>
      </categories>
      <tags>
        <tag>pytorch，深度学习框架</tag>
      </tags>
  </entry>
  <entry>
    <title>条件随机场</title>
    <url>/2020/08/21/tiao-jian-sui-ji-chang/</url>
    <content><![CDATA[<p>另一种常用模型为条件随机场(CRF)模型,条件随机场是给定一组输入随机变量条件下另一组输出随机变量的条件概率分布模型，其特点是假设输出随机变量构成马尔可夫随机场。本章节按照以下几部分组织： - 概率无向图模型 - 条件随机场定义与形式 - 条件随机场概率计算 - 条件随机场学习算法 - 条件随机场预测算法 <a id="more"></a></p>
<h3 id="概率无向图模型">概率无向图模型</h3>
<h4 id="概率无向图模型定义">概率无向图模型定义</h4>
<p>图是由节点和及连接的边组成的集合，表示为<span class="math inline">\(G = (V,E)\)</span>，若连接的边无方向，则称为无向图，反之则称为有向图。 概率图模型是由图表示的概率分布，图中的节点表示随机变量，节点之间的边表示变量之间的概率依赖关系。 给定一个联合概率分布<span class="math inline">\(P(Y)\)</span>和表示它的无向图<span class="math inline">\(G\)</span>,首先定义无向图表示的随机变量之间存在的成对马尔可夫性、局部马尔可夫性和全局马尔可夫性。 &gt; - 成对马尔可夫性：设<span class="math inline">\(\mu\)</span>和<span class="math inline">\(v\)</span>是无向图<span class="math inline">\(G\)</span>中任意两个没有边连接的节点，两个节点分别对应随机变量<span class="math inline">\(Y_{\mu},Y_v\)</span>。其他所有节点的集合记做<span class="math inline">\(O\)</span>,对应随机变量组是<span class="math inline">\(Y_O\)</span>。成对马尔可夫性是指给定随机变量组<span class="math inline">\(Y_O\)</span>的条件下，随机变量<span class="math inline">\(Y_{\mu}\)</span>和<span class="math inline">\(Y_v\)</span>是条件独立的,即： <span class="math display">\[
    P(Y_{\mu},Y_v|Y_O) = P(Y_{\mu}|Y_O)P(Y_{v}|Y_O)
\]</span> &gt; - 局部马尔可夫性：设<span class="math inline">\(v \in V\)</span>是无向图<span class="math inline">\(G\)</span>中任意一个节点，<span class="math inline">\(W\)</span>是与<span class="math inline">\(v\)</span>有边连接的所有节点，<span class="math inline">\(O\)</span>是<span class="math inline">\(v\)</span>和<span class="math inline">\(W\)</span>以外的其他所有节点。<span class="math inline">\(v,W,O\)</span>对应的随机变量(组)记做<span class="math inline">\(Y_v,Y_W,Y_O\)</span>,局部马尔可夫性是指在给定随机变量组<span class="math inline">\(Y_W\)</span>条件下，随机变量<span class="math inline">\(Y_v\)</span>与随机变量组<span class="math inline">\(Y_O\)</span>是相互独立的: <span class="math display">\[
    P(Y_{O},Y_v|Y_W) = P(Y_{v}|Y_W)P(Y_{O}|Y_W)
\]</span> 在<span class="math inline">\(P(Y_O|Y_W)&gt;0\)</span>情况下，有： <span class="math display">\[
    P(Y_v|Y_W) = P(Y_v|Y_W,Y_O)
\]</span> &gt; - 全局马尔可夫性：设结点集合<span class="math inline">\(A,B\)</span>是在无向图<span class="math inline">\(G\)</span>中被节点集合<span class="math inline">\(C\)</span>分开的任意节点集合，它们所对应的变量组记做<span class="math inline">\(Y_A,Y_B,Y_C\)</span>。全局马尔可夫性是指给定随机变量组<span class="math inline">\(Y_C\)</span>条件下随机变量组<span class="math inline">\(Y_A，Y_B\)</span>是条件独立的，即： <span class="math display">\[
    P(Y_A,Y_B|Y_C) = P(Y_A|Y_C)P(Y_B|Y_C)
\]</span></p>
<p>下面定义概率无向图模型: &gt; <strong>概率无向图模型:</strong> 设有联合概率分布<span class="math inline">\(P(Y)\)</span>，由无向图<span class="math inline">\(G = (V,E)\)</span>表示，在图<span class="math inline">\(G\)</span>中，节点表示随机变量，边表示随机变量之间的概率依赖关系。若联合概率分布<span class="math inline">\(P(Y)\)</span>满足<strong>成对、局部或全局马尔可夫性</strong>,就称此概率分布为概率无向图模型，或马尔可夫随机场。</p>
<h4 id="概率无向图模型的因子分解">概率无向图模型的因子分解</h4>
<p>能够使用图模型来对联合概率分布进行编码，主要出发点便是变量之间的马尔可夫性假设，这让我们可以以更加紧凑的形式来表示联合概率分布。 首先给出无向图中团与最大团的定义: &gt; <strong>团与最大团</strong>:无向图中任何两个节点均有边连接的子集称为团(clique)。若<span class="math inline">\(C\)</span>是无向图<span class="math inline">\(G\)</span>的一个团，并且不能够再加入任何一个<span class="math inline">\(G\)</span>的节点使其成为一个更大的团，则称此<span class="math inline">\(C\)</span>为最大团。</p>
<p>将概率无向图模型的联合概率分布表示为其最大团上的随机变量的函数的乘积的操作，称为概率无向图模型的因子分解。 给定概率图模型，设其无向图为<span class="math inline">\(G\)</span>，<span class="math inline">\(C\)</span>为<span class="math inline">\(G\)</span>上的最大团，<span class="math inline">\(Y_C\)</span>表示<span class="math inline">\(C\)</span>对应的随机变量，那么联合概率分布<span class="math inline">\(P(Y)\)</span>可以表示成图中所有最大团<span class="math inline">\(C\)</span>上函数<span class="math inline">\(\varPhi_C(Y_C)\)</span>的<strong>连乘形式</strong>: <span class="math display">\[
    P(Y) = \frac{1}{Z} \prod_{C} \varPhi_C(Y_C)
\]</span> 公式中<span class="math inline">\(Z\)</span>是一个归一化因子<span class="math inline">\(Z = \sum_Y \prod_{C} \varPhi_C(Y_C)\)</span>。函数<span class="math inline">\(\varPhi_C(Y_C)\)</span>称为势函数。势函数通常定义为指数函数: <span class="math display">\[
    \varPhi_C(Y_C) = exp\{ -E(Y_C)\}
\]</span> 概率无向图的因子分解由Hammersley-Clifford定理保证: &gt; Hammersley-Clifford定理: 概率无向图模型的联合概率分布<span class="math inline">\(P(Y)\)</span>可以表示为如下形式: <span class="math display">\[
    P(Y) = \frac{1}{Z} \prod_C \varPhi_C(Y_C)
\]</span> 其中，<span class="math inline">\(C\)</span>是无向图的最大团，<span class="math inline">\(Y_C\)</span>是<span class="math inline">\(C\)</span>的节点对应的随机变量，<span class="math inline">\(\varPhi_C(Y_C)\)</span>是<span class="math inline">\(C\)</span>上定义的严格正函数，乘积是在无向图所有的最大团上进行的，关于该定理的证明参考链接<a href="https://www.jianshu.com/p/dd27249b8c4a">Hammersley-Clifford定理证明</a></p>
<h3 id="条件随机场的定义与形式">条件随机场的定义与形式</h3>
<h4 id="条件随机场的定义">条件随机场的定义</h4>
<p>条件随机场是给定随机变量<span class="math inline">\(X\)</span>条件下，随机变量<span class="math inline">\(Y\)</span>的马尔可夫随机场。本文主要介绍线性链条件随机场，该模型可用于标注等问题。这时，在条件概率模型<span class="math inline">\(P(Y|X)\)</span>中，<span class="math inline">\(Y\)</span>是输出变量，表示标记序列(状态序列)，<span class="math inline">\(X\)</span>是输入变量，表示需要标注的观测序列。学习时，利用训练数据集通过极大似然估计或正则化的极大似然估计得到条件概率模型<span class="math inline">\(\hat{P}(Y|X)\)</span>;预测时，对于给定的输入序列<span class="math inline">\(x\)</span>,求出条件概率<span class="math inline">\(\hat{P}(y|x)\)</span>最大的输出序列<span class="math inline">\(\hat{y}\)</span>。 首先给出一般的条件随机场的定义: &gt; <strong>条件随机场</strong>：设<span class="math inline">\(X\)</span>和<span class="math inline">\(Y\)</span>是随机变量，<span class="math inline">\(P(Y|X)\)</span>是在给定<span class="math inline">\(X\)</span>条件下<span class="math inline">\(Y\)</span>的条件概率分布。若随机变量<span class="math inline">\(Y\)</span>构成一个由无向图<span class="math inline">\(G = (V,E)\)</span>表示的马尔可夫随机场，即： &gt; <span class="math display">\[
    P(Y_v|X,Y_w,w \neq v) = P(Y_v|X,Y_w,w\sim v)
&gt; \]</span> 对任意节点<span class="math inline">\(v\)</span>成立，则称条件概率分布<span class="math inline">\(P(Y|X)\)</span>为条件随机场。公式中<span class="math inline">\(w \sim v\)</span>表示在图<span class="math inline">\(G = (V,E)\)</span>中与节点<span class="math inline">\(v\)</span>有边连接的所有节点<span class="math inline">\(w\)</span>,<span class="math inline">\(w \neq v\)</span>表示节点<span class="math inline">\(v\)</span>以外的所有节点，<span class="math inline">\(Y_v,Y_w,Y_{\mu}\)</span>为节点<span class="math inline">\(v,\mu,w\)</span>所对应的随机变量。</p>
<p>虽然在定义中并没有要求<span class="math inline">\(X\)</span>和<span class="math inline">\(Y\)</span>具有相同的结构，但在现实中，一般假设<span class="math inline">\(X\)</span>和<span class="math inline">\(Y\)</span>具有相同的结构，而对于线性链，则是图<span class="math inline">\(G = (V,E)\)</span>可以表示成： <span class="math display">\[
    G = (V = \{ 1,2,\dots,n\}, E = \{(i,i+1)\}),\quad i = 1,2,\dots,n-1
\]</span> <img src="https://raw.githubusercontent.com/xuejy19/xuejy19.github.io/source/Img/SRF.png" alt="线性链条件随机场" /> 下面给出线性链条件随机场的定义： &gt; <strong>线性链条件随机场</strong>：设<span class="math inline">\(X = (X_1,X_2,\dots,X_n),Y= (Y_1,Y_2,\dots,Y_n)\)</span>均为线性链表示的随机变量序列，若在给定随机变量序列<span class="math inline">\(X\)</span>的条件下，随机变量序列<span class="math inline">\(Y\)</span>的条件概率分布<span class="math inline">\(P(Y|X)\)</span>构成条件随机场，即满足马尔可夫性： &gt; <span class="math display">\[
    P(Y_i|X,Y_1,\dots,Y_{i-1},Y_{i+1},\dots,Y_n) = P(Y_i|X,Y_{i-1},Y_{i+1}),i =1,\dots,n
&gt; \]</span> 则称<span class="math inline">\(P(Y|X)\)</span>为线性链条件随机场。在标注问题中，<span class="math inline">\(X\)</span>表示输入观测序列，<span class="math inline">\(Y\)</span>表示对应的输出标记序列或状态序列。 #### 条件随机场的参数化形式 下面给出条件随机场的参数化形式： &gt; <strong>线性链条件随机场参数化形式</strong>：设<span class="math inline">\(P(Y|X)\)</span>为线性链条件随机场，则在随机变量<span class="math inline">\(X\)</span>取值为<span class="math inline">\(x\)</span>条件下，随机变量<span class="math inline">\(Y\)</span>取值为<span class="math inline">\(y\)</span>的条件概率具有如下形式: &gt; <span class="math display">\[
    P(y|x) = \frac{1}{Z(x)} exp(\sum_{i,k} \lambda_k t_k(y_{i-1},y_i,x,i) + \sum_{i,l} \mu_l s_l(y_i,x,i) )
&gt; \]</span> 其中<span class="math inline">\(Z(x)\)</span>为归一化因子，保证<span class="math inline">\(P(y|x)\)</span>满足概率形式，<span class="math inline">\(t_k\)</span>和<span class="math inline">\(s_l\)</span>是特征函数，<span class="math inline">\(\lambda_k\)</span>和<span class="math inline">\(\mu_l\)</span>是对应的权值。</p>
<p>定义中公式表示给定输入序列<span class="math inline">\(x\)</span>，对输出序列<span class="math inline">\(y\)</span>预测的条件概率。<span class="math inline">\(t_k\)</span>是定义在边上的特征函数，称为转移特征，依赖于当前和前一个位置；<span class="math inline">\(s_l\)</span>是定义在节点上的特征函数，称为状态特征，依赖于当前位置。<span class="math inline">\(t_k\)</span>和<span class="math inline">\(s_l\)</span>都依赖于位置，是局部特征函数。通常，特征函数<span class="math inline">\(t_k\)</span>和<span class="math inline">\(s_l\)</span>取值为0或1，当满足特征条件时取值为1，否则为0。条件随机场完全由特征函数<span class="math inline">\(t_k,s_l\)</span>和对应的权值<span class="math inline">\(\lambda_k,\mu_l\)</span>确定。 为便于理解，下面给出一个线性链条件随机场的栗子： &gt; <strong>🌰：</strong> 设有一标注问题：输入观测序列为<span class="math inline">\(X = (X_1,X_2,X_3)\)</span>,输出标记序列为<span class="math inline">\(Y = (Y_1,Y_2,Y_3)\)</span>,输出标记取之于<span class="math inline">\(\mathcal{Y} = \{ 1,2 \}\)</span> &gt; 假设特征<span class="math inline">\(t_k,s_l\)</span>和对应的权值<span class="math inline">\(\lambda_k,\mu_l\)</span>如下： &gt; <span class="math display">\[
    \begin{aligned}
        t_1 &amp;=  t_1(y_{i-1} = 1,y_i = 2,x,i) \quad i =2,3, \lambda_1 = 1 \\
        t_2 &amp;= t_2(y_1 = 1,y_2 = 1,x,2),\quad \lambda_2 = 0.6 \\
        t_3 &amp;= t_3(y_2 = 2,y_3 =1,x,3), \quad \lambda_3 = 1 \\
        t_4 &amp;= t_4(y_1 = 2,y_2 = 1,x,2), \quad \lambda_4 = 1 \\
        t_5 &amp;= t_5(y_2 = 2,y_3 =2,x,3),\quad \lambda_5 = 0.2 \\
        s_1 &amp;= s_1(y_1 = 1,x,1), \qquad \qquad \mu_1 = 1 \\
        s_2 &amp;= s_2(y_i =2,x,i),i =1,2 \quad \mu_2 = 0.5 \\
        s_3 &amp;= s_3(y_i =1,x,i),i =1,2 \quad \mu_2 = 0.8 \\
        s_4 &amp;= s_4(y_3 =2,x,3),i =1,2 \quad \mu_2 = 0.5  
    \end{aligned}
&gt; \]</span> &gt; 对于给定的观测序列，求标记序列为<span class="math inline">\(y = (y_1,y_2,y_3) = (1,2,2)\)</span>的非规范条件概率(不进行归一化) &gt; <strong>解</strong>：由线性链条件随机场的参数化形式可得: &gt; <span class="math display">\[
    P(y_1 = 1,y_2 = 2,y_3 =2|x) \propto exp(3.2)
&gt; \]</span></p>
<h4 id="条件随机场的简化形式">条件随机场的简化形式</h4>
<p>注意到条件随机场的参数化形式中同一特征在各个位置都有定义，因此可以考虑对同一个特征在各个位置求和，将局部特征函数转化成一个全局特征函数，这样就可以将条件随机场写成权值向量和特征向量的内积形式，即条件随机场的简化形式。 假设有<span class="math inline">\(K_1\)</span>个转移特征，<span class="math inline">\(K_2\)</span>个状态特征，<span class="math inline">\(K = K_1 + K_2\)</span>,记： <span class="math display">\[
    f_k(y_{i-1},y_i,x,i) = \begin{cases}
        t_k(y_{i-1},y_i,x,i), &amp; k =1,2,\dots,K_1 \\
        s_l(y_i,x,i), &amp; k = K_1 + l; l = 1,2,\dots,K_2
    \end{cases}
\]</span> 然后对转移与状态特征在各个位置<span class="math inline">\(i\)</span>求和，记做： <span class="math display">\[
    f_k(y,x) = \sum_{i=1}^n f_k(y_{i-1},y_i, x,i),\quad k = 1,2,\dots,K
\]</span> 用<span class="math inline">\(w_k\)</span>表示特征<span class="math inline">\(f_k(y,x)\)</span>的权值，即： <span class="math display">\[
    w_k = \begin{cases}
        \lambda_k, &amp; k =1,2,\dots, K_1 \\
        \mu_l , &amp; k = K_1 + l; l= 1,2,\dots,K_2
    \end{cases}
\]</span> 于是条件随机场参数化可以写成更加紧凑的形式： <span class="math display">\[
    \begin{aligned}
         P(y|x) &amp;= \frac{1}{Z(x)} exp \sum_{k=1}^K w_k f_k(y,x)  \\
         Z(x) &amp;= \sum_y exp \sum_{k=1}^K w_k f_k(y,x)
    \end{aligned}
\]</span> 若以<span class="math inline">\(\boldsymbol{w}\)</span>表示权值向量，即： <span class="math display">\[
    \boldsymbol{w} = (w_1,w_2,\dots,w_K)^T
\]</span> 以<span class="math inline">\(\boldsymbol{F(y,x)}\)</span>表示全局特征向量: <span class="math display">\[
    \boldsymbol{F(y,x)} = (f_1(y,x),f_2(y,x),\dots, f_K(y,x))^T
\]</span> 则条件随机场可以以两个向量内积的形式来表示: <span class="math display">\[
    P_w(y|x) = \frac{exp(\boldsymbol{w \cdot F(y,x)})}{Z_w(x)}
\]</span> #### 条件随机场的矩阵形式 假设<span class="math inline">\(P_w(y|x)\)</span>是线性链条件随机场，表示对给定观测序列，相应的标记序列<span class="math inline">\(y\)</span>的条件概率。对每个标记序列引入特殊的起点和终点状态标记<span class="math inline">\(y_0 = start\)</span>和<span class="math inline">\(y_{n+1} = stop\)</span>,这时标注序列的概率可以通过矩阵形式表示并有效计算。 对观测序列<span class="math inline">\(x\)</span>的每一个位置<span class="math inline">\(i =1,2,\dots,n+1\)</span>,由于<span class="math inline">\(y_{i-1}\)</span>和<span class="math inline">\(y_i\)</span>在<span class="math inline">\(m\)</span>个标记中取值，因此可以定义一个<span class="math inline">\(m\)</span>阶矩阵随机变量： <span class="math display">\[
    M_i(x) = [M_i(y_{i-1},y_i|x)]
\]</span> 矩阵随机变量元素为: <span class="math display">\[
    \begin{aligned}
        M_i(y_{i-1},y_i|x) &amp;= exp(W_i(y_{i-1},y_i|x)) \\
        W_i(y_{i-1},y_i|x) &amp;= \sum_{k=1}^K w_k f_k(y_{i-1},y_i,x,i)
    \end{aligned}
\]</span> 这样，给定观测序列<span class="math inline">\(x\)</span>,相应的标记序列<span class="math inline">\(y\)</span>的非规范概率可以通过该序列<span class="math inline">\(n+1\)</span>个矩阵的适当元素的乘积 <span class="math inline">\(\prod_{i=1}^{n+1} M_i(y_{i-1},y_i|x)\)</span>,于是，条件概率<span class="math inline">\(P_w(y|x)\)</span>可以表示为： <span class="math display">\[
    P_w(y|x) = \frac{1}{Z_w(x)} \prod_{i=1}^{n+1} M_i(y_{i-1},y_i|x)
\]</span> 其中<span class="math inline">\(Z_w(x) = \sum_y \prod_{i=1}^{n+1} M_i(y_{i-1},y_i|x)\)</span>,可以证明，该归一化因子也可以表示成<span class="math inline">\(n+1\)</span>个矩阵连乘积的矩阵的元素。 下面的部分依次介绍条件随机场的三个经典问题： - 概率计算问题，即给点条件随机场<span class="math inline">\(P(Y|X)\)</span>,输入序列<span class="math inline">\(x\)</span>和输出序列<span class="math inline">\(y\)</span>,计算相应条件概率的问题 - 学习问题，即在给定训练数据集情况下估计条件随机场模型参数的问题 - 预测问题，即在给定条件随机场<span class="math inline">\(P(Y|X)\)</span>和输入序列<span class="math inline">\(x\)</span>,求条件概率最大的输出序列<span class="math inline">\(y^*\)</span>,即对观测序列进行标注。</p>
<p>概率计算问题比较简单，也是后两个问题的基础，而在实际进行应用中，我们首先需要明确线性链条件随机场是否适用于待解决的问题，然后利用已有的训练数据来进行模型学习，最终再用学习得到的模型进行预测(标注)。</p>
<h3 id="条件随机场概率计算问题">条件随机场概率计算问题</h3>
<p>与隐马尔可夫模型的概率计算类似，对于这种有明显前后关联的概率模型， 往往都是采用递推方式来进行概率计算，通过引入前向-后向向量，递归地进行概率计算，该算法也被称为前向-后向算法。</p>
<h4 id="前向-后向算法">前向-后向算法</h4>
<p>对每个指标$ i =0,1,,n+1<span class="math inline">\(,定义前向向量\)</span>_i(x)$: <span class="math display">\[
    \alpha_0(y|x) = \begin{cases}
        1, &amp;y = start \\
        0, &amp; other  
    \end{cases}
\]</span> 递推公式为: <span class="math display">\[
    \alpha_i^T(y_i|x) = \alpha_{i-1}^T(y_{i-1}|x)[M_i(y_{i-1},y_i|x)],\quad i = 1,2,\dots,n+1
\]</span> 也可表示为： <span class="math display">\[
    \alpha_i^T(x) = \alpha_{i-1}^T(x) M_i(x)
\]</span> <span class="math inline">\(\alpha_i(y_i|x)\)</span>表示在位置<span class="math inline">\(i\)</span>的标记是<span class="math inline">\(y_i\)</span>，并且从1到<span class="math inline">\(i\)</span>的前部分标记序列的非规范化概率，<span class="math inline">\(y_i\)</span>可以取的值有<span class="math inline">\(m\)</span>个，在上述公式中，<span class="math inline">\(\alpha_{i-1}(x)\)</span>是一个<span class="math inline">\(m\)</span>维列向量，而<span class="math inline">\(M_i(x)\)</span>是一个<span class="math inline">\(m\)</span>阶矩阵，实际上<span class="math inline">\(y_i\)</span>的任意取值均可能由<span class="math inline">\(y_{n-1}\)</span>的<span class="math inline">\(m\)</span>种状态转移过来，因此上述的计算实际上是对各种转移可能性的求和。 同样，对每个指标<span class="math inline">\(i = 0,1,\dots,n+1\)</span>,定义后向向量<span class="math inline">\(\beta_i(x)\)</span>: <span class="math display">\[
    \begin{aligned}
        \beta_{n+1}(y_{n+1|x}) &amp;= \begin{cases}
            1, &amp; y_{n+1} = stop \\
            0, &amp; other 
        \end{cases} \\
        \beta_i(y_i|x) &amp;= [M_{i+1}(y_i,y_{i+1}|x)] \beta_{i+1}(y_{i+1}|x)
    \end{aligned}
\]</span> 又可表示为： <span class="math display">\[
    \beta_i(x) = M_{i+1}(x) \beta_{i+1}(x)
\]</span> <span class="math inline">\(\beta_i(y_i|x)\)</span>表示在位置<span class="math inline">\(i\)</span>的标记为<span class="math inline">\(y_i\)</span>并且从<span class="math inline">\(i+1\)</span>到<span class="math inline">\(n\)</span>后半部分标记序列的非规范化序列。</p>
<h4 id="概率计算">概率计算</h4>
<p>按照前向后向概率的定义，很容易计算标记序列在位置<span class="math inline">\(i\)</span>是标记<span class="math inline">\(y_i\)</span>的条件概率和在位置<span class="math inline">\(i-1\)</span>和<span class="math inline">\(i\)</span>是标记<span class="math inline">\(y_{i-1}\)</span>和<span class="math inline">\(y_i\)</span>的条件概率： <span class="math display">\[
    \begin{aligned}
        P(Y_i = y_i|x) &amp;= \frac{\alpha_i^T(y_i|x) \beta_i(y_i|x)}{Z(x)} \\
        P(Y_{i-1} = y_{i-1},Y_i = y_i|x) &amp;= \frac{\alpha_{i-1}^T(y_{i-1}|x)M_i(y_{i-1},y_i|x) \beta_i(y_i|x)}{Z(x)}
    \end{aligned}
\]</span> 其中， <span class="math display">\[
    Z(x) = \alpha_n^T(x) \boldsymbol{1} = \boldsymbol{1} \beta_1(x)
\]</span> <span class="math inline">\(\boldsymbol{1}\)</span>是元素全为1的<span class="math inline">\(m\)</span>维列向量</p>
<h4 id="期望值计算">期望值计算</h4>
<p>利用前向、后向向量，可以计算特征函数关于联合分布<span class="math inline">\(P(X,Y)\)</span>和条件分布<span class="math inline">\(P(Y|X)\)</span>的数学期望。 特征函数<span class="math inline">\(f_k\)</span>关于条件分布<span class="math inline">\(P(Y|X)\)</span>的数学期望是: <span class="math display">\[
    \begin{aligned}
        E_{P(Y|X)}[f_k] &amp;= \sum_y P(y|x) f_k(y,x)   \\
        &amp;= \sum_{i=1}^{n+1} \sum_{y_{i-1},y_i} f_k(y_{i-1},y_i,x,i)  P(Y_{i-1} = y_{i-1},Y_i = y_i|x) \\
        &amp;\qquad k = 1,2,\dots,K 
    \end{aligned}
\]</span> 能够这样进行展开，实际上还是从<span class="math inline">\(f_k(y,x)\)</span>的定义出发： <span class="math display">\[
    f_k(y,x) = \sum_{i=1}^{n+1} f_k(y_{i-1},y_i, x,i),\quad k = 1,2,\dots,K
\]</span> 展开之后便是上述形式，这中写法也显示了在概率图中，联合概率分布可以表示成一系列局部概率分布的乘积形式。 假设经验分布为<span class="math inline">\(\tilde{P}(X)\)</span>,则特征函数关于联合分布<span class="math inline">\(P(X,Y)\)</span>的数学期望可以表示为： <span class="math display">\[
    \begin{aligned}
        E_{P(X,Y)}[f_k] &amp;= \sum_{x,y}P(x,y) \sum_{i=1}^{n+1} f_k(y_{i-1},y_i,x,i)  \\
        &amp;= \sum_{x} \tilde{P}(x) \sum_{i=1}^{n+1} \sum_{y_{i-1},y_i} f_k(y_{i-1},y_i,x,i)  P(Y_{i-1} = y_{i-1},Y_i = y_i|x) \\
        &amp;\quad, i =1,2,\dots,K 
    \end{aligned}
\]</span> 有了前向、后向算法，对于给定的观测序列<span class="math inline">\(x\)</span>与标记序列<span class="math inline">\(y\)</span>，可以通过一次前向扫描计算<span class="math inline">\(\alpha_i\)</span>及<span class="math inline">\(Z(x)\)</span>,通过一次后向扫描计算<span class="math inline">\(\beta_i\)</span>,从而计算所有的概率和特征的期望。</p>
<h3 id="条件随机场的学习算法">条件随机场的学习算法</h3>
<p><strong>条件随机场模型实际上是定义在时序数据上的对数线性模型</strong>，其学习方法包括极大似然估计和正则化的极大似然估计，具体的优化实现算法有改进的迭代尺度法IIS、梯度下降法及拟牛顿法。</p>
<h4 id="改进的迭代尺度法">改进的迭代尺度法</h4>
<p>已知训练数据集，由此可知经验概率分布<span class="math inline">\(\tilde{P}(X,Y)\)</span>，可以通过极大化训练数据的对数似然函数来求模型参数。 训练数据的对数似然函数为: <span class="math display">\[
    \begin{aligned}
        L(w) &amp;= L_{\tilde{P}}(P_w)  = \log \prod_{x,y} P_w(y|x)^{\tilde{P}(x,y)} = \sum_{x,y} \tilde{P}(x,y) \log P_w(y|x) \\
        &amp;= \sum_{x,y} [\tilde{P}(x,y) \sum_{k=1}^K w_k f_k(y|x) -\tilde{P}(x,y) \log Z_w(x)] \\
        &amp;= \sum_{j=1}^N \sum_{k=1}^K w_k f_k(y_j,x_j) - \sum_{j=1}^N \log Z_w(x_j)
    \end{aligned}
\]</span> 这一部分先挖坑，等我补一下相关知识再来填坑........</p>
<h3 id="条件随机场预测算法">条件随机场预测算法</h3>
<p>条件随机场的预测问题是指给定条件随机场<span class="math inline">\(P(Y|X)\)</span>和输入序列(观测序列)<span class="math inline">\(x\)</span>,求条件概率最大的输出序列<span class="math inline">\(y^*\)</span>,即对观测序列进行标注。与HMM模型中的预测问题类似，该问题可以通过维特比算法进行解决。 由条件随机场的简化形式: <span class="math display">\[
    P_w(y|x) = \frac{exp(\boldsymbol{w \cdot F(y,x)})}{Z_w(x)}
\]</span> 可知： <span class="math display">\[
    \begin{aligned}
        y^* &amp;= \arg \max_{y} P_w(y|x) \\
            &amp;= \arg \max_{y} \frac{exp(\boldsymbol{w \cdot F(y,x)})}{Z_w(x)} \\
            &amp;= \arg \max_{y} exp(\boldsymbol{w \cdot F(y,x)}) \\ 
            &amp;= \arg \max_{y}   \boldsymbol{w \cdot F(y,x)}
    \end{aligned}
\]</span> 于是，条件随机场的预测问题成为求非规范概率最大的最优路径问题： <span class="math display">\[
    \max_y (\boldsymbol{w \cdot F(y,x)})
\]</span> 这里路径表示标记序列，其中: <span class="math display">\[
    \begin{aligned}
        \boldsymbol{w} &amp;= (w_1,w_2,\dots,w_K)^T \\
        \boldsymbol{F(y,x)} &amp;= (f_1(y,x),f_2(y,x),\dots,f_K(y,x))^T  \\
        f_k(y,x) &amp;= \sum_{i=1}^n f_k(y_{i-1},y_i,x,i),\quad k = 1,2,\dots,K
    \end{aligned}
\]</span> 为了求解方便，考虑将优化问题写成如下形式： <span class="math display">\[
    \max_y \sum_{i=1}^n \boldsymbol{w} \cdot F_i(y_{i-1},y_i,x)
\]</span> 其中: <span class="math display">\[
    F_i(y_{i-1},y_i,x) = (f_1(y_{i-1},y_i,x,i),f_2(y_{i-1},y_i,x,i),\dots,f_K(y_{i-1},y_i,x,i))
\]</span> 是局部特征向量。下面给出维特比算法流程: &gt; <strong>维特比算法</strong>： &gt; 输入:模型特征向量<span class="math inline">\(\boldsymbol{F(y,x)}\)</span>,权值向量<span class="math inline">\(\boldsymbol{w}\)</span>,观测序列<span class="math inline">\(\boldsymbol{x} = (x_1,x_2,\dots,x_n)\)</span> &gt; 输出:最优路径<span class="math inline">\(\boldsymbol{y^\ast = (y_1^\ast,y_2^\ast,\dots,y_n^\ast)}\)</span> &gt; 1. 初始化： &gt; <span class="math display">\[
    \delta_1(j) = \boldsymbol{w} \cdot F_1(y_0 = start,y_1=j,x), \quad j = 1,2,\dots,m
&gt; \]</span> &gt; 2. 递推：对<span class="math inline">\(i = 2,3,\dots,n\)</span> &gt; <span class="math display">\[
    \begin{aligned}
        \delta_i(l) &amp;= \max_{1 \le j \le m}\{ \delta_{i-1}(j) + \boldsymbol{w} \cdot F_i(y_{i-1} = j,y_i = l,x)\},\quad l = 1,2,\dots,m  \\
        \varphi_i(l) &amp;= \arg \max_{1 \leq j \leq m}\{ \delta_{i-1}(j) + \boldsymbol{w} \cdot F_i(y_{i-1} = j,y_i = l,x)\},\quad l = 1,2,\dots,m 
    \end{aligned}
&gt; \]</span> &gt; 3. 终止: &gt; <span class="math display">\[
    \begin{aligned}
        \max_y(\boldsymbol{w} \cdot \boldsymbol{F(x,y)}) &amp;= \max_{1 \leq j \leq m} \delta_n(j) \\
        y_n^\ast &amp;= \arg \max_{1 \leq j \leq m} \delta_n(j)
    \end{aligned}
&gt; \]</span> &gt; 4. 回溯，得最优路径: &gt; <span class="math display">\[
    y_i^\ast = \varphi_{i+1}(y_{i+1}^\ast),\quad i = n-1,n-2,\dots,1
&gt; \]</span> &gt; 求得最优路径<span class="math inline">\(\boldsymbol{y^\ast} = (y_1^\ast,y_2^\ast,\dots,y_n^\ast)\)</span></p>
]]></content>
      <categories>
        <category>统计学习</category>
      </categories>
      <tags>
        <tag>概率图，条件随机场</tag>
      </tags>
  </entry>
  <entry>
    <title>深度学习基础</title>
    <url>/2020/09/02/shen-du-xue-xi-ji-chu/</url>
    <content><![CDATA[<p>近些年来，神经网络算法家族蓬勃发展，本部分主要介绍一下这些算法的一些通用原理基础，该部分按照以下结构组织: - 神经元模型 - 多层神经网络 - <strong>反向传播算法(BP)</strong> - 网络训练中常见问题 - 常见网络模型介绍</p>
<a id="more"></a>
<h3 id="神经元模型">神经元模型</h3>
<p>时至今日，神经网络已经是一个相当大的、多学科交叉的学科领域，下面给出神经网络的定义： &gt; <strong>神经网络:</strong> 神经网络是由具有适应性的简单单元所组成的广泛并进行互联的网络，它的组织能够模拟生物神经系统对真实世界物体所做出的交互反应。</p>
<p>神经网络的基本组成成分是神经元模型，即神经网络定义中的“简单单元”。在生物神经网络中，每个神经元与其他神经元相连，当它“兴奋”时，就会向相连的神经元发送化学物质，从而改变这些神经元内的电位；如果某神经元的电位超过了一个“阈值”，那么它就会被激活，向其他神经元发送化学物质。</p>
<p><img src="https://raw.githubusercontent.com/xuejy19/xuejy19.github.io/source/Img/Neuro-model1.png" alt="神经元" /> 将生物学概念中的神经元模型可以抽象成如下图所示的数学模型，这就是一直沿用至今的“M-P神经元模型”，在这个模型中，神经元接受到来自<span class="math inline">\(n\)</span>个其他神经元传递过来的输入信号，这些输入信号通过带权重的连接进行传递，神经元接收到的总输入值将与神经元的阈值做比较，然后同过“激活函数”处理以产生神经元的输出。 <img src="https://raw.githubusercontent.com/xuejy19/xuejy19.github.io/source/Img/Neuro-model.jpg" alt="神经元模型" /> 理想的激活函数是阶跃函数，它将输入值映射为输出值0或1，1对应着神经元兴奋，0对应着神经元抑制，然而阶跃函数具有不连续、不光滑等性质，因此实际常用sigmoid函数作为激活函数: <span class="math display">\[
    sigmoid(x) = \frac{1}{1 + e^{-x}}
\]</span> sigmoid函数能够将在较大范围内变化的输入值压缩到<span class="math inline">\((0,1)\)</span>输出范围内，因此有时也被称为“挤压函数”。 <img src="https://raw.githubusercontent.com/xuejy19/xuejy19.github.io/source/Img/act_function.png" alt="激活函数" /> 把许多这样的神经元按照一定的层次结构连接起来，就得到了一个神经网络，尽管神经网络类型有很多，但其组成基本单元几乎都是“M-P神经元模型”。从计算机科学的角度看，我们可以先不考虑神经网络是否真的模拟了神经网络，只需要将一个神经网络视为包含了许多参数的数学模型，这个模型是若干函数，例如<span class="math inline">\(y_j = f(\sum_i w_i x_i - \theta_j)\)</span>相互嵌套代入而得，有效的神经网络算法大多以数学证明为支撑。</p>
<h3 id="多层感知机">多层感知机</h3>
<h4 id="定义">定义</h4>
<p>在前面已经介绍过感知机模型，感知机模型也可以看作一个以符号函数为激活函数的神经网络，只有两层，一个是输入层，一个是输出层,如下图所示 <img src="https://raw.githubusercontent.com/xuejy19/xuejy19.github.io/source/Img/Perceptron.jpg" alt="Perceptron" /> 但是这只是一个线性模型，只能够处理线性可分数据，其学习能力非常有限，甚至对于简单的异或问题都无法解决，更一般的，常见的神经网络是多层结构，如下图所示 <img src="https://raw.githubusercontent.com/xuejy19/xuejy19.github.io/source/Img/MLP.png" alt="多层感知机" /> 在多层感知机中，每层神经元与下一层神经元全互连，神经元之间不存在同层连接，也不存在跨层连接，这样的神经网络结构通常称为“多层前馈神经网络”，其中输入层神经元接收外界输出，隐层与输出层神经元对信号进行加工，最终结果由输出层神经网络输出；换言之，输入层神经元仅是接受输入，不进行函数处理，隐层与输出层包含功能神经元，一个神经网络只要包含隐层，就可以称为多层网络。神经网络的学习过程，就是根据训练数据来调整神经元之间的连接权以及每个功能神经元的阈值；换言之，神经网络学到的东西，蕴含在连接权与阈值之中。</p>
<h4 id="多层感知机逼近能力">多层感知机逼近能力</h4>
<p>多层感知机的逼近能力由如下定理保证: &gt; <strong>定理：</strong> 令<span class="math inline">\(\phi(\cdot)\)</span> 为有界、非常量单调递增连续函数，<span class="math inline">\(I_p\)</span>表示<span class="math inline">\(p\)</span>维单位超立方体<span class="math inline">\([0,1]^p\)</span>,<span class="math inline">\(C(I_p)\)</span>表示定义在<span class="math inline">\(I_p\)</span>上的连续函数构成的集合，则给定任意函数<span class="math inline">\(f \in C(I_p)\)</span>和<span class="math inline">\(\epsilon &gt;0\)</span> ,存在整数<span class="math inline">\(M\)</span>和一组实常数<span class="math inline">\(\alpha_i,\theta_i,w_{ij}\)</span>,其中<span class="math inline">\(i=1,2,\dots,M;j=1,2,\dots,p\)</span>,使得网络输出: &gt; <span class="math display">\[
    F(x_1,x_2,\dots,x_p) = \sum_{i=1}^M \alpha_i \phi(\sum_{j=1}^p w_{ij}x_j - \theta_i)
&gt; \]</span> &gt; 可任意逼近<span class="math inline">\(f(\cdot)\)</span>,即： &gt; <span class="math display">\[
    |F(x_1,x_2,\dots,x_p) - f(x_1,x_2,\dots,x_p)| &lt; \epsilon , \forall (x_1,\dots,x_p) \in I 
&gt; \]</span></p>
<h3 id="反向传播算法">反向传播算法</h3>
<p>多层网络的学习能力比单层感知机强得多，欲训练多层网络，则需要更加强大的学习算法，误差逆传播(BP)算法就是其中最杰出的代表，它是迄今为止最成功的神经网络学习算法，现实任务中在使用神经网络时，大多是使用BP算法进行训练。</p>
<p>下面对BP算法做简单推导，给定训练集<span class="math inline">\(D = \{ (\boldsymbol{x_1,y_1}),(\boldsymbol{x_2,y_2}),\dots , (\boldsymbol{x_N,y_N})\}\)</span> ,其中<span class="math inline">\(\boldsymbol{x_i} \in \mathbb{R}^d,\boldsymbol{y_i} \in \mathbb{R}^l\)</span>,即输入是由<span class="math inline">\(d\)</span>个属性描述，输出<span class="math inline">\(l\)</span>维实值向量，下面以一个三层前馈网络来进行反向传播算法推导，输入层有<span class="math inline">\(d\)</span>个神经元，隐藏层有<span class="math inline">\(q\)</span>个神经元，输出层有<span class="math inline">\(l\)</span>个神经元。输出层第<span class="math inline">\(j\)</span>个神经元的阈值用<span class="math inline">\(\theta_j\)</span>表示，隐层第<span class="math inline">\(h\)</span>个神经元的阈值用<span class="math inline">\(\gamma_h\)</span>表示，输入层第<span class="math inline">\(i\)</span>个神经元与隐层第<span class="math inline">\(h\)</span>个神经元之间的连接权重为<span class="math inline">\(v_{ih}\)</span>,隐层第<span class="math inline">\(h\)</span>个神经元与输出层第<span class="math inline">\(j\)</span>个神经元之间的连接权重为<span class="math inline">\(w_{hj}\)</span>,记隐层第<span class="math inline">\(h\)</span>个神经元接收到的输入为<span class="math inline">\(\alpha_h = \sum_{i=1}^d v_{ih} x_i\)</span>,输出层第<span class="math inline">\(j\)</span>个神经元输入为$<em>j = </em>{h=1}^q w_{hj} b_h <span class="math inline">\(,其中\)</span>b_h<span class="math inline">\(为隐层第\)</span>h<span class="math inline">\(个神经元的输出。假设隐层和输出层神经元都是用Sigmoid函数作为激活函数。 对训练例\)</span>()<span class="math inline">\(,假定网络输出为\)</span>_k = (_1^k, _2^k, , _l^k)$,即： <span class="math display">\[
    \hat{y}_j^k = f(\beta_j - \theta_j)
\]</span> 则网络在<span class="math inline">\((\boldsymbol{x_k,y_k})\)</span>用方差和来定义: <span class="math display">\[
    E_k = \frac{1}{2} \sum_{j=1}^l (\hat{y_j}^k - y_j^k)^2
\]</span> 首先来考虑对于这样一个三层网络，有多少个参数需要确定，权重参数有<span class="math inline">\(d \times q + q \times l\)</span>个,阈值参数有$ q + l<span class="math inline">\(个，因此总共需要学习的参数个数为\)</span>(d+l+1)q +l<span class="math inline">\(个，BP是一个迭代学习算法，在每一轮中采用广义的感知机策略(链式法则求梯度)进行参数学习，任意参数\)</span>v$的更新估计式为: <span class="math display">\[
    v \leftarrow v + \Delta v
\]</span> 具体参数的求解其实就是一个链式法则求导的过程，比如以隐藏层到输出层的权重<span class="math inline">\(w_{hj}\)</span>为例，其实只需要找到从<span class="math inline">\(w_{hj}\)</span>到误差函数中间有哪些中间变量，然后反向传播时便将误差按照变量路径传递回来即可，对于<span class="math inline">\(w_{hj}\)</span>,我们可以写出一条变量路径: <span class="math display">\[
    w_{hj} \rightarrow  \beta_j \rightarrow \hat{y}_j^k \rightarrow E_k
\]</span> 因此由链式法则，可得: <span class="math display">\[
    \frac{\partial E_k}{\partial w_{hj}} = \frac{\partial E_k}{\partial \hat{y}_j^k} \frac{ \partial \hat{y}_j^k}{\partial \beta_j} \frac{\partial \beta_j}{\partial w_{hj}} = (\hat{y}_j^k - y_j^k) \hat{y}_j^k (1 - \hat{y}_j^k) b_h
\]</span> $ <span class="math inline">\(应用了Sigmoid函数\)</span>f'(x) = f(x)(1 - f(x))$的性质，由此梯度下降算法原理，可得： <span class="math display">\[
    \Delta w_{hj} = -\eta \frac{\partial E_k}{\partial w_{hj}} 
\]</span> 下面给出<span class="math inline">\(v_{ih}\)</span>的前向变量传递路径： <span class="math display">\[
    v_{ih} \rightarrow \alpha_h \rightarrow b_h \rightarrow \beta_1,\dots,\beta_l \rightarrow \hat{y}_1^k, \hat{y}_2^k \dots \hat{y}_l^k \rightarrow E_k
\]</span> 此时前向路径共有<span class="math inline">\(l\)</span>条，因此计算梯度时需要分别极计算<span class="math inline">\(l\)</span>部分，计算结果为: <span class="math display">\[
    \begin{aligned}
         \Delta v_{ih} &amp;= -\eta \frac{\partial E_k}{b_h} \frac{\partial b_h}{\partial \alpha_h} \frac{\partial \alpha_h}{\partial v_{ih}}  \\
         &amp;= -\eta [\sum_{j=1}^l \frac{\partial E_k}{\partial \beta_j} \frac{\partial \beta_j}{\partial b_h}] f&#39;(\alpha_h - \gamma_h) x_i 
    \end{aligned}
\]</span> 其他参数更新也都是同样思路，在这里便不做过多赘述，其实BP算法就只是应用链式求导法则反向计算梯度(更新权重)的方法。与随机梯度下降以及批梯度下降相对应，BP算法也可以分为标准BP算法(一次更新一个实例)，累积BP算法(一次更新所有样例)，在实际中训练时往往采取折中的策略，每次取一批数据进行权重更新。</p>
<h3 id="网络训练中常见问题">网络训练中常见问题</h3>
<p>对于这样一个简单BP网络，在训练中也会面临很多问题，这些问题是神经网络算法所固有的一些问题，很多研究也是以解决这些问题为出发点，这部分主要介绍三个问题: - 过拟合问题 - 梯度消失与梯度爆炸 - 局部极小值问题</p>
<h4 id="过拟合问题">过拟合问题</h4>
<p>正是由于BP网络具有强大的表示能力，BP神经网络经常遭遇过拟合，即随着训练的进行，训练误差进一步降低，但测试误差却可能上升。有两种策略可以缓解过拟合问题： - 早停：将数据划分为训练集和验证集，训练集用来计算梯度、更新连接权和阈值，验证集用来估计误差，若训练集误差降低但验证集误差升高，则停止训练，并返回具有最小验证集误差的连接权和阈值。 - 正则化： 在目标误差函数中增加一个用于描述网络复杂度的部分，例如连接权和阈值的平方和，仍令<span class="math inline">\(E_k\)</span>表示第<span class="math inline">\(k\)</span>个训练样例上的误差，<span class="math inline">\(w_i\)</span>表示连接权和阈值，则对于批反向传播的误差函数可以写为: <span class="math display">\[
    E = \lambda \frac{1}{m} \sum_{k=1}^m E_k + (1 - \lambda) \sum_i w_i^2
\]</span> 其中<span class="math inline">\(\lambda\)</span>用于对经验误差和网络复杂度进行折中，常通过交叉验证法进行估计。</p>
<h4 id="梯度消失与梯度爆炸">梯度消失与梯度爆炸</h4>
<p>我们在反向计算梯度时实际上是一个链式求导的过程，假设有一个层数为5的神经网络(包含输入层和输出层)，假设我们欲更新输入层到第一隐层的一个权重<span class="math inline">\(w\)</span>，中间需要经过若干变量(<span class="math inline">\(w_1,\dots,w_k\)</span>)才能到达损失函数,则进行反向传播时，损失函数<span class="math inline">\(L\)</span>对<span class="math inline">\(w\)</span>求偏导是一个连乘积的形式： <span class="math display">\[
    \frac{\partial L}{\partial w} = \frac{\partial L}{\partial w_k} \cdot \frac{\partial w_k}{\partial w_{k-1}} \dots \frac{\partial w_1}{\partial w}
\]</span> 若这些连乘积每一项都大于1，则就出现了梯度爆炸情况，导致参数更新不稳定，若连乘积每一项都小于1，则连乘之后就会是一个非常小的值，也就是说出现了梯度消失。 梯度在反向传播过程中，没经过一层就会经过一次对激活函数求导的过程，加入选的是Sigmoid函数作为激活函数，其导数<span class="math inline">\(f&#39;(x) \leq 0.25\)</span>,因此随着网络的变深，使用Sigmoid函数作为激活函数会不可避免地导致出现梯度消失。 <img src="https://raw.githubusercontent.com/xuejy19/xuejy19.github.io/source/Img/sigmoid.png" alt="sigmoid" /> 下面介绍三个解决梯度消失/爆炸问题的思路。 ##### Relu激活函数 为解决使用Sigmoid激活函数导致梯度消失的问题，在深层神经网络的训练中人们往往采用另一种激活函数——Relu以及它的变形Leakage-Relu，函数图像如下图所示: <img src="https://raw.githubusercontent.com/xuejy19/xuejy19.github.io/source/Img/relu.jpg" alt="relu" /> 可以看到，Relu函数在被激活后(<span class="math inline">\(&gt;0\)</span>),梯度恒为1，既不会导致梯度消失也不会导致梯度爆炸，但relu函数有一个明显的缺点:当relu的输入值为负时，输出始终为0，这样会导致神经元不能够更新参数，也就是该神经元无法学习了，为了解决relu函数这个问题，在relu的负半区引入了一个泄漏值，也就是说当输入小于0时仍旧有一个小的梯度，该泄漏值可以指定也可以作为一个参数与神经网络其他参数一同被学习。</p>
<h5 id="batchnorm">Batchnorm</h5>
<p>机器学习领域有一个非常重要的假设:IID，即独立同分布假设， 假设训练数据和测试数据是满足相同分布的，这也是通过训练数据获得的模型能够在测试集获得好的效果的一个基本保障，而： &gt; <strong>Batchnorm就是在深度神经网络训练过程中使每一层神经网络的输入保持相同分布。</strong></p>
<p>由于深度神经网络往往具有复杂的网络结构，在进行训练过程中，各层神经元的参数都在变化，导致各层神经元的输入分布不断发生偏移，BN的思想相当直观，之所以训练收敛慢，一般是整体分布逐渐往非线性函数取值区间的上下限两端端靠近，这就导致向后传播时低层神经网络出现梯度消失现象，这是训练神经网络收敛越来越慢的本质原因，而BN就是通过一定规范化手段，把每层神经网络任意神经元的输入值强行拉回到均值为0，方差为1的正态分布，这样使得激活函数的输入落在非线性函数对输入较为敏感的区域(以Sigmoid函数为例，落在0附近区域)，这样进行梯度反向传播时求导得到的梯度值就会较大，一定程度上缓和了梯度消失现象。</p>
<p>但是很明显，从Sigmoid函数图像可以看出，0附近区域其实是接近线性的，如果把所有的输入都拉到Sigmoid函数的线性区域内，那么不就相当于是激活函数是线性函数了么，而我们知道多层线性网络与一层线性网络其实是等价的，所以BN为了保持激活函数的非线性，对norm之后的输入又做了一步变换: <span class="math display">\[
    y_i = \gamma \hat{x}_i +    \beta
\]</span> 每个神经元增加了两个参数：尺度参数<span class="math inline">\(\gamma\)</span>，偏移参数<span class="math inline">\(\beta\)</span>,这等价于让输入从线性区域向非线性区域移动了一点，引入这两个参数本质上是想找到一个线性和非线性的平衡点，使得网络既具有非线性的强表达能力，又在一定程度上减轻了梯度消失问题。 下面给出Batchnorm的具体流程: <img src="https://raw.githubusercontent.com/xuejy19/xuejy19.github.io/source/Img/BN.png" alt="Batchnorm" /></p>
<h5 id="残差结构">残差结构</h5>
<p>首先来谈一谈深层网络的退化问题，理论上来说，深层网络的性能应当不会比浅层网络性能更差，因为深层网络可以考虑对浅层网络进行拷贝，然后剩下来的层作为一个恒等映射，这样最起码深层网络的性能应当与浅层网络性能一致，然而事实却并非这样，实验表明，深层网络表现并不总是优于浅层网络。 <img src="https://raw.githubusercontent.com/xuejy19/xuejy19.github.io/source/Img/deep.png" alt="网络性能对比" /></p>
<p>深度网络的退化说明深度网络不易训练，前面讨论到深层网络往往并不能够通过建立恒等映射的形式来达到与浅层网络相同的性能，因此何凯明等考虑显式的引入该恒等映射，而让中间的网络学习一个残差，假设我们希望一个堆叠非线性层(stacked nonlinear layers)学习的映射是<span class="math inline">\(\mathcal{H(x)}\)</span>,我们现在让该堆叠非线性层学习另一个映射<span class="math inline">\(\mathcal{F(x)} = \mathcal{H(x)} - x\)</span>，通过<span class="math inline">\(\mathcal{F(x)}+x\)</span>来重构期望映射， 而在网络中实现这个目的的操作则是“跳连”(shortcut connection)，如下图所示： <img src="https://raw.githubusercontent.com/xuejy19/xuejy19.github.io/source/Img/res.png" alt="resnet" /></p>
<p>上图所示的结构一般被称为一个残差单元，下面我们来仔细讨论下这样的结构为什么可以解决梯度消失问题，假设上述残差单元的输入为<span class="math inline">\(x\)</span>,则残差单元的输出可以表示为: <span class="math display">\[
    \boldsymbol{y} = \mathcal{F(\boldsymbol{x},\{(\boldsymbol{W_i})\})} + \boldsymbol{x}，i=1,2,\dots,k
\]</span> <span class="math inline">\(k\)</span>表示该残差单元包含几个权重层，需要说明的是，一个残差单元至少应该包含两个层，若只有一个层，则会出现: <span class="math display">\[
    \boldsymbol{y} = \boldsymbol{W_1^T x + x} = \boldsymbol{(W_1^T + 1)x}
\]</span> 而若包含两层，则残差单元的输出写做: <span class="math display">\[
    \boldsymbol{y} = \boldsymbol{W_2^T \sigma(W_1^Tx) + \boldsymbol{x}} 
\]</span> 其中,假设<span class="math inline">\(\boldsymbol{x} \in \mathbb{R}^d\)</span>,残差单元第一层有<span class="math inline">\(n_1\)</span>个神经元，则上式中<span class="math inline">\(\boldsymbol{W_1} \in \mathbb{R}^{d \times n_1}, \boldsymbol{W_2} \in \mathbb{R}^{n_1 \times d}\)</span>,损失函数到<span class="math inline">\(\boldsymbol{y}\)</span>的梯度为<span class="math inline">\(\frac{\partial L}{\partial \boldsymbol{y}}\)</span>,那么我们可以用该梯度表示<span class="math inline">\(\frac{\partial L}{\partial \boldsymbol{x}}\)</span>: <span class="math display">\[
    \frac{\partial L}{\partial \boldsymbol{x}} = \frac{\partial L}{\partial \boldsymbol{y}} \frac{\partial \boldsymbol{y}}{\partial \boldsymbol{x}} = \frac{\partial L}{\partial \boldsymbol{y}} (\boldsymbol{I} +  \boldsymbol{(W_1 W_2)^T}) 
\]</span> 其中激活函数取得Relu激活函数，可以看到，在进行反向传播过程中，到<span class="math inline">\(\boldsymbol{y}\)</span>的梯度会无损再传递到其前层的<span class="math inline">\(\boldsymbol{x}\)</span>，哪怕是残差梯度等于0或者是一个大于<span class="math inline">\(-1\)</span>的负值，也可以保证有梯度传递到前面的层，这使得Resnet相对于普通的神经网络更不容易发生梯度消失现象。 何凯明博士这篇提出Resnet的论文也因为其突出贡献获得了2016年CVPR最佳论文奖，整篇论文非常简洁易懂，非常建议大家去读一下，这里给出论文链接<a href="https://openaccess.thecvf.com/content_cvpr_2016/papers/He_Deep_Residual_Learning_CVPR_2016_paper.pdf">Deep Residual Learning for Image Recognition</a></p>
<h4 id="局部极小值问题">局部极小值问题</h4>
<p>神经网络训练过程其实就是一个参数寻优的过程，反向传播算法可以看作是在神经网络这种架构下梯度下降法的实现形式，因此最终收敛的点往往具有梯度为0的特点，这是一个局部极小点，若优化问题是一个凸问题，则局部极小点同时也是全局极小值，而神经网络的优化大多数情况都是一个非凸优化问题，这也就意味着最终训练收敛的点大概率只是一个“局部极小值点”，而非“全局极小值”点,如下图所示: <img src="https://raw.githubusercontent.com/xuejy19/xuejy19.github.io/source/Img/min.png" alt="局部极小与全局极小" /> 在现实任务中，人们常采用以下策略试图跳出局部极小，到达全局极小： - 以多组不同参数值初始化多个网络，按标准方法训练后，取其中误差最小的解作为最终参数，这相当于从多个不同的初始点开始搜索，这样就可能陷入不同的局部极小，从中进行选择有可能获得更接近全局最小的结果。 - 使用“模拟退火”策略，模拟退火在每一步都以一定概率接受比当前解更差的结果，从而有助于跳出局部极小，在迭代过程中，接受“次优解“的概率要随着时间推移而逐渐降低，从而保证算法稳定。 - 使用随机梯度下降(SGD)算法，即便陷入了局部极小点，所计算出的梯度仍可能不为0，这样就有机会跳出局部极小继续搜索。</p>
<h3 id="常见网络模型介绍">常见网络模型介绍</h3>
<p>神经网络家族成员庞杂，除了前面介绍的BP神经网络外，还有很多类型的神经网络，这部分就一些常见的做简要介绍，后面用到的话再补充。 #### 卷积神经网络 卷积神经网络在图像处理领域有着广泛应用，一个完整的卷积神经网络其实包含两个部分: - <strong>特征提取</strong> - 分类</p>
<p>后面分类所用的网络其实就是前面所讨论的全连接神经网络或者它的一些变形，卷积神经网络名字的由来则主要是在特征提取阶段，下面就以图片处理为例重点讲解一下特征提取过程。 <img src="https://raw.githubusercontent.com/xuejy19/xuejy19.github.io/source/Img/CNN.png" alt="CNN" /> 其实我们不论做什么样的机器学习任务，都可以分为特征提取和算法学习两个阶段，而CNN则将这两个阶段结合了起来，然后一起优化，是一种“end2end”的算法，对于一张<span class="math inline">\(n \times n\)</span>像素的黑白图像，其实该图像所有信息都包含在<span class="math inline">\(n \times n\)</span>个值当中，若要完成一个分类任务，最简单粗暴的方法可能是直接将这<span class="math inline">\(n \times n\)</span>个值直接作为特征输入到到全连接层来进行分类，但是这样做有以下两个缺点: - 输入特征维度非常高，因此网络的规模会很大，参数量会非常大，计算开销会非常大，训练困难。 - 单个像素值本身并不具备什么意义，最终神经网络结果很有可能并不理想。</p>
<p>卷积神经网络的出发点也是为了解决这样一个问题，特征提取部分主要包含三种操作: - 卷积层 - ReLu层 - 池化层</p>
<h4 id="卷积层">卷积层</h4>
<p>一个图像中的单个像素点并不具备什么含义，但是一簇像素点(比如一幅图像中的眼睛区域)可能就具备某种含义了，而卷积层的出发点就是希望能够提取图像中某部分区域的信息，而具体操作则是通过卷积核与图像区域进行加权求和，这其实本质上就是一个线性映射，如下图所示，图像处理中的卷积与信号处理中的卷积不同之处在于其仅仅只有加权求和操作，而并没有翻转操作。 <img src="https://raw.githubusercontent.com/xuejy19/xuejy19.github.io/source/Img/conv.png" alt="图像卷积" /> 上图中所示的是一个<span class="math inline">\(3 \times 3\)</span>的卷积核，可以看出该卷积核对于一个区域左上角像素与有下降像素更加关注，而一正一负操作则表明该卷积核更关注的是左上角像素值与由下角像素值的差。选用不同的卷积核可以从原始图像中提取不同的特征，因此在实际应用中，一般会选用多个大小不一的卷积核以全面捕捉不同大小的区域所包含的信息。 在进行卷积操作时，有时会对图像周围区域进行填充，以提高卷积操作对图像边缘特征提取的能力。 #### ReLu层 经过Relu层则主要是为了实现非线性变换，以提高网络的表示能力，比如上图中的-8经过ReLu层之后就变成了0。 #### 池化层(下采样层) 经过卷积操作之后，我们得到了一张张有着不同值的特征图(feature map)，尽管数据量比原图少了很多，但还是过于庞大，而池化层的作用就是进一步减少数据量,扩大特征图的感受野。 <img src="https://raw.githubusercontent.com/xuejy19/xuejy19.github.io/source/Img/Pooling.png" alt="池化" /> 池化一般有两种操作，一种是Max pooling,一种是Average pooling,前一种就是取一个区域的最大值，而后一种则是取一个区域的平均值。 #### 总结 以上这三种操作就是就是卷积神经网络特征提取部分的三个基本单元，对这三种基本单元进行组合便可以组成特征提取部分，需要说明的是卷积神经网络中一个非常重要的操作就是<strong>共享权重</strong>： &gt; 假设输入图像为<span class="math inline">\(32 \times 32\)</span>,用两个<span class="math inline">\(5 \times 5\)</span>卷积核，则一幅图片经过卷积操作之后变为<span class="math inline">\(28 \times 28\)</span>的特征图，该特征图中每个神经元还有一个偏置，因此连接数为: &gt; <span class="math display">\[
    (5 \times 5 + 1) \times 28 \times 28 \times 2
&gt; \]</span> &gt; 但在卷积神经网络中，一个特征图共用一套权值，因此实际需要更新的连接权数为: &gt; <span class="math display">\[
    (5 \times 5 +1 )\times 2 
&gt; \]</span></p>
<p>最后给出经过卷积和池化操作后特征图尺寸变化计算公式: <span class="math display">\[
    \begin{aligned}
        &amp;卷积： Size_{out} = (Size_{in} - Kernel_{conv} + 2*Padding)/Stride + 1 \\
        &amp;池化： Size_{out} = (Size_{in} - Kernel_{pooling}) /stride + 1
    \end{aligned}
\]</span></p>
<h3 id="循环神经网络rnn">循环神经网络(RNN)</h3>
<h4 id="标准循环神经网络">标准循环神经网络</h4>
<h5 id="直观理解">直观理解</h5>
<p>在实际应用中，我们常常会碰到序列数据，这类数据的特点是相邻数据点之间有着较强的联系，比如语句数据、车辆轨迹数据、财经数据等，传统的神经网络已经有着很强的表示能力，但传统的神经网络只能够单独地处理一个接一个输入，前一个输入与后一个输入并没有任何关系，但是对于序列数据，很明显我们对于后一个数据点的预测是依赖于前面的数据的，下面举一个简单的🌰： &gt; <strong>词性标注任务：</strong> &gt; <strong>任务输入：</strong> 我/吃/苹果 &gt; <strong>任务输出：</strong> 我/nn 吃/v 苹果/nn &gt; 对于这样一个任务，我们当然可以直接用普通的神经网络来做，输入为单独的单词，输出为词性标注好的单词。<strong>但是很明显，一个句子中，前一个单词其实对当前单词的词性预测有很大的影响，比如预测“苹果”词性的时候，由于前面的“吃”是一个动词，那么“苹果”作为名次的概率就会远大于动词的概率，因为动词后面接名次很常见，而动词后面接动词不常见</strong></p>
<p>为了解决这样类似的问题，更好地处理序列信息，循环神经网络就诞生了，下图就是一个简单的RNN的示意图。 <img src="https://raw.githubusercontent.com/xuejy19/xuejy19.github.io/source/Img/RNN.png" alt="RNN" /> 如果将上图中<span class="math inline">\(h^{(t-1)} \rightarrow h^{(t)}\)</span>的连线断开，那么一个RNN就被分解成了若干独立的三层神经网络，现在加入了隐层之间的连接，就使得时刻靠后的神经网络的输出会依赖于前面时刻神经网络的值，以<span class="math inline">\(O^{(t )}\)</span>为例，往前回推，可以写出一条变量链如下: <span class="math display">\[
    O^{(t)} \rightarrow h^{(t)} \rightarrow x^{(t)},h^{(t-1)} \rightarrow x^{(t)}, x^{(t-1)},h^{(t-2)} \rightarrow \dots
\]</span> 从该变量链可以看出，<span class="math inline">\(t\)</span>时刻的预测输出是依赖于其之前所有的输入。</p>
<h5 id="权重共享">权重共享</h5>
<p>在RNN中也沿用了权重共享的思想，可以看到，不同时刻的神经网络共享输入权重矩阵<span class="math inline">\(\boldsymbol{U}\)</span>，输出权重矩阵<span class="math inline">\(\boldsymbol{V}\)</span>以及循环权重矩阵<span class="math inline">\(\boldsymbol{W}\)</span>,下面简单分析下这样做的好处： - 运算量减少，模型训练加快 - 序列长度可以不固定，模型更具泛化能力</p>
<p>在进行BP的时候，由于权重共享，因此误差传递每个时刻都会有，假设序列长度为<span class="math inline">\(T\)</span>，要对<span class="math inline">\(\boldsymbol{U}\)</span>进行权重更新，可以写出误差传递链为： <span class="math display">\[
    E \rightarrow E_1, E_2,\dots E_T \rightarrow O^{(1)},O^{(2)},\dots,O^{(T)} \rightarrow  \rightarrow h^{(1)}, h^{(2)}, \dots, h^{(T)} \rightarrow x^{(1)}, x^{(2)},\dots,x^{(T)}
\]</span> 当然，在反向传播的过程中，<span class="math inline">\(h^{t}\)</span>到<span class="math inline">\(h^{(t-1)},\dots,h^{(1)}\)</span>都是有着传播链条的， 是一个连乘积求和的形式,下面给出<span class="math inline">\(\frac{\partial L_t}{\partial U}\)</span>的公式: <span class="math display">\[
    \frac{\partial L_t}{\partial U} = \sum_{k=0}^t \frac{\partial L_t}{\partial O^{(t)}}  \frac{\partial O^{(t)}}{\partial h^{(t)}}(\prod_{j=k+1}^t \frac{\partial h^{(j)}}{\partial h^{(j-1)}}) \frac{\partial h^{(k)}}{U}
\]</span> ##### RNN梯度消失 从上面的梯度表达式可以看出，<span class="math inline">\(L_t\)</span>对输入矩阵<span class="math inline">\(U\)</span>的梯度由它<span class="math inline">\(t\)</span>项组成，由于有着激活函数存在，因此对靠前时刻的<span class="math inline">\(U\)</span>求导是连乘积形成，因此随着序列的增长，累加项中距离当前时刻较远的时刻的项会出现梯度消失或者梯度爆炸，这就导致梯度被近距离梯度主导，模型难以学得远距离的依赖关系。 #### LSTM 前面讲到普通的RNN由于存在对远距离依赖的梯度消失或者爆炸现象，因此对于长序列表现并不是很好，为解决这一问题，LSTM便诞生了，首先给出LSTM的结构图。 <img src="https://raw.githubusercontent.com/xuejy19/xuejy19.github.io/source/Img/LSTM.png" alt="LSTM" /> 与标准RNN相比，LSTM增加了三个门控组建，输入门控制当前时刻<span class="math inline">\(t\)</span>的计算输出，<strong>与普通RNN相比，LSTM还另外又用遗忘门来控制之前时刻的细胞状态相连接，这个操作类似于前面介绍的Resnet的跳连(skip connection)操作，通过这样的跳连操作，使得若遗忘门初始化较大(接近1)，则在误差反向传播时，可以通过该通路无损地传递到前面时刻，这在一定程度上缓解了RNN中的梯度消失问题，使得LSTM可以处理较长序列</strong>。下面重点讨论下为何引入这三个门控： - 输入门<span class="math inline">\(i_t\)</span>：输入门控制当前词<span class="math inline">\(\boldsymbol{x_t}\)</span>融入细胞状态<span class="math inline">\(\boldsymbol{c_t}\)</span>, 在做句子理解任务时，当前词<span class="math inline">\(\boldsymbol{x_t}\)</span>可能对整句话的意思很重要，也可能并不重要，要不要将它加入输出中则是由输入门来进行控制。 - 遗忘门<span class="math inline">\(f_t\)</span>：<span class="math inline">\(f_t\)</span>控制上一时刻的细胞状态<span class="math inline">\(\boldsymbol{c_{t-1}}\)</span>融入当前细胞状态<span class="math inline">\(\boldsymbol{c_t}\)</span>。在理解一句话时，当前词<span class="math inline">\(\boldsymbol{x_t}\)</span>可能继续延续上文的意思进行叙述，也可能从当前词<span class="math inline">\(\boldsymbol{x_t}\)</span>开始叙述新的内容，与上文无关。 - 输出们<span class="math inline">\(o_t\)</span>：输出门的目的是从细胞状态<span class="math inline">\(\boldsymbol{c_t}\)</span>产生隐层单元<span class="math inline">\(\boldsymbol{h_t}\)</span>,细胞状态中的信息并非都与输出隐层状态有关，其中可能包含了很多对<span class="math inline">\(\boldsymbol{h_t}\)</span>无用的信息，通过输出门可以对细胞状态中的信息进行筛选。</p>
<p>总结而言，门机制的引入带来了两点好处： - 门机制极大地减轻了梯度消失问题，降低了调参难度 - 门机制引入了特征过滤，将有用的特征保存，没用的特征丢弃，这极大地丰富了我们向量的表示信息。 同样RNN的参数共享思想在LSTM中也得到了继承，但由于引入了三个门控组件，LSTM参数量相较于RNN增多，训练速度较RNN要慢一些。</p>
<h4 id="gru">GRU</h4>
<p>GRU的全称是 Gated Recurrent Unit(门控循环单位)，与LSTM相比，GRU仅仅只有两个门控组件，一个是重置门<span class="math inline">\(r_t\)</span>,一个是更新门<span class="math inline">\(z_t\)</span>。 <img src="https://raw.githubusercontent.com/xuejy19/xuejy19.github.io/source/Img/gru.png" alt="GRU" /></p>
<p>从上图可以看出，gru实际上就是LSTM的变形，取消了LSTM中的细胞状态(cell state)，只用隐藏状态(hidden state)，并使用更新门来代替LSTM中的输入门与遗忘门，取消了输出门，加入了重置门。实验证明，gru效果与LSTM差不多，但是参数更少，训练计算开销小，训练速度更快。</p>
]]></content>
      <categories>
        <category>统计学习</category>
      </categories>
      <tags>
        <tag>BP算法， 深度学习， CNN</tag>
      </tags>
  </entry>
  <entry>
    <title>生成对抗网络GAN</title>
    <url>/2020/09/09/sheng-cheng-dui-kang-wang-luo-gan/</url>
    <content><![CDATA[<p>这一部分介绍一种特殊的神经网路模型——生成对抗网络(GAN)，生成对抗网络由<a href="https://en.wikipedia.org/wiki/Ian_Goodfellow">Lan Goodfellow</a>于2014年提出，该算法在形式上表现为两个神经网络的彼此对抗，对于生成对抗网络，我们可以从以下几个角度来对其进行限定： - <strong>本质：</strong> 学习训练数据集的分布<br />
- <strong>作用：</strong> 产生新的样本，对于小样本任务可以起到数据增强的作用 - <strong>实现形式：</strong> 两个神经网络彼此对抗</p>
<p>本文按照以下结构进行组织: - GAN算法思想 - GAN背后数学推导<br />
- GAN<code>pytorch</code>实现</p>
<a id="more"></a>
<h3 id="gan-算法思想">GAN 算法思想</h3>
<p><img src="https://raw.githubusercontent.com/xuejy19/xuejy19.github.io/source/Img/GAN.jpg" alt="GAN" /> 考虑这样一个场景，一个小镇里有一个造假钞的人，同时有一个警察，它们两个各自的目标分别是： - <strong>罪犯：</strong> 不断提高自己的造假钞技术，使得自己的假钞足以以假乱真，让警察鉴别不出来 - <strong>警察：</strong> 不断提高自己的鉴别水平，能够准确地识别小偷制造的假钞</p>
<p>从博弈论的角度来看，这其实是一个零和博弈： &gt; 小偷的造假技术越高超，则警察鉴别起来就越困难;反之，警察的鉴别技术越高超，则小偷造出能欺骗过警察的假钞就更加困难，在对抗中，两个人都在变强。</p>
<p>接下来我们分析构想一个场景，我们有一组数据，但是数据的量不足，我们期望能够找到这样一个&quot;造假数据&quot;的人帮助我们产生一些新的图像，以达到数据增强的目的。但在这个&quot;造假数据&quot;的人造出一个假数据之后，我们需要判断一下这个数据到底合不合格，因此我们就需要另外一个鉴别&quot;假数据&quot;的&quot;警察&quot;。</p>
<p>其实我们的本质目的是为了<strong>造假数据</strong>,但这个任务若无监督是很难进行下去的，因此我们需要为其提供一个&quot;警察&quot;对其进行监督，因此我们就需要有两个学习器，一个学习如何生成&quot;假数据&quot;，另一个则需要学习如何判别出这些&quot;假数据&quot;，在这两个学习器博弈的过程中我们最终得到了一个&quot;造假数据&quot;比较高超学习器。</p>
<h3 id="gan-背后数学推导">GAN 背后数学推导</h3>
<blockquote>
<p><strong>沃兹基</strong>曾经说过：算法思想也就图一乐，真涨姿势还是要看数学推导</p>
</blockquote>
<h4 id="从最大似然估计谈起">从最大似然估计谈起</h4>
<p>因为我们本质上是希望能够得到一个学习器，使得其产生的数据的能够与训练集数据同分布，那么数学推导就从分布入手，假设训练集数据概率分布为<span class="math inline">\(p(x|\theta)\)</span>,其中<span class="math inline">\(\theta\)</span>为该概率分布所依赖的参数，当我们得到一组数据<span class="math inline">\(X = (x_1,x_2,\dots, x_N)\)</span>时，我们想要对参数进行估计，那么此时就要祭出已经老生常谈的<strong>最大似然估计</strong>，假设样本独立同分布，则写出似然函数如下: <span class="math display">\[
    L(X|\theta) = \prod_{i=1}^N p(x_i | \theta)
\]</span> 而<span class="math inline">\(\theta\)</span>的求解其实就是一个优化问题: <span class="math display">\[
    \begin{aligned}
        \theta^* &amp;= argmax L(X|\theta) \\
        &amp;= argmax \ln L(X|\theta) \\  
        &amp;= argmax \frac{1}{N} L(X|\theta) \\
        &amp;= argmax \frac{1}{N} \sum_{i=1}^N \ln p(x_i|\theta) \\
        &amp;= argmax E_{x \sim p(x)} \ln p(x|\theta)  \\
        &amp;= argmax \int_{x} p(x) \ln p(x|\theta) dx
    \end{aligned}
\]</span> 这里需要特别说明一下这一步： <span class="math display">\[
    argmax \frac{1}{N} \sum_{i=1}^N \ln p(x_i|\theta) \Leftrightarrow argmax E_{x \sim p(x)} \ln p(x|\theta)
\]</span> 当样本数量足够大时，空间中每个样本点都被覆盖，而具体空间中某一个点<span class="math inline">\(x_i\)</span>会落入多少样本点则取决于数据分布<span class="math inline">\(p(x)\)</span>在该点概率密度函数的大小，换句话说，如果从采样的角度，要对某点的概率密度函数进行估计，那么只需要原始数据进行采样，采样<span class="math inline">\(N\)</span>个点，若最终有<span class="math inline">\(n_{x_i}\)</span>个点落在了<span class="math inline">\(x_i\)</span>处，那么该点的概率密度函数就可以估计为: <span class="math display">\[
    p(x_i) = \frac{n_{x_i}}{N}
\]</span> 于是上面的等价性也是可以按照这种思想推导出来： <span class="math display">\[
    \begin{aligned}
        \lim_{N \rightarrow \infty} \frac{1}{N} \ln p(x_i|\theta) &amp;= \sum_{x} \frac{n_{x}}{N} \ln p(x_i|\theta)  \\
        &amp;= \int_x p(x) \ln p(x|\theta)  dx  \\
        &amp;= E_{x \sim p(x)} \ln p(x|\theta)  
    \end{aligned}
\]</span> 最大似然函数估计的目的是找到一组最合适的参数<span class="math inline">\(\theta\)</span>使得分布<span class="math inline">\(p(x|\theta)\)</span>更加符合数据分布，但是能够应用最大似然估计的场合一般是对数据分布有一个假设，这样才有参数可以优化，搜索空间限制在<span class="math inline">\(\mathcal{p(x|\theta)}\)</span>,现在我们将这个约束去掉，在所有可能分布中找到一个分布<span class="math inline">\(q(x)\)</span>,使得<span class="math inline">\(q(x)\)</span>能够尽可能接近原始数据分布<span class="math inline">\(p_{data}(x)\)</span>。 至此我们证明了： <span class="math display">\[
    \theta^* = argmax L(X|\theta)  \Leftrightarrow  argmax E_{x \sim p(x)} \ln q(x)
\]</span></p>
<h4 id="最大似然估计与kl散度">最大似然估计与KL散度</h4>
<p>紧接上文，我们来看下<span class="math inline">\(E_{x \sim p(x)} \ln q(x)\)</span>: <span class="math display">\[
    \begin{aligned}
    argmax_{q(x)}    E_{x \sim p(x)} \ln q(x) &amp;= argmax_{q(x)}\int_x p(x) \ln q(x) dx  \\ 
    &amp;= argmin_{q(x)} -\int_x p(x) \ln q(x) dx  \\
    &amp;= argmin_{q(x)} -\int_x p(x) \ln q(x) dx + \int_x p(x) \ln p(x) dx \\
    &amp;= argmin_{q(x)} D_{KL}(p(x)||q(x))
    \end{aligned}
\]</span> 这说明我们要找到一个分布<span class="math inline">\(q(x)\)</span>使得其对于原始分布<span class="math inline">\(p(x)\)</span>产生数据的极大似然函数最大就等价于找到一个分布<span class="math inline">\(q(x)\)</span>使得两个分布之间的KL散度最小(不知道啥是KL散度的，来<a href="https://xuejy19.github.io/2020/08/26/%E6%A6%82%E7%8E%87%E5%88%86%E5%B8%83%E4%B8%AD%E7%9A%84%E8%B7%9D%E7%A6%BB%E5%BA%A6%E9%87%8F/#more">这里</a>补课)。同时中间结果则是也等价于最小化交叉熵，所以我们又得到了一个有用的结论: &gt; 最大化似然函数 <span class="math inline">\(\Leftrightarrow\)</span> 最小化交叉熵 <span class="math inline">\(\Leftrightarrow\)</span> 最小化KL散度</p>
<p>然后为了解决KL散度的不对称性，又引入了JS散度： <span class="math display">\[
    JS(p(x)||q(x)) = \frac{1}{2} D_{KL}(p(x)||\frac{p(x)+ q(x)}{2}) + \frac{1}{2} D_{KL} (q(x) || \frac{p(x) + q(x)}{2})
\]</span> 本来想推导下优化KL散度等价于优化JS散度，但没推出来，只有一个不等关系: <span class="math display">\[
    JS(p||q) \leq \frac{1}{4} D_{KL}(p||q) + \frac{1}{4} D_{KL}(q||p)
\]</span> 本身KL散度的取值范围是<span class="math inline">\([0, +\infty]\)</span>,而JS散度的取值范围是<span class="math inline">\([0,1]\)</span>(<span class="math inline">\(\log\)</span>以2为底)，但是KL散度与JS散度取最小值的条件是一样的，即两个分布完全相同，因此使用JS散度或者KL散度进行优化，应当是同样的优化效果。 因此可以继续将结论补充： &gt; 最大化似然函数 <span class="math inline">\(\Leftrightarrow\)</span> 最小化交叉熵 <span class="math inline">\(\Leftrightarrow\)</span> 最小化KL散度<span class="math inline">\(\Leftrightarrow\)</span> 最小化JS散度</p>
<h4 id="生成对抗网络">生成对抗网络</h4>
<p>之前进行分布拟合，一般通过假设分布是一个高斯分布(或混合高斯分布)，然后对参数进行优化，使得对数据有一个较好的拟合效果，但高斯分布假设很多时候并不满足，因此需要一个更大的模型搜索空间。随着神经网络科学的发展，人们开始思考，能否使用神经网络来将一个高斯分布映射为我们期望的分布<span class="math inline">\(p_G\)</span>，但由于<span class="math inline">\(p_{data}\)</span>是未知的，我们并不知道应该如何衡量两个分布之间的差异，因此就需要有一个判别器。GAN的目标函数如下: <span class="math display">\[
    V(D, G) = E_{x \sim data} \log D(x) + E_{x \sim p_G} \log (1 - D(x))
\]</span> 前一项中的<span class="math inline">\(D(x)\)</span>表示判别器对来自训练集数据的评分，后一项中的<span class="math inline">\(D(x)\)</span>表示判别器对来自生成器数据的评分，如果想提高判别器的性能，则要求判别器对来自训练数据集的数据评分较高，对来自生成器生成数据评分较低，因为<span class="math inline">\(D(x) \in [0,1]\)</span>,因此<span class="math inline">\(D(x)\)</span>也可以认为是一个样本是来自原始分布的概率。下面我们将<span class="math inline">\(V(D,G)\)</span>写开: <span class="math display">\[
    \begin{aligned}
        V(D,G) &amp;=  E_{x \sim data} \log D(x) + E_{x \sim p_G} \log (1 - D(x)) \\ 
        &amp;= \int_x p_{data}(x) \log D(x) dx + \int_x p_G(x) \log(1-D(x)) dx \\
        &amp;= \int_x [p_{data}(x) \log D(x) + p_G(x) \log(1 - D(x))] dx  
    \end{aligned}
\]</span> 判别器希望最大化<span class="math inline">\(V(D,G)\)</span>,即对于来自于真实样本分布的样本的打分尽可能多，对来自于生成器的样本的打分尽可能低，想要优化上式，即低于任意一个输入<span class="math inline">\(x\)</span>，判别器应当: <span class="math display">\[
    \max_{D} v(D) = p_{data}(x) \log D(x) + p_G(x) \log(1 - D(x))
\]</span> 该函数是一个凹函数，可以直接通过求导等于0求得<span class="math inline">\(D(x)\)</span>最优解： <span class="math display">\[
    \frac{\partial v(D)}{\partial D(x)} = \frac{p_{data}(x)}{D(x)} - \frac{p_G(x)}{1 - D(x)} = 0
\]</span> 可以求得： <span class="math display">\[
    D^*(x) = \frac{p_{data}(x)}{p_{data}(x) + p_{G}(x)}
\]</span> 将求得的<span class="math inline">\(D(x)\)</span>回代<span class="math inline">\(V(D,G)\)</span>: <span class="math display">\[
    \begin{aligned}
         V(D^*,G) &amp;= \int_x p_{data}(x) \log \frac{p_{data}(x)}{p_{data}(x) + p_{G}(x)} dx + \int_x p_G(x) \frac{p_{G}(x)}{p_{data}(x) + p_{G}(x)}  dx \\
         &amp;= -2\log2 + 2JS(p_{data}(x) || p_G(x)) 
    \end{aligned}
\]</span> 所以说我们接下来对生成器G的优化，其实就是: <span class="math display">\[
    G^* = \arg \min_G V(D^*,G) = \arg \min_{G} JS(p_{data}(x)||p_G(x))
\]</span> 这说明我们对生成器进行优化，实质上就是期望能够找到一个分布，使得<span class="math inline">\(p_G(x)\)</span>与原始数据分布<span class="math inline">\(p_{data}(x)\)</span>的JS散度最小，由前面的等价性，其实就是找到一个分布使得对训练数据的最大似然函数最大。 因此整个优化问题可以写做: <span class="math display">\[
    \min_{G} \max_{D} V(D,G)
\]</span></p>
<h4 id="gan-pytorch实现">GAN <code>Pytorch</code>实现</h4>
<p>在实现的时候需要关注的问题我觉得主要有以下几个： - 损失函数选择 - 网络结构设计 - 优化顺序 #### 损失函数选择 首先讨论下损失函数选择，对于判别器而言，其面临的其实就是一个二分类问题，我们看下<span class="math inline">\(V(D,G)\)</span>: <span class="math display">\[
    \begin{aligned}
        V(D,G) &amp;= E_{x \sim data} \log D(x) + E_{x \sim p_G} \log (1 - D(x)) 
    \end{aligned}
\]</span> 从采样的角度来看，<span class="math inline">\(V(D,G)\)</span>可以写做: <span class="math display">\[
    \begin{aligned}
        V(D,G) = \sum_{i=1, x \sim p_{data}(x)}^{batch\_size} \log D(x) + 
        \sum_{i=1, x \sim p_{G}(x)}^{batch\_size} \log (1 - D(x))
    \end{aligned}
\]</span> 若设置来自于真实数据集的数据的label为1，来自于生成器的数据的label为0，则上式其实可以看作两个二分类交叉熵的和，二分类交叉熵计算公式为： <span class="math display">\[
    BCE(x_n,y_n) = y_n \log x_n + (1-y_n) \log (1 - x_n) 
\]</span> 对于label为1的样本，保留前一项；而对于label为0的样本，保留后一项，刚好就是<span class="math inline">\(V(D,G)\)</span>。当然也可以通过直接就两个类别做one-hot，然后使用普通的交叉熵损失函数也可以。</p>
<p>在对生成器进行优化的时候，因为我们期望造出的假样本能够尽可能真，因此只需要将假样本的label设置为1，然后进行反向传播优化生成器即可。 #### 网络结构设计 生成对抗网络是出了名的难训练，因此合理的网络结构非常重要，对于新入门的人来说就只能参考一些现成的网络结构,同时对于不同的任务也应当选用不同的网络结构。目前生成对抗网络在图像处理领域较多，因此往往采用卷积神经网络。 <img src="https://raw.githubusercontent.com/xuejy19/xuejy19.github.io/source/Img/generative-adversarial-network.png" alt="GAN" /></p>
<h4 id="优化顺序">优化顺序</h4>
<p>生成对抗网络的优化是一个交替优化的过程，一般是先对判别器进行优化，接下来再对生成器进行优化，两者进行交替优化，互相博弈，下面简单地写下计算图： &gt; <strong>判别器：</strong> &gt; 真实样本、虚假样本(生成器生成) <span class="math inline">\(\rightarrow\)</span> 判别器网络D <span class="math inline">\(\rightarrow\)</span> predict <span class="math inline">\(\rightarrow\)</span> Loss_D &gt; <strong>生成器：</strong> &gt; 虚假样本 <span class="math inline">\(\rightarrow\)</span> 生成器网络 <span class="math inline">\(\rightarrow\)</span> 虚假样本 <span class="math inline">\(\rightarrow\)</span> 判别器 <span class="math inline">\(\rightarrow\)</span> predict <span class="math inline">\(\rightarrow\)</span> Loss_G</p>
<h4 id="pytorch-代码实现"><code>pytorch</code> 代码实现</h4>
<p>数据集采用的是<a href="http://mmlab.ie.cuhk.edu.hk/projects/CelebA.html">celeb-A faces Dataset</a>,代码实现如下:</p>
<div class="sourceCode" id="cb1"><pre class="sourceCode python"><code class="sourceCode python"><a class="sourceLine" id="cb1-1" data-line-number="1"><span class="im">import</span> argparse</a>
<a class="sourceLine" id="cb1-2" data-line-number="2"><span class="im">import</span> os</a>
<a class="sourceLine" id="cb1-3" data-line-number="3"><span class="im">import</span> random</a>
<a class="sourceLine" id="cb1-4" data-line-number="4"><span class="im">import</span> numpy <span class="im">as</span> np</a>
<a class="sourceLine" id="cb1-5" data-line-number="5"><span class="im">import</span> matplotlib.pyplot <span class="im">as</span> plt</a>
<a class="sourceLine" id="cb1-6" data-line-number="6"><span class="im">import</span> matplotlib.animation</a>
<a class="sourceLine" id="cb1-7" data-line-number="7"><span class="im">from</span> tqdm <span class="im">import</span> tqdm   </a>
<a class="sourceLine" id="cb1-8" data-line-number="8"><span class="im">from</span> IPython.display <span class="im">import</span> HTML</a>
<a class="sourceLine" id="cb1-9" data-line-number="9"></a>
<a class="sourceLine" id="cb1-10" data-line-number="10"><span class="im">import</span> torch </a>
<a class="sourceLine" id="cb1-11" data-line-number="11"><span class="im">import</span> torch.nn <span class="im">as</span> nn</a>
<a class="sourceLine" id="cb1-12" data-line-number="12"><span class="im">import</span> torch.nn.parallel</a>
<a class="sourceLine" id="cb1-13" data-line-number="13"><span class="im">import</span> torch.backends.cudnn <span class="im">as</span> cudnn</a>
<a class="sourceLine" id="cb1-14" data-line-number="14"><span class="im">import</span> torch.optim <span class="im">as</span> optim</a>
<a class="sourceLine" id="cb1-15" data-line-number="15"><span class="im">import</span> torch.utils.data </a>
<a class="sourceLine" id="cb1-16" data-line-number="16"><span class="im">import</span> torchvision.datasets <span class="im">as</span> datasets</a>
<a class="sourceLine" id="cb1-17" data-line-number="17"><span class="im">import</span> torchvision.transforms <span class="im">as</span> transforms</a>
<a class="sourceLine" id="cb1-18" data-line-number="18"><span class="im">import</span> torchvision.utils <span class="im">as</span> vision_utils</a>
<a class="sourceLine" id="cb1-19" data-line-number="19"><span class="kw">def</span> matplotlib_imshow(img , one_channel <span class="op">=</span> <span class="va">False</span>):</a>
<a class="sourceLine" id="cb1-20" data-line-number="20">    <span class="cf">if</span> one_channel:</a>
<a class="sourceLine" id="cb1-21" data-line-number="21">        img <span class="op">=</span> img.mean(dim <span class="op">=</span> <span class="dv">0</span>)</a>
<a class="sourceLine" id="cb1-22" data-line-number="22">    img <span class="op">=</span> img<span class="op">/</span><span class="dv">2</span> <span class="op">+</span> <span class="fl">0.5</span></a>
<a class="sourceLine" id="cb1-23" data-line-number="23">    npimg <span class="op">=</span> img.numpy()</a>
<a class="sourceLine" id="cb1-24" data-line-number="24">    <span class="cf">if</span> one_channel:</a>
<a class="sourceLine" id="cb1-25" data-line-number="25">        plt.imshow(img, cmap <span class="op">=</span> <span class="st">&#39;Greys&#39;</span>) </a>
<a class="sourceLine" id="cb1-26" data-line-number="26">    <span class="cf">else</span>:</a>
<a class="sourceLine" id="cb1-27" data-line-number="27">        npimg  <span class="op">=</span> np.transpose(npimg, (<span class="dv">1</span>,<span class="dv">2</span>,<span class="dv">0</span>)) </a>
<a class="sourceLine" id="cb1-28" data-line-number="28">        plt.imshow(npimg)</a>
<a class="sourceLine" id="cb1-29" data-line-number="29">    plt.show()</a>
<a class="sourceLine" id="cb1-30" data-line-number="30"><span class="kw">def</span> load_data(dataroot, image_size, batch_size, num_workers):</a>
<a class="sourceLine" id="cb1-31" data-line-number="31">    transform <span class="op">=</span> transforms.Compose([</a>
<a class="sourceLine" id="cb1-32" data-line-number="32">                   transforms.Resize(image_size) ,</a>
<a class="sourceLine" id="cb1-33" data-line-number="33">                   transforms.CenterCrop(image_size),</a>
<a class="sourceLine" id="cb1-34" data-line-number="34">                   transforms.ToTensor(),</a>
<a class="sourceLine" id="cb1-35" data-line-number="35">                   transforms.Normalize((<span class="fl">0.5</span>,<span class="fl">0.5</span>,<span class="fl">0.5</span>),(<span class="fl">0.5</span>,<span class="fl">0.5</span>,<span class="fl">0.5</span>))])</a>
<a class="sourceLine" id="cb1-36" data-line-number="36">    dataset <span class="op">=</span> datasets.ImageFolder(root <span class="op">=</span> dataroot, transform <span class="op">=</span> transform) </a>
<a class="sourceLine" id="cb1-37" data-line-number="37">    <span class="co"># Create the dataloader</span></a>
<a class="sourceLine" id="cb1-38" data-line-number="38">    dataloader <span class="op">=</span> torch.utils.data.DataLoader(dataset, batch_size <span class="op">=</span> batch_size, </a>
<a class="sourceLine" id="cb1-39" data-line-number="39">                                        shuffle <span class="op">=</span> <span class="va">True</span>, num_workers <span class="op">=</span> num_workers)</a>
<a class="sourceLine" id="cb1-40" data-line-number="40">    <span class="co"># plot some training images </span></a>
<a class="sourceLine" id="cb1-41" data-line-number="41">    <span class="co"># real_batch, _ = iter(dataloader).next()</span></a>
<a class="sourceLine" id="cb1-42" data-line-number="42">    </a>
<a class="sourceLine" id="cb1-43" data-line-number="43">    <span class="co">#batch_image = vision_utils.make_grid(real_batch, padding = 2) </span></a>
<a class="sourceLine" id="cb1-44" data-line-number="44">    <span class="co">#matplotlib_imshow(batch_image)</span></a>
<a class="sourceLine" id="cb1-45" data-line-number="45">    <span class="cf">return</span> dataloader</a>
<a class="sourceLine" id="cb1-46" data-line-number="46"><span class="kw">def</span> weights_init(model):</a>
<a class="sourceLine" id="cb1-47" data-line-number="47">    classname <span class="op">=</span> model.__class__.<span class="va">__name__</span></a>
<a class="sourceLine" id="cb1-48" data-line-number="48">    <span class="cf">if</span> classname.find(<span class="st">&#39;Conv&#39;</span>) <span class="op">!=</span> <span class="dv">-1</span>:</a>
<a class="sourceLine" id="cb1-49" data-line-number="49">        nn.init.normal_(model.weight.data, <span class="fl">0.0</span>, <span class="fl">0.02</span>) </a>
<a class="sourceLine" id="cb1-50" data-line-number="50">    <span class="cf">elif</span> classname.find(<span class="st">&#39;BatchNorm&#39;</span>) <span class="op">!=</span> <span class="dv">-1</span>:</a>
<a class="sourceLine" id="cb1-51" data-line-number="51">        nn.init.normal_(model.weight.data, <span class="fl">1.0</span>, <span class="fl">0.02</span>)</a>
<a class="sourceLine" id="cb1-52" data-line-number="52">        nn.init.constant_(model.bias.data, <span class="dv">0</span>)</a>
<a class="sourceLine" id="cb1-53" data-line-number="53"></a>
<a class="sourceLine" id="cb1-54" data-line-number="54"><span class="kw">class</span> Generator(nn.Module):</a>
<a class="sourceLine" id="cb1-55" data-line-number="55">    <span class="kw">def</span> <span class="fu">__init__</span>(<span class="va">self</span>, nz, ngf, nc):</a>
<a class="sourceLine" id="cb1-56" data-line-number="56">        <span class="bu">super</span>(Generator, <span class="va">self</span>).<span class="fu">__init__</span>() </a>
<a class="sourceLine" id="cb1-57" data-line-number="57">        <span class="co"># nz = 100 H_out = (H_in - 1)*stride - 2*padding + kernel_size</span></a>
<a class="sourceLine" id="cb1-58" data-line-number="58">        <span class="va">self</span>.conv1 <span class="op">=</span> nn.Sequential(</a>
<a class="sourceLine" id="cb1-59" data-line-number="59">            nn.ConvTranspose2d(nz, ngf<span class="op">*</span><span class="dv">8</span>, <span class="dv">4</span>, <span class="dv">1</span>, <span class="dv">0</span>, bias <span class="op">=</span> <span class="va">False</span>),</a>
<a class="sourceLine" id="cb1-60" data-line-number="60">            nn.BatchNorm2d(ngf<span class="op">*</span><span class="dv">8</span>),</a>
<a class="sourceLine" id="cb1-61" data-line-number="61">            nn.ReLU())  </a>
<a class="sourceLine" id="cb1-62" data-line-number="62">        <span class="co"># state size: (ngf*8)*4*4</span></a>
<a class="sourceLine" id="cb1-63" data-line-number="63">        <span class="va">self</span>.conv2 <span class="op">=</span> nn.Sequential(</a>
<a class="sourceLine" id="cb1-64" data-line-number="64">            nn.ConvTranspose2d(ngf<span class="op">*</span><span class="dv">8</span>, ngf<span class="op">*</span><span class="dv">4</span>, <span class="dv">4</span>, <span class="dv">2</span>, <span class="dv">1</span>, bias <span class="op">=</span> <span class="va">False</span>),</a>
<a class="sourceLine" id="cb1-65" data-line-number="65">            nn.BatchNorm2d(ngf<span class="op">*</span><span class="dv">4</span>),</a>
<a class="sourceLine" id="cb1-66" data-line-number="66">            nn.ReLU())</a>
<a class="sourceLine" id="cb1-67" data-line-number="67">        <span class="co"># state size: (ngf*4)*8*8 </span></a>
<a class="sourceLine" id="cb1-68" data-line-number="68">        <span class="va">self</span>.conv3 <span class="op">=</span> nn.Sequential(</a>
<a class="sourceLine" id="cb1-69" data-line-number="69">            nn.ConvTranspose2d(ngf<span class="op">*</span><span class="dv">4</span>, ngf<span class="op">*</span><span class="dv">2</span>, <span class="dv">4</span>, <span class="dv">2</span>, <span class="dv">1</span>, bias <span class="op">=</span> <span class="va">False</span>),</a>
<a class="sourceLine" id="cb1-70" data-line-number="70">            nn.BatchNorm2d(ngf<span class="op">*</span><span class="dv">2</span>),</a>
<a class="sourceLine" id="cb1-71" data-line-number="71">            nn.ReLU())</a>
<a class="sourceLine" id="cb1-72" data-line-number="72">        <span class="co"># state size: (ngf*2)*16*16 </span></a>
<a class="sourceLine" id="cb1-73" data-line-number="73">        <span class="va">self</span>.conv4 <span class="op">=</span> nn.Sequential(</a>
<a class="sourceLine" id="cb1-74" data-line-number="74">            nn.ConvTranspose2d(ngf<span class="op">*</span><span class="dv">2</span>, ngf, <span class="dv">4</span>, <span class="dv">2</span>, <span class="dv">1</span>, bias <span class="op">=</span> <span class="va">False</span>),</a>
<a class="sourceLine" id="cb1-75" data-line-number="75">            nn.BatchNorm2d(ngf),</a>
<a class="sourceLine" id="cb1-76" data-line-number="76">            nn.ReLU()) </a>
<a class="sourceLine" id="cb1-77" data-line-number="77">        <span class="co"># state size: ngf*32*32 </span></a>
<a class="sourceLine" id="cb1-78" data-line-number="78">        <span class="va">self</span>.conv5 <span class="op">=</span> nn.Sequential(</a>
<a class="sourceLine" id="cb1-79" data-line-number="79">            nn.ConvTranspose2d(ngf, nc, <span class="dv">4</span>, <span class="dv">2</span>, <span class="dv">1</span>, bias <span class="op">=</span> <span class="va">False</span>),</a>
<a class="sourceLine" id="cb1-80" data-line-number="80">            nn.Tanh())</a>
<a class="sourceLine" id="cb1-81" data-line-number="81">    <span class="kw">def</span> forward(<span class="va">self</span>, x):</a>
<a class="sourceLine" id="cb1-82" data-line-number="82">        x <span class="op">=</span> <span class="va">self</span>.conv1(x)</a>
<a class="sourceLine" id="cb1-83" data-line-number="83">        x <span class="op">=</span> <span class="va">self</span>.conv2(x)</a>
<a class="sourceLine" id="cb1-84" data-line-number="84">        x <span class="op">=</span> <span class="va">self</span>.conv3(x)</a>
<a class="sourceLine" id="cb1-85" data-line-number="85">        x <span class="op">=</span> <span class="va">self</span>.conv4(x)</a>
<a class="sourceLine" id="cb1-86" data-line-number="86">        <span class="cf">return</span> <span class="va">self</span>.conv5(x)</a>
<a class="sourceLine" id="cb1-87" data-line-number="87"><span class="kw">class</span> Discriminator(nn.Module):</a>
<a class="sourceLine" id="cb1-88" data-line-number="88">    <span class="kw">def</span> <span class="fu">__init__</span>(<span class="va">self</span>, nc, ndf):</a>
<a class="sourceLine" id="cb1-89" data-line-number="89">        <span class="bu">super</span>(Discriminator,<span class="va">self</span>).<span class="fu">__init__</span>()</a>
<a class="sourceLine" id="cb1-90" data-line-number="90">        <span class="co"># input size: nc*64*64 </span></a>
<a class="sourceLine" id="cb1-91" data-line-number="91">        <span class="va">self</span>.conv1 <span class="op">=</span> nn.Sequential(</a>
<a class="sourceLine" id="cb1-92" data-line-number="92">            nn.Conv2d(nc, ndf, <span class="dv">4</span>, <span class="dv">2</span>, <span class="dv">1</span>, bias <span class="op">=</span> <span class="va">False</span>), </a>
<a class="sourceLine" id="cb1-93" data-line-number="93">            nn.LeakyReLU(<span class="fl">0.2</span>)) </a>
<a class="sourceLine" id="cb1-94" data-line-number="94">        <span class="co"># state size: ndf*32*32 </span></a>
<a class="sourceLine" id="cb1-95" data-line-number="95">        <span class="va">self</span>.conv2 <span class="op">=</span> nn.Sequential(</a>
<a class="sourceLine" id="cb1-96" data-line-number="96">            nn.Conv2d(ndf, <span class="dv">2</span><span class="op">*</span>ndf, <span class="dv">4</span>, <span class="dv">2</span>, <span class="dv">1</span>, bias <span class="op">=</span> <span class="va">False</span>),</a>
<a class="sourceLine" id="cb1-97" data-line-number="97">            nn.BatchNorm2d(<span class="dv">2</span><span class="op">*</span>ndf),</a>
<a class="sourceLine" id="cb1-98" data-line-number="98">            nn.LeakyReLU(<span class="fl">0.2</span>))  </a>
<a class="sourceLine" id="cb1-99" data-line-number="99">        <span class="co"># state size: (ndf*2)*16*16 </span></a>
<a class="sourceLine" id="cb1-100" data-line-number="100">        <span class="va">self</span>.conv3 <span class="op">=</span> nn.Sequential(</a>
<a class="sourceLine" id="cb1-101" data-line-number="101">            nn.Conv2d(ndf<span class="op">*</span><span class="dv">2</span>, ndf<span class="op">*</span><span class="dv">4</span>, <span class="dv">4</span>, <span class="dv">2</span>, <span class="dv">1</span>, bias <span class="op">=</span> <span class="va">False</span>),</a>
<a class="sourceLine" id="cb1-102" data-line-number="102">            nn.BatchNorm2d(ndf<span class="op">*</span><span class="dv">4</span>),</a>
<a class="sourceLine" id="cb1-103" data-line-number="103">            nn.LeakyReLU(<span class="fl">0.2</span>)) </a>
<a class="sourceLine" id="cb1-104" data-line-number="104">        <span class="co"># state size: (ndf*4)*8*8 </span></a>
<a class="sourceLine" id="cb1-105" data-line-number="105">        <span class="va">self</span>.conv4 <span class="op">=</span> nn.Sequential(</a>
<a class="sourceLine" id="cb1-106" data-line-number="106">            nn.Conv2d(ndf<span class="op">*</span><span class="dv">4</span>, ndf<span class="op">*</span><span class="dv">8</span>, <span class="dv">4</span>, <span class="dv">2</span>, <span class="dv">1</span>, bias <span class="op">=</span> <span class="va">False</span>),</a>
<a class="sourceLine" id="cb1-107" data-line-number="107">            nn.BatchNorm2d(ndf<span class="op">*</span><span class="dv">8</span>),</a>
<a class="sourceLine" id="cb1-108" data-line-number="108">            nn.LeakyReLU(<span class="fl">0.2</span>)) </a>
<a class="sourceLine" id="cb1-109" data-line-number="109">        <span class="co"># state size: (ndf*8)*4*4</span></a>
<a class="sourceLine" id="cb1-110" data-line-number="110">        <span class="va">self</span>.conv5 <span class="op">=</span> nn.Sequential(</a>
<a class="sourceLine" id="cb1-111" data-line-number="111">            nn.Conv2d(ndf<span class="op">*</span><span class="dv">8</span>,<span class="dv">1</span>, <span class="dv">4</span>,<span class="dv">1</span>,<span class="dv">0</span>,bias <span class="op">=</span> <span class="va">False</span>),</a>
<a class="sourceLine" id="cb1-112" data-line-number="112">            nn.Sigmoid()) </a>
<a class="sourceLine" id="cb1-113" data-line-number="113">        <span class="co"># state size: 1*1 [0,1] </span></a>
<a class="sourceLine" id="cb1-114" data-line-number="114">    <span class="kw">def</span> forward(<span class="va">self</span>, x):</a>
<a class="sourceLine" id="cb1-115" data-line-number="115">        x <span class="op">=</span> <span class="va">self</span>.conv1(x) </a>
<a class="sourceLine" id="cb1-116" data-line-number="116">        x <span class="op">=</span> <span class="va">self</span>.conv2(x) </a>
<a class="sourceLine" id="cb1-117" data-line-number="117">        x <span class="op">=</span> <span class="va">self</span>.conv3(x)</a>
<a class="sourceLine" id="cb1-118" data-line-number="118">        x <span class="op">=</span> <span class="va">self</span>.conv4(x)</a>
<a class="sourceLine" id="cb1-119" data-line-number="119">        <span class="cf">return</span> <span class="va">self</span>.conv5(x)</a>
<a class="sourceLine" id="cb1-120" data-line-number="120"><span class="kw">def</span> get_model():</a>
<a class="sourceLine" id="cb1-121" data-line-number="121">    device <span class="op">=</span> args.device</a>
<a class="sourceLine" id="cb1-122" data-line-number="122">    netG <span class="op">=</span> Generator(args.nz, args.ngf, args.channels).to(device)</a>
<a class="sourceLine" id="cb1-123" data-line-number="123">    netG.<span class="bu">apply</span>(weights_init)</a>
<a class="sourceLine" id="cb1-124" data-line-number="124">    netD <span class="op">=</span> Discriminator(args.channels, args.ndf).to(device) </a>
<a class="sourceLine" id="cb1-125" data-line-number="125">    netD.<span class="bu">apply</span>(weights_init)</a>
<a class="sourceLine" id="cb1-126" data-line-number="126">    optimizerD <span class="op">=</span> optim.Adam(netD.parameters(), lr<span class="op">=</span>args.lr, betas <span class="op">=</span> (args.beta1,<span class="fl">0.999</span>))</a>
<a class="sourceLine" id="cb1-127" data-line-number="127">    optimizerG <span class="op">=</span> optim.Adam(netG.parameters(), lr<span class="op">=</span>args.lr, betas <span class="op">=</span> (args.beta1,<span class="fl">0.999</span>))</a>
<a class="sourceLine" id="cb1-128" data-line-number="128">    criterion <span class="op">=</span> nn.BCELoss() </a>
<a class="sourceLine" id="cb1-129" data-line-number="129">    <span class="cf">return</span> netG, netD, optimizerD, optimizerG, criterion</a>
<a class="sourceLine" id="cb1-130" data-line-number="130"><span class="kw">def</span> plot_figure(G_loss, D_loss, Dx, D_z1, D_z2, img_list):</a>
<a class="sourceLine" id="cb1-131" data-line-number="131">    plt.figure(figsize <span class="op">=</span> (<span class="dv">10</span>,<span class="dv">5</span>)) </a>
<a class="sourceLine" id="cb1-132" data-line-number="132">    plt.title(<span class="st">&#39;Generator and Discriminator Loss During Training&#39;</span>) </a>
<a class="sourceLine" id="cb1-133" data-line-number="133">    plt.plot(G_loss, label <span class="op">=</span> <span class="st">&#39;G&#39;</span>) </a>
<a class="sourceLine" id="cb1-134" data-line-number="134">    plt.plot(D_loss, label <span class="op">=</span> <span class="st">&#39;D&#39;</span>) </a>
<a class="sourceLine" id="cb1-135" data-line-number="135">    plt.xlabel(<span class="st">&#39;iterations&#39;</span>) </a>
<a class="sourceLine" id="cb1-136" data-line-number="136">    plt.ylabel(<span class="st">&#39;Loss&#39;</span>) </a>
<a class="sourceLine" id="cb1-137" data-line-number="137">    plt.legend()</a>
<a class="sourceLine" id="cb1-138" data-line-number="138">    plt.savefig(<span class="st">&#39;curve_folder/Loss.png&#39;</span>)</a>
<a class="sourceLine" id="cb1-139" data-line-number="139">    plt.figure(figsize <span class="op">=</span> (<span class="dv">10</span>, <span class="dv">5</span>)) </a>
<a class="sourceLine" id="cb1-140" data-line-number="140">    plt.title(<span class="st">&#39;Dx and Dz&#39;</span>) </a>
<a class="sourceLine" id="cb1-141" data-line-number="141">    plt.plot(Dx, label <span class="op">=</span> <span class="st">&#39;Dx&#39;</span>) </a>
<a class="sourceLine" id="cb1-142" data-line-number="142">    plt.plot(D_z1, label <span class="op">=</span> <span class="st">&#39;Dz1&#39;</span>)</a>
<a class="sourceLine" id="cb1-143" data-line-number="143">    plt.plot(D_z2, label <span class="op">=</span> <span class="st">&#39;Dz2&#39;</span>) </a>
<a class="sourceLine" id="cb1-144" data-line-number="144">    plt.xlabel(<span class="st">&#39;iterations&#39;</span>)</a>
<a class="sourceLine" id="cb1-145" data-line-number="145">    plt.ylabel(<span class="st">&#39;P&#39;</span>) </a>
<a class="sourceLine" id="cb1-146" data-line-number="146">    plt.legend() </a>
<a class="sourceLine" id="cb1-147" data-line-number="147">    plt.savefig(<span class="st">&#39;curve_folder/D.png&#39;</span>) </a>
<a class="sourceLine" id="cb1-148" data-line-number="148">    <span class="cf">for</span> i <span class="kw">in</span> <span class="bu">range</span>(<span class="bu">len</span>(img_list)):</a>
<a class="sourceLine" id="cb1-149" data-line-number="149">        fig <span class="op">=</span> plt.figure(figsize <span class="op">=</span> (<span class="dv">8</span>,<span class="dv">8</span>)) </a>
<a class="sourceLine" id="cb1-150" data-line-number="150">        plt.axis(<span class="st">&#39;off&#39;</span>) </a>
<a class="sourceLine" id="cb1-151" data-line-number="151">        plt.imshow(np.transpose(img_list[i], (<span class="dv">1</span>,<span class="dv">2</span>,<span class="dv">0</span>))) </a>
<a class="sourceLine" id="cb1-152" data-line-number="152">        plt.savefig(<span class="st">&#39;fake_image/fake_img&#39;</span> <span class="op">+</span> <span class="bu">str</span>(i) <span class="op">+</span> <span class="st">&#39;.png&#39;</span>)</a>
<a class="sourceLine" id="cb1-153" data-line-number="153"><span class="kw">def</span> fit(epoches, dataloader, device, netG, netD, optimizerG, optimizerD):</a>
<a class="sourceLine" id="cb1-154" data-line-number="154">    <span class="co"># Lists to keep track of progress </span></a>
<a class="sourceLine" id="cb1-155" data-line-number="155">    img_list <span class="op">=</span> [] </a>
<a class="sourceLine" id="cb1-156" data-line-number="156">    G_losses <span class="op">=</span> [] </a>
<a class="sourceLine" id="cb1-157" data-line-number="157">    D_losses <span class="op">=</span> []</a>
<a class="sourceLine" id="cb1-158" data-line-number="158">    Dx_list <span class="op">=</span> [] </a>
<a class="sourceLine" id="cb1-159" data-line-number="159">    Dz1_list <span class="op">=</span> []</a>
<a class="sourceLine" id="cb1-160" data-line-number="160">    Dz2_list <span class="op">=</span> []</a>
<a class="sourceLine" id="cb1-161" data-line-number="161">    iters <span class="op">=</span> <span class="dv">0</span></a>
<a class="sourceLine" id="cb1-162" data-line-number="162">    real_label <span class="op">=</span> <span class="fl">1.0</span></a>
<a class="sourceLine" id="cb1-163" data-line-number="163">    fake_label <span class="op">=</span> <span class="fl">0.0</span></a>
<a class="sourceLine" id="cb1-164" data-line-number="164">    fixed_noise <span class="op">=</span> torch.randn(<span class="dv">64</span>, args.nz, <span class="dv">1</span>, <span class="dv">1</span>, device <span class="op">=</span> device)</a>
<a class="sourceLine" id="cb1-165" data-line-number="165">    <span class="bu">print</span>(<span class="st">&#39;Starting Training Loop...&#39;</span>) </a>
<a class="sourceLine" id="cb1-166" data-line-number="166"></a>
<a class="sourceLine" id="cb1-167" data-line-number="167">    <span class="cf">for</span> epoch <span class="kw">in</span> <span class="bu">range</span>(epoches):</a>
<a class="sourceLine" id="cb1-168" data-line-number="168">        <span class="cf">for</span> i, data <span class="kw">in</span> <span class="bu">enumerate</span>(dataloader, <span class="dv">0</span>):</a>
<a class="sourceLine" id="cb1-169" data-line-number="169">            <span class="co"># Update D network: maximize log(D(x)) + log(1-D(G(z))) </span></a>
<a class="sourceLine" id="cb1-170" data-line-number="170">            <span class="co">## Train with all real batch</span></a>
<a class="sourceLine" id="cb1-171" data-line-number="171">            netD.zero_grad() </a>
<a class="sourceLine" id="cb1-172" data-line-number="172">            image_batch <span class="op">=</span> data[<span class="dv">0</span>].to(device) </a>
<a class="sourceLine" id="cb1-173" data-line-number="173">            batch_size <span class="op">=</span> image_batch.shape[<span class="dv">0</span>] </a>
<a class="sourceLine" id="cb1-174" data-line-number="174">            label <span class="op">=</span> torch.full((batch_size,), real_label, dtype <span class="op">=</span> torch.<span class="bu">float</span>,device <span class="op">=</span> device) </a>
<a class="sourceLine" id="cb1-175" data-line-number="175">            <span class="co">#Forward pass real batch through D </span></a>
<a class="sourceLine" id="cb1-176" data-line-number="176">            output <span class="op">=</span> netD(image_batch).view(<span class="op">-</span><span class="dv">1</span>) </a>
<a class="sourceLine" id="cb1-177" data-line-number="177">            errD_real <span class="op">=</span> criterion(output, label)</a>
<a class="sourceLine" id="cb1-178" data-line-number="178">            <span class="co">#Calculate gradients for D in backward pass </span></a>
<a class="sourceLine" id="cb1-179" data-line-number="179">            errD_real.backward() </a>
<a class="sourceLine" id="cb1-180" data-line-number="180">            D_x <span class="op">=</span> output.mean().item() </a>
<a class="sourceLine" id="cb1-181" data-line-number="181"></a>
<a class="sourceLine" id="cb1-182" data-line-number="182">            <span class="co">## Train with all-fake batch </span></a>
<a class="sourceLine" id="cb1-183" data-line-number="183">            <span class="co"># Generate batch of latent vectors  </span></a>
<a class="sourceLine" id="cb1-184" data-line-number="184">            noise <span class="op">=</span> torch.randn(batch_size,args.nz, <span class="dv">1</span>, <span class="dv">1</span>, device <span class="op">=</span> device)  </a>
<a class="sourceLine" id="cb1-185" data-line-number="185">            <span class="co"># Generate fake image batch with G </span></a>
<a class="sourceLine" id="cb1-186" data-line-number="186">            fake <span class="op">=</span> netG(noise) </a>
<a class="sourceLine" id="cb1-187" data-line-number="187">            label.fill_(fake_label)</a>
<a class="sourceLine" id="cb1-188" data-line-number="188">            <span class="co"># Classify all fake batch with D </span></a>
<a class="sourceLine" id="cb1-189" data-line-number="189">            <span class="co"># use detach() to make sure only change netD params </span></a>
<a class="sourceLine" id="cb1-190" data-line-number="190">            output <span class="op">=</span> netD(fake.detach()).view(<span class="op">-</span><span class="dv">1</span>)  </a>
<a class="sourceLine" id="cb1-191" data-line-number="191">            errD_fake <span class="op">=</span> criterion(output, label) </a>
<a class="sourceLine" id="cb1-192" data-line-number="192">            errD_fake.backward() </a>
<a class="sourceLine" id="cb1-193" data-line-number="193">            D_G_z1 <span class="op">=</span> output.mean().item() </a>
<a class="sourceLine" id="cb1-194" data-line-number="194">            errD <span class="op">=</span> errD_real <span class="op">+</span> errD_fake</a>
<a class="sourceLine" id="cb1-195" data-line-number="195">            <span class="co"># update D </span></a>
<a class="sourceLine" id="cb1-196" data-line-number="196">            optimizerD.step() </a>
<a class="sourceLine" id="cb1-197" data-line-number="197"></a>
<a class="sourceLine" id="cb1-198" data-line-number="198">            <span class="co">#############################</span></a>
<a class="sourceLine" id="cb1-199" data-line-number="199">            <span class="co">#(2) Update G network: maximize log(D(G(z))) </span></a>
<a class="sourceLine" id="cb1-200" data-line-number="200">            <span class="co">############################# </span></a>
<a class="sourceLine" id="cb1-201" data-line-number="201">            netG.zero_grad() </a>
<a class="sourceLine" id="cb1-202" data-line-number="202">            label.fill_(real_label) <span class="co"># fake labels are real for generator cost</span></a>
<a class="sourceLine" id="cb1-203" data-line-number="203">            <span class="co"># Since we just updated D, perform another forward pass of all fake</span></a>
<a class="sourceLine" id="cb1-204" data-line-number="204">            <span class="co"># batch through D</span></a>
<a class="sourceLine" id="cb1-205" data-line-number="205">            output <span class="op">=</span> netD(fake).view(<span class="op">-</span><span class="dv">1</span>) </a>
<a class="sourceLine" id="cb1-206" data-line-number="206">            <span class="co"># Calculate G&#39;s loss based on this output </span></a>
<a class="sourceLine" id="cb1-207" data-line-number="207">            errG <span class="op">=</span> criterion(output, label) </a>
<a class="sourceLine" id="cb1-208" data-line-number="208">            <span class="co"># Calculate gradients for  G</span></a>
<a class="sourceLine" id="cb1-209" data-line-number="209">            errG.backward() </a>
<a class="sourceLine" id="cb1-210" data-line-number="210">            D_G_z2 <span class="op">=</span> output.mean().item() </a>
<a class="sourceLine" id="cb1-211" data-line-number="211">            <span class="co"># Update G </span></a>
<a class="sourceLine" id="cb1-212" data-line-number="212">            optimizerG.step() </a>
<a class="sourceLine" id="cb1-213" data-line-number="213"></a>
<a class="sourceLine" id="cb1-214" data-line-number="214">            <span class="co"># Output training stats </span></a>
<a class="sourceLine" id="cb1-215" data-line-number="215">            <span class="cf">if</span> i<span class="op">%</span><span class="dv">50</span> <span class="op">==</span> <span class="dv">0</span>:</a>
<a class="sourceLine" id="cb1-216" data-line-number="216">                <span class="bu">print</span>(<span class="st">&#39;[</span><span class="sc">%d</span><span class="st">/</span><span class="sc">%d</span><span class="st">][</span><span class="sc">%d</span><span class="st">/</span><span class="sc">%d</span><span class="st">]</span><span class="ch">\t</span><span class="st">Loss_D: </span><span class="sc">%.4f</span><span class="ch">\t</span><span class="st"> Loss_G: </span><span class="sc">%.4f</span><span class="ch">\t</span><span class="st">D(x):</span><span class="sc">%.4f</span><span class="ch">\t</span><span class="st">D(G(z)): </span><span class="sc">%.4f</span><span class="st">/</span><span class="sc">%.4f</span><span class="st">&#39;</span> <span class="op">%</span> (epoch, epoches, i,</a>
<a class="sourceLine" id="cb1-217" data-line-number="217">                    <span class="bu">len</span>(dataloader), errD.item(), errG.item(), D_x, D_G_z1, D_G_z2))</a>
<a class="sourceLine" id="cb1-218" data-line-number="218">                <span class="co"># Save Losses for plotting later</span></a>
<a class="sourceLine" id="cb1-219" data-line-number="219">                G_losses.append(errG.item()) </a>
<a class="sourceLine" id="cb1-220" data-line-number="220">                D_losses.append(errD.item()) </a>
<a class="sourceLine" id="cb1-221" data-line-number="221">                Dx_list.append(D_x) </a>
<a class="sourceLine" id="cb1-222" data-line-number="222">                Dz1_list.append(D_G_z1)</a>
<a class="sourceLine" id="cb1-223" data-line-number="223">                Dz2_list.append(D_G_z2)</a>
<a class="sourceLine" id="cb1-224" data-line-number="224">                <span class="co"># Check how the generator is doing by saving G&#39;s output on</span></a>
<a class="sourceLine" id="cb1-225" data-line-number="225">                <span class="co"># fixed noise</span></a>
<a class="sourceLine" id="cb1-226" data-line-number="226">                <span class="cf">if</span> iters <span class="op">%</span> <span class="dv">50</span> <span class="op">==</span> <span class="dv">0</span>:</a>
<a class="sourceLine" id="cb1-227" data-line-number="227">                    <span class="cf">with</span> torch.no_grad():</a>
<a class="sourceLine" id="cb1-228" data-line-number="228">                        fake <span class="op">=</span> netG(fixed_noise).detach().cpu()</a>
<a class="sourceLine" id="cb1-229" data-line-number="229">                    img_list.append(vision_utils.make_grid(fake[:<span class="dv">64</span>], padding<span class="op">=</span><span class="dv">2</span>,</a>
<a class="sourceLine" id="cb1-230" data-line-number="230">                        normalize <span class="op">=</span> <span class="va">True</span>)) </a>
<a class="sourceLine" id="cb1-231" data-line-number="231">                iters <span class="op">+=</span> <span class="dv">1</span></a>
<a class="sourceLine" id="cb1-232" data-line-number="232">                    </a>
<a class="sourceLine" id="cb1-233" data-line-number="233">    plot_figure(G_losses, D_losses, Dx_list, Dz1_list, Dz2_list, img_list)</a>
<a class="sourceLine" id="cb1-234" data-line-number="234"></a>
<a class="sourceLine" id="cb1-235" data-line-number="235"><span class="cf">if</span> <span class="va">__name__</span> <span class="op">==</span> <span class="st">&#39;__main__&#39;</span>:</a>
<a class="sourceLine" id="cb1-236" data-line-number="236">    manualSeed <span class="op">=</span> <span class="dv">999</span></a>
<a class="sourceLine" id="cb1-237" data-line-number="237">   <span class="co"># print(&#39;Random Seed: &#39;, manualSeed) </span></a>
<a class="sourceLine" id="cb1-238" data-line-number="238">    random.seed(manualSeed)</a>
<a class="sourceLine" id="cb1-239" data-line-number="239">    torch.manual_seed(manualSeed)</a>
<a class="sourceLine" id="cb1-240" data-line-number="240">    </a>
<a class="sourceLine" id="cb1-241" data-line-number="241">    <span class="co"># argparse </span></a>
<a class="sourceLine" id="cb1-242" data-line-number="242">    parser <span class="op">=</span> argparse.ArgumentParser(description <span class="op">=</span> <span class="st">&#39;Train DCGAN&#39;</span>) </a>
<a class="sourceLine" id="cb1-243" data-line-number="243">    parser.add_argument(<span class="st">&#39;--root&#39;</span>, <span class="bu">type</span> <span class="op">=</span> <span class="bu">str</span>, default <span class="op">=</span> <span class="st">&#39;data&#39;</span>, </a>
<a class="sourceLine" id="cb1-244" data-line-number="244">                        <span class="bu">help</span> <span class="op">=</span> <span class="st">&#39;Root directory for dataset&#39;</span>) </a>
<a class="sourceLine" id="cb1-245" data-line-number="245">    parser.add_argument(<span class="st">&#39;--device&#39;</span>, <span class="bu">type</span> <span class="op">=</span> <span class="bu">str</span>, default<span class="op">=</span> <span class="st">&#39;cuda:0&#39;</span>,</a>
<a class="sourceLine" id="cb1-246" data-line-number="246">                        <span class="bu">help</span> <span class="op">=</span> <span class="st">&#39;Choose the training device&#39;</span>)</a>
<a class="sourceLine" id="cb1-247" data-line-number="247">    parser.add_argument(<span class="st">&#39;--workers&#39;</span>, <span class="bu">type</span> <span class="op">=</span> <span class="bu">int</span>, default <span class="op">=</span> <span class="dv">2</span>, </a>
<a class="sourceLine" id="cb1-248" data-line-number="248">                        <span class="bu">help</span> <span class="op">=</span> <span class="st">&#39;Number of workers for dataloader&#39;</span>)</a>
<a class="sourceLine" id="cb1-249" data-line-number="249">    parser.add_argument(<span class="st">&#39;--batch_size&#39;</span>, <span class="bu">type</span> <span class="op">=</span> <span class="bu">int</span>, default <span class="op">=</span> <span class="dv">128</span>, </a>
<a class="sourceLine" id="cb1-250" data-line-number="250">                        <span class="bu">help</span> <span class="op">=</span> <span class="st">&#39;Batch size during training&#39;</span>)</a>
<a class="sourceLine" id="cb1-251" data-line-number="251">    parser.add_argument(<span class="st">&#39;--image_size&#39;</span>, <span class="bu">type</span> <span class="op">=</span> <span class="bu">int</span>, default <span class="op">=</span> <span class="dv">64</span>,</a>
<a class="sourceLine" id="cb1-252" data-line-number="252">                        <span class="bu">help</span> <span class="op">=</span> <span class="st">&#39;Spatial size of training images&#39;</span>)</a>
<a class="sourceLine" id="cb1-253" data-line-number="253">    parser.add_argument(<span class="st">&#39;--channels&#39;</span>, <span class="bu">type</span> <span class="op">=</span> <span class="bu">int</span>, default <span class="op">=</span> <span class="dv">3</span>,</a>
<a class="sourceLine" id="cb1-254" data-line-number="254">                        <span class="bu">help</span> <span class="op">=</span> <span class="st">&#39;Number of channels in the training images&#39;</span>)</a>
<a class="sourceLine" id="cb1-255" data-line-number="255">    parser.add_argument(<span class="st">&#39;--nz&#39;</span>, <span class="bu">type</span> <span class="op">=</span> <span class="bu">int</span>, default <span class="op">=</span> <span class="dv">100</span>,</a>
<a class="sourceLine" id="cb1-256" data-line-number="256">                        <span class="bu">help</span> <span class="op">=</span> <span class="st">&#39;Size of generator input&#39;</span>)</a>
<a class="sourceLine" id="cb1-257" data-line-number="257">    parser.add_argument(<span class="st">&#39;--ngf&#39;</span>, <span class="bu">type</span> <span class="op">=</span> <span class="bu">int</span>, default <span class="op">=</span> <span class="dv">64</span>,</a>
<a class="sourceLine" id="cb1-258" data-line-number="258">                        <span class="bu">help</span> <span class="op">=</span> <span class="st">&#39;Size of feature maps in generator&#39;</span>) </a>
<a class="sourceLine" id="cb1-259" data-line-number="259">    parser.add_argument(<span class="st">&#39;--ndf&#39;</span>, <span class="bu">type</span> <span class="op">=</span> <span class="bu">int</span>, default <span class="op">=</span> <span class="dv">64</span>,</a>
<a class="sourceLine" id="cb1-260" data-line-number="260">                        <span class="bu">help</span> <span class="op">=</span> <span class="st">&#39;Size of feature maps in discriminator&#39;</span>) </a>
<a class="sourceLine" id="cb1-261" data-line-number="261">    parser.add_argument(<span class="st">&#39;--epoches&#39;</span>, <span class="bu">type</span> <span class="op">=</span> <span class="bu">int</span>, default <span class="op">=</span> <span class="dv">10</span>,</a>
<a class="sourceLine" id="cb1-262" data-line-number="262">                        <span class="bu">help</span> <span class="op">=</span> <span class="st">&#39;Number of training epoches&#39;</span>) </a>
<a class="sourceLine" id="cb1-263" data-line-number="263">    parser.add_argument(<span class="st">&#39;--lr&#39;</span>, <span class="bu">type</span> <span class="op">=</span> <span class="bu">float</span>, default <span class="op">=</span> <span class="fl">2e-4</span>,</a>
<a class="sourceLine" id="cb1-264" data-line-number="264">                        <span class="bu">help</span> <span class="op">=</span> <span class="st">&#39;Learning rate for optimizer&#39;</span>) </a>
<a class="sourceLine" id="cb1-265" data-line-number="265">    parser.add_argument(<span class="st">&#39;--beta1&#39;</span>, <span class="bu">type</span> <span class="op">=</span> <span class="bu">float</span>, default <span class="op">=</span> <span class="fl">0.5</span>,</a>
<a class="sourceLine" id="cb1-266" data-line-number="266">                        <span class="bu">help</span> <span class="op">=</span> <span class="st">&#39;Beta1 hyperparam for Adam optimizer&#39;</span>) </a>
<a class="sourceLine" id="cb1-267" data-line-number="267">    args <span class="op">=</span> parser.parse_args() </a>
<a class="sourceLine" id="cb1-268" data-line-number="268">    dataloader <span class="op">=</span> load_data(args.root, args.image_size, args.batch_size,</a>
<a class="sourceLine" id="cb1-269" data-line-number="269">                           args.workers)</a>
<a class="sourceLine" id="cb1-270" data-line-number="270">    netG, netD, optimizerD, optimizerG, criterion <span class="op">=</span> get_model()</a>
<a class="sourceLine" id="cb1-271" data-line-number="271">    </a>
<a class="sourceLine" id="cb1-272" data-line-number="272">    fit(args.epoches, dataloader,args.device, netG, netD, optimizerG, optimizerD)</a></code></pre></div>
<p>最后附上一些结果图： <strong>celeb-A faces Dataset</strong> <img src="https://raw.githubusercontent.com/xuejy19/xuejy19.github.io/source/Img/Loss.png" alt="Loss" /> <img src="https://raw.githubusercontent.com/xuejy19/xuejy19.github.io/source/Img/fake_image.png" alt="fake_image" /></p>
<p><strong>MNIST</strong> <img src="https://raw.githubusercontent.com/xuejy19/xuejy19.github.io/source/Img/Loss_mn.png" alt="Loss" /> <img src="https://raw.githubusercontent.com/xuejy19/xuejy19.github.io/source/Img/mnist_gan.png" alt="fake_msist" /></p>
<p>网络结构比较简单，真正在使用时应当需要根据任务来进行调整，参数初始化以及部分超参数选择，<strong>沃兹基</strong>曾经说过： &gt; 深度神经网络理论学习是一门学问，代码实现又是一门学问，网络调参更是一门学问，想把神经网络搞掂，三种学问缺一不可。</p>
<p>GAN目前还面临很多问题，比如训练困难，在图像处理之外的领域效果并不理想，同时在理论支撑方面也不够扎实，如果想进一步学习GAN，可以阅读下面几篇文章： - <a href="https://zhuanlan.zhihu.com/p/56943597">这份攻略帮你「稳住」反复无常的 GAN</a> - <a href="https://zhuanlan.zhihu.com/p/75789936">海量案例！生成对抗网络（GAN）的18个绝妙应用</a></p>
]]></content>
      <categories>
        <category>统计学习</category>
      </categories>
      <tags>
        <tag>GAN</tag>
      </tags>
  </entry>
</search>
